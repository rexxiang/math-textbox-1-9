#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab

== §3.4 平均数、中位数与众数 <sec-3-4>

#vocab[平均数 mean / 中位数 median / 众数 mode]

#crisis[
  期末考试后，老师宣布：「全班数学平均分 85 分！」可是你考了 78 分，回头一问，班上 30 个人里有 22 个人不到 80 分。

  *平均分 85，大部分人却不到 80——这怎么可能？*

  原来班上有几个「学霸」考了接近满分，把平均分拉高了。那么，用什么数才能更真实地代表全班的「一般水平」呢？
]

#side-hack[
  极端值陷阱——假设一个小区有 100 户人家，99 户年收入 10 万元，1 户年收入 1 亿元。平均年收入 $approx$ 109 万元！但 99% 的人都远低于平均数。这时候用中位数（10 万元）才是合理的代表。
]

#discovery[
  来看一组数据——7 位同学的身高（厘米）：

  $ 155, quad 158, quad 160, quad 160, quad 162, quad 165, quad 190 $

  *尝试一*：算平均数。

  $ frac(155 + 158 + 160 + 160 + 162 + 165 + 190, 7) = frac(1150, 7) approx 164.3 $

  但 190 这个极端值把平均数拉高了，7 人中有 5 人低于 164.3。

  *尝试二*：找中间那个数。把数据从小到大排好，第 4 个数是 $160$——这就是*中位数*。它不受极端值影响。

  *尝试三*：找出现次数最多的数。$160$ 出现了 2 次，最多——这就是*众数*。它反映最「普遍」的情况。

  三种代表各有优缺，没有哪个永远最好——关键看*数据的特点*和*你的目的*。
]

#blueprint[
  === 算术平均数

  $n$ 个数据 $x_1, x_2, dots.h, x_n$ 的*算术平均数*：

  $ overline(x) = frac(x_1 + x_2 + dots.h + x_n, n) $

  *加权平均数*：当各数据的重要程度（权重）不同时：

  $ overline(x) = frac(x_1 w_1 + x_2 w_2 + dots.h + x_n w_n, w_1 + w_2 + dots.h + w_n) $

  例：某科目平时成绩 80 分（权重 30%）、期末成绩 90 分（权重 70%）：

  $ overline(x) = 80 times 0.3 + 90 times 0.7 = 24 + 63 = 87 $

  === 中位数

  把一组数据*从小到大*排列：
  - 数据个数为奇数：中间那个数就是中位数
  - 数据个数为偶数：中间两个数的平均数是中位数

  例：$3, 5, 7, 9$ 的中位数 $= frac(5 + 7, 2) = 6$。

  *特点*：不受极端值影响，适合数据有「偏态」时使用。

  === 众数

  一组数据中*出现次数最多*的数就是众数。

  - 一组数据可能有一个众数、多个众数，甚至没有众数（所有数据只出现一次）
  - 众数适合描述「最常见」的情况（如最畅销的鞋码）

  === 三种统计量的选择策略

  #table(
    columns: (1fr, 1fr),
    align: (left, left),
    table.header[*数据特点*][*推荐使用*],
    [数据分布均匀、无极端值], [平均数],
    [有极端值或数据偏斜], [中位数],
    [关注最常见的类别], [众数],
  )
]

#mastery[
  *基础*

  + 求下列数据的平均数、中位数和众数：$5, 7, 7, 8, 10, 12, 21$。
  + 6 名同学的跳远成绩（米）：$2.1, 2.3, 2.3, 2.5, 2.5, 2.5$。求平均数、中位数和众数。
  + 小红的综合成绩由三部分组成：笔试 75 分（权重 $50%$）、实验 88 分（权重 $30%$）、平时 92 分（权重 $20%$）。求加权平均分。

  *进阶*

  + 某公司 8 名员工月薪（万元）：$0.3, 0.3, 0.4, 0.4, 0.5, 0.5, 0.6, 8.0$。
    - 分别求平均数、中位数和众数。
    - 如果要向应聘者介绍「员工的一般收入水平」，用哪个统计量更合适？为什么？
  + 鞋店要进一批运动鞋，以下是上月各尺码的销量：

    #table(
      columns: (auto, auto, auto, auto, auto, auto),
      align: center,
      table.header[*尺码*][38][39][40][41][42],
      [*销量/双*], [15], [28], [35], [20], [12],
    )

    店主应该重点进哪个尺码的货？用平均数、中位数还是众数来决定？

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_
  + 平均数 $= frac(5+7+7+8+10+12+21, 7) = frac(70, 7) = 10$；中位数 $= 8$（第 4 个数）；众数 $= 7$（出现 2 次）。
  + 平均数 $= frac(2.1+2.3+2.3+2.5+2.5+2.5, 6) = frac(14.2, 6) approx 2.37$ 米；中位数 $= frac(2.3+2.5, 2) = 2.4$ 米；众数 $= 2.5$ 米。
  + $75 times 0.5 + 88 times 0.3 + 92 times 0.2 = 37.5 + 26.4 + 18.4 = 82.3$ 分。
  + 平均数 $= frac(0.3+0.3+0.4+0.4+0.5+0.5+0.6+8.0, 8) = frac(11.0, 8) = 1.375$ 万元；中位数 $= frac(0.4+0.5, 2) = 0.45$ 万元；众数 $= 0.3$ 万元和 $0.4$ 万元和 $0.5$ 万元（各出现 2 次）。用*中位数*（0.45 万元）更合适，因为平均数被 8 万元的极端高薪拉高了，不能反映多数员工的实际收入。
  + 应该重点进尺码 *40*（销量 35 双，最多）。这里用*众数*来决定最合适，因为店主需要知道最畅销的尺码，而不是「平均尺码」。
]
