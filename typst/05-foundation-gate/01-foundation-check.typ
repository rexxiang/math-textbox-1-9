#import "../lib/theme-v2.typ": blueprint, mastery, side-hack, secref

== §5.1 基础总检：四条主干是否已经连起来 <sec-5-1>

这一节不讲新方法，只做一件事：检查你能不能把已经学过的工具一起调用，而不是一节一节孤立记忆。

#side-hack[
  *使用方法*：

  - 先独立想，再回看对应小节
  - 如果一道题卡住，不要急着找“新套路”，先问自己缺的是数、量、位置，还是关系
  - 真正过关的标志，不是每题立刻算对，而是知道该回看 #secref("1.5")、#secref("2.4")、#secref("3.5") 还是 #secref("4.4")
]

#blueprint[
  *四条主干的过关信号*

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *算术基础*（#secref("1.5"), #secref("1.6")）
      - 会按顺序处理混合运算
      - 会主动重组更好算的部分
      - 看到单位变化时，不会把数和单位分开想
    ],
    block[
      *分数、比与百分数*（#secref("2.2"), #secref("2.4"), #secref("2.5")）
      - 会分清“部分 : 整体”和“部分 : 部分”
      - 会在分数、小数、百分数之间切换
      - 会用统一标准比较大小
    ],
  )

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *几何基础*（#secref("3.2"), #secref("3.3"), #secref("3.5")）
      - 会读图上的长度、角度和坐标
      - 会把“位置”写成数对
      - 会把面积、体积、比例尺看成度量问题
    ],
    block[
      *数轴与代数桥*（#secref("4.1"), #secref("4.3"), #secref("4.4")）
      - 会用数轴理解正负、距离和大小规律
      - 会把情境写成代数式
      - 会把方程看成平衡关系，而不只是套步骤
    ],
  )
]

#mastery[
  *诊断题*

  + 一根彩带原来长 $3.2$ 米，剪去全长的 $25%$ 后，还剩多少米？

  + 点 $A(-2, 3)$ 向右平移 5 格后到点 $B$。写出点 $B$ 的坐标，并判断点 $B$ 在第几象限。

  + 一杯果汁里，果汁原液 : 水 $= 2 : 5$。如果一共配了 210 毫升，原液有多少毫升？原液占整杯的百分之几？

  + 在数轴上，$-1.5$ 和 $1/2$ 谁更靠右？它们之间的距离是多少？

  + 用代数式表示：“每本练习册 6 元，买 $x$ 本，再加 8 元运费，一共付多少钱。”

  + 小明记录一个长方形花坛：长是 12 米，宽是长的 $3/4$。先求宽，再求面积。

  + 解方程：$x - 2.4 = 5.6$。算完后，用一句话解释你做的是哪一种“保持平衡”的操作。

  + 自问：上面哪一题最容易做成“会算但说不清”？如果要回看，只选一节，你会先回看哪一节？
]
