#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, tryit, pitfall, secref

== §5.1 基础总检：四条主干是否已经连起来 <sec-5-1>

#crisis[
  走到分支入口前，最危险的不是“没学过新东西”，而是把旧工具分散地记着，用不到一起。

  比如会算百分数，却一到配比就乱；会读点，却一碰平移和数轴距离就串线；会列式，却一解释平衡就只剩步骤。

  *如果旧工具还没连成网，后面的专题深化就会处处卡顿。*
]

#history-note[
  真正的闸门不是再教一轮新课，而是像出发前的安检：确认常用工具已经能协同工作。古代商人、测量者和工程师在换一种任务前，也都先要确认旧办法还能不能连起来用。
]

#discovery[
  这道闸门真正要检查的，不是“你记不记得单个公式”，而是四条公共主干有没有接通：

  - 算术基础：运算、单位、顺序能不能一起工作
  - 表示与比较：分数、比、百分数会不会互相翻译
  - 图形与位置：长度、角度、平行垂直、坐标会不会互相配合
  - 数轴与代数桥：正负、字母、平衡观能不能一起解释情境

  所以做题时，最重要的不是死记，而是看自己会不会主动把旧工具拉到同一张桌上。
]

#tryit[
  先别急着看下面的过关信号，先用一句话自测：

  + 遇到一道综合题时，你会先想“需要调哪些旧工具”吗？
  + 你能分清自己卡在“不会算”“不会比”“不会读位置”还是“不会列关系”吗？
  + 如果不能，做完本节诊断单后，就要按回看路线补短板，再进分支。
]

#side-hack[
  *使用方法*：

  - 先独立做，再核对每组后面的检查点
  - 对答案时不只看数字，也看自己能不能说清为什么
  - 如果“常见卡点”正好说中了你，就立刻按回看路线补那一块
  - 真正过关的标志，不是每题立刻算对，而是知道该回看 #secref("1.5")、#secref("2.4")、#secref("3.5") 还是 #secref("4.4")
]

#blueprint[
  *四条主干的过关信号*

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *算术基础*（#secref("1.5"), #secref("1.6")）
      - 会按顺序处理混合运算
      - 会主动重组更好算的部分
      - 看到单位变化时，不会把数和单位分开想
    ],
    block[
      *分数、比与百分数*（#secref("2.2"), #secref("2.4"), #secref("2.5")）
      - 会分清“部分 : 整体”和“部分 : 部分”
      - 会在分数、小数、百分数之间切换
      - 会用统一标准比较大小
    ],
  )

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *几何基础*（#secref("3.2"), #secref("3.3"), #secref("3.4"), #secref("3.5")）
      - 会读图上的长度、角度、方向和坐标
      - 会把“位置”写成数对
      - 会把面积、体积、比例尺看成度量问题
    ],
    block[
      *数轴与代数桥*（#secref("4.1"), #secref("4.3"), #secref("4.4")）
      - 会用数轴理解正负、距离和大小规律
      - 会把情境写成代数式
      - 会把方程看成平衡关系，而不只是套步骤
    ],
  )
]

#pitfall[
  *过闸门时最常见的三种误判：*

  - 只看答案对不对，不看自己到底调用了哪些旧工具
  - 一卡住就整章重刷，而不是先定位短板
  - 觉得“会做单题 = 能进分支”，却忽略综合题里一混合就断线
]

#mastery[
  *诊断单：先做，再核对*

  === 诊断 1：总量、部分和单位有没有连在一起

  + 一根彩带原来长 $3.2$ 米，剪去全长的 $25%$ 后，还剩多少米？

  + 小明记录一个长方形花坛：长是 12 米，宽是长的 $3/4$。先求宽，再求面积。

  *检查点*

  - 彩带还剩全长的 $75%$，所以还剩 $3.2 times 75% = 2.4$ 米。
  - 花坛宽 $= 12 times frac(3, 4) = 9$ 米，面积 $= 12 times 9 = 108$ 平方米。

  *常见卡点*

  - 把 $25%$ 误当成 0.25 米，而不是“全长的 $25%$”
  - 只算出宽，没有再回到面积
  - 计算时把数和单位拆开想

  *回看路线*

  - 数量和单位一起想：#secref("1.5")
  - “几分之几”的意思：#secref("2.2")
  - 分数、百分数互换：#secref("2.5")

  === 诊断 2：比、部分与整体能不能分清

  + 一杯果汁里，果汁原液 : 水 $= 2 : 5$。如果一共配了 210 毫升，原液有多少毫升？原液占整杯的百分之几？

  *检查点*

  - 总份数是 $2 + 5 = 7$ 份，所以原液有 $210 times frac(2, 7) = 60$ 毫升。
  - 原液占整杯的 $frac(2, 7)$，也就是约 $28.6%$。

  *常见卡点*

  - 把 $2 : 5$ 直接当成“原液占整杯的 $20%$”
  - 先算出 60 毫升，却忘了再回答“占整杯多少”
  - 分不清“部分 : 整体”和“部分 : 部分”

  *回看路线*

  - 比和份数：#secref("2.4")
  - 百分数表达同一个量：#secref("2.5")
  - 总量、部分量的计算：#secref("1.5")

  === 诊断 3：位置、方向和距离会不会串线

  + 点 $A(-2, 3)$ 向右平移 5 格后到点 $B$。写出点 $B$ 的坐标，并判断点 $B$ 在第几象限。

  + 在数轴上，$-1.5$ 和 $1/2$ 谁更靠右？它们之间的距离是多少？

  *检查点*

  - 点 $B$ 的坐标是 $(3, 3)$，因为向右平移只改横坐标；点 $B$ 在第一象限。
  - $1/2$ 更靠右；两点之间的距离是 $1/2 - (-1.5) = 2$。

  *常见卡点*

  - “向右平移”时把纵坐标也改了
  - 把坐标顺序写反成 $(3, -2)$
  - 距离题只比较大小，没有把“相差多少”算出来

  *回看路线*

  - 坐标和位置：#secref("3.5")
  - 数轴上的左右与距离：#secref("4.1")
  - 正负数比较：#secref("4.2")

  === 诊断 4：字母关系和平衡观有没有站稳

  + 用代数式表示：“每本练习册 6 元，买 $x$ 本，再加 8 元运费，一共付多少钱。”

  + 解方程：$x - 2.4 = 5.6$。算完后，用一句话解释你做的是哪一种“保持平衡”的操作。

  *检查点*

  - 代数式是 $6 x + 8$。
  - 方程的解是 $x = 8$；解释时应说“等号两边同时加上 2.4，平衡仍然保持”。

  *常见卡点*

  - 把“每本 6 元”写成 $6 + x$
  - 只背步骤，没有说清“左右一样多”为什么还成立
  - 方程算对了，但解释时说成“把 2.4 移过去”

  *回看路线*

  - 字母表示数量关系：#secref("4.3")
  - 方程的平衡观：#secref("4.4")
  - 运算顺序与式子阅读：#secref("1.6")
]

#side-hack[
  *做完以后怎么决定下一步*：

  - 如果四组里大多数都能算对，而且你也能说清“为什么这样做”，继续看下一节的分支入口总览。
  - 如果某一组的常见卡点正好说中了你，先按那一组的回看路线补齐，再回来重做这一组。
  - 如果第四组还说不清字母关系或平衡观，先别急着进 06 和 08；如果第三组还不稳，先别急着进 07。
]
