#import "../lib/theme-v2.typ": side-hack, secref

== §5.2 分支入口回看：带着哪些旧工具继续前进 <sec-5-2>

后面的章节会各自加深，但入口提醒都应该很短：不是先偷跑新内容，而是先确认旧工具已经在手边。

#side-hack[
  *进入 06 代数深化前*：
  先确认你还能顺手调用 #secref("1.6") 的运算律、#secref("2.4") 的比例意识、#secref("4.3") 的代数式表达和 #secref("4.4") 的平衡观。后面会把这些直觉扩成更完整的整式与方程工具。
]

#side-hack[
  *进入 07 几何深化前*：
  先确认你还认得 #secref("3.3") 的角、#secref("3.4") 的平行与垂直、#secref("3.5") 的坐标定位，也别忘了 #secref("2.4") 里“同一标准再比较”的比例意识。后面很多图形关系，都会在这些基础上长出来。
]

#side-hack[
  *进入 08 函数模型前*：
  先确认你能把 #secref("4.1") 的数轴变化、#secref("3.5") 的点坐标、#secref("2.4") 的正比直觉和 #secref("4.3") 的字母模板放在同一张图里想。函数不是凭空出现的新对象，而是把“怎么一起变”说得更统一。
]

#side-hack[
  *进入 09 数据统计前*：
  先确认你会在 #secref("2.5") 里切换不同表示，会在 #secref("1.5") 里稳稳算出总量与部分，也会在 #secref("2.4") 里分清“部分占整体多少”和“两部分相比较”。后面读图、算平均、谈波动时，最常用的还是这些基础判断。
]
