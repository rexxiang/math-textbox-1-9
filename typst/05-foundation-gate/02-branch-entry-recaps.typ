#import "../lib/theme-v2.typ": side-hack, secref

== §5.2 分支入口回看：带着哪些旧工具继续前进 <sec-5-2>

后面的章节会各自加深，但入口提醒都应该很短：不是先偷跑新内容，而是先确认旧工具已经在手边。

#side-hack[
  *进入 06 代数深化前，先打勾*：
  - 我能把“每个多少、总共多少”写成含字母的式子（#secref("4.3")）
  - 我能用“左右一样多”解释一步方程，而不只会套步骤（#secref("4.4")）
  - 我能把比和倍数关系顺手写进同一个式子里（#secref("2.4")）
  - 如果其中任何一项明显不稳，就先回 #secref("4.3")、#secref("4.4")、#secref("2.4")，再来这里
]

#side-hack[
  *进入 07 几何深化前，先打勾*：
  - 我能读出角的大小，也能分清平行、垂直和方向关系（#secref("3.3"), #secref("3.4")）
  - 我能把图上的位置写成坐标，知道先横后纵（#secref("3.5")）
  - 我能在比较图形时先找同一标准，而不是只凭眼睛猜（#secref("2.4")）
  - 如果其中任何一项明显不稳，就先回 #secref("3.3")、#secref("3.4")、#secref("3.5")、#secref("2.4")
]

#side-hack[
  *进入 08 函数模型前，先打勾*：
  - 我能在数轴上描述“变大、变小、相差多少”（#secref("4.1")）
  - 我能在坐标平面上定位点，并把位置和数量联系起来（#secref("3.5")）
  - 我能认出“一个量跟着另一个量变”的比例直觉，并用字母先写出关系（#secref("2.4"), #secref("4.3")）
  - 如果其中任何一项明显不稳，就先回 #secref("4.1")、#secref("3.5")、#secref("2.4")、#secref("4.3")
]

#side-hack[
  *进入 09 数据统计前，先打勾*：
  - 我能在分数、小数、百分数之间切换同一个量（#secref("2.5")）
  - 我能从总量和部分量出发，把“占整体多少”算清楚（#secref("1.5"), #secref("2.4")）
  - 我能分清“部分占整体多少”和“两部分相比较”不是一回事（#secref("2.4")）
  - 如果其中任何一项明显不稳，就先回 #secref("2.5")、#secref("1.5")、#secref("2.4")
]
