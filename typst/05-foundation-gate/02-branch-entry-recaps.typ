#import "../lib/theme-v2.typ": side-hack, secref

== §5.2 分支入口回看：带着哪些旧工具继续前进 <sec-5-2>

后面的章节会各自加深，但这里先把旧工具重新拿顺手。进分支后，你关注的是“下一步怎么用”，不是临时回头补装备。

#side-hack[
  *进入 06 代数深化前，先打勾*：
  - 我能把运算律当成整理式子的共同语法，而不只把它们留在纯算术里（#secref("1.6")）
  - 我能把比和倍数关系顺手写进同一个式子里（#secref("2.4")）
  - 我能把“每个多少、总共多少”写成含字母的式子（#secref("4.3")）
  - 我能用“左右一样多”解释方程，不只会套步骤（#secref("4.4")）
  - 如果有哪项不稳，就先回 #secref("1.6")、#secref("2.4")、#secref("4.3")、#secref("4.4") 补稳，再进入 06。
]

#side-hack[
  *进入 07 几何深化前，先打勾*：
  - 我能在比较图形时先找同一标准，而不是只凭眼睛猜（#secref("2.4")）
  - 我还记得面积、拼补和分割的直觉，能把它们接到后面的证明与测量里（#secref("3.2")）
  - 我能读出角的大小（#secref("3.3")）
  - 我能分清平行、垂直和方向关系（#secref("3.4")）
  - 我能把图上的位置写成坐标，知道先横后纵（#secref("3.5")）
  - 如果有哪项不稳，就先回 #secref("2.4")、#secref("3.2")、#secref("3.3")、#secref("3.4")、#secref("3.5") 补稳，再进入 07。
]

#side-hack[
  *进入 08 函数模型前，先打勾*：
  - 我能认出“一个量跟着另一个量变”的比例直觉（#secref("2.4")）
  - 我能在方格纸上定位点，知道坐标是先横后纵（#secref("3.5")）
  - 我能在数轴和坐标平面里读出正负方向、左右高低与相差多少（#secref("4.1")）
  - 我能先用字母把两个量的关系写出来，再去比较表格、图象和情境（#secref("4.3")）
  - 如果有哪项不稳，就先回 #secref("2.4")、#secref("3.5")、#secref("4.1")、#secref("4.3") 补稳，再进入 08。
]

#side-hack[
  *进入 09 数据统计前，先打勾*：
  - 我能在分数、小数、百分数之间切换同一个量（#secref("2.5")）
  - 我能从总量和部分量出发，把“占整体多少”算清楚（#secref("1.5"), #secref("2.4")）
  - 我能分清“部分占整体多少”和“两部分相比较”不是一回事（#secref("2.4")）
  - 如果有哪项不稳，就先回 #secref("2.5")、#secref("1.5")、#secref("2.4") 补稳，再进入 09。
]
