#import "../lib/theme-v2.typ": blueprint, mastery, side-hack, secref

== 第五章回顾：主干收束闸门

#side-hack[
  如果你在这一页卡住了，先别急着重刷整章：先判断自己卡在计算与单位、表示与比较、位置与坐标，还是字母与平衡，再回 #secref("5.1") 找对应诊断组补一小块，然后回来重做这里的题。
]

#blueprint[
  *知识速查卡*

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *计算这一块*（#secref("1.5"), #secref("1.6"), #secref("1.7")）
      - 先看运算顺序，再决定是否需要重组
      - 加法里会配对凑整，乘法里会先成整或用分配律去括号
      - 单位变化要和数量一起想
    ],
    block[
      *表示与比较*（#secref("2.4"), #secref("2.5")）
      - 部分 : 整体和部分 : 部分要先分清
      - 分数、小数、百分数都能说同一个量
      - 比大小前先统一成同一标准
    ],
  )

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *图形与位置*（#secref("3.6B"), #secref("3.8")）
      - 角说的是“张开多少”
      - 坐标写法是先横后纵
      - 图上的位置变化，也能写成数量关系
    ],
    block[
      *数轴与关系*（#secref("4.1"), #secref("4.3"), #secref("4.4")）
      - 数轴越靠右，数越大
      - 字母可以先做占位符，再做关系模板
      - 方程说的是“左右一样多”
    ],
  )
]

#mastery[
  *混合自测题*

  + 比较大小：$-2$，$0$，$1/4$。

  + 把 $3/5$ 写成小数和百分数。

  + 一个长方形长 8 厘米、宽 6 厘米，求周长和面积。

  + 一杯饮料中果汁 : 水 $= 3 : 7$。如果共 280 毫升，果汁有多少毫升？果汁占整杯的百分之几？

  + 点 $P(-3, 2)$ 先向右平移 4 格，再向下平移 5 格。写出新点的坐标，并描述它相对原点的位置。

  + 当 $a = -4$ 时，求 $2a + 5$ 的值。

  + 解方程：$x div 3 = 5$。

  + 某地气温从 $-2$ 摄氏度上升了 $5.5$ 摄氏度，后来又下降了 $1$ 摄氏度。最后气温是多少？

  + 把 $0.125$ 写成最简分数和百分数。

  + 一本练习册 4.5 元，买 3 本再加 2 元运费。如果付 20 元，应找回多少钱？
]

#line(length: 100%, stroke: 0.3pt + luma(200))
_参考答案：_

+ $-2 < 0 < 1/4$

+ $3/5 = 0.6 = 60%$

+ 周长 $= 2(8 + 6) = 28$ 厘米；面积 $= 8 times 6 = 48$ 平方厘米。

+ 总份数 $= 10$；果汁 $= 280 times frac(3, 10) = 84$ 毫升；先把部分和整体对齐，果汁占 $frac(3, 10) = 30%$

+ 新点是 $(1, -3)$，在原点右侧、$x$ 轴下方。

+ $2a + 5 = 2 times (-4) + 5 = -3$

+ $x div 3 = 5$，把“除以 3” undo 成“乘 3”，得 $x = 15$

+ 先从 $-2$ 上升到 $3.5$，再下降 1 到 $2.5$，所以最后气温是 $2.5$ 摄氏度。

+ $0.125 = frac(1, 8) = 12.5%$

+ 先算总价：$4.5 times 3 + 2 = 15.5$ 元；再算找回：$20 - 15.5 = 4.5$ 元。

*螺旋复习*

+ 回看 #secref("2.5")：把 $0.125$ 写成分数，再判断它和 $12%$ 哪个更大。

+ 回看 #secref("3.4")：一个长方形长 5 米、宽 2.4 米，面积是多少？如果宽增加到 3 米，面积增加多少？

+ 综合挑战（#secref(“1.6”)、#secref(“2.4”)、#secref(“3.4”)、#secref(“4.4”)）：
  一个长方形花坛，长是宽的 $frac(5, 4)$，面积是 $45$ 平方米。
  - 设宽为 $x$，用 #secref(“4.4”) 的平衡法列方程并求解
  - 用 #secref(“2.4”) 的比直接写出长 : 宽，无需解方程
  - 用 #secref(“1.6”) 的乘法分配律或结合律，巧算 $frac(5, 4) times x times x$（将你求出的 $x$ 代入，验算面积）
  - 周长是多少？（用 #secref(“3.4”) 的公式）

*综合挑战参考答案*

设宽 $= x$，则长 $= frac(5, 4) x$，面积方程：$frac(5, 4) x times x = 45$，即 $frac(5, 4) x^2 = 45$，$x^2 = 36$，$x = 6$。
- 长 $= frac(5, 4) times 6 = 7.5$ 米；比为 $7.5 : 6 = 5 : 4$（与题目一致）。
- $frac(5, 4) times 6 times 6 = frac(5 times 36, 4) = frac(180, 4) = 45$，验算正确。
- 周长 $= 2 times (6 + 7.5) = 27$ 米。
