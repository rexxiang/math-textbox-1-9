#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §2.2 公因数、公倍数与整齐分配 <sec-2-2>

#vocab[质因数分解 prime factorization / 最大公因数 Greatest Common Divisor (GCD) / 最小公倍数 Least Common Multiple (LCM)]

#crisis[
  两根绳子分别长 12 米和 18 米，要剪成同样长的短绳且不能有剩余；另一边，两盏灯每 6 分钟、8 分钟各闪一次，什么时候会再次同时闪亮？

  *一个问题在找“共同能整分的最大长度”，另一个问题在找“共同会重合的最早时刻”。*
]

#history-note[
  古代人在丈量土地、安排轮值、编排历法时，经常碰到“能不能整齐分完”和“什么时候再次重合”这两类问题。最大公因数和最小公倍数，就是把这两种现实需要整理成了稳定工具。
]

#discovery[
  先把 12 和 18 的因数列出来：

  - 12 的因数：$1, 2, 3, 4, 6, 12$
  - 18 的因数：$1, 2, 3, 6, 9, 18$

  共同的因数有 $1, 2, 3, 6$，其中最大的就是 6。

  再看倍数：

  - 6 的倍数：$6, 12, 18, 24, dots.h$
  - 8 的倍数：$8, 16, 24, 32, dots.h$

  最先重合的是 24。

  当数字更大时，只靠一项项列出来会变慢，所以我们想要一种更稳的拆法。
]

#tryit[
  *先自己试试*：

  + 把 36 和 48 写成质因数相乘的形式。
  + 再用它们的公共质因数，试着求出 $gcd(36, 48)$。
  + 顺手想想：若要求 $"lcm"(36, 48)$，哪些质因数必须都保留下来？
]

#blueprint[
  - *质因数分解*：把一个合数写成若干个质数相乘的形式，如
    $ 36 = 2^2 times 3^2 $
  - *最大公因数*（Greatest Common Divisor, GCD）：两个数公有因数中最大的
  - *最小公倍数*（Least Common Multiple, LCM）：两个数公有倍数中最小的

  用质因数分解时：

  - 求 GCD：取*公共*质因数的较小幂
  - 求 LCM：取*所有需要*的质因数的较大幂

  例如：

  $ 36 = 2^2 times 3^2 $

  $ 48 = 2^4 times 3 $

  所以

  $ gcd(36, 48) = 2^2 times 3 = 12 $

  $ "lcm"(36, 48) = 2^4 times 3^2 = 144 $
]

#side-hack[
  *看题先问一句：它在找“最大能整分”还是“最早会重合”？*

  - 最长一样长、最大能平均分 → 想 GCD
  - 最早同时出现、最短重合周期 → 想 LCM
]

#pitfall[
  *高频错误*

  ❌ 求 GCD 时把所有质因数的较大幂都乘进去
  → ✓ GCD 只取公共部分，而且取较小幂。

  ❌ 求 LCM 时只留下公共部分
  → ✓ LCM 要让两个原数都能整除，所以需要把两边用到的质因数都保留下来。
]

#mastery[
  *基础*

  + 求 $gcd(24, 36)$。
  + 求 $"lcm"(12, 18)$。
  + 把 60 写成质因数分解。

  *应用*

  + 两根绳子分别长 15 m、25 m，要剪成同样长的短绳且没有剩余。每根短绳最长多少米？
  + 两辆公交车分别每 9 分钟、12 分钟发一班。若现在同时发车，最少多少分钟后再次同时发车？

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_
  + $gcd(24, 36) = 12$
  + $"lcm"(12, 18) = 36$
  + $60 = 2^2 times 3 times 5$
  + 最长 $= gcd(15, 25) = 5$ m
  + 最少 $= "lcm"(9, 12) = 36$ 分钟
]
