#import "../lib/theme-v2.typ": history-note, vocab

_分数、比与百分数基础——把「一部分」说清，也把「相比多少」说清。_

在全书的四段地图里，这一章仍然属于*共同地基*。你要把“部分”“整体”“相比多少”这些后面会反复出现的语言先练熟。

整数擅长回答「有多少个」，但生活里还有另一类问题：

+ 一块披萨吃了多少？
+ 1 米再往下细分能写成什么？
+ 果汁和水的配方是 $1 : 4$，这算不算 $20%$？
+ 同样是打折，$1/4$、$0.25$、$25%$ 为什么能说同一件事？

这一章会把这些看似不同的写法连成一条线：

+ *因数与倍数* —— 先看清整数怎么拆、怎么合，为约分、通分和比较打底。
+ *分数* —— 当整数不够用时，用「平均分成几份、取其中几份」来表达部分。
+ *小数* —— 把十进分数写得更紧凑，让测量和计算更方便。
+ *比、比例与百分数* —— 从「部分和部分」走到「部分占整体多少」，学会描述浓度、折扣和配方。
+ *互化与比较* —— 把分数、小数、百分数和合适的比统一到同一标准，再做判断。

#history-note[
  分数、小数、比和百分数不是彼此孤立的发明。它们都在回答同一个核心问题：*当整数太粗糙时，怎样更精确地描述数量关系？* 从古埃及的单位分数，到中国数学里的分数与比例，再到商贸中普及的百分数，人们一直在为「比较」寻找更聪明的语言。
]
