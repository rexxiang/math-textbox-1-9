#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §2.2 分数的意义与基本性质 <sec-2-2>

#vocab[分数 fraction / 真分数 proper fraction / 假分数 improper fraction / 带分数 mixed number]

#crisis[
  3 个好朋友平分 2 张披萨，每人吃多少？

  $2 div 3 = ?$ 答案不是整数！*整数世界装不下这个结果。*

  我们需要一种新的数——它能描述"部分"，能描述"整体的一块"，还能作为除法的结果。
]

#history-note[
  古埃及人只用"单位分数"——分子必须是 1，比如 $1/3$、$1/7$。要表示 $2/5$，他们得写成 $1/3 + 1/15$，非常麻烦。直到中国《九章算术》（约公元 1 世纪）才系统使用一般分数，把分子分母都当作独立的数来处理。今天我们写 $2/5$，正是沿用了这个传统。
]

#discovery[
  把一张披萨切成 3 等份，每份是这张披萨的 $1/3$。

  2 张披萨，每张取 $1/3$，合起来就是 $2/3$。所以 $2 div 3 = 2/3$。

  再来看不同的分数长什么样：

  - $3/4$：分母 4，分子 3，取了 4 份中的 3 份——分子比分母小，还没到一整个
  - $5/3$：分子比分母大，已经超过"一整个"了
  - $1 + 2/3 = 1 2/3$：用整数加真分数来表达，读作"一又三分之二"

  三种写法描述的是同一类东西，只是换了"服装"。
]

#tryit[
  先自己想一想：

  + $3/4$ 和 $4/3$，哪个比 1 大，哪个比 1 小？
  + $7/4$ 能写成带分数吗？怎么写？
  + 一条丝带剪去了它的 $2/5$，剩下的部分用分数怎么表示？
]

#blueprint[
  - *分数* $a/b$：把整体分成 $b$ 等份，取其中 $a$ 份；$b$ 叫分母，$a$ 叫分子，$b eq.not 0$。同时它也表示除法 $a div b$
  - *真分数*：$a < b$（如 $2/3$），值小于 1
  - *假分数*：$a >= b$（如 $5/3$），值大于或等于 1
  - *带分数*：整数部分加真分数（如 $1 2/3$），与假分数可以互相转换
    - 假分数 → 带分数：$7/4 = 4/4 + 3/4 = 1 + 3/4 = 1 3/4$
    - 带分数 → 假分数：$2 3/5 = (2 times 5 + 3)/5 = 13/5$
  - *分数基本性质*：分子分母同乘或同除以同一个不为零的数，分数的值不变
    $ a/b = (a times k)/(b times k) quad (k eq.not 0) $
]

#side-hack[
  分数和除法是"同一件事的两张脸"：$a/b = a div b$。遇到"平均分"问题，自然就能写出分数；看到分数，也可以想象成一个除法。
]

#pitfall[
  *高频错误*

  ❌ 把带分数 $1 2/3$ 读成 $1 times 2/3$，计算成 $2/3$
  → ✓ 带分数是加法：$1 2/3 = 1 + 2/3 = 5/3$，整数和分数要合并，不是相乘。

  ❌ 认为假分数"不正常"，遇到就必须换成带分数
  → ✓ 假分数和带分数各有用处：做乘除法时，假分数往往更方便；描述实际数量时，带分数更直观。
]

#mastery[
  *基础*

  + 把 $11/4$ 化为带分数。
  + 把 $3 2/7$ 化为假分数。
  + 下列分数中，哪些是真分数，哪些是假分数？$3/5,\ 7/4,\ 9/9,\ 2/11$

  *应用*

  + 小华走了全程的 $3/8$，小明走了全程的 $5/8$，谁走得更多？不用计算，只用分数意义来解释。
  + 一块蛋糕切成 6 等份，吃了 5 份，用真分数/假分数/带分数各写一种表达（只有一个数时也写出来）。

  *挑战*

  + 分数 $a/b$ 和 $c/d$，如果 $a times d = b times c$，能说明什么？试举例说明（如 $1/2$ 和 $3/6$）。（提示：用分数基本性质思考。下一节我们还会用 GCD 来"约简"这类分数。）

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_
  + $11/4 = 2 3/4$。
  + $3 2/7 = 23/7$。
  + 真分数：$3/5$，$2/11$；假分数：$7/4$，$9/9$。
  + 分母相同，分子越大表示取的份数越多，$5/8 > 3/8$，小明走得更多。
  + 吃了 $5/6$（真分数）；切了 6 份吃了 5 份，也可以用假分数表达剩余 $1/6$，或者整块表示 $6/6 = 1$，吃了 $5/6$。
  + $a times d = b times c$ 说明两个分数相等（可以用基本性质：$1/2 = 3/6$ 因为 $1 times 6 = 2 times 3 = 6$）。
]
