#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §2.1 因数、倍数与整除线索 <sec-2-1>

#vocab[因数 factor / 倍数 multiple / 整除线索 divisibility cues]

#crisis[
  24 个同学要分组做实验，老师要求每组人数相同、不能有人落单。

  可以分成几组？每组几人？如果看到一个更大的数，又怎样快速判断它能不能平均分给 2 人、3 人或 5 人？
]

#history-note[
  古人研究“能不能整齐地分”，一方面是为了丈量和分配，另一方面也是为了后面更复杂的分数、比例和约分做准备。整数先学会怎么拆、怎么合，后面的分数运算才有抓手。
]

#discovery[
  把 24 写成两个正整数相乘：

  $ 24 = 1 times 24 = 2 times 12 = 3 times 8 = 4 times 6 $

  这说明：

  - 能把 24 整齐分开的数有 $1, 2, 3, 4, 6, 8, 12, 24$
  - 24 又是这些数的整倍结果

  再看一些更快的判断方法：

  - 末位是 $0, 2, 4, 6, 8$，常常能被 2 整除
  - 末位是 $0$ 或 $5$，常常能被 5 整除
  - 各位数字之和能被 3 整除，原数也常能被 3 整除

  所以整数世界里有两条互相照应的线：一条叫“谁能整齐地分进去”，一条叫“谁是整倍地拼出来”。
]

#tryit[
  先自己试试：

  + 写出 18 的所有因数。
  + 判断 126 能不能被 2、3、5 整除。
  + 说说 36 为什么同时是 6 和 9 的倍数。
]

#blueprint[
  - 若 $a = b times c$（$b, c$ 为正整数），则 $b, c$ 是 $a$ 的*因数*，$a$ 是 $b, c$ 的*倍数*。
  - 判断一个数能否被另一个数整齐分开时，可以先抓*整除线索*：
    - 2：末位是 $0, 2, 4, 6, 8$
    - 5：末位是 $0$ 或 $5$
    - 3：各位数字之和是 3 的倍数
  - 找因数时，可以从小到大试除；找到一组 $d$ 与 $a div d$，往往就能同时得到一对因数。
  - 看倍数时，可以顺着乘法表往上接：$6, 12, 18, 24, dots.h$ 都是 6 的倍数。
]

#side-hack[
  看见“能不能平均分”的题，先别急着整道长算。先扫末位、再看数字和，常常就能很快抓住 2、3、5 这三条最常用的整除线索。
]

#pitfall[
  *高频错误*

  ❌ 把“因数”和“倍数”看成同一个方向：说“3 是 24 的倍数”
  → ✓ 因为 $24 = 3 times 8$，所以 3 是 24 的因数，24 才是 3 的倍数。

  ❌ 只找到一半因数就停下：36 的因数只写 $1, 2, 3, 4, 6$
  → ✓ 还要配对补全：$36 = 1 times 36 = 2 times 18 = 3 times 12 = 4 times 9 = 6 times 6$。
]

#mastery[
  *基础*

  + 写出 24 和 30 的所有因数。
  + 写出 7 的前 6 个正倍数。
  + 判断 135、248、410 分别能被 2、3、5 中哪些数整除。

  *应用*

  + 36 个苹果平均分给若干个小组，不许剩下。每组人数可能有哪些？
  + 48 人排队做操，如果每排 6 人或 8 人都正好排完，为什么 48 会同时出现在两张乘法表里？

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_
  + 24 的因数：$1, 2, 3, 4, 6, 8, 12, 24$；30 的因数：$1, 2, 3, 5, 6, 10, 15, 30$。
  + $7, 14, 21, 28, 35, 42$。
  + 135 能被 3 和 5 整除；248 能被 2 整除；410 能被 2 和 5 整除。
  + 每组人数可以是 1、2、3、4、6、9、12、18、36。
  + 因为 $48 = 6 times 8$，它既是 6 的倍数，也是 8 的倍数。
]
