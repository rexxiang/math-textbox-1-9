#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §2.1 因数、倍数与整除线索 <sec-2-1>

#vocab[因数 factor / 倍数 multiple / 质数与合数 prime and composite numbers]

#crisis[
  24 个同学要分组做实验，老师要求每组人数相同、不能有人落单。

  可以分成几组？每组几人？*到底有哪些分法？*
]

#history-note[
  古希腊数学家埃拉托斯特尼发明了一种巧妙的"筛法"：把 2 的倍数划掉、3 的倍数划掉……最后剩下的全是质数。这个方法沿用至今，叫做*埃拉托斯特尼筛*。
]

#discovery[
  试一试把 24 写成两个数相乘：

  $ 24 = 1 times 24 = 2 times 12 = 3 times 8 = 4 times 6 $

  所以 24 的*因数*有：$1, 2, 3, 4, 6, 8, 12, 24$。

  反过来看：$24 = 3 times 8$，说明 24 是 3 的*倍数*，也是 8 的*倍数*。

  再观察：有些数只能写成 $1 times$ 自己（如 $2, 3, 5, 7$），它们叫*质数*；能继续拆的（如 $4 = 2 times 2$）叫*合数*。
]

#tryit[
  *先自己试试*：

  + 写出 18 的所有因数。
  + 判断 29 是质数还是合数。
  + 不做长除法，只看线索判断 246 能不能被 2、3、5 整除。
]

#blueprint[
  - 若 $a = b times c$（$b, c$ 为正整数），则 $b, c$ 是 $a$ 的*因数*，$a$ 是 $b, c$ 的*倍数*
  - *质数*：大于 1 且只有 $1$ 和自身两个因数的数
  - *合数*：大于 1 且有两个以上因数的数（$1$ 既不是质数也不是合数）
  - 常见整除线索：
    - $2$ 的倍数——末位是 $0, 2, 4, 6, 8$（偶数）
    - $5$ 的倍数——末位是 $0$ 或 $5$
    - $3$ 的倍数——各位数字之和是 $3$ 的倍数
]

#side-hack[
  *先分清三件事*：

  - 能把一个数整齐拆开的是因数
  - 不断按整倍增长的是倍数
  - 只能被 $1$ 和自己整除的，是质数
]

#pitfall[
  *高频错误*

  ❌ 把 $1$ 也当成质数
  → ✓ $1$ 只有一个因数，所以既不是质数也不是合数

  ❌ 看到“能被 3 整除”就误以为“它的每一位都能被 3 整除”
  → ✓ 判断 3 的倍数要看*各位数字之和*，不是逐位看
]

#mastery[
  *基础*

  + 写出 36 的所有因数。
  + 判断：$17$ 是质数还是合数？$51$ 呢？
  + 50 以内 $3$ 的倍数有哪些？

  *进阶*

  + 判断 31、51、57 分别是质数还是合数。
  + 写出 50 以内所有既是 2 的倍数又是 3 的倍数的数。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_
  + $1, 2, 3, 4, 6, 9, 12, 18, 36$
  + $17$ 是质数（只被 $1$ 和 $17$ 整除）；$51 = 3 times 17$，是合数
  + $3, 6, 9, 12, 15, 18, 21, 24, 27, 30, 33, 36, 39, 42, 45, 48$
  + 31 是质数；$51 = 3 times 17$、$57 = 3 times 19$，所以后两者是合数
  + 6，12，18，24，30，36，42，48
]
