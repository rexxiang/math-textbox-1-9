#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab

== §3.2 比与比例 <sec-3-2>

#vocab[比 ratio / 比例 proportion / 正比例 direct proportion / 反比例 inverse proportion]

#crisis[
  周末你要做蛋糕，食谱写着：面粉和糖的比例是 $3 : 1$（每 300 克面粉配 100 克糖）。

  现在要做 2 倍的量。面粉用 600 克，糖要放多少？直觉告诉你是 200 克——可是如果换成更复杂的配方（5 种材料、做 $2.5$ 倍量），光靠直觉就不够了。

  *有没有一种通用方法，能让任意倍数的配方自动算出来？*
]

#history-note[
  黄金比例 $1 : 1.618...$ 自古以来被认为最「美」的比例。古希腊帕特农神庙的正面、达芬奇的《维特鲁威人》都暗藏这个比例。数学上它等于 $frac(1 + sqrt(5), 2)$。
]

#discovery[
  回到蛋糕配方。面粉和糖是 $3 : 1$，意思是：

  $ frac("面粉", "糖") = frac(3, 1) = 3 $

  不管做多少量，这个*比值*始终是 3。做 2 倍量：

  $ frac(600, "糖") = 3 quad arrow.r quad "糖" = frac(600, 3) = 200 "克" $

  做 $2.5$ 倍量：

  $ frac(750, "糖") = 3 quad arrow.r quad "糖" = frac(750, 3) = 250 "克" $

  核心发现：只要保持比值不变，配方就是「等比放大」的。两个比值相等的等式，就叫*比例*。
]

#blueprint[
  === 比的意义与性质

  两个数 $a$ 与 $b$ 的*比*记作 $a : b$（读作「a 比 b」），其中 $b eq.not 0$。

  - *前项*：$a$；*后项*：$b$；*比值*：$frac(a, b)$
  - *比的基本性质*：比的前项和后项同时乘以（或除以）同一个不为零的数，比值不变

  $ a : b = (a times k) : (b times k) quad (k eq.not 0) $

  例：$6 : 4 = 3 : 2$（同除以 2）。

  === 比例

  如果两个比的比值相等，就组成*比例*：

  $ a : b = c : d quad "即" quad frac(a, b) = frac(c, d) $

  *交叉相乘*（比例的基本性质）：

  $ frac(a, b) = frac(c, d) quad arrow.l.r quad a times d = b times c $

  这是解比例问题的核心工具。

  === 正比例与反比例

  *正比例*：$y = k x$（$k$ 为常数，$k eq.not 0$）。$x$ 变大，$y$ 也变大，$frac(y, x) = k$ 恒定。

  例：每千克苹果 8 元，买 $x$ 千克花 $y$ 元——$y = 8x$。

  *反比例*：$y = frac(k, x)$（$k$ 为常数，$k eq.not 0$）。$x$ 变大，$y$ 变小，$x y = k$ 恒定。

  例：100 千米的路，速度 $x$ 千米/时，时间 $y$ 小时——$x y = 100$。

  === 按比例分配

  把总量 $S$ 按 $a : b$ 分成两份：

  $ "第一份" = S times frac(a, a + b), quad "第二份" = S times frac(b, a + b) $

  === 比例尺

  $ "比例尺" = frac("图上距离", "实际距离") $

  比例尺 $1 : 50000$ 表示图上 1 厘米代表实际 500 米。
]

#mastery[
  *基础*

  + 化简下列比：$12 : 8$，$0.6 : 1.5$，$frac(2, 3) : frac(4, 9)$。
  + 解比例：$frac(x, 6) = frac(5, 3)$。
  + 把 540 元按 $4 : 5$ 分给甲、乙两人，各得多少？

  *进阶*

  + 一幅地图的比例尺是 $1 : 200000$，图上量得甲、乙两地距离为 3.5 厘米，实际距离是多少千米？
  + 汽车匀速行驶，走完 240 千米用了 3 小时。如果速度提高到原来的 1.5 倍，走完同样的路程需要多少小时？（判断正比例还是反比例）
  + 配制一种药液，药和水的比是 $1 : 500$。要配制 1002 克药液，需要药和水各多少克？

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_
  + $12 : 8 = 3 : 2$；$0.6 : 1.5 = 2 : 5$；$frac(2, 3) : frac(4, 9) = frac(2, 3) times frac(9, 4) = frac(3, 2)$，即 $3 : 2$
  + 交叉相乘：$3x = 30$，$x = 10$
  + 甲：$540 times frac(4, 9) = 240$ 元；乙：$540 times frac(5, 9) = 300$ 元
  + $3.5 times 200000 = 700000$ 厘米 $= 7$ 千米
  + 路程不变、速度与时间成反比例。原速 $= frac(240, 3) = 80$ 千米/时，新速 $= 120$ 千米/时。$t = frac(240, 120) = 2$ 小时
  + 总份数 $= 1 + 500 = 501$。药 $= 1002 times frac(1, 501) = 2$ 克，水 $= 1002 - 2 = 1000$ 克
]
