#import "../lib/theme-v2.typ": blueprint, mastery, side-hack, secref

== 第六章回顾：代数深化

#blueprint[
  *知识速查卡*

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *整式运算*（#secref("6.1")）
      - 合并同类项：字母指数不变，系数相加减
      - 常用公式：$(a+b)(a-b) = a^2 - b^2$
      - 幂运算：同底相乘指数加，幂的乘方指数乘
    ],
    block[
      *因式分解*（#secref("6.2")）
      - 顺序：一提公因式、二看公式、三十字相乘
      - $a^2 - b^2 = (a+b)(a-b)$
      - $a^2 plus.minus 2 a b + b^2 = (a plus.minus b)^2$
    ],
  )

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *分式*（#secref("6.3")）
      - 先因式分解，再约分、通分
      - 分式方程：去分母后一定验根
      - 增根只会出现在让原分母为 0 的值上
    ],
    block[
      *一元一次方程*（#secref("6.4")）
      - 五步：去分母→去括号→移项→合并→系数化 1
      - 移项要变号
      - 应用题先设未知数，再把条件写成平衡关系
    ],
  )

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *方程组与二次方程*（#secref("6.5")、#secref("6.6")）
      - 方程组：代入法 / 加减消元法
      - 二次方程：先看能否因式分解，不顺手时用配方法 / 求根公式
      - 求根公式是“配方法的打包版”
    ],
    block[
      *不等式*（#secref("6.7")）
      - 加减同数不变号，乘除负数必须反向
      - 数轴表示：空心 / 实心圆区分端点
      - 不等式组取交集
    ],
  )
]

#mastery[
  *混合自测题*（不标知识点，综合调用）

  + 化简：$(3x - 2)^2 - (3x + 2)(3x - 2)$

  + 因式分解：$2x^3 - 8x$

  + 化简：$frac(x^2 - 9, x^2 - 6x + 9)$

  + 解方程：$frac(x + 1, 3) - frac(2x - 1, 6) = 1$

  + 解方程组：$cases(3x + 2y = 16, 5x - 2y = 8)$

  + 用求根公式解：$2x^2 - 3x - 2 = 0$

  + 解不等式，在数轴上标出解集：$-3x + 6 > 0$

  + 用配方法解：$x^2 - 4x - 1 = 0$

  + 已知 $a + b = 3$，$a b = -10$，求 $a^2 + b^2$ 的值。

  + 解分式方程：$frac(2, x - 1) + 1 = frac(3, x - 1)$（注意验根）

  + 一个长方形的长比宽多 $4$ 厘米，面积为 $60$ cm²。求长和宽。

  + *进阶*：解不等式组 $cases(2x - 1 > 3, x + 4 <= 10)$，并求满足条件的所有整数 $x$。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  + $(3x-2)^2 - (3x+2)(3x-2) = (3x-2)[(3x-2) - (3x+2)] = (3x-2)(-4) = -12x + 8$
  + $2x^3 - 8x = 2x(x^2 - 4) = 2x(x+2)(x-2)$
  + $frac(x^2 - 9, x^2 - 6x + 9) = frac((x+3)(x-3), (x-3)^2) = frac(x+3, x-3)$，但原式分母不能为 0，所以 $x != 3$
  + 两边乘 6：$2(x+1) - (2x-1) = 6$，化简得 $2x+2-2x+1 = 6$，也就是 $3 = 6$——矛盾！说明不是算错，而是这个方程本来就无解。
  + 两式相加消去 $y$：$8x = 24$，$x = 3$；代入 $3x + 2y = 16$，得 $9 + 2y = 16$，所以 $y = frac(7, 2)$
  + $x = frac(3 plus.minus sqrt(9+16), 4) = frac(3 plus.minus 5, 4)$，$x = 2$ 或 $x = -frac(1, 2)$
  + $-3x > -6$，除以 $-3$ 时不等号要变号，所以 $x < 2$；数轴上在 2 处画空心圆，向左涂黑
  + $x^2 - 4x = 1$，两边加 4，得 $(x - 2)^2 = 5$，所以 $x = 2 plus.minus sqrt(5)$
  + $a^2 + b^2 = (a + b)^2 - 2 a b = 9 - 2 times (-10) = 29$
  + 两边乘 $(x-1)$：$2 + (x-1) = 3$，得 $x = 2$。验根时别漏掉原分母：$x=2$ 时 $x-1=1 != 0$，所以是有效解。
  + 设宽 $x$，长 $x+4$：$x(x+4) = 60$，$x^2+4x-60=0$，$(x+10)(x-6)=0$。代数上有 $x=-10$ 或 $x=6$，但宽不能是负数，所以取 $x=6$。宽 6 cm，长 10 cm。
  + 解第一个：$x > 2$；解第二个：$x <= 6$。解集 $2 < x <= 6$，整数为 $3, 4, 5, 6$。

  *螺旋复习（与前面主干的联系）*

  + 回看 #secref("4.3")：把“长方形长为 $x+3$、宽为 $x-1$”先写成面积代数式，再化简。你应得到 $(x+3)(x-1) = x^2 + 2x - 3$。

  + 回看 #secref("4.4")：桥上用 undo 解决的是一步方程；现在请用同样的平衡观解释，为什么从 $-3x > -6$ 到 $x < 2$ 时不等号必须反向。

  + 回看 #secref("1.6") 和 #secref("2.4")：一种果汁按浓缩液 : 水 $= 1 : 4$ 调配。现在要配成总量 750 mL 的果汁。先用比例判断浓缩液是多少，再解释为什么这个比例判断和列方程求解说的是同一件事。（答：浓缩液 150 mL，水 600 mL。）
]
