#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §6.5 分式 <sec-6-5>

#vocab[分式 rational expression / 增根 extraneous root / 最简分式 irreducible fraction]

#crisis[
  甲队单独完成一项工程需要 $a$ 天，乙队需要 $b$ 天。两队合作一天，完成工程的多少？

  甲一天完成 $frac(1, a)$，乙一天完成 $frac(1, b)$，合作一天完成：

  $ frac(1, a) + frac(1, b) = ? $

  这不是普通的分数加法——分母里含有*字母*！

  我们需要一套新工具来处理「分母含字母」的运算。
]

#discovery[
  *什么是分式？*

  形如 $frac(A, B)$ 的式子，其中 $A$、$B$ 都是整式，且 $B$ 中含有字母——这就是*分式*。

  例如 $frac(1, x)$、$frac(x + 1, x - 2)$、$frac(a^2 - b^2, a + b)$ 都是分式。

  关键限制：*分母不能为零*。$frac(1, x)$ 要求 $x != 0$；$frac(x + 1, x - 2)$ 要求 $x != 2$。

  *分式与分数的类比*：分式之于整式，就像分数之于整数。分数的运算规则，几乎可以原封不动地搬到分式上来！
]

#tryit[
  先试一试：计算 $frac(2, x - 1) - frac(1, x + 1)$

  思路：这两个分式的分母不同，你需要找到最简公分母（想想 $x-1$ 和 $x+1$ 的最简公倍式是什么），然后通分再相减。

  写出过程，再对照 blueprint 里的例 5 检验。
]

#blueprint[
  *一、分式的基本性质*

  分式的分子分母*同乘或同除*一个不为零的整式，分式的值不变：

  $ frac(A, B) = frac(A dot C, B dot C) = frac(A div C, B div C) quad (C != 0) $

  *约分*——把公因式除掉，化为*最简分式*：

  *例 1*：化简 $frac(6 x^2 y, 9 x y^2)$

  $ frac(6 x^2 y, 9 x y^2) = frac(2 x, 3 y) $

  *例 2*：化简 $frac(x^2 - 4, x^2 + 4 x + 4)$

  先因式分解：

  $ frac(x^2 - 4, x^2 + 4 x + 4) = frac((x + 2)(x - 2), (x + 2)^2) = frac(x - 2, x + 2) $

  *通分*——把不同分母化为相同分母（最简公分母）。

  *二、分式的四则运算*

  *乘法*：分子乘分子，分母乘分母。

  $ frac(A, B) dot frac(C, D) = frac(A C, B D) $

  *除法*：乘以倒数。

  $ frac(A, B) div frac(C, D) = frac(A, B) dot frac(D, C) = frac(A D, B C) $

  *例 3*：计算 $frac(x^2 - 1, x) dot frac(x^2, x + 1)$

  $ frac(x^2 - 1, x) dot frac(x^2, x + 1) = frac((x + 1)(x - 1), x) dot frac(x^2, x + 1) = frac((x - 1) dot x^2, x) = x(x - 1) = x^2 - x $

  *同分母加减*：分母不变，分子相加减。

  $ frac(A, C) plus.minus frac(B, C) = frac(A plus.minus B, C) $

  *异分母加减*：先通分，再加减。

  *例 4*：计算 $frac(1, a) + frac(1, b)$（回到开头的危机！）

  最简公分母是 $a b$：

  $ frac(1, a) + frac(1, b) = frac(b, a b) + frac(a, a b) = frac(a + b, a b) $

  *例 5*：计算 $frac(2, x - 1) - frac(1, x + 1)$

  最简公分母是 $(x - 1)(x + 1)$：

  $ frac(2, x - 1) - frac(1, x + 1) = frac(2(x + 1), (x - 1)(x + 1)) - frac(x - 1, (x - 1)(x + 1)) = frac(2 x + 2 - x + 1, (x - 1)(x + 1)) = frac(x + 3, x^2 - 1) $

  *三、分式方程*

  分母中含有未知数的方程叫做*分式方程*。

  解分式方程的步骤：
  + *去分母*：方程两边乘以最简公分母，将分式方程转化为整式方程
  + 解这个整式方程
  + *验根*：把解代入原方程的分母，检查是否为零

  为什么要验根？因为去分母时可能引入*增根*——让分母为零的「假解」。

  *例 6*：解方程 $frac(2, x - 1) = frac(x + 1, x - 1) + 1$

  最简公分母为 $x - 1$，两边乘以 $x - 1$：

  $ 2 = (x + 1) + (x - 1) $

  $ 2 = 2 x $

  $ x = 1 $

  *验根*：$x = 1$ 时分母 $x - 1 = 0$，分母为零！所以 $x = 1$ 是增根。

  结论：*原方程无解*。

  *例 7*：解方程 $frac(3, x + 2) + frac(1, x - 2) = frac(8, x^2 - 4)$

  注意 $x^2 - 4 = (x + 2)(x - 2)$，最简公分母为 $(x + 2)(x - 2)$：

  $ 3(x - 2) + (x + 2) = 8 $

  $ 3 x - 6 + x + 2 = 8 $

  $ 4 x - 4 = 8 $

  $ x = 3 $

  验根：$x = 3$ 时，$x + 2 = 5 != 0$，$x - 2 = 1 != 0$。✓

  答：$x = 3$。
]

#pitfall[
  *分式方程最大的坑——解出来忘记验根！*

  ❌ 解方程 $frac(2, x - 1) = frac(x + 1, x - 1) + 1$ 得 $x = 1$，直接写「$x = 1$」作为答案。

  ✓ 代入分母检验：$x = 1$ 时 $x - 1 = 0$，分母为零，原式无意义！

  这个 $x = 1$ 是*增根*——去分母时人为引入的假解，*必须舍去*。

  结论：原方程*无解*。

  *每道分式方程最后一步：把解代入所有分母，检查是否出现分母为零。*
]

#side-hack[
  增根只可能是让*分母为零的值*。所以验根时，只需要把解代入分母检查就行了——不用重新代入整个方程。
]

#mastery[
  *基础*

  + 化简：
    - $frac(3 a^2 b, 6 a b^3)$
    - $frac(x^2 - 9, x^2 - 6 x + 9)$
  + 计算：
    - $frac(x, y) dot frac(y^2, x^2)$
    - $frac(2, x) + frac(3, x)$
    - $frac(1, x) - frac(1, x + 1)$

  *应用*

  + 解分式方程：
    - $frac(1, x) = frac(2, x + 3)$
    - $frac(x, x - 2) - 1 = frac(4, x - 2)$
  + 甲单独做一件工作需 10 天，乙需 15 天。两人合作几天可以完成？

  *挑战*

  + 化简：$frac(1, x - 1) - frac(1, x + 1) - frac(2, x^2 - 1)$
  + 解方程：$frac(2, x - 1) + frac(1, x + 1) = frac(7 x - 1, x^2 - 1)$
  + 若 $frac(x, x - 3)$ 的值为整数，求满足条件的所有整数 $x$。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + $frac(3 a^2 b, 6 a b^3) = frac(a, 2 b^2)$；$frac(x^2 - 9, x^2 - 6 x + 9) = frac((x + 3)(x - 3), (x - 3)^2) = frac(x + 3, x - 3)$
  + $frac(x, y) dot frac(y^2, x^2) = frac(y, x)$；$frac(2, x) + frac(3, x) = frac(5, x)$；$frac(1, x) - frac(1, x + 1) = frac(x + 1 - x, x(x + 1)) = frac(1, x(x + 1))$

  *应用*
  + $frac(1, x) = frac(2, x + 3)$：交叉相乘得 $x + 3 = 2 x$，$x = 3$。验根：分母不为零 ✓
  + $frac(x, x - 2) - 1 = frac(4, x - 2)$：两边乘 $x - 2$，得 $x - (x - 2) = 4$，即 $2 = 4$，矛盾。原方程无解。
  + 设合作 $t$ 天完成。$frac(t, 10) + frac(t, 15) = 1$，通分 $frac(3 t + 2 t, 30) = 1$，$5 t = 30$，$t = 6$ 天。

  *挑战*
  + $frac(1, x - 1) - frac(1, x + 1) - frac(2, x^2 - 1) = frac(x + 1 - (x - 1) - 2, (x - 1)(x + 1)) = frac(0, (x - 1)(x + 1)) = 0$
  + 两边乘 $(x + 1)(x - 1)$：$2(x + 1) + (x - 1) = 7 x - 1$，化简得 $3 x + 1 = 7 x - 1$，所以 $x = frac(1, 2)$。验根：分母均不为零 ✓
  + $frac(x, x - 3) = 1 + frac(3, x - 3)$。分式值为整数，需 $frac(3, x - 3)$ 为整数，即 $(x - 3) | 3$。$x - 3 in {1, -1, 3, -3}$，$x in {4, 2, 6, 0}$。
]
