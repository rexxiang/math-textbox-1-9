#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, secref, tryit, pitfall

== §6.5 分式：字母也能当分母 <sec-6-5>

#vocab[分式 rational expression / 最简分式 irreducible rational expression / 约分 simplification]

#crisis[
  在 #secref("2.2") 里，你学过分数：$frac(3, 4)$、$frac(5, 12)$——分母都是具体的数字。

  但有一天，你遇到了这样的问题：

  甲队单独完成一项工程需要 $a$ 天，乙队需要 $b$ 天。两队合作，甲一天完成 $frac(1, a)$，乙一天完成 $frac(1, b)$。

  等等——$frac(1, a)$ 算是什么？分母里有字母，这在我们学过的分数里从来没出现过。

  还有：$frac(x + 1, x - 2)$、$frac(a^2 - b^2, a + b)$……这些式子到底是什么？

  我们需要给它们一个名字，也需要搞清楚怎么化简它们。
]

#discovery[
  *从分数到分式的类比*

  回想整数和整式的关系：$3$、$-5$、$12$ 是整数；$3x$、$x^2 + 1$、$2a - b$ 是整式——把整数中的数字位置换成含字母的式子，就变成了整式。

  分数和分式的关系完全类似：

  - 分数：$frac(3, 4)$，分母是整数
  - 分式：$frac(3, x)$，分母含字母

  所以，*分式就是分母里含有字母的分数*——背后的逻辑完全一样。

  那么，分式能约分吗？当然可以！$frac(6, 9) = frac(2, 3)$ 的道理——分子分母除以公因数——完全可以搬到分式上来：

  $ frac(6 x^2 y, 9 x y^2) = frac(2 x, 3 y) $

  只要找到分子分母共有的因式，除掉就好了。这个操作，我们在分数里叫"约分"，在分式里也叫约分。
]

#tryit[
  先自己试试，不要偷看下面的蓝图！

  化简分式：$frac(x^2 - 9, x^2 - 6x + 9)$

  提示：分子和分母都能因式分解——试着用 §6.4 学过的平方差公式和完全平方公式。分解之后，看看有没有公因式可以约掉。

  写完之后对照 blueprint 里的例 2 检验。
]

#blueprint[
  *工具一：分式*

  形如 $frac(A, B)$ 的式子，其中 $A$、$B$ 都是整式，且 $B$ 中含有字母，叫做*分式*。

  - $A$ 叫做分子，$B$ 叫做分母
  - *分母不能为零*：这是分式成立的必要条件

    $frac(1, x)$ 要求 $x != 0$；$frac(x + 1, x - 2)$ 要求 $x != 2$

  不含字母的分母（分母是具体数字）就退化成普通分数——分式是分数的推广。

  *工具二：分式的基本性质*

  分式的分子和分母*同乘或同除一个不为零的整式*，分式的值不变：

  $ frac(A, B) = frac(A dot C, B dot C) = frac(A div C, B div C) quad (C != 0) $

  这和分数的基本性质完全一致：$frac(1, 2) = frac(2, 4) = frac(3, 6)$，背后是同一个道理。

  *工具三：约分*

  把分式分子和分母的*公因式*除掉，使分式变成*最简分式*，这个过程叫做*约分*。

  最简分式的判断标准：分子和分母再也没有公因式了（除了常数 $1$）。

  *例 1*：化简 $frac(6 x^2 y, 9 x y^2)$

  找公因式：分子 $6 x^2 y$，分母 $9 x y^2$，公因式是 $3 x y$

  $ frac(6 x^2 y, 9 x y^2) = frac(3 x y dot 2 x, 3 x y dot 3 y) = frac(2 x, 3 y) $

  *例 2*：化简 $frac(x^2 - 9, x^2 - 6 x + 9)$

  先因式分解（调用 §6.4 的工具）：

  $ frac(x^2 - 9, x^2 - 6 x + 9) = frac((x + 3)(x - 3), (x - 3)^2) = frac(x + 3, x - 3) $

  约掉了公因式 $(x - 3)$。注意：约分后仍然要求 $x != 3$（分母不能为零）。

  *例 3*：化简 $frac(a^2 - b^2, a + b)$

  $ frac(a^2 - b^2, a + b) = frac((a + b)(a - b), a + b) = a - b quad (a != -b) $
]

#pitfall[
  *约分最大的两个坑：*

  *坑一：忘记分母不能为零的条件*

  $frac(x^2 - 9, x^2 - 6 x + 9) = frac(x + 3, x - 3)$ ——约分后，$x = 3$ 仍然不合法！不能因为约完就忘了这个限制。

  *坑二：把加减项当乘积因式来约掉*

  ❌ 把 $frac(x + 3, x + 5)$ 里的 $x$ 约掉，得 $frac(3, 5)$

  你以为 $x$ 是公因式，但其实 $x + 3$ 和 $x + 5$ 里，$x$ 只是加在一起的项，不是整体乘出来的因式。

  ✓ 能约掉的，必须是分子和分母*各自整体都能提出来的乘法因式*，加在一起的项不能直接消掉。

  记住：*先因式分解，再找公因式，最后才约分。*
]

#side-hack[
  约分之前，*先因式分解*——这是统一诀窍。不管分式看起来多复杂，先把分子和分母各自分解成乘积形式，再看哪些因式相同，才能合法约掉。
]

#mastery[
  *基础*

  + 以下哪些是分式？
    - $frac(3, 4)$
    - $frac(x + 1, 5)$
    - $frac(2, x - 1)$
    - $frac(x^2 + 1, x^2 + 2)$
  + 化简（约分）：
    - $frac(3 a^2 b, 6 a b^3)$
    - $frac(x^2 - 4, x^2 + 4 x + 4)$
    - $frac(2 x^2 - 8, x^2 - 4 x + 4)$

  *应用*

  + 化简并写出取值限制：
    - $frac(x^2 + x, x^2 - 1)$
    - $frac(a^2 - 2 a b + b^2, a^2 - b^2)$
  + 甲有 $a$ 本书，乙有 $b$ 本书，$a > b$。甲给乙多少本书后，两人书的数量相等？用分式表示每人最终的书数。

  *挑战*

  + 化简：$frac(x^3 - x, x^2 - 2 x + 1)$（提示：分子先提公因式再因式分解）
  + 已知 $frac(a^2 - 1, a + 1)$ 化简后的值为 $3$，求 $a$。
  + 如果 $frac(x + 3, x - 2)$ 的值是整数，$x$ 是整数时有哪些可能？（提示：把分子改写为 $(x - 2) + 5$）

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + $frac(3, 4)$ 不是分式（分母没有字母）；$frac(x + 1, 5)$ 也不是（分母 $5$ 没有字母）；$frac(2, x - 1)$ 和 $frac(x^2 + 1, x^2 + 2)$ 是分式。
  + $frac(3 a^2 b, 6 a b^3) = frac(a, 2 b^2)$；$frac(x^2 - 4, x^2 + 4 x + 4) = frac((x+2)(x-2), (x+2)^2) = frac(x-2, x+2)$；$frac(2 x^2 - 8, x^2 - 4 x + 4) = frac(2(x+2)(x-2), (x-2)^2) = frac(2(x+2), x-2)$

  *应用*
  + $frac(x^2+x, x^2-1) = frac(x(x+1), (x+1)(x-1)) = frac(x, x-1)$（$x != -1$ 且 $x != 1$）；$frac(a^2-2ab+b^2, a^2-b^2) = frac((a-b)^2, (a+b)(a-b)) = frac(a-b, a+b)$（$a != b$ 且 $a != -b$）
  + 甲给乙 $frac(a-b, 2)$ 本，每人最终有 $frac(a+b, 2)$ 本。

  *挑战*
  + $frac(x^3-x, x^2-2x+1) = frac(x(x+1)(x-1), (x-1)^2) = frac(x(x+1), x-1)$（$x != 1$）
  + $frac(a^2-1, a+1) = a - 1 = 3$，所以 $a = 4$。
  + $frac(x+3, x-2) = 1 + frac(5, x-2)$。$x$ 为整数时，需要 $(x-2)$ 整除 $5$，即 $x - 2 in {1, -1, 5, -5}$，$x in {3, 1, 7, -3}$。
]
