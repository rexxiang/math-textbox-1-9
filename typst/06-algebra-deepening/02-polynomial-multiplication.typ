#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref

== §6.2 整式相乘与乘法公式 <sec-6-2>

#vocab[展开 expansion / 平方差公式 difference of squares / 完全平方公式 perfect-square formula]

#crisis[
  只会整理还不够。有些关系一开始就是“一个整体乘另一个整体”：

  - 长方形长是 $(x + 3)$，宽是 $(x - 2)$，面积怎么写成整理后的式子？
  - 把 $2(x + 5)$ 这样的“一个数乘整个括号”乘开时，你已经会分配；那 $(x + 3)(x - 2)$ 呢？
  - $(a + b)(a + b)$ 和 $(a + b)(a - b)$ 这样的乘积，结果会不会藏着固定的结构？

  所以这一节的主线很清楚：*先把乘积乘开，再从乘开的结果里长出可以记住的公式*。
]

#history-note[
  很多乘法公式并不是数学家“凭空发明”的口诀，而是人们在反复展开面积关系、平方关系后，发现某些结果总会重复出现，于是把这些高频结果单独记下来。公式，其实是被保存下来的展开经验。
]

#discovery[
  在 #secref("1.7") 里，你已经见过分配律：一个数乘括号，要把括号里的每一项都乘到。现在只是把这个想法继续往前推。

  - $2(x + 5)$ 是“一项去乘两项”
  - $(x + 3)(x - 2)$ 是“两项去乘两项”
  - $(a + b)(a + b)$ 则是“同一个整体乘自己”

  所以这一节要发明的，不是一个完全陌生的动作，而是把“分到括号里去乘”这件事扩成更完整的整式乘法。
]

#tryit[
  先自己展开 $(2x + 1)(x - 3)$。

  提示：让前一个括号里的每一项都去乘后一个括号里的每一项，再把结果整理。
]

#blueprint[
  *一、整式相乘：先乘开，再整理*

  *单项式乘多项式*：把这一项分配到括号里的每一项。

  $ 2(x + 5) = 2x + 10 $

  *多项式乘多项式*：前一个括号里的每一项，都要去乘后一个括号里的每一项。

  $ (a + b)(c + d) = a c + a d + b c + b d $

  当同一个因式乘自己时，常写成“平方”：

  $ x dot x = x^2, quad (a + b)(a + b) = (a + b)^2 $

  这里先把平方看成“两个一样的因式相乘”的短写；更一般的幂记号和指数规则放到 #secref("6.3") 再统一整理。

  *例 1*：展开 $(2x + 1)(x - 3)$

  $ &= 2x dot x + 2x dot (-3) + 1 dot x + 1 dot (-3) \
    &= 2x^2 - 6x + x - 3 \
    &= 2x^2 - 5x - 3 $

  *例 2*：展开 $(x + 4)^2$

  $ (x + 4)^2 = (x + 4)(x + 4) = x^2 + 4x + 4x + 16 = x^2 + 8x + 16 $

  *二、平方差公式*

  如果两个括号里只有中间符号不同：

  $ (a + b)(a - b) = a^2 - b^2 $

  中间那一正一负的两项会刚好抵消掉。

  *例 3*：$101 times 99 = (100 + 1)(100 - 1) = 100^2 - 1^2 = 9999$

  *例 4*：展开 $(3x + 2)(3x - 2)$

  $ (3x + 2)(3x - 2) = (3x)^2 - 2^2 = 9x^2 - 4 $

  *三、完全平方公式*

  当一个整体乘自己时，会得到：

  $ (a + b)^2 = a^2 + 2 a b + b^2 \
    (a - b)^2 = a^2 - 2 a b + b^2 $

  *例 5*：展开 $(3x - 2)^2$

  $ (3x - 2)^2 = (3x)^2 - 2 dot 3x dot 2 + 2^2 = 9x^2 - 12x + 4 $

  *例 6*：展开 $(2a + 3b)^2$

  $ (2a + 3b)^2 = (2a)^2 + 2 dot 2a dot 3b + (3b)^2 = 4 a^2 + 12 a b + 9 b^2 $
]

#pitfall[
  *两个常见误用：*

  *① 多项式乘多项式时漏乘。*

  展开 $(x + 2)(x + 5)$：

  ❌ $x^2 + 10$（只乘了“前前”和“后后”）

  ✓ 应该每一项都乘：$x^2 + 5x + 2x + 10 = x^2 + 7x + 10$

  *② 完全平方公式的中间项不能丢。*

  ❌ $(a + b)^2 = a^2 + b^2$

  ✓ $(a + b)^2 = a^2 + 2 a b + b^2$

  那个中间项，正是“一项乘另一项”出现了两次。
]

#side-hack[
  先别急着背公式，先问自己：

  - 这是普通乘开，还是两个特别像的括号？
  - 如果两个括号一正一负，能不能想到平方差？
  - 如果是同一个括号乘自己，能不能想到完全平方？

  公式不是额外多出来的路，而是“乘开以后经常会重现的近路”。
]

#mastery[
  *基础*

  + 展开：
    - $3(x + 2)$
    - $(x + 1)(x + 4)$
    - $(2x - 1)(x + 3)$
  + 用公式直接写结果：
    - $(a + 5)(a - 5)$
    - $(m + 2)^2$
    - $(2x - 3)^2$

  *应用*

  + 一块长方形菜地长为 $(x + 2)$ m、宽为 $(x + 5)$ m。把面积写成整理后的整式。
  + 用平方差公式计算：$1003 times 997$
  + 先展开，再整理：$(x + 2)^2 - (x + 2)(x - 1)$

  *挑战*

  + 已知 $(a + b)(a - b) = 21$，$a + b = 7$，求 $a - b$。
  + 不直接展开所有项，计算 $(y + 1)^2 - (y - 1)^2$。
  + 解释为什么 $(x + 3)^2$ 的中间项是 $6x$，而不是 $3x$。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + $3(x + 2) = 3x + 6$；$(x + 1)(x + 4) = x^2 + 5x + 4$；$(2x - 1)(x + 3) = 2x^2 + 5x - 3$
  + $(a + 5)(a - 5) = a^2 - 25$；$(m + 2)^2 = m^2 + 4m + 4$；$(2x - 3)^2 = 4x^2 - 12x + 9$

  *应用*
  + $(x + 2)(x + 5) = x^2 + 7x + 10$
  + $(1000 + 3)(1000 - 3) = 1000^2 - 3^2 = 999991$
  + $(x + 2)^2 - (x + 2)(x - 1) = (x^2 + 4x + 4) - (x^2 + x - 2) = 3x + 6$

  *挑战*
  + 因为 $(a + b)(a - b) = 21$ 且 $a + b = 7$，所以 $a - b = 3$
  + $(y + 1)^2 - (y - 1)^2 = (y^2 + 2y + 1) - (y^2 - 2y + 1) = 4y$
  + 因为 $(x + 3)^2 = (x + 3)(x + 3)$，中间会出现两次 $3x$：一次来自前面的 $x dot 3$，一次来自后面的 $3 dot x$，所以一共是 $6x$
]
