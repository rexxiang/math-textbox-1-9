#import "../lib/theme-v2.typ": blueprint, mastery, side-hack, secref

== §6.7 代数基础总结：从整式到一元一次方程 <sec-6-7>

#blueprint[
  *基础段速查卡*

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *整式基础*（#secref("6.1")、#secref("6.2")、#secref("6.3")）
      - 先看哪些项能整理，再看要不要乘开
      - 乘法公式不是另一套魔法，而是乘开的近路
      - 指数规则先认类型：同底、幂套幂、整积乘方
    ],
    block[
      *因式分解与分式*（#secref("6.4")、#secref("6.5")）
      - 展开和因式分解是可逆的两步
      - 公因式优先，公式第二，结构观察始终跟着走
      - 分式先看定义域，再谈约分、通分、去分母；分式方程最后一定验根
    ],
  )

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *一元一次方程*（#secref("6.6")）
      - 本质上是在保持平衡地整理关系
      - 去分母、去括号、移项、合并、系数化 1 是常用流程
      - 应用题先设未知数，再把条件写成平衡关系
    ],
    block[
      *基础段收束*（#secref("6.1") 到 #secref("6.6")）
      - 顺序是：整理 → 乘开 → 指数 → 分解 → 分式 → 方程
      - 每往后一步，都是把关系写得更稳、更能操作
      - 到这里，“写式子、变式子、守平衡”已经连成一条完整基础链
    ],
  )
]

#side-hack[
  *半程先收好*：

  到这里，代数路线的基础半程已经把“写式子、变式子、守平衡”三件事练顺了。先把这条基础链收好：看见关系，知道怎样写；式子变形，知道怎样稳；需要列方程时，也知道怎样一步步把关系整理清楚。
]

#mastery[
  *过桥自检*

  + 看到一个代数式时，你会先用哪两步判断“能否直接整理”，还是“应先乘开 / 分解再看”？

  + 为什么分式题里“先看原分母能不能为 0”必须早于最后的答案整理？

  + 请用一句话把 #secref("6.4") 的因式分解和 #secref("6.6") 的解方程连起来：它们都在做哪一种“把复杂关系变得更可操作”的工作？

  + 如果一道应用题最后要列方程求解，你会按”设未知数 → 写关系 → 整理式子 → 守平衡”的哪条顺序开工？请用一句话说清你的起手步骤。

  + *【跨章回顾】* 把 #secref(“2.1B”) 里讲过的”分数约分”和 #secref(“6.5”) 里的”分式约分”放在一起看：
    - $frac(6, 9)$ 是分数约分（ch2），$frac(6x, 9x)$ 是分式约分（ch6）——它们做的事情有什么共同点？
    - 分数 $frac(6, 9) = frac(2, 3)$ 的依据是什么规则？分式 $frac(6x, 9x) = frac(2, 3)$（$x != 0$）的依据又是什么？
    - 为什么 $x != 0$ 这个条件在 ch6 里必须写，但在 ch2 的纯数分数里不需要？
]
