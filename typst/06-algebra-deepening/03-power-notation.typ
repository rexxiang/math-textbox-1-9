#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref

== §6.3 幂的记号与整式里的指数运算 <sec-6-3>

#vocab[幂 power / 指数 exponent / 底数 base]

#crisis[
  乘开会让很多式子越来越长，可有时真正麻烦的不是“要不要乘开”，而是“同一个因子反复写太长了”：

  - $x dot x dot x dot x$ 有没有更短的写法？
  - $(2a) dot (2a) dot (2a)$ 能不能一眼看出结果里有几个 2、几个 $a$？
  - 一串 $x dot x dot x dot x$ 再接上一串 $x dot x dot x$，最后再去掉一串 $x dot x$，难道每次都要全写开再数一遍吗？

  所以这一节要做的事是：*先发明重复相乘的短写法，再整理这种短写法怎样运算*。
]

#history-note[
  指数记号并不是一开始就这么统一的。人们先是不停遇到“同一个量重复相乘”的长串写法，后来才慢慢把它压缩成更短的符号。符号一旦变短，规律就更容易被看出来，运算也就更稳定了。
]

#discovery[
  在 #secref("6.2") 里，你已经见过 $x^2$ 这种“平方”的短写。现在把这个想法再往前推进：

  - $x dot x dot x$ 可以看成“同一个 $x$ 连乘了 3 次”
  - $a dot a dot a dot a dot a$ 可以看成“同一个 $a$ 连乘了 5 次”
  - 如果两串同底数的重复乘法接到一起，次数之间会不会有简单关系？

  所以这一节先把记号讲清，再把最常用的指数规则收成一套能直接调用的工具。
]

#tryit[
  先不看下面，自己试着整理：

  - 把 $x dot x dot x dot x$ 写成短写法
  - 把 $a dot a dot a times a dot a$ 写成一次更短的写法
  - 把 $(m dot m)$ 这一整块重复 3 次，会不会有更短的写法？
]

#blueprint[
  *一、幂的记号*

  同一个因子重复相乘，可以写成*幂*：

  $ a dot a dot a = a^3 $

  这里：

  - $a$ 叫*底数*
  - $3$ 叫*指数*

  $a^3$ 读作“$a$ 的三次幂”，意思就是 3 个 $a$ 相乘。

  *例 1*：把 $x dot x dot x dot x dot x$ 写成幂。

  $ x dot x dot x dot x dot x = x^5 $

  *例 2*：把 $(2a) dot (2a) dot (2a)$ 写成幂。

  $ (2a) dot (2a) dot (2a) = (2a)^3 $

  *二、同底数幂接在一起*

  （$a, b != 0$，$m, n$ 为非负整数）

  这一类题表面上有乘也有除，其实都只在问一件事：同一个底数接在一起后，因子个数是变多了，还是被约掉了一些。

  *同底数幂相乘*：

  $ a^m times a^n = a^(m+n) $

  因为前面有 $m$ 个 $a$，后面有 $n$ 个 $a$，接起来就是 $m+n$ 个。

  *同底数幂相除*（当 $m >= n$ 时）：

  $ a^m div a^n = a^(m-n) $

  因为分子分母里有相同个数的 $a$ 可以约掉。

  *零次幂*：

  零次幂不是额外冒出来的一条新口诀，而是“刚好约完”的结果：

  $ a^0 = 1 quad (a != 0) $

  它可以从上面的规则看出来：$a^3 div a^3 = a^(3-3) = a^0$，而左边明明等于 1，所以 $a^0 = 1$。

  *例 3*：化简 $x^4 times x^3$

  $ x^4 times x^3 = x^(4+3) = x^7 $

  *例 4*：化简 $x^7 div x^3$

  $ x^7 div x^3 = x^(7-3) = x^4 $

  *例 5*：化简 $x^4 times x^3 div x^2$

  $ x^4 times x^3 div x^2 = x^(4+3-2) = x^5 $

  *三、整个块再乘方*

  有时不是同底数接在一起，而是“整个东西”外面又套了一层乘方。这时别急着一项一项数，先看外面这一层会不会把整块里的每个因子都重复了同样多次。

  *幂的乘方*：

  $ (a^m)^n = a^(m n) $

  *积的乘方*：

  $ (a b)^n = a^n b^n $

  *例 6*：化简 $(x^2)^3$

  $ (x^2)^3 = x^(2 times 3) = x^6 $

  *例 7*：化简 $(2a)^3$

  $ (2a)^3 = 2^3 a^3 = 8a^3 $
]

#pitfall[
  *两个很容易混淆的地方：*

  *① 底数不同，指数不能乱加。*

  ❌ $a^3 times b^4 = (a b)^7$

  ✓ 只有同底数幂相乘，才能把指数相加：$a^m times a^n = a^(m+n)$

  *② 幂的乘方是指数相乘，不是相加。*

  ❌ $(a^3)^4 = a^7$

  ✓ $(a^3)^4 = a^(3 times 4) = a^12$
]

#side-hack[
  看到指数题时，先分三步：

  - 先看是不是在把重复乘法写短
  - 如果同底数接在一起了，就想“是接上去，还是约掉一些”
  - 如果外面又套了一层乘方，再分辨是“幂套幂”还是“整积一起乘方”

  先认类型，再套规则，就不容易乱。
]

#mastery[
  *基础*

  + 写成幂的形式：
    - $y dot y dot y$
    - $m dot m dot m dot m dot m$
  + 化简：
    - $x^3 times x^5$
    - $(a^2)^4$
    - $(3m)^2$
    - $b^7 div b^2$

  *应用*

  + 化简：$x^2 times x^3 div x$
  + 化简：$frac((2a)^3 times a^2, 4a)$
  + 已知一个正方体棱长是 $x$，体积写成 $x dot x dot x$。请把它改写成幂。

  *挑战*

  + 解释为什么 $a^3 div a^3$ 一定等于 1，从而说明 $a^0 = 1$。
  + 若 $x^m times x^2 = x^9$，求 $m$。
  + 比较 $(a b)^2$ 和 $a^2 + b^2$：为什么它们不是一回事？

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + $y^3$；$m^5$
  + $x^3 times x^5 = x^8$；$(a^2)^4 = a^8$；$(3m)^2 = 9m^2$；$b^7 div b^2 = b^5$

  *应用*
  + $x^2 times x^3 div x = x^(2+3-1) = x^4$
  + $frac((2a)^3 times a^2, 4a) = frac(8a^3 times a^2, 4a) = frac(8a^5, 4a) = 2a^4$
  + $x^3$

  *挑战*
  + 因为同底数幂相除会约掉完全相同的因子，所以 $a^3 div a^3 = 1$；而按规则又等于 $a^(3-3) = a^0$，因此 $a^0 = 1$
  + $m + 2 = 9$，所以 $m = 7$
  + $(a b)^2$ 表示 $(a b)(a b) = a^2 b^2$；而 $a^2 + b^2$ 是两个平方再相加，它们的运算结构不同
]
