#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref

== §6.6 一元二次方程 <sec-6-6>

#vocab[一元二次方程 quadratic equation / 配方法 completing the square / 求根公式 quadratic formula]

#crisis[
  一块长方形花圃，长比宽多 1 米，面积是 7 平方米。

  如果设宽为 $x$ 米，那么长就是 $x + 1$ 米，面积条件会写成：

  $ x(x + 1) = 7 $

  展开后就是

  $ x^2 + x - 7 = 0 $

  未知数的*平方*出现了！一元一次方程的方法不够用了——我们需要新的解方程工具。
]

#history-note[
  古巴比伦人早在公元前 1800 年就已经会解一些二次方程。后来，数学家逐渐发现：很多看起来不同的题，其实都在想办法把式子变成“一个平方等于某个数”，再从那里把解读出来。现代的求根公式，本质上就是把这个过程打包保存下来。
]

#discovery[
  *什么是一元二次方程？*

  含一个未知数，最高次数为 2 的方程，叫作*一元二次方程*。它的一般形式是：

  $ a x^2 + b x + c = 0 quad (a != 0) $

  先看我们已经会解的特殊样子：

  - $x^2 = 9$，所以 $x = plus.minus 3$
  - $(x - 1)^2 = 4$，所以 $x - 1 = plus.minus 2$，进而 $x = 3 "或" x = -1$

  这说明：只要左边已经是“一个平方”，问题就简单了。

  可一般方程往往不是这个样子。比如：

  $ x^2 + 6 x + 5 = 0 $

  它既不是现成的平方，也不一定总能一眼分解。那能不能把它*补成平方*，再回到我们熟悉的路上？
]

#tryit[
  不看下面的内容，先试着处理：

  $ x^2 + 6 x + 5 = 0 $

  先移项成 $x^2 + 6x = -5$。

  现在想一想：为了把左边补成 $(x + m)^2$ 的形式，应该补上哪个数？补完之后，能不能把方程解出来？
]

#blueprint[
  *一、先认出已经是平方的方程：直接开平方法*

  适用于 $x^2 = k$ 或 $(x + m)^2 = k$ 这样的形式。

  *例 1*：解 $(2 x - 1)^2 = 9$

  $2 x - 1 = plus.minus 3$

  $2 x = 4 "或" 2 x = -2$

  $x = 2 "或" x = -1$

  这一招很直接，但前提是：左边已经是平方。

  *二、把不是平方的方程补成平方：配方法*

  关键观察：  
  $x^2 + 6x$ 只差一个 $9$，就会变成 $(x + 3)^2$，因为

  $ (x + 3)^2 = x^2 + 6 x + 9 $

  这里补上的 $9$，正是一次项系数一半的平方：$(frac(6, 2))^2 = 9$。

  *例 2*：解 $x^2 + 6 x + 5 = 0$

  $ x^2 + 6 x = -5 $

  两边同时加 $9$：

  $ x^2 + 6 x + 9 = -5 + 9 $

  $ (x + 3)^2 = 4 $

  $ x + 3 = plus.minus 2 $

  $ x = -1 "或" x = -5 $

  *例 3*：解 $2 x^2 - 4 x - 1 = 0$

  先把二次项系数化成 1：

  $ x^2 - 2 x - frac(1, 2) = 0 $

  $ x^2 - 2 x = frac(1, 2) $

  两边同时加 $1$：

  $ x^2 - 2 x + 1 = frac(1, 2) + 1 $

  $ (x - 1)^2 = frac(3, 2) $

  $ x - 1 = plus.minus sqrt(frac(3, 2)) $

  $ x = 1 plus.minus frac(sqrt(6), 2) $

  *三、把配方法打包：求根公式*

  对一般方程 $a x^2 + b x + c = 0$，如果每次都重新配方，会有点慢。于是我们把配方法的结果打包成统一公式。

  这里 $a$ 是二次项系数，$b$ 是一次项系数，$c$ 是常数项。

  $ x^2 + frac(b, a) x = -frac(c, a) $

  $ x^2 + frac(b, a) x + (frac(b, 2 a))^2 = (frac(b, 2 a))^2 - frac(c, a) $

  $ (x + frac(b, 2 a))^2 = frac(b^2 - 4 a c, 4 a^2) $

  $ x = frac(-b plus.minus sqrt(b^2 - 4 a c), 2 a) $

  这就是*求根公式*。它不是新魔法，而是“配方法的打包版”。
  如果根号里的 $b^2 - 4 a c$ 是负数，那么在实数范围内就没有解。

  *例 4*：解 $3 x^2 - 5 x + 1 = 0$

  $a = 3$，$b = -5$，$c = 1$

  $ x = frac(-(-5) plus.minus sqrt((-5)^2 - 4 times 3 times 1), 2 times 3) $

  $ x = frac(5 plus.minus sqrt(13), 6) $

  *例 5*：回到开头的花圃问题 $x^2 + x - 7 = 0$

  这里 $a = 1$，$b = 1$，$c = -7$

  $ x = frac(-1 plus.minus sqrt(1^2 - 4 times 1 times (-7)), 2 times 1) $

  $ x = frac(-1 plus.minus sqrt(29), 2) $

  宽要取正值，所以

  $ x = frac(-1 + sqrt(29), 2) approx 2.19 $

  长约为 $3.19$ 米。

  *四、旧工具还能用：因式分解是快捷入口*

  有些二次方程在前面 #secref("6.2") 已经练过：如果左边能顺手因式分解，就可以直接走“零乘积”这条快路。

  *例 6*：解 $x^2 - 5 x + 6 = 0$

  $ (x - 2)(x - 3) = 0 $

  $ x = 2 "或" x = 3 $

  *例 7*：解 $2 x^2 + 3 x = 0$

  $ x(2 x + 3) = 0 $

  $ x = 0 "或" x = -frac(3, 2) $
]

#pitfall[
  *三个经典陷阱*：

  *① 开平方时漏掉 $plus.minus$*

  例如 $(x - 2)^2 = 9$：

  ❌ 只写 $x - 2 = 3$，漏掉另一种可能

  ✓ 应写 $x - 2 = plus.minus 3$，所以 $x = 5 "或" x = -1$

  *② 配方时“补几”写错*

  $x^2 + 6x$ 应补 $(frac(6, 2))^2 = 9$，不是 $6^2 = 36$。

  *③ 代求根公式时把负号带错*

  对 $3x^2 - 5x + 1 = 0$，这里 $b = -5$，所以

  ❌ $x = frac(-5 plus.minus sqrt(13), 6)$

  ✓ $x = frac(-(-5) plus.minus sqrt(13), 6) = frac(5 plus.minus sqrt(13), 6)$
]

#side-hack[
  *选方法时先问三句：*

  + 已经是 $(x + m)^2 = k$ 吗？ → *直接开平方法*
  + 左边能顺手因式分解吗？ → *因式分解快捷入口*
  + 前两条都不顺手吗？ → *求根公式*

  配方法是这整章的“发明过程”，求根公式是它的“打包结果”。
]

#mastery[
  *基础*

  + 用直接开方法解：
    - $(x + 2)^2 = 16$
    - $4 x^2 - 25 = 0$
  + 用因式分解法解：
    - $x^2 - 7 x + 10 = 0$
    - $x^2 + 4 x = 0$
  + 用求根公式解：
    - $2 x^2 - 3 x - 2 = 0$
    - $x^2 + 4 x + 1 = 0$

  *应用*

  + 一块矩形花圃，长比宽多 3 m，面积为 40 m²。求长和宽。
  + 一个数比它的倒数大 $frac(3, 2)$，求这个数。
  + 两个连续正整数的积是 72。求这两个整数。

  *挑战*

  + 用配方法解 $3 x^2 + 8 x - 3 = 0$。
  + 用配方法说明 $x^2 - 4 x + 5 = 0$ 在实数范围内无解。
  + 若矩形的面积是 48 cm²，长比宽多 2 cm，求长和宽。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + $x + 2 = plus.minus 4$，$x = 2 "或" x = -6$；$x^2 = frac(25, 4)$，$x = plus.minus frac(5, 2)$
  + $(x - 2)(x - 5) = 0$，$x = 2 "或" x = 5$；$x(x + 4) = 0$，$x = 0 "或" x = -4$
  + $x = frac(3 plus.minus sqrt(9 + 16), 4) = frac(3 plus.minus 5, 4)$，$x = 2 "或" x = -frac(1, 2)$；$x = frac(-4 plus.minus sqrt(16 - 4), 2) = -2 plus.minus sqrt(3)$

  *应用*
  + 设宽为 $x$ m，则长为 $x + 3$ m。$x(x + 3) = 40$，$x^2 + 3x - 40 = 0$，$(x + 8)(x - 5) = 0$。取正值，宽 5 m，长 8 m。
  + 设这个数为 $x$。$x - frac(1, x) = frac(3, 2)$，两边乘以 $2x$，得 $2x^2 - 3x - 2 = 0$。解得 $x = 2 "或" x = -frac(1, 2)$。
  + 设较小的整数为 $x$，则 $x(x + 1) = 72$，即 $x^2 + x - 72 = 0$。因式分解得 $(x + 9)(x - 8) = 0$，所以 $x = 8$ 或 $x = -9$。题目要正整数，所以这两个整数是 8 和 9。

  *挑战*
  + 先除以 3：$x^2 + frac(8, 3)x - 1 = 0$。补上 $(frac(4, 3))^2 = frac(16, 9)$，得 $(x + frac(4, 3))^2 = frac(25, 9)$。所以 $x = -frac(4, 3) plus.minus frac(5, 3)$，即 $x = frac(1, 3) "或" x = -3$。
  + $x^2 - 4x = -5$，两边加 4，得 $(x - 2)^2 = -1$。平方不可能等于负数，所以它在实数范围内无解。
  + 设宽为 $x$ cm，则长为 $x + 2$ cm。$x(x + 2) = 48$，$x^2 + 2x - 48 = 0$，$(x + 8)(x - 6) = 0$。取正值，宽 6 cm，长 8 cm。
]
