#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref

== §6.1 代数式、整式与幂的运算 <sec-6-1>

#vocab[代数式 algebraic expression / 单项式 monomial / 多项式 polynomial / 同类项 like terms / 幂 power / 底数 base / 指数 exponent]

#crisis[
  在桥接章里，字母已经能做“计算模板”。可一旦模板开始彼此组合，新的问题立刻冒出来：

  - 一个边长为 $(x + 3)$ 的正方形，比边长为 $x$ 的正方形面积多多少？
  - 一个宽为 $a$、高为 $(a - 2)$ 的长方形，面积该怎样展开？
  - $10^6 times 10^3$ 为什么可以一下子写成 $10^9$？

  这时，光会“把关系写出来”还不够。我们还得学会把式子*分类、化简、展开、压缩*，让代数式像数字一样可以被系统操作。
]

#history-note[
  16 世纪法国数学家弗朗索瓦·韦达（François Viète）把字母表示数这件事真正系统化之后，代数才从“用文字叙述问题”变成“用符号操作关系”。一旦式子能被可靠地加、减、乘、除，数学家就拥有了一种远比口头描述更强的思考工具。
]

#discovery[
  在 #secref("4.3") 里，你已经把字母看成数量的占位符。现在往前一步：*这些模板之间也要会计算*。

  - $25 n + 80$、$2(a + b)$、$3 x^2 - 5 x + 7$ 都是*代数式*
  - 像 $3 x^2$、$-5 a b$、$7$ 这样由数和字母乘起来的式子叫*单项式*
  - 几个单项式加减起来，就得到*多项式*
  - $3 x^2$ 和 $-7 x^2$ 可以合并，因为它们的字母部分完全相同，是*同类项*
  - 当同一个量重复相乘时，就需要更紧凑的幂记号：$x times x times x = x^3$

  所以本节真正要建立的，不是“字母是什么”，而是：*整式怎样像一个整体那样被整理和运算*。
]

#side-hack[
  合并同类项的口诀——*字母指数不变，系数相加减*。就像收拾房间：3 个苹果加 5 个苹果等于 8 个苹果，$3 x$ 加 $5 x$ 等于 $8 x$，但 $3 x$ 和 $5 y$ 不能合并——苹果和橘子不是一类！
]

#tryit[
  拿起笔，先自己化简：$(5 a^2 b - 3 a b) - (2 a^2 b + a b - 4)$

  注意：减号后面有括号！写出每一步，然后在 blueprint 例 2 里对照。
]

#blueprint[
  *一、整式加减——合并同类项*

  合并同类项的步骤：
  + 找出同类项（字母和指数都相同的项）
  + 系数相加减，字母部分不变

  *例 1*：化简 $(3 x^2 - 2 x + 5) + (x^2 + 4 x - 3)$

  $ &= 3 x^2 - 2 x + 5 + x^2 + 4 x - 3 \
    &= (3 + 1) x^2 + (-2 + 4) x + (5 - 3) \
    &= 4 x^2 + 2 x + 2 $

  *例 2*：化简 $(5 a^2 b - 3 a b) - (2 a^2 b + a b - 4)$

  $ &= 5 a^2 b - 3 a b - 2 a^2 b - a b + 4 \
    &= (5 - 2) a^2 b + (-3 - 1) a b + 4 \
    &= 3 a^2 b - 4 a b + 4 $

  注意：去括号时，减号后面的括号内*每一项都变号*。

  *二、整式乘法*

  *单项式乘单项式*：系数相乘，同底数幂的指数相加。

  $ 3 x^2 dot (-2 x^3) = -6 x^5 $

  *单项式乘多项式*：分配律。

  $ 2 x(3 x^2 - 4 x + 1) = 6 x^3 - 8 x^2 + 2 x $

  *多项式乘多项式*：每一项都要乘。

  $ (a + b)(c + d) = a c + a d + b c + b d $

  *例 3*：展开 $(2 x + 3)(x - 5)$

  $ &= 2 x dot x + 2 x dot (-5) + 3 dot x + 3 dot (-5) \
    &= 2 x^2 - 10 x + 3 x - 15 \
    &= 2 x^2 - 7 x - 15 $

  *三、乘法公式*

  通过多项式乘法，我们发现两个特别重要的公式：

  *平方差公式*：

  $ (a + b)(a - b) = a^2 - b^2 $

  验证：$(a + b)(a - b) = a^2 - a b + a b - b^2 = a^2 - b^2$ ✓

  *例 4*：$101 times 99 = (100 + 1)(100 - 1) = 100^2 - 1^2 = 9999$

  *完全平方公式*：

  $ (a + b)^2 = a^2 + 2 a b + b^2 \
    (a - b)^2 = a^2 - 2 a b + b^2 $

  *例 5*：展开 $(3 x - 2)^2$

  $ (3 x - 2)^2 = (3 x)^2 - 2 dot 3 x dot 2 + 2^2 = 9 x^2 - 12 x + 4 $

  *四、整式除法*

  *单项式除以单项式*：系数相除，同底数幂的指数相减。

  $ 12 x^3 y^2 div 4 x y = 3 x^2 y $

  *多项式除以单项式*：每一项分别除。

  $ (6 x^3 - 9 x^2 + 3 x) div 3 x = 2 x^2 - 3 x + 1 $
]

#blueprint[
  *五、幂的运算法则*（$a != 0$，$m, n$ 为整数）

  #table(
    columns: (auto, 1fr, auto),
    align: (left, left, left),
    [*名称*], [*法则*], [*例子*],
    [同底数幂相乘], [$a^m times a^n = a^(m+n)$], [$x^3 times x^5 = x^8$],
    [幂的乘方], [$(a^m)^n = a^(m n)$], [$(x^2)^3 = x^6$],
    [积的乘方], [$(a b)^n = a^n b^n$], [$(2x)^3 = 8x^3$],
    [同底数幂相除], [$a^m div a^n = a^(m-n)$], [$x^7 div x^3 = x^4$],
    [零次幂], [$a^0 = 1$], [$5^0 = 1$],
    [负整数指数], [$a^(-n) = frac(1, a^n)$], [$2^(-3) = frac(1, 8)$],
  )

  *例 6*：化简 $x^4 times x^3 div x^2$。

  *解*：$x^4 times x^3 div x^2 = x^(4+3) div x^2 = x^7 div x^2 = x^(7-2) = x^5$。

  *例 7*：计算 $(2a^2)^3$。

  *解*：$(2a^2)^3 = 2^3 times (a^2)^3 = 8a^6$。

  *六、科学记数法*

  一个非零数的科学记数法形式为 $a times 10^n$，其中 $1 <= |a| < 10$，$n$ 为整数。

  - $3 800 000 = 3.8 times 10^6$
  - $0.000042 = 4.2 times 10^(-5)$

  *七、指数与根式的桥接*

  对非负数，$a^(1/2)$ 可以理解为 $sqrt(a)$，因为
  $ (a^(1/2))^2 = a^(1/2 times 2) = a $

  所以指数和根式并不是两套完全无关的语言，而是同一件事的两种写法。
]

#pitfall[
  *两个高频错误，每节课都有人中招：*

  *① 减号后的括号，里面每一项都要变号，不只是第一项！*

  $(5 a^2 b - 3 a b) - (2 a^2 b + a b - 4)$

  ❌ 错误：$= 5 a^2 b - 3 a b - 2 a^2 b + a b - 4$（漏掉了 $a b$ 的变号）

  实际上 $a b$ 是正的，变号后应为 $-a b$：

  ✓ 正确：$= 5 a^2 b - 3 a b - 2 a^2 b - a b + 4$

  *② $a^2 b$ 和 $a b^2$ 不是同类项，别合并！*

  虽然都含有 $a$ 和 $b$，但次数分布不同：$a^2 b$ 是 $a$ 的 2 次、$b$ 的 1 次；$a b^2$ 是 $a$ 的 1 次、$b$ 的 2 次。就像苹果饼干和苹果汁，都含苹果，但不是同一种东西。
]

#pitfall[
  *幂的运算再补两刀，专砍常见误用：*

  *① 底数不同，指数不能乱加。*

  ❌ $a^3 times b^4 = (a b)^7$

  ✓ 只有同底数幂相乘，才能把指数相加：$a^m times a^n = a^(m+n)$。

  *② 幂的乘方是指数相乘，不是相加。*

  ❌ $(a^3)^4 = a^7$

  ✓ $(a^3)^4 = a^(3 times 4) = a^12$
]

#side-hack[
  口诀记忆：

  - 同底相乘，指数相加
  - 幂再乘方，指数相乘
  - 积的乘方，各自乘方

  看到 $a^m times a^n$ 先看“底数是不是同一个”；看到 $(a^m)^n$ 再看“是不是幂套幂”。
]

#mastery[
  *基础*

  + 指出下列各式的系数和次数：$-3 x^2 y$、$frac(2, 3) a^3 b^2$、$-x$
  + 合并同类项：
    - $5 a - 3 b + 2 a + 7 b$
    - $x^2 - 3 x y + 2 x^2 + x y - 4$
  + 计算：
    - $(-2 x)^3$
    - $3 a^2 b dot (-4 a b^3)$
    - $-2 x(x^2 - 3 x + 1)$

  *应用*

  + 用乘法公式计算：
    - $(x + 5)(x - 5)$
    - $(2 a + 3 b)^2$
    - $998 times 1002$（提示：写成 $(1000 - 2)(1000 + 2)$）
  + 展开并化简：$(x + 1)(x - 3) - (x - 2)^2$

  *挑战*

  + 若 $x + y = 5$，$x y = 6$，求 $x^2 + y^2$ 和 $(x - y)^2$ 的值。
  + 已知 $(a + b)^2 - (a - b)^2 = k a b$，求常数 $k$ 的值。
  + 化简：$frac((2a^2 b)^3, 4a^3 b^2)$（$a != 0$，$b != 0$）。
  + 若 $2^x = 8$，$3^y = 27$，计算 $frac(2^(x+1) times 3^(y-1), 2^x times 3^y)$。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + $-3 x^2 y$：系数 $-3$，次数 $3$；$frac(2, 3) a^3 b^2$：系数 $frac(2, 3)$，次数 $5$；$-x$：系数 $-1$，次数 $1$
  + $5 a - 3 b + 2 a + 7 b = 7 a + 4 b$；$x^2 - 3 x y + 2 x^2 + x y - 4 = 3 x^2 - 2 x y - 4$
  + $(-2 x)^3 = -8 x^3$；$3 a^2 b dot (-4 a b^3) = -12 a^3 b^4$；$-2 x(x^2 - 3 x + 1) = -2 x^3 + 6 x^2 - 2 x$

  *应用*
  + $(x + 5)(x - 5) = x^2 - 25$；$(2 a + 3 b)^2 = 4 a^2 + 12 a b + 9 b^2$；$998 times 1002 = (1000 - 2)(1000 + 2) = 1000^2 - 4 = 999996$
  + $(x + 1)(x - 3) - (x - 2)^2 = (x^2 - 2 x - 3) - (x^2 - 4 x + 4) = 2 x - 7$

  *挑战*
  + $x^2 + y^2 = (x + y)^2 - 2 x y = 25 - 12 = 13$；$(x - y)^2 = (x + y)^2 - 4 x y = 25 - 24 = 1$
  + 左边 $= (a^2 + 2 a b + b^2) - (a^2 - 2 a b + b^2) = 4 a b$。所以 $k = 4$。
  + $frac((2a^2 b)^3, 4a^3 b^2) = frac(8a^6 b^3, 4a^3 b^2) = 2a^3 b$。
  + 因为 $2^x = 8 = 2^3$，所以 $x = 3$；$3^y = 27 = 3^3$，所以 $y = 3$。原式 $= frac(2^4, 2^3) times frac(3^2, 3^3) = 2 times frac(1, 3) = frac(2, 3)$。
]
