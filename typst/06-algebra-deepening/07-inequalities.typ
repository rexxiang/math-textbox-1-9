#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §6.7 不等式 <sec-6-7>

#vocab[不等式 inequality / 解集 solution set / 不等式组 system of inequalities]

#crisis[
  你手头最多有 100 元。你打算买 3 本价格相同的书，买完之后还需要至少留 10 元坐车回家。

  每本书最多能花多少钱？

  设每本书 $x$ 元。「买完后至少剩 10 元」意味着：

  $ 100 - 3 x >= 10 $

  这不是方程——左边不一定*等于*右边，而是*大于等于*。我们需要新的工具来处理这类「不等」关系。
]

#discovery[
  *什么是不等式？*

  用 $<$、$>$、$<=$、$>=$ 连接的式子叫做*不等式*。

  - $3 x + 5 > 0$
  - $100 - 3 x >= 10$
  - $-2 x <= 6$

  *不等式的核心性质*：

  先从等式类比。等式两边加减同一个数，等式成立；乘除同一个正数，也成立。不等式呢？

  *性质 1*：两边加（或减）同一个数，不等号方向*不变*。
  $ "若" a > b, "则" a + c > b + c $

  *性质 2*：两边乘（或除以）一个*正数*，不等号方向*不变*。
  $ "若" a > b, c > 0, "则" a c > b c $

  *性质 3*（关键！）：两边乘（或除以）一个*负数*，不等号方向*反转*。
  $ "若" a > b, c < 0, "则" a c < b c $

  直觉：$3 > 2$，两边乘 $-1$ → $-3 < -2$。在数轴上，正数的大小关系，乘以负数后翻转了！
]

#side-hack[
  *乘负变号*口诀——「负号一来，箭头反转」。

  这是不等式中*最容易犯的错误*。每次乘以或除以负数，都停下来问自己：「我变号了吗？」
]

#tryit[
  不翻书，自己试一试：解不等式 $-2 x + 1 >= 5$。

  提示：移项之后，你需要除以一个负数——记得处理不等号方向！

  写出每一步，再往下看 blueprint 里的例子对照。
]

#blueprint[
  *一、一元一次不等式*

  含一个未知数，最高次数为 1 的不等式。一般形式 $a x + b > 0$（$a != 0$），其中 $>$ 也可以是 $<$、$>=$、$<=$。

  解法与一元一次方程几乎相同，唯一的区别：*乘除负数时变号*。

  *例 1*：解不等式 $100 - 3 x >= 10$（回到开头的问题）

  $ -3 x >= 10 - 100 $
  $ -3 x >= -90 $

  两边除以 $-3$（*负数！变号！*）：

  $ x <= 30 $

  答：每本书最多花 30 元。

  *例 2*：解不等式 $2 x - 3 > 5 x + 6$

  移项：$2 x - 5 x > 6 + 3$

  $-3 x > 9$

  除以 $-3$（变号！）：$x < -3$

  *例 3*：解不等式 $frac(2 x - 1, 3) <= frac(x + 1, 2)$

  两边乘 6（正数，不变号）：

  $ 2(2 x - 1) <= 3(x + 1) $
  $ 4 x - 2 <= 3 x + 3 $
  $ x <= 5 $

  *二、解集与数轴表示*

  不等式的解通常不是一个数，而是*一个范围*，叫做*解集*。

  - $x > 3$：所有大于 3 的数。数轴上 3 的位置画*空心圆*（不包含 3），向右画箭头。
  - $x >= 3$：所有大于等于 3 的数。数轴上 3 的位置画*实心圆*（包含 3），向右画箭头。
  - $x < -2$：数轴上 $-2$ 处空心圆，向左画箭头。

  *三、一元一次不等式组*

  两个或多个一元一次不等式组成的叫*不等式组*。解集是各个不等式解集的*公共部分*（交集）。

  口诀：*同大取大，同小取小，大小小大取中间，大大小小无解。*

  详细说明：
  - $x > a$ 且 $x > b$（$a < b$）→ $x > b$（取大的）
  - $x < a$ 且 $x < b$（$a < b$）→ $x < a$（取小的）
  - $x > a$ 且 $x < b$（$a < b$）→ $a < x < b$（取中间）
  - $x > b$ 且 $x < a$（$a < b$）→ 无解（大大小小）

  *例 4*：解不等式组 $cases(2 x - 1 > 3, x + 4 <= 10)$

  解第一个：$2 x > 4$，$x > 2$

  解第二个：$x <= 6$

  公共部分：$2 < x <= 6$

  *例 5*：解不等式组 $cases(3 x - 2 >= 4, 5 x + 1 > 16)$

  解第一个：$x >= 2$

  解第二个：$x > 3$

  同大取大：$x > 3$

  *例 6*：解不等式组 $cases(x + 3 > 5, x - 1 < 0)$

  解第一个：$x > 2$

  解第二个：$x < 1$

  $x > 2$ 且 $x < 1$——没有交集！*无解*。

  *四、应用*

  *例 7*：某工厂生产一种产品，每件成本 20 元。定价至少要保证不低于成本的 15% 利润，但根据市场调查，定价超过 30 元就卖不出去。定价 $x$ 的范围是？

  不低于 15% 利润：$x >= 20 times 1.15 = 23$

  不超过 30 元：$x <= 30$

  $ 23 <= x <= 30 $

  *例 8*：一根绳子长 80 cm，要围成一个长方形，使长比宽多至少 10 cm，但长不超过宽的 3 倍。宽的范围是？

  设宽 $x$ cm，长 $(40 - x)$ cm（因为半周长 40）。

  长比宽多至少 10：$(40 - x) - x >= 10$，即 $40 - 2 x >= 10$，$x <= 15$

  长不超过宽的 3 倍：$40 - x <= 3 x$，即 $40 <= 4 x$，$x >= 10$

  还有基本限制：$x > 0$ 且 $40 - x > 0$，即 $0 < x < 40$

  综合：$10 <= x <= 15$

  答：宽在 $10$ cm 到 $15$ cm 之间（含端点）。
]

#pitfall[
  *乘除负数，不等号必须反转！*

  #grid(columns: (1fr, 1fr), gutter: 8pt,
    block[
      ❌ *错误*

      $-3 x >= -90$

      $x >= 30$（忘记变号）
    ],
    block[
      ✓ *正确*

      $-3 x >= -90$

      除以 $-3$（负数！）：

      $x <= 30$（不等号反转）
    ]
  )

  *等式和不等式的关键区别*：等式两边乘除任何非零数，等号不变；不等式两边乘除*负数*时，不等号*必须反转*。正数则不变。
]

#mastery[
  *基础*

  + 解不等式，并在数轴上表示解集：
    - $3 x - 5 > 7$
    - $-2 x + 1 >= 5$
    - $frac(x - 2, 3) > frac(x + 1, 2)$
  + 解不等式组：
    - $cases(x + 2 > 5, 2 x - 3 < 7)$
    - $cases(3 x - 1 >= 8, 4 x + 3 <= 19)$
    - $cases(2 x > -4, 3 x < -9)$

  *应用*

  + 小王考了 4 次数学测验，成绩分别为 85、90、78、92。第 5 次至少考多少分，才能使 5 次的平均分不低于 85 分？
  + 某次活动需要租车，大车可坐 40 人，租金 400 元；小车可坐 25 人，租金 280 元。共有 200 人，至少租 3 辆大车。要使总租金不超过 2800 元，大车最多租几辆？
  + 求同时满足以下条件的所有整数 $x$：$frac(x + 1, 2) >= frac(x - 1, 3)$ 且 $3 x - 2 < 10$

  *挑战*

  + 若不等式 $a x > b$ 的解集为 $x < frac(b, a)$，则 $a$ 满足什么条件？
  + 若关于 $x$ 的不等式组 $cases(x > a, x < b)$ 有解，证明 $a < b$。
  + 已知不等式组 $cases(2 x - 1 >= 3, x < a)$ 恰好有 3 个整数解，求 $a$ 的取值范围。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + $3 x > 12$，$x > 4$；$-2 x >= 4$，$x <= -2$；两边乘 6：$2(x - 2) > 3(x + 1)$，$2 x - 4 > 3 x + 3$，$-x > 7$，$x < -7$
  + $x > 3$ 且 $x < 5$，所以 $3 < x < 5$；$x >= 3$ 且 $x <= 4$，所以 $3 <= x <= 4$；$x > -2$ 且 $x < -3$，无解。

  *应用*
  + 设第 5 次 $x$ 分。$frac(85 + 90 + 78 + 92 + x, 5) >= 85$，$345 + x >= 425$，$x >= 80$。至少 80 分。
  + 设租大车 $n$ 辆（$n >= 3$），则需要小车运 $(200 - 40 n)$ 人，需小车 $ceil(frac(200 - 40 n, 25))$ 辆。简化：设小车 $m$ 辆，$40 n + 25 m >= 200$，$400 n + 280 m <= 2800$。当 $n = 3$：需 $25 m >= 80$，$m >= 4$（取 4）；$400 times 3 + 280 times 4 = 1200 + 1120 = 2320 <= 2800$ ✓。当 $n = 4$：$25 m >= 40$，$m >= 2$；$1600 + 560 = 2160 <= 2800$ ✓。当 $n = 5$：$m >= 0$；$2000 <= 2800$ ✓。当 $n = 6$：$40 times 6 = 240 >= 200$，$m = 0$；$2400 <= 2800$ ✓。当 $n = 7$：$2800 <= 2800$ ✓。当 $n = 8$：$3200 > 2800$ ✗。最多 7 辆。
  + 第一个：$frac(x + 1, 2) >= frac(x - 1, 3)$，$3(x + 1) >= 2(x - 1)$，$3 x + 3 >= 2 x - 2$，$x >= -5$。第二个：$3 x < 12$，$x < 4$。所以 $-5 <= x < 4$，整数为 $-5, -4, -3, -2, -1, 0, 1, 2, 3$。

  *挑战*
  + 不等式 $a x > b$ 除以 $a$ 后不等号反转，说明 $a < 0$。
  + 若 $cases(x > a, x < b)$ 有解，则存在某个 $x$ 使得 $a < x < b$，所以必有 $a < b$。
  + 第一个不等式 $2 x >= 4$，$x >= 2$。与 $x < a$ 合并得 $2 <= x < a$。恰好 3 个整数解即 $x in {2, 3, 4}$。需要 $x = 4$ 满足 $x < a$（即 $a > 4$），且 $x = 5$ 不满足 $x < a$（即 $a <= 5$）。所以 $4 < a <= 5$。
]

=== 拓展：算术—几何均值不等式（AM-GM）

*高中预告*：AM-GM 不等式和二次不等式是高中数学的正式内容。这里作为拓展，帮助有余力的同学提前感受不等式的威力。

*定理*：若 $a >= 0, b >= 0$，则
$ frac(a + b, 2) >= sqrt(a b) $
等号成立当且仅当 $a = b$。

用中文说：*算术平均数 $>=$ 几何平均数*。

*证明*：$(sqrt(a) - sqrt(b))^2 >= 0$，展开得 $a - 2 sqrt(a b) + b >= 0$，即 $a + b >= 2 sqrt(a b)$。

*应用——最值问题*

*例*：长方形周长为 20 cm，面积最大是多少？

设长 $a$、宽 $b$，则 $a + b = 10$。由 AM-GM：$frac(a + b, 2) >= sqrt(a b)$，即 $5 >= sqrt(a b)$，$a b <= 25$。等号当 $a = b = 5$（正方形）时成立。最大面积 = 25 cm²。

*逆向应用*：面积固定时，周长最小的长方形也是正方形。
