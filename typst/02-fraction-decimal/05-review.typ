#import "../lib/theme-v2.typ": blueprint, mastery, secref

== 第二章回顾：分与合

#blueprint[
  *知识速查卡*

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *因数、倍数与质数*（#secref("2.1")）
      - $a$ 是 $b$ 的因数 ↔ $b$ 是 $a$ 的倍数
      - 质数：只有 1 和自身两个因数（1 既不是质数也不是合数）
      - 质因数分解：$12 = 2^2 times 3$
      - GCD（最大公因数）：取各质因数最小次幂的积
      - LCM（最小公倍数）：取各质因数最大次幂的积
    ],
    block[
      *分数运算*（#secref("2.2")）
      - 加减：先通分（找 LCM），再加减分子
      - 乘法：分子×分子，分母×分母
      - 除法：乘以倒数
      - 基本性质：$frac(a, b) = frac(a times k, b times k)$（$k != 0$）
      - 约分：分子分母同除最大公因数
    ],
  )

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *小数运算*（#secref("2.3")）
      - 加减：小数点对齐
      - 乘法：先按整数乘，再定小数点（位数=两因数小数位数之和）
      - 除法：移动小数点，化为整数除
    ],
    block[
      *分数与小数互化*（#secref("2.4")）
      - 分数→小数：分子÷分母
      - 小数→分数：按位值写分母（一位小数分母10，两位分母100……），再约分
      - 循环小数：$0.overline(3) = frac(1, 3)$，$0.overline(142857) = frac(1, 7)$
    ],
  )
]

#mastery[
  *混合自测题*

  + 求 48 的所有因数，并写出 48 的质因数分解。

  + 求 gcd(36, 84) 和 lcm(36, 84)。

  + 计算：$frac(3, 8) + frac(5, 12)$

  + 计算：$2frac(1, 3) - 1frac(3, 4)$（带分数）

  + 计算：$frac(5, 6) times frac(9, 10) div frac(3, 4)$

  + 把下列小数化成分数（最简形式）：$0.6$；$0.125$；$0.overline(3)$

  + 把下列分数化成小数（或循环小数）：$frac(3, 8)$；$frac(5, 6)$

  + 比较大小：$frac(7, 9)$ 和 $frac(5, 6)$（提示：通分）

  + 一桶油重 $4frac(1}{2}$ 千克，分成若干小瓶，每瓶装 $frac(3}{8}$ 千克。能装多少瓶？

  + 甲工厂生产同种零件，第一天完成 $frac{3}{8}$，第二天完成 $frac{1}{4}$，剩余多少？若总任务是 480 个，第三天还需生产多少个？

  + *进阶*：一个分数，分子比分母小 3，分子和分母的最大公因数为 1，约分后等于 $frac{5}{8}$。求原来的分数。（提示：分子 $5k$，分母 $8k$，差 $3k = 3$）

  + *进阶*：$frac{1}{2} + frac{1}{6} + frac{1}{12} + frac{1}{20} + frac{1}{30} = ?$（提示：观察分母规律——相邻整数之积）

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  + 48 的因数：1,2,3,4,6,8,12,16,24,48；$48 = 2^4 times 3$
  + gcd(36,84)：$36 = 2^2 times 3^2$，$84 = 2^2 times 3 times 7$，gcd = $2^2 times 3 = 12$；lcm = $2^2 times 3^2 times 7 = 252$
  + 公分母 24：$frac{9}{24} + frac{10}{24} = frac{19}{24}$
  + $2frac{1}{3} - 1frac{3}{4} = frac{7}{3} - frac{7}{4} = frac{28}{12} - frac{21}{12} = frac{7}{12}$
  + $frac{5}{6} times frac{9}{10} times frac{4}{3} = frac{5 times 9 times 4}{6 times 10 times 3} = frac{180}{180} = 1$
  + $0.6 = frac{3}{5}$；$0.125 = frac{1}{8}$；$0.overline{3} = frac{1}{3}$
  + $frac{3}{8} = 0.375$；$frac{5}{6} = 0.8overline{3}$
  + 通分：$frac{14}{18}$ vs $frac{15}{18}$，$frac{7}{9} < frac{5}{6}$
  + $4frac{1}{2} div frac{3}{8} = frac{9}{2} times frac{8}{3} = 12$ 瓶
  + 完成 $frac{3}{8} + frac{1}{4} = frac{5}{8}$，剩余 $frac{3}{8}$；第三天 $480 times frac{3}{8} = 180$ 个
  + $5k$，$8k$，差 $3k = 3$，$k = 1$；原分数就是 $frac{5}{8}$（题意表示已是最简，所以原本就是 $frac{5}{8}$）
  + $frac{1}{2} = 1 - frac{1}{2}$，$frac{1}{6} = frac{1}{2} - frac{1}{3}$，$frac{1}{12} = frac{1}{3} - frac{1}{4}$，$frac{1}{20} = frac{1}{4} - frac{1}{5}$，$frac{1}{30} = frac{1}{5} - frac{1}{6}$，总和 $= 1 - frac{1}{6} = frac{5}{6}$

  *螺旋复习*

  + 本章学习的通分是 #secref("5.3") 分式方程的基础——试着通分：$frac{1}{x} + frac{1}{x+1}$（把字母当未知数，与 §2.2 同样的步骤）

  + 第 3 章即将学百分数。百分数是一种特殊的分数，$p% = frac{p}{100}$。60% 等于哪个最简分数？（答：$frac{3}{5}$）
]
