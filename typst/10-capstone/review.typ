#import "../lib/theme-v2.typ": blueprint, mastery, secref

== 全书回看：把整棵树收成可再出发的工具包 <sec-10-review>

#blueprint[
  *知识速查卡*

  #table(
    columns: 2,
    inset: 6pt,
    stroke: 0.4pt,
    table.header([*回看层*], [*现在应该能顺手调出的旧工具*]),
    [共同地基 1--4], [数与运算、分数与比例、图形与坐标、数轴与字母已经能互相翻译。],
    [总闸门 5], [会先检查旧工具是否够用，再决定走代数、几何、函数还是数据路线。],
    [专题深化 6--9], [能把同一问题换成“式子 / 图形 / 图象 / 数据”的不同说法，再挑合适工具。],
    [终章 10], [知道数学不是背单线流程，而是把旧发明重新连线、继续生长。],
  )
]

#mastery[
  *混合自测题*

  + 从 #secref("1.6") 到 #secref("6.6") 看，为什么“先整理、再守平衡”会成为解方程前很稳的起手式？
  + 拿”单价、数量、总价”这类量来说，在 #secref(“2.4”)、#secref(“4.3”) 和 #secref(“8.1”) 里，它分别会被说成什么？
  + 在 #secref("7.6") 的圆题里，一看到弦和圆心，为什么要先连半径？
  + 看到一个情境“先升后降，而且有最高点”时，按 #secref("8.4") 的方法，你第一眼会先读哪三类信息？
  + 把 #secref("6.10") 的不等式题和 #secref("9.8") 的样本估计放在一起看：前者要盯什么边界，后者又要提醒自己什么限制？
  + 用一句话把 #secref("3.8") 和 #secref("4.1") 连起来，说明坐标平面是怎样一步步补完整的。
  + 题目里一出现“直角、两边已知、求第三边”，为什么很多同学会立刻想到 #secref("7.8")？
  + 看到“这个数据结论是根据样本推出来的”时，先用 #secref("9.8") 的哪两点提醒自己？
  + 同样带有二次关系的情境，什么时候更该像 #secref("6.9") 那样先解方程，什么时候更该像 #secref("8.4") 那样先看图象和顶点？
  + 如果一道综合题里同时出现式子、图形、图象和数据，你会先按什么标准决定从哪一种表示切入？
]

=== 参考答案

+ 因为方程真正动手前，先要把数量关系整理清楚；等关系写稳以后，再用“左右一样多”的平衡观一步步解下去，才不容易乱。
+ #secref("2.4") 先把它看成比或倍数关系，#secref("4.3") 把关系写成含字母的式子，#secref("8.1") 再把它读成随另一个量变化的线性模型。
+ 因为半径相等会自动造出等腰三角形，进一步就能调用全等、角关系、弦被平分这些旧工具。
+ 开口方向、顶点位置、与 $x$ 轴的交点（也就是函数值回到 $0$ 的位置）。
+ 不等式要盯“哪些值允许、边界落在哪里”；样本估计则要提醒自己：这只是对总体的估计，还得看抽样是否随机、样本是否有代表性。
+ #secref(“3.8”) 先教”先横后纵”的定位入口，#secref(“4.1”) 再把正负方向补进来，于是坐标才能向左、向下完整展开。
+ 因为直角三角形一旦成形，勾股定理就能把未知长度和已知长度连成同一个平方关系。
+ 样本结论只能作为对总体的估计；另外还要先检查抽样是不是随机、样本有没有明显偏向某一部分。
+ 需要把未知量求出来时，更像 #secref("6.9") 的解方程任务；需要读整体走势、顶点和最值时，更像 #secref("8.4") 的函数图象任务。
+ 先看题目最核心的是哪一种关系最清楚：能列关系就先写式子，靠位置与性质就先画图，盯整体变化就先读图象，要比较样本与分布就先整理数据。

=== 螺旋复习

- 结合 #secref("4.1") 与 #secref("8.1")，请用一个情境说明“方向”怎样一步步长成“变化趋势”。
- 结合 #secref(“3.7”)、#secref(“7.1”) 与 #secref(“7.6”)，请写出一条”先补线，再推出结论”的两步推理链。
- 结合 #secref(“2.4”)、#secref(“4.3”) 与 #secref(“8.1”)，请写出一条”先抓数量关系，再写式子，最后读变化”的三步链。
