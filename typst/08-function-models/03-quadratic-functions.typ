#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §8.3 二次函数：弯曲变化与最值模型 <sec-8-3>

#vocab[二次函数 quadratic function / 抛物线 parabola / 顶点 vertex / 对称轴 axis of symmetry / 配方法 completing the square]

#crisis[
  喷泉水柱从喷口射出后，先升高，再落下。某次测量得到水柱高度 $y$（米）与水平距离 $x$（米）的关系大致是：

  $ y = -0.5 x^2 + 2 x $

  这时最关心的问题不再是“代一个 $x$ 算一个 $y$”，而是：

  - 最高点在哪里？
  - 最高能到多高？
  - 水柱什么时候落回地面？

  一次函数只能描述稳定增减，显然不够。我们需要一种能表达“变化本身也在改变”的模型。
]

#history-note[
  伽利略研究抛体运动时发现：忽略空气阻力后，很多飞出去再落回来的轨迹都像同一种曲线。后来这种曲线被叫作*抛物线*。二次函数正是描述这种“先快后慢、先升后降、先减后增”现象的基础模型。
]

#discovery[
  先把喷泉公式代入几个简单的 $x$：

  #align(center, table(
    columns: 6,
    align: center,
    [$x$], [$0$], [$1$], [$2$], [$3$], [$4$],
    [$y$], [$0$], [$1.5$], [$2$], [$1.5$], [$0$],
  ))

  这里最显眼的现象是：

  - $x = 1$ 和 $x = 3$ 时，$y$ 一样高
  - $x = 0$ 和 $x = 4$ 时，都回到地面
  - 中间的 $x = 2$ 给出了最高点

  这提示我们：图象不是直线，而是关于某条竖线对称的弯曲图形。

  再把公式改写一下：

  $ y
    &= -0.5(x^2 - 4 x) \
    &= -0.5(x^2 - 4 x + 4 - 4) \
    &= -0.5(x - 2)^2 + 2 $

  由于任意平方都满足 $(x - 2)^2 >= 0$，所以

  $-0.5(x - 2)^2 <= 0$

  故整个函数在 $x = 2$ 时取得最大值 $2$。这说明：*把二次函数改写成“平方 + 常数”的形式后，最值位置会直接露出来。*
]

#tryit[
  先自己练一遍：

  把 $y = x^2 - 6 x + 10$ 化成顶点式，并写出：

  + 顶点坐标
  + 对称轴
  + 最小值

  提示：先盯住 $x^2 - 6 x$，想想要补上什么数，才能变成完整平方。
]

#side-hack[
  看二次函数时，优先找三件事：

  - *开口朝上还是朝下*——由 $a$ 的正负决定
  - *顶点在哪里*——最值往往就在这里
  - *与 $x$ 轴交不交、交几次*——这决定情境里“归零”或“落地”发生没有
]

#blueprint[
  *一、什么是二次函数？*

  形如

  $ y = a x^2 + b x + c quad (a != 0) $

  的函数叫*二次函数*。它的图象是一条抛物线。

  和一次函数相比，二次函数描述的是“变化率还在变”的情境：每前进一步，增加量本身也会改变。

  *二、顶点式为什么重要？*

  把二次函数写成

  $ y = a (x - h)^2 + k $

  叫作*顶点式*。它会直接告诉我们：

  - 顶点是 $(h, k)$
  - 对称轴是 $x = h$
  - 若 $a > 0$，顶点给出最小值
  - 若 $a < 0$，顶点给出最大值

  *三、从标准式到顶点式：配方法*

  对一般式 $y = a x^2 + b x + c$，可以通过配方改写。

  例如

  $ y = x^2 - 6 x + 10 $

  配方得

  $ y
    &= x^2 - 6 x + 9 + 1 \
    &= (x - 3)^2 + 1 $

  所以顶点是 $(3, 1)$，对称轴是 $x = 3$，最小值是 1。

  *四、图象怎样读？*

  二次函数图象有三条常见信息：

  - *开口方向*：$a > 0$ 向上，$a < 0$ 向下
  - *顶点位置*：决定最高点或最低点
  - *与 $x$ 轴的交点*：令 $y = 0$ 后得到的方程解

  所以“求函数最值”和“解一元二次方程”在这里会重新会合。

  *五、例题：从情境回到函数*

  例 1：喷泉模型

  $ y = -0.5 x^2 + 2 x = -0.5(x - 2)^2 + 2 $

  所以顶点为 $(2, 2)$，最高点在距喷口 2 米处，高度为 2 米。

  再令 $y = 0$：

  $-0.5 x^2 + 2 x = 0$

  $-0.5 x (x - 4) = 0$

  得 $x = 0$ 或 $x = 4$，说明水柱在 4 米处落地。

  例 2：周长固定为 20 的长方形，设长为 $x$，面积为 $y$。

  宽是 $10 - x$，所以

  $ y = x (10 - x) = -x^2 + 10 x $

  配方得

  $ y = -(x - 5)^2 + 25 $

  所以面积最大值为 25，在 $x = 5$ 时取得。也就是说：周长固定时，正方形面积最大。
]

#pitfall[
  *陷阱 1：配方前忘了先把二次项系数提出来。*

  例如 $2 x^2 - 8 x$，不能直接补成 $(x - 4)^2$；要先写成 $2(x^2 - 4 x)$，再去补平方。

  *陷阱 2：把顶点横坐标的符号看反。*

  看到 $(x + 1)^2$，顶点横坐标不是 $1$，而是 $-1$。因为

  $ (x + 1)^2 = (x - (-1))^2 $
]

#mastery[
  *基础*

  + 判断下列函数是否为二次函数：
    - $y = 2 x^2 - 3 x + 1$
    - $y = 4 x + 7$
    - $y = -x^2$
    - $y = frac(6, x)$

  + 将 $y = x^2 - 4 x + 7$ 化成顶点式，写出顶点、对称轴、最小值。

  + 将 $y = -2 x^2 + 8 x - 3$ 化成顶点式，写出顶点和最大值。

  *应用*

  + 某球被竖直抛起，高度模型为 $h = -5 t^2 + 20 t + 1$。求球的最大高度及对应时间。

  + 某矩形花坛周长固定为 32 米。设一边长为 $x$，面积为 $y$。写出函数关系式，并求最大面积。

  *挑战*

  + 已知抛物线 $y = x^2 - 2 x - 3$。求它与 $x$ 轴的交点，并说明这些交点在情境中通常表示什么。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*

  + 第一、第三个是二次函数；第二个不是；第四个不是。

  + $y = (x - 2)^2 + 3$。顶点 $(2, 3)$，对称轴 $x = 2$，最小值为 3。

  + $y = -2(x^2 - 4 x) - 3 = -2(x^2 - 4 x + 4 - 4) - 3 = -2(x - 2)^2 + 5$。顶点 $(2, 5)$，最大值为 5。

  *应用*

  + $h = -5(t^2 - 4 t) + 1 = -5(t - 2)^2 + 21$。最大高度 21，在 $t = 2$ 秒时取得。

  + 另一边长为 $16 - x$，所以 $y = x(16 - x) = -x^2 + 16 x = -(x - 8)^2 + 64$。最大面积为 64 平方米。

  *挑战*

  + 令 $y = 0$，得 $x^2 - 2 x - 3 = 0$，即 $(x - 3)(x + 1) = 0$，所以交点是 $(3, 0)$ 和 $(-1, 0)$。在情境里，它们常表示“高度归零”“利润归零”“面积为零”等关键时刻或位置。
]
