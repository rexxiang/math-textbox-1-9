#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, vocab, side-hack, secref, tryit, pitfall

== §8.2 反比例函数：固定乘积的变化模型 <sec-8-2>

#vocab[反比例函数 inverse proportional function / 固定乘积 fixed product]

#crisis[
  一块长方形菜地的面积固定为 $48$ 平方米。如果长是 $12$ 米，宽就是 $4$ 米；如果长改成 $8$ 米，宽就变成 $6$ 米。

  长再变宽，宽就得变窄。这里不是“每多 1 米就少多少”的固定增量，而是另一种联动：

  *只要面积不变，长和宽会怎样一起变化？*
]

#history-note[
  反比例关系最早常出现在路程、面积、效率等问题里：路程固定时，速度越快，时间越短；面积固定时，一条边越长，另一条边越短。后来人们发现，这些问题虽然场景不同，背后都由同一条规则支配：*乘积不变*。
]

#discovery[
  把面积固定为 $48$ 的长方形列成表：

  #align(center, table(
    columns: 6,
    align: center,
    [长 $x$（米）], [$2$], [$3$], [$4$], [$6$], [$12$],
    [宽 $y$（米）], [$24$], [$16$], [$12$], [$8$], [$4$],
  ))

  这张表最醒目的不是“差”，而是“积”：

  - $2 times 24 = 48$
  - $3 times 16 = 48$
  - $4 times 12 = 48$

  所以关系式是

  $ x y = 48 $

  把它改写成函数形式：

  $ y = frac(48, x) $

  这说明：当 $x$ 变成原来的 2 倍时，$y$ 会变成原来的一半；当 $x$ 变成原来的 3 倍时，$y$ 会变成原来的 $frac(1, 3)$。这正是 #secref("2.4") 里“反比”的函数版本：*不是商固定，而是积固定。*
]

#tryit[
  先自己想一想：

  某工程队修一条路，总工作量固定为 240 工时。若每天安排 $x$ 人，每人每天工作 1 工时，完工所需天数为 $y$。

  + 写出 $x$ 与 $y$ 的关系式
  + 当人数从 12 人增加到 24 人时，天数怎样变化？
  + 这里为什么不适合用一次函数？
]

#side-hack[
  看到“总量固定”“面积固定”“总工作量固定”“总路程固定”这类词时，先试着问：是不是有一个乘积始终不变？如果答案是“是”，优先考虑反比例函数。
]

#blueprint[
  *一、反比例函数的定义*

  形如

  $ y = frac(k, x) quad (k != 0) $

  的函数叫*反比例函数*。等价地，它也可以写成

  $ x y = k $

  这里的核心不是“加多少”，而是“乘起来总等于同一个常数”。

  *二、它描述什么样的变化？*

  反比例函数适合处理：

  - 总路程固定时，速度与时间
  - 总工作量固定时，人数与工期
  - 面积固定时，长与宽
  - 单价与数量在总价固定时的联动

  共同特征是：一个量变大，另一个量必须按相反方向调整，才能让乘积守住同一个值。

  *三、图象怎样读？*

  反比例函数的图象由两支分开的曲线组成。

  - $k > 0$ 时，图象在第一、三象限
  - $k < 0$ 时，图象在第二、四象限
  - 曲线不断靠近坐标轴，但不会真正碰到坐标轴

  这和一次函数很不一样：一次函数是直线，反比例函数是弯曲的，而且越靠近坐标轴变化越剧烈。

  *四、怎样求比例系数 $k$？*

  只要知道图象上一点 $(x_0, y_0)$，就能立刻求出

  $ k = x_0 y_0 $

  例如图象经过点 $(3, 8)$，则

  $ k = 3 times 8 = 24 $

  所以函数为

  $ y = frac(24, x) $

  *五、例题：从情境读出固定乘积*

  例 1：距离固定为 180 千米，速度为 $x$ 千米 / 时，时间为 $y$ 小时。

  $ x y = 180, quad y = frac(180, x) $

  当速度从 60 提高到 90 时，时间从 3 小时变成 2 小时。

  例 2：某矩形面积固定为 30，长为 $x$，宽为 $y$。

  $ y = frac(30, x) $

  当 $x = 5$ 时，$y = 6$；当 $x = 10$ 时，$y = 3$。长加倍，宽减半。

  *六、与正比例函数对比*

  - 正比例函数：$frac(y, x)$ 固定，图象是过原点的直线
  - 反比例函数：$x y$ 固定，图象是双曲线

  所以“都有比例味道”不等于“属于同一种模型”；关键在于你守住的是商还是积。
]

#pitfall[
  *陷阱 1：看到 $k > 0$ 就以为一定递增。*

  例如 $y = frac(48, x)$。在第一象限里，$x$ 越大，$y$ 反而越小。因为这里守的是乘积，不是固定增加量。

  *陷阱 2：把“减少了 2”误当成反比例。*

  如果 $y = 20 - 2x$，那是一次函数，因为每次都少 2；只有像 $y = frac(48, x)$ 这样“乘起来固定”的关系，才是反比例函数。
]

#mastery[
  *基础*

  + 判断下列关系是否为反比例函数；如果是，写出 $k$：
    - $y = frac(18, x)$
    - $x y = -12$
    - $y = 18 - x$
    - $y = frac(x + 2, x)$

  + 已知反比例函数图象经过点 $(4, 6)$，求函数解析式，并求 $x = 3$ 时的 $y$。

  + 某长方形面积固定为 72 平方厘米。设长为 $x$，宽为 $y$，写出函数关系式，并求长为 9 厘米时的宽。

  *应用*

  + 某批零件总数固定为 600 个。若每天生产 $x$ 个，需要 $y$ 天完成。写出函数关系式；若要求在 12 天内完成，每天至少生产多少个？

  + 一段路全长 240 千米。写出速度 $x$ 与时间 $y$ 的函数关系式，并比较 $x = 80$ 与 $x = 120$ 时的时间。

  *挑战*

  + 已知反比例函数 $y = frac(k, x)$ 的图象经过点 $(2, 5)$。再取图象上一点 $A(x, y)$，说明为什么以坐标轴为边围成的矩形面积总是 10。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*

  + $y = frac(18, x)$：是，$k = 18$。$x y = -12$：是，$k = -12$。$y = 18 - x$：不是。$y = frac(x + 2, x)$：不是，它等于 $1 + frac(2, x)$，不属于 $frac(k, x)$ 形式。

  + $k = 4 times 6 = 24$，所以 $y = frac(24, x)$。当 $x = 3$ 时，$y = 8$。

  + $y = frac(72, x)$。当 $x = 9$ 时，$y = 8$（厘米）。

  *应用*

  + $y = frac(600, x)$。若 12 天内完成，则 $frac(600, x) <= 12$，所以 $600 <= 12 x$，得 $x >= 50$，每天至少生产 50 个。

  + $y = frac(240, x)$。当 $x = 80$ 时，$y = 3$；当 $x = 120$ 时，$y = 2$。速度提高，时间缩短。

  *挑战*

  + 因为点 $(2, 5)$ 在图象上，所以 $k = 10$。任取图象上一点 $A(x, y)$，都有 $x y = 10$，所以围成的矩形面积始终是 10。
]
