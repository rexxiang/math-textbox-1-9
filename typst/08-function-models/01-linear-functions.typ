#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, vocab, side-hack, secref, tryit, pitfall
#import "../lib/diagram-packages.typ": cetz

== §8.1 一次函数：稳定变化的第一模型 <sec-8-1>

#vocab[函数 function / 正比例函数 direct proportional function / 一次函数 linear function]

#crisis[
  某共享自习室按使用时长收费：

  - 开门服务费 $6$ 元
  - 之后每小时 $8$ 元

  用 1 小时要付多少？3 小时呢？如果用 $x$ 小时，费用 $y$ 该怎样写？

  更重要的是：如果换成“只按每小时 8 元收费、没有服务费”，这种变化和上面的收费方式有什么本质不同？
]

#history-note[
  笛卡尔把点坐标和方程联系起来以后，人们第一次能系统地说：*一条直线对应一种最简单的稳定变化*。这也是函数思想最重要的一步——公式不只是在算，还在描述“每前进一步会发生什么”。
]

#discovery[
  先把共享自习室的费用列成表：

  #align(center, table(
    columns: 5,
    align: center,
    [时长 $x$（小时）], [$0$], [$1$], [$2$], [$3$],
    [费用 $y$（元）], [$6$], [$14$], [$22$], [$30$],
  ))

  观察每走一步发生了什么：

  - $x$ 每增加 $1$，$y$ 都增加 $8$
  - 增加量是固定的，不会忽大忽小
  - 当 $x = 0$ 时，$y = 6$

  现在再看“没有服务费”的版本：

  #align(center, table(
    columns: 5,
    align: center,
    [时长 $x$（小时）], [$0$], [$1$], [$2$], [$3$],
    [费用 $y$（元）], [$0$], [$8$], [$16$], [$24$],
  ))

  它和前一个收费方式都表示“每多 1 小时，多 8 元”，但第二个模型还多出一个特点：

  - 一开始就是 $0$ 元
  - $frac(y, x)$ 始终等于 $8$
  - 图象会经过原点

  也就是说，这两类变化都很稳定；只是一个有固定起点，一个从零开始。这正是从 #secref("2.4") 的比与百分数直觉走向函数模型时，最先要分清的两种现象。
]

#tryit[
  先自己做，再看蓝图：

  某配送平台收取“起步价 5 元，每公里 2 元”的配送费。设距离为 $x$ 千米，费用为 $y$ 元。

  + 写出 $y$ 与 $x$ 的关系式
  + 求 $x = 4$ 时的费用
  + 如果去掉起步价，哪一部分会变化，哪一部分不变？
]

#side-hack[
  判断直线模型时，先问两句：

  - *每前进一步，是不是总加同样多？*
  - *当 $x = 0$ 时，起点是不是正好在 $0$？*

  如果这两句都看清了，再去决定该用哪一种最简单的直线模型。
]

#blueprint[
  *一、什么叫函数？*

  在某个变化过程中，如果对 $x$ 的每一个值，$y$ 都有唯一确定的值与之对应，就说 $y$ 是 $x$ 的函数。

  常见的四种读法是：

  - *情境*：故事里谁跟谁一起变
  - *表格*：列出几个对应值
  - *公式*：写成解析式
  - *图象*：在坐标系中看整体走势

  *二、正比例函数：从原点出发的稳定变化*

  形如

  $ y = k x quad (k != 0) $

  的函数叫*正比例函数*。这里

  $ frac(y, x) = k $

  始终不变，所以它适合描述“单位价格固定”“单位效率固定”“缩放比例固定”这类情境。

  图象特点：

  - 是一条直线
  - 过原点 $(0, 0)$
  - $k > 0$ 时从左下到右上，$k < 0$ 时从左上到右下

  *三、一次函数：稳定增量 + 固定起点*

  形如

  $ y = k x + b quad (k != 0) $

  的函数叫*一次函数*。

  - $k$ 表示“每增加 1 个单位，$y$ 增减多少”
  - $b$ 表示“当 $x = 0$ 时，$y$ 的起点在哪里”

  所以一次函数最适合描述：

  - 固定单价 + 固定服务费
  - 水箱每分钟稳定进 / 出水
  - 距离随时间匀速变化

  当 $b = 0$ 时，一次函数就退化成正比例函数。

  如果表格中 $x$ 每增加相同的量，$y$ 也总增加（或减少）相同的量，那么它通常对应一次函数。

  图象上，一次函数是一条直线。最常用的画法是先找两个点：

  - 令 $x = 0$，得到 $(0, b)$
  - 再任选一个方便的 $x$ 求对应 $y$

  #figure(
    cetz.canvas(length: 0.85cm, {
      import cetz.draw: *
      // axes
      line((-3.5, 0), (4.5, 0), stroke: 0.7pt, mark: (end: ">", size: 0.15))
      line((0, -2.5), (0, 4.5), stroke: 0.7pt, mark: (end: ">", size: 0.15))
      content((4.5, 0), $x$, anchor: "west", padding: 3pt)
      content((0, 4.5), $y$, anchor: "south", padding: 3pt)
      content((0, 0), $O$, anchor: "north-east", padding: 3pt)
      // y = 2x - 2 (k=2, b=-2): passes (0,-2) and (3,4)
      line((-1, -4), (3, 4), stroke: (paint: blue, thickness: 0.8pt))
      circle((0, -2), radius: 0.07, fill: blue, stroke: none)
      content((0, -2), text(8pt)[$b{=}{-}2$], anchor: "west", padding: 3pt)
      // y = 2x (k=2, b=0): passes (0,0) and (2,4)
      line((-2, -4), (2, 4), stroke: (paint: eastern, thickness: 0.8pt))
      // labels
      content((3.2, 3.5), text(8pt, fill: blue)[$y{=}2x{-}2$], anchor: "west")
      content((2.2, 3.5), text(8pt, fill: eastern)[$y{=}2x$], anchor: "east")
      // tick at x=1 to show slope
      content((1.5, -0.35), text(7pt)[$1$])
      content((-0.35, 1.5), text(7pt)[$k{=}2$])
    }),
    caption: [一次函数（蓝）与正比例函数（绿）的图象对比：$k$ 控制斜率，$b$ 控制纵截距]
  )

  例 1：出租车收费“起步价 13 元，每公里 2.3 元”。设里程为 $x$ 千米，车费为 $y$ 元。

  $ y = 2.3 x + 13 $

  这里 $2.3$ 表示每多 1 千米，车费多 2.3 元；$13$ 表示 0 千米时也要付 13 元。

  例 2：某长方形的长固定比宽多 4 厘米。设宽为 $x$，长为 $y$。

  $ y = x + 4 $

  这也是一次函数，因为每当宽增加 1 厘米，长也稳定增加 1 厘米。

  例 3：已知一次函数过点 $(0, -2)$ 和 $(3, 4)$。

  先由 $(0, -2)$ 得 $b = -2$。再代入 $(3, 4)$：

  $ 4 = 3 k - 2 $

  所以

  $ 3 k = 6, quad k = 2 $

  故函数解析式为

  $ y = 2 x - 2 $
]

#pitfall[
  *陷阱 1：把“有比”误当成“正比例”。*

  例如“买 3 千克苹果花 24 元，再加 5 元包装费”，虽然也出现了“每千克 8 元”，但总费用是

  $ y = 8 x + 5 $

  这不是正比例，因为 $frac(y, x)$ 不恒定，图象也不过原点。

  *陷阱 2：把 $k$ 和 $b$ 的工作搞混。*

  - $k$ 管“每一步变多少”
  - $b$ 管“起点在哪儿”

  所以看到“上车先收 13 元”，那是 $b$；看到“每公里 2.3 元”，那才是 $k$。
]

#mastery[
  *基础*

  + 判断下列模型是正比例函数、一次函数，还是都不是：
    - $y = 5 x$
    - $y = 5 x + 2$
    - $y = frac(12, x)$
    - $y = 18 - 3 x$

  + 某打印店收费“开机费 4 元，每页 0.6 元”。写出页数 $x$ 与费用 $y$ 的函数关系式，并求打印 10 页的费用。

  + 已知一次函数过点 $(0, 3)$ 和 $(2, 9)$，求解析式。

  *应用*

  + 水箱原有 80 升水，每分钟注入 6 升。写出注水后 $x$ 分钟水量 $y$ 的函数关系式。多少分钟后水量达到 140 升？

  + 某步行计划规定：先完成 1 公里热身，再以稳定速度每分钟 80 米继续前进。设总路程 $y$（米）与热身后时间 $x$（分钟）的关系式，并说明这里为什么不是正比例函数。

  *挑战*

  + 在同一坐标系里，直线 $y = 3 x$ 与 $y = 3 x + 4$ 为什么永远不相交？请用“每一步的变化”和“起点不同”两种语言解释。

  + 一次函数 $y = (m + 1)x + 2m - 3$ 过原点，求 $m$，并写出此时的函数解析式。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*

  + $y = 5 x$：正比例函数，也是一次函数。$y = 5 x + 2$：一次函数，但不是正比例函数。$y = frac(12, x)$：都不是。$y = 18 - 3 x$：一次函数，但不是正比例函数。

  + $y = 0.6 x + 4$。打印 10 页时，$y = 0.6 times 10 + 4 = 10$（元）。

  + 由 $(0, 3)$ 得 $b = 3$。代入 $(2, 9)$：$9 = 2k + 3$，得 $k = 3$，所以 $y = 3 x + 3$。

  *应用*

  + $y = 80 + 6 x$。令 $80 + 6 x = 140$，得 $6 x = 60$，所以 $x = 10$（分钟）。

  + $y = 1000 + 80 x$。因为当 $x = 0$ 时，$y = 1000 != 0$，所以它不是正比例函数，而是一次函数。

  *挑战*

  + 两条直线每前进一步都增加 3，所以它们的“每步变化量”相同；但一个从 0 起步，一个从 4 起步，始终相差 4，所以平行且不相交。

  + 过原点意味着 $2m - 3 = 0$，所以 $m = frac(3, 2)$。此时解析式为 $y = frac(5, 2) x$。
]
