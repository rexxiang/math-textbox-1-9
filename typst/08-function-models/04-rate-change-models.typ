#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §8.4 变化率模型：从利率到增长率 <sec-8-4>

#vocab[变化率 rate of change / 利率 interest rate / 增长率 growth rate / 复利 compound growth / 倍率 multiplier]

#crisis[
  同样是“每年增长 $5%$”，下面两句话看起来很像，实际却对应不同模型：

  - 方案 A：每年都按最初的 1000 元算利息
  - 方案 B：每年都按上一年末的余额继续增长

  两年后它们还会一样吗？五年后呢？

  这里真正要学的不是再做一遍百分数四则，而是判断：*当情境说“按比例增长”时，应该把它翻译成哪一种函数模型？*
]

#history-note[
  商人、银行家和人口统计学家很早就发现：有些变化像直线——每期多同样多；有些变化像滚雪球——每期都按现有总量再乘一个固定倍率。利率、增长率之所以重要，不是因为它们会算百分数，而是因为它们逼着我们分清两类完全不同的变化规则。
]

#discovery[
  设初始金额为 1000 元，年利率都是 $5%$。

  *方案 A：按最初本金算利息（简单模型）*

  #align(center, table(
    columns: 5,
    align: center,
    [年份 $t$], [$0$], [$1$], [$2$], [$3$],
    [金额 $y$], [$1000$], [$1050$], [$1100$], [$1150$],
  ))

  每过 1 年都固定增加 50 元，所以它本质上是一次函数：

  $ y = 1000 + 50 t $

  *方案 B：每年按当前余额继续增长（滚动模型）*

  #align(center, table(
    columns: 5,
    align: center,
    [年份 $t$], [$0$], [$1$], [$2$], [$3$],
    [金额 $y$], [$1000$], [$1050$], [$1102.5$], [$1157.625$],
  ))

  这里每次都不是“加 50”，而是“乘 1.05”：

  $ y = 1000 dot 1.05^t $

  这张表说明了一个关键区别：

  - 固定增加量 → 一次函数
  - 固定倍率 → 重复乘法模型

  所以“利率”或“增长率”真正告诉你的，不只是一个百分数，而是*每一步应该加，还是应该乘*。
]

#tryit[
  先自己判断：

  某短视频账号现有 2000 名关注者，之后每周按上一周总数增长 $10%$。

  + 第 1 周、第 2 周后的关注者数量各是多少？
  + 这个情境更像“一次函数”，还是“每次乘同一个倍率”的模型？
  + 若把“每周增加 200 人”与它比较，哪一类变化规则不同？
]

#side-hack[
  遇到“增长率”“利率”“折旧率”时，先别急着算。先问：

  - 是*每期加同样多*吗？——那是一次函数思维
  - 还是*每期乘同一个倍率*吗？——那是按比例变化的增长模型

  百分数只是表面语言，真正决定模型的是“加”还是“乘”。
]

#blueprint[
  *一、变化率模型的核心任务*

  这类题的重点不是孤立地算百分数，而是把情境翻译成函数关系式。最常见的两类是：

  - *固定增加量模型*：$y = k x + b$
  - *固定倍率模型*：$y = a (1 + r)^x$

  其中：

  - $a$ 是初始量
  - $r$ 是每一步的变化率
  - $1 + r$ 是每一步的倍率

  *二、什么时候仍然是一次函数？*

  如果“利息”或“增长量”每期都按*最初基数*来算，那么每一步增加量固定，模型仍然是一次函数。

  例如本金 1000 元，年利率 $5%$，每年都只按本金算利息：

  $ y = 1000 + 50 t $

  因为每年都稳定增加 50。

  *三、什么时候要用倍率模型？*

  如果每一期都按*当前总量*继续变化，就不是固定增加，而是固定倍率。

  例如本金 1000 元，每年增长 $5%$：

  $ y = 1000 dot 1.05^t $

  再例如某地人口 8 万，每年增长 $3%$：

  $ y = 8 dot 1.03^t $

  这里图象不会是直线，因为每期增加量会越来越大。

  *四、怎样从情境读公式？*

  读题时抓三件事：

  - *起点*：一开始有多少？
  - *步长*：按年、按月、按周，还是按次？
  - *规则*：每步是加固定量，还是乘固定倍率？

  例 1：某厂今年产量为 500 台，以后每年比上一年增加 40 台。

  $ y = 500 + 40 t $

  例 2：某厂今年产量为 500 台，以后每年在上一年的基础上增长 $8%$。

  $ y = 500 dot 1.08^t $

  这两个情境表面都在讲“增长”，但模型完全不同。

  *五、如何比较不同模型？*

  - 一次函数：差固定，图象是直线
  - 反比例函数：积固定，图象是双曲线
  - 二次函数：变化率本身变化，图象是抛物线
  - 倍率增长模型：每步乘同一倍率，图象通常越来越陡

  所以函数建模的核心，不是死记情境名词，而是判断*哪个量保持不变*：差、商、积，还是倍率。
]

#pitfall[
  *陷阱 1：把“增长 $5%$”写成“每次加 0.05”。*

  增长 $5%$ 的真正意思是乘

  $ 1 + 5% = 1.05 $

  不是直接加 0.05。

  *陷阱 2：把“按本金算利息”和“按余额继续增长”混成一种模型。*

  前者每期增加量固定，更像一次函数；后者每期增加量不固定，要用倍率模型。
]

#mastery[
  *基础*

  + 判断下列情境更适合用“一次函数”还是“固定倍率模型”：
    - 本金 2000 元，每年利息都按本金的 $4%$ 计算
    - 某班级公众号阅读量每周都在上一周基础上增长 $10%$
    - 水箱每分钟多进水 5 升

  + 某商品现价 300 元，之后每月在上月基础上增长 $2%$。写出价格模型。

  + 某活动第一天有 500 人参加，之后每天固定增加 60 人。写出人数模型。

  *应用*

  + 某账户初始金额 1000 元，按复利每年增长 $5%$。写出函数关系式，并求 2 年后的金额。

  + 某城市今年人口为 80 万，之后每年增长 $2%$。写出函数关系式，并说明它为什么不是一次函数。

  + 某设备价值 5000 元，每年按当前价值折旧 $10%$。写出价值模型，并求 1 年后的价值。

  *挑战*

  + 甲方案：$y = 1000 + 80 t$；乙方案：$y = 1000 dot 1.06^t$。解释为什么开始几年两者可能很接近，但时间拉长后差距会越来越明显。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*

  + 第一个：一次函数；第二个：固定倍率模型；第三个：一次函数。

  + $y = 300 dot 1.02^t$。

  + $y = 500 + 60 t$。

  *应用*

  + $y = 1000 dot 1.05^t$。2 年后金额为 $1000 dot 1.05^2 = 1102.5$（元）。

  + $y = 80 dot 1.02^t$。它不是一次函数，因为每年增加量不是固定的，而是按当时总人口继续变化。

  + 折旧后倍率为 $0.9$，所以 $y = 5000 dot 0.9^t$。1 年后价值为 $4500$ 元。

  *挑战*

  + 甲方案每期都只加 80，增长节奏不变；乙方案每期都按当前总量继续乘 1.06，所以起初增加得不多，后面会越来越快。时间越长，倍率模型的优势或差距就越明显。
]
