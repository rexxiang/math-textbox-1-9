#import "../lib/theme-v2.typ": blueprint, mastery, secref

== 第八章回顾：函数模型

#blueprint[
  *知识速查卡*

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *模型识别*
      - 稳定比值：$y = k x$（#secref("8.1")）
      - 固定增量：$y = k x + b$（#secref("8.1")）
      - 固定乘积：$y = frac(k, x)$（#secref("8.2")）
      - 最值与对称：$y = a x^2 + b x + c$（#secref("8.3")）
    ],
    block[
      *四种表示互译*
      - 情境 $arrow.r$ 表格：先找“怎么一起变”
      - 表格 $arrow.r$ 公式：看差、看商、看积，以及是否在连续按比例变化
      - 公式 $arrow.r$ 图象：直线 / 双曲线 / 抛物线
      - 图象 $arrow.r$ 情境：把走势翻回“固定增加、固定乘积、最值”，并比较增长率应用里的不同写法
    ],
  )

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *参数怎样读*
      - 一次函数里，$k$ 管每步变多少，$b$ 管起点
      - 反比例函数里，$k$ 管固定乘积
      - 二次函数里，$a$ 管开口方向与弯曲程度
      - 顶点式 $y = a(x - h)^2 + k$ 直接给顶点 $(h, k)$
    ],
    block[
      *回看旧工具*
      - 比例意识来自 #secref("2.4")
      - 坐标读图来自 #secref("3.5")
      - 字母模板来自 #secref("4.3")
      - 列式 / 列方程意识来自 #secref("4.4")
      - 函数不是另起炉灶，而是把这些工具重新拧成一条变化语言
    ],
  )
]

#mastery[
  *混合自测题*（不标知识点，综合调用）

  + 判断下列关系分别更像哪一种模型：$y = 4 x$、$y = 4 x + 7$、$y = frac(36, x)$、$y = -x^2 + 6 x$、$y = 200 dot 1.03^x$。

  + 已知一次函数过点 $(0, 5)$ 和 $(3, 11)$，求解析式。

  + 某长方形面积固定为 60。设长为 $x$，宽为 $y$，写出函数关系式，并求 $x = 12$ 时的 $y$。

  + 将 $y = x^2 - 8 x + 20$ 化成顶点式，写出顶点、对称轴、最小值。

  + 某水箱原有 50 升水，每分钟流入 4 升。写出函数关系式，并求 15 分钟后的水量。

  + 某账户初始金额为 3000 元，之后每年按上一年余额增长 $4%$。写出函数关系式，并求 1 年后的金额。

  + 一条抛物线与 $x$ 轴交于两点。把这件事翻成情境语言时，常常意味着什么？

  + 已知反比例函数图象经过点 $(5, -2)$。求解析式，并判断图象所在象限。

  + 下列哪个情境更适合用一次函数，哪个更适合用“连续按比例变化”的写法？说明理由：
    - 每月稳定存入 500 元
    - 每月余额都在上月基础上增加 $2%$

  + *进阶*：某商家销售额第一月为 100 万元。方案 A 以后每月增加 8 万元；方案 B 以后每月在上月基础上增长 $6%$。不用精确求很远的月份，只解释：开始时为什么可能感觉差不多，后面为什么会拉开？

  + *进阶*：解释为什么本章的函数图象虽然不同，但几乎都离不开坐标、变量和“把条件写成式子”这三件旧工具。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  + 依次是：正比例函数、一次函数、反比例函数、二次函数、增长率应用里的连续变化写法。

  + 由 $(0, 5)$ 得 $b = 5$。再代入 $(3, 11)$：$11 = 3k + 5$，得 $k = 2$，所以 $y = 2 x + 5$。

  + $y = frac(60, x)$。当 $x = 12$ 时，$y = 5$。

  + $y = (x - 4)^2 + 4$。顶点 $(4, 4)$，对称轴 $x = 4$，最小值为 4。

  + $y = 50 + 4 x$。15 分钟后 $y = 110$（升）。

  + $y = 3000 dot 1.04^t$。1 年后金额为 $3120$ 元。

  + 常表示“这个量在两个时刻或两个位置变成 0”，例如高度落地、利润归零、面积消失等。

  + $k = 5 times (-2) = -10$，所以 $y = frac(-10, x)$。图象在第二、四象限。

  + “每月稳定存入 500 元”适合一次函数，因为每期固定增加量；“每月余额都在上月基础上增加 $2%$”适合“连续按比例变化”的写法，因为每期都在乘同一个倍率。

  + 方案 A 每期都加 8，方案 B 每期都乘 1.06。前期总量还不大时，两者增加量可能接近；后期随着基数变大，方案 B 每期增加得越来越多，所以会逐渐拉开。

  + 因为函数本质上仍是在处理“两个量的关系”。坐标负责把对应点放到图上，变量负责把一般关系写成模板，列式 / 列方程负责把情境语言翻译成数学语言。图象变了，底层工具没变。

  *螺旋复习（与前面主干的联系）*

  + 回到 #secref("2.4")：正比例与反比例进入函数章后，没有消失，而是从“比与比例”的语言升级成了“稳定商”和“固定积”的模型语言。

  + 回到 #secref("3.5")：函数图象之所以能被看见，前提正是你已经会在坐标系里定位点、比较走势、读出交点。

  + 回到 #secref("4.3")：$y = k x + b$、$y = frac(k, x)$、$y = a x^2 + b x + c$ 都是在把字母当模板使用；函数不是脱离代数的对象，而是代数模板的动态读法。

  + 回到 #secref("4.4")：从“两点求一次函数”“令 $y = 0$ 找抛物线交点”到“由情境列出增长模型”，都在继续调用“把条件变成等式 / 关系式”的习惯。
]
