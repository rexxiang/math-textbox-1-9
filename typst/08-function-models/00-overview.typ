#import "../lib/theme-v2.typ": history-note, side-hack, secref

_函数模型——把“怎么一起变”看成可切换、可比较、可解释的变化模型。_

这一章也属于*专题深化*。你会把比例、坐标、字母和方程这些旧工具接起来，开始用“两个量怎样一起变化”来读情境。

在 #secref("4.3") 和 #secref("4.4") 里，字母已经能承载关系式，方程也已经能表达“条件同时成立”；在 #secref("3.5") 里，点坐标让位置能落在同一张图上。现在你不再只问“代进去算多少”，而是改问：*两个量怎样一起变化？这种变化更像直线、双曲线，还是抛物线？*

本章把函数明确当成*变化模型*来学：同一种情境，可以从表格、公式、图象和文字四种入口来读；不同模型，则对应不同的“变化规则”。本章聚焦一次函数、反比例函数、二次函数以及相关应用比较；三角比测量则放在几何深化路线中展开。

#history-note[
  从笛卡尔把坐标系和方程接起来开始，数学家逐渐发现：公式不只是“算答案”的短句，也是描述变化的语言。人口、路程、喷泉、面积、利率、增长率，这些看似分散的问题，只要抓住“每一步怎样变”，往往就能落到某一种函数模型上。
]

#side-hack[
  *分支入口回看*：

  继续前进前，先确认你还能直接调用 #secref("3.5") 的坐标定位、#secref("4.3") 的字母模板、#secref("4.4") 的列方程意识，以及 #secref("2.4") 的正比 / 反比直觉。这里的任务不是重讲这些旧工具，而是把它们接成“表格—公式—图象—情境”四种表示之间的来回翻译。
]

本章的主线是：

- *#secref("8.1") 一次函数*——把稳定比值的正比例关系推进成“固定增量”的直线模型，学会在情境、表格、公式、图象之间切换
- *#secref("8.2") 反比例函数*——把“乘积固定”的变化看成双曲线模型，理解一快一慢、一宽一窄这类联动
- *#secref("8.3") 二次函数*——把“变化本身也在变化”的情境放进抛物线，抓住顶点、对称和最值
- *#secref("8.4") 利率与增长率应用比较*——把利率、增长率等后期情境放回函数视角，作为函数应用与模型比较来读，而不是另开一条新主线
- *本章回顾：函数模型*——综合判断模型类型，并回看比例、坐标、变量与方程怎样在函数里重新会合
