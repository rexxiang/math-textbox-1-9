#import "../lib/theme-v2.typ": history-note, side-hack, secref

_函数模型——把“怎么一起变”看成可切换、可比较、可解释的变化模型。_

这一章也属于*专题深化*。你会把比例、坐标、字母和方程这些旧工具重新接成一条线，开始用“两个量怎样一起变化”来读情境。

在 #secref(“2.4”) 里，你已经能抓住”一个量跟着另一个量变”的比例直觉；在 #secref(“3.8”) 里，点坐标先把位置落到方格纸上；在 #secref(“4.1”) 里，正负方向又把坐标平面真正补全；在 #secref(“4.3”) 里，字母已经能承载关系式。现在这些旧工具要开始协同工作：你不再只问”代进去算多少”，而是改问：*两个量怎样一起变化？这种变化更像直线、双曲线，还是抛物线？*

本章把函数明确当成*变化模型*来学：同一种情境，可以从表格、公式、图象和文字四种入口来读；不同模型，则对应不同的“变化规则”。接下来的路线也很清楚：先看稳定增减的直线，再看一快一慢联动的双曲线，然后进入会出现顶点和最值的抛物线，最后把这些判断带回利率、增长率等应用比较里。三角比测量则放在几何深化路线中展开，而二次主题在这里主要负责图象、对称与最值，不把本章入口改成解二次方程训练。

#history-note[
  从笛卡尔把坐标系和方程接起来开始，数学家逐渐发现：公式不只是“算答案”的短句，也是描述变化的语言。人口、路程、喷泉、面积、利率、增长率，这些看似分散的问题，只要抓住“每一步怎样变”，往往就能落到某一种函数模型上。
]

#side-hack[
  *分支入口回看*：

  继续前进前，先确认你还能直接调用 #secref("2.4") 的正比 #text(fill: luma(150))[•] 反比直觉、#secref("3.8") 的坐标定位、#secref("4.1") 的正负方向感，以及 #secref("4.3") 的字母模板。这里先只做旧工具回看：看见两个量一起变化时，能先把位置、方向和关系写清楚，不急着给它贴上新模型的名字。
]

本章的主线是：

- *#secref("8.1") 一次函数*——把稳定比值的正比例关系推进成“固定增量”的直线模型，学会在情境、表格、公式、图象之间切换
- *#secref("8.2") 反比例函数*——把“乘积固定”的变化看成双曲线模型，理解一快一慢、一宽一窄这类联动
- *#secref("8.3") 函数基础总结*——先把直线模型和双曲线模型的读图、读式、读情境方法收束成一张基础卡
- *#secref("8.4") 二次函数*——把“变化本身也在变化”的情境放进抛物线，抓住顶点、对称和最值
- *#secref("8.5") 利率与增长率应用比较*——比较“每期固定增加”和“每期按当前总量继续变化”两种节奏，先学会判断模型，不急着把它扩成新公式家族
- *本章回顾：函数模型*——综合判断模型类型，并回看比例、坐标、变量与方程怎样在函数里重新会合
