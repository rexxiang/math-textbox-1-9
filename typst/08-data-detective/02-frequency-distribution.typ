#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §8.2 频率分布直方图 <sec-8-2>

#vocab[频率 frequency (relative) / 频次 count / 直方图 histogram / 频率分布 frequency distribution]

#crisis[
  学校体检测量了全校 $500$ 名学生的身高数据。校医拿到一张密密麻麻的表格，上面列着 $500$ 个数字。

  校长问：「我们学校学生的身高*分布规律*是什么？大部分学生在哪个范围？特别高或特别矮的有多少？」

  面对 $500$ 个散乱的数字，*怎样才能一眼看出数据的整体面貌？*
]

#history-note[
  数据可视化的先驱是苏格兰工程师*普莱费尔*（William Playfair, 1759--1823）。他在 1786 年发明了折线图和条形图，1801 年发明了饼图。在他之前，数据只能用表格呈现；在他之后，人们第一次能「看到」数据的故事。直方图则由法国数学家 A.M. Guerry 在 1833 年首次使用。
]

#discovery[
  *第一步*：面对 $500$ 个身高数据（单位：cm），先看极端值。

  最矮 $140.2$ cm，最高 $179.8$ cm。数据范围：$179.8 - 140.2 = 39.6$ cm。

  *第二步*：分组。

  把整个范围分成若干小段（通常 $5 tilde 8$ 组）。每组宽度相同，比如 $5$ cm：
  - $[140, 145)$, $[145, 150)$, $[150, 155)$, $[155, 160)$, $[160, 165)$, $[165, 170)$, $[170, 175)$, $[175, 180]$

  *第三步*：计数。数每组里有多少个数据（频次），再算频率。

  #align(center, table(
    columns: 4,
    align: (left, center, center, center),
    [身高范围（cm）], [频次], [频率], [频率/组距],
    [$[140, 145)$], [15], [0.030], [0.006],
    [$[145, 150)$], [40], [0.080], [0.016],
    [$[150, 155)$], [85], [0.170], [0.034],
    [$[155, 160)$], [130], [0.260], [0.052],
    [$[160, 165)$], [120], [0.240], [0.048],
    [$[165, 170)$], [70], [0.140], [0.028],
    [$[170, 175)$], [30], [0.060], [0.012],
    [$[175, 180]$], [10], [0.020], [0.004],
    [合计], [500], [1.000], [],
  ))

  *第四步*：画直方图。横轴是身高范围，纵轴是频率/组距。每个矩形的*面积*（而非高度）代表该组的频率。

  *关键发现*：直方图显示身高主要集中在 $155 tilde 165$ cm，形成一个「中间高、两头低」的形状。这比看 $500$ 个数字清楚多了！
]

#tryit[
  先自己算一算：

  某班共有 $20$ 个数据，频率分布直方图中某组有 $8$ 个数据落在其中。

  + 这一组的频率（相对频率）是多少？
  + 如果组距为 $5$，这一组在直方图中矩形的高（纵轴值）是多少？

  写出计算过程，再对照下面的概念说明验证。
]

#blueprint[
  === 基本概念

  *频次*（计数）：落在某个范围内的数据个数。

  *频率*：某个范围的频次占总数的比例。

  $ "频率" = frac("频次", "数据总数") $

  频率的性质：
  - 每个组的频率 $>= 0$
  - 所有组的频率之和 $= 1$

  === 频率分布表的制作步骤

  + *计算极差*：最大值 $-$ 最小值
  + *决定组距和组数*：通常取 $5 tilde 8$ 组，组距 $=$ 极差 $div$ 组数（取整数或方便计算的值）
  + *确定分组*：$[a, a + d)$, $[a + d, a + 2d)$, ... 最后一组取闭区间
  + *统计频次*：用划记法数每组的数据个数
  + *计算频率*：频次 $div$ 总数

  === 频率分布直方图

  - 横轴：数据范围（各组的端点）
  - 纵轴：*频率/组距*（这样矩形面积 $=$ 频率）
  - 每个矩形宽 $=$ 组距，高 $=$ 频率/组距
  - 所有矩形面积之和 $= 1$

  *为什么纵轴是频率/组距而不是频率？*

  如果各组*组距不等*，直接用频率做纵轴会产生误导。用频率/组距做纵轴，能保证矩形*面积*准确反映频率。在组距相等的情况下，两种方式的图形形状相同。

  === 从直方图读取信息

  - 最高的矩形对应的组是*众数所在区间*
  - 从左到右累加面积到 $0.5$ 时对应的位置，大约是*中位数*
  - 某几个组的面积之和就是数据落在该范围的频率

  *例题 1*：根据上面的身高数据，估计身高在 $155$ cm 到 $170$ cm 之间的学生占比。

  解：$0.260 + 0.240 + 0.140 = 0.640$，即约 $64%$ 的学生身高在 $155 tilde 170$ cm 之间。

  *例题 2*：某班 $40$ 名学生的数学成绩如下（满分 $100$）：

  #align(center, table(
    columns: 5,
    align: center,
    [分数段], [$[50, 60)$], [$[60, 70)$], [$[70, 80)$], [$[80, 90)$],
    [频次], [4], [8], [16], [12],
  ))

  求各组频率，画频率分布直方图，估计中位数所在区间。

  解：频率分别为 $frac(4, 40) = 0.10$，$frac(8, 40) = 0.20$，$frac(16, 40) = 0.40$，$frac(12, 40) = 0.30$。

  从左累加：$0.10 + 0.20 = 0.30$（前两组），加上第三组 $0.30 + 0.40 = 0.70 > 0.50$。所以中位数在 $[70, 80)$ 这一组中。

  *例题 3*：一个频率分布直方图中，某个矩形的高为 $0.04$，组距为 $5$。这一组的频率是多少？

  解：频率 $=$ 高 $times$ 组距 $= 0.04 times 5 = 0.20$，即 $20%$。
]

#pitfall[
  *陷阱 1*：频率（比例）$\ne$ 频次（个数）。

  #grid(columns: (1fr, 1fr), gutter: 8pt,
    block[*频次*：\ 某组中数据的*个数*（如 $8$ 个）],
    block[*频率*：\ 该组个数占总数的*比例*（如 $8/20 = 0.4$）]
  )

  *陷阱 2*：频率分布直方图的纵轴是*频率/组距*，不是频率本身。这样设计使得矩形*面积*等于频率，而不是矩形高度等于频率。在组距相等时，图形形状一样，但纵轴单位不同；若组距不等，必须用频率/组距才能正确反映比例。
]

#mastery[
  *基础*

  + 某班 $50$ 名学生的体重数据（kg），极差为 $30$ kg，计划分成 $6$ 组。每组的组距应该取多少？
  + 频率分布直方图中所有矩形的面积之和等于多少？为什么？
  + 某组数据的频率为 $0.15$，总数据量为 $200$，这组的频次是多少？

  *应用*

  + 某社区对 $100$ 户家庭的月用电量（度）进行统计，结果如下：

    #align(center, table(
      columns: 6,
      align: center,
      [用电量], [$[50, 100)$], [$[100, 150)$], [$[150, 200)$], [$[200, 250)$], [$[250, 300]$],
      [频次], [10], [25], [35], [20], [10],
    ))

    - 制作频率分布表
    - 估计月用电量在 $100$ 度到 $200$ 度之间的家庭比例
    - 估计中位数所在区间

  *挑战*

  + 已知一组数据的频率分布直方图中，$[10, 15)$ 组的矩形高为 $0.06$，$[15, 20)$ 组的矩形高为 $0.10$，$[20, 25)$ 组的矩形高为 $0.04$。若这三组的频次之和为 $40$，求数据总数。
  + 为什么频率分布直方图用「频率/组距」做纵轴比用「频率」做纵轴更科学？请举例说明。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + 组距 $= frac(30, 6) = 5$（kg）。
  + 面积之和 $= 1$，因为所有组的频率之和为 $1$，而每个矩形的面积就是对应组的频率。
  + 频次 $= 0.15 times 200 = 30$。

  *应用*
  + 频率分别为 $0.10, 0.25, 0.35, 0.20, 0.10$。$100 tilde 200$ 度的家庭比例 $= 0.25 + 0.35 = 0.60$，即 $60%$。累加频率：$0.10 + 0.25 = 0.35$，加上 $[150, 200)$ 组得 $0.70 > 0.50$，中位数在 $[150, 200)$ 组中。

  *挑战*
  + 三组的频率分别为 $0.06 times 5 = 0.30$，$0.10 times 5 = 0.50$，$0.04 times 5 = 0.20$。频率之和 $= 1.00$（恰好覆盖全部数据）。频次之和 $40$ 即总数据量。答：数据总数为 $40$。
  + 如果两组的组距不同（例如一组宽 $5$，另一组宽 $10$），用频率做纵轴会使宽组的矩形面积过大，视觉上夸大该组的比重。用频率/组距做纵轴后，面积 $=$ 高 $times$ 宽 $=$ (频率/组距) $times$ 组距 $=$ 频率，无论组距是否相等都能准确反映比例。
]
