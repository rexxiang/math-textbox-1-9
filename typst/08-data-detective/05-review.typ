#import "../lib/theme-v2.typ": blueprint, mastery, secref

== 第八章回顾：数据侦探

#blueprint[
  *知识速查卡*

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *离散程度*（#secref("8.1")）
      - 极差：$R = x_max - x_min$（最粗糙的度量）
      - 方差：$s^2 = frac(1, n) sum (x_i - bar(x))^2$（平均偏差平方）
      - 方差越大，数据越分散；方差越小，数据越集中
      - 比较两组数据：先比均值，再比方差
    ],
    block[
      *频率分布*（#secref("8.2")）
      - 频率 $= frac("该组频数", "总频数")$，所有组频率之和 $= 1$
      - 频率分布直方图：纵轴为"频率/组距"，面积表示频率
      - 组距选择影响直方图形状
      - 从样本频率估计总体概率
    ],
  )

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *概率*（#secref("8.3")）
      - 随机事件的概率：$0 <= P(A) <= 1$
      - 必然事件：$P = 1$；不可能事件：$P = 0$
      - 古典概型：$P(A) = frac("有利结果数", "总等可能结果数")$
      - 互斥事件：$P(A "或" B) = P(A) + P(B)$
      - 对立事件：$P(bar(A)) = 1 - P(A)$
    ],
    block[
      *抽样与估计*（#secref("8.4")）
      - 总体：研究对象的全体
      - 样本：从总体中抽取的一部分
      - 用样本频率估计总体概率（样本量越大越准）
      - 抽样方法：系统抽样、分层抽样
      - 避免偏差：样本要有代表性
    ],
  )
]

#mastery[
  *混合自测题*

  + 数据组：$5, 8, 6, 9, 7, 5, 10, 6$，求平均数、极差和方差。

  + 两个班级期末数学成绩：甲班平均 $82$ 分，方差 $16$；乙班平均 $82$ 分，方差 $36$。哪个班成绩更整齐？

  + 抛一枚均匀硬币，连抛 3 次，求恰好出现 2 次正面的概率。

  + 从 $\{1, 2, 3, 4, 5\}$ 中随机取 2 个数，求两数之和大于 6 的概率。

  + 某次调查 $100$ 人，年龄分布如下：20 岁以下 $15$ 人，20–30 岁 $40$ 人，30–40 岁 $30$ 人，40 岁以上 $15$ 人。随机抽 1 人，他在 20–30 岁之间的概率是多少？

  + 某工厂检测 $200$ 件产品，其中 $6$ 件不合格。用频率估计该产品的不合格率，并估计一批 $5000$ 件产品中约有多少件不合格。

  + 两组数据的方差分别为 $s_1^2 = 4.5$ 和 $s_2^2 = 0.8$，哪组数据更稳定？

  + 频率分布直方图中，某区间 $[10, 15)$ 的频率/组距 $= 0.04$，该区间的频率是多少？

  + 袋中有 $3$ 个红球、$2$ 个白球，不放回取 $2$ 次，求两次都是红球的概率。

  + 某校用系统抽样从 $600$ 名学生中抽取 $50$ 人做问卷。抽样间隔是多少？若第一个抽到的是第 $7$ 号，下一个被抽到的是第几号？

  + *进阶*：某数据有 $10$ 个数，平均数为 $6$，方差为 $4$。若每个数都加上 $3$，新数据的平均数和方差各是多少？

  + *进阶*：掷两枚骰子，求两数之积为偶数的概率。（提示：用对立事件——两数之积为奇数等价于两个都是奇数）

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  + 平均数 $bar(x) = (5+8+6+9+7+5+10+6)/8 = 56/8 = 7$；极差 $= 10 - 5 = 5$；方差 $s^2 = frac(1, 8)[(5-7)^2+(8-7)^2+(6-7)^2+(9-7)^2+(7-7)^2+(5-7)^2+(10-7)^2+(6-7)^2] = frac(1, 8)(4+1+1+4+0+4+9+1) = frac(24, 8) = 3$

  + 均值相同，但乙班方差 $36 >$ 甲班方差 $16$，说明*甲班*成绩更整齐（更集中）。

  + 总结果数 $2^3 = 8$；恰好 2 次正面：HHT, HTH, THH，共 3 种；$P = frac(3, 8)$。

  + 共 $C_5^2 = 10$ 种取法；和大于 6：$(3,4), (3,5), (4,5), (2,5)$，即 $(3,4),(3,5),(4,5),(2,5)$，共 4 种；$P = frac(4, 10) = frac(2, 5)$。

  + $P = frac(40, 100) = 40%$。

  + 不合格率 $approx frac(6, 200) = 3%$；5000 件中约 $5000 times 3% = 150$ 件不合格。

  + $s_2^2 = 0.8 < s_1^2 = 4.5$，第二组数据更稳定（更集中）。

  + 频率 $= 0.04 times 5 = 0.2$（即 $20%$）。

  + 共 $5 times 4 = 20$ 种有序取法；两次都是红球：$3 times 2 = 6$ 种；$P = frac(6, 20) = frac(3, 10)$。

  + 间隔 $= 600 div 50 = 12$；下一个 $= 7 + 12 = 19$ 号。

  + 平均数变为 $6 + 3 = 9$；方差不变，仍为 $4$（加常数不改变离散程度）。

  + 两数之积为奇数：两个都是奇数，各有 3 种（1,3,5），$P(两奇) = frac(3, 6) times frac(3, 6) = frac(1, 4)$；故积为偶数的概率 $= 1 - frac(1, 4) = frac(3, 4)$。

  *螺旋复习*

  + 本章频率 $=$ 某类结果次数 $div$ 总次数，这正是 #secref("3.1") 百分数的实际应用——频率可以写成百分比，帮助我们估计总体。

  + 概率计算中的"等可能结果数"，涉及枚举所有情况——这种系统列举方法与 #secref("1.5") 的排列思想一脉相承：先列出所有情况，再数有利情况。
]
