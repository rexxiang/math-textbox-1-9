#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §8.1 数据的波动程度 <sec-8-1>

#vocab[极差 range / 方差 variance / 波动 variability]

#crisis[
  学校射箭队要选一名队员参加比赛。甲和乙各射了 $5$ 箭，成绩（环数）如下：

  #align(center, table(
    columns: 7,
    align: center,
    [], [第1箭], [第2箭], [第3箭], [第4箭], [第5箭], [平均],
    [甲], [8], [9], [7], [10], [6], [8],
    [乙], [9], [8], [8], [7], [8], [8],
  ))

  两人的平均成绩*完全相同*，都是 $8$ 环。但教练一眼就选了乙。为什么？

  *平均数一样的情况下，怎么比较谁的成绩更「稳定」？*
]

#discovery[
  *第一步*：画图直观感受。

  把甲和乙的成绩画在数轴上：
  - 甲：6, 7, 8, 9, 10——分布得*很散*
  - 乙：7, 8, 8, 8, 9——集中在 $8$ 附近

  乙的数据更「集中」，说明乙发挥更*稳定*。

  *第二步*：用数字衡量「散」的程度。

  最简单的办法——*极差*：最大值 $-$ 最小值。
  - 甲的极差：$10 - 6 = 4$
  - 乙的极差：$9 - 7 = 2$

  极差越小，数据越集中。但极差只看了两个极端值，忽略了中间的数据。

  *第三步*：让每个数据都「发言」。

  我们想知道每个数据偏离平均数多远。算每个数据与平均数的差：
  - 甲：$8 - 8 = 0$，$9 - 8 = 1$，$7 - 8 = -1$，$10 - 8 = 2$，$6 - 8 = -2$
  - 这些差的和是 $0$（正负抵消了！）

  怎么办？把差*平方*，消除正负号，然后*求平均*：
  $ S_"甲"^2 = frac(0^2 + 1^2 + (-1)^2 + 2^2 + (-2)^2, 5) = frac(0 + 1 + 1 + 4 + 4, 5) = frac(10, 5) = 2 $
  $ S_"乙"^2 = frac(1^2 + 0^2 + 0^2 + (-1)^2 + 0^2, 5) = frac(1 + 0 + 0 + 1 + 0, 5) = frac(2, 5) = 0.4 $

  $S_"乙"^2 < S_"甲"^2$，所以乙的成绩更稳定。这个量就是*方差*！
]

#tryit[
  先自己计算，再看蓝图中的例题：

  数据组：$\{6, 7, 8, 9, 10\}$

  + 先求平均数 $overline(x)$
  + 再算每个数据与平均数的差：$(6 - overline(x))$，$(7 - overline(x))$，……
  + 把每个差*平方*
  + 求所有平方的平均值，即方差 $S^2$

  计算完后对照下面例题验证。
]

#side-hack[
  *方差计算四步走*：
  + 算*平均数* $overline(x)$
  + 算每个数据与平均数的*差*
  + 把差*平方*
  + 所有平方的*平均*就是方差
]

#blueprint[
  === 极差

  $ "极差" = "最大值" - "最小值" $

  极差反映数据的变化幅度，但只用了两个极端数据，信息量有限。

  === 方差

  设有 $n$ 个数据 $x_1, x_2, dots.h, x_n$，平均数为 $overline(x)$，则*方差*为：

  $ S^2 = frac(1, n) [(x_1 - overline(x))^2 + (x_2 - overline(x))^2 + dots.h + (x_n - overline(x))^2] $

  即：$ S^2 = frac(1, n) sum_(i=1)^(n) (x_i - overline(x))^2 $

  *方差的意义*：
  - 方差越*大*，数据越*分散*，波动越大
  - 方差越*小*，数据越*集中*，越稳定
  - 方差为 $0$ 当且仅当所有数据都相等

  *简化计算公式*：

  $ S^2 = frac(1, n) sum_(i=1)^(n) x_i^2 - overline(x)^2 = overline(x^2) - overline(x)^2 $

  用文字说：*方差 = 平方的平均 $-$ 平均的平方*。

  *例题 1*：数据 $3, 5, 7, 9, 6$ 的方差。

  解：$overline(x) = frac(3 + 5 + 7 + 9 + 6, 5) = 6$

  $ S^2 = frac((3-6)^2 + (5-6)^2 + (7-6)^2 + (9-6)^2 + (6-6)^2, 5) = frac(9 + 1 + 1 + 9 + 0, 5) = 4 $

  *例题 2*：用简化公式验证。

  $ overline(x^2) = frac(9 + 25 + 49 + 81 + 36, 5) = frac(200, 5) = 40 $
  $ S^2 = 40 - 6^2 = 40 - 36 = 4 quad checkmark $

  *例题 3*：已知一组数据 $a, a, a, a, a$（5 个相同的数），方差是多少？

  解：$overline(x) = a$，每个 $(x_i - overline(x))^2 = 0$，方差 $S^2 = 0$。所有数据相同时，方差为零。

  *例题 4*：数据 $x_1, x_2, dots.h, x_n$ 的方差为 $S^2$。如果每个数据都加上常数 $c$，新数据的方差是多少？如果每个数据都乘以常数 $k$，新数据的方差是多少？

  解：
  - 每个数据加 $c$：新平均数 $= overline(x) + c$，每个差 $(x_i + c) - (overline(x) + c) = x_i - overline(x)$ 不变，方差仍为 $S^2$。
  - 每个数据乘 $k$：新平均数 $= k overline(x)$，每个差 $k x_i - k overline(x) = k(x_i - overline(x))$，方差变为 $k^2 S^2$。
]

#pitfall[
  *陷阱 1*：方差公式是「先求差，再平方，再平均」——顺序不能颠倒。不能「先平均再平方」（那等于 $(overline(x))^2$，是另一回事）。

  #grid(columns: (1fr, 1fr), gutter: 8pt,
    block[*正确*：\ $S^2 = frac(1,n) sum (x_i - overline(x))^2$\ （差先平方，所有平方再平均）],
    block[*错误*：\ 把各数据加起来平均后再平方\ （这只是平均数的平方 $overline(x)^2$，不是方差）]
  )

  *陷阱 2*：方差*越大*表示数据越*分散*（波动越大），方差越小表示数据越集中（越稳定）。不要搞反方向！
]

#mastery[
  *基础*

  + 求数据 $4, 6, 5, 3, 7$ 的极差和方差。
  + 甲组数据：$70, 72, 71, 69, 68$；乙组数据：$65, 75, 80, 60, 70$。分别求方差，判断哪组更稳定。
  + 一组数据的方差为 $3$，如果每个数据都乘以 $2$ 再减去 $5$，新数据的方差是多少？

  *应用*

  + 某工厂生产零件，要求直径为 $10$ 毫米。甲、乙两台机器各生产 $5$ 个零件，直径如下：
    - 甲：$10.1, 9.9, 10.2, 9.8, 10.0$
    - 乙：$10.0, 10.1, 10.0, 9.9, 10.0$

    哪台机器的精度更高？（精度高 = 方差小）

  *挑战*

  + 已知 $5$ 个数据的平均数为 $10$，方差为 $2$。如果再增加一个数据 $10$，新的 $6$ 个数据的方差是多少？
  + 甲班 $40$ 人某次考试平均分 $80$，方差 $16$；乙班 $60$ 人平均分 $80$，方差 $24$。合并两个班后（共 $100$ 人），方差是多少？（提示：总方差公式。）

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + 极差 $= 7 - 3 = 4$。$overline(x) = 5$。$S^2 = frac((4-5)^2 + (6-5)^2 + (5-5)^2 + (3-5)^2 + (7-5)^2, 5) = frac(1 + 1 + 0 + 4 + 4, 5) = 2$。
  + 甲：$overline(x) = 70$，$S^2 = frac(0 + 4 + 1 + 1 + 4, 5) = 2$。乙：$overline(x) = 70$，$S^2 = frac(25 + 25 + 100 + 100 + 0, 5) = 50$。甲组更稳定。
  + 乘以 $2$ 方差变为 $4 times 3 = 12$，再减 $5$ 不影响方差，仍为 $12$。

  *应用*
  + 甲：$overline(x) = 10$，$S^2 = frac(0.01 + 0.01 + 0.04 + 0.04 + 0, 5) = 0.02$。乙：$overline(x) = 10$，$S^2 = frac(0 + 0.01 + 0 + 0.01 + 0, 5) = 0.004$。乙的方差更小，精度更高。

  *挑战*
  + 原来 $sum (x_i - 10)^2 = 5 times 2 = 10$。新增数据 $10$，$(10 - 10)^2 = 0$。新 $sum = 10 + 0 = 10$，新平均数仍为 $10$。新方差 $= frac(10, 6) = frac(5, 3) approx 1.67$。
  + 两班平均数相同（都是 $80$），合并平均数仍为 $80$。合并方差 $= frac(40 times 16 + 60 times 24, 100) = frac(640 + 1440, 100) = frac(2080, 100) = 20.8$。
]
