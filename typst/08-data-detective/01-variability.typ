#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, side-hack, vocab, tryit, pitfall

== §8.1 数据的波动程度 <sec-8-1>

#vocab[极差 range / 方差 variance / 平均数 mean / 偏差 deviation / 波动 variability]

#crisis[
  学校射箭队要选一名队员参加比赛。甲和乙各射了 $5$ 箭，成绩如下：

  #align(center, table(
    columns: 7,
    align: center,
    [], [第1箭], [第2箭], [第3箭], [第4箭], [第5箭], [平均],
    [甲], [8], [9], [7], [10], [6], [8],
    [乙], [9], [8], [8], [7], [8], [8],
  ))

  两人的平均成绩都等于 $8$ 环，但教练更愿意派乙上场。

  *平均数一样时，我们怎样比较谁发挥更稳定？*
]

#discovery[
  *第一步：先看“散不散”。*

  甲的数据是 $6, 7, 8, 9, 10$，乙的数据是 $7, 8, 8, 8, 9$。乙更集中在 $8$ 附近，甲更分散。

  *第二步：最粗糙的办法是看极差。*

  - 甲的极差：$10 - 6 = 4$
  - 乙的极差：$9 - 7 = 2$

  乙的极差更小，所以看起来更稳定。

  但极差只看了最大值和最小值，中间的数据都没发言。

  *第三步：让每个数据都发言。*

  我们想知道每一箭离平均数 $8$ 有多远：

  - 甲的偏差：$0, 1, -1, 2, -2$
  - 乙的偏差：$1, 0, 0, -1, 0$

  可是这些偏差直接相加会发生正负抵消，和总是 $0$。

  *第四步：那就把偏差平方，再求平均。*

  甲的偏差平方和是

  $0^2 + 1^2 + (-1)^2 + 2^2 + (-2)^2 = 10$

  所以甲的方差是

  $frac(10, 5) = 2$

  乙的偏差平方和是

  $1^2 + 0^2 + 0^2 + (-1)^2 + 0^2 = 2$

  所以乙的方差是

  $frac(2, 5) = 0.4$

  乙的方差更小，所以发挥更稳定。

  *第五步：能不能把计算再压缩一点？*

  如果每次都先求平均数，再求偏差，再平方，再平均，步骤有点长。能不能把定义式本身化简？这就是简化公式的来历。
]

#tryit[
  先自己算，再看蓝图：

  数据组：$4, 6, 5, 3, 7$

  + 平均数是多少？
  + 极差是多少？
  + 方差是多少？

  特别留意：方差的顺序是“先减平均数，再平方，再平均”。
]

#side-hack[
  *方差四步走*：

  + 求平均数 $bar(x)$
  + 求每个数据到平均数的偏差
  + 把偏差平方
  + 把这些平方再平均
]

#blueprint[
  === 极差

  $ "极差" = "最大值" - "最小值" $

  极差反映数据的变化幅度，但只用了两个极端数据，信息量有限。

  === 方差

  设有 $n$ 个数据 $x_1, x_2, dots.h, x_n$，平均数为 $bar(x)$，则*方差*定义为：

  $ S^2 = frac(1, n) [(x_1 - bar(x))^2 + (x_2 - bar(x))^2 + dots.h + (x_n - bar(x))^2] $

  即

  $ S^2 = frac(1, n) sum_(i=1)^(n) (x_i - bar(x))^2 $

  *方差的意义*：
  - 方差越大，数据越分散，波动越大
  - 方差越小，数据越集中，越稳定
  - 方差为 $0$ 当且仅当所有数据都相等

  === 简化计算公式的推导

  从定义出发：

  $ S^2
    &= frac(1, n) sum_(i=1)^(n) (x_i - bar(x))^2 \
    &= frac(1, n) sum_(i=1)^(n) (x_i^2 - 2 x_i bar(x) + bar(x)^2) \
    &= frac(1, n) sum_(i=1)^(n) x_i^2 - frac(2 bar(x), n) sum_(i=1)^(n) x_i + frac(1, n) sum_(i=1)^(n) bar(x)^2 $

  现在分别处理三部分：

  - $frac(1, n) sum x_i^2$ 是“平方的平均”，记作 $bar(x^2)$
  - 因为 $sum x_i = n bar(x)$，所以第二项变成 $2 bar(x)^2$
  - $bar(x)^2$ 一共被加了 $n$ 次，再除以 $n$，仍然是 $bar(x)^2$

  所以

  $ S^2 = bar(x^2) - bar(x)^2 $

  也就是：*方差 = 平方的平均 $-$ 平均的平方*。

  *例题 1*：数据 $3, 5, 7, 9, 6$ 的方差。

  解：$bar(x) = frac(3 + 5 + 7 + 9 + 6, 5) = 6$

  $ S^2 = frac((3-6)^2 + (5-6)^2 + (7-6)^2 + (9-6)^2 + (6-6)^2, 5) = frac(9 + 1 + 1 + 9 + 0, 5) = 4 $

  *例题 2*：用简化公式验证同一题。

  $ bar(x^2) = frac(9 + 25 + 49 + 81 + 36, 5) = 40 $

  所以

  $ S^2 = bar(x^2) - bar(x)^2 = 40 - 6^2 = 4 $

  *例题 3*：已知一组数据 $a, a, a, a, a$，方差是多少？

  解：$bar(x) = a$，每个偏差都等于 $0$，所以方差 $S^2 = 0$。

  *例题 4*：如果每个数据都加上常数 $c$，或都乘以常数 $k$，方差怎样变化？

  解：
  - 每个数据加 $c$：偏差不变，所以方差不变
  - 每个数据乘 $k$：每个偏差都乘了 $k$，平方后变成原来的 $k^2$ 倍，所以方差变为 $k^2 S^2$
]

#pitfall[
  *陷阱 1：顺序不能乱。*

  一定是“先减平均数，再平方，再平均”，不是“先平均再平方”。

  #grid(columns: (1fr, 1fr), gutter: 8pt,
    block[*正确*：\ $S^2 = frac(1,n) sum (x_i - bar(x))^2$\ （差先平方，再平均）],
    block[*错误*：\ 先把平均数平方\ （这是 $bar(x)^2$，不是方差）]
  )

  *陷阱 2：方差越大，数据越分散；方差越小，数据越稳定。* 不要把方向弄反。

  *陷阱 3：$bar(x^2)$ 和 $bar(x)^2$ 不是一回事。*

  一个是“平方后再平均”，一个是“平均后再平方”，通常不会相等。
]

#mastery[
  *基础*

  + 求数据 $4, 6, 5, 3, 7$ 的极差和方差。
  + 甲组数据：$70, 72, 71, 69, 68$；乙组数据：$65, 75, 80, 60, 70$。分别求方差，判断哪组更稳定。
  + 一组数据的方差为 $3$，如果每个数据都乘以 $2$ 再减去 $5$，新数据的方差是多少？

  *应用*

  + 某工厂生产零件，要求直径为 $10$ 毫米。甲、乙两台机器各生产 $5$ 个零件，直径如下：
    - 甲：$10.1, 9.9, 10.2, 9.8, 10.0$
    - 乙：$10.0, 10.1, 10.0, 9.9, 10.0$

    哪台机器的精度更高？（精度高 = 方差小）

  *挑战*

  + 已知 $5$ 个数据的平均数为 $10$，方差为 $2$。如果再增加一个数据 $10$，新的 $6$ 个数据的方差是多少？
  + 已知某组数据的平均数为 $6$，方差为 $4$。如果每个数据都先减去 $2$，再乘以 $3$，新数据的平均数和方差各是多少？

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + 极差 $= 7 - 3 = 4$。$bar(x) = 5$，方差 $= frac((4-5)^2 + (6-5)^2 + (5-5)^2 + (3-5)^2 + (7-5)^2, 5) = frac(10, 5) = 2$。
  + 甲：$bar(x) = 70$，方差 $= frac(0 + 4 + 1 + 1 + 4, 5) = 2$。乙：$bar(x) = 70$，方差 $= frac(25 + 25 + 100 + 100 + 0, 5) = 50$。甲组更稳定。
  + 乘以 $2$ 后方差变为 $4 times 3 = 12$，再减去 $5$ 不影响方差，所以仍为 $12$。

  *应用*
  + 甲：$bar(x) = 10$，方差 $= frac(0.01 + 0.01 + 0.04 + 0.04 + 0, 5) = 0.02$。乙：$bar(x) = 10$，方差 $= frac(0 + 0.01 + 0 + 0.01 + 0, 5) = 0.004$。乙的方差更小，所以精度更高。

  *挑战*
  + 原来 $sum (x_i - 10)^2 = 5 times 2 = 10$。新增的数据正好等于平均数，所以新增偏差平方是 $0$。新平均数仍为 $10$，新方差 $= frac(10, 6) = frac(5, 3) approx 1.67$。
  + 先减去 $2$，平均数变为 $4$，方差仍为 $4$；再乘以 $3$，平均数变为 $12$，方差变为 $9 times 4 = 36$。
]
