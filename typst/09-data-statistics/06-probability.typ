#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §9.6 概率：先把结果列清楚 <sec-9-6>

#vocab[事件 event / 样本空间 sample space / 概率 probability]

#crisis[
  天气预报说“明天下雨概率 $70%$”。有人说：“那就还是可能不下，等于没说。”也有人说：“是不是代表城市里有 $70%$ 的地方会下雨？”

  *概率到底在说什么？怎样把“可能性”说成一个有意义的数字？*
]

#history-note[
  概率论最早出名，居然是因为赌局问题。人们想比较游戏谁更占优势，才一步步学会先把可能结果列清楚，再比较各自占了多大一部分。
]

#discovery[
  先看一枚均匀硬币。

  所有可能结果只有两种：$
  \{"正", "反"\}$。

  如果我们关心的事件是“出现正面”，那这个事件在全部可能结果里占了一半，所以它的可能性就是 $1/2$。

  再看一枚均匀骰子。所有可能结果是 $
  \{1, 2, 3, 4, 5, 6\}$。

  - 事件“点数为偶数”对应结果 $
  \{2, 4, 6\}$
  - 事件“点数大于 4”对应结果 $
  \{5, 6\}$

  只要先把全部可能结果列清楚，再看目标事件占了几份，概率就有了落脚点。
]

#tryit[
  先自己算一算：

  掷一枚均匀骰子，求：

  + 点数为偶数的概率
  + 点数大于 4 的概率
  + 点数不大于 4 的概率
]

#side-hack[
  *算概率前先问两句*：

  + 这些结果是不是等可能
  + 我能不能先把所有可能结果列清楚
]

#blueprint[
  === 三个入口

  - *事件*：你正在关心的那件事，例如“掷出偶数”“抽到红球”
  - *样本空间*：这次实验的全部可能结果
  - *概率*：目标事件在全部可能结果里所占的份额

  === 等可能情形下怎样算概率

  如果样本空间里的结果都等可能，那么：

  概率 $= frac("有利结果数", "全部结果数")$

  这时概率一定在 $0$ 和 $1$ 之间。

  例如掷一枚均匀骰子，事件“点数小于 3”的有利结果是 $
  \{1, 2\}$，全部结果共有 6 个，所以概率是 $frac(2, 6) = frac(1, 3)$。

  再例如两枚硬币一起掷，样本空间可列为：

  $ (“正”,”正”), (“正”,”反”), (“反”,”正”), (“反”,”反”) $

  事件”恰好一枚为正”占其中 2 种，所以概率是 $frac(2, 4) = frac(1, 2)$。

  === 对立事件

  事件 $A$ 不发生，记作它的*对立事件*，记号通常写成 $overline(A)$。它满足

  $ P(overline(A)) = 1 - P(A) $

  这在”直接数有利结果太麻烦、反过来数更快”时很有用。

  例：两枚硬币一起掷，求至少有一枚正面的概率。

  直接列举可得 4 种结果，其中只有 $(“反”,”反”)$ 不满足。所以

  $ P(#[至少一正面]) = 1 - P(#[全反面]) = 1 - frac(1, 4) = frac(3, 4) $
]

#pitfall[
  *常见错误 1：没有先把样本空间列完整。*

  漏掉结果，后面的概率就会跟着错。

  *常见错误 2：忘了检查“等可能”前提。*

  如果骰子偏重、抽签机会不一样，就不能直接用“有利结果数 $div$ 总结果数”。

  *常见错误 3：把“小概率”理解成“不可能”。*

  概率小，仍然可能发生。
]

#mastery[
  *基础*

  + 写出下列实验的样本空间：
    - 掷一枚均匀硬币
    - 掷一枚均匀骰子
  + 掷一枚均匀骰子，求点数为奇数的概率。
  + 从数集 $\{1, 2, 3, 4, 5\}$ 中随机取一个数，求取到素数的概率。

  *应用*

  + 袋中有 3 个红球、2 个蓝球，随机取 1 个。求取到红球和取到蓝球的概率。
  + 两枚硬币一起掷，求：
    - 两枚都为正面的概率
    - 恰好一枚为正面的概率

  *挑战*

  + 从数集 $\{2, 4, 5, 7, 9, 10\}$ 中随机取一个数。事件 A：取到偶数；事件 B：取到大于 6 的数。分别求它们的概率，并比较哪个更大。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  + 硬币样本空间是 $\{"正", "反"\}$；骰子样本空间是 $\{1, 2, 3, 4, 5, 6\}$。
  + 奇数有 1、3、5，所以概率是 $frac(3, 6) = frac(1, 2)$。
  + 素数有 2、3、5，共 3 个，所以概率是 $frac(3, 5)$。
  + 取到红球的概率是 $frac(3, 5)$，取到蓝球的概率是 $frac(2, 5)$。
  + 两正的概率是 $frac(1, 4)$；恰好一正的概率是 $frac(2, 4) = frac(1, 2)$。
  + 事件 A 的概率是 $frac(3, 6) = frac(1, 2)$；事件 B 的概率也是 $frac(3, 6) = frac(1, 2)$，二者一样大。
]
