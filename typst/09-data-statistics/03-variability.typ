#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §9.3 数据的波动程度 <sec-9-3>

#crisis[
  两位同学各练习了 5 次投篮：

  #align(center, table(
    columns: 7,
    align: center,
    [], [第 1 次], [第 2 次], [第 3 次], [第 4 次], [第 5 次], [平均],
    [甲], [8], [10], [6], [9], [7], [8],
    [乙], [8], [8], [9], [7], [8], [8],
  ))

  两人的平均命中数都等于 $8$，可教练还是觉得乙更稳。

  *当平均数相同时，我们怎样把“更稳”“波动更小”说清楚？*
]

#history-note[
  只看平均数，常常会把“稳定”和“起伏大”混在一起。于是统计学家开始研究：每个数据离平均数到底有多远？这种“离中心有多散”的想法，后来发展出专门衡量波动大小的办法。
]

#discovery[
  先看两组数据：

  - 甲：$6, 7, 8, 9, 10$
  - 乙：$7, 8, 8, 8, 9$

  它们的平均数都等于 $8$，但乙明显更集中。

  *第一种办法：看最大值和最小值的差。*

  - 甲：$10 - 6 = 4$
  - 乙：$9 - 7 = 2$

  乙这一步得到的差更小，所以波动更小。

  可是这种“最大减最小”的办法只看两个端点，中间的数据还没有全部参与。

  *第二种办法：看每个数据离平均数有多远。*

  甲相对平均数 $8$ 的差是：$-2, -1, 0, 1, 2$

  乙相对平均数 $8$ 的差是：$-1, 0, 0, 0, 1$

  直接把这些差相加会互相抵消，所以我们把它们平方后再求平均。

  甲得到：

  $ frac(4 + 1 + 0 + 1 + 4, 5) = 2 $

  乙得到：

  $ frac(1 + 0 + 0 + 0 + 1, 5) = 0.4 $

  这个平方后的平均越小，说明数据越集中、越稳定。
]

#tryit[
  先自己算：

  数据 $4, 5, 5, 6, 10$

  + 最大值减最小值是多少？
  + 平均数是多少？
  + 把每个数据离平均数的差平方后再平均，结果是多少？

  顺序记住：先减平均数，再平方，再平均。
]

#side-hack[
  *比稳定性时先问两句*：

  + 两组平均数是不是差不多
  + 如果平均数差不多，谁的“最大减最小”或“偏差平方平均”更小
]

#blueprint[
  === 极差

  $ "极差" = "最大值" - "最小值" $

  极差能快速反映数据跨度，但只用了两个数据。

  === 方差

  设数据为 $x_1, x_2, dots.h, x_n$，平均数为 $bar(x)$，则方差定义为

  $ S^2 = frac(1, n) sum_(i=1)^n (x_i - bar(x))^2 $

  它表示“所有数据离平均数的偏差平方的平均”。前面先用极差做快速比较，这里则是把每个数据都纳入计算，所以更能反映整体波动。

  - 方差越大，数据越分散
  - 方差越小，数据越集中
  - 方差等于 $0$，说明所有数据都相同

  真正算题时，按定义一步一步做最稳；想加快时，也可以把它改写成熟悉的等价形式。计算时也常用

  $ S^2 = bar(x^2) - bar(x)^2 $

  也就是：*平方的平均 $-$ 平均的平方*。

  例：数据 $3, 5, 7, 9, 6$

  - 平均数：$bar(x) = frac(3 + 5 + 7 + 9 + 6, 5) = 6$
  - 极差：$9 - 3 = 6$
  - 方差：

  $ S^2 = frac((3 - 6)^2 + (5 - 6)^2 + (7 - 6)^2 + (9 - 6)^2 + (6 - 6)^2, 5) $
  $ = frac(9 + 1 + 1 + 9 + 0, 5) = 4 $

  有了“方差衡量波动”的主工具后，再看数据整体平移或整体伸缩时，方差会怎样跟着变，这样后面的简算与判断才有抓手。

  === 方差的变换规律

  - 每个数据都加同一个常数，方差不变
  - 每个数据都乘同一个常数 $k$，方差会变成原来的 $k^2$ 倍
]

#pitfall[
  *常见错误 1：把方差算成“先平均再平方”。*

  方差不是 $bar(x)^2$，而是“偏差平方的平均”。

  *常见错误 2：只看平均数就下结论。*

  平均数相同，并不代表稳定性相同。

  *常见错误 3：把“方差大”理解成“表现更好”。*

  方差大只表示更分散，不表示更优秀。
]

#mastery[
  *基础*

  + 求数据 $4, 5, 5, 6, 10$ 的极差和方差。
  + 已知两组数据平均数都为 $12$，甲的方差是 $1.2$，乙的方差是 $4.5$。哪组更稳定？
  + 一组数据方差为 $3$。若每个数据都加上 $5$，新方差是多少？

  *应用*

  + 甲、乙两台机器各生产 5 个零件，直径（毫米）分别为：
    - 甲：9.8、10.1、10.0、10.2、9.9
    - 乙：10.0、10.0、10.1、9.9、10.0

    哪台机器更稳定？说明理由。

  *挑战*

  + 某组数据平均数为 $6$，方差为 $2$。若每个数据都先减去 $1$，再乘以 $3$，新平均数和新方差各是多少？

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  + 极差 $= 10 - 4 = 6$；平均数 $= 6$；方差 $= frac(4 + 1 + 1 + 0 + 16, 5) = frac(22, 5) = 4.4$。
  + 甲组更稳定，因为在平均数相同的情况下，甲的方差更小。
  + 新方差仍是 $3$。
  + 两台机器平均都接近 10 毫米，但乙的数据更集中，方差更小，所以乙更稳定。
  + 新平均数先变成 $5$，再变成 $15$；新方差先不变，后乘 $3^2$，所以变成 $18$。
]
