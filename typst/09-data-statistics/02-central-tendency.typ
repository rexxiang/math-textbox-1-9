#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §9.2 平均数、中位数与众数 <sec-9-2>

#crisis[
  班主任说：“这次数学测验平均分 84 分。”可是你一打听，发现不少同学都在 70 多分，只有少数几位接近满分。

  *既然“平均”看起来挺高，为什么很多人却觉得这句话没把班里的真实情况讲清楚？*
]

#history-note[
  很多人第一次接触统计时，只记住了“求平均数”。但在真实世界里，工资、房价、比赛成绩、配送时间常常会出现极端值。于是统计学慢慢发展出不止一种“代表值”：有的更适合看整体平均水平，有的更适合看中间位置，有的更适合看最常见的情况。
]

#discovery[
  看一组数据：7 家商店一天卖出的冰淇淋数量分别是

  $ 18, quad 19, quad 20, quad 20, quad 21, quad 22, quad 40 $

  *先算大家熟悉的“平均”。*

  $ frac(18 + 19 + 20 + 20 + 21 + 22 + 40, 7) = frac(160, 7) approx 22.9 $

  可是这组数据里，大多数商店都在 18 到 22 之间，只有一家卖了 40。这个极端高值把平均数往上拉了。

  *再看排好序后正中间那个数。*

  把数据从小到大排好后，第 4 个数是 $20$。它告诉我们“站在正中间的水平”。

  *再看哪个数出现得最多。*

  $20$ 出现了 2 次，次数最多，所以它代表“最常见的情况”。

  于是同一组数据，可以有三种不同但都合理的概括：

  - 一个适合看整体平均水平
  - 一个适合看中间位置
  - 一个适合看最常见的取值
]

#tryit[
  先自己算一算：

  数据 $5, 6, 6, 7, 10$：

  + 加起来除以个数，结果是多少？
  + 排好序后正中间的数是多少？
  + 出现次数最多的数是多少？
]

#side-hack[
  *三种代表值怎么选*：

  + 想看整体平均水平——先想“总和 ÷ 个数”
  + 有明显极端值——优先看排好序后的中间位置
  + 想知道“最常见”——看哪个数出现最多
]

#blueprint[
  === 平均数

  $n$ 个数据 $x_1, x_2, dots.h, x_n$ 的平均数是

  $ bar(x) = frac(x_1 + x_2 + dots.h + x_n, n) $

  平均数会利用全部数据，但会受到极端值影响。

  === 中位数

  先把数据从小到大排列：

  - 数据个数为奇数：中间那个数就是中位数
  - 数据个数为偶数：中间两个数的平均数就是中位数

  例：$3, 5, 7, 9$ 的中位数是 $frac(5 + 7, 2) = 6$。

  === 众数

  一组数据里出现次数最多的数叫众数。

  - 可能只有一个众数
  - 也可能有两个或多个众数
  - 如果每个数都只出现一次，就没有众数

  #table(
    columns: (1fr, 1fr),
    align: (left, left),
    table.header([*数据特点*], [*更适合的代表值*]),
    [数据比较均匀、想看整体平均水平], [平均数],
    [有明显极端值、想看一般位置], [中位数],
    [想看最常见的取值], [众数],
  )

  先把三个代表值放到同一组数据里并排比较，就更容易看出它们各自负责回答什么问题。例：某组数据是 $8, 9, 9, 10, 13, 30$。

  - 平均数：$frac(8 + 9 + 9 + 10 + 13 + 30, 6) = frac(79, 6) approx 13.2$
  - 中位数：$frac(9 + 10, 2) = 9.5$
  - 众数：$9$

  因为 30 是极端高值，所以如果要描述“这一组的通常水平”，中位数往往比平均数更贴近大多数数据。
]

#pitfall[
  *常见错误 1：偶数个数据直接取其中一个作中位数。*

  偶数个数据要看中间两个，再取平均。

  *常见错误 2：把“平均数最大”误解成“最能代表整体”。*

  能不能代表整体，要看数据分布，不是看数值大不大。

  *常见错误 3：众数不一定存在，也不一定只有一个。*
]

#mastery[
  *基础*

  + 求数据 $5, 6, 6, 7, 10$ 的平均数、中位数、众数。
  + 求数据 $3, 5, 8, 8, 9, 12$ 的中位数。
  + 数据 $2, 2, 3, 3, 5, 7$ 的平均数、中位数、众数分别是什么？这组数据的众数有几个？

  *应用*

  + 某公司 6 名员工月薪（元）：3200、3400、3500、3600、3700、20000。分别求平均数和中位数，并判断向求职者介绍“一般工资水平”时更适合用哪个统计量。
  + 某地 7 天快递送达时间（天）为：2、2、3、3、3、4、8。求平均数、中位数、众数，并判断哪一个更适合描述“通常几天能到”。

  *挑战*

  + 某地 8 天快递送达时间（天）为：2、2、3、3、3、4、8、9。若商家想对顾客说“通常几天能到”，你会优先给出平均数、中位数还是众数？请算出这些统计量并解释你的选择。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  + 平均数 $= frac(5 + 6 + 6 + 7 + 10, 5) = 6.8$；中位数 $= 6$；众数 $= 6$。
  + 排序后中间两个数是 $8$ 和 $8$，中位数是 $8$。
  + 平均数 $= frac(22, 6) approx 3.7$；中位数 $= frac(3 + 3, 2) = 3$；众数有两个，是 $2$ 和 $3$。
  + 平均数 $= frac(3200 + 3400 + 3500 + 3600 + 3700 + 20000, 6) = 6233.3 dot dot dot$ 元；中位数 $= frac(3500 + 3600, 2) = 3550$ 元。若要介绍“一般工资水平”，更适合用*中位数*，因为 20000 元把平均数明显拉高了。
  + 平均数 $= frac(25, 7) approx 3.6$ 天；中位数 $= 3$ 天；众数 $= 3$ 天。若说“通常几天能到”，更适合用中位数或众数，因为最后一天的 8 天会把平均数拉高。
  + 平均数 $= frac(34, 8) = 4.25$ 天；中位数 $= frac(3 + 3, 2) = 3$ 天；众数 $= 3$ 天。若说“通常多久能到”，优先给*中位数*或*众数*都比平均数更稳，因为 8 天、9 天这两个慢件会把平均数拉高。
]
