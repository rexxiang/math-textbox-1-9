#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, secref, tryit, pitfall
#import "../lib/diagram-packages.typ": cetz

== §6.10 投影与三视图 <sec-6-10>

#vocab[投影 projection / 正投影 orthographic projection / 三视图 three views / 主视图 front view / 左视图 left (side) view / 俯视图 top view]

#crisis[
  建筑师设计一栋楼、工程师制造一台发动机——他们脑中的立体物体，最终必须画在*一张纸*上，交给工人去建造。

  拍一张照片？照片有透视变形，远小近大，无法直接量尺寸。

  *如何把三维物体准确、无歧义地表达在二维图纸上？*
]

#history-note[
  *达·芬奇*（Leonardo da Vinci）在 15 世纪的手稿中大量使用多角度投影来描绘机械装置。18 世纪法国数学家*蒙日*（Gaspard Monge）将这种方法发展为*画法几何*（descriptive geometry），奠定了现代工程制图的数学基础。今天全世界的工程图纸仍遵循正投影的标准——一个零件，三张视图加上尺寸标注，是表达形状的基本手段。
]

#discovery[
  *实验*：用手电筒照射一个小物体，观察它在墙上的影子。

  - 手电筒*斜着照*（如从左上方），影子会变形、拉长——这叫*斜投影*。
  - 把手电筒移到*很远的正前方*（光线近似平行，且垂直于墙面），影子的形状就最接近物体的"真实轮廓"——这就是*正投影*。

  再换个方向：从*正上方*往下照，影子又不同了。

  *关键发现*：从不同方向做正投影，得到的轮廓不同。一个方向往往不够——需要多个方向组合，才能完整描述物体。

  *试一试*：拿一个圆柱形水杯。从正前方看是什么形状？从正上方看呢？从右侧看呢？
]

#tryit[
  拿出纸笔，凭直觉画一画：

  一个正方体（如骰子），从正前方看、从正上方往下看、从左侧看，分别会是什么形状？

  先把三个形状画出来，再翻到下面的表格对照——你画对了吗？
]

#blueprint[
  *正投影*

  光线平行且垂直于投影面，物体在投影面上的影像叫*正投影*。正投影保持平行于投影面的线段长度不变、平行线仍然平行——这正是工程制图需要的性质。

  *三视图*

  用三个互相垂直的方向对物体做正投影，得到三张视图：

  #table(
    columns: (auto, auto, auto),
    align: (center, center, center),
    [*视图名称*], [*观察方向*], [*所见*],
    [主视图（正视图）], [从正前方看], [物体的正面轮廓],
    [俯视图], [从正上方往下看], [物体的顶部轮廓],
    [左视图], [从左侧看], [物体的左侧轮廓],
  )

  *摆放规则*（国标）：

  #figure(
    cetz.canvas(length: 1cm, {
      import cetz.draw: *
      // 主视图框（左上）
      rect((0, 3), (4, 6), stroke: 0.6pt)
      content((2, 4.5), text(9pt)[主视图])
      // 俯视图框（左下）——与主视图左对齐、宽度相同
      rect((0, 0), (4, 2.5), stroke: 0.6pt)
      content((2, 1.25), text(9pt)[俯视图])
      // 左视图框（右上）——与主视图顶对齐、高度相同
      rect((5, 3), (8, 6), stroke: 0.6pt)
      content((6.5, 4.5), text(9pt)[左视图])
      // 对齐辅助线（虚线）
      line((4, 5.8), (5, 5.8), stroke: (dash: "dashed", paint: luma(160), thickness: 0.4pt))
      line((4, 3.2), (5, 3.2), stroke: (dash: "dashed", paint: luma(160), thickness: 0.4pt))
      line((1, 3), (1, 2.5), stroke: (dash: "dashed", paint: luma(160), thickness: 0.4pt))
      line((3, 3), (3, 2.5), stroke: (dash: "dashed", paint: luma(160), thickness: 0.4pt))
      // 标注对齐关系
      content((2, 2.8), anchor: "north", text(7.5pt, fill: blue)[长对正], padding: 1pt)
      content((4.5, 4.5), anchor: "center", text(7.5pt, fill: blue)[高平齐], padding: 1pt)
    }),
    caption: [三视图摆放规则：主视图在左上，俯视图在左下，左视图在右上],
  )

  三条对齐原则：*长对正*（主视图与俯视图宽度对齐）、*高平齐*（主视图与左视图高度对齐）、*宽相等*（俯视图的上下宽度 = 左视图的左右宽度）。

  #side-hack[
    记忆口诀：*"主俯长对正，主左高平齐，俯左宽相等"*。
  ]

  *常见几何体的三视图*

  #side-hack[
    *阅读提示*：三视图与物体的摆放方式有关。下面统一采用最常见的摆放方式：圆柱、圆锥的轴线竖直；三棱柱的侧棱竖直，且一个侧面朝向主视图。
  ]

  #table(
    columns: (auto, 1fr, 1fr, 1fr),
    align: center,
    [*几何体*], [*主视图*], [*俯视图*], [*左视图*],
    [正方体], [正方形], [正方形], [正方形],
    [圆柱], [矩形], [圆], [矩形],
    [圆锥], [等腰三角形], [圆（带圆心）], [等腰三角形],
    [三棱柱], [矩形], [三角形], [矩形],
    [球], [圆], [圆], [圆],
  )

  *看不见的棱用虚线*：三视图中，能看到的轮廓画实线，被遮挡的棱画*虚线*。

  *例 1*：一个底面为正方形的四棱锥（金字塔形），主视图和左视图都是等腰三角形，俯视图是正方形加两条对角线（从顶点到四个底角的投影）。

  *例 2*：一个圆柱上面叠一个小圆锥（像铅笔尖）。主视图是下方矩形 + 上方三角形；俯视图是两个同心圆；左视图与主视图相同。
]

#pitfall[
  *陷阱 1*：三视图的三个方向容易搞混——

  #grid(columns: (1fr, 1fr, 1fr), gutter: 8pt,
    block[*主视图*：\ 从正*前方*看],
    block[*俯视图*：\ 从正*上方*往下看],
    block[*左视图*：\ 从*左侧*看]
  )

  「正视图」和「主视图」是同一个东西；「侧视图」通常指左视图。

  *陷阱 2*：三视图中被遮挡的棱要用*虚线*，可见轮廓用实线。画图时不要把虚实线搞混——漏画虚线会让人误以为那条棱不存在。
]

#mastery[
  *基础*

  + 正投影与斜投影的主要区别是什么？为什么工程制图采用正投影？
  + 分别说出三视图的三条对齐原则。
  + 写出球体的三个视图各是什么形状。

  *应用*

  + 一个圆柱体的三视图中，主视图是宽 $4 "cm"$、高 $6 "cm"$ 的矩形，俯视图是直径 $4 "cm"$ 的圆。求圆柱的底面半径和高。
  + 画出三棱柱（底面为等边三角形，侧面为矩形）的三视图草图，标注实线和虚线。
  + 按上面的默认摆放方式，某物体的主视图是矩形，俯视图是圆，左视图也是矩形。这个物体最可能是什么几何体？

  *挑战*

  + 某物体的三视图如下：主视图是"T"形（上宽下窄的矩形组合），俯视图是"十"字形，左视图也是"T"形。试想象并描述这个立体的形状。
  + 两个不同的立体，能否有完全相同的三视图？举例说明或解释原因。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + 正投影的光线平行且垂直于投影面，不会产生透视变形，平行于投影面的线段长度真实不变，适合精确量取尺寸。斜投影的光线倾斜，投影会拉伸变形。
  + 长对正（主视图与俯视图宽度对齐）、高平齐（主视图与左视图高度对齐）、宽相等（俯视图的纵向尺寸 = 左视图的横向尺寸）。
  + 球体的主视图、俯视图、左视图都是*圆*。

  *应用*
  + 主视图宽 $4 "cm"$ 对应底面直径，故半径 $r = 2 "cm"$；主视图高 $6 "cm"$ 就是圆柱的高 $h = 6 "cm"$。
  + 按上面的默认摆放方式：主视图为矩形（底边为三角形一边的长度，高为棱柱高度）；俯视图为等边三角形；左视图为矩形（宽度为三角形的高，高度为棱柱高度）。后面两个矩形中无虚线，主视图中也无虚线（所有棱可见）。
  + 按上面的默认摆放方式，最可能是圆柱体。（主视图和左视图为矩形，俯视图为圆。）

  *挑战*
  + 这是一个"十"字形截面的棱柱（或者说两个长方体十字交叉形成的组合体）。从前方和侧面看都呈"T"形，从上方看呈"十"字形。
  + 可以。因为三视图主要记录三个方向的投影轮廓，以及可见线和必要的隐藏线；不同的立体在某些特殊情况下可能恰好产生相同的三张投影。因此在工程实践中，三视图通常还需配合*剖面图*和*尺寸标注*，才能更可靠地确定零件形状。
]
