#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §4.2 相反数、绝对值与有理数运算 <sec-4-2>

#vocab[相反数 opposite number / 绝对值 absolute value / 有理数运算 rational-number operations]

#crisis[
  小林早上账户是 $-18$ 元，中午收到 25 元零花钱，下午又买文具花了 9 元。

  *负数进了计算，旧的加减乘除还怎么理解？*
]

#history-note[
  负数一旦进入账目、温度和方向问题，就不可能只停留在“会读”阶段。真正有用的是：把正负号、距离和运算规则接成同一套语言。
]

#discovery[
  - $5$ 和 $-5$ 分居 $0$ 两边，离 $0$ 一样远、方向相反。
  - $-4$ 到 $0$ 的距离是 4，所以“带负号”和“离 0 多远”要分开看。
  - 加上正数像向右走，加上负数像向左走。

  这样一来，像 $(-18) + 25 - 9$ 这种式子，就能被看成一串有方向的移动。
]

#tryit[
  *先自己算一算*：

  + 写出 7 和 $-3.2$ 的相反数。
  + 求 $abs(-6)$ 和 $abs(4)$。
  + 计算：$(-8) + 11 - 5$。
]

#blueprint[
  - *相反数*：在数轴上分居 $0$ 两边、离 $0$ 一样远的两个数
    - $a$ 的相反数是 $-a$
    - $a + (-a) = 0$
  - *绝对值*：一个数到 $0$ 的距离
    - $abs(-4) = 4$
    - $abs(4) = 4$
    - $abs(0) = 0$
  - *有理数运算*继续按规则工作
    - 同号相加：绝对值相加，符号不变
    - 异号相加：大绝对值减小绝对值，符号跟着绝对值较大的那个数
    - 减法改写成加上相反数：$a - b = a + (-b)$
    - 乘除法先看绝对值，再判符号：同号得正，异号得负

  *先看一个同号例子*：$(-7) - 3 = (-7) + (-3)$

  这相当于先站在 $-7$，再向左走 3 格。两步都朝同一个方向，所以离 $0$ 的距离直接变成 $7 + 3 = 10$，结果落在左边，是 $-10$。

  *再看一个异号例子*：$(-9) + 4$

  这相当于先向左走 9 格，再向右走 4 格。左右像在拔河，最后左边还剩 5 格，所以结果是 $-5$。换成规则说法，就是“大绝对值减小绝对值，符号跟着绝对值较大的那个数”。
]

#side-hack[
  *每次见到负号先问两句*：

  - 它在表示方向，还是在表示离 0 哪一边？
  - 这一步是在向右走，还是向左走？
]

#pitfall[
  ❌ 把绝对值算成负数
  → ✓ 绝对值表示距离，永远不小于 0。

  ❌ 做减法时还按原方向想
  → ✓ “减一个数”要改写成“加它的相反数”。
]

#mastery[
  *基础*

  + 写出 $-7$、$3.2$、$0$ 的相反数和绝对值。
  + 计算：$(-9) + 4$。
  + 计算：$6 - (-5)$。
  + 计算：$(-3) times (-8)$。

  *应用*

  + 小亮的账户余额是 $-18$ 元，后来收到 25 元零花钱，现在余额是多少？
  + 某地早晨气温是 $-6 degree$，中午上升了 $9 degree$，中午气温是多少？

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_
  + $-7$ 的相反数是 7，绝对值是 7；$3.2$ 的相反数是 $-3.2$，绝对值是 $3.2$；$0$ 的相反数和绝对值都是 $0$
  + $(-9) + 4 = -5$
  + $6 - (-5) = 11$
  + $(-3) times (-8) = 24$
  + $-18 + 25 = 7$ 元
  + $-6 + 9 = 3 degree$
]
