#import "../lib/theme-v2.typ": blueprint, mastery, secref

== 第四章回顾：数轴与代数桥

#blueprint[
  *知识速查卡*

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *数轴与正负方向*（#secref("4.1"), #secref("4.1A")）
      - 数轴上越靠右的数越大
      - 相反数到 $0$ 的距离相同、方向相反
      - 绝对值表示到 $0$ 的距离，永远非负
      - 减法可改写成加上相反数
      - 乘除法先看绝对值，再判符号：同号得正，异号得负
    ],
    block[
      *实数的入口*（#secref("4.3")）
      - $sqrt(a)$ 先当作一个确定的数来认识
      - $sqrt(4)=2$，带根号不一定是无理数
      - 常见无理数例子：$sqrt(2)$、$pi$
      - 近似值要用 $approx$
      - 有理数和无理数合起来叫实数
    ],
  )

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *字母与代数式*（#secref("4.4")）
      - 字母先是占位符，再是计算模板里的变量
      - 代数式表达数量关系，不一定有等号
      - 代入就是把字母换成具体数
      - 同一个情境里，同一个字母表示同一个量
    ],
    block[
      *方程直觉*（#secref("4.5")）
      - 方程在说“左右两边一样多”
      - undo 与 balance 都是在保持等式成立
      - 这一章只处理非常简单的一步方程
      - 求出答案后要代回原方程检验
    ],
  )
]

#mastery[
  *混合自测题*

  + 把 $-3$、$2$、$-1/2$ 按从小到大排列。

  + 写出 $6$、$-4$ 的相反数。

  + 求绝对值：$abs(-7)$，$abs(2.5)$

  + 计算：$(-8) + 11 - 5$

  + 计算：$4 - (-9) + (-3)$

  + 计算：$(-6) times 5$

  + 判断下列各数是有理数还是无理数：$sqrt(9)$，$sqrt(2)$，$0.75$，$pi$

  + 写出“一支钢笔 9 元，买 $x$ 支共多少钱”的代数式。

  + 当 $x = -3$ 时，求 $2x + 5$ 与 $abs(x)$ 的和。

  + 判断下列各式，哪些是方程：$3a + 1$，$3a + 1 = 10$，$x div 2 = 4$

  + 解方程：$x + 7 = 12$，并代回原方程检验。

  + 解方程：$3y = -15$
]

#line(length: 100%, stroke: 0.3pt + luma(200))
_参考答案：_

+ 数轴上越靠左越小，所以 $-3 < -1/2 < 2$

+ $6$ 的相反数是 $-6$；$-4$ 的相反数是 $4$

+ $abs(-7) = 7$；$abs(2.5) = 2.5$

+ 先算 $(-8) + 11 = 3$，再减 5，得 $-2$

+ 减负数就是加相反数：$4 - (-9) + (-3) = 4 + 9 - 3 = 10$

+ $(-6) times 5 = -30$

+ $sqrt(9)=3$，有理数；$sqrt(2)$ 无理数；$0.75$ 有理数；$pi$ 无理数

+ $9x$

+ 先代入：$2x + 5 = 2 times (-3) + 5 = -1$；再算绝对值 $abs(x) = 3$；所以和是 $2$

+ 方程有：$3a + 1 = 10$，$x div 2 = 4$

+ $x + 7 = 12$，两边都减 7，得 $x = 5$；代回检验：$5 + 7 = 12$，成立

+ $3y = -15$，所以 $y = -5$

*螺旋复习*

+ 回看 #secref("1.5") 的混合运算：计算 $3 + (-5) + 2$，并说说为什么可以先把后两个数看成一个整体。

+ 回看 #secref("1.6") 的加法运算律：计算 $(-4) + 9 + (-6)$，并说明怎样重新分组会更方便。

+ 回看 #secref("2.6") 的分数、小数互化：把 $-1.5$ 写成分数，并在数轴上说明它在 $-2$ 和 $-1$ 之间的什么位置。
