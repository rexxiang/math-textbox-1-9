#import "../lib/theme-v2.typ": history-note, side-hack, secref

_数轴与代数桥——把数放回位置，把关系写成式子。_

前面的算术基础，让我们学会了数数、运算、分数、小数，也学会了用图形和坐标描述位置。可一到“零下温度”“欠款”“一个数加 5 以后是多少”这些问题，旧工具就会显得不够用了：我们既需要更完整的数，也需要更灵活的表达方式。

这一章正是从共同基础主干走向代数深化分支的*桥*。它不急着堆公式，而是先把几件最关键的事讲清楚：

+ 为什么数轴上会出现负数，为什么绝对值说的是“离 0 多远”
+ 为什么像 $sqrt(2)$ 这样的数也应该被纳入数的世界
+ 为什么字母可以先当“占位符”，再变成表达关系的模板
+ 为什么方程的本质是“左右一样多”，而不是机械套步骤

本章只搭桥，不冲刺。整式分类、根式运算、完整的解方程流程、更多应用模型，会留到后面的代数深化章节再系统展开。这里的任务，是先把“位置、方向、距离、关系、平衡”这些共同语言建立起来。

#history-note[
  数学史上，很多重要概念都不是凭空发明出来的，而是在旧方法不够用时被“逼”出来的：负数来自盈亏与方向，实数来自测量与逼近，字母来自通用表达，方程来自配平与求未知。桥接章节的意义，就是把这些发明背后的共同动机重新看清。
]

#side-hack[
  *阅读路径建议*：

  - 想先把带符号的数真正想明白：从 #secref("4.1") 开始
  - 想知道数轴为什么不只装得下分数：接着读 #secref("4.2")
  - 想过渡到“用字母说关系”：重点看 #secref("4.3") 和 #secref("4.4")
]

本章的主线是：

- *#secref("4.1") 数轴上的正负与有理数*——把负数、相反数、绝对值和带符号运算放回数轴理解
- *#secref("4.2") 实数的入口*——轻轻推开实数的大门，只建立“平方根也是数、实数填满数轴”的直觉
- *#secref("4.3") 用字母写计算模板*——让字母先做占位符，再做关系表达的模板
- *#secref("4.4") 方程：左右一样多*——把方程理解成平衡关系，用 undo 和检验解决最简单的一步问题
- *本章回顾：数轴与代数的桥*——用混合自测和螺旋复习，把这座桥走稳
