#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §4.2 把数轴补完整 <sec-4-2>

#vocab[平方根记号 square root notation / $sqrt(2)$ 表示 2 的算术平方根]

#crisis[
  我们已经认识了很多有理数，可还有一个问题总会冒出来：

  *有没有一个数，自己乘自己正好等于 2？*

  $1^2 = 1$ 太小，$2^2 = 4$ 太大。

  试得更细一点：

  - $1.4^2 = 1.96$
  - $1.5^2 = 2.25$

  看来这个数夹在 $1.4$ 和 $1.5$ 之间，但它不是一个容易写完的分数。这时，原来的“有理数世界”又显得有点挤了。
]

#history-note[
  人们并不是先有完整的新名字，再去发现这些数；恰好相反，是在不断测量、逼近、比较的过程中，大家发现“有些量不像分数那样能写尽”，于是才慢慢建立起更完整的数的世界。
]

#discovery[
  *第一步*：先抓住一个“夹在中间”的长度。

  画一个边长为 1 的正方形，它的对角线长度不是 1，也不是 2，而是夹在它们之间的某个确定长度。

  用小数去逼近它：

  - 比 1.4 长一点
  - 比 1.5 短一点

  再细一点看：

  - 比 1.41 长一点
  - 比 1.42 短一点

  这样一步一步缩小范围，说明数轴上确实有一个点在等着我们把它标出来。

  *第二步*：逼近值不是精确值。

  $1.4$、$1.41$、$1.414$ 都越来越接近那个点，但它们还不是那个点本身。

  这提醒我们：*数轴上的点，不一定都能靠已经熟悉的分数或有限小数一次写完。*

  *第三步*：所以数轴还需要更完整的“名字系统”。

  已经学过的整数、分数、有限小数、循环小数能标出很多点；但像正方形对角线这样的长度告诉我们，*还有一些确定的点，不该被留成空白。*
]

#tryit[
  先自己判断，再往下看：

  + 如果一个点在 $1.4$ 和 $1.5$ 之间，它会不会因为“写不成整齐的分数”就从数轴上消失？
  + $1.4$、$1.41$、$1.414$ 这三个数，哪一个是精确值？还是它们都只是越来越接近？

  先用自己的话解释，再往下看。
]

#side-hack[
  *这一节先做“补数轴”的工作*：

  只要先认得“有些点需要用新的记号来命名”就够了，不把这一节扩成一整套新算术训练。
]

#blueprint[
  *一、先承认：数轴上还有需要新名字的点*

  边长为 1 的正方形，对角线长度是一个*确定的数*。虽然它不能像 $1.4$ 或 $1.41$ 那样被这些近似值完全代替，但它在数轴上有自己准确的位置。

  所以，数轴不是“熟悉的分数点排一排”就结束了；有些点需要我们另外命名。

  *二、给这个点一个简短记号*

  数学里，常把这个对角线长度记作 $sqrt(2)$。

  这一记号的作用，是把“那个一直夹在 $1.4$ 和 $1.5$ 之间、再细分又夹在 $1.41$ 和 $1.42$ 之间的准确长度”单独叫出来。

  #side-hack[
    本节先把 $sqrt(2)$ 当成“一个有确定位置的新记号”来认识；它的系统分类与运算，放到后面的学习里再展开。
  ]

  *三、桥上的结论：数轴不只靠熟悉分数来填满*

  我们已经知道：

  - $1.4 < sqrt(2) < 1.5$
  - $1.41 < sqrt(2) < 1.42$
  - $sqrt(2) approx 1.41$ 只是近似，不是精确相等

  *熟悉的分数只标出了数轴上的部分点，数轴上还有更多点等待新的记号。*
]

#pitfall[
  *常见错误 1：把近似值当成精确值。*

  $sqrt(2) approx 1.4$

  表示“接近”，不是“完全相等”。写成

  $sqrt(2) = 1.4$

  就错了。

  *常见错误 2：觉得“写不成熟悉分数”的点就不在数轴上。*

  正方形对角线的长度明明存在、也能定位；只是我们需要新的记号去叫它，而不是假装它不存在。
]

#mastery[
  *练一练*

  + 根据 $1.4^2 = 1.96$、$1.5^2 = 2.25$，判断 $sqrt(2)$ 在哪两个整数之间、哪两个一位小数之间。
  + 如果继续试算发现 $1.41^2 < 2 < 1.42^2$，这说明了什么？
  + 一个边长为 1 m 的正方形，它的对角线长度记作什么？约等于多少米？
  + 判断并改正：有人写出 $sqrt(2) = 1.41$
  + 用自己的话解释：为什么说“数轴不只靠分数就能填满”？

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  + $1^2 < 2 < 2^2$，所以 $sqrt(2)$ 在 1 和 2 之间；又因为 $1.4^2 < 2 < 1.5^2$，所以它也在 1.4 和 1.5 之间
  + 说明这个点还能继续被更细地逼近；$1.41$ 和 $1.42$ 仍然只是把它夹住，不是把它完全写完
  + 记作 $sqrt(2) "m"$，约等于 $1.41 "m"$
  + 应改成 $sqrt(2) approx 1.41$
  + 例如：因为像正方形对角线这样的长度也对应数轴上的确定点，而分数和有限小数只能不断逼近它，所以数轴上的点比我们熟悉的分数世界更完整
]
