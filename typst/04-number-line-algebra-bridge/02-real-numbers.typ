#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §4.2 实数的入口 <sec-4-2>

#vocab[平方根 square root / 无理数 irrational number / 实数 real number]

#crisis[
  我们已经认识了很多有理数，可还有一个问题总会冒出来：

  *有没有一个数，自己乘自己正好等于 2？*

  $1^2 = 1$ 太小，$2^2 = 4$ 太大。

  试得更细一点：

  - $1.4^2 = 1.96$
  - $1.5^2 = 2.25$

  看来这个数夹在 $1.4$ 和 $1.5$ 之间，但它不是一个容易写完的分数。这时，原来的“有理数世界”又显得有点挤了。
]

#history-note[
  人们并不是先有“实数”这个总名字，再去发现新数；恰好相反，是在不断测量、逼近、比较的过程中，大家发现“有些量不像分数那样能写尽”，于是才慢慢建立起更完整的数的世界。
]

#discovery[
  *第一步*：把“自己乘自己等于 2”的数记下来。

  我们把这个数记作 $sqrt(2)$，读作“根号二”。它表示那个满足

  $ x^2 = 2 $

  的正数。

  所以，根号不是装饰，而是在给一个*确定的数*命名。

  *第二步*：先看能开得尽的例子。

  - $sqrt(4) = 2$，因为 $2^2 = 4$
  - $sqrt(9) = 3$，因为 $3^2 = 9$
  - $sqrt(0.25) = 0.5$，因为 $0.5^2 = 0.25$

  这些平方根本身仍然是有理数。

  *第三步*：再看开不尽的例子。

  $sqrt(2)$ 不是整数，也很难写成一个能停下来的小数。继续算下去，会得到

  $ sqrt(2) approx 1.41421356 dots.c $

  像 $sqrt(2)$、$pi$ 这样的数，可以一直写下去，却不会进入“整齐重复”的循环节。我们把这类数叫做*无理数*。

  *第四步*：把有理数和无理数放在一起看。

  有理数能落在数轴上的很多点上，无理数也能落在数轴上的点上。把它们合在一起，数轴上的点才被完整地描述出来。

  这整个更完整的世界，叫做*实数*。
]

#tryit[
  先自己判断，再往下看：

  + $sqrt(4)$
  + $sqrt(2)$
  + $pi$
  + $-frac(3, 2)$

  哪些是有理数？哪些是无理数？
]

#blueprint[
  *一、平方根先当“数”来认识*

  如果一个非负数 $x$ 满足 $x^2 = a$，那么这个非负数叫做 $a$ 的算术平方根，记作 $sqrt(a)$。

  例如：

  - $sqrt(1) = 1$
  - $sqrt(16) = 4$
  - $sqrt(0) = 0$

  *二、有理数与无理数*

  - 有理数：整数、分数、有限小数、循环小数
  - 无理数：不能写成分数的数，常见例子有 $sqrt(2)$、$sqrt(3)$、$pi$

  #side-hack[
    “带根号”不等于“无理数”。先看能不能开得尽：$sqrt(4)=2$ 是有理数，$sqrt(2)$ 才是无理数。
  ]

  *三、实数把数轴填满*

  $ "实数" cases(
    "有理数" cases("整数", "分数", "有限小数", "循环小数"),
    "无理数"
  ) $

  数轴上的每一个点都对应一个实数；反过来，每一个实数都能在数轴上找到自己的位置。

  *四、近似值不是精确值*

  $sqrt(2) approx 1.41$ 表示“接近”，不是“完全相等”。在需要估算或测量时，常常会用近似值。
]

#pitfall[
  *常见错误 1：看到根号就判成无理数。*

  $sqrt(25)$ 其实等于 $5$，它是整数，也就是有理数。

  *常见错误 2：把近似值当成等于。*

  $sqrt(2) = 1.4$

  这是错的。正确的写法是：

  $sqrt(2) approx 1.4$

  *常见错误 3：觉得实数只是“更难写的数”。*

  实数不是“写法更复杂”而已，它是在告诉我们：数轴上的点，不只靠分数就能全部描述。
]

#mastery[
  *练一练*

  + 在下面两个整数之间填上合适的范围：$sqrt(2)$ 在哪两个整数之间？$sqrt(10)$ 在哪两个整数之间？

  + 判断下列各数是有理数还是无理数：$sqrt(9)$，$sqrt(2)$，$pi$，$-0.75$

  + 写出满足 $x^2 = 36$ 的两个数。

  + 一个正方形的面积是 $2 "m"^2$，它的边长可以记作什么数？约等于多少米？

  + 用“约等于”写出 $sqrt(5)$ 的一个一位小数近似值。

  + *挑战*：请用自己的话解释，为什么说“实数把数轴填满”。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  + $1^2 < 2 < 2^2$，所以 $sqrt(2)$ 在 1 和 2 之间；$3^2 < 10 < 4^2$，所以 $sqrt(10)$ 在 3 和 4 之间

  + $sqrt(9)=3$，有理数；$sqrt(2)$ 无理数；$pi$ 无理数；$-0.75$ 有理数

  + 满足 $x^2 = 36$ 的两个数是 $6$ 和 $-6$

  + 边长记作 $sqrt(2) "m"$，约等于 $1.41 "m"$

  + $sqrt(5) approx 2.2$

  + 例如：因为不但分数能在数轴上找到点，像 $sqrt(2)$、$pi$ 这样的数也能对应数轴上的点，所以要把有理数和无理数都合起来，才能把整条数轴上的点都描述出来
]
