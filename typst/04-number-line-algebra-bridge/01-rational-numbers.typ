#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §4.1 数轴上的正负与有理数 <sec-4-1>

#vocab[数轴 number line / 负数 negative number / 相反数 opposite number / 绝对值 absolute value / 有理数 rational number]

#crisis[
  早上妈妈给小林 30 元午餐费，放学前他又帮班级垫付了 50 元材料费。

  如果只用前面学过的“够不够减”，$30 - 50$ 好像卡住了；可现实里这件事一点也不神秘——他现在是*少了 20 元*。

  还有天气预报里的“零下 6 度”、电梯的“地下 2 层”、银行账户里的“欠款 80 元”，都在提醒我们：光靠 $0, 1, 2, 3, dots.h$ 已经不够用了。
]

#history-note[
  中国古代《九章算术》已经用不同颜色的算筹区分“正”和“负”，把收入、支出，盈余、亏空放进同一套计算里。负数并不是“反常”的数，而是把现实里相反方向、相反意义的量一起纳入计算后的自然结果。
]

#discovery[
  *第一步*：把数放到一条线上。

  先画出熟悉的部分：

  $ 0 #h(2em) 1 #h(2em) 2 #h(2em) 3 #h(2em) 4 #h(2em) arrow.r $

  再把这条线向左也延长，并且保持单位长度一样：

  $ arrow.l #h(1.5em) -4 #h(1.5em) -3 #h(1.5em) -2 #h(1.5em) -1 #h(1.5em) 0 #h(1.5em) 1 #h(1.5em) 2 #h(1.5em) 3 #h(1.5em) 4 #h(1.5em) arrow.r $

  这条线叫*数轴*。$0$ 右边表示正方向，$0$ 左边出现的新数叫*负数*。

  *第二步*：从“方向相反”认识负数。

  - 向东走 3 米，记作 $+3$
  - 向西走 3 米，记作 $-3$
  - 存入 80 元，记作 $+80$
  - 欠款 80 元，记作 $-80$

  正号和负号先表示“方向”或“意义”，然后才参加运算。

  *第三步*：认识相反数。

  $5$ 和 $-5$ 在数轴上分居 $0$ 的两边，离 $0$ 一样远，但方向相反，所以它们互为*相反数*。

  任何数和它的相反数相加都回到 $0$：

  $ 5 + (-5) = 0 $

  *第四步*：认识绝对值。

  数 $-4$ 到 $0$ 的距离是 $4$，数 $4$ 到 $0$ 的距离也是 $4$。这个“离 $0$ 有多远”的量叫*绝对值*。

  - $abs(-4) = 4$
  - $abs(4) = 4$
  - $abs(0) = 0$

  绝对值说的是*距离*，所以永远不会是负数。

  *第五步*：把分数、小数也放上数轴。

  $-1.5$、$frac(2, 3)$、$-0.2$ 都能在数轴上找到位置。像这样能写成整数、分数，或者有限小数、循环小数的数，统称为*有理数*。
]

#tryit[
  先不要往下看，自己在数轴思路里算一算：

  $ (-3) + 5 - (-2) $

  提示：

  + 把减去一个数改写成加上它的相反数
  + 想一想每一步是在数轴上往左走还是往右走
]

#blueprint[
  *一、在数轴上比较大小*

  数轴上越靠右的数越大，越靠左的数越小。

  - $3 > -2$
  - $-1 > -5$
  - $0 > -0.8$

  *二、相反数与绝对值*

  - $a$ 的相反数是 $-a$
  - $a + (-a) = 0$
  - $abs(a)$ 表示 $a$ 到 $0$ 的距离

  #side-hack[
    看到绝对值时，先别急着想公式，先问一句：*它离 0 有多远？*
  ]

  *三、有理数加减法*

  *加法*可以理解成在数轴上移动：

  - 加上正数：向右走
  - 加上负数：向左走

  规则可以概括成：

  - 同号相加：把绝对值相加，符号不变
  - 异号相加：用较大的绝对值减较小的绝对值，符号跟着绝对值较大的那个数

  例如：

  - $(-3) + (-4) = -7$
  - $(-7) + 4 = -3$

  *减法*改写成加法更稳：

  $ a - b = a + (-b) $

  例如：

  $ (-3) + 5 - (-2) = (-3) + 5 + 2 = 4 $

  *四、有理数乘除法*

  乘除法的大小先按绝对值算，符号再单独判断：

  - 同号得正：$(-3) times (-4) = 12$
  - 异号得负：$(-3) times 4 = -12$
  - $0$ 乘任何数都等于 $0$

  除法和乘法的符号规律相同，例如：

  - $(-24) div 6 = -4$
  - $(-24) div (-6) = 4$

  *五、运算律没有失效*

  加法交换律、结合律，乘法交换律、结合律，分配律，在有理数里仍然成立。所以前面章节积累的算术经验，现在依然能用。
]

#pitfall[
  *常见错误 1：把负号当成数的一部分，却忘了整体移动。*

  例如：

  $ (-8) - 3 = -5 $

  这是错的。因为“减 3”表示再向左走 3 格，所以：

  $ (-8) - 3 = -11 $

  *常见错误 2：把绝对值算成负数。*

  $ abs(-6) = -6 $

  这是错的。绝对值表示距离，所以：

  $ abs(-6) = 6 $

  *常见错误 3：觉得负数里数字大的就更大。*

  在负数里，越靠左反而越小，所以：

  $ -9 < -2 $
]

#mastery[
  *练一练*

  + 在数轴上按从左到右顺序排列：$-2.5, 0, 3, -1, frac(1, 2)$

  + 写出下列各数的相反数和绝对值：$-7$，$3.2$，$0$

  + 计算：$(-9) + 4$

  + 计算：$6 - (-5)$

  + 计算：$(-3) times (-8)$

  + 计算：$35 div (-7)$

  + 小亮的账户余额是 $-18$ 元，后来收到 25 元零花钱，现在余额是多少？

  + 某地早晨气温是 $-6 degree$，中午上升了 $9 degree$，中午气温是多少？

  + *挑战*：已知 $abs(a) = 5$，写出 $a$ 可能取的值。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  + 从左到右：$-2.5, -1, 0, frac(1, 2), 3$

  + $-7$ 的相反数是 $7$，绝对值是 $7$；$3.2$ 的相反数是 $-3.2$，绝对值是 $3.2$；$0$ 的相反数和绝对值都是 $0$

  + $(-9) + 4 = -5$

  + $6 - (-5) = 11$

  + $(-3) times (-8) = 24$

  + $35 div (-7) = -5$

  + $-18 + 25 = 7$（元）

  + $-6 + 9 = 3 degree$

  + $a = 5$ 或 $a = -5$
]
