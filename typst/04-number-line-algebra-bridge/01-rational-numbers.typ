#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref

== §4.1 数轴、正负方向与有理数入口 <sec-4-1>

#crisis[
  早上妈妈给小林 30 元午餐费，放学前他又帮班级垫付了 50 元材料费。

  如果只用前面学过的“够不够减”，$30 - 50$ 好像卡住了；可现实里这件事一点也不神秘——他现在是*少了 20 元*。

  还有天气预报里的“零下 6 度”、电梯的“地下 2 层”、银行账户里的“欠款 80 元”，都在提醒我们：光靠 $0, 1, 2, 3, dots.h$ 已经不够用了。
]

#history-note[
  中国古代《九章算术》已经用不同颜色的算筹区分“正”和“负”，把收入、支出，盈余、亏空放进同一套计算里。负数并不是“反常”的数，而是把现实里相反方向、相反意义的量一起纳入计算后的自然结果。
]

#discovery[
  *第一步*：把熟悉的刻度线向两边都延长。

  先画出熟悉的部分：

  $ 0 #h(2em) 1 #h(2em) 2 #h(2em) 3 #h(2em) 4 #h(2em) arrow.r $

  再把这条线向左也延长，并且保持单位长度一样：

  $ arrow.l #h(1.5em) -4 #h(1.5em) -3 #h(1.5em) -2 #h(1.5em) -1 #h(1.5em) 0 #h(1.5em) 1 #h(1.5em) 2 #h(1.5em) 3 #h(1.5em) 4 #h(1.5em) arrow.r $

  这样一来，$0$ 的右边和左边都能表示位置，而且“离 $0$ 一样远”的位置会成对出现。

  *第二步*：先把“方向相反”的量写出来。

  - 向东走 3 米，记作 $+3$
  - 向西走 3 米，记作 $-3$
  - 存入 80 元，记作 $+80$
  - 欠款 80 元，记作 $-80$

  这里的正号和负号，先是在区分“方向”或“意义”，然后才进入计算。

  *第三步*：把分数、小数也放到同一条线上。

  $-1.5$、$frac(2, 3)$、$-0.2$ 都能在这条线上找到位置。可见，整数、分数、小数并不是三套互不相干的东西，而是在同一个位置系统里一起工作。
]

#tryit[
  先不要往下看，自己试一试：

  + 把 $-2.5, 0, 3, -1, frac(1, 2)$ 放到刚才那条延长后的线上，想一想谁更靠左，谁更靠右。
  + 哪些数比 $-1$ 更靠左？哪些数比 $-1$ 更靠右？
]

#blueprint[
  #vocab[数轴 number line / 负数 negative number / 有理数 rational number]

  *一、数轴把正负号放进同一张位置图里*

  把数按顺序排在一条直线上，这条线叫*数轴*。

  - 规定一个原点，记作 $0$
  - 向右是正方向
  - 向左出现的数叫*负数*
  - 数轴上越靠右的数越大，越靠左的数越小

  例如：

  - $3 > -2$
  - $-1 > -5$
  - $0 > -0.8$

  *二、有理数把整数、分数、小数接到一起*

  像整数、分数、有限小数、循环小数这样都能放到数轴上的数，统称为*有理数*。

  *三、运算律没有失效*

  在 #secref("1.6") 和 #secref("1.7") 里学过的加法交换律、结合律，乘法交换律、结合律，分配律，在有理数里仍然成立。所以前面章节积累的算术经验，现在依然能用。

]

#pitfall[
  *常见错误 1：觉得负数里数字大的就更大。*

  在负数里，越靠左反而越小，所以：

  $ -9 < -2 $

  *常见错误 2：把“能放上数轴的小数和分数”看成另一套数。*
  → ✓ 只要能落到数轴上，它们就都属于同一个数的世界。
]

#mastery[
  *练一练*

  + 在数轴上按从左到右顺序排列：$-2.5, 0, 3, -1, frac(1, 2)$

  + 判断大小：$-3$ 和 $1/2$ 谁更大？$-1.5$ 和 $-0.8$ 谁更大？
  + 在数轴上说明为什么 $0 > -0.8$。
  + 把 $-1.5$、$0$、$3$、$-1$、$frac(1, 2)$ 按从左到右顺序排列。
  + 回看前面的方格纸定位练习：如果点 $A(-2, 3)$ 和点 $B(4, -1)$ 出现在同一张坐标纸上，它们分别要从原点往哪个方向走几格才能到达？

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  + 从左到右：$-2.5, -1, 0, frac(1, 2), 3$

  + $1/2 > -3$；$-0.8 > -1.5$
  + 因为 0 在 $-0.8$ 的右边
  + 从左到右：$-2.5, -1, 0, frac(1, 2), 3$
  + 点 $A$：先向左 2 格，再向上 3 格；点 $B$：先向右 4 格，再向下 1 格。这里只是在复用正负号的方向意义，不是开启新的坐标章节。
]
