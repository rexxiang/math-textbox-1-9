#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §4.3 用字母写计算模板 <sec-4-3>

#vocab[字母表示数 variable / 代数式 algebraic expression]

#crisis[
  文具店里，每本练习册 6 元，一支笔 3 元。

  如果买 4 本练习册和 1 支笔，总价很容易算：

  $6 times 4 + 3 = 27$

  可如果练习册的本数还没定呢？今天买 4 本，明天买 7 本，后天买多少本还不知道。我们不可能每次都重新写一句完整的话。

  我们需要一种更灵活的写法：先把“数量的位置”空出来，再把具体数字填进去。
]

#history-note[
  代数里的字母，本来就不是为了“显得高级”，而是为了把重复出现的数量关系压缩成短短几笔。字母让我们把“这次是 4 本，下次是 7 本”这样的变化，放进同一个计算模板里。
]

#discovery[
  *第一步*：先把“位置”空出来。

  练习册每本 6 元，再加上一支 3 元的笔。总价其实一直都按同一个模板在算：

  $ "总价" = 6 times "练习册本数" + 3 $

  如果本数是 4，就得到 $6 times 4 + 3 = 27$；
  如果本数是 7，就得到 $6 times 7 + 3 = 45$。

  *第二步*：模板比一句完整的话更省力。

  与其每次都重写一句新算式，不如先把“本数位置”留着。等知道买几本时，再把那个位置填上。

  同样的想法还会出现在别的情境里：

  - 正方形周长 $= 4 times "边长"$
  - 5 分钟路程 $= 5 times "每分钟路程"$
  - 长方形周长 $= 2 times ("长" + "宽")$

  *第三步*：同一个空位，前后必须说的是同一个量。

  如果前面这个空位表示“练习册本数”，后面就不能忽然改成“钢笔支数”。模板能成立，靠的就是同一个位置始终表示同一个量。
]

#tryit[
  先自己写一写：

  + 一张门票 12 元，先不用字母，试着把“买若干张门票的总价”写成一个带空位的计算模板。
  + 一个长方形长 8 cm、宽还没定。先不用字母，试着写出它的周长模板。

  写完后，再往下看。
]

#blueprint[
  *一、字母表示数*

  当一个数量还没固定，或者我们想把很多相似情形压成同一个模板时，可以先用字母占住这个位置。

  例如：

  - 买了 $n$ 本练习册，总价里的“本数位置”先由 $n$ 占着
  - 正方形边长还没定时，可以先用 $a$ 表示边长
  - 每分钟跑 $x$ 米时，$x$ 表示“每分钟的路程”

  同一个情境里，同一个字母必须始终表示同一个量。

  *二、什么是代数式？*

  用数字、字母和运算符号组成的“计算模板”，叫做*代数式*。

  例如：

  - $5x$
  - $12m$
  - $2(l + w)$
  - $a - 3$

  *三、代入就是把占位符换成具体数字*

  已知 $x = 6$，求 $2x + 5$ 的值：

  $ 2x + 5 = 2 times 6 + 5 = 17 $

  写代数式前，最好先说“设什么表示什么”；求值时，再把字母换成具体数字。

  #side-hack[
    在代数里，$3x$ 表示 $3 times x$，这个乘号常常省略不写；但 $3 + x$ 绝不能写成 $3x$，因为“乘”和“加”不是一回事。
  ]

  顺便分清：

  - $3x + 2$ 是*代数式*，它在表达一个计算关系
  - $3x + 2 = 17$ 是*方程*，因为它有等号，在说左右两边相等

  下一节，我们专门研究这种“左右一样多”的关系。
]

#pitfall[
  *常见错误 1：同一个字母前后表示不同的量。*

  “每盒有 $x$ 个苹果，一共有 3 盒，又多送了 2 个苹果”，应该写成 $3x + 2$，不能把最后的 2 也偷偷塞进 $x$ 里。

  *常见错误 2：把 $2a$ 和 $a + 2$ 混为一谈。*

  - $2a$ 表示“两个 $a$ 相加”
  - $a + 2$ 表示“在 $a$ 的基础上再加 2”

  *常见错误 3：一看到字母就觉得是“未知、不能算”。*

  只要给出字母的值，代数式就能像普通算式一样代入求值。
]

#mastery[
  *练一练*

  + 每支钢笔 9 元，买 $x$ 支钢笔一共多少钱？请写出代数式。

  + 设一个数是 $a$，写出“这个数减 5”的式子。

  + 设边长为 $s$ cm，写出正方形的周长。

  + 当 $m = 5$ 时，求 $12m$ 的值。

  + 当 $x = -2$ 时，求 $3x + 1$ 的值。

  + 判断下列各式中，哪些是代数式，哪些是方程：$4y - 3$，$4y - 3 = 9$，$2(a+b)$

  + 小明坐出租车，起步价 8 元，每千米 2 元。设行驶了 $t$ 千米，总价怎么表示？当 $t = 6$ 时，总价是多少？

  + *挑战*：已知长方形长是宽的 2 倍。设宽是 $w$ cm，写出长方形周长的代数式。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  + $9x$

  + $a - 5$

  + $4s$

  + $12m = 12 times 5 = 60$

  + $3x + 1 = 3 times (-2) + 1 = -5$

  + $4y - 3$、$2(a+b)$ 是代数式；$4y - 3 = 9$ 是方程

  + 总价是 $8 + 2t$；当 $t = 6$ 时，$8 + 2 times 6 = 20$（元）

  + 长是 $2w$，周长 $= 2(2w + w) = 6w$
]
