#import "../lib/theme-v2.typ": blueprint, mastery, secref

== 第七章回顾：几何深化

#blueprint[
  *知识速查卡*

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *证明工具链*（#secref("7.1")、#secref("7.2")）
      - 全等判定：SSS、SAS、ASA、AAS、HL
      - 相似判定：AA、SAS、SSS
      - 相似比 $= k$，面积比 $= k^2$
      - 平行线常负责制造相等角
    ],
    block[
      *四边形与圆*（#secref("7.3")、#secref("7.4")）
      - 平行四边形：对边相等，对角相等，对角线互相平分
      - 矩形：平行四边形 + 直角；对角线相等
      - 菱形：平行四边形 + 四边相等；对角线互相垂直
      - 圆周角 = 同弧圆心角的一半
    ],
  )

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *测量工具链*（#secref("7.5")、#secref("7.6")）
      - 勾股定理：$a^2 + b^2 = c^2$
      - 逆定理：满足平方关系且最大边为 $c$ 时，可判定为直角三角形
      - $sin = frac("对边", "斜边")$
      - $cos = frac("邻边", "斜边")$
      - $tan = frac("对边", "邻边")$
    ],
    block[
      *表达与运动*（#secref("7.7")、#secref("7.8")）
      - 轴对称：对称轴垂直平分对应点连线
      - 中心对称：对应点连线被中心平分
      - 平移 / 旋转保持图形全等
      - 三视图：主视图、俯视图、左视图
    ],
  )
]

#mastery[
  *混合自测题*（不标知识点，综合调用）

  + 判断下列条件中，哪些能直接判定两个三角形全等：SSS、SAS、SSA、HL。

  + 两个相似三角形的对应边之比为 $3 : 5$，较小三角形面积为 $27$。求较大三角形面积。

  + 平行四边形 $A B C D$ 中，$angle A = 72 degree$。求 $angle B$、$angle C$、$angle D$。

  + 一条弧所对圆心角为 $110 degree$。求同弧所对圆周角。

  + 直角三角形两条直角边为 $8$ 和 $15$。求斜边。

  + 一棵树到观测点的水平距离是 20 米，树顶仰角是 $30 degree$。求树顶相对观测眼睛的高度。

  + 一个点 $(4, -1)$ 关于原点中心对称后的坐标是什么？

  + 某几何体主视图、左视图都是矩形，俯视图是圆。这个几何体最可能是什么？

  + 半径为 $6$ 的圆中，一条弦长为 $6$。求圆心到弦的距离。

  + *进阶*：等腰三角形三边之比为 $5 : 5 : 6$，周长为 $64$ cm。求底边上的高，并说明过程中调用了哪些旧工具。

  + *进阶*：一个坡道坡度为 $3 : 4$，水平距离为 40 米。求升高量与坡面长度。

  + *进阶*：解释为什么“先证三角形全等 / 相似”会成为很多四边形与圆题的共同入口。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  + SSS、SAS、HL 可以；SSA 不行，因为两边和一个非夹角不能锁定唯一形状，可能摆出两种三角形。

  + 面积比为 $9 : 25$，故较大面积 $= 27 times frac(25, 9) = 75$。

  + $angle B = 108 degree$，$angle C = 72 degree$，$angle D = 108 degree$。

  + 圆周角等于同弧圆心角的一半，所以是 $55 degree$

  + $17$

  + 用 $tan = frac("对边", "邻边")$：高度 $= 20 times tan 30 degree = frac(20 times sqrt(3), 3)$。

  + $(-4, 1)$

  + 圆柱。因为俯视图是圆，说明底面是圆；主视图、左视图都是矩形，说明侧面竖直展开后像长方形。

  + 圆心到弦的垂线会平分弦，所以半弦长为 $3$；再在直角三角形里用勾股：距离 $= sqrt(6^2 - 3^2) = 3 times sqrt(3)$。

  + 先用比求边长：总份数 $= 16$，每份 $= 64 div 16 = 4$，所以三边是 $20$、$20$、$24$。高把底边平分成两段各长 $12$，所以 $h = sqrt(20^2 - 12^2) = 16$。这里调用了 #secref("2.4") 的比、#secref("7.1") 的等腰结构和 #secref("7.5") 的勾股定理。

  + 升高量 $= 40 times frac(3, 4) = 30$ 米；坡面长度 $= sqrt(30^2 + 40^2) = 50$ 米。

  + 因为这些题表面对象不同，但底层都在追问“哪些角相等、哪些边成比例、哪些边角被锁定”。一旦先把三角形关系证明出来，后续结论往往就会自动跟出来。

  *螺旋复习（与前面主干的联系）*

  + 回到 #secref("2.4")：一根高 $1.5 "m"$ 的竹竿影长 $2 "m"$。同一时刻一棵树影长 $8 "m"$。利用同一种比的想法，求树高。

  + 回到 #secref("3.2")：赵爽弦图和扇形面积都在提醒你，面积不仅是“算数”，也是证明工具。

  + 回到 #secref("3.4")：平行线带来的同位角、内错角，会在相似、俯仰角、投影读图里反复出现。

  + 回到 #secref("3.5")：变换与三视图虽然不是重开坐标几何，但都在调用你对位置、方向和对齐的理解。
]
