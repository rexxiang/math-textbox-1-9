#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, secref, tryit, pitfall
#import "../lib/diagram-packages.typ": cetz

== §7.8 勾股定理：把直角三角形变成长度机器 <sec-7-8>

#vocab[勾股定理 Pythagorean theorem / 逆定理 converse of Pythagorean theorem / 勾股数 Pythagorean triple]

#crisis[
  你想靠墙放一根 5 米长的梯子，梯脚离墙 3 米。梯顶能够到多高？量不到的那条边，常常恰好藏在一个直角三角形里。

  *只要遇到直角，三条边之间是不是就有固定关系？又怎样把这个关系用在对角线、斜边、高度和距离上？*
]

#history-note[
  勾股定理几乎是全世界都独立发现过的结论。中国的《周髀算经》和赵爽弦图，古希腊的毕达哥拉斯学派，巴比伦的泥板记录，都在说明同一件事：直角三角形的三边关系太基本了，任何认真测量长度的文明都会碰到它。
]

#discovery[
  *第一步：从面积观察规律。*

  对一个直角三角形，在三条边上各作一个正方形。你会反复看到：两条直角边上的正方形面积之和，恰好等于斜边上的正方形面积。

  *第二步：这不是巧合，而是可证明的。*

  不管你用赵爽弦图的拼图思路，还是后面熟悉的代数展开，本质都是把同一个大图形的面积用两种方式计算，再令它们相等。

#figure(
  cetz.canvas(length: 1.2cm, {
    import cetz.draw: *
    // Right triangle with legs a=3, b=4, hypotenuse c=5
    let A = (0, 0)
    let B = (3, 0)
    let C = (0, 4)
    line(A, B, stroke: 0.8pt)
    line(B, C, stroke: 0.8pt)
    line(C, A, stroke: 0.8pt)
    // right angle mark at A
    line((0.25, 0), (0.25, 0.25), stroke: 0.5pt)
    line((0, 0.25), (0.25, 0.25), stroke: 0.5pt)
    // labels for sides
    content((1.5, -0.3), text(8pt)[$a$])
    content((-0.3, 2.0), text(8pt)[$b$])
    content((1.8, 2.2), text(8pt)[$c$])
    // vertex labels
    content(A, $A$, anchor: "north-east", padding: 3pt)
    content(B, $B$, anchor: "north-west", padding: 3pt)
    content(C, $C$, anchor: "south", padding: 3pt)
    // square on side a (bottom)
    line((0,-1.3),(3,-1.3), stroke: 0.6pt)
    line((0,0),(0,-1.3), stroke: 0.6pt)
    line((3,0),(3,-1.3), stroke: 0.6pt)
    content((1.5,-0.65), text(7pt, fill: blue)[$a^2$])
    // square on side b (left)
    line((-1.3,0),(-1.3,4), stroke: 0.6pt)
    line((0,0),(-1.3,0), stroke: 0.6pt)
    line((0,4),(-1.3,4), stroke: 0.6pt)
    content((-0.65,2.0), text(7pt, fill: blue)[$b^2$])
    // label hypotenuse square (just text, not drawn to avoid complexity)
    content((2.2, 2.8), text(7pt, fill: eastern)[$c^2$])
  }),
  caption: [勾股定理：直角三角形三边上作正方形，$a^2 + b^2 = c^2$（面积关系）]
)

  *第三步：逆向也成立。*

  如果一个三角形的三边满足同样的平方关系，那么它就必须是直角三角形。
]

#tryit[
  先自己算：

  + 验证 $3, 4, 5$ 是否满足勾股关系。
  + 一个直角三角形的两条直角边分别是 $5$ 和 $12$，斜边是多少？
  + 一个矩形长 $6$、宽 $8$，它的对角线是多少？
]

#side-hack[
  *见到直角先想三件事：*

  - 已知两边求第三边
  - 已知三边判定是否直角
  - 看到矩形、菱形对角线、圆心到弦的垂线时，看看有没有藏着直角三角形
]

#blueprint[
  *一、勾股定理*

  在直角三角形中，若两条直角边是 $a, b$，斜边是 $c$，则

  $ a^2 + b^2 = c^2 $

  *二、逆定理*

  若一个三角形三边为 $a, b, c$（其中 $c$ 最大），且满足

  $ a^2 + b^2 = c^2 $

  则这个三角形是直角三角形。

  *三、常见勾股数*

  - $3, 4, 5$
  - $5, 12, 13$
  - $8, 15, 17$
  - $7, 24, 25$

  以及它们的同倍数。

  *四、最常见的应用*

  - 已知两直角边求斜边：$c = sqrt(a^2 + b^2)$
  - 已知斜边和一条直角边求另一边：$b = sqrt(c^2 - a^2)$
  - 判断一个三角形是否直角：先找最大边，再检验平方关系

  *五、几何里的隐藏入口*

  勾股定理在本章里常从这些地方冒出来：

  - 矩形对角线
  - 菱形半对角线与边
  - 圆心到弦的距离（回看 #secref("7.6")）
  - 等腰三角形的高
  - 梯子、斜坡、屋顶等测量问题

  *例 1*：直角三角形两条直角边为 $6$ 和 $8$。求斜边。

  解：

  $ c = sqrt(6^2 + 8^2) = sqrt(36 + 64) = 10 $

  *例 2*：判断三边为 $7, 24, 25$ 的三角形是否为直角三角形。

  解：$7^2 + 24^2 = 49 + 576 = 625 = 25^2$，所以它是直角三角形。

  *例 3*：一个矩形长为 $8$、宽为 $6$。求对角线长。

  解：对角线与长宽构成直角三角形，所以

  $ d = sqrt(8^2 + 6^2) = 10 $

  *例 4*：等腰三角形的腰长为 $10$，底边长为 $12$。求底边上的高。

  解：高把底边平分成两段各长 $6$，于是

  $ h = sqrt(10^2 - 6^2) = 8 $
]

#pitfall[
  *陷阱 1：$c$ 必须是斜边，也是最长边。*

  不能随便把任意一条边塞进 $c$ 的位置。

  *陷阱 2：逆定理前要先确认谁最大。*

  判断是否直角时，必须让最大边单独站在等号右边。

  *陷阱 3：开平方后记得回到几何语境。*

  长度应取非负值，单位也别丢。
]

#mastery[
  *基础*

  + 直角三角形两条直角边分别为 $9$ 和 $12$。求斜边。

  + 直角三角形斜边长为 $13$，一条直角边长为 $5$。求另一条直角边。

  + 判断以下各组数能否构成直角三角形的三边：
    - $5, 12, 13$
    - $6, 7, 10$
    - $9, 40, 41$

  *应用*

  + 一个梯子长 5 米，底端离墙脚 3 米。求梯子顶端离地面的高度。

  + 一个矩形长 15、宽 8。求对角线长。

  + 半径为 $5$ 的圆中，一条弦长为 $8$。求圆心到弦的距离。

  *挑战*

  + *挑战 1*：判断三角形是否为直角三角形。下面三组边长，哪组能构成直角三角形？
    - $5, 12, 13$
    - $6, 8, 11$
    - $7, 24, 25$

    （提示：用 $a^2 + b^2 = c^2$ 验证，其中 $c$ 是最长边）

  + *挑战 2*：一道开放挑战。证明 $3$-$4$-$5$ 直角三角形是直角三角形，用两种方法：
    - 方法一：直接验证 $3^2 + 4^2 = 5^2$
    - 方法二：设直角三角形面积等于以三边为边长的正方形面积之差的一半（你能写出这个等式吗？）

  + 一条菱形的两条对角线分别为 $6$ 和 $8$。求它的边长。

  + 用”面积的两种算法”简述赵爽弦图证明的思路。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*

  + $15$

  + $12$

  + 是；不是；是。

  *应用*

  + 高度 $= sqrt(5^2 - 3^2) = 4$ 米。

  + 对角线 $= sqrt(15^2 + 8^2) = 17$。

  + 半弦长为 $4$，距离 $= sqrt(5^2 - 4^2) = 3$。

  *挑战*

  + 挑战 1 答案：$5^2 + 12^2 = 25 + 144 = 169 = 13^2$，是直角三角形；$6^2 + 8^2 = 36 + 64 = 100 eq.not 121 = 11^2$，不是；$7^2 + 24^2 = 49 + 576 = 625 = 25^2$，是直角三角形。

  + 挑战 2 答案：$3^2 + 4^2 = 9 + 16 = 25 = 5^2$，直接验证成立。面积等式：以三边为边的正方形面积分别为 $a^2 = 9$、$b^2 = 16$、$c^2 = 25$，恰好 $9 + 16 = 25$，说明三边关系成立。

  + 半对角线为 $3$ 和 $4$，边长 $= sqrt(3^2 + 4^2) = 5$。

  + 同一个边长为 $(a+b)$ 的大正方形，一次看成四个直角三角形加中间小正方形，一次直接展开成 $(a+b)^2$，令两式相等即可推出 $a^2 + b^2 = c^2$。
]
