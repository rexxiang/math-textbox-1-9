#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §7.9 圆的拓展（选学）：正多边形与圆的位置关系 <sec-7-9>

#vocab[正多边形 regular polygon / 外接圆 circumcircle / 圆心距 distance between centers]

#crisis[
  正六边形为什么和圆这么亲密？两个圆有时外离、有时外切、有时相交，这些位置关系能不能用一套简单规则判断？

  *在不把圆的侧厅塞进主线的前提下，还能再看见哪些稳定结构？*
]

#history-note[
  从阿基米德用正多边形逼近圆，到几何里用圆心距整理圆与圆的位置关系，人们一直在用“把复杂问题压缩成几个稳定量”的方式继续扩展圆的主题。本节只做轻量选学，不把更高阶定理挤进核心主线。
]

#discovery[
  *第一步：把正多边形放进圆里，圆心角会平均分配。*

  一个正 $n$ 边形内接于圆时，每条边对应的圆心角都会平均分成

  $ frac(360 degree, n) $

  所以正六边形最特别：它把整圆平均分成 6 份，每份圆心角都是 $60 degree$，这会让边长和半径产生非常紧的关系。

  *第二步：两个圆的位置关系主要看圆心距和半径。*

  设两圆半径分别为大圆的 $R$ 和小圆的 $r$，圆心距为 $d$。只要比较 $d$ 和 $R + r$、$R - r$，外离、外切、相交、内切、内含这些位置关系就能判断出来。

  *第三步：这类内容能扩眼界，但不是本章证明链和测量链的闸门。*
]

#tryit[
  先想一想：

  + 正六边形内接于圆时，边长和半径有什么关系？
  + 若两圆半径分别为 5 和 3，圆心距为 8，它们是什么位置关系？
  + 若两圆半径分别为 6 和 2，圆心距为 3，它们又是什么位置关系？
]

#side-hack[
  *这节只抓两把钥匙*：

  - 正多边形内接圆时，先看圆心角怎样平均分
  - 两圆关系先比较 $d$、$R + r$、$R - r$
]

#blueprint[
  *一、正多边形与圆*

  正 $n$ 边形内接于圆时：

  - 每条边对应的圆心角为 $frac(360 degree, n)$
  - 正六边形内接圆时，边长恰好等于外接圆半径
  - 边数越多，内接正多边形越接近圆

  *例 1*：正六边形内接于半径为 7 的圆。求它的边长。

  解：边长也为 7。

  *二、圆与圆的位置关系*

  设两圆半径分别为 $R$、$r$（其中 $R >= r$），圆心距为 $d$，则

  - 外离：$d > R + r$
  - 外切：$d = R + r$
  - 相交：$R - r < d < R + r$
  - 内切：$d = R - r > 0$
  - 内含：$0 <= d < R - r$

  *例 2*：两个圆半径分别为 5 和 3，圆心距为 8。判断位置关系。

  解：因为 $8 = 5 + 3$，所以两圆外切。

  *例 3*：两个圆半径分别为 6 和 2，圆心距为 3。判断位置关系。

  解：因为 $6 - 2 = 4$，且 $3 < 4$，所以小圆在大圆内部。

  *三、这节为什么放在侧厅*

  这些内容能帮助你看见“圆”的主题还能继续生长，但它们不是本章证明链和测量链的必经节点，所以明确放在选学位置，不挤进核心主线。
]

#pitfall[
  *陷阱 1：正六边形边长 = 半径，只是六边形这个特殊现象。*

  不是所有正多边形都和半径一样长。

  *陷阱 2：判断两圆关系前，先把大半径记作 $R$。*

  不先分清大小，就容易把 $R + r$ 和 $R - r$ 用乱。

  *陷阱 3：同心圆要单独看 $d = 0$。*

  它们既不是“相交”的特殊情况，也不是“外切”。
]

#mastery[
  *拓展练习*

  + 正八边形内接于圆时，每条边对应的圆心角是多少？

  + 两个圆半径分别为 6 和 2，圆心距为 8。判断位置关系。

  + 两个圆半径分别为 6 和 2，圆心距为 3。判断位置关系。

  + 解释为什么正多边形边数越多，看起来越像圆。

  + 说明本节为什么被放在“拓展”而不是“核心”位置。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  + $frac(360 degree, 8) = 45 degree$

  + 因为 $8 = 6 + 2$，所以两圆外切。

  + 因为 $6 - 2 = 4$，且 $3 < 4$，所以是内含。

  + 因为圆周被切成越来越多更短的小弧，正多边形的边界会不断逼近圆周。

  + 因为这些内容能拓展视野，但不属于本章证明链和测量链的必经入口。
]
