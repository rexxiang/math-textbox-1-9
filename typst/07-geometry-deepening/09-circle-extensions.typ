#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §7.9 圆的拓展（选学）：正多边形、圆与圆、圆幂一瞥 <sec-7-9>

#vocab[正多边形 regular polygon / 外接圆 circumcircle / 公切线 common tangent / 圆幂 power of a point]

#side-hack[
  *选学提醒*：本节是明确的拓展阅读，不进入本章核心主线，也不作为本章回顾页的必做前置。把它当成“圆的侧厅”：能开眼界，但不是本分支的通关闸门。
]

#crisis[
  为什么正六边形和圆的关系这么亲密？为什么两个圆有时互不相交，有时外切、内切？又为什么竞赛题里常会出现“从圆外一点引切线”的神秘乘积关系？

  *圆的核心性质之外，还能继续长出哪些更高阶的结构？*
]

#history-note[
  从阿基米德用正多边形逼近圆，到近代几何把“圆与圆”的位置关系统一归纳，再到竞赛和高中里的圆幂定理，圆始终是一个会不断向外扩展的主题。本节只做轻量入门，让你知道门后还有什么。
]

#discovery[
  *第一步：把正多边形放进圆里，圆心角会平均分配。*

  一个正 $n$ 边形内接于圆时，每条边对应的圆心角都是

  $ frac(360 degree, n) $

  *第二步：两个圆的位置关系其实只看三个数。*

  半径 $R$、$r$ 和圆心距 $d$ 一比较，外离、外切、相交、内切、内含就都出来了。

  *第三步：圆幂像是更远处的伏笔。*

  一旦一条割线、切线和外部点同时出现，长度乘积会出现稳定关系。这里先认识它，不要求熟练证明。
]

#tryit[
  先想想：

  + 正六边形内接于圆时，边长和圆半径有什么关系？
  + 若两个圆半径分别为 $5$ 和 $3$，圆心距为 $8$，它们是什么位置关系？
  + 若圆外一点到切点的线段长为 $6$，它的平方 $36$ 在圆幂语言里意味着什么？
]

#blueprint[
  *一、正多边形与圆*

  正 $n$ 边形内接于圆时：

  - 每条边对应的圆心角为 $frac(360 degree, n)$
  - 正六边形内接圆时，边长恰好等于外接圆半径
  - 当边数越来越多时，正多边形会越来越接近圆

  *二、圆与圆的位置关系*

  设两圆半径分别为 $R$、$r$（其中 $R >= r$），圆心距为 $d$，则

  - 外离：$d > R + r$
  - 外切：$d = R + r$
  - 相交：$R - r < d < R + r$
  - 内切：$d = R - r > 0$
  - 内含：$0 <= d < R - r$

  *三、圆幂一瞥*

  设点 $P$ 到圆心的距离为 $d$，圆半径为 $r$，则点 $P$ 的圆幂可写成

  $ d^2 - r^2 $

  与它相关的三个典型关系是：

  - 相交弦定理：$P A dot P B = P C dot P D$
  - 割线定理：$P A dot P B = P C dot P D$
  - 切割线定理：$P T^2 = P A dot P B$

  本节只做认识，不要求把它们纳入本章核心技能树。

  *例 1*：两个圆半径分别为 $5$ 和 $3$，圆心距为 $8$。判断位置关系。

  解：因为 $8 = 5 + 3$，所以两圆外切。

  *例 2*：圆内两弦交于点 $P$，若 $P A = 3$，$P B = 8$，$P C = 4$，求 $P D$。

  解：由相交弦定理，

  $ 3 times 8 = 4 times P D $

  所以 $P D = 6$。

  *例 3*：正六边形内接于半径为 $7$ 的圆，求边长。

  解：边长也为 $7$。
]

#pitfall[
  *陷阱 1：本节是选学，不要把它误当成本章主线必会。*

  *陷阱 2：同心圆不是“相交”的特殊情况。*

  当 $d = 0$ 时，要另看半径是否相等。

  *陷阱 3：圆幂定理先认识结构，不必在这里硬记全部证明。*
]

#mastery[
  *拓展练习*

  + 正八边形内接于圆时，每条边对应的圆心角是多少？

  + 两个圆半径分别为 $6$ 和 $2$，圆心距为 $3$。判断位置关系。

  + 圆外一点 $P$ 引切线 $P T$，若 $P T = 5$，一条割线与圆交于 $A, B$，且 $P A = 2$。求 $P B$。

  + 解释为什么正多边形边数越多，看起来越像圆。

  + 说明本节为什么被放在“拓展”而不是“核心”位置。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  + $frac(360 degree, 8) = 45 degree$

  + 因为 $6 - 2 = 4$，且 $3 < 4$，所以是内含。

  + 由切割线定理，$P T^2 = P A dot P B$，所以 $25 = 2 times P B$，故 $P B = 12.5$。

  + 因为圆周被切成越来越多更短的小弧，正多边形的边界不断逼近圆周。

  + 因为这些内容能拓展视野，但不属于本章证明链和测量链的必经节点。
]
