#import "../lib/theme-v2.typ": blueprint, mastery, secref

== 第七章回顾：几何深化

#blueprint[
  *知识速查卡*

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *证明工具链*（#secref("7.1")、#secref("7.2")、#secref("7.3")、#secref("7.4")）
      - 全等先说清对应和证明链，再分步学会 `SSS / SAS / ASA / AAS / HL`
      - `SSA` 不能直接判全等
      - 相似判定：AA、SAS、SSS
      - 相似比 $= k$，面积比 $= k^2$
    ],
    block[
      *四边形与圆*（#secref("7.5")、#secref("7.6")）
      - 平行四边形：对边相等，对角相等，对角线互相平分
      - 矩形：平行四边形 + 直角；对角线相等
      - 菱形：平行四边形 + 四边相等；对角线互相垂直
      - 圆周角 = 同弧圆心角的一半
    ],
  )

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *测量工具链*（#secref("7.8")、#secref("7.9")、#secref("7.10")）
      - 勾股定理：$a^2 + b^2 = c^2$
      - 逆定理：满足平方关系且最大边为 $c$ 时，可判定为直角三角形
      - $sin = frac("对边", "斜边")$
      - $cos = frac("邻边", "斜边")$
      - $tan = frac("对边", "邻边")$
      - 仰角 / 俯角先压回直角三角形；坡度 $= frac("升高", "水平距离")$
    ],
    block[
      *表达与运动*（#secref("7.11")、#secref("7.12")）
      - 轴对称：对称轴垂直平分对应点连线
      - 旋转 $180 degree$ 时会落成中心对称
      - 平移 / 旋转保持图形全等
      - 三视图：主视图、俯视图、左视图
    ],
  )

]

若你做过 #secref("7.13")，把它当作选学回看：想一想正多边形和圆的位置关系，怎样帮你把“圆上有什么稳定结构”看得更细一些。

#mastery[
  *混合自测题*（不标知识点，综合调用）

  + 判断下列条件中，哪些能直接判定两个三角形全等：SSS、SAS、SSA、HL。

  + 两个相似三角形的对应边之比为 $3 : 5$，较小三角形面积为 $27$。求较大三角形面积。

  + 平行四边形 $A B C D$ 中，$angle A = 72 degree$。求 $angle B$、$angle C$、$angle D$。

  + 一条弧所对圆心角为 $110 degree$。求同弧所对圆周角。

  + 直角三角形两条直角边为 $8$ 和 $15$。求斜边。

  + 一棵树到观测点的水平距离是 20 米，树顶仰角是 $30 degree$。求树顶相对观测眼睛的高度。

  + 一个点 $(4, -1)$ 关于原点中心对称后的坐标是什么？

  + 某几何体主视图、左视图都是矩形，俯视图是圆。这个几何体最可能是什么？

  + 点 $P(2, 1)$ 先向右平移 3 个单位，再关于 $x$ 轴对称。写出最终坐标，并说明这两步里什么量始终没有变。

  + 半径为 $6$ 的圆中，一条弦长为 $6$。求圆心到弦的距离。

  + *进阶*：等腰三角形三边之比为 $5 : 5 : 6$，周长为 $64$ cm。求底边上的高，并说明过程中调用了哪些旧工具。

  + *进阶*：一个坡道坡度为 $3 : 4$，水平距离为 40 米。求升高量与坡面长度。

  + 菱形 $A B C D$ 的对角线 $A C = 6$，$B D = 8$。求菱形的边长和面积。

  + 判断三组边长中，哪组能构成直角三角形：$(7, 24, 25)$，$(5, 7, 9)$，$(6, 8, 10)$。

  + 如图，$triangle A B C$ 中 $angle A = 50 degree$，$triangle D E F$ 中 $angle D = 50 degree$，$angle E = angle B$。判断两三角形是否相似，并写出使用的判定方法名称和对应边的比。

]

#line(length: 100%, stroke: 0.3pt + luma(200))
*参考答案*

  + SSS、SAS、HL 可以；SSA 不行，因为两边和一个非夹角不能锁定唯一形状，可能摆出两种三角形。

  + 面积比为 $9 : 25$，故较大面积 $= 27 times frac(25, 9) = 75$。

  + $angle B = 108 degree$，$angle C = 72 degree$，$angle D = 108 degree$。

  + 圆周角等于同弧圆心角的一半，所以是 $55 degree$

  + $17$

  + 用 $tan = frac("对边", "邻边")$：高度 $= 20 times tan 30 degree = frac(20 times sqrt(3), 3)$。

  + $(-4, 1)$

  + 圆柱。因为俯视图是圆，说明底面是圆；主视图、左视图都是矩形，说明侧面竖直展开后像长方形。

  + 先平移到 $(5, 1)$，再关于 $x$ 轴对称到 $(5, -1)$。在这两步里，图形的形状和大小始终没有变；若看单个点，则平移时点间距离保持不变，对称时到对称轴的距离成镜像。

  + 圆心到弦的垂线会平分弦，所以半弦长为 $3$；再在直角三角形里用勾股：距离 $= sqrt(6^2 - 3^2) = 3 times sqrt(3)$。

  + 先用比求边长：总份数 $= 16$，每份 $= 64 div 16 = 4$，所以三边是 $20$、$20$、$24$。高把底边平分成两段各长 $12$，所以 $h = sqrt(20^2 - 12^2) = 16$。这里调用了 #secref("2.4") 的比、#secref("7.2") 的等腰结构和 #secref("7.8") 的勾股定理。

  + 升高量 $= 40 times frac(3, 4) = 30$ 米；坡面长度 $= sqrt(30^2 + 40^2) = 50$ 米。

  + 对角线互相垂直平分，半对角线分别为 $3$ 和 $4$，边长 $= sqrt(3^2 + 4^2) = 5$。面积 $= frac(1, 2) times 6 times 8 = 24$（对角线之积的一半）。

  + $7^2 + 24^2 = 49 + 576 = 625 = 25^2$，是直角三角形；$5^2 + 7^2 = 74 != 81 = 9^2$，不是；$6^2 + 8^2 = 100 = 10^2$，是直角三角形。

  + $angle A = angle D = 50 degree$，$angle B = angle E$，所以 $triangle A B C tilde.op triangle D E F$（AA 判定）。对应边比：$A B : D E = B C : E F = A C : D F$。

*螺旋复习（与前面主干的联系）*

  + 回看 #secref("2.4")：一根高 $1.5 "m"$ 的竹竿影长 $2 "m"$。同一时刻一棵树影长 $8 "m"$。利用同一种比的想法，求树高。

  + 回看 #secref("3.7")：请写出一个会用到同位角或内错角的场景，并说明它怎样帮助你推进相似、俯仰角或投影读图。

  + *【等腰三角形综合】* 等腰三角形（#secref("7.3")）的腰长与底边之比为 $3 : 2$（#secref("2.4")）。若周长为 $40$ 厘米：
    - 设腰长为 $x$，用 #secref("6.6") 的方法列方程并求腰长和底边
    - 用 #secref("7.8") 的勾股定理，求底边上的高（写出列式即可，可以写 $sqrt(...)$ 不化简）

*螺旋复习参考答案*

  + 设树高为 $h$，同一时刻影长与物高成比例：$frac(1.5, 2) = frac(h, 8)$，$h = 6$ 米。

  + 开放题，答案不唯一。示例：在铁路或公路设计中，两条并行线路之间的切割线会形成同位角和内错角，可用它们判断平行性，进而用相似比估计远处目标距离。

  + 答：设腰长 $= x$，底边 $= frac(2, 3) x$；周长方程 $2x + frac(2, 3) x = 40$，即 $frac(8, 3) x = 40$，$x = 15$ 厘米，底边 $= 10$ 厘米。勾股：高 $h = sqrt(15^2 - 5^2) = sqrt(225 - 25) = sqrt(200) = 10 sqrt(2)$ 厘米。
