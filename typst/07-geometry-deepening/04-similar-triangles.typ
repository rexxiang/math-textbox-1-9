#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, secref, tryit, pitfall

== §7.4 同形放缩与测量 <sec-7-4>

#vocab[相似三角形 similar triangles / 相似比 similarity ratio / 相似判定 similarity criteria / 角角相似 Angle-Angle similarity (AA) / 边角边相似 Side-Angle-Side similarity (SAS) / 边边边相似 Side-Side-Side similarity (SSS)]

#crisis[
  你想知道旗杆有多高，却既爬不上去，也不能把它放倒。你只能量出一根小木杆的高度、两根影子的长度，再借助太阳同一时刻形成的角度关系来推算。

  *两个大小不同的三角形，什么时候可以被看成“同一种形状”？又怎样把这种“同形不同大”变成比例计算工具？*
]

#history-note[
  从古埃及的影子测高，到地图缩放和工程制图，人们反复遇到同一类问题：不能把对象搬到手边量，但可以找到一个更小、更近、更容易处理的“同形版本”。这种“按同一倍数放大或缩小”的关系，后来被整理成了可证明、可计算的规则。
]

#discovery[
  *第一步：全等太严格了，“同形不同大”更贴近现实。*

  边长为 $3, 4, 5$ 的直角三角形和边长为 $6, 8, 10$ 的直角三角形显然不能重合，但它们每个角都一样大，边长也都按同一个倍数放大了。它们像是“同一种形状的放大版”。

  *第二步：平行线会稳定制造“同样的角”。*

  在一个大三角形里，如果一条线段和第三边平行，那么由 #secref("3.8") 的平行线角性质，就能得到两组三角形的对应角相等，于是“同形放缩”的关系自然出现了。

  *第三步：一旦图形按同一个倍数放缩，长度按 $k$ 变，面积按 $k^2$ 变。*

  这条规律会在影长测高、比例尺、图形放缩这些场景里反复出现：图形样子不变，能算的量却一下子多了起来。
]

#tryit[
  先自己算：

  + 两个三角形的边长分别为 $3, 4, 5$ 和 $6, 8, 10$，它们是全等还是“同形放缩”？
  + 放大的倍数是多少？
  + 若较小三角形面积为 $12$，较大三角形面积是多少？
]

#blueprint[
  *一、相似三角形*

  如果两个三角形对应角分别相等、对应边成比例，就叫*相似三角形*，记作

  $ triangle A B C tilde triangle D E F $

  若相似比为 $k$，则

  $ frac(D E, A B) = frac(E F, B C) = frac(F D, C A) = k $

  *二、相似三角形的判定*

  - *角角*（Angle-Angle, AA）：两组角分别相等
  - *边角边*（Side-Angle-Side, SAS）：两组对应边成比例，且夹角相等
  - *边边边*（Side-Side-Side, SSS）：三组对应边成比例

  *三、放缩规律与测量*

  - 对应角相等
  - 对应边成比例
  - 对应周长之比等于相似比
  - 对应面积之比等于相似比的平方

  平行线常常会制造这样的关系：

  在 $triangle A B C$ 中，若点 $D$ 在 $A B$ 上，点 $E$ 在 $A C$ 上，且 $D E parallel B C$，那么

  - $angle A D E = angle A B C$
  - $angle A E D = angle A C B$
  - $angle A$ 公共

  所以 $triangle A D E tilde triangle A B C$（AA）。

  中位线就是一个典型的半尺寸版本：

  若 $M$、$N$ 分别是 $A B$、$A C$ 的中点，那么

  $ frac(A M, A B) = frac(A N, A C) = frac(1, 2) $

  所以 $triangle A M N tilde triangle A B C$。于是

  - $M N parallel B C$
  - $M N = frac(1, 2) B C$

  这套放缩规律还能直接进入测量：

  同一时刻的太阳光近似平行，所以小木杆和旗杆形成的两个直角三角形往往相似。于是可以列比例：

  $ frac("旗杆高", "木杆高") = frac("旗杆影长", "木杆影长") $

  *例 1*：已知 $triangle A B C tilde triangle D E F$，且 $A B = 6$，$D E = 9$，$B C = 8$。求 $E F$。

  解：相似比 $k = frac(9, 6) = frac(3, 2)$，所以

  $ E F = 8 times frac(3, 2) = 12 $

  *例 2*：一根 1.5 米木杆影长 2 米，旗杆影长 12 米。求旗杆高。

  解：由相似得

  $ frac(h, 1.5) = frac(12, 2) = 6 $

  所以 $h = 9$，即旗杆高 9 米。

  *例 3*：两个相似三角形的边长比为 $2 : 3$。求面积比。

  解：面积比为 $2^2 : 3^2 = 4 : 9$。

  #side-hack[
    *相似比 $k$ 管三件事：*

    - 对应边之比 $= k$
    - 对应周长之比 $= k$
    - 对应面积之比 $= k^2$

    记忆时把“长度”和“面积”分开：长度跟一次方走，面积跟平方走。
  ]
]

#pitfall[
  *陷阱 1：相似不等于全等。*

  全等要求“大小也一样”；相似只要求“形状一样”，允许整体放大或缩小。

  *陷阱 2：面积比不能直接抄边长比。*

  若相似比是 $3$，面积不是变成 $3$ 倍，而是变成 $9$ 倍。
]

#mastery[
  *基础*

  + 判断边长分别为 $3, 4, 5$ 和 $6, 8, 10$ 的两个三角形是全等还是相似，并写出相似比。

  + 已知两个相似三角形的相似比为 $2 : 5$，较小三角形周长为 $18$。求较大三角形周长。

  + 已知两个相似三角形的相似比为 $3 : 4$，较小三角形面积为 $27$。求较大三角形面积。

  *应用*

  + 在 $triangle A B C$ 中，$D E parallel B C$，且 $A D = 4$，$A B = 10$，$B C = 15$。求 $D E$。

  + 在 $triangle A B C$ 中，$M$、$N$ 分别是 $A B$、$A C$ 的中点。若 $B C = 18$，求 $M N$。

  + 一面墙的影子长 8 米；同时，一根 1.2 米标杆影长 1.5 米。求墙高。

  *挑战*

  + 证明：在三角形中，一条线段若平行于第三边，则截出的较小三角形与原三角形相似。

  + 已知两个相似三角形的面积比是 $25 : 49$。求对应边之比。

  + 解释为什么相似能把“形状相同”直接变成“对应边成比例”。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*

  + 相似，不全等；相似比为 $2$。

  + 周长之比与边长之比相同，所以较大周长 $= 18 times frac(5, 2) = 45$。

  + 面积比为 $9 : 16$，所以较大面积 $= 27 times frac(16, 9) = 48$。

  *应用*

  + 由相似得 $frac(D E, 15) = frac(4, 10)$，所以 $D E = 6$。

  + 中位线定理：$M N = frac(1, 2) times 18 = 9$。

  + 由相似得 $frac(h, 1.2) = frac(8, 1.5)$，所以 $h = 6.4$，即墙高 6.4 米。

  *挑战*

  + 用平行线制造两组相等角，再用 AA 判定即可。

  + 边长之比为 $5 : 7$。

  + 因为对应角相等把图形的形状锁定后，两个图形只剩“按同一个倍数放大或缩小”这一种变化，所以对应边会保持同样的比例。
]
