#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, secref, tryit, pitfall

== §7.6 三角比与测量：把角度翻译成长度 <sec-7-6>

#vocab[正弦 sine / 余弦 cosine / 正切 tangent / 仰角 angle of elevation / 俯角 angle of depression / 测量 measurement]

#crisis[
  你站在操场上，想知道旗杆、楼房、山坡到底有多高。你量不到那些竖直长度，却常常能量到一段水平距离，再测出一个仰角。

  *只知道一个角和一条边，真的能把“够不着”的长度算出来吗？*
]

#history-note[
  三角比最初并不是为了讲函数，而是为了测量：天文学家要从观测角度推出天体位置，航海者要从仰角和方位估计航程，工程师要从坡角和距离推回高度。它的起点是“几何测量”，而不是“图象变化”。
]

#discovery[
  *第一步：把问题缩到直角三角形里。*

  测高、测距、坡度、楼顶视线，最后常常都会落在一个直角三角形上：一条边已知，一个锐角已知，另一条边未知。

  *第二步：为什么角一固定，边的比值就固定？*

  因为角相同的直角三角形彼此相似（回看 #secref("7.2")）。所以虽然大小不同，但“对边 / 斜边”“邻边 / 斜边”“对边 / 邻边”这些比值不会变。

  *第三步：三角比本质上是测量捷径。*

  它把“角度信息”翻译成“长度比例”，让我们能从容易量的边推出难量的边。
]

#tryit[
  在直角三角形中，设一个锐角为 $A$，对边长 $3$，斜边长 $5$。

  + $sin A$ 是多少？
  + 若先用 #secref("7.5") 求出邻边，再求 $cos A$ 和 $tan A$
]

#side-hack[
  *SOH-CAH-TOA*：

  - $sin = frac("对边", "斜边")$
  - $cos = frac("邻边", "斜边")$
  - $tan = frac("对边", "邻边")$

  这是*测量口诀*，不是函数口号。先认清“相对于哪个角”的对边和邻边，再代比值。
]

#blueprint[
  *一、三角比的定义*

  在直角三角形中，对于锐角 $A$：

  $ sin A = frac("对边", "斜边") $

  $ cos A = frac("邻边", "斜边") $

  $ tan A = frac("对边", "邻边") $

  *二、为什么这些比值只由角决定*

  因为所有含有同一个锐角 $A$ 的直角三角形彼此相似，所以相应边的比值保持不变。这就是三角比能够拿来做测量的根本原因。

  *三、特殊角的常用值*

  - $sin 30 degree = frac(1, 2)$，$cos 30 degree = frac(sqrt(3), 2)$，$tan 30 degree = frac(sqrt(3), 3)$
  - $sin 45 degree = frac(sqrt(2), 2)$，$cos 45 degree = frac(sqrt(2), 2)$，$tan 45 degree = 1$
  - $sin 60 degree = frac(sqrt(3), 2)$，$cos 60 degree = frac(1, 2)$，$tan 60 degree = sqrt(3)$

  *四、解直角三角形的常见路线*

  - 已知斜边和锐角，求高或水平距离：常用 $sin$ 或 $cos$
  - 已知水平距离和仰角，求高度：常用 $tan$
  - 先用三角比求一边，再用 #secref("7.5") 补另一边

  *五、仰角、俯角与坡度*

  - *仰角*：视线在水平线*上方*形成的角
  - *俯角*：视线在水平线*下方*形成的角
  - *坡度*：$frac("升高", "水平距离") = tan alpha$

  *六、测量不可达长度的基本步骤*

  + 先画出直角三角形示意图
  + 标出哪个角是仰角或俯角
  + 找到已知边和未知边相对于该角的位置
  + 选最直接的三角比列式
  + 需要时再用勾股定理补边

  *例 1*：水平距离为 200 米，仰角为 $35 degree$。求山高。

  解：设山高为 $h$，则

  $ tan 35 degree = frac(h, 200) $

  所以 $h = 200 tan 35 degree$。用计算器可得 $tan 35 degree approx 0.7002$，因此

  $ h approx 140 $

  即山高约 140 米。

  *例 2*：梯子长 5 米，与地面成 $60 degree$ 角。求梯子顶端离地面高度。

  解：

  $ sin 60 degree = frac(h, 5) $

  所以

  $ h = 5 times frac(sqrt(3), 2) = frac(5 times sqrt(3), 2) $

  *例 3*：楼高 30 米，从楼顶看地面目标的俯角为 $45 degree$。求目标到楼底的水平距离。

  解：俯角等于地面对应仰角，所以

  $ tan 45 degree = frac(30, d) = 1 $

  因此 $d = 30$。

  *例 4*：一条道路坡度为 $1 : 5$。若水平距离为 500 米，求升高量。

  解：坡度表示

  $ frac("升高", 500) = frac(1, 5) $

  所以升高量为 100 米。
]

#pitfall[
  *陷阱 1：对边和邻边是“相对于所选角”而言的。*

  同一个三角形，换一个角观察，对边和邻边都会交换。

  *陷阱 2：计算器要看清角度模式。*

  本章默认用*度*，不是弧度。

  *陷阱 3：别把三角比当成函数章节里的图象概念。*

  在这里，它首先是右三角形测量工具。
]

#mastery[
  *基础*

  + 在直角三角形中，若一个锐角的对边为 $3$，斜边为 $5$，求该角的 $sin$、$cos$、$tan$。

  + 直接写出 $sin 60 degree$、$cos 45 degree$、$tan 30 degree$。

  + 已知 $sin A = frac(5, 13)$（$A$ 为锐角），求 $cos A$ 和 $tan A$。

  *应用*

  + 一棵树到观测点的水平距离为 15 米，树顶仰角为 $40 degree$，观测者眼高 1.6 米。若 $tan 40 degree approx 0.839$，求树高。

  + 一条坡道坡度为 $1 : 4$，水平距离为 20 米。求升高量。

  + 一架无人机在地面上方 120 米处，地面观察点看它的仰角为 $30 degree$。求观察点到其正下方的水平距离。

  *挑战*

  + 解释为什么本节必须建立在 #secref("7.2") 的相似思想上。

  + 已知一根杆长 2 米，影长 2.5 米；同一时刻一座塔影长 30 米。求塔高。

  + 从高处看地面目标的俯角是 $35 degree$。说明为什么列式时可以直接在地面对应点使用 $35 degree$ 的仰角。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*

  + 邻边为 $4$，所以 $sin A = frac(3, 5)$，$cos A = frac(4, 5)$，$tan A = frac(3, 4)$。

  + $frac(sqrt(3), 2)$，$frac(sqrt(2), 2)$，$frac(sqrt(3), 3)$。

  + 这是 $5, 12, 13$ 直角三角形，所以 $cos A = frac(12, 13)$，$tan A = frac(5, 12)$。

  *应用*

  + 设树高为 $h$。则 $frac(h - 1.6, 15) = tan 40 degree approx 0.839$，故 $h - 1.6 approx 12.585$，所以 $h approx 14.185$，约为 14.2 米。

  + 升高量 $= 20 times frac(1, 4) = 5$ 米。

  + 设水平距离为 $d$，则 $tan 30 degree = frac(120, d)$，所以 $d = frac(120, tan 30 degree) = 120 times sqrt(3)$。

  *挑战*

  + 因为三角比要想“只由角决定”，必须依赖角相同的直角三角形彼此相似。

  + 由相似得 $frac(h, 2) = frac(30, 2.5) = 12$，所以 $h = 24$ 米。

  + 因为俯角和对应的仰角是内错角相等，来自平行线与截线关系（回看 #secref("3.4")）。
]
