#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, secref, tryit, pitfall

== §7.11 图形变换：移动、翻折与旋转中的不变量 <sec-7-11>

#vocab[轴对称 reflection / 平移 translation / 旋转 rotation]

#crisis[
  地砖花纹会重复平移，剪纸靠轴对称展开，风车叶片围绕中心旋转，品牌图标又常利用中心对称来追求稳定感。

  *图形明明“动了”，为什么我们还说它没有变形？哪些量会保持不变，哪些信息会被改变？*
]

#history-note[
  变换几何把“图形是什么”推进到“图形怎样运动还保持自己”。从伊斯兰拼花到现代计算机图形学，人们关心的不只是一个静止图形，更关心它在移动、翻折、旋转之后，哪些结构仍然稳定。
]

#discovery[
  *第一步：平移是整体滑动。*

  每个点都朝同一个方向移动同样的距离，图形的形状、大小和方向感保持不变。

  *第二步：轴对称是沿一条直线翻折。*

  对应点连线会被对称轴垂直平分。这条性质解释了为什么镜像能“左右对换”。

  *第三步：旋转和中心对称都围着点转。*

  旋转保持到中心的距离不变；中心对称其实就是旋转 $180 degree$ 的特例。
]

#tryit[
  点 $P(3, 2)$：

  + 关于 $x$ 轴对称后坐标是什么？
  + 关于原点中心对称后坐标是什么？
  + 向右平移 4 个单位后坐标是什么？

  先根据 #secref("3.9") 的坐标直觉自己猜，再对照蓝图。
]

#side-hack[
  *看变换先看“谁不变”*：

  - 平移：方向和距离统一
  - 轴对称：到对称轴的距离成镜像
  - 旋转：到旋转中心的距离不变

  若旋转角恰好是 $180 degree$，就会落成中心对称这种特例。
]

#blueprint[
  *先看同一个三角形怎样“动”*

  设 $triangle A B C$ 的三个顶点分别是 $A(1, 1)$、$B(3, 1)$、$C(2, 3)$。

  - 若把它整体向右平移 4 个单位，三个点都会一起右移，但三边长度和三个角都不变
  - 若把它关于 $x$ 轴对称，图形会翻到下方，但每个点到对称轴的距离关系保持镜像
  - 若把它绕原点旋转 $180 degree$，整块图形会转到对面，但点到旋转中心的距离仍保持不变

  所以变换题里最该先抓住的，不是“名字像不像”，而是：*位置或朝向会变，形状和大小却没变*。

  *一、变换的共同点*

  本节讨论的轴对称、平移、旋转、中心对称，都会保持图形的*形状和大小*不变，因此变换前后图形全等。

  *二、轴对称*

  若一个图形沿某条直线折叠后能和自身重合，就说它关于这条直线轴对称。

  性质：

  - 对称轴垂直平分每对对应点连线
  - 对应边相等，对应角相等

  常见轴对称图形：

  - 等腰三角形
  - 矩形
  - 菱形
  - 正方形
  - 圆

  *三、平移*

  平移由两个要素确定：

  - 方向
  - 距离

  平移后：

  - 对应点连线平行或共线
  - 对应点距离相等
  - 图形方向不翻转

  *四、旋转*

  旋转由三个要素确定：

  - 旋转中心
  - 旋转方向
  - 旋转角度

  旋转后：

  - 点到旋转中心的距离不变
  - 对应点与中心连线所成的角等于旋转角

  若旋转角恰好是 $180 degree$，就得到*中心对称*这个特例。这时每对对应点连线的中点都是同一个中心。

  常见中心对称图形有：平行四边形、矩形、菱形、正方形、圆。

  *五、坐标中的轻量表达*

  在坐标纸上，最常见的几种表达是：

  - 关于 $x$ 轴对称：$P(x, y) arrow.r P'(x, -y)$
  - 关于 $y$ 轴对称：$P(x, y) arrow.r P'(-x, y)$
  - 关于原点中心对称：$P(x, y) arrow.r P'(-x, -y)$
  - 向右平移 $a$ 个单位：$P(x, y) arrow.r P'(x + a, y)$

  这些只是对 #secref("3.9") 坐标入门的轻量调用，不是在这里重开一章坐标几何。

  *例 1*：点 $A(3, -2)$ 关于 $x$ 轴对称后的坐标是什么？关于原点中心对称后的坐标是什么？

  解：分别是 $(3, 2)$ 和 $(-3, 2)$。

  *例 2*：将 $triangle A B C$ 向右平移 4 个单位，若 $A(1, 3)$，$B(4, 1)$，$C(2, 5)$，求平移后的坐标。

  解：$A'(5, 3)$，$B'(8, 1)$，$C'(6, 5)$。

  *例 3*：为什么一般平行四边形是中心对称图形？

  解：因为它的对角线互相平分，交点正好成为所有对应点连线的中点。
]

#pitfall[
  *陷阱 1：平移不会翻转图形。*

  镜像来自轴对称，不来自平移。

  *陷阱 2：中心对称不是“任意旋转”。*

  它特指旋转 $180 degree$。

  *陷阱 3：坐标规则只是表达方式，别忘了背后的几何意义。*
]

#mastery[
  *基础*

  + 点 $P(4, -3)$ 关于 $x$ 轴、$y$ 轴、原点对称后的坐标分别是什么？

  + 一个图形向左平移 5 个单位，点 $(7, 2)$ 平移后坐标是什么？

  + 判断下列图形是否轴对称、是否中心对称：等腰三角形、平行四边形、正方形。

  *应用*

  + 说明为什么轴对称会保持边长和角度不变。

  + 一个图形绕点 $O$ 旋转 $90 degree$ 后，点到 $O$ 的距离为什么不会变？

  + 为什么矩形既是轴对称图形也是中心对称图形？

  *挑战*

  + 解释“变换前后图形全等”与 #secref("7.1") 有什么联系。

  + 为什么拼花设计里常同时用到平移和旋转？

  + 若图形先关于 $x$ 轴对称，再关于 $y$ 轴对称，结果与关于原点中心对称有什么关系？

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*

  + $(4, 3)$，$(-4, -3)$，$(-4, 3)$。

  + $(2, 2)$。

  + 等腰三角形：轴对称，不一定中心对称。平行四边形：中心对称，一般不轴对称。正方形：两者都是。

  *应用*

  + 轴对称是翻折重合，所以对应边、对应角都保持不变。

  + 旋转只是改变方向，不改变点到旋转中心的半径长度。

  + 矩形有两条对称轴，同时其对角线交点也是中心对称中心。

  *挑战*

  + 因为变换前后全等，所以很多“证明两个图形一样大”的问题，本质上也能借用全等视角来理解。

  + 因为平移负责铺开重复，旋转负责改变朝向，两者叠加能生成丰富而有规律的图案。

  + 两次操作的结果与关于原点中心对称相同，坐标会变成 $(-x, -y)$。
]
