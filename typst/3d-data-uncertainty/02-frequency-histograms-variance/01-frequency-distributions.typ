#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref

== 频率分布表：把一把数据排成结构 <tool:dt04-frequency-distributions>

#vocab[频数 frequency count / 频率 relative frequency / 分布表 frequency distribution table]

#crisis[
  某次年级数学测验 $50$ 位同学成绩（分）打乱顺序写在草稿纸上：

  #align(center, table(
    columns: 10,
    align: center,
    stroke: 0.4pt,
    [$78$], [$82$], [$65$], [$93$], [$71$], [$88$], [$56$], [$73$], [$80$], [$95$],
    [$68$], [$74$], [$85$], [$79$], [$62$], [$77$], [$84$], [$90$], [$48$], [$72$],
    [$76$], [$81$], [$67$], [$89$], [$75$], [$83$], [$70$], [$92$], [$58$], [$78$],
    [$86$], [$64$], [$74$], [$80$], [$69$], [$77$], [$45$], [$82$], [$96$], [$73$],
    [$79$], [$87$], [$66$], [$71$], [$91$], [$75$], [$83$], [$78$], [$63$], [$85$],
  ))

  老师想回答：“大部分同学集中在哪一档？有多少人 $60$ 分以下？不及格率多少？高分段（$90$ 分以上）占多少？”

  直接盯原始表*很难数*——$50$ 个数字零散、没结构。但如果我们*先按 $10$ 分一档分组*，再数一数每档多少人，答案一分钟就能出来。这就是*频率分布表*的动机。

  *这一节发明把一把原始数据整理成分档表的方法，并解决两个自由度：分多少档、每档多宽？*
]

#history-note[
  分档整理数据的习惯可追溯到 $18$ 世纪的人口统计——政府要知道“每个 $10$ 岁年龄段有多少人”来估算兵源和税收。$19$ 世纪末英国统计学家 Karl Pearson 系统化了“把区间 $+$ 频数画成直方图”的做法，作为一种发现数据*形状*的工具。
]

#discovery[
  *分组：一把数据变成几档*

  先定两件事：
  - *组距*（class width）：每档的宽度。
  - *组数*（number of classes）：一共分多少档。

  它们不独立——*组距 $times$ 组数 $approx$ 极差*。经验公式：数据量 $n = 50$ 附近时组数一般取 $5$~$8$ 档；$n = 100$~$500$ 时取 $8$~$12$ 档。组数太少 → 细节被抹掉；组数太多 → 每档太稀。

  上面 $50$ 个分数，极差 $= 96 - 45 = 51$。选*组距 $= 10$*、组数 $= 6$——$[40, 50), [50, 60), ..., [90, 100]$——恰好覆盖 $40$ 到 $100$。

  *数出频数*

  *频数* $f_k$ $=$ 第 $k$ 档里*落入的数据个数*。一个一个对上面 $50$ 个分数打勾数：

  #align(center, table(
    columns: 4,
    align: (left, center, center, center),
    stroke: 0.4pt,
    [*分数段*], [*频数 $f_k$*], [*频率 $p_k = f_k / n$*], [*百分比*],
    [$[40, 50)$], [$2$], [$0.04$], [$4%$],
    [$[50, 60)$], [$2$], [$0.04$], [$4%$],
    [$[60, 70)$], [$8$], [$0.16$], [$16%$],
    [$[70, 80)$], [$18$], [$0.36$], [$36%$],
    [$[80, 90)$], [$14$], [$0.28$], [$28%$],
    [$[90, 100]$], [$6$], [$0.12$], [$12%$],
    [*合计*], [$50$], [$1.00$], [$100%$],
  ))

  这就是*频率分布表*。三个要点：

  + *频数* $f_k$ 是*整数*；所有 $f_k$ 之和 $= n$。
  + *频率* $p_k = f_k / n$ 是*小数*（介于 $0$ 与 $1$）；所有 $p_k$ 之和 $= 1$。
  + *百分比* $= p_k times 100%$ 只是频率的另一种书写方式。

  一张表回答了所有初始问题：
  - 大部分同学集中在 $[70, 80)$ 这档（$36%$）。
  - $60$ 分以下：$f_1 + f_2 = 2 + 2 = 4$ 人，占 $8%$。
  - 高分段 $[90, 100]$ 占 $12%$。

  *分组的三条原则*

  + *组距相等*：每档宽度一致（否则频率不可直接对比——下一节直方图会放大这个问题）。
  + *区间左闭右开*（最后一档可以右闭）：写成 $[a, a + w)$——恰好落在边界 $a + w$ 的值归*下一档*，避免“边界值到底算哪边”的歧义。
  + *覆盖完备*：最小数据 $>=$ 第一档下限，最大数据 $<=$ 最后一档上限；不允许数据落在区间之外。

  *组距的选择不是唯一正确*

  同一份数据，组距选 $5$、$10$ 或 $20$ 会得出不同表。选 $5$ 细节多、选 $20$ 细节少。一般*先按经验试一个*（比如组距 $= 10$）、画出来看形状是否清晰；若太粗 / 太细再调。
]

#tryit[
  + 一个班 $30$ 位同学身高（cm）：最矮 $148$、最高 $179$。想分 $5$~$8$ 档，*组距*选多少合适？给一组 $[\,\,)$ 区间划分。
  + 某次跳绳记录 $40$ 人的*$1$ 分钟跳绳数*：$80$ 以下 $6$ 人；$[80, 100)$ $12$ 人；$[100, 120)$ $14$ 人；$[120, 140)$ $6$ 人；$140$ 及以上 $2$ 人。做一张频率分布表（填写 $f_k$、$p_k$、百分比）。
  + 一份原始数据共 $n = 50$ 个，最小 $= 12$、最大 $= 88$。如果组距 $= 15$，要分多少档？把区间列出来。
]

#blueprint[
  *工具：频率分布表与分组规则*

  *三个基本量*

  - *频数* $f_k$：第 $k$ 档里数据的个数。$sum_k f_k = n$。
  - *频率* $p_k = f_k / n$：占总数的比例。$sum_k p_k = 1$。
  - *组距* $w$：每档的宽度（本书默认*各档等宽*）。

  *分组流程*

  + 算*极差* $= max - min$。
  + 定*组数* $m$（经验：$n in [20, 50]$ 取 $5$~$8$；$n in [50, 500]$ 取 $8$~$12$）。
  + 定*组距* $w$：$w approx "极差" / m$，圆到方便的数（$5, 10, 20$ ……）。
  + 定*第一档起点*：略低于或等于最小值。
  + 按*左闭右开*写出所有区间。
  + 逐项打勾数频数；算频率 $=$ 频数 $\/ n$。
  + *检查*：$sum f_k = n$、$sum p_k = 1$、各档宽度 $= w$ ✓。
]

#pitfall[
  *高频错误*

  ✗ 把*频率*理解成*频数*（或反过来）
  → ✓ *频数* $f_k$ 是*个数*（整数），*频率* $p_k$ 是*比例*（$0$~$1$ 的小数）。题目问哪个要看清。

  ✗ 区间重叠（两档都包含边界值）或留缝（边界值不属于任何一档）
  → ✓ 统一使用*左闭右开* $[a, a + w)$；只有最后一档为了覆盖最大值可改为闭区间。

  ✗ 各档*组距不等*
  → ✓ 本书中频率分布表*默认各档等宽*。不等宽时频率不能直接比，必须用“频率 / 组距”（直方图那一节展开）。

  ✗ 把极差除以组距直接作为组数，不管能否整除
  → ✓ 若极差 $= 51$、组距 $= 10$，应取 $6$ 档（$ceil(51 / 10) = 6$）并让首档起点略小于最小值。
]

#mastery[
  *基础*

  + 下表空格处应填什么？
    #align(center, table(
      columns: 4,
      align: (left, center, center, center),
      stroke: 0.4pt,
      [*区间*], [*频数*], [*频率*], [*百分比*],
      [$[0, 10)$], [$4$], [?], [?],
      [$[10, 20)$], [$16$], [?], [?],
      [$[20, 30)$], [?], [$0.4$], [?],
      [*合计*], [?], [?], [?],
    ))
    已知总数 $n = 50$。
  + 一份数据最小 $= 2$、最大 $= 47$，选组距 $= 5$ 分档。列出所有区间。
  + 一张频率分布表显示 $[80, 90)$ 的频率 $= 0.24$，总数 $n = 200$。该档有多少人？

  *应用*

  + 某次 $1$ 分钟俯卧撑测试 $50$ 人，记录最低 $8$ 个、最高 $42$ 个。
    - (a) 若选组距 $= 5$，第一档起点取 $8$，列出所有区间。
    - (b) 若改成组距 $= 10$，起点取 $5$，列出所有区间。
    - 两种方案各自适合什么场合？
  + 一组 $100$ 人的身高按组距 $= 5$ cm 分档，得到 $[150, 155)$ 频率 $0.10$、$[155, 160)$ 频率 $0.18$、$[160, 165)$ 频率 $0.28$、$[165, 170)$ 频率 $0.24$、$[170, 175)$ 频率 $0.14$、$[175, 180)$ 频率 $0.06$。身高 $<$ $160$ 的有多少人？$>= 170$ 的呢？频率之和是否 $= 1$？
  + 两个班各 $40$ 人的考试频率分布（区间、频率）：
    - 甲：$[60, 70)$ $0.10$；$[70, 80)$ $0.25$；$[80, 90)$ $0.45$；$[90, 100]$ $0.20$；
    - 乙：$[60, 70)$ $0.20$；$[70, 80)$ $0.30$；$[80, 90)$ $0.30$；$[90, 100]$ $0.20$。
    哪个班 $[80, 100]$ 分数段的比例更高？绝对人数各多少？

  *挑战*

  + 你拿到一份 $n = 200$ 的数据，组距取 $10$ 得出频率 $0.02, 0.10, 0.40, 0.30, 0.12, 0.06$（共 $6$ 档）。如果把组距改成 $20$（合并相邻两档），新频率分布是多少？信息损失了什么？
  + 有同学说：“把组距调得越小越好——细节越多、信息越丰富。” 用一个 $n = 50$、组距太小的例子反驳这种说法。
  + 一份数据有异常点（唯一一个远离其他数据的大值）。它在频率分布表里会造成什么现象？这会对上一章“极差”的读数有怎样的影响？

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + $[0, 10)$：频率 $= 4 / 50 = 0.08$，$8%$。$[10, 20)$：频率 $= 16 / 50 = 0.32$，$32%$。$[20, 30)$：频数 $= 0.4 times 50 = 20$，$40%$。合计：频数 $= 4 + 16 + 20 = 40$（未满 $50$，说明还有其他数据落在表外或题意只展示部分）；若题目只含这三档，则 $n = 40$；按题设 $n = 50$ 的情况下需补一档或说明数据不完整。此题以前三档之和看 $4 + 16 + 20 = 40$、对应频率和 $0.8$——说明题中 $n = 50$ 成立时缺 $10$ 人（如补一档 $[30, 40)$ 频数 $10$，频率 $0.2$）。
  + 极差 $= 45$，组距 $= 5$：$[0, 5), [5, 10), [10, 15), ..., [45, 50)$ 共 $10$ 档（起点 $0$ 保证覆盖下界 $2$）；或从 $[0, 5)$ 起到 $[45, 50)$ 止，$47$ 落入 $[45, 50)$ ✓。
  + $0.24 times 200 = 48$ 人。

  *应用*
  + (a) 极差 $= 34$，组距 $= 5$：$[8, 13), [13, 18), [18, 23), [23, 28), [28, 33), [33, 38), [38, 43)$——共 $7$ 档。适合想细看分布的场合。
    (b) 组距 $= 10$，起点 $5$：$[5, 15), [15, 25), [25, 35), [35, 45)$——共 $4$ 档。适合只想看大致形状的场合。
  + $< 160$ 的人 $= (0.10 + 0.18) times 100 = 28$ 人；$>= 170$ 的人 $= (0.14 + 0.06) times 100 = 20$ 人。频率和 $= 0.10 + 0.18 + 0.28 + 0.24 + 0.14 + 0.06 = 1.00$ ✓。
  + 甲 $[80, 100]$ 频率 $= 0.45 + 0.20 = 0.65$，绝对人数 $= 0.65 times 40 = 26$ 人。乙 $= 0.30 + 0.20 = 0.50$，人数 $= 0.50 times 40 = 20$ 人。甲班 $[80, 100]$ 比例更高。

  *挑战*
  + 合并相邻两档：$[0, 20)$ 频率 $= 0.02 + 0.10 = 0.12$；$[20, 40)$ $= 0.40 + 0.30 = 0.70$；$[40, 60)$ $= 0.12 + 0.06 = 0.18$。信息损失：原来 $[10, 20)$ 与 $[0, 10)$ 两档不同被合并；$[20, 30)$ 是众档与 $[30, 40)$ 这档也被合并——合并后“哪一档最集中”的分辨率下降。
  + $n = 50$、组距取 $0.5$，极差 $= 40$ 的情况下可能得到 $80$ 档。平均每档不到 $1$ 个数据，很多档频数为 $0$、频率为 $0$——分布表变得几乎没有“分布”可言，反而看不出集中趋势。
  + 异常点会*单独占据一档（频数 $1$、频率很低）*，而其他档照常；读取时这档看起来“稀疏”但不至于显眼。但它会使*极差*被*显著拉长*（最大值跳到异常点）——上一章已指出极差对极端值敏感；频率分布表对它的反应比极差*温和*。这也是下一节引入方差的一个动机。
]
