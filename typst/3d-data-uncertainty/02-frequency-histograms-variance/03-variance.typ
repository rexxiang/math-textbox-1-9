#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref

=== 方差与标准差：比极差更细的一把尺子 <tool:dt06-variance>

#vocab[方差 variance / 标准差 standard deviation / 偏差 deviation / 离散程度 spread]

#crisis[
  甲班 $5$ 位同学的作业用时（分钟）：$0, 5, 5, 5, 10$。
  乙班 $5$ 位同学的作业用时（分钟）：$0, 0, 5, 10, 10$。

  两班*平均都是 $5$ 分钟*，*极差都是 $10$ 分钟*。老师盯着这两组看：

  - 甲班*三个人都压在 $5$ 分钟*左右，只有两端各一人偏了；
  - 乙班*两人很快、两人很慢*，中间一个人——明显比甲班“分得更开”。

  从直觉讲，两班*不一样*；可极差说它们一样。

  *极差只看最大最小两个值，对“中间的数据分得有多开”完全无感。我们需要一把看*所有*数据点的新尺子：方差。*
]

#history-note[
  $1918$ 年英国统计学家 Ronald A. Fisher 在研究遗传学数据时正式提出*方差（variance）*概念——“把每个偏差平方后取平均”。平方的巧思同时做了两件事：让正负偏差不互相抵消，又让较大的偏差被放大（从而惩罚“分得很开”的数据）。此后方差成为所有后续统计理论的基石。
]

#discovery[
  *偏差 $x_i - bar(x)$：每个点到中心的有符号距离*

  先算平均数 $bar(x) = 5$。
  - 甲班偏差：$0 - 5, 5 - 5, 5 - 5, 5 - 5, 10 - 5 = -5, 0, 0, 0, 5$。
  - 乙班偏差：$0 - 5, 0 - 5, 5 - 5, 10 - 5, 10 - 5 = -5, -5, 0, 5, 5$。

  *能直接把偏差平均吗？*

  看甲班偏差之和：$(-5) + 0 + 0 + 0 + 5 = 0$。乙班偏差之和：$(-5) + (-5) + 0 + 5 + 5 = 0$。

  *偏差之和永远是 $0$*——因为 $bar(x)$ 本来就定义为“让所有偏差加起来归零”的那个数。偏差的平均数也*永远是 $0$*，对区分两班毫无用处。

  *解决办法：把偏差平方后再平均*

  平方后两件事发生：
  - 负号消失（$(-5)^2 = 25$）——正负偏差不再抵消；
  - 大偏差被*放大*（$5^2 = 25$ vs $1^2 = 1$）——分得越开，贡献越大。

  - 甲班偏差平方：$25, 0, 0, 0, 25$；平均 $= (25 + 0 + 0 + 0 + 25) / 5 = 50 / 5 = 10$。
  - 乙班偏差平方：$25, 25, 0, 25, 25$；平均 $= (25 + 25 + 0 + 25 + 25) / 5 = 100 / 5 = 20$。

  得到两个数 $10$ 和 $20$——*乙班分得更开*这件事终于能被定量区分了。

  *方差公式*

  一组数据 $x_1, x_2, ..., x_n$，平均 $bar(x)$。定义*方差*：

  $ s^2 = 1 / n sum_(i = 1)^n (x_i - bar(x))^2. $

  $s^2$ 总是*非负*（平方和的平均）；$s^2 = 0$ 当且仅当所有 $x_i = bar(x)$——数据完全一样。

  *单位问题与标准差*

  甲班作业用时*单位是分钟*。方差 $s^2 = 10$——但单位变成了*平方分钟*（因为平方过了），读起来怪怪的。为了回到“分钟”的单位，把方差再开平方：

  $ s = sqrt(s^2) = sqrt(1 / n sum (x_i - bar(x))^2). $

  $s$ 叫*标准差*（standard deviation）。
  - 甲班：$s = sqrt(10) approx 3.16$ 分钟；
  - 乙班：$s = sqrt(20) = 2 sqrt(5) approx 4.47$ 分钟。

  标准差和原始数据*同一单位*，可读性更好。$s$ 粗略理解为“数据到均值的*典型距离*”——乙班的典型距离明显比甲班大。

  *极差 vs. 方差：什么时候用谁？*

  #align(center, table(
    columns: (1fr, 1fr),
    inset: 6pt,
    stroke: 0.4pt,
    [*极差*], [*方差 / 标准差*],
    [只看最大、最小], [看每一个数据点],
    [对唯一极端点特别敏感], [对所有点平权（平方后大偏差略放大）],
    [单位与原数据相同], [方差是平方单位；标准差同原单位],
    [计算一步], [多步（先均值、再偏差、再平方、再平均、再开方）],
    [初筛、快速对比], [正式报告、后续建模的标配],
  ))

  *经验*：快速口算 / 粗比较 → 极差；认真对比两组 → 方差 / 标准差。两者都能用时，优先报标准差（同单位、信息更全）。
]

#tryit[
  + 数据 $2, 4, 6, 8, 10$。求 $bar(x)$、各偏差、各偏差平方、方差、标准差。
  + 数据 $4, 5, 6, 7, 8$。重复上题的计算。与第 $1$ 题对比——均值同，哪组更集中？
  + 一组数据 $10, 10, 10, 10, 10$。方差是多少？结合定义说明直观含义。
]

#blueprint[
  *工具：方差 $s^2$ 与标准差 $s$*

  *定义*

  对一组数据 $x_1, ..., x_n$，均值 $bar(x) = (x_1 + ... + x_n) / n$。

  $ s^2 = 1 / n sum_(i = 1)^n (x_i - bar(x))^2, quad s = sqrt(s^2). $

  *计算流程*

  + 算 $bar(x)$。
  + 对每个 $i$ 算 $x_i - bar(x)$（偏差）。
  + 把每个偏差*平方*。
  + 所有平方加起来再除以 $n$——得到 $s^2$。
  + 开平方 — 得到 $s$。

  *性质*

  - $s^2 >= 0$；$s^2 = 0 arrow.l.r.double$ 所有 $x_i = bar(x)$。
  - $s^2$ 对*平移*不变：把数据每个都加 $c$，$bar(x)$ 也加 $c$，偏差不变 → $s^2$ 不变。
  - $s^2$ 对*缩放*二次响应：把数据每个都乘 $k$，偏差也乘 $k$，偏差平方乘 $k^2$ → $s^2$ 乘 $k^2$、$s$ 乘 $|k|$。

  *与极差的组合策略*

  #table(
    columns: (1fr, 1.6fr),
    inset: 6pt,
    stroke: 0.4pt,
    [*场景*], [*用哪把 spread 尺子*],
    [快速初筛 / 两组粗比], [极差（一步能出）],
    [正式对比 / 写进报告], [标准差（$s$，同原单位，信息全）],
    [怀疑有极端点时验证], [同时报极差和 $s$；极差被极端点拉扯，$s$ 被整组影响较温和],
  )
]

#pitfall[
  *高频错误*

  ✗ 算方差时把 $(x_i - bar(x))^2$ 写成 $|x_i - bar(x)|$
  → ✓ 方差*必须平方*，不是取绝对值。取绝对值得到的是另一种 spread 度量（平均绝对偏差 MAD），不是方差。

  ✗ 求方差时偏差不平方直接求和——结果总是 $0$
  → ✓ 偏差之和恒为 $0$（$bar(x)$ 的性质）——这正是我们*必须*先平方再求和的原因。

  ✗ 把*标准差*读成“与原数据同*单位*、与原数据同*大小*”
  → ✓ $s$ 与数据同*单位*（都是分钟 / 厘米 …），但*数值*是各偏差的一种“平均距离”，通常比极差小得多。

  ✗ 计算时用*样本方差除以 $n - 1$* 还是 $n$ 搞混
  → ✓ *本书统一用* $1 / n$（总体方差公式）。统计课里另有一种“样本方差”除以 $n - 1$（贝塞尔修正），动机在高中 / 大学讲。

  ✗ 算方差时先算 $s$ 再平方得 $s^2$
  → ✓ 流程是*先平方后平均*（得到 $s^2$），再开方得到 $s$。颠倒顺序与一般的“不等式”（$sqrt("均值") != "均值"(sqrt(\ ))$）有关，结果错。
]

#mastery[
  *基础*

  + 求 $1, 2, 3, 4, 5$ 的方差和标准差。
  + 求 $3, 5, 5, 7, 10$ 的均值、方差、标准差（保留两位小数）。
  + 数据 $7, 7, 7, 7$ 的方差是多少？

  *应用*

  + 甲、乙两名射击选手各 $5$ 次成绩（环）：
    - 甲：$9, 9, 10, 8, 9$；
    - 乙：$10, 7, 10, 7, 11$。
    （注：第 $5$ 环若 $= 11$，表示十环靶上靠近靶心的精确环，属合法记录。）算两人均值、极差、方差、标准差。哪位更稳？
  + 某厂两条生产线每日合格率（%）$5$ 天记录：
    - 线 A：$96, 97, 95, 96, 96$；
    - 线 B：$98, 92, 99, 93, 98$。
    哪条更稳定？用标准差说明。
  + 一组数据 $x_1, ..., x_n$ 的方差是 $4$。如果把每个数据都*加 $10$*，新方差是多少？如果把每个数据都*乘 $3$*，新方差是多少？

  *挑战 ☞ 选做*

  + 设 $n = 5$ 的一组正数数据方差 $= 0$，均值 $= 7$。这组数据唯一吗？写出它。
  + 一组 $n = 5$ 的数据：$1, 3, 5, 7, 9$。计算 $s^2$。另一组将每个数平移 $+100$ 得 $101, 103, 105, 107, 109$，$s^2$ 改变吗？若再把每个数*乘 $2$* 变成 $202, 206, 210, 214, 218$，$s^2$ 又变多少？
  + 方差 $s^2$ 的*单位*是“（原单位）$""^2$”（平方分钟、平方厘米等）。标准差 $s$ 与原单位一致。这为什么让 $s$ 比 $s^2$ 更适合写进报告正文？

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + $bar(x) = 3$；偏差 $-2, -1, 0, 1, 2$；偏差平方 $4, 1, 0, 1, 4 = 10$；$s^2 = 10 / 5 = 2$；$s = sqrt(2) approx 1.41$。
  + 和 $= 30$，$bar(x) = 6$；偏差 $-3, -1, -1, 1, 4$；平方 $9, 1, 1, 1, 16 = 28$；$s^2 = 28 / 5 = 5.6$；$s = sqrt(5.6) approx 2.37$。
  + 所有数相等 $=> s^2 = 0$。

  *应用*
  + 甲：$bar(x) = (9 + 9 + 10 + 8 + 9) / 5 = 45 / 5 = 9$；极差 $= 10 - 8 = 2$；偏差 $0, 0, 1, -1, 0$；平方 $0, 0, 1, 1, 0 = 2$；$s^2 = 2 / 5 = 0.4$；$s = sqrt(0.4) approx 0.63$。乙：$bar(x) = (10 + 7 + 10 + 7 + 11) / 5 = 45 / 5 = 9$；极差 $= 11 - 7 = 4$；偏差 $1, -2, 1, -2, 2$；平方 $1, 4, 1, 4, 4 = 14$；$s^2 = 14 / 5 = 2.8$；$s = sqrt(2.8) approx 1.67$。甲的 $s$ 更小——*甲更稳*。
  + A：$bar(x) = 96$；偏差 $0, 1, -1, 0, 0$；平方 $0, 1, 1, 0, 0 = 2$；$s^2 = 0.4$；$s approx 0.63$。B：$bar(x) = 96$；偏差 $2, -4, 3, -3, 2$；平方 $4, 16, 9, 9, 4 = 42$；$s^2 = 8.4$；$s approx 2.90$。A 更稳定（$s$ 更小）。
  + 平移不改方差，仍为 $4$。每个数乘 $3 =>$ 方差乘 $3^2 = 9 =>$ 新方差 $= 36$。

  *挑战 ☞ 选做*
  + $s^2 = 0 =>$ 所有数据相等 $=>$ 唯一：$7, 7, 7, 7, 7$。
  + 原组：$bar(x) = 5$；偏差 $-4, -2, 0, 2, 4$；平方 $16, 4, 0, 4, 16 = 40$；$s^2 = 40 / 5 = 8$。平移 $+100$ 后 $s^2$ *不变*，仍 $= 8$。再乘 $2$：$s^2$ 乘 $4 =>$ 新 $s^2 = 32$。
  + $s^2$ 单位是平方量（$"分钟"^2$、$"厘米"^2$），无直接物理含义；$s$ 与原数据单位一致，可以说“这些用时的典型偏离大约是 $3$ 分钟”——读者能*直接*在原量纲下理解 spread 的大小。报告时用 $s$ 更直观。
]
