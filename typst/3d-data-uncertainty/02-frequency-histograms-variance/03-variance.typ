// [wave7-format-break: 模式C]
#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref, answer-cut, self-check
#import "../../lib/diagram-packages.typ": cetz

=== 方差与标准差：比极差更细的一把尺子 <tool:dt06-variance>

#vocab[方差 variance][标准差 standard deviation][偏差 deviation][离散程度 spread]

#crisis[
  甲班 $5$ 位同学的作业用时（分钟）：$0, 5, 5, 5, 10$。
  乙班 $5$ 位同学的作业用时（分钟）：$0, 0, 5, 10, 10$。

  两班*平均都是 $5$ 分钟*，*极差都是 $10$ 分钟*。老师盯着这两组看：

  - 甲班*三个人都压在 $5$ 分钟*左右，只有两端各一人偏了；
  - 乙班*两人很快、两人很慢*，中间一个人——明显比甲班“分得更开”。

  从直觉讲，两班*不一样*；可极差说它们一样。

  *极差只看最大最小两个值，对“中间的数据分得有多开”完全无感。有没有一把尺子，能看到*所有*数据点的离散程度？*
]

#tryit[
  + 甲班作业用时（分钟）：$0, 5, 5, 5, 10$；乙班：$0, 0, 5, 10, 10$。两班均值和极差都相同。不用任何公式，发明一种计算方法，给两班各算出一个数，使乙班的数比甲班大。（任何方法都可以，只要能区分出来。）
]

#discovery[
  *先用旧工具试试。* 甲班作业用时 $0, 5, 5, 5, 10$，乙班 $0, 0, 5, 10, 10$。先试极差：甲班 $10 - 0 = 10$，乙班 $10 - 0 = 10$——相同，无法区分。再试"偏差"——计算每个数距均值 $bar(x) = 5$ 的差，然后平均。

  试着推下去——

  甲班偏差：$-5, 0, 0, 0, 5$；偏差之和 $= -5 + 0 + 0 + 0 + 5 = 0$，平均偏差 $= 0$。乙班偏差：$-5, -5, 0, 5, 5$；偏差之和 $= -5 - 5 + 0 + 5 + 5 = 0$，平均偏差也 $= 0$。两班的"平均偏差"都是零，和极差一样，根本区分不了。

  *这里卡住了。* 极差只看两个极端值；直接平均偏差永远等于零（因为均值的定义就是让正负偏差互消）。两种方法都看不到"所有数据点的分散程度"，需要一种能让正负偏差*不互相抵消*的新工具。

  *偏差 $x_i - bar(x)$：每个点到中心的有符号距离*

  先算平均数 $bar(x) = 5$。
  - 甲班偏差：$0 - 5, 5 - 5, 5 - 5, 5 - 5, 10 - 5 = -5, 0, 0, 0, 5$。
  - 乙班偏差：$0 - 5, 0 - 5, 5 - 5, 10 - 5, 10 - 5 = -5, -5, 0, 5, 5$。

  *能直接把偏差平均吗？*

  看甲班偏差之和：$(-5) + 0 + 0 + 0 + 5 = 0$。乙班偏差之和：$(-5) + (-5) + 0 + 5 + 5 = 0$。

  *偏差之和永远是 $0$*——因为 $bar(x)$ 本来就定义为“让所有偏差加起来归零”的那个数。偏差的平均数也*永远是 $0$*，对区分两班毫无用处。

  *解决办法：把偏差平方后再平均*

  平方后两件事发生：
  - 负号消失（$(-5)^2 = 25$）——正负偏差不再抵消；
  - 大偏差被*放大*（$5^2 = 25$ vs $1^2 = 1$）——分得越开，贡献越大。

  - 甲班偏差平方：$25, 0, 0, 0, 25$；平均 $= (25 + 0 + 0 + 0 + 25) / 5 = 50 / 5 = 10$。
  - 乙班偏差平方：$25, 25, 0, 25, 25$；平均 $= (25 + 25 + 0 + 25 + 25) / 5 = 100 / 5 = 20$。

  把两班数据画在数轴上，用线段标出每个点到 $bar(x) = 5$ 的偏差：

  #figure(
    cetz.canvas(length: 0.45cm, {
      import cetz.draw: *
      // 甲班
      content((-2, 2), text(size: 7pt)[*甲班*], anchor: "east")
      line((-0.5, 2), (12, 2), stroke: 0.4pt, mark: (end: ">"))
      for val in (0, 2, 4, 5, 6, 8, 10) {
        line((val, 1.7), (val, 2.3), stroke: 0.2pt)
        content((val, 1.2), text(size: 5pt)[#val])
      }
      line((5, 0.8), (5, 4.5), stroke: (paint: rgb("#C62828"), thickness: 0.5pt, dash: "dashed"))
      content((5, 4.8), text(size: 5pt, fill: rgb("#C62828"))[$bar(x) = 5$], anchor: "south")
      // 甲班点 0,5,5,5,10
      for v in (0, 5, 5, 5, 10) {
        circle((v, 2.7), radius: 0.15, fill: rgb("#1976D2"))
      }
      line((0, 3.1), (5, 3.1), stroke: 0.4pt + rgb("#1976D2"))
      line((10, 3.1), (5, 3.1), stroke: 0.4pt + rgb("#1976D2"))
      // 乙班
      content((-2, -1), text(size: 7pt)[*乙班*], anchor: "east")
      line((-0.5, -1), (12, -1), stroke: 0.4pt, mark: (end: ">"))
      for val in (0, 2, 4, 5, 6, 8, 10) {
        line((val, -1.3), (val, -0.7), stroke: 0.2pt)
        content((val, -1.8), text(size: 5pt)[#val])
      }
      line((5, -2.5), (5, 0.5), stroke: (paint: rgb("#C62828"), thickness: 0.5pt, dash: "dashed"))
      // 乙班点 0,0,5,10,10
      for v in (0, 0, 5, 10, 10) {
        circle((v, -0.4), radius: 0.15, fill: rgb("#C62828"))
      }
      line((0, -0.1), (5, -0.1), stroke: 0.4pt + rgb("#C62828"))
      line((10, -0.1), (5, -0.1), stroke: 0.4pt + rgb("#C62828"))
    }),
    caption: [甲班 vs 乙班：到 $bar(x) = 5$ 的偏差线。甲班偏差短（$s^2 = 10$），乙班偏差长（$s^2 = 20$）]
  )

  得到两个数 $10$ 和 $20$——*乙班分得更开*这件事终于能被定量区分了。

  *方差公式*

  一组数据 $x_1, x_2, ..., x_n$，平均 $bar(x)$。定义*方差*：

  $ s^2 = 1 / n sum_(i = 1)^n (x_i - bar(x))^2. $

  $s^2$ 总是*非负*（平方和的平均）；$s^2 = 0$ 当且仅当所有 $x_i = bar(x)$——数据完全一样。

  *单位问题与标准差*

  甲班作业用时*单位是分钟*。方差 $s^2 = 10$——但单位变成了*平方分钟*（因为平方过了），读起来怪怪的。为了回到“分钟”的单位，把方差再开平方：

  $ s = sqrt(s^2) = sqrt(1 / n sum (x_i - bar(x))^2). $

  $s$ 叫*标准差*（standard deviation）。
  - 甲班：$s = sqrt(10) approx 3.16$ 分钟；
  - 乙班：$s = sqrt(20) = 2 sqrt(5) approx 4.47$ 分钟。

  标准差和原始数据*同一单位*，可读性更好。$s$ 粗略理解为“数据到均值的*典型距离*”——乙班的典型距离明显比甲班大。

  *极差 vs. 方差：什么时候用谁？*

  #align(center, table(
    columns: (1fr, 1fr),
    inset: 6pt,
    stroke: 0.4pt,
    [*极差*], [*方差 / 标准差*],
    [只看最大、最小], [看每一个数据点],
    [对唯一极端点特别敏感], [对所有点平权（平方后大偏差略放大）],
    [单位与原数据相同], [方差是平方单位；标准差同原单位],
    [计算一步], [多步（先均值、再偏差、再平方、再平均、再开方）],
    [初筛、快速对比], [正式报告、后续建模的标配],
  ))

  *经验*：快速口算 / 粗比较 → 极差；认真对比两组 → 方差 / 标准差。两者都能用时，优先报标准差（同单位、信息更全）。
]

#tryit[
  + 数据 $2, 4, 6, 8, 10$。求 $bar(x)$、各偏差、各偏差平方、方差、标准差。
  + 数据 $4, 5, 6, 7, 8$。重复上题的计算。与第 $1$ 题对比——均值同，哪组更集中？
]

#history-note[
  方差的思想根源可追溯到高斯（Carl F. Gauss）：他在 $19$ 世纪初研究天文观测误差时就使用了"偏差平方和"。但正式把*方差（variance）*定义为独立概念并赋予它统计理论核心地位的，是 $1918$ 年英国统计学家费歇尔（Ronald A. Fisher）。他在研究遗传学数据时提出"把每个偏差平方后取平均"。平方的巧思同时做了两件事：让正负偏差不互相抵消，又让较大的偏差被放大（从而惩罚"分得很开"的数据）。此后方差成为所有后续统计理论的基石。
]

#blueprint[
  *工具：方差 $s^2$ 与标准差 $s$*

  *定义*

  对一组数据 $x_1, ..., x_n$，均值 $bar(x) = (x_1 + ... + x_n) / n$。

  $ s^2 = 1 / n sum_(i = 1)^n (x_i - bar(x))^2, quad s = sqrt(s^2). $

  *计算流程*

  + 算 $bar(x)$。
  + 对每个 $i$ 算 $x_i - bar(x)$（偏差）。
  + 把每个偏差*平方*。
  + 所有平方加起来再除以 $n$——得到 $s^2$。
  + 开平方 — 得到 $s$。

  *性质*

  - $s^2 >= 0$；$s^2 = 0 arrow.l.r.double$ 所有 $x_i = bar(x)$。
  - $s^2$ 对*平移*不变：把数据每个都加 $c$，$bar(x)$ 也加 $c$，偏差不变 → $s^2$ 不变。
  - $s^2$ 对*缩放*二次响应：把数据每个都乘 $k$，偏差也乘 $k$，偏差平方乘 $k^2$ → $s^2$ 乘 $k^2$、$s$ 乘 $|k|$。

  某工厂质检：抽检 $10$ 个零件重量（克），目标 $50.0$ 克。数据为 $49.8, 50.1, 50.0, 49.9, 50.2, 50.0, 49.7, 50.3, 50.0, 50.0$，标准差 $s approx 0.17$ 克——典型偏离不到 $0.2$ 克。

  #figure(
    cetz.canvas(length: 0.45cm, {
      import cetz.draw: *
      line((-0.5, 0), (14, 0), stroke: 0.5pt, mark: (end: ">"))
      content((14.3, 0), text(size: 6pt)[克], anchor: "west")
      for val in (497, 498, 499, 500, 501, 502, 503) {
        let x = (val - 495) * 1.2
        line((x, -0.3), (x, 0.3), stroke: 0.2pt)
        let label = val / 10
        content((x, -0.8), text(size: 4pt)[#calc.round(label, digits: 1)], anchor: "north")
      }
      // 手工放置点（50.0 出现 4 次需叠放）
      let xs = (2.4, 3.6, 4.8, 6.0, 6.0, 6.0, 6.0, 7.2, 8.4, 9.6)
      let ys = (0.6, 0.6, 0.6, 0.6, 1.05, 1.5, 1.95, 0.6, 0.6, 0.6)
      for i in range(xs.len()) {
        circle((xs.at(i), ys.at(i)), radius: 0.16, fill: rgb("#1976D2"))
      }
      // 均值
      let mx = 6.0
      line((mx, -1.5), (mx, -0.5), stroke: 0.6pt + rgb("#C62828"), mark: (end: ">"))
      content((mx, -1.8), text(size: 5pt, fill: rgb("#C62828"))[$bar(x) = 50.0$], anchor: "north")
      // 标准差范围
      let sl = 6.0 - 0.17 * 12
      let sr = 6.0 + 0.17 * 12
      line((sl, 2.6), (sr, 2.6), stroke: 0.5pt + rgb("#388E3C"), mark: (start: "|", end: "|"))
      content(((sl + sr) / 2, 3.0), text(size: 5pt, fill: rgb("#388E3C"))[$plus.minus s approx plus.minus 0.17$], anchor: "south")
    }),
    caption: [零件重量点图：大部分点落在 $bar(x) plus.minus s$ 范围内——$s$ 就是"典型离散宽度"]
  )

  *与极差的组合策略*

  #table(
    columns: (1fr, 1.6fr),
    inset: 6pt,
    stroke: 0.4pt,
    [*场景*], [*用哪把 spread 尺子*],
    [快速初筛 / 两组粗比], [极差（一步能出）],
    [正式对比 / 写进报告], [标准差（$s$，同原单位，信息全）],
    [怀疑有极端点时验证], [同时报极差和 $s$；极差被极端点拉扯，$s$ 被整组影响较温和],
  )
]

#self-check[
  方差越大，意味着数据“分散”还是“集中”？说出方差公式里“减去平均值再平方”的目的。
]


#pitfall[
  *高频错误*

  ✗ 算方差时把 $(x_i - bar(x))^2$ 写成 $|x_i - bar(x)|$
  → ✓ 方差*必须平方*，不是取绝对值。取绝对值得到的是另一种 spread 度量（平均绝对偏差 MAD），不是方差。

  ✗ 求方差时偏差不平方直接求和——结果总是 $0$
  → ✓ 偏差之和恒为 $0$（$bar(x)$ 的性质）——这正是我们*必须*先平方再求和的原因。

  ✗ 把*标准差*读成“与原数据同*单位*、与原数据同*大小*”
  → ✓ $s$ 与数据同*单位*（都是分钟 / 厘米 …），但*数值*是各偏差的一种“平均距离”，通常比极差小得多。

  ✗ 计算时用*样本方差除以 $n - 1$* 还是 $n$ 搞混
  → ✓ *本书统一用* $1 / n$（总体方差公式）。统计课里另有一种“样本方差”除以 $n - 1$（贝塞尔修正），动机在高中 / 大学讲。

  ✗ 算方差时先算 $s$ 再平方得 $s^2$
  → ✓ 流程是*先平方后平均*（得到 $s^2$），再开方得到 $s$。颠倒顺序与一般的“不等式”（$sqrt("均值") != "均值"(sqrt(\ ))$）有关，结果错。
]

#mastery[
  *帮小颖找错*——下面是同学小颖在算方差时的解题过程，她在均值 / 偏差平方 / 单位 / 反推上踩了几个典型坑。请你逐题指出她错在哪一步、为什么错，并写出正确的算法或结论。

  *基础*

  + 小颖求 $1, 2, 3, 4, 5$ 的均值：「$bar(x) = (1 + 2 + 3 + 4 + 5) = 15$。」
  + 小颖求 $1, 2, 3, 4, 5$ 的方差：先得偏差 $-2, -1, 0, 1, 2$；然后写「$s^2 = (-2 + (-1) + 0 + 1 + 2) / 5 = 0$，所以方差是 $0$。」请指出她哪一步漏了关键操作。
  + 数据 $7, 7, 7, 7$，小颖计算："不全是同一个数吗？我直接报告 $s^2 = 7$。"请改正。
  + 小颖求 $3, 5, 5, 7, 10$ 的方差：均值算成 $bar(x) = 5$；偏差 $-2, 0, 0, 2, 5$；偏差平方和 $= 4 + 0 + 0 + 4 + 25 = 33$；$s^2 = 33 / 5 = 6.6$。请找出她哪一步算错了。

  *应用*

  + 甲射击成绩 $9, 9, 10, 8, 9$（$bar(x) = 9, s^2 = 0.4$），乙射击成绩 $10, 7, 10, 7, 11$（$bar(x) = 9, s^2 = 2.8$）。小颖说："乙的最大值 $11$ 比甲的最大值 $10$ 大，所以*乙更稳*。"请改正她的判断依据。
  + 一组数据方差 $= 4$，每个数加上 $10$。小颖说："每个数都增加了 $10$，所以方差也增加 $10$，新方差是 $14$。"请用方差的平移性质改正她。

  *挑战*

  + 小颖算甲班作业用时 $0, 5, 5, 5, 10$ 的方差，得到 $s^2 = 10$。她在报告里写："甲班的*典型偏离约 $10$ 分钟*。"请指出她漏掉了哪一步换算（提示：$10$ 的单位是什么？），并写出正确的"典型偏离"和单位。
  + 小颖看到一组数据"方差 $= 0$、均值 $= 7$、$n = 5$"，她说："*方差为 $0$ 只是说大部分数据接近 $7$，所以可能是 $6, 7, 7, 7, 8$ 这样的组合。*"请用方差为零的充要条件反驳她，并写出唯一可能的数据。

  + *[反直觉]*：若把一组数据每个值都扩大 $2$ 倍，方差会变成原来的几倍？标准差呢？为什么和"平移不变"不一样？
  + *[组合]*：甲组数据 $1, 3, 5, 7, 9$，乙组数据 $3, 4, 5, 6, 7$。两组均值都是 $5$，不用公式，直接通过观察解释哪组方差更大。再用公式验证。
  + 方差的"加法公式"：若两组数据 $A$ 和 $B$ 分别有均值 $mu_A = mu_B$ 且方差 $s^2_A$、$s^2_B$，将它们合并成一组，合并后的方差是否等于 $frac(s^2_A + s^2_B, 2)$？用具体例子检验。
  + *[构造题]*：构造一组 $4$ 个数，使均值 $= 5$，方差 $= 5$。

#answer-cut[

  *基础*
  + 漏了"除以 $n$"。$bar(x) = 15 div 5 = 3$，不是 $15$。"求和"只是均值的第一步，必须再除以个数。
  + 漏了*平方*这一步。直接对*偏差*求平均一定得 $0$（这是 $bar(x)$ 的定义），所以才需要先平方再求平均。正确：偏差平方 $4, 1, 0, 1, 4$ 之和 $= 10$；$s^2 = 10/5 = 2$。
  + 全等数据应当 $s^2 = 0$，不是 $7$。她把数据值当成了方差。$s^2 = 0$ 当且仅当所有 $x_i = bar(x)$，正是这种情况。
  + 第三个偏差错了：$5 - bar(x) = 5 - 6 = -1$，不是 $0$（她把 $bar(x)$ 当成 $5$ 了；正确均值 $bar(x) = (3 + 5 + 5 + 7 + 10) / 5 = 6$）。重算：偏差 $-3, -1, -1, 1, 4$；平方 $9, 1, 1, 1, 16 = 28$；$s^2 = 28/5 = 5.6$。

  *应用*
  + 单点比较与"稳"无关。"稳"是看*整体离散程度*，要用方差或标准差比。$s^2_"甲" = 0.4 < s^2_"乙" = 2.8$，*甲*更稳。乙的最大值大只说明它范围更宽，不等于稳定。
  + 平移*不改变方差*。每个 $x_i$ 加 $c$，$bar(x)$ 也加 $c$，偏差 $x_i - bar(x)$ 完全不变 $=>$ $s^2$ 不变 $=$ $4$，不是 $14$。她把"数据的变化"和"方差的变化"等同起来了。

  *挑战*
  + 漏了开平方。$s^2 = 10$ 的单位是*平方分钟*（$"分钟"^2$），不是分钟。"典型偏离"应当读*标准差* $s = sqrt(10) approx 3.16$ *分钟*——和原数据同单位。她直接把方差当成了"分钟数"，相当于把面积值当成长度报告。
  + 错。$s^2 = 0$ 的*充要条件*是所有 $x_i = bar(x)$；只要有任何一个 $x_i != bar(x)$，方差严格 $> 0$。所以"$6, 7, 7, 7, 8$"的方差不是 $0$（是 $0.4$）。唯一可能：$7, 7, 7, 7, 7$。她把"方差小"和"方差等于零"混为一谈。
  + *[反直觉]*：缩放（每个乘以 $k = 2$）：新偏差 $= 2(x_i - bar(x))$，平方后乘 $4$，方差乘 $k^2 = 4$；标准差乘 $|k| = 2$。不同于平移的原因：平移后偏差不变，而缩放后偏差等比例放大（方差对缩放二次响应 vs 对平移零响应）。
  + 直觉：甲组 $1, 3, 5, 7, 9$ 中最小 $1$ 和最大 $9$ 离均值 $5$ 很远，而乙组 $3, 4, 5, 6, 7$ 最小 $3$ 和最大 $7$ 离均值近得多，所以甲组方差更大。公式验证：甲偏差平方 $= 16 + 4 + 0 + 4 + 16 = 40$，$s^2_甲 = 8$；乙偏差平方 $= 4 + 1 + 0 + 1 + 4 = 10$，$s^2_乙 = 2$。$s^2_甲 = 8 > s^2_乙 = 2$ ✓（直觉 + 方差计算验证）。
  + 当两组*大小相等*时成立。$A = {3, 7}$（$n=2$，均值 $5$，方差 $4$），$B = {4, 6}$（$n=2$，均值 $5$，方差 $1$）：合并 ${3, 4, 6, 7}$，方差 $= 10/4 = 2.5 = (4+1)/2$ ✓。但若两组大小不同，公式就不成立：$A = {5}$（$n=1$，方差 $0$），$B = {1, 9}$（$n=2$，方差 $16$）；合并 ${5, 1, 9}$（均值 $5$，偏差平方 $0 + 16 + 16$，方差 $= 32/3 approx 10.67$），而 $(0+16)/2 = 8 != 10.67$——反例成立。结论：等大小且均值相同时公式成立，大小不同时一般不成立（方差的合并性质 + 反例构造）。
  + *[构造题]*：$n = 4$，均值 $5$，方差 $5$：$sum(x_i - 5)^2 = 4 times 5 = 20$。设偏差为 $d_1, d_2, d_3, d_4$，满足 $sum d_i = 0$，$sum d_i^2 = 20$。取 $d = 1, 1, -1, -1$：平方和 $= 4 != 20$。取 $d = 3, 1, -1, -3$：平方和 $= 9+1+1+9 = 20$ ✓，和 $= 0$ ✓。数据：$2, 4, 6, 8$（均值 $5$ ✓，方差 $= 20/4 = 5$ ✓）（方差计算 + 构造思路）。
]
]
