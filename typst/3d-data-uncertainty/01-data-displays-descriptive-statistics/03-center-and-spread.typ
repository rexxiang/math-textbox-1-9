#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref, lab, answer-cut, self-check
#import "../../lib/diagram-packages.typ": cetz

=== 用一两个数字概括：center 与 spread <tool:dt03-center-and-spread>

#vocab[平均数 mean][中位数 median][众数 mode]

#crisis[
  某小公司 $6$ 位员工月薪（元）：$3000, 3000, 3000, 3500, 4000, 15000$。

  老板招聘时把月薪总和 $31500$ 除以 $6$，得到*平均工资 $5250$ 元*，写进了招聘广告。
  新员工入职后发现——$6$ 个人里有 $5$ 个拿得*比 $5250$ 少*，“平均工资”这个数字*代表不了谁*。

  这引出两件事：

  + 一把数据*有没有一个“代表性的中间”？* “平均 $5250$”是，那是唯一选择吗？
  + 就算有代表性的中间，*光报它够不够？* 同样是“平均 $85$ 分”，有的班大家都考 $80$~$90$，有的班有人考 $60$ 有人考 $100$——这两个班显然不一样。

  我们要*两种*数字去描述一把数据：一个告诉“中间在哪”，一个告诉“散得多宽”。本节发明第一套 center + spread 工具。
]

#tryit[
  甲乙两位射击选手各打 $5$ 枪：甲 $8, 9, 9, 10, 9$；乙 $6, 10, 10, 7, 12$。
  - 谁的平均环数高？
  - 谁发挥更稳定？用哪个数字说明？
]

#discovery[
  甲班 $5$ 人成绩 $80, 80, 80, 80, 80$，平均 $80$；乙班 $5$ 人成绩 $60, 70, 80, 90, 100$，平均也是 $80$。算到这里，两个班"完全一样"。

  可甲班人人都是 $80$，稳如一块砖；乙班最高 $100$、最低 $60$，跨度 $40$ 分。两班用同一份试卷、同一套教法，课堂感受却不会一样——平均分却看不出这种差别。

  把每个数据都压扁到一个点，分散程度就被丢光了。一组数据要描述清楚，光说"中间在哪"还不够，还得说"散得有多开"。

  *中间在哪：三种“center”*

  *平均数（mean）*：所有数加起来除以个数。记作 $bar(x)$。
  $ bar(x) = (x_1 + x_2 + ... + x_n) / n. $

  小公司薪资：$(3000 + 3000 + 3000 + 3500 + 4000 + 15000) / 6 = 31500 / 6 = 5250$ 元。

  *中位数（median）*：把数据*从小到大排序*后位于正中间的那个数。
  - $n$ 奇数：正中间的那个就是中位数；
  - $n$ 偶数：中间两个数的平均。

  小公司薪资排序：$3000, 3000, 3000, 3500, 4000, 15000$；中间两个是第 $3$、第 $4$ 位 $= 3000, 3500$；中位数 $= (3000 + 3500) / 2 = 3250$ 元。

  *众数（mode）*：出现次数*最多*的那个数。可能不唯一、甚至没有。

  小公司薪资里 $3000$ 出现 $3$ 次、其他各 $1$ 次，众数 $= 3000$ 元。

  *三个 center 放在一起*

  $ bar(x) = 5250, quad "中位数" = 3250, quad "众数" = 3000. $

  把 $6$ 个薪资画成数轴上的点，再标出三个 center 的位置：

  #figure(
    cetz.canvas(length: 0.45cm, {
      import cetz.draw: *
      line((-0.5, 0), (17, 0), stroke: 0.5pt, mark: (end: ">"))
      content((17.3, 0), text(size: 6pt)[元], anchor: "west")
      for val in (0, 3000, 6000, 9000, 12000, 15000) {
        let x = val / 1000
        line((x, -0.3), (x, 0.3), stroke: 0.3pt)
        content((x, -0.9), text(size: 5pt)[#val], anchor: "north")
      }
      circle((3, 0.6), radius: 0.18, fill: rgb("#1976D2"))
      circle((3, 1.1), radius: 0.18, fill: rgb("#1976D2"))
      circle((3, 1.6), radius: 0.18, fill: rgb("#1976D2"))
      circle((3.5, 0.6), radius: 0.18, fill: rgb("#1976D2"))
      circle((4, 0.6), radius: 0.18, fill: rgb("#1976D2"))
      circle((15, 0.6), radius: 0.18, fill: rgb("#1976D2"))
      line((5.25, -2.0), (5.25, -0.6), stroke: 0.6pt + rgb("#C62828"), mark: (end: ">"))
      content((5.25, -2.3), text(size: 5pt, fill: rgb("#C62828"))[$bar(x) = 5250$], anchor: "north")
      line((3.25, 2.5), (3.25, 1.8), stroke: 0.6pt + rgb("#388E3C"), mark: (end: ">"))
      content((4.8, 2.8), text(size: 5pt, fill: rgb("#388E3C"))[中位数 $= 3250$], anchor: "south")
      line((3, 2.5), (3, 2.0), stroke: 0.6pt + rgb("#7B1FA2"), mark: (end: ">"))
      content((1.2, 2.8), text(size: 5pt, fill: rgb("#7B1FA2"))[众数 $= 3000$], anchor: "south")
    }),
    caption: [小公司 $6$ 人月薪点图：$15000$ 的极端值把 $bar(x)$ 拉到右侧，中位数和众数紧贴大多数]
  )

  平均数被 $15000$ 这个*极端值*（老板那份）拉高了 $2000$；中位数和众数却几乎感受不到它。这正是为什么“平均工资 $5250$”代表不了普通员工——极端值对平均数影响大，对中位数、众数影响小。

  *center 的选择经验*

  - 若数据*没有极端值、分布比较对称*：用*平均数*，信息最全。
  - 若存在*极端值*（贫富悬殊、个别异常）：用*中位数*更具代表性。
  - 若要找“*最常见*的那个值”（最常见的鞋码、最热销的商品）：用*众数*。

  *散得多宽：最朴素的 spread——极差*

  只报 center 不够。对比两个班期末数学成绩：

  #align(center, table(
    columns: 7,
    align: center,
    stroke: 0.4pt,
    [*甲班*], [$80$], [$82$], [$85$], [$88$], [$90$], [平均 $85$，极差 $10$],
    [*乙班*], [$70$], [$75$], [$85$], [$95$], [$100$], [平均 $85$，极差 $30$],
  ))

  用点图把两班数据画在同一把尺子上：

  #figure(
    cetz.canvas(length: 0.45cm, {
      import cetz.draw: *
      content((-2, 1.5), text(size: 7pt)[*甲班*], anchor: "east")
      line((0, 1.5), (13, 1.5), stroke: 0.4pt, mark: (end: ">"))
      for val in (70, 75, 80, 85, 90, 95, 100) {
        let x = (val - 70) * 0.4
        line((x, 1.2), (x, 1.8), stroke: 0.2pt)
        content((x, 0.7), text(size: 4pt)[#val])
      }
      let jia = (80, 82, 85, 88, 90)
      for v in jia {
        circle(((v - 70) * 0.4, 2.1), radius: 0.16, fill: rgb("#1976D2"))
      }
      content((-2, -1), text(size: 7pt)[*乙班*], anchor: "east")
      line((0, -1), (13, -1), stroke: 0.4pt, mark: (end: ">"))
      for val in (70, 75, 80, 85, 90, 95, 100) {
        let x = (val - 70) * 0.4
        line((x, -1.3), (x, -0.7), stroke: 0.2pt)
        content((x, -1.7), text(size: 4pt)[#val])
      }
      let yi = (70, 75, 85, 95, 100)
      for v in yi {
        circle(((v - 70) * 0.4, -0.4), radius: 0.16, fill: rgb("#C62828"))
      }
      let avg-x = (85 - 70) * 0.4
      line((avg-x, -2.2), (avg-x, 2.8), stroke: (paint: rgb("#388E3C"), thickness: 0.5pt, dash: "dashed"))
      content((avg-x, 3.2), text(size: 6pt, fill: rgb("#388E3C"))[$bar(x) = 85$], anchor: "south")
    }),
    caption: [甲班（蓝）与乙班（红）期末成绩点图：平均都是 $85$，但甲班紧凑、乙班分散]
  )

  两班平均都是 $85$ 分，但甲班紧凑、乙班分散。只报“平均 $85$”无法区分二者。

  *极差（range）$=$ 最大值 $-$ 最小值*。计算最简单：排个序、头减尾。甲班 $90 - 80 = 10$，乙班 $100 - 70 = 30$——乙班散得多。

  *center + spread：配套使用*

  报告一把数据时，*标配*是一对数字（center $+$ spread）。比如：“这次考试平均 $85$ 分、极差 $10$ 分” → 能传达*两件*事，比只报“平均 $85$”丰富得多。

  *极差的小局限*：极差只看*两头*，不看中间。两组数据可能极差一样、但“中间的密度”差得远——下一章的*方差*会补上这个短板。本节先把极差用熟。
]

#lab[
  *班级数据调查：亲手体会 center 与 spread*

  *材料*：纸、笔、直尺（画数轴用）。

  *步骤*：

  + 调查班上（或身边）$10$~$15$ 位同学的一项数值数据，例如：昨晚睡了几小时？上周读了几页课外书？今天书包有多重（千克）？
  + 把收集到的数据从小到大排序，写在表格里。
  + 分别计算：平均数 $bar(x)$、中位数、众数（如有）、极差。
  + 在一条数轴上画出每个数据点（点图），并标出三个 center 的位置。

  *观察*：
  - 三个 center 位置接近吗？如果差得远，你的数据里有没有"极端值"在捣乱？
  - 如果把数据中最大的那个数改成原来的两倍，哪些 center 会变？哪些几乎不动？
  - 光看平均数能不能区分"大家差不多"和"有人特别多有人特别少"？你需要 spread 帮忙吗？
]

#tryit[
  + 一组数：$2, 4, 4, 5, 7, 9$。求平均数、中位数、众数、极差。
]

#history-note[
  *平均数*的使用至少可追溯到 $18$ 世纪天文学：高斯（Carl F. Gauss）和勒让德（Adrien-Marie Legendre）把多次观测取平均来减小误差，由此发展出最小二乘法。*中位数*则在 $1880$ 年代由高尔顿（Francis Galton）系统推广；因为计算只需要*排序*不需要*求和*，中位数在"少数极端值拉歪平均"的场合特别稳健。*极差*是各种离散度量里最朴素的一种——"最大值减最小值"几乎不需要数学，却已能对比两组数据散得开不开。
]

#blueprint[
  *工具：三个 center + 一个 spread*

  *center（集中趋势，刻画“中间在哪”）*

  #table(
    columns: (1fr, 1.5fr, 1.2fr),
    inset: 6pt,
    stroke: 0.4pt,
    [*名称*], [*定义*], [*对极端值的敏感度*],
    [平均数 $bar(x)$], [$(x_1 + ... + x_n) / n$], [*高*（极端值会把它拉走）],
    [中位数], [排序后位于正中的值；偶数个数据取中间两数平均], [*低*（只看位置，不看数值）],
    [众数], [出现次数最多的值；可能多个或没有], [*低*（只关心频次）],
  )

  *spread（离散程度，刻画“散得多宽”）*

  - 最朴素：*极差* $= max - min$。
  - 优点：计算极简，对“直觉上散不散”抓得准。
  - 缺点：只看两端，对中间分布不敏感；对*唯一一个极端点*特别敏感。第 2 章会升级到*方差*。

  *“center + spread”一起报*

  任何一份数据都至少该报一对 $("center", "spread")$。只报 center 是“丢掉一半信息”。
]

#self-check[
  平均数、中位数、众数三者一定相等吗？什么时候差距最大？举一个你自己想到的例子。
]


#pitfall[
  *高频错误*

  ✗ 把*数据*直接加起来不排序就算中位数
  → ✓ 中位数必须*先排序*。位置是按排序后的顺序数出来的。

  ✗ 偶数个数据时，中位数取中间*任意一个*
  → ✓ 必须取中间两个数的*平均*。

  ✗ 众数 $=$ “数据里最大的数”或“出现频率最高的数字符”
  → ✓ 众数 $=$ 出现*次数*最多的数值。如果所有值都只出现一次，则*无众数*。

  ✗ 把“极端值 $15000$”直接从平均数中剔除以“让平均数更准”
  → ✓ 不该随便剔除数据。这种情况下应该*改用中位数*；剔除极端值需要明确的统计理由。

  ✗ 报告数据只给平均数不给 spread
  → ✓ 只给 center 的数据描述不完整——两组 mean 相同、spread 差得远的场景太常见。
]

#side-hack[
  *两条路对比：平均数 vs 中位数（何时哪个更能代表"典型"？）*

  #table(
    columns: (auto, 1fr, 1fr),
    inset: 6pt,
    stroke: 0.4pt,
    [*情境*], [*平均数 $overline(x)$*], [*中位数*],
    [何时更具代表性], [数据分布接近对称、无极端值（如班级成绩 $78, 80, 82, 85, 88$：均值 $82.6$，中位数 $82$，两者接近，均值包含全部数据的信息）], [有明显极端值或长尾（如月薪 $3000, 3000, 3000, 3500, 4000, 15000$：均值 $5250$ 高于五人实际水平，中位数 $3250$ 更接近"大多数人"的薪资）],
    [典型陷阱], [有一两个极端大值就把均值拉偏：上例中一个 $15000$ 把均值拉高了约 $2000$，让"平均薪资"失去代表性], [偶数个数据时中位数是"中间两数的平均"，不能只取其中一个；另外不排序就"数到中间位置"的结果也是错的],
    [建议先用], [描述"整体水平""总体分布"或后续需要计算方差时用均值（如班级人均分、人均消耗量）], [描述"典型值""大多数人的水平"时用中位数（如房价中位数、工资中位数）；均值与中位数相差超过 $10\%$ 时，说明数据中有极端值在拉偏均值],
  )

  *经验法则*：数据对称且无极端值 → 均值（信息最全）；有极端值或严重偏斜 → 中位数更稳健。均值与中位数相差很大，几乎可以断定数据中存在极端值。
]

#mastery[
  *基础*

  + 一组数：$1, 3, 3, 5, 8, 9$。求平均数、中位数、众数、极差。
  + 求 $10, 10, 20, 30, 50$ 的平均数、中位数、众数、极差。
  + 某次 $5$ 道题作业一位学生得分 $7, 8, 8, 9, 10$。老师说“平均成绩 $8.4$ 分”——这是哪类数字？极差是多少？

  *应用*

  + 某小店 $7$ 天日销量（件）：$10, 12, 13, 12, 40, 11, 12$。第 $5$ 天是店庆，存在极端值。
    - 用平均数描述“日销量水平”合适吗？
    - 用哪个 center 更稳？
    - 报告 spread 时用极差合不合适？
  + 一个班 $20$ 位同学身高（cm）中位数 $162$、平均数 $163$、极差 $30$。转来一位新同学身高 $180$ cm。描述*更新后*的平均数、中位数、极差各会怎样变化（定性即可）。
  + 射击队三人成绩（$10$ 发环数）：
    - Kofi：$9, 9, 9, 9, 9, 9, 9, 9, 9, 9$；
    - 小王：$7, 7, 7, 13, 13, 13, 7, 7, 13, 13$；
    - 小张：$8, 8, 10, 10, 8, 10, 8, 10, 10, 8$。
    三人总环数 / 平均各是多少？用 center + spread 解释谁最稳。

  *挑战*

  + 一组数据 $n = 5$，平均数 $= 10$，中位数 $= 10$，众数 $= 10$，极差 $= 0$。这组数据能是什么？可以不同吗？
  + 再设一组 $n = 5$，平均数 $= 10$、中位数 $= 10$、极差 $= 0$，但*没有众数*。这可能吗？说明理由。
  + 在一组 $n = 5$ 的正数数据里，能否同时做到“平均数 $= 50$、中位数 $= 10$”？如果能，举例；不能则给出简短论证。

  + *[反直觉]*：从一组数据中删去最大值，平均数一定变小吗？删去最大值，中位数一定变小吗？各举一个反例或验证。
  + *[组合]*：$5$ 个数据的平均数为 $8$，中位数为 $6$，极差为 $10$。能否确定这 $5$ 个数据？如果不能，给出两组不同的满足条件的数据集。
  + *[构造题]*：构造一组 $6$ 个正整数，使得平均数 $= 10$、中位数 $= 8$、极差 $= 20$。

#answer-cut[

  *基础*
  + 平均数 $= (1 + 3 + 3 + 5 + 8 + 9) / 6 = 29 / 6 approx 4.83$（平均数公式 $bar(x) = sum / n$）；排序已好，中位数 $= (3 + 5) / 2 = 4$（中位数：排序后中间值）；众数 $= 3$（出现 $2$ 次）；极差 $= 9 - 1 = 8$（极差 $= max - min$）。
  + 平均数 $= 120 / 5 = 24$（$bar(x) = sum / n$）；中位数 $= 20$（中位数：排序后中间值）；众数 $= 10$；极差 $= 50 - 10 = 40$（极差 $= max - min$）。
  + 平均数（$bar(x)$）；极差 $= 10 - 7 = 3$（极差 $= max - min$）。

  *应用*
  + 先算平均数：$bar(x) = (10 + 12 + 13 + 12 + 40 + 11 + 12) / 7 = 110 / 7 approx 15.7$——被 $40$ 拉高。再排序求中位数：$10, 11, 12, 12, 12, 13, 40$，中位数 $= 12$，更能代表普通日销量（中位数抗极端值）。极差 $= 40 - 10 = 30$，受极端值影响大——如果只想了解“日常波动”，应考虑*去除店庆日*或改用方差类度量。
  + 逐项分析新数据对三个指标的影响：平均数会*上升*（新增 $180 > 163$）；中位数*上升很小或不变*（中位数只看位置，一个数加入仅移动半档）；极差*变大*（$180$ 若大于原最大值，则极差 $= 180 - min$ 增大）。
  + 先算 center：三人总环数都 $90$，$bar(x) = 9$。再算 spread：Kofi*极差 $0$*（最稳）；Arjun*极差 $6$*（最不稳）；小张*极差 $2$*（中等）。仅凭平均数分不出高下；center + spread 配套才看得清。

  *挑战*
  + 从极差入手：极差 $= 0$ 意味着 $max = min$——所有数据相同。所以只能是 $10, 10, 10, 10, 10$，*唯一*（极差 $= 0$ 的充要条件）。
  + 由极差推导：极差 $= 0 =>$ 所有数据相同 $=> $ 这个共同值必定是众数。因此*不可能*“极差 $= 0$ 又没有众数”（众数的定义 + 极差性质）。
  + 构造极端值拉高均值：$10, 10, 10, 10, 210$。$bar(x) = (40 + 210) / 5 = 50$，中位数 $= 10$ ✓。存在——极端值 $210$ 把平均数拉高了 $40$，中位数完全不动（平均数对极端值敏感，中位数稳健）。
  + 删去最大值后平均数不一定变小：如果最大值恰好等于平均数，删去后平均数不变；若数据集为 $5, 5, 5, 5, 5$，删去一个 $5$，平均数仍为 $5$。删去最大值，中位数不一定变小：若最大值不影响中间位置（如 $1, 2, 3, 4, 100$，中位数为 $3$；删去 $100$ 后 $1, 2, 3, 4$，中位数变为 $2.5$——反而变小了，但这是变小的例子；若 $1, 1, 3, 3, 3$，删去 $3$（最大），剩 $1, 1, 3, 3$，中位数 $= 2$，确实变小）。反例构造：$2, 5, 5, 5, 8$，删去 $8$，剩 $2, 5, 5, 5$，中位数 $= 5$，与原来 $5$ 相同——不变（平均数与中位数对删除操作的敏感性）。
  + 不能唯一确定。例一：$4, 6, 6, 8, 14$（均值 $38/5 = 7.6 != 8$，需重新构造）。令 $5$ 数之和 $= 40$，中位数（第三个）$= 6$，极差 $= 10$。设 $a_1 <= a_2 <= 6 <= a_4 <= a_5$，$a_5 - a_1 = 10$，$sum = 40$。例：$2, 4, 6, 8, 12$（和 $=32 != 40$）。设 $a_1 = c$，$a_5 = c + 10$，令 $a_2 = a_3 = 6$，$a_4 = ?$：$c + 6 + 6 + a_4 + c + 10 = 40 => 2c + a_4 = 18$。取 $c = 2, a_4 = 14$（但 $a_4 <= a_5 = 12$，矛盾）。取 $c = 3, a_4 = 12$，$a_5 = 13$——$a_4 <= a_5$ ✓，例一：$3, 6, 6, 12, 13$（和 $= 40$ ✓，中位数 $= 6$ ✓，极差 $= 10$ ✓）。例二：取 $a_2 = 5$：$c + 5 + 6 + a_4 + c + 10 = 40 => 2c + a_4 = 19$，取 $c = 4, a_4 = 11, a_5 = 14$：$4, 5, 6, 11, 14$（和 $= 40$ ✓）（center + spread 联合约束 + 构造）。
  + *[构造题]*：平均数 $= 10$，$6$ 数之和 $= 60$；中位数 $= 8$（第 $3$、$4$ 个平均为 $8$，可取都是 $8$）；极差 $= 20$（最大减最小 $= 20$）。设最小 $= m$，最大 $= m + 20$。令 $a_3 = a_4 = 8$，$a_2 = 7, a_5 = 9$：和 $= m + 7 + 8 + 8 + 9 + (m+20) = 2m + 52 = 60 => m = 4$，最大 $= 24$。验证：$4, 7, 8, 8, 9, 24$，均值 $= 60/6 = 10$ ✓，中位数 $= (8+8)/2 = 8$ ✓，极差 $= 24-4 = 20$ ✓（center + spread 同时约束 + 系统构造）。
]
]

#history-note[
  *海伦·沃克（Helen Walker，$1891$–$1983$）*——美国统计学家与统计学史学家，哥伦比亚大学教授。她的著作《统计方法的研究》系统梳理了"平均值与离差"这套核心工具的演化史：从早期天文学家把多次测量取平均、到高斯用方差刻画测量误差，再到现代统计学把"中心 + 离散"作为描述任意一组数据的两根支柱。$1944$ 年她当选美国统计学会首位女会长。这一节学的"平均数 + 极差/方差"，正是她笔下的统计基本套路。
]
