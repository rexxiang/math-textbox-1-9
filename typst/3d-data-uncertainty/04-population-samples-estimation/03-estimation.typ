#import "../../lib/theme-v2.typ": vocab, crisis, history-note, discovery, tryit, blueprint, pitfall, mastery, lab, answer-cut
#import "../../lib/diagram-packages.typ": cetz

=== 点估计：用样本推总体 <tool:dt12-estimation>

#vocab[
  *本节发明“用样本均值 $bar x$ 估总体均值 $mu$、用样本频率 $f_n$ 估总体比例 $p$”的点估计工具*，并刻画其*随机性*——每次抽样得到的估计值不同，但随 $n$ 增大集中到真值附近。
]

#crisis[
  - 想知道全校初三*平均身高 $mu$*。真正把 $300$ 人都量一遍显然不现实。抽 $n = 30$ 人，量出 $bar x = 164.2$ cm——*能就这样说 $mu = 164.2$ 吗？*
  - 同一批大米，两位质检员各随机抽了 $20$ 袋称重，$bar x_1 = 5.02$ kg，$bar x_2 = 4.98$ kg——*谁对？*
  - 民调机构报“$1200$ 人随机抽样，支持率 $72 \%$”——*这 $72 \%$ 离真实支持率 $p$ 到底有多准？*
]

#discovery[
  *点估计：一个参数配一个估计量*

  未知的总体参数（人类关心的数）和样本算出的统计量（手上能算的数）之间的对应：

  #table(
    columns: (1.2fr, 1.2fr, 1.2fr),
    inset: 6pt,
    stroke: 0.4pt,
    [*总体参数*], [*样本统计量*], [*意义*],
    [总体均值 $mu$], [样本均值 $bar x$], [$bar x -> mu$ 当 $n$ 增大],
    [总体比例 $p$], [样本频率 $f_n$], [$f_n -> p$（第 $3$ 章大数定律）],
    [总体方差 $sigma^2$], [样本方差 $s^2$], [$s^2 -> sigma^2$ 当 $n$ 增大],
  )

  *例 1（身高估计）*

  总体：本校初三 $300$ 人。$mu$ 是真实平均身高（未知）。SRS 抽 $n = 30$ 人量得
  $ bar x = 164.2 "cm". $
  我们*报告*：“$mu$ 的点估计为 $164.2$ cm”。用 $bar x$ 当作 $mu$ 的*估计值*——但并不宣称 $mu$ 恰好 $= 164.2$。

  *例 2（比例估计）*

  总体：某市成年人。$p$ 是真实支持率。SRS 抽 $n = 1200$，支持 $864$ 人，
  $ f_n = 864 / 1200 = 0.72. $
  报告：“$p$ 的点估计为 $72 \%$”——这是一个*数值估计*，并不是“真实 $p$ 恰好 $= 72 \%$”。

  *估计值是随机的*

  两位质检员各抽 $n = 20$ 袋大米（某品牌 $5$ kg 装）：$bar x_1 = 5.02$ kg、$bar x_2 = 4.98$ kg。谁对？——*都对也都不完全对*。真实 $mu$ 是唯一确定的数，但*每次抽样抽到的 $n$ 个样本不同*，算出来的 $bar x$ 自然不同。这叫*抽样随机性*（sampling variability）。

  #figure(
    cetz.canvas(length: 0.45cm, {
      import cetz.draw: *
      line((-0.5, 0), (16, 0), stroke: 0.5pt, mark: (end: ">"))
      content((16.3, 0), text(size: 6pt)[kg], anchor: "west")
      for val in (4.90, 4.95, 5.00, 5.05, 5.10) {
        let x = (val - 4.85) * 50
        line((x, -0.3), (x, 0.3), stroke: 0.3pt)
        content((x, -0.8), text(size: 5pt)[#calc.round(val, digits: 2)], anchor: "north")
      }
      // 多次抽样结果点
      let samples = (4.98, 5.02, 5.00, 4.97, 5.03, 5.01, 4.99, 5.04, 4.96, 5.01)
      for i in range(samples.len()) {
        let x = (samples.at(i) - 4.85) * 50
        circle((x, 0.6 + calc.rem(i, 3) * 0.45), radius: 0.14, fill: rgb("#1976D2"))
      }
      // 真值标记
      let mu-x = (5.00 - 4.85) * 50
      line((mu-x, -1.8), (mu-x, -0.5), stroke: 0.7pt + rgb("#C62828"), mark: (end: ">"))
      content((mu-x, -2.1), text(size: 5pt, fill: rgb("#C62828"))[$mu = 5.00$（真值）], anchor: "north")
      // 波动范围
      content((mu-x, 2.8), text(size: 5pt, fill: rgb("#388E3C"))[每次 $bar x$ 在真值附近随机波动], anchor: "south")
    }),
    caption: [$10$ 次 SRS 的 $bar x$ 值（蓝点）：每次抽样结果不同，但都在 $mu = 5.00$ 附近波动]
  )

  换一组例子更直观：

  *模拟：从 $mu = 170$ 的总体中反复抽 $n = 30$ 做身高样本*

  #table(
    columns: (auto, auto),
    inset: 6pt,
    stroke: 0.4pt,
    [*第 $k$ 次抽样*], [*$bar x_k$（cm）*],
    [$1$], [$169.4$],
    [$2$], [$171.8$],
    [$3$], [$170.0$],
    [$4$], [$168.9$],
    [$5$], [$171.2$],
    […], […],
  )

  每一行都是*真实抽样*会得到的结果。同一总体、同一 $n = 30$、同一抽样方式——$bar x$ 在 $mu = 170$ 附近*随机波动*。

  *$n$ 越大估计越稳*

  核心规律：*样本容量 $n$ 越大，$bar x$ 波动范围越小、越靠近 $mu$*。

  以某校初三学生身高（总体 $mu = 170$ cm）为例，画出不同 $n$ 下 $bar x$ 的典型波动范围：

  #figure(
    cetz.canvas(length: 0.45cm, {
      import cetz.draw: *
      line((-0.5, 0), (14, 0), stroke: 0.4pt, mark: (end: ">"))
      line((0, -0.5), (0, 9), stroke: 0.4pt, mark: (end: ">"))
      content((14.3, 0), text(size: 6pt)[$n$], anchor: "west")
      content((0, 9.5), text(size: 6pt)[$bar x$ 波动宽度], anchor: "south")
      // 数据：n 越大波动越小
      let ns = (2, 5, 8, 12)
      let widths = (8, 4, 2, 0.8)
      let n-labels = ("10", "40", "160", "1000")
      for i in range(ns.len()) {
        let x = ns.at(i)
        let h = widths.at(i)
        rect((x - 0.6, 0), (x + 0.6, h), fill: rgb("#1976D2").lighten(60%), stroke: 0.5pt + rgb("#1976D2"))
        content((x, -0.7), text(size: 5pt)[$n = #n-labels.at(i)$], anchor: "north")
        content((x, h + 0.3), text(size: 4pt)[$plus.minus #widths.at(i)$], anchor: "south")
      }
    }),
    caption: [$n$ 越大，$bar x$ 的波动范围越小——大数定律在连续量上的版本]
  )

  同一总体做三组模拟，每组做 $K$ 次 SRS：

  #table(
    columns: (auto, auto, auto),
    inset: 6pt,
    stroke: 0.4pt,
    [*$n$*], [*$bar x$ 的典型范围*], [*波动幅度*],
    [$10$], [$[165, 175]$], [$plus.minus 5$],
    [$40$], [$[168, 172]$], [$plus.minus 2$],
    [$160$], [$[169, 171]$], [$plus.minus 1$],
    [$1000$], [$[169.6, 170.4]$], [$plus.minus 0.4$],
  )

  （数值仅示意；精确刻画要到高中 / 大学学“标准误差 $= sigma \/ sqrt(n)$”。）

  直觉解释：这是*大数定律在连续量上的版本*——$n$ 越大，个体波动越能互相抵消，平均就越接近真值。
]

#lab[
  *摸球估比例：袋子里到底有多少红球？*

  *材料*：不透明布袋一个、红色小球 $20$ 颗、蓝色小球 $30$ 颗（让一位同学秘密放入，你不知道比例）、记录表。

  *步骤*：

  + 请一位同学在袋中放入红、蓝球共 $50$ 颗（你不知道各多少）。
  + *有放回抽样*：从袋中随机摸出一颗球，记录颜色，放回摇匀。重复 $50$ 次。
  + 计算红球出现的相对频率 $f_(50) = "红球次数" \/ 50$。用它估计袋中红球的比例 $p$，再推算红球数 $approx p times 50$。
  + 继续抽到 $100$ 次，重新算 $f_(100)$。估计值变了吗？更接近真实比例了吗？
  + 最后打开袋子数一数真实的红蓝比例。

  *观察*：
  - $50$ 次和 $100$ 次的估计值哪个更接近真实比例？
  - 如果不放回（每次摸完不放回去），结果会有什么不同？为什么本实验要求"放回"？
  - 这个过程和本节"用 $bar x$ 估计 $mu$"的逻辑有什么相似之处？
]

#tryit[
  + 从 $mu = 60$、$sigma = 10$ 的总体中做 SRS。某次 $n = 20$ 样本算出 $bar x = 58.7$。这个*估计值*和 $mu$ 相差 $1.3$——这差距算“偏差”还是“随机波动”？
  + 同一总体，两组 SRS：$n = 25$ 得 $bar x_1 = 62.0$；$n = 400$ 得 $bar x_2 = 60.4$。哪个估计更可信？
]

#history-note[
  $19$ 世纪末英国统计学家皮尔逊（Karl Pearson）奠定了统计估计的早期框架。$20$ 世纪初费歇尔（Ronald A. Fisher）系统建立*点估计*理论，核心想法：把未知的总体参数（均值 $mu$、比例 $p$、方差 $sigma^2$）当作*待估对象*，用样本算出的*统计量*（$bar x$、$f_n$、$s^2$）当作*估计值*。Fisher 在 $1920$ 年代证明了：只要抽样是 SRS，$bar x$ 就是 $mu$ 的*无偏估计*——长远平均意义上，$bar x$ 不会系统高于或低于 $mu$。下一步怎么刻画"$bar x$ 有多准"，则要等到高中 $\/$ 大学讲抽样分布才能正式展开。
]

#blueprint[
  *工具 1：点估计（point estimate）*

  - 未知总体参数（$mu$、$p$、$sigma^2$）用样本统计量（$bar x$、$f_n$、$s^2$）*估计*。
  - 最常见对：
    - *$mu$ 的点估计 $= bar x$*。
    - *$p$ 的点估计 $= f_n$*。
  - “点估计”意思是：给出*一个*数作为估计，不给误差范围（那是区间估计，高中 / 大学讲）。

  *工具 2：估计的两种误差*

  + *随机误差（variability）*：每次 SRS 抽到不同样本 $=>$ $bar x$ 在 $mu$ 附近*随机*波动。这种误差用 $n$ 增大来*减小*。
  + *系统误差（bias）*：抽样方式本身有偏 $=>$ $bar x$ 稳定偏向一个*错*的值。这种误差*不能靠增大 $n$ 减小*——要靠 §2 说的 *SRS 原则*消除。

  合起来：*好估计 $=$ SRS（消除系统误差）$+$ 大 $n$（压缩随机误差）*。

  *工具 3：样本容量的经验阈值*

  - $n >= 30$：大多数实际场景的*下限*——样本均值可用正态近似（理由见高中课程）。
  - $n >= 100$：通常能给出相当稳定的估计。
  - $n >= 1000$：全国级民调的常见容量（抽样误差 $approx plus.minus 3\%$，已足够报道使用）。

  *注意*：以上阈值只针对*随机误差*。如果抽样方式有偏，$n$ 再大都白搭——《Literary Digest》$240$ 万也救不了。
]

#pitfall[
  ✗ 样本均值 $164.2$ $=>$ “$mu$ *就是* $164.2$”
  → ✓ $164.2$ 是*估计值*，$mu$ 是未知真值；两者一般不严格相等，但可以说“$mu$ 约为 $164.2$”。

  ✗ 两次估计不同 $=>$ “其中一次抽错了”
  → ✓ 都没错——都是*随机抽样*的合理结果。估计值本身是随机的。$n$ 更大的那次*一般*更可靠。

  ✗ $n$ 大 $=>$ 估计*一定*更接近 $mu$
  → ✓ $n$ 大 $=>$ 估计*平均*更接近、*波动范围*更小。单次抽样*不保证*。大数定律是*长远趋势*。

  ✗ 用*方便样本* $\/$ *自选样本*算出的 $bar x$ 当作估计
  → ✓ 系统偏差不能靠增大 $n$ 救——$bar x$ 会稳定指向错误方向。先修方法（SRS）再谈估计。

  ✗ 混淆“$bar x$ 是随机变量”和“$mu$ 是随机变量”
  → ✓ *$mu$ 是确定的常数*（只是我们不知道）；*$bar x$ 依样本而变*，是随机变量。
]

#mastery[
  *基础*

  + $mu$ 是 \_\_\_；$p$ 是 \_\_\_；它们通常 \_\_\_（已知 / 未知）。我们用样本算出的 $bar x$、$f_n$ *估计* 它们，这种方法叫 \_\_\_ 。
  + 从某箱苹果里 SRS 抽 $n = 25$ 称重，$bar x = 183.2$ g。箱子里全部苹果的平均重量的*点估计*是多少？这是不是说全部苹果平均重量*恰好* $= 183.2$ g？
  + 某民调：$n = 1500$，支持 $915$ 人。$p$ 的点估计是多少？

  *应用*

  + 工厂日产灯泡 $10000$ 只。随机检 $n = 50$ 只，$8$ 只不合格。*不合格率 $p$* 的点估计是多少？当天预计不合格灯泡数约多少只？
  + 同一家商店，两天各 SRS 抽 $n = 20$ 位顾客问消费额：第 $1$ 天 $bar x_1 = 86$ 元，第 $2$ 天 $bar x_2 = 79$ 元。差 $7$ 元。你能断言“第 $2$ 天消费变低了”吗？至少要补充哪些信息才敢下结论？
  + 一个总体 $mu = 50$，$sigma = 12$。做 $100$ 次 SRS 模拟：
    - (a) 若 $n = 25$，$bar x$ 大致会落在什么范围？
    - (b) 若 $n = 900$，这范围变成多大？
    - (c) $n$ 翻 $36$ 倍，波动范围大致缩小到原来的几分之几？（提示：$1 / sqrt(36) = 1 / 6$）

  *挑战 ☞ 选做*

  + 国家统计局发布“全国城镇居民人均可支配收入 $= 5$ 万元”，依据是对 $10$ 万户的抽样调查。两位评论员争议：
    - 评论员 A：“$10$ 万户也就是全国 $0.02 \%$ 的家庭，样本太小，结论不可信。”
    - 评论员 B：“SRS 的误差主要看 $n$ 不看*比例* $n \/ N$，$10$ 万户已经相当稳健。”
    谁更有道理？用本节工具 $2$ / 工具 $3$ 的语言展开论证。
  + 想估计某湖中鲤鱼总数 $N$（标记 $-$ 重捕法）。第一次捕 $50$ 条标记放回，第二次捕 $n_2 = 80$ 条中有 $k = 16$ 条有标记。
    - (a) 哪个*样本比例*被用于估计哪个*总体比例*？
    - (b) 用点估计原则写出 $N$ 的估计公式并算出估计值。
    - (c) 若第二次改捕 $n_2 = 800$ 条有 $k = 160$ 条标记，点估计值变吗？这个更大的 $n_2$ 给你更多信心吗？为什么？

  #answer-cut[

  + $mu$ 是*总体均值*；$p$ 是*总体比例*；通常*未知*；方法叫*点估计*（点估计：$bar(x)$ 估 $mu$）。
  + $mu$ 的点估计 $= bar x = 183.2$ g。*不是*恰好 $= 183.2$；这只是*估计值*。
  + $f_n = 915 \/ 1500 = 0.61 =>$ $p$ 的点估计 $= 61 \%$（频率估概率）。
  + $f_n = 8 \/ 50 = 0.16 =>$ $p$ 的点估计 $= 16 \%$（频率估概率）。当天预计不合格 $approx 0.16 times 10000 = 1600$ 只。
  + *不能断言*（点估计：$bar(x)$ 估 $mu$，但每次估计值不同）。$bar x_1 - bar x_2 = 7$ 元完全可能是*随机波动*。需要补充：两天样本的方差 / 标准差；两天是不是 SRS；其他因素（天气、促销、节假日）。“差异是否显著”属于后续统计推断课题。
  + (a) $n = 25 =>$ $bar x$ 波动范围大致 $[mu plus.minus 2 sigma \/ sqrt(n)] = [50 plus.minus 2 times 12 \/ 5] = [50 plus.minus 4.8] = [45.2, 54.8]$；(b) $n = 900 =>$ $[50 plus.minus 2 times 12 \/ 30] = [49.2, 50.8]$；(c) $bar x$ 波动范围缩小为原来的 $1 \/ sqrt(36) = 1 \/ 6$。（$plus.minus 2 sigma \/ sqrt(n)$ 的公式要到高中才严格证明，本章先感性理解。）
  + *评论员 B 有道理*（点估计：$bar(x)$ 估 $mu$，精度取决于 $n$ 而非 $n \/N$）。随机误差主要取决于 $n$（样本容量本身），而不取决于 $n \/ N$（样本占总体的比例）。$10$ 万户的 SRS 在精度上远超 $n = 1000$ 的 SRS，且早已满足 $n >= 1000$ 的民调阈值。评论员 A 的错觉把“$n \/ N$ 小”当作“不可信”，这是对*抽样随机性*的误解。$n \/ N$ 只在*有限总体修正*（高中选修）里才有微小影响。
  + (a) 第二次捕中“有标记的比例” $k \/ n_2 = 16 \/ 80 = 0.20$ 被用于估计“湖中有标记鱼的比例” $50 \/ N$（频率估概率）。(b) $50 \/ N approx 16 \/ 80 =>$ $N approx 50 times 80 \/ 16 = 250$。(c) $n_2 = 800$，$k = 160$：比例仍是 $0.20$，点估计仍 $= 250$。*但信心更大*——因为 $n_2$ 大，随机波动更小，$k \/ n_2 = 0.20$ 离真实比例更近的可信度更高。
  ]
]
