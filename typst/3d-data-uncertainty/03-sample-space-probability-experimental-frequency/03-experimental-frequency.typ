#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref, lab
#import "../../lib/diagram-packages.typ": cetz

=== 实验频率与概率：做得多了会稳定 <tool:dt09-experimental-frequency>

#vocab[相对频率 relative frequency][经验概率 empirical probability][大数定律 law of large numbers（朴素版）]

#crisis[
  古典概率公式 $P(A) = |A| \/ |Omega|$ 只在*有限 $+$ 等可能*的场景用得上。很多现实问题两个前提都不满足：

  - 一枚图钉抛出去*钉尖朝上*的概率是多少？没人事先知道——图钉不像硬币那样对称。
  - 某药一次治疗成功率是多少？同样不能靠“数样本点”推导。
  - 某条生产线一批灯泡合格率是多少？每只灯泡是不是合格是实验*出来*的，不是*推*出来的。

  遇到这种场景，大家的办法是：*做大量试验，看结果比例稳定在哪儿*。图钉抛 $1000$ 次，$620$ 次朝上——大致可以说“这种图钉朝上的概率大约 $0.62$”。

  *这套“用做实验的频率估计概率”的做法背后，藏着一条统计学中最根本的事实——大数定律。本节给出它的朴素版与一套动手估计概率的流程。*
]

#history-note[
  $18$ 世纪 Jacob Bernoulli 在《*Ars Conjectandi*》中严格证明了“当试验次数很大时，成功的相对频率几乎一定接近真实概率”——这就是*大数定律*（law of large numbers）。正是这条定律让“概率”这个看似抽象的数字有了*可验证*的现实含义：做一次感知不到概率，做够多次概率就从数据里*显形*。
]

#discovery[
  *相对频率*

  一次重复 $n$ 次的试验里，事件 $A$ 发生 $n_A$ 次。定义事件 $A$ 的*相对频率*：

  $ f_n(A) = n_A / n. $

  例：抛硬币 $100$ 次，正面 $53$ 次——$f_n("正") = 53 \/ 100 = 0.53$。

  相对频率*本身就是*上一节“古典概率公式”在“把每次试验都当作样本点”这张大样本空间上的特例：$|Omega| = n$（所有 $n$ 次试验等可能）；事件 $=$ $A$ 发生的那几次。

  *一次试验 vs. 大量试验*

  一次试验里 $A$ 要么发生要么不发生，相对频率只能是 $0$ 或 $1$——毫无信息。
  $10$ 次试验里相对频率可能仍然很偏：$7 / 10 = 0.7$ 或 $3 / 10 = 0.3$。
  到了 $1000$ 次、$10000$ 次，相对频率逐渐*稳定*下来。

  *朴素大数定律*

  一个核心事实：

  #align(center)[
    *随着试验次数 $n$ 增大，相对频率 $f_n(A)$ 会稳定在某个数附近——这个数就是事件 $A$ 的概率 $P(A)$*。
  ]

  用公式表示：$ lim_(n -> infinity) f_n(A) = P(A). $（严格的表述与证明是高中 / 大学内容；本章只用“稳定在……附近”的朴素版。）

  *历史著名实验*：$18$ 世纪的 Buffon 抛硬币 $4040$ 次，正面 $2048$ 次 $=>$ $f_n approx 0.5069$。$20$ 世纪初 Pearson 抛 $24000$ 次，正面 $12012$ 次 $=>$ $f_n approx 0.5005$。越做越贴近 $0.5$——这正是“正面概率 $= 0.5$”最经验的印证。

  把某班同学做的抛硬币实验结果画成折线图，可以看到频率如何收敛到 $0.5$：

  #figure(
    cetz.canvas(length: 0.45cm, {
      import cetz.draw: *
      line((-0.5, 0), (16, 0), stroke: 0.4pt, mark: (end: ">"))
      line((0, -0.5), (0, 8), stroke: 0.4pt, mark: (end: ">"))
      content((16.3, 0), text(size: 6pt)[$n$], anchor: "west")
      content((0, 8.5), text(size: 6pt)[$f_n$], anchor: "south")
      // P=0.5 理论线
      line((0, 4), (15, 4), stroke: (paint: rgb("#C62828"), thickness: 0.4pt, dash: "dashed"))
      content((15.5, 4), text(size: 5pt, fill: rgb("#C62828"))[$0.5$], anchor: "west")
      // 模拟数据
      let ns = (1, 2.5, 4, 6, 8, 10, 12, 14)
      let fs = (6, 5.2, 4.8, 4.3, 3.8, 4.1, 3.9, 4.05)
      for i in range(ns.len()) {
        circle((ns.at(i), fs.at(i)), radius: 0.12, fill: rgb("#1976D2"))
        if i > 0 {
          line((ns.at(i - 1), fs.at(i - 1)), (ns.at(i), fs.at(i)), stroke: 0.5pt + rgb("#1976D2"))
        }
      }
      let n-labels = ("10", "20", "50", "100", "200", "500", "1k", "2k")
      for i in range(ns.len()) {
        content((ns.at(i), -0.6), text(size: 4pt)[#n-labels.at(i)], anchor: "north")
      }
      for val in (0.3, 0.5, 0.7) {
        let y = val * 8
        line((-0.3, y), (0, y), stroke: 0.2pt)
        content((-0.6, y), text(size: 4pt)[#val], anchor: "east")
      }
    }),
    caption: [抛硬币频率收敛图：$n$ 越大，$f_n$ 波动越小，逐渐贴近 $P = 0.5$]
  )

  *用实验频率估计未知概率*

  当一个随机事件的概率*无法从样本空间推出*（图钉、药效、生产合格率等），标准办法是：

  + *大量*重复独立试验（$n$ 越大越好，至少 $>= 100$）。
  + 计算相对频率 $f_n(A) = n_A \/ n$。
  + 用它作为*经验概率*的估计。

  这个估计值不是精确的真概率——但它*会随着 $n$ 变大逐步变稳*。例如图钉抛 $100$ 次得 $f_n = 0.65$、抛 $500$ 次得 $f_n = 0.61$、抛 $1000$ 次得 $f_n = 0.62$——数字逐渐稳定在 $0.6$ 附近。

  *模拟：用随机数代替手工试验*

  实际上很多“试验”成本高、速度慢。这时候用电脑 / 计算器的*随机数*代劳——称为*模拟*（simulation）。

  例：想估计“抛 $3$ 枚硬币至少 $1$ 次正面”的概率。写程序生成 $10000$ 组独立的 $\{0, 1\}^3$，数“每组至少含 $1$ 个 $1$”的占比。结果应接近理论值 $7 \/ 8 = 0.875$。模拟次数越大越接近。

  *模拟是现代概率统计的核心工具之一*——尤其在事件复杂、样本空间难以枚举时，模拟往往比纸笔推导更容易。

  *实验频率 $<->$ 理论概率：两件事的关系*

  下面以某班 $40$ 名同学"抛图钉"实验为例：每位同学抛 $50$ 次，记录"钉尖朝上"的相对频率。$40$ 个频率值散布在 $0.55$~$0.70$ 之间，集中在 $0.62$ 附近——经验概率估计为 $hat(P) approx 0.62$。

  #figure(
    cetz.canvas(length: 0.45cm, {
      import cetz.draw: *
      line((-0.5, 0), (14, 0), stroke: 0.4pt, mark: (end: ">"))
      line((0, -0.5), (0, 8), stroke: 0.4pt, mark: (end: ">"))
      content((14.3, 0), text(size: 6pt)[$f_n$], anchor: "west")
      content((0, 8.5), text(size: 6pt)[人数], anchor: "south")
      // 频率分布条
      let ranges = ("0.55", "0.58", "0.60", "0.62", "0.64", "0.66", "0.70")
      let freqs = (2, 5, 8, 12, 8, 3, 2)
      for i in range(freqs.len()) {
        let x = i * 2
        let h = freqs.at(i) * 0.55
        rect((x, 0), (x + 1.8, h), fill: rgb("#1976D2").lighten(60%), stroke: 0.5pt + rgb("#1976D2"))
        content((x + 0.9, -0.6), text(size: 4pt)[#ranges.at(i)])
        content((x + 0.9, h + 0.3), text(size: 5pt)[#freqs.at(i)])
      }
    }),
    caption: [$40$ 位同学各抛图钉 $50$ 次后的相对频率分布：集中在 $0.62$ 附近——经验概率的直觉]
  )

  #align(center, table(
    columns: (1fr, 1fr),
    inset: 6pt,
    stroke: 0.4pt,
    [*理论概率 $P(A)$*], [*相对频率 $f_n(A)$*],
    [事件的真实可能性（“全知”角度）], [一次次试验*观测*到的比例],
    [由样本空间或模型推导（古典 / 几何 / ……）], [由数据统计得到],
    [一个*固定的数*], [*随机波动*的数——每做一轮就可能不同],
    [常用来做预测], [常用来*估计*真实概率（尤其当概率未知时）],
  ))

  *一句话*：概率在“背后”规定节奏；频率在“前台”显示节奏——做得多了，前台越来越像背后的样子。
]

#lab[
  *抛硬币实验：频率真的会稳定吗？*

  *材料*：一枚硬币、记录表、铅笔。

  *步骤*：

  + 准备一张表格，列标题为：次数 $n$、本次结果（正/反）、累计正面次数 $n_A$、相对频率 $f_n = n_A \/ n$。
  + 开始抛硬币，每抛一次就记录一行。至少抛 $50$ 次（如果有耐心，抛到 $100$ 次更好）。
  + 每隔 $10$ 次，在坐标纸上描一个点：横轴是 $n$，纵轴是 $f_n$。把这些点用折线连起来。
  + 在图上画一条 $f_n = 0.5$ 的水平虚线。

  *观察*：
  - 前 $10$ 次的 $f_n$ 波动大不大？$50$ 次之后呢？
  - 折线是不是越来越贴近 $0.5$ 那条虚线？
  - 有没有哪一段连续好几次都是正面（或反面）？这是不是说明硬币"不公平"？还是说小样本里这种"连续"其实很正常？
  - 如果全班同学各做一次，把大家的折线画在同一张图上，会看到什么？
]

#tryit[
  + 抛一枚硬币 $50$ 次，记录正面次数。你猜正面次数会是 $25$ 附近吗？会不会精确等于 $25$？
  + 一位同学抛图钉 $200$ 次，钉尖朝上 $124$ 次。估计“朝上”的概率。
  + 模拟“投两颗骰子、点数和 $= 7$”：理论概率 $1 \/ 6 approx 0.167$。如果你用计算器生成 $600$ 组 $(i, j)$，大致期望得到多少组和 $= 7$？
]

#blueprint[
  *工具：相对频率、经验概率与大数定律（朴素版）*

  *相对频率*

  $ f_n(A) = n_A / n quad (A "发生的次数 / 总试验次数"). $

  *大数定律（朴素版）*

  当 $n$ 越大，$f_n(A)$ 越稳定在 $P(A)$ 附近。形象地：*概率是频率的长期趋势*。

  *估计未知概率的流程*

  + *重复独立试验*：要求每次试验*独立、条件一致*。
  + *累计计数*：$n$ 和 $n_A$ 都要一并记下。
  + *算相对频率*：$f_n(A) = n_A \/ n$。
  + *报*估计概率：$hat(P)(A) approx f_n(A)$。$n$ 越大，估计越准。

  *模拟（用随机数代替人工试验）*

  - 优点：快、便宜、可重复任意多次。
  - 缺点：只能估计*模型允许*的概率（随机数的分布要与真实试验匹配）。
  - 判准：模拟 $n = 10^4$~$10^6$ 次后，$f_n(A)$ 通常已稳定到小数点后 $2$~$3$ 位。
]

#pitfall[
  *高频错误*

  ✗ 认为“抛 $10$ 次硬币必有 $5$ 次正面”（小样本的绝对期望）
  → ✓ 大数定律只说“*大量*试验后频率接近概率”。小样本随机波动大——$10$ 次得 $3$~$7$ 次正面都很常见。

  ✗ “前 $9$ 次都是正面，第 $10$ 次该出反面了”（赌徒谬误）
  → ✓ 独立硬币的每次投掷与历史无关。第 $10$ 次正面概率仍然是 $0.5$。

  ✗ 用相对频率 $f_n(A) = 0$（$n$ 次都没发生）直接得出 $P(A) = 0$
  → ✓ $n$ 有限时 $f_n = 0$ 只意味着“在这 $n$ 次里没观测到”——真实概率可能很小但不为 $0$。需要更大的 $n$ 才能稳定估计。

  ✗ 频率与概率概念混用
  → ✓ *频率*是数据统计量（随机波动）；*概率*是理论数字（固定）。在大样本极限下两者几乎相等，但在语义上始终区分。

  ✗ 用同一次小样本结果反复充当“独立实验”
  → ✓ 每次模拟 / 实验必须*独立*进行。记录 $n = 1000$ 的结果不能拆成 $100$ 份“独立结论”。
]

#mastery[
  *基础*

  + 抛硬币 $200$ 次，$112$ 次正面。估计 $P("正面")$。该估计和理论 $0.5$ 差多少？
  + 抛图钉 $500$ 次，$305$ 次朝上。估计 $P("朝上")$。
  + 一个袋子里有若干红球和蓝球，比例未知。放回抽 $1000$ 次，红球 $358$ 次。估计红球比例。若总共 $50$ 颗球，估计红球有多少颗？

  *应用*

  + 某新药做了 $400$ 次临床试验，$312$ 次见效。估计该药见效概率。若投入市场后一年有 $10000$ 名患者使用，期望见效人数大约多少？
  + 小明用计算器模拟抛两颗骰子 $10000$ 次，其中“点数之和 $= 7$”出现 $1680$ 次。$f_n("和 = 7")$ 是多少？与理论值 $1 \/ 6 approx 0.167$ 差多少？
  + 一场投篮训练做 $60$ 次投篮，命中 $42$ 次。估计命中率。如果第二天又投 $60$ 次命中 $45$ 次，估计值会怎样变化？*两天合并* 后的 $f_n$ 与仅第一天 / 第二天相比更可信吗？

  *挑战 ☞ 选做*

  + 一个游戏：$3$ 档奖券，特等 $0.01$、一等 $0.1$、二等 $0.5$，其他“未中”。用模拟估计“一张奖券至少中二等”的概率——应接近多少？若你实际连续模拟 $10000$ 张，$f_n$ 可能在什么范围内波动？
  + 一场考试前老师说“今天打了两年硬币，正面次数 $3210$，反面 $3190$，正面相对频率 $0.501$”。这组数据的 $n = $ ? 大致可以得出什么结论？
  + 模拟“$3$ 枚硬币至少 $1$ 次正面”的概率：理论 $= 7 \/ 8 = 0.875$。如果 $n = 100$ 估计得 $f_n = 0.84$，$n = 10000$ 估计得 $f_n = 0.873$，这两个值哪个更可信？是不是 $n$ 越大越一定更接近 $0.875$？

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + $f_n = 112 \/ 200 = 0.56$；与理论 $0.5$ 差 $0.06$（正常随机波动，若 $n$ 再增大可能更接近 $0.5$）。
  + $f_n = 305 \/ 500 = 0.61$。
  + $f_n = 358 \/ 1000 = 0.358 approx 0.36$；若总共 $50$ 颗 $=>$ 红球估计约 $0.36 times 50 = 18$ 颗。

  *应用*
  + $f_n = 312 \/ 400 = 0.78 =>$ $hat(P) approx 0.78$。预期见效 $approx 0.78 times 10000 = 7800$ 人。
  + $f_n = 1680 \/ 10000 = 0.168$；与 $0.1667$ 差 $approx 0.0013$——随机波动范围内，印证了理论。
  + 第一天 $f_n = 42 \/ 60 = 0.70$；第二天 $45 \/ 60 = 0.75$。合并后 $(42 + 45) \/ 120 = 87 \/ 120 = 0.725$。合并*更可信*：样本量更大、随机波动更小。
  
  *挑战 ☞ 选做*
  + 理论 $P("至少二等") = 0.01 + 0.1 + 0.5 = 0.61$。$n = 10000$ 下 $f_n$ 一般波动范围大约在 $[0.60, 0.62]$（凭大数定律估计）；具体数值因每轮模拟不同。
  + $n = 3210 + 3190 = 6400$。两年得 $f_n = 0.501$——与 $0.5$ 极其接近，符合“公平硬币”假设。
  + $n = 10000$ 时的 $f_n = 0.873$ 更可信（大样本随机波动更小）。但注意——*大数定律只保证“趋向”*，不保证每一轮更大的 $n$ 就一定更接近真值；单次 $n = 10000$ 的模拟里仍有可能出现 $f_n = 0.868$ 这样略偏的结果。$n$ 越大概率越大地接近，但不是单调确定的。
]
