#import "../../lib/theme-v2.typ": blueprint, side-hack, tryit, secref

=== 跨章综合练习 <meta:dt-synthesis-problems>

#blueprint[
  *怎样用这一节*

  下面 $5$ 道题每一题都明确标注了它所用到的*知识链*——即这一支数据分支里的哪几节。做每题之前，先把标出的章节引用过一遍，想一下“这里具体会用到那一节的哪个动作”，再动笔。

  做完后对照 #secref("meta:data-summary") 里的参考答案。如果某题卡住，到 #secref("meta:dt-repair-routes") 查“查漏指引”。
]

#tryit[
  *综合题 1* \
  涉及章节：#secref("dt01-organizing-data") + #secref("dt03-center-and-spread") + #secref("dt06-variance")

  某次小测 $10$ 人成绩为 $72, 85, 90, 68, 76, 88, 95, 70, 82, 74$。

  + 排序后给出*极差*与*中位数*；
  + 计算*平均数* $bar x$；
  + 计算*方差* $s^2$；说明哪个人对 $s^2$ 的“贡献”最大。

  ---

  *综合题 2* \
  涉及章节：#secref("dt04-frequency-distributions") + #secref("dt05-histograms")

  一份 $n = 100$ 的数据按组距 $5$ 分档得到频率 $0.04, 0.18, 0.30, 0.28, 0.14, 0.06$（区间 $[60, 90)$，共 $6$ 档）。

  + 验证 $sum p_k = 1$；
  + 计算每档*频率密度*；
  + 说明：若改成不等组距 $[60, 70), [70, 80), [80, 90)$（合并相邻两档），新的频率分布是什么？这时再画直方图，y 轴该取“频率”还是“频率密度”？为什么？

  ---

  *综合题 3* \
  涉及章节：#secref("dt07-sample-spaces") + #secref("dt08-probability-language")

  袋中 $5$ 红、$3$ 蓝、$2$ 黄共 $10$ 球，*不放回*依次摸两次。

  + 写出样本空间 $|Omega|$（用乘法原理）；
  + 求 $P("两次都是红")$；
  + 求 $P("一红一蓝")$（注意顺序：先红后蓝 + 先蓝后红）。
  + 这两个事件互斥吗？

  ---

  *综合题 4* \
  涉及章节：#secref("dt08-probability-language") + #secref("dt09-experimental-frequency")

  把一枚硬币抛 $200$ 次，得到正面 $112$ 次。

  + 计算正面的*相对频率* $f_n("正")$；
  + 这个值与“理论概率 $1/2$”的差距有多大？这是“硬币不公平”的证据吗？说明*为什么样本量不足以下结论*。
  + 若再抛 $1800$ 次（合计 $2000$ 次）得到正面 $1018$ 次，新的 $f_n$ 是多少？比较两次 $f_n$ 与 $0.5$ 的接近程度。

  ---

  *综合题 5* \
  涉及章节：#secref("dt10-population-vs-sample") + #secref("dt11-sampling-bias") + #secref("dt12-estimation")

  某市要估计中学生平均每周课外阅读时间。调研者*只在某重点中学的图书馆门口*随机访问了 $300$ 人，得到 $bar x = 6.4$ 小时。

  + 写出这次估计的*总体*、*样本*、*样本容量* $n$；
  + 这个 $bar x = 6.4$ 用作整个市的总体均值估计，是否可靠？指出*抽样方式*里的至少两条偏差风险；
  + 若把 $n$ 提到 $3000$，但仍然只在该图书馆门口访问，估计会变更准吗？解释原因（区分*随机误差*与*系统偏差*）。
]

#side-hack[
  综合题的难点通常不是“某一个动作”，而是：*在纸上同时记得哪几条工具要串起来*。写解时主动把“这一步用的是哪一节的哪个动作”在空白处标一下，会显著减少漏步。
]
