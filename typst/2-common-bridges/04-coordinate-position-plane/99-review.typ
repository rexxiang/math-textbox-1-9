#import "../../lib/chapter-kit.typ": blueprint, mastery, secref, diagnostic-table, answer-cut, knowledge-card

== 本章回顾 <meta:coordinate-position-plane>

#knowledge-card(title: "知识速查卡")[
    #table(
    columns: (1fr, 1fr),
    inset: 6pt,
    stroke: 0.4pt,
    [*主题*], [*一句话小结*],
    [坐标系], [两条垂直数轴 + 共用原点 + 同单位；“先横后纵”。],
    [四象限], [逆时针编号 1–4；轴上和原点不属于任何象限。],
    [描点 / 读点], [描点：先走 $x$ 再走 $y$；读点：向两轴作垂线。],
    [平移 / 对称], [只改跟动作方向一致的分量；翻轴改另一个分量的符号。],
  )

  *关键法则*

  + 坐标对*有序*：$(2, 0)$ 与 $(0, 2)$ 不是同一个点；先横后纵不可调换。
  + 四象限符号：I 象限 $(+, +)$，II $(-, +)$，III $(-, -)$，IV $(+, -)$。
  + 关于 $x$ 轴对称：纵坐标变号；关于 $y$ 轴对称：横坐标变号；关于原点对称：两个都变号。
  + 平移只在动作方向上加减：向右 $+a$ 改 $x$ 加 $a$，向上 $+b$ 改 $y$ 加 $b$；不动的轴对应的坐标不变。
  + 横线段长度 $= |x_2 - x_1|$，竖线段长度 $= |y_2 - y_1|$；斜线段要等到勾股定理才能直接算。
]

#mastery[
  *混合自测*

  + 画出 $P(2, -3)$、$Q(-4, 0)$、$R(-1, 5)$、$S(0, -2)$，并指出每一个所在象限或轴。
  + 写出 $A(-6, 2)$ 关于 $x$ 轴的对称点和关于 $y$ 轴的对称点。
  + 把 $B(3, 4)$ 向下平移 $6$、向左平移 $7$，写出最终坐标并判断象限。
  + 点 $(a, a+1)$ 在第一象限，求 $a$ 的范围。
  + 判断对错：在坐标系中 $(2, 0)$ 与 $(0, 2)$ 是同一个点。
  + 一个三角形三个顶点是 $(0, 0)$、$(4, 0)$、$(4, 3)$。它是什么三角形？边长如何？
  + 若 $(x, y)$ 关于原点对称变为 $(-x, -y)$，那么 $(3, -5)$ 的对称点是什么？
  + 一个长方形的四个顶点是 $(-2, -1)$、$(3, -1)$、$(3, 2)$、$(-2, 2)$。求其面积。
  + 把点 $(−1, 2)$ 先关于 $y$ 轴对称、再向右平移 $3$，写出结果。
  + 点 $(x, y)$ 在第二象限、在 $x$ 轴上的投影是 $(-4, 0)$，在 $y$ 轴上的投影是 $(0, 1)$。写出 $(x, y)$。
  // 跨章回顾
  + 点 $A(-3, 2)$ 与 $B(5, 2)$ 纵坐标相同。$A B$ 的长度是多少？如果按 $3 : 5$ 从 $A$ 起分这段距离，分点的横坐标是多少？（回顾 #secref("cb01-ratio-basics") 比与数轴距离）
  // 跨章回顾
  + 写出 $-2.5$ 在数轴上的绝对值；以 $-2.5$ 为纵坐标，构造一个在第三象限的点。（回顾 #secref("cb02-opposite-and-absolute") 相反数与绝对值）
  // 跨章回顾 · 构造题
  + 构造一个边长为 $4$、边平行于坐标轴的正方形，使其中心恰好在原点（回顾 #secref("pf06-polygons-and-circle") 正方形的定义：四边等长且四角均为直角）。写出四个顶点坐标，并用面积公式（回顾 #secref("pf07-perimeter-area")）计算该正方形的面积。

  *[选择题（多选）]* 下列各点中，位于第二象限的是（多选）（）

  - A. $(-3, 5)$
  - B. $(2, -1)$
  - C. $(-4, -2)$
  - D. $(-7, 0)$
  - E. $(-2, 3)$

  *[选择题（单选）]* 点 $P(-5, 3)$ 关于 $y$ 轴对称的点是（）

  - A. $(5, 3)$ B. $(-5, -3)$ C. $(5, -3)$ D. $(-3, 5)$

  *[选择题（单选）]* 已知点 $A(a, b)$ 在第三象限，则点 $B(-a, b)$ 在（）

  - A. 第一象限 B. 第二象限 C. 第三象限 D. 第四象限

  *挑战*

  + *反例构造*：举一个点，它的两个坐标都不是正数，但也不在第三象限，并解释原因。
  + *找 Bug*：阿米纳（Amina）说："$(3, 4)$ 和 $(4, 3)$ 是同一个点，因为它们包含的数字完全一样。"找出错误并用坐标定义解释。

]

==== 参考答案

+ $P$ 第四象限；$Q$ 在 $x$ 轴上；$R$ 第二象限；$S$ 在 $y$ 轴上。图略。
+ 关于 $x$ 轴：$(-6, -2)$；关于 $y$ 轴：$(6, 2)$。
+ 最终 $(-4, -2)$，在第三象限。
+ $a > 0$（两个分量都要为正）。
+ 错；顺序不同，位置不同。
+ 直角三角形；三边长 $3$、$4$、$5$。
+ $(-3, 5)$。
+ 长 $5$、宽 $3$，面积 $15$。
+ $(-1, 2)$ 先沿 $y$ 轴对称得到 $(1, 2)$，再向右平移 $3$ 得 $(4, 2)$。
+ $(-4, 1)$。
+ $A B = |5 - (-3)| = 8$（数轴上两点距离）。按 $3 : 5$ 从 $A$ 分：分点横坐标 $= -3 + 8 times 3/8 = -3 + 3 = 0$（按比分配）。
+ $|-2.5| = 2.5$（绝对值定义）；例如 $(-1, -2.5)$（第三象限要求 $x < 0$ 且 $y < 0$）。
+ $(-2, -2)$、$(2, -2)$、$(2, 2)$、$(-2, 2)$。边长 $4$，中心在原点，各顶点关于两轴对称。面积 $= 4 times 4 = 16$ 平方单位。（正方形面积公式 $= s^2$ 其中 $s$ 为边长，参见 #secref("pf07-perimeter-area")）
+ 选择题（多选）答案：A、E。A $(-3, 5)$：$x < 0, y > 0$ → 第二象限 ✓；E $(-2, 3)$：同理 ✓。B 在第四象限；C 在第三象限；D 在 $x$ 轴上（不属于任何象限）。
+ 选择题（单选 1）答案：A。关于 $y$ 轴对称：$x$ 符号取反，$y$ 不变，$(-5, 3) -> (5, 3)$。
+ 选择题（单选 2）答案：D。$A(a, b)$ 在第三象限，故 $a < 0, b < 0$。点 $B(-a, b)$：$-a > 0, b < 0$ → 第四象限。
+ 反例：轴上的点，如 $(-3, 0)$（在 $x$ 轴负半轴上）。两坐标不都是正数，但它在轴上，不属于任何象限，更不在第三象限（第三象限要求 $x < 0$ 且 $y < 0$，而这里 $y = 0$）。
+ 阿米纳错误：坐标是有序数对，顺序至关重要。$(3, 4)$ 表示横坐标 $3$、纵坐标 $4$，$(4, 3)$ 表示横坐标 $4$、纵坐标 $3$，两点位置完全不同。

#blueprint[
  *巩固复习*

  - 回看 #secref("pf08-coordinate-intuition")：从“位置 = 数对”的直觉，到本章“四象限”被正式命名，逻辑是连续的。
  - 回看 #secref("pf07-angles")：两轴交成的 $90 degree$ 是坐标系能“竖着量、横着量”的依据。
  - 回看 #secref("cb02-number-line-extends-left")：$x$ 轴、$y$ 轴本身都是你已经熟悉的数轴。

  *☞ 高中桥*

  _以下是预告，现在看不懂完全正常 — 等到高中再回来。_

  - 高中会讨论平面向量，它们的坐标表示就是本章坐标的直接继承。
  - 直线、圆、圆锥曲线的方程都建立在平面直角坐标系之上。
]

#diagnostic-table(
  [$x$、$y$ 坐标常看反],
  secref("cb04-rectangular-system"),
  [先横后纵："先走走廊（$x$ 轴），再坐电梯（$y$ 轴）"；写坐标时 $(x, y)$ 水平在前],

  [象限判断出错],
  secref("cb04-quadrants"),
  [逆时针编号 1–4；$x < 0, y > 0$ → 第二象限；轴上的点不属于任何象限],

  [平移或轴对称后坐标写错],
  secref("cb04-plotting-transforming"),
  [平移只改方向对应分量；翻 $x$ 轴只变 $y$ 符号；翻 $y$ 轴只变 $x$ 符号],
)
