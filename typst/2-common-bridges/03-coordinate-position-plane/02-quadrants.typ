#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref

=== 四象限：平面被两条轴分成的四块 <tool:cb03-quadrants>

#vocab[象限 quadrant / 第一象限 / 第二象限 / 第三象限 / 第四象限]

#crisis[
  给你一个坐标 $(x, y)$，光靠画图才能知道它在平面的“哪一块”太慢。你希望*只看符号*就能立刻报出区域。

  *我们要给平面的四块区域命名，并把符号与区域一一对齐。*
]

#history-note[
  把平面分成四块并按逆时针编号，是数学史上的一种惯例：从“正—正”开始，绕原点沿逆时针方向数一圈。一旦约定下来，所有图象的讨论都省话。
]

#discovery[
  两条坐标轴把平面分成四块，按*逆时针方向*从右上开始依次编号为：

  #table(
    columns: (auto, auto, auto, auto),
    inset: 6pt,
    stroke: 0.4pt,
    [*区域*], [*位置*], [*$x$ 的符号*], [*$y$ 的符号*],
    [第一象限], [右上], [$x > 0$], [$y > 0$],
    [第二象限], [左上], [$x < 0$], [$y > 0$],
    [第三象限], [左下], [$x < 0$], [$y < 0$],
    [第四象限], [右下], [$x > 0$], [$y < 0$],
  )

  两条坐标轴本身*不属于任何象限*；原点也不属于任何象限。

  只要拿到 $(x, y)$，先读符号——两正在第一，左上两异号在第二，两负在第三，右下再一次异号在第四——就能立刻说出位置。
]

#side-hack[
  口诀“$++$ 一、$-+$ 二、$--$ 三、$+-$ 四”；按逆时针记，对比着写。
]

#tryit[
  + 判断下列点各在哪个象限：$(4, 3)$、$(-2, 5)$、$(-1, -6)$、$(7, -8)$。
  + $(0, -3)$ 在第几象限？若不在任何象限，请说明它在哪条轴上。
  + 写出两个一定不在第二象限的点。
]

#blueprint[
  - *四象限*按逆时针从右上编号 1–4；
    - 第一象限：$x > 0$ 且 $y > 0$；
    - 第二象限：$x < 0$ 且 $y > 0$；
    - 第三象限：$x < 0$ 且 $y < 0$；
    - 第四象限：$x > 0$ 且 $y < 0$。
  - *两轴与原点*：坐标轴上的点（含原点）不归任何象限。
  - *两种常见对称*：
    - 关于 $x$ 轴对称：$(x, y) -> (x, -y)$；
    - 关于 $y$ 轴对称：$(x, y) -> (-x, y)$。
]

#pitfall[
  *高频错误*

  ✗ 把 $(0, 5)$ 说成“第二象限”
  → ✓ 它在 $y$ 轴上，不属于任何象限。

  ✗ 顺时针编号
  → ✓ 四象限是*逆时针*编号，从右上开始。

  ✗ 看到负号就慌，直接说“第三象限”
  → ✓ 还得看另一个坐标的符号；两个负才是第三。
]

#mastery[
  *基础*

  + 填空：点在第三象限 $<=>$ 横纵坐标都是 $"(\_\_)"$ 数。
  + 写出第二象限中横坐标是 $-3$ 的一个点。
  + 判断 $(0, 0)$ 所在象限。

  *应用*

  + 点 $A(-a, b)$ 在第二象限，问 $a$、$b$ 各为正还是负？
  + 点 $B(m, m - 3)$ 在第四象限，$m$ 的取值范围是什么？
  + 将 $(5, -2)$ 依次关于 $x$ 轴和 $y$ 轴作对称，最终得到什么点？在哪个象限？

  *挑战 ☞ 选做*

  + 若 $(x, y)$ 在第二象限，$(-x, -y)$ 在第几象限？
  + 平面上有无数个整点坐标落在第一象限。请给出横纵坐标之和等于 $5$ 的所有第一象限整点。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_
  + 负。
  + 例如 $(-3, 2)$。
  + 不在任何象限（它是原点）。
  + $a > 0$（否则 $-a$ 不会是负数，与第二象限 $x < 0$ 不符），$b > 0$。
  + $0 < m < 3$。
  + 最终得到 $(-5, 2)$，在第二象限。
  + 第四象限（两分量符号都取反）。
  + $(1,4)$、$(2,3)$、$(3,2)$、$(4,1)$。
]

#side-hack[
  *☞ 这会在后面哪里再出现*

  - 函数分支讨论函数图象的单调性、极值位置时会频繁说“该段在第二象限”。
  - 几何分支在讨论点的对称、旋转时直接引用象限编号。
  - 数据分支在残差图、散点图的象限分析里也会用到四象限语言。
]
