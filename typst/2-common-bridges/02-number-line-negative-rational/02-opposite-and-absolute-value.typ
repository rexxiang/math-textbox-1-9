#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref, answer-cut, self-check
#import "../../lib/diagram-packages.typ": cetz

=== 相反数与绝对值：方向与远近分开记 <tool:cb02-opposite-and-absolute>

#vocab[相反数 opposite][绝对值 absolute value][距离 distance]

#crisis[
  上一节学完负数，现在数轴上挤满了带符号的数。可这些"符号 + 数"立刻让一句简单的话*说不清楚*——

  - 教练让两位运动员沿数轴前进：甲到了 $-7$，乙到了 $+5$。问"谁离起点更远"？凭直觉看图，甲距原点 $7$ 步、乙距原点 $5$ 步，*甲更远*。可你写"甲 $> $ 乙"对吗？数大小上 $-7 < +5$——按数的*大小*应该说乙大；按*距离*应该说甲远。同一对数，给出两种相反的答案。
  - 看温度计：今天比昨天"差 $7$ 度"。是 $+7$ 还是 $-7$？说"差"通常只关心*差距有多大*，不关心朝哪边——可有符号的数里，"差 $7$"和"差 $-7$"是两个不同的数。
  - 商店卖文具，"亏 $20$ 元"和"赚 $20$ 元"账面上记 $-20$ 和 $+20$。要做"全月哪一天波动最大"的统计——*波动大小* 看的是数离 $0$ 多远，不是看数本身的正负。

  *卡点*：每个有符号数其实同时携带两条信息——*往哪边走*（方向）和*走多远*（远近）。这两条信息每次都被打包在一个数里写出来。一旦题目只关心其中一条，旧的"按数的大小直接比较"就给出错答案：

  - 比"哪个更靠近原点"？$-7$ 和 $+5$，旧规则按 $-7 < +5$ 报"前者小"——可"小"和"近"根本不是一回事。
  - 比"哪个数大"？$-7$ 和 $-3$，前者距原点更远，可它在数轴上*更靠左、数值更小*。

  混着写、混着读，每碰到一道题都得先纠结半天"这里到底是问大小还是问远近"。我们需要两件分开的工具：一件只管*方向翻转*（把"$+7$ 路上"和"$-7$ 路上"配成一对），一件只管*距离大小*（不管朝哪边、只看离 $0$ 多远）。这一节就把这两件工具立起来，并给它们各起一个名字。
]

#tryit[
  + 数轴上 $A = -3$、$B = 4$。求 $A B$ 的距离。
]

#discovery[
  有理数已经带了符号——$+7$ 表示"右边 7"，$-7$ 表示"左边 7"。现在要单独回答"甲（$-7$）距原点有多远"——这个距离是多少？

  #self-check[
    合上书或遮住下面，先自己写一个回答，再读后续。具体写：$-7$ 距原点有多远？$+5$ 距原点有多远？给这两个"远近"写出一个通用的计算方法，再往下读。

    ☞ 写完再继续：
    - ✓ 如果你想到的是"把负号去掉、正号也照抄"或者"取它在数轴上离 $0$ 的距离"，方向完全对——下面会把这件事写成统一的*绝对值*符号 $|a|$。
    - ✗ 如果你写成"距离 $= -7$"或者"距离可以是负数"，下面会先纠正这一点：距离永远 $>= 0$。
    - ✗ 如果你只会对负数做"去负号"、对正数没说明，那只是覆盖了一半情形——下面要把正负两种情况合并成*同一个*公式。
  ]

  试着推下去——

  想直接读出距离：$-7$ 的距离 $= -7$？但距离不能是负数——"负 7 步远"不是一段距离。那把负号去掉：距离 $= 7$。再看乙（$-3$）：距离 $= 3$。比较"谁距原点更远"：$7 > 3$，甲更远。这套办法对负数管用。

  可是如果题目里冒出 $-7$ 和 $+5$，谁距原点更远？把 $-7$ 的负号去掉得 $7$，$+5$ 本来就是 $5$，所以甲更远。问题是："去掉负号"原本是为负数想出来的写法，搬到正数身上凭什么也对？没有一个统一的名字，每出现一种符号组合都得重新讨论一次。

  带符号的数字把"方向"和"远近"混在一起，没有现成的语言单独谈"有多远"；想说"两个数距离一样、只是朝向相反"，话到嘴边却找不到合适说法。

  只改方向、不改远近，就得到一对*相反数*：

  - $3$ 的相反数是 $-3$；$-3$ 的相反数是 $3$。
  - $0$ 的相反数还是 $0$。
  - 一句话：相反数在数轴上关于原点对称，且它们之和为 $0$（$3 + (-3) = 0$）。

  #figure(
    cetz.canvas(length: 0.4cm, {
      import cetz.draw: *
      let ox = 10
      let s = 2.0
      // Axis
      line((ox - 5 * s, 0), (ox + 5.3 * s, 0), stroke: 1.5pt + rgb("#333"), mark: (end: ">"))
      for i in range(-4, 5) {
        let tx = ox + i * s
        line((tx, -0.3), (tx, 0.3), stroke: 0.8pt + rgb("#555"))
        content((tx, -0.8), text(size: 6pt)[#str(i)], anchor: "north")
      }
      // Origin
      circle((ox, 0), radius: 0.25, fill: rgb("#333"), stroke: 1pt + rgb("#333"))
      content((ox, 0.8), text(size: 7pt, weight: "bold")[O], anchor: "south")
      // Pair: +3 and -3
      let p3 = ox + 3 * s
      let m3 = ox - 3 * s
      circle((p3, 0), radius: 0.25, fill: rgb("#1565C0"), stroke: 1pt + rgb("#0D47A1"))
      circle((m3, 0), radius: 0.25, fill: rgb("#C62828"), stroke: 1pt + rgb("#B71C1C"))
      content((p3, 1.5), text(size: 8pt, weight: "bold", fill: rgb("#1565C0"))[$+3$], anchor: "south")
      content((m3, 1.5), text(size: 8pt, weight: "bold", fill: rgb("#C62828"))[$-3$], anchor: "south")
      // Symmetry arrows
      let arc-h = 2.5
      bezier((m3, 1.0), (ox, arc-h + 0.5), ((m3 + ox) / 2, arc-h + 1.0),
             stroke: (dash: "dashed", paint: rgb("#888"), thickness: 0.8pt))
      bezier((p3, 1.0), (ox, arc-h + 0.5), ((p3 + ox) / 2, arc-h + 1.0),
             stroke: (dash: "dashed", paint: rgb("#888"), thickness: 0.8pt))
      content((ox, arc-h + 1.5), text(size: 7pt, fill: rgb("#555"))[关于原点对称], anchor: "south")
    }),
    caption: [$+3$ 与 $-3$ 是一对相反数，关于原点对称],
  )

  只看远近、不看方向，就得到*绝对值*——也就是这个数到原点的距离：

  $ |a| = cases(a\, a >= 0, -a\, a < 0) $

  于是 $|-4| = 4$，$|4| = 4$，$|0| = 0$。

  两点 $a$、$b$ 在数轴上的距离也可以用绝对值表达：$|a - b|$。

  #figure(
    cetz.canvas(length: 0.4cm, {
      import cetz.draw: *
      let ox = 10
      let s = 2.0
      // Axis
      line((ox - 5 * s, 0), (ox + 5.3 * s, 0), stroke: 1.5pt + rgb("#333"), mark: (end: ">"))
      for i in range(-4, 5) {
        let tx = ox + i * s
        line((tx, -0.3), (tx, 0.3), stroke: 0.8pt + rgb("#555"))
        content((tx, -0.8), text(size: 6pt)[#str(i)], anchor: "north")
      }
      circle((ox, 0), radius: 0.2, fill: rgb("#333"), stroke: 1pt + rgb("#333"))
      // Point at -4: show |−4| = 4
      let pt = ox - 4 * s
      circle((pt, 0), radius: 0.25, fill: rgb("#E65100"), stroke: 1pt + rgb("#BF360C"))
      content((pt, 1.2), text(size: 8pt, weight: "bold", fill: rgb("#E65100"))[$-4$], anchor: "south")
      // Distance brace
      line((pt, -1.5), (ox, -1.5), stroke: 1.2pt + rgb("#E65100"),
           mark: (start: "|", end: "|"))
      content(((pt + ox) / 2, -2.2),
        text(size: 8pt, weight: "bold", fill: rgb("#E65100"))[$|{-4}| = 4$], anchor: "north")
    }),
    caption: [绝对值 $|-4| = 4$：$-4$ 到原点的距离是 $4$],
  )
]

#side-hack[
  看到表达式 $-(-3)$ 不必紧张：它是“再取一次相反数”，结果就是 $3$。“负负得正”的直觉，本质就是方向转两次又转回来。
]

#tryit[
  + 写出下列数的相反数：$6$、$-2/3$、$0$、$-1.4$。
]

#history-note[
  $1806$ 年，法国数学家阿尔让（Jean-Robert Argand）在一篇小册子里首次把复数画在平面上，每个数既有"大小"又有"方向"。这个思路后来被高斯（Gauss）发展壮大，最终催生了向量的概念——代数、几何、物理从此有了统一的表述语言。在实数轴上，"绝对值就是去掉方向只留大小"正是这条思路最简单的起点。
]

#blueprint[
  - *相反数*：$a$ 的相反数是 $-a$；它们满足 $a + (-a) = 0$，在数轴上关于原点对称；$0$ 的相反数是自己。
  - *绝对值*：$|a|$ 是 $a$ 到原点的距离；$|a| >= 0$，且 $|a| = 0$ 当且仅当 $a = 0$。
  - *两点距离*：数轴上两点 $a$、$b$ 之间的距离为 $|a - b|$。
]

#side-hack[
  *相反数 vs 绝对值*

  #table(
    columns: (1fr, 1fr),
    inset: 6pt,
    stroke: 0.4pt,
    [*相反数*], [*绝对值*],
    [关于 $0$ 对称：$a$ 与 $-a$ 在数轴两侧对应。], [到 $0$ 的距离：$|a|$ 永远不为负。],
    [关心方向翻转。], [关心离原点有多远。],
  )
]

#self-check[
  $|-7|$ 和 $-|7|$ 一样吗？*把这两个数都算出来*，再一句话说说它们各自代表什么——一个是“距离”，一个是“距离取反”。
]


#pitfall[
  *高频错误*

  ✗ 以为 $|a| = a$ 对所有 $a$ 都成立
  → ✓ 若 $a < 0$，则 $|a| = -a$（读作“去掉那个负号”，得到的是正数）。

  ✗ 看到 $-a$ 就写成“负数”
  → ✓ $-a$ 是 $a$ 的相反数；若 $a$ 本身是负数，$-a$ 就是正数。

  ✗ 把相反数和倒数混淆
  → ✓ 相反数满足 $a + (-a) = 0$；倒数满足 $a times 1/a = 1$，两者不一样。
  两个名字都带“反”字——但一个反在符号上，一个反在乘法位置上。
]

#mastery[
  *基础*

  + 写出 $-11$、$5.5$、$-3/8$、$0$ 的相反数。
  + 计算：$|-12|$、$|3.6|$、$|-1/5|$、$-|−9|$。
  + 判断：$|a|$ 可能等于 $-2$ 吗？为什么？
  + 如果 $a$ 和 $b$ 互为相反数，那么 $a + b = ?$。
  + 在数轴上，$|-6|$ 和 $|6|$ 都等于几？它们到原点的距离相同吗？

  *应用*

  + 数轴上 $A = -7$、$B = -1$、$C = 5$，求 $A B$、$B C$、$A C$。
  + *双解法对比题*——已知 $|x - 3| = 5$，求 $x$ 的所有可能值。
    - *方法 A（数轴上的距离解释）*：把 $|x - 3|$ 读作"$x$ 到 $3$ 的距离"。在数轴上找出所有"到 $3$ 的距离 $= 5$"的点，写出它们的坐标。
    - *方法 B（代数分类讨论）*：根据绝对值定义分两种情况：(i) $x - 3 >= 0$ 时 $|x - 3| = x - 3 = 5$；(ii) $x - 3 < 0$ 时 $|x - 3| = -(x - 3) = 5$。各自解出 $x$。
    - *比较*：两种方法的答案应当一致。请写出哪种更直观、哪种更"机械可靠"，并说明*若把题目改为* $|2 x - 3| = 5$，你更愿意先用哪一种？为什么？

  *挑战*

  + 若 $a < 0$, 化简 $|a| + a$。
  + *[反例 / 解释]*：判断："对任意有理数 $a, b$，都有 $|a + b| = |a| + |b|$。" 这条等式总是成立吗？给出一个使等式不成立的具体反例（取 $a, b$ 为整数即可），并说明 $|a + b|$ 和 $|a| + |b|$ 之间一般是什么关系（提示：思考 $a, b$ 同号 vs 异号的情况）。
  + *[反直觉]*：判断对错并改正——"$|a| = a$ 对所有有理数 $a$ 成立"。再判断："$-|a| <= a <= |a|$ 对所有有理数 $a$ 成立"，给出证明思路。
  + *[构造题]*：构造三个互不相同的有理数 $a, b, c$，使 $|a| = |b| = |c|$ 且它们的和等于 $0$。如果不能，写出反证；如果能，给出一组并验证。
  + *[跨知识点]*：在 #secref("tool:cb02-rational-numbers") 我们把分数搬上数轴。请用绝对值给数轴上两点 $A: -3/4$、$B: 5/6$ 的距离，先列式 $|A - B|$，再用通分（#secref("tool:pf04-fraction-comparison")）算出最简结果。
  + *[找 Bug]*：小明解 $|x + 2| = 5$，写："去绝对值得 $x + 2 = 5$，所以 $x = 3$。" 他丢了几个解？请补全所有解，并解释他遗漏的原因。

  #answer-cut[
  + $11$、$-5.5$、$3/8$、$0$（相反数 $=$ 符号取反，$0$ 的相反数仍是 $0$）。
  + $12$、$3.6$、$1/5$、$-9$（绝对值定义：负数去负号，非负数不变；$-|{-9}| = -9$）。
  + 不可能（绝对值定义：$|a| >= 0$，永远非负）。
  + $a + b = 0$（相反数定义：和为零）。
  + 都等于 $6$；相同（绝对值只看距离，不看方向）。
  + $A B = |-7 - (-1)| = 6$，$B C = |-1 - 5| = 6$，$A C = |-7 - 5| = 12$（两点距离公式 $|a - b|$）。
  + *双解法对比题*：
    - 方法 A（数轴）：$|x - 3| = 5$ 表示"$x$ 到 $3$ 的距离 $= 5$"。从 $3$ 向右 $5$ 个单位得 $x = 8$；向左 $5$ 个单位得 $x = -2$。所以 $x = 8$ 或 $x = -2$。
    - 方法 B（分类讨论）：(i) 若 $x - 3 >= 0$：$x - 3 = 5 => x = 8$，验证 $8 - 3 = 5 >= 0$ ✓。(ii) 若 $x - 3 < 0$：$-(x - 3) = 5 => x - 3 = -5 => x = -2$，验证 $-2 - 3 = -5 < 0$ ✓。所以 $x = 8$ 或 $x = -2$。
    - 比较：两法结果一致。方法 A 直观快速，适合简单形式 $|x - a| = b$；方法 B 更机械可靠，遇到复杂情况（如 $|2 x - 3| = 5$）也能照搬。改成 $|2 x - 3| = 5$ 时方法 B 更稳：直接 $2 x - 3 = plus.minus 5$ 解出 $x = 4$ 或 $x = -1$；方法 A 也能用（"$2 x$ 到 $3$ 的距离 $= 5$，再除以 $2$ 得 $x$"），但要多一层换算。一般倾向：单层绝对值用 A，含系数或多层时用 B。
  + 甲 $+6$ 千米、乙 $-4$ 千米（以出发点为原点、东为正）；距离 $= |6 - (-4)| = 10$ 千米。
  + $0$（当 $a < 0$ 时 $|a| = -a$，所以 $|a| + a = -a + a = 0$，绝对值分类讨论）。
  + 不总成立。反例：$a = 3, b = -5$，$|a + b| = |-2| = 2$，而 $|a| + |b| = 3 + 5 = 8$，$2 != 8$。一般规律：$|a + b| <= |a| + |b|$（三角不等式）；同号时取等号，异号时严格小于。直觉：同号相加距离累加；异号相加部分抵消。
  + 第一句错，反例 $a = -3$ 时 $|a| = 3 != -3$。正确说法：$|a| = a$ 当且仅当 $a >= 0$；$a < 0$ 时 $|a| = -a > a$。第二句对：当 $a >= 0$，$-|a| <= 0 <= a = |a|$；当 $a < 0$，$-|a| = a < 0 < |a|$；两边夹住，等价于"$|a| >= a$ 且 $|a| >= -a$"，即绝对值定义本身。
  + 不可能。若 $|a| = |b| = |c| = m > 0$，则每个数只能是 $+m$ 或 $-m$，三个互不相同至少需要三种值，但 $plus.minus m$ 只有两种取值。所以三个互不相同的同绝对值有理数*不存在*（除非允许 $m = 0$，但那样三数都是 $0$，又不互不相同）。结论：写不出。
  + 距离 $= |(-3/4) - 5/6| = |(-9/12) - 10/12| = |-19/12| = 19/12$。
  + 丢了一个解。绝对值方程要分*正负两种*情形：$x + 2 = 5$ 给 $x = 3$；$x + 2 = -5$ 给 $x = -7$。完整解集 $x = 3$ 或 $x = -7$。验证：$|3 + 2| = 5$ ✓，$|-7 + 2| = |-5| = 5$ ✓。他只考虑了去绝对值后取正的情况。
  ]
]

#side-hack[
  ☞ 绝对值方程 $|2x-1|=5$、几何的距离公式、数据的绝对偏差 $|x_i - bar(x)|$——这一节建立的精确定义，在三个分支里会反复被直接调用。
]
