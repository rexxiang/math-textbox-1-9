#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref, answer-cut, self-check
#import "../../lib/diagram-packages.typ": cetz

=== 字母代替数：规则代替一次计算 <tool:cb05-letters-represent-numbers>

#vocab[变量 variable][常量 constant][代数式 algebraic expression][系数 coefficient]

#crisis[
  设想你是公元前 $18$ 世纪美索不达米亚的一位立法官。国王命你把一条新税法刻在巨大的石板上，公告全国：

  *“凡耕地，按面积征收四分之一的谷物作税。”*

  你提起凿子准备开工——可立刻发现一个麻烦：

  - $20$ 亩地 $->$ 收 $5$ 担谷？写一行；
  - $60$ 亩地 $->$ 收 $15$ 担？再写一行；
  - $100$ 亩地、$237$ 亩地、$1\,884$ 亩地……？

  全国上千户人家，每户的土地都不同。要把每种可能的耕地面积都列一行计算结果，整片山的石头都不够刻。

  问题真正卡住的地方是：算术只会写*某一次具体的计算结果*——$20 div 4 = 5$、$60 div 4 = 15$——它没有任何符号能用来代表"任意一个数"。换句话说，*算术写得出"$5$ 担"，写不出"那个数的四分之一"作为一个普适规则本身*。

  你想刻的不是某一户的税额，而是*一条对所有面积都成立的规则*。可你手上的全部工具，都是在处理具体的数。

  *有没有一种办法，能把"对任意数量都成立的计算规则"压缩成一行式子？*
]

#discovery[
  *先承认问题：算术里没有"任意数"这个位置*

  立法官第一个念头，往往是再想几个具体例子凑一凑：$20 -> 5$、$60 -> 15$、$80 -> 20$ ……希望读者自己看出规律。但石板上刻一万行也只是更多的具体数，规则本身从未被写出来——读者只能猜。

  *第二个念头：留一个空格让人填*

  写成"$square.stroked div 4 = triangle.stroked$"，让收税人把面积填进 $square.stroked$、把结果填进 $triangle.stroked$。这已经接近答案了——但 $square.stroked$ 不是数，它在算式里没法被运算。如果税法稍复杂一点（"先减去 $5$ 亩免税额，再按四分之一收"），方框就连不起来了。

  *决定性一步：用一个字母占住"任意数"的位置*

  让 $n$ 代表"那一户的耕地亩数"——它*就是一个数*，只是我们暂时不指定它具体是几。税额规则就压缩成了一行：

  $ frac(n, 4) quad "（担谷）" $

  这是*代数式*。它本身没有结果，但只要把 $n$ 换成具体数，结果立刻出来：

  - $n = 20$：$20 div 4 = 5$；
  - $n = 60$：$60 div 4 = 15$；
  - $n = 1\,884$：$1\,884 div 4 = 471$。

  一行式子，覆盖了所有可能的耕地。$n$ 这种"能代表不同数"的字母叫*变量*；$4$ 这种固定不变的数叫*常量*。

  *字母也是数：四则运算、运算律照样成立*

  这一步最关键。换一个例子——一斤苹果 $6$ 元，买 $n$ 斤的钱数写成 $6 n$（乘号通常省略）。如果你想算"买 $n$ 斤、再多送 $2$ 斤"的总钱数，按分配律（#secref("pf02-order-laws")）有：

  $ 6 (n + 2) = 6 n + 12. $

  代入 $n = 3$ 验证：左边 $6 times 5 = 30$，右边 $18 + 12 = 30$ ✓。代入 $n = 7$：左 $6 times 9 = 54$，右 $42 + 12 = 54$ ✓。无论 $n$ 取什么具体数，等式都成立——因为 $n$ 只是"占位的数"，所有作用在数上的运算律对它一视同仁。

  字母在式子里其实可以扮演不同角色——这一节它是"代表任意取值的*变量*"；下一章 #secref("ch:equation-balance-inverse-operations") 它会变成"待求解的*未知数*"；函数分支里 $y = k x$ 中的 $k$ 还会扮演"暂时固定的*参数*"。三种角色都建立在同一句承诺上：

  *字母并没有脱离数的世界——只要它代表一个数，四则运算规则照样成立。*

  #figure(
    cetz.canvas(length: 0.45cm, {
      import cetz.draw: *
      // Rectangle: width = 6, height = n
      let w = 10
      let h = 5
      rect((0, 0), (w, h), fill: rgb("#E3F2FD"), stroke: 1.5pt + rgb("#1565C0"), radius: 3pt)
      // Labels
      content((w / 2, -0.8), text(size: 9pt, weight: "bold", fill: rgb("#1565C0"))[$6$], anchor: "north")
      content((-0.8, h / 2), text(size: 9pt, weight: "bold", fill: rgb("#1565C0"))[$n$], anchor: "east")
      // Area label inside
      content((w / 2, h / 2), text(size: 12pt, weight: "bold", fill: rgb("#0D47A1"))[$S = 6 n$])
      // Dimension arrows
      line((-0.3, 0), (-0.3, h), stroke: 0.8pt + rgb("#555"), mark: (start: "|", end: "|"))
      line((0, -0.3), (w, -0.3), stroke: 0.8pt + rgb("#555"), mark: (start: "|", end: "|"))
    }),
    caption: [面积模型：长 $6$、宽 $n$ 的长方形面积就是代数式 $6 n$],
  )
]

#side-hack[
  看到代数式时，默念一句“当 $n$ 被具体数替换时，这条式子就变回小学算术”。这句话能帮你克服“字母看起来很抽象”的紧张感。
]

#tryit[
  + 把“甲每月存 $200$ 元，存了 $m$ 个月”写成代数式。
  + 当 $a = 3$ 时，求代数式 $4 a - 5$ 的值。
]

#history-note[
  $1591$ 年，法国数学家韦达（François Viète）在《分析方法入门》中首次系统地用元音字母（$A$、$E$、$I$...）表示未知数、辅音字母（$B$、$C$、$D$...）表示已知常数，让代数第一次能写出通用公式。更早的尝试来自古希腊的丢番图（Diophantus，约 $250$ 年），他在《算术》一书中用特殊符号表示未知量，但还远未形成系统的字母代数。把未知数写成 $x$ 的习惯则是笛卡儿 $1637$ 年在《几何学》中确立的。

  ☞ 历史接力 → 笛卡儿 $1637$ 年确立 $x$、$y$ 写法后，坐标系把字母代数与几何直接连通；见 #secref("cb04-rectangular-system")。
]

#blueprint[
  - *代数式*：由数、字母、运算符号（加、减、乘、除、乘方）按运算规则组成的一串式子。单独一个数或字母也算代数式。
  - *变量*：可以代表不同数的字母；*常量*：固定不变的数。
  - *系数*：代数式中字母前面的数字部分；$-3 x$ 的系数是 $-3$，不是 $3$。
  - *代入求值*：把变量换成具体数，按运算顺序（#secref("pf02-order-laws")）计算。

  #side-hack[
    *字母在式子里其实有三种角色*——同样是 $x$，扮演的"工作"不一样：

    #table(
      columns: (auto, 1fr, 1fr),
      inset: 6pt,
      stroke: 0.4pt,
      align: left,
      [*角色*], [*意思*], [*典型场景*],
      [*变量*], [可以取一组值，关心"如何随取值变化"], [$y = 6 x$ 中的 $x$（每个 $x$ 都对应一个 $y$）],
      [*未知数*], [一个还不知道、需要解出来的具体值], [$3 x + 5 = 17$ 中的 $x$（解后得到唯一值 $4$）],
      [*参数*], [题目里"暂时固定的常数"，不同题目里取值不同], [$y = k x$ 中的 $k$（先固定 $k$，再让 $x$ 变化）],
    )

    *记忆口诀*：变量在变，未知数等着被解出来，参数在题目里站岗不动。本章先用"变量"，#secref("ch:equation-balance-inverse-operations") 引入"未知数"，函数分支 #secref("ch:function-representations") 才用到"参数"。
  ]

  #figure(
    cetz.canvas(length: 0.45cm, {
      import cetz.draw: *
      // Input-formula-output machine
      let bx = 0
      // Input box
      rect((bx, 0), (bx + 4, 2.5), fill: rgb("#E8F5E9"), stroke: 1.2pt + rgb("#388E3C"), radius: 3pt)
      content((bx + 2, 1.25), text(size: 9pt, weight: "bold", fill: rgb("#2E7D32"))[$n = 7$])
      content((bx + 2, -0.6), text(size: 7pt, fill: rgb("#555"))[输入], anchor: "north")
      // Arrow
      line((bx + 4.5, 1.25), (bx + 7.5, 1.25), stroke: 1.5pt + rgb("#555"), mark: (end: ">"))
      // Formula box
      rect((bx + 8, 0), (bx + 14, 2.5), fill: rgb("#FFF9C4"), stroke: 1.2pt + rgb("#F9A825"), radius: 3pt)
      content((bx + 11, 1.25), text(size: 9pt, weight: "bold", fill: rgb("#F57F17"))[$6 times n$])
      content((bx + 11, -0.6), text(size: 7pt, fill: rgb("#555"))[公式], anchor: "north")
      // Arrow
      line((bx + 14.5, 1.25), (bx + 17.5, 1.25), stroke: 1.5pt + rgb("#555"), mark: (end: ">"))
      // Output box
      rect((bx + 18, 0), (bx + 22, 2.5), fill: rgb("#FFCDD2"), stroke: 1.2pt + rgb("#C62828"), radius: 3pt)
      content((bx + 20, 1.25), text(size: 9pt, weight: "bold", fill: rgb("#C62828"))[$42$])
      content((bx + 20, -0.6), text(size: 7pt, fill: rgb("#555"))[输出], anchor: "north")
    }),
    caption: [代入求值：把 $n = 7$ 代入公式 $6 n$，输出 $42$],
  )
]

#self-check[
  在表达式 $3n + 2$ 里，$n$ 代表的是某一个具体的数，还是“任何”一个数？两种理解会让你解题方式有什么不同？
]


#pitfall[
  *高频错误*

  ✗ 把 $6 n$ 理解成“$6$ 后面写了个 $n$”
  → ✓ 它表示 $6 times n$；字母和数相邻时有隐含的乘号。

  ✗ 把 $-3 x$ 的系数写成 $3$
  → ✓ 系数要带符号；$-3 x$ 的系数是 $-3$。

  ✗ 代入求值时忘了括号
  → ✓ 当代入的是负数或带运算的表达式时，必须加括号，如 $4 a - 5$ 在 $a = -2$ 时要写 $4 times (-2) - 5$。
]

#mastery[
  *基础*

  + 用代数式表示：“一本书每本 $x$ 元，买 $5$ 本共花多少钱？”
  + 用代数式表示：“甲的年龄是乙的 $2$ 倍减 $3$，乙今年 $y$ 岁，甲多少岁？”
  + 写出代数式 $-7 a$、$0.5 b$、$-x$ 的系数。
  + 当 $x = -1$ 时，求代数式 $x^2 + 2 x$ 的值。
  + 代数式 $t / 3$ 的系数是多少？

  *应用*

  + 当 $x = 5$ 时，求 $2 x^2 - 3 x + 1$ 的值。
  + 当 $a = -2$ 时，求 $4 - 3 a$ 的值。
  + 长方形长 $a$ 米、宽 $b$ 米。写出周长和面积各自的代数式。

  *挑战 ☞ 选做*

  + 若 $a + b = 6$，求 $3(a + b) - 7$ 的值；你用到了哪条运算律？
  + 某商品原价 $p$ 元，打 $r%$ 出售（其中 $r$ 为百分号前的数字）。写出售价的代数式。

  #answer-cut[
  + $5 x$ 元（变量 $x$ 代表单价，常量 $5$ 是数量）。
  + $2 y - 3$ 岁（“$2$ 倍减 $3$”翻译为代数式）。
  + $-7$、$0.5$、$-1$（系数要带符号；$-x$ 的系数是 $-1$，不是 $1$）。
  + $(-1)^2 + 2 times (-1) = 1 - 2 = -1$（代入求值，注意负数要加括号）。
  + $1/3$（系数是字母前的数字因子，$t / 3 = 1/3 times t$）。
  + $2 times 5^2 - 3 times 5 + 1 = 50 - 15 + 1 = 36$（先算乘方，再算乘、减、加，运算顺序）。
  + $4 - 3 times (-2) = 4 + 6 = 10$（负数代入时加括号，$-3 times (-2) = 6$）。
  + 周长 $2 (a + b)$；面积 $a b$。
  + $3 times 6 - 7 = 18 - 7 = 11$（把 $a + b$ 看作整体代入 $= 6$，这是“整体代入”技巧，本质是分配律）。
  + 售价 $= p times r / 100$（或写成 $p r / 100$）。注意 $r$ 是百分号前的数字，回顾百分数结构。
  ]
]

#side-hack[
  *☞ 这会在后面哪里再出现*

  - 代数分支第 1 章整式与合并同类项会把“字母也要跟数一样被运算”正式推开。
  - 函数分支把 $f(x)$ 看成“输入 $x$、按代数式计算、输出一个数”的过程。
  - 数据分支用 $x_i$、$bar(x)$ 表达原始数据和均值，本节的变量约定是前提。
]
