#import "../../lib/theme-v2.typ": blueprint, mastery, tryit, secref

== 本章回顾：四分支入口准备的答案汇总 <meta:bridge-readiness-contracts-review>

#blueprint[
  本节把 #secref("meta:ready-for-algebra")、#secref("meta:ready-for-geometry-proof")、#secref("meta:ready-for-functions")、#secref("meta:ready-for-data") 四节的自测诊断答案集中给出。最后再附上一题*跨四分支综合小试*，用于确认你已经可以顺畅地在四条分支之间切换。
]

#mastery[
  *诊断 · 代数分支（参考答案）*

  + $4 a - 8 - 6 a + 3 = -2 a - 5$。
  + $2 x - 6 = 3 x - 8 => -x = -2 => x = 2$。代回：左 $= -2$，右 $= -2$。
  + 列比例：$3 / 5 = 240 / y => y = 400$ 件；也可设总数 $n$，$3 n / 8 = 240 => n = 640$，$y = 400$。
]

#mastery[
  *诊断 · 几何证明分支（参考答案）*

  + 内错角相等：$2 x + 10 = 3 x - 20 => x = 30$；两角分别是 $70 degree$、$70 degree$。
  + 三角之比 $2 : 3 : 4$，共 $9$ 份对应 $180 degree$，每份 $20 degree$；三角为 $40 degree$、$60 degree$、$80 degree$。
  + $A(-3, 2)$ 关于 $x$ 轴对称得到 $A'(-3, -2)$；再向右平移 $5$ 得 $B(2, -2)$，在第四象限。
]

#mastery[
  *诊断 · 函数分支（参考答案）*

  + $y = -2 x + 5$：$x = -1$ 时 $y = 7$；$x = 0$ 时 $y = 5$；$x = 3$ 时 $y = -1$。在坐标系中描出三点即可。
  + $y = k x$ 中代入 $x = 4, y = -12$ 得 $k = -3$；关系式 $y = -3 x$；$x = -2$ 时 $y = 6$。
  + $s = 60 t$；$s = 90$ 时 $t = 1.5$ 小时。
]

#mastery[
  *诊断 · 数据分支（参考答案）*

  + 总和 $72 + 85 + 90 + 65 + 78 + 88 + 92 + 80 = 650$；均值 $bar(x) = 650 / 8 = 81.25$。字母公式 $bar(x) = (x_1 + x_2 + dots + x_8) / 8$。
  + 支持率 $= 170 / 250 = 0.68 = 68.0%$；基数是“回收的 $250$ 份问卷”。
  + 七天总和 $149$；平均气温 $149 / 7 approx 21.3 degree$C；第五天气温 $25$，偏差 $25 - 21.3 = +3.7 degree$C（保留一位小数）。
]

#tryit[
  *跨四分支综合小试*

  某学校 $600$ 名学生中，男生与女生的比是 $7 : 5$。在一次数学测试中，男生平均分 $78$、女生平均分 $82$。学校在坐标系中把“班级编号 $x$”与“班级平均分 $y$”画成散点图，记录了 $1$ 班到 $10$ 班的数据，其中第 $3$ 班 $(3, 80)$、第 $8$ 班 $(8, 85)$。

  + 算出全校的男生人数与女生人数。
  + 把全校的总平均分写成代数式（用 $m$、$w$、$bar(x)_m$、$bar(x)_w$ 表达），再代入具体数字。
  + 记平均分关于班级编号的关系 $y = k x + b$ 穿过 $(3, 80)$ 与 $(8, 85)$ 两点。用反向操作解出 $k$、$b$。
  + 判断：第 $10$ 班按这条关系对应多少分？这个数能代表它真实的平均分吗？为什么？
  // 跨章回顾
  + 化简 $sqrt(18) + sqrt(2)$。再判断结果是有理数还是无理数。（回顾平方根化简与无理数）
  // 跨章回顾
  + 解方程 $5(x - 2) = 3x + 4$，把解代入坐标 $(x, x - 3)$，判断该点在第几象限。（回顾方程 + 坐标系）
]

#mastery[
  *跨四分支综合小试 — 参考答案*

  + 总人数 $600$ 按 $7 : 5$ 分：男生 $600 times 7 / 12 = 350$，女生 $250$。
  + 总平均分 $bar(x) = (m bar(x)_m + w bar(x)_w) / (m + w)$。代入 $m = 350$、$w = 250$、$bar(x)_m = 78$、$bar(x)_w = 82$，得 $bar(x) = (350 times 78 + 250 times 82) / 600 = (27300 + 20500) / 600 = 47800 / 600 approx 79.67$。
  + 由两点列方程组：$3 k + b = 80$，$8 k + b = 85$。两式相减 $5 k = 5$，$k = 1$；代回 $b = 77$。所以 $y = x + 77$。
  + 代入 $x = 10$ 得 $y = 87$。这只是按这条模型外推出来的“预测值”，不一定等于第 $10$ 班真实平均分；统计关系并不保证每个点都严格贴合，数据分支后面会讨论拟合与预测的局限。
  + $sqrt(18) + sqrt(2) = 3sqrt(2) + sqrt(2) = 4sqrt(2)$（合并同类根号，类似合并同类项）。$4sqrt(2)$ 是无理数。
  + 展开 $5x - 10 = 3x + 4$，移项 $2x = 14$，$x = 7$。坐标 $(7, 4)$，$x > 0, y > 0$，在第一象限。
]

#blueprint[
  *最终前进信号*

  - 你在四条分支的诊断里都能口头说出用了哪些桥梁知识、哪些公共基础里学过的知识。
  - 你愿意把一道综合题拆成“比例部分 / 几何部分 / 函数关系部分 / 数据解释部分”。
  - 你在做错时，能立刻按查漏表定位具体章节，而不是重刷整章。

  满足这三条，你已经正式准备好进入分支进阶。
]
