#import "../../lib/chapter-kit.typ": blueprint, mastery, secref, diagnostic-table, answer-cut, knowledge-card

== 本章回顾 <meta:square-root-irrational-number-line>

#knowledge-card(title: "知识速查卡")[
    #table(
    columns: (1fr, 1fr),
    inset: 6pt,
    stroke: 0.4pt,
    [*主题*], [*一句话小结*],
    [平方 / 平方根], [反向动作；$a$（$a >= 0$）的两根是 $plus.minus sqrt(a)$，$sqrt(a)$ 本身非负。],
    [算术平方根], [$sqrt(a)$ 约定取非负；$sqrt(a^2) = |a|$。],
    [无理数], [不能写成 $p / q$ 的实数；十进制不终止也不循环。],
    [实数 $RR$], [把 $QQ$ 的“洞”用无理数填上；数轴和 $RR$ 一一对应。],
  )
]

#blueprint[
  *做题前你应该会*

  - 会区分“两个平方根”与“算术平方根 $sqrt(a)$”。
  - 会用 $sqrt(a^2)=|a|$ 处理正负号。
  - 会把非完全平方数写成最简根号，并用相邻平方数估算位置。
  - 会分清有理数、无理数、实数，以及它们在数轴上的位置。
]

#mastery[
  *混合自测*

  _技能分层：1–3 题查平方根概念，4–7 题查化简与估算，后面的跨章与挑战题查数域判断。_

  + 求 $225$、$169$ 的两个平方根；写出各自的算术平方根。
  + 计算：$sqrt(81)$、$sqrt(0.04)$、$sqrt(16/25)$。
  + 判断对错：$sqrt((-7)^2) = -7$。
  + 化简：$sqrt(72)$、$sqrt(45)$。
  + 一正方形面积 $12$ 平方米，写出它的边长（最简根号形式）。
  + 判断下列数各属于哪些数集（$NN / ZZ / QQ / RR$）：$-6$；$sqrt(11)$；$0.dot(7)$；$pi$；$1/4$。
  + 估算 $sqrt(30)$ 在哪两个相邻整数之间，再给出精确到一位小数的近似。
  + 判断：$sqrt(3) + sqrt(3)$ 是不是有理数？为什么？
  + 举出一对“两个无理数相加得有理数”的例子。
  + 在区间 $(0, 0.1)$ 内写出一个无理数。
  // 跨章回顾
  + 解方程 $x^2 = 49$，写出所有解。这些解是有理数还是无理数？（回顾 #secref("cb06-solving-linear") 方程解法与本章平方根）
  // 跨章回顾
  + 一个正方形的面积用代数式 $a^2$ 表示（回顾 #secref("cb05-letters-represent-numbers") 字母代数）。当 $a^2 = 12$ 时，$a$ 是有理数还是无理数？把 $a$ 写成最简根号形式。（本章工具 #secref("cb07-square-and-square-root")）
  // 跨章回顾 · 估算判断题
  + 不用计算器，判断 $sqrt(50)$ 是否大于有理数 $7$（回顾 #secref("cb02-rational-numbers") 有理数的定义与排列，理解 $7 in QQ$ 而 $sqrt(50) in.not QQ$）。写出你的推理过程。

  *[选择题（多选）]* 下列数中，属于无理数的是（多选）（）

  - A. $sqrt(4)$
  - B. $sqrt(5)$
  - C. $0.dot(3)$（循环小数 $0.333...$）
  - D. $pi$
  - E. $-sqrt(9)$

  *[选择题（单选）]* $sqrt((-5)^2) = $（）

  - A. $-5$ B. $5$ C. $plus.minus 5$ D. $25$

  *[选择题（单选）]* 比较 $sqrt(10)$ 与 $3$ 的大小（）

  - A. $sqrt(10) < 3$ B. $sqrt(10) = 3$ C. $sqrt(10) > 3$ D. 无法判断

  *挑战*

  + *反例构造*：举具体实数 $a$ 说明 $sqrt(a^2) = a$ 不总成立，并说明正确表达式是什么。
  + *找 Bug*：梅兰（Mei Lan）说："因为 $sqrt(2) times sqrt(8) = sqrt(16) = 4$，所以 $sqrt(2)$ 和 $sqrt(8)$ 都是有理数，因为它们的乘积是有理数。"找出推理错误。
  + *估算 / 量级题*：不用计算器，估算 $sqrt(200)$ 的值精确到个位，并判断 $sqrt(200)$ 比 $14$ 大还是小。写出你的推理（提示：找最近的完全平方数）。
]

==== 参考答案（含关键步骤）

+ $plus.minus 15$，算术平方根 $15$；$plus.minus 13$，算术平方根 $13$。
+ $9$、$0.2$、$4/5$。
+ 错；正确值是 $7$。
+ $sqrt(72) = 6 sqrt(2)$；$sqrt(45) = 3 sqrt(5)$。
+ $sqrt(12) = 2 sqrt(3)$ 米。
+ $-6 in ZZ$；$sqrt(11) in RR$（无理）；$0.dot(7) = 7/9 in QQ$；$pi in RR$（无理）；$1/4 in QQ$。
+ $5 < sqrt(30) < 6$；$sqrt(30) approx 5.5$（$5.5^2 = 30.25$）。
+ 不是；$sqrt(3) + sqrt(3) = 2 sqrt(3)$，仍是无理数。
+ 例如 $sqrt(2)$ 与 $-sqrt(2)$，和为 $0$。
+ 例如 $sqrt(2) / 20 approx 0.0707 dots$。
+ $x = 7$ 或 $x = -7$（$x^2 = 49$ 有两个平方根）。$7$ 和 $-7$ 都是有理数（整数属于 $QQ$）。
+ $a = sqrt(12) = 2sqrt(3)$（化简根号）。$12$ 不是完全平方数，所以 $a$ 是无理数。
+ $sqrt(50) > 7$。理由：$7^2 = 49 < 50$，所以 $sqrt(50) > sqrt(49) = 7$（利用"被开方数越大，算术平方根越大"）。
+ 选择题（多选）答案：B、D。$sqrt(4) = 2$（有理数）；$0.dot(3) = 1/3$（有理数）；$-sqrt(9) = -3$（有理数）；$sqrt(5)$ ✓ 无理数；$pi$ ✓ 无理数。
+ 选择题（单选 1）答案：B。$sqrt((-5)^2) = sqrt(25) = 5 = |-5|$；算术平方根约定非负。
+ 选择题（单选 2）答案：C。$3^2 = 9 < 10$，所以 $sqrt(10) > sqrt(9) = 3$。
+ 反例：取 $a = -3$，$sqrt((-3)^2) = sqrt(9) = 3 != -3$。正确表达式：$sqrt(a^2) = |a|$（对任意实数 $a$ 成立）。
+ 梅兰的推理错误：两个无理数之积可以是有理数（例如 $sqrt(2) times sqrt(2) = 2$）。有理数的乘积一定是有理数，但反推不成立：乘积是有理数不能推出因子是有理数。$sqrt(2)$ 和 $sqrt(8) = 2sqrt(2)$ 都是无理数。
+ $14^2 = 196 < 200 < 225 = 15^2$，所以 $14 < sqrt(200) < 15$，精确到个位约为 $14$（更接近 $196$ 那侧）。$sqrt(200) = sqrt(4 times 50) = 2 sqrt(50) approx 2 times 7.07 = 14.14$。故 $sqrt(200) > 14$。

#blueprint[
  *巩固复习*

  - 面积 / 体积给了平方与立方，反过来求边长就需要平方根：面积公式不稳时回看 #secref("tool:pf07-perimeter-area")，体积直觉不稳时回看 #secref("tool:pf07-volume")。
  - 有理数在数轴上很密，但仍没有填满；先回看 #secref("tool:cb02-rational-numbers")，再理解为什么 $sqrt(2)$ 要把 $QQ$ 扩成 $RR$。
  - 反证 $sqrt(2)$ 无理会用到“既约分数”不能再同时约分；这和 #secref("tool:pf04-fraction-equivalence") 的等值分数工具相连。

  *☞ 高中桥*

  _以下是预告，现在看不懂完全正常 — 等到高中再回来。_

  这只是向前看的预告，不作为后续主线先修。

  - 高中会正式处理 $n$ 次方根、有理指数和无理指数，并在实数完备性上讨论极限与连续。
  - 虚数 / 复数是 $RR$ 的下一次扩展，用来回答“$sqrt(-1)$ 在哪”这种问题。
]

#diagnostic-table(
  [算术平方根和“两个平方根”概念混淆], secref("tool:cb07-square-and-square-root"), [$sqrt(a)$（$a >= 0$）约定非负；完整的两个平方根写 $plus.minus sqrt(a)$],
  [把 $sqrt(a^2)$ 直接写成 $a$], secref("tool:cb07-square-and-square-root"), [正确是 $sqrt(a^2)=|a|$；若 $a<0$，平方后再开算术平方根会回到正距离],
  [化简根号时没提出最大平方因子], secref("tool:cb07-square-and-square-root"), [先找被开方数中的完全平方因子，如 $72=36 times 2$，所以 $sqrt(72)=6sqrt(2)$],
  [估算平方根时只靠小数感觉], secref("tool:cb07-real-number-line"), [找相邻完全平方数：若 $25<30<36$，就有 $5<sqrt(30)<6$],
  [把所有无限小数都叫无理数], secref("tool:cb07-irrational-numbers"), [循环无限小数仍是有理数；无理数的十进制不终止且不循环],
  [以为 $QQ$ 很密就等于铺满数轴], secref("tool:cb07-real-number-line"), [有理数很密，但仍有 $sqrt(2)$、$pi$ 这样的“洞”；实数 $RR$ 才对应整条数轴],
  [解 $x^2=49$ 只写正根], secref("tool:cb07-square-and-square-root"), [平方会把正负两侧都送到同一个正数；方程解要写 $x=7$ 或 $x=-7$],
  [两个无理数运算后得到有理数，就反推它们原来是有理数], secref("tool:cb07-irrational-numbers"), [反推不成立；例如 $sqrt(2) times sqrt(8)=4$，但两个因子仍是无理数],
)
