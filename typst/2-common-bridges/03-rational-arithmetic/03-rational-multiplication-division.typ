#import "../../lib/chapter-kit.typ": crisis, discovery, blueprint, mastery, tryit, pitfall, history-note, side-hack, vocab, secref, answer-cut

=== 有理数乘除：方向反转与倒数 <tool:cb03-rational-multiplication-division>

#vocab[乘法 multiplication][除法 division][倒数 reciprocal]

#crisis[
  连续 $4$ 天每天降 $2$ °C，从今天的 $5$ °C 出发，$4$ 天后是几度？

  用乘法：每天变化 $-2$ °C，$4$ 天就是 $-2 times 4 = -8$，从 $5$ 出发得 $5 + (-8) = -3$ °C——这没有问题，就是"反复加负数"。

  但如果问的是"$4$ 天*前*的气温"呢？那就是 $5 - (-2) times 4 = 5 + 8 = 13$ °C，这里出现了 $(-2) times (-4)$（向前推 $4$ 天，每天降 $-2$）。"两个负数相乘"到底是正还是负？旧的"乘法 = 反复加"在这里解释不通，我们需要重新想清楚符号规律。
]

#tryit[
  从 $2 times 3 = 6$ 开始，每次把第一个因数减少 $1$：

  $2 times 3 = 6$

  $1 times 3 = 3$

  $0 times 3 = 0$

  $(-1) times 3 = ?$

  $(-2) times 3 = ?$

  再从 $(-2) times 3 = -6$ 开始，每次把第二个因数减少 $1$，推到 $(-2) times (-2)$。写出每一步的结果。
]

#discovery[
  乘法的老定义是"反复加"：$3 times 4$ 是 4 个 3 相加，$3 + 3 + 3 + 3 = 12$。换成 $(-2) times 3$：3 个 $(-2)$ 相加，$(-2) + (-2) + (-2) = -6$，照做也行。

  那 $(-2) times (-3)$ 呢？"$-3$ 个 $(-2)$ 相加"——"加 $-3$ 次"是什么意思？反复加要求次数是非负整数，次数为负根本说不通。强行类推：$(-2) times 3 = -6$，$(-2) times 2 = -4$，$(-2) times 1 = -2$，$(-2) times 0 = 0$；下一行 $(-2) times (-1)$ 该是 $+2$、$-2$，还是 $0$？反复加在这里彻底失声。

  这件事有意思的地方是：定义虽然停了，但*那串数本身还在沿着一条规律往下走*。如果把"乘法 = 反复加"换成"乘法 = 让那条规律继续保持"，能不能把符号问题接着算下去？

  *两条独立线索，同一个结论*

  *线索一（保持递减规律）*：从 tryit 的表格可以看到，第一个因数每减 $1$，乘积减少 $3$；继续这条规律，$(-1) times 3 = -3$，$(-2) times 3 = -6$。

  再从 $(-2) times 3 = -6$ 开始，第二个因数每减 $1$，乘积*增加* $2$（因为每次减去了 $-2$，就是加了 $2$）：$(-2) times 2 = -4$，$(-2) times 1 = -2$，$(-2) times 0 = 0$，$(-2) times (-1) = +2$，$(-2) times (-2) = +4$。*负负得正*。

  *线索二（方向反转）*：在数轴上，乘以 $-1$ 的含义是"把方向翻转"——$(-1) times 3 = -3$（正方向翻成负方向）。那么 $(-1) times (-3)$：先有 $-3$（负方向），再翻转一次，回到正方向，得 $+3$。*翻转两次 = 回到原方向 = 正号*。

  两条线索都指向同一个符号法则：*同号相乘为正，异号相乘为负*。

  *除法：乘以倒数*

  整数里 $12 div 4 = 3$ 因为 $3 times 4 = 12$。同样地，若 $a div b = c$，则 $c times b = a$。于是"除以 $b$"可以改写成"乘以满足 $b times x = 1$ 的那个 $x$"，这个 $x$ 就是 $b$ 的*倒数*，记作 $1/b$。

  $a div b = a times (1/b) quad (b eq.not 0)$

  符号规律与乘法一致：同号得正，异号得负。
]

#blueprint[
  *乘法符号法则*

  - 同号相乘：结果为正。$(-3) times (-7) = 21$；$4 times 5 = 20$。
  - 异号相乘：结果为负。$(-3) times 7 = -21$；$3 times (-7) = -21$。
  - 任何数乘以 $0$：结果为 $0$。

  *推论：绝对值相乘 $|a times b| = |a| times |b|$*

  这条不是新规则——它是上面*符号法则*与*绝对值定义*（绝对值就是去掉符号后剩下的算术大小）的直接推论。把符号交给"同号 / 异号"那条规则去定，剩下纯算术大小直接相乘，自然就有 $|a times b| = |a| times |b|$。例：$|(-3) times 7| = |-21| = 21 = 3 times 7 = |-3| times |7|$。它在化简、估量"放大缩小幅度"时非常好用，但*请记住它是推论，不要把它和符号法则平起平坐*。

  *倒数*

  非零有理数 $a$ 的倒数是
  $ 1/a $
  （即满足 $a times x = 1$ 的数）。$a$ 与其倒数同号。特别地，$0$ 没有倒数。

  *除法转乘法*

  $ a div b = a times (1/b) quad (b eq.not 0) $

  特例：$0 div b = 0$（$b eq.not 0$）；除以 $0$ 没有意义。
]

#history-note[
  "负负得正"曾让许多欧洲数学家困惑了好几个世纪。18 世纪的数学家欧拉（Euler）在《代数基础》（1770）里给出过一个直观比方："债务的债务是资产"——欠一笔欠款（负数），再把这笔欠款取消（再乘一个"取消"= 负号），净效果就是获得资产（正数）。与此同时，中国宋元数学对多个未知数的多项式运算早已驾轻就熟，负数的符号运算从来都是理所当然的工具。
]

#pitfall[
  ✗ $-3 + (-5) = +8$（把"负负得正"套用到加法）

  → ✓ 负负得正只在*乘除*中成立。加法中 $-3 + (-5) = -8$（同号加法）。

  ✗ $0 div (-7)$ 无意义

  → ✓ $0$ 除以任何非零数都是 $0$。无意义的是"$(-7) div 0$"——除以零才无意义。

  ✗ $(-2)/(-3)$ 是负数

  → ✓ 同号相除得正：$(-2)/(-3) = 2/3$。
]

#mastery[
  *基础*

  + 计算：$(-3) times (-7)$。
  + 计算：$8 times (-5)$。
  + 计算：$-12 div 4$。
  + 计算：$(-15) div (-3)$。
  + 判断对错并改正："$(-2) + (-3) = +5$，因为负负得正。"

  *应用*

  + 一只热气球以 $-3$ 米/秒的速度（即每秒下降 $3$ 米）下沉，$8$ 秒前的高度比现在高多少米？
  + 一根弹簧每秒拉长 $5$ 厘米。如果记伸长方向为正、收缩为负，倒回 $-2$ 秒（即 $2$ 秒前），它比现在长还是短？长（短）多少？

  *挑战*

  + 不计算，判断符号：$(-1) times (-2) times (-3) times dots times (-99)$。一般规则是什么？
  + 求满足 $a times b < 0$ 且 $a + b > 0$ 的有理数对 $(a, b)$ 三组（数字不能重复使用）。这种情况下 $a, b$ 必须满足什么共同条件？

  + 计算 $(-1)^{100} + (-1)^{101}$，不需要展开乘法——直接用"偶次幂 vs 奇次幂"判断每一项的符号。
  + 某商品先涨价 $10 %$，再降价 $10 %$，最终比原价高还是低？用有理数乘法精确计算（设原价为 $a$）。
  + *[构造题]*：构造一个有理数 $x$，使得 $x^2 < x$。这类 $x$ 满足什么范围？
  + 计算 $(-3) times (-frac(1, 3)) times 2 times (-frac(1, 2)) times 5 times (-frac(1, 5))$，并说出你数负号个数的一般规则。

#answer-cut[
    + $(-3) times (-7) = 21$（同号正）。
    + $8 times (-5) = -40$（异号负）。
    + $-12 div 4 = -3$（异号负）。
    + $(-15) div (-3) = 5$（同号正）。
    + 错；负负得正只对乘除成立，加法应为 $(-2) + (-3) = -5$。
    + $8$ 秒前的高度比现在高 $(-3) times (-8) = 24$ 米（乘以 $-8$ 秒表示往前推 $8$ 秒）。
    + $5 times (-2) = -10$，意为比现在短 $10$ 厘米（$2$ 秒前弹簧比现在短）。
    + 共 $99$ 个负数相乘，负号个数为奇数，结果为负。一般规则：$n$ 个非零数相乘，负号个数为偶数时结果为正，奇数时结果为负。
    + 异号（$a > 0, b < 0$）且正数的绝对值更大。例：$(5, -3)$：$5 + (-3) = 2 > 0$，$5 times (-3) = -15 < 0$；$(7, -2)$、$(3/2, -1/2)$ 同理。
  + $(-1)^{100} = 1$（偶次幂为正），$(-1)^{101} = -1$（奇次幂为负），$1 + (-1) = 0$（有理数乘法符号规则）。
  + 先涨 $10 %$：$a times 1.1$；再降 $10 %$：$a times 1.1 times 0.9 = a times 0.99 = 0.99a < a$。最终比原价*低* $1 %$（反直觉：先涨后降不会回到原价——有理数乘法 + 百分数）。
  + *[构造题]*：$x^2 < x$ 即 $x^2 - x < 0$，即 $x(x-1) < 0$，解得 $0 < x < 1$。例：$x = frac(1,2)$，$(frac(1,2))^2 = frac(1,4) < frac(1,2)$ ✓；$x = 0.9$，$0.81 < 0.9$ ✓（有理数乘法 + 不等式思维）。
  + 负号共 $4$ 个（偶数），结果为正。$(-3) times (-frac(1,3)) times 2 times (-frac(1,2)) times 5 times (-frac(1,5)) = (3 times frac(1,3)) times (2 times frac(1,2)) times (5 times frac(1,5)) = 1 times 1 times 1 = 1$（配对消去 + 有理数乘法符号规则：负号偶数个结果为正）。
]
]

#side-hack[
  ☞ 负斜率、负系数展开括号、平方差公式里乘以负数——函数和代数里凡是涉及符号的结果，都从这里的"两负得正"规律推出来。
]
