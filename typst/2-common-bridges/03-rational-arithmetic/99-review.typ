#include "01-rational-addition.typ"
#include "02-rational-subtraction.typ"
#include "03-rational-multiplication-division.typ"
#include "04-laws-revisited.typ"

#import "../../lib/chapter-kit.typ": blueprint, mastery, side-hack, secref, answer-cut

=== 第 3 章回顾

#blueprint[
  *速查卡：有理数四则的统一规则*

  #table(
    columns: (auto, 1fr),
    inset: 6pt,
    stroke: none,
    fill: (_, y) => if y == 0 { rgb("#BBDEFB") } else if calc.odd(y) { luma(247) } else { none },
    table.header([*运算*], [*核心规则*]),
    [加法（同号）], [取共同符号，绝对值相加。例：$(-3) + (-5) = -8$。],
    [加法（异号）], [取较大绝对值的符号，绝对值相减。例：$(-3) + 5 = 2$。],
    [减法], [统一进加法 — 减一个数 = 加它的相反数。例：$3 - (-5) = 3 + 5 = 8$。],
    [乘法], [同号正、异号负、与零得零；绝对值相乘。例：$(-2) times (-4) = 8$。],
    [除法], [统一进乘法 — 除以非零 $b$ = 乘以 $1/b$；除以 $0$ 没有意义。],
    [运算律], [加法、乘法的交换/结合/分配律全部成立；减法、除法不直接交换或结合。],
  )
]

==== 混合自测

#mastery[
  *基础（每题 1 分）*

  + 计算：$-7 + (-3) - (-5)$。
  + 计算：$(-4) times (-6) div (-3)$。
  + 用分配律：$(-25) times 96$（提示：$96 = 100 - 4$）。
  + 判断对错并改正："$0 div (-5) = $ 无意义。"

  *应用（每题 2 分）*

  + 海底地形：礁石 $A$ 在 $-12$ 米，礁石 $B$ 在 $-37$ 米，渔网张在 $-25$ 米。渔网到礁石 $A$、$B$ 的距离分别是多少？
  + 阶梯式电费：第一档每度 $-0.5$ 元（成本，记为负），售价每度 $0.85$ 元；本月卖出 $200$ 度，纯利润是多少？

  *挑战（每题 3 分）*

  + 已知 $a + b + c = 0$，$a$、$b$、$c$ 是三个互不相等的非零有理数。证明：它们当中至少有一个是正数，至少有一个是负数。
  + 反例构造：举一个具体的例子，说明"$a div (b + c)$ 不等于 $a div b + a div c$"。

  #answer-cut[
    + $-7 + (-3) - (-5) = -7 + (-3) + 5 = -5$。
    + $(-4) times (-6) div (-3) = 24 div (-3) = -8$。
    + $(-25)(100 - 4) = -2500 + 100 = -2400$。
    + 错；$0$ 除以任何非零数都是 $0$，所以 $0 div (-5) = 0$。无意义的是"除以 $0$"。
    + 距 $A$：$|-25 - (-12)| = 13$ 米；距 $B$：$|-25 - (-37)| = 12$ 米。
    + 单度利润 $0.85 + (-0.5) = 0.35$ 元；$200$ 度共 $70$ 元。
    + 假设三者全非负：均非零，则和 $> 0$，与 $a + b + c = 0$ 矛盾；全非正时和 $< 0$，同样矛盾。故必有正有负。
    + 例：$a = 12$，$b = 3$，$c = 1$。$a div (b + c) = 12 div 4 = 3$；$a div b + a div c = 4 + 12 = 16$；$3 eq.not 16$。
  ]
]

#side-hack[
  *☞ 这会在哪里再用到*：

  - 进入 #secref("ch:letters-expressions-relationship-templates") 后，所有代数式化简（合并同类项、分配律展开）都默认你已经能处理含负号的乘除。
  - 进入 #secref("ch:equation-balance-inverse-operations") 后，移项变号、两边同除负数都建立在本章规则之上。
  - 在数据分支统计计算中，离均差与方差的运算同样依赖这套规则。
]
