#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §1.5 混合运算 <sec-1-5>

#crisis[
  便利店购物：3 包薯片每包 5 元，再加 1 瓶水 3 元。

  小明算：$3 + 5 times 3 = 8 times 3 = 24$ 元

  小红算：$3 times 5 + 3 = 15 + 3 = 18$ 元

  两人算法不同，答案也不同！*到底应该先算哪个？*
]

#side-hack[
  读题时画圈——先把乘除法圈出来，它们优先计算。有括号的更优先！
]

#discovery[
  先来看另一道题：$3 + 4 times 2$

  小明说：先算加法，$3 + 4 = 7$，再乘 $2$，得 $7 times 2 = 14$。

  小华说：不对，先算乘法，$4 times 2 = 8$，再加 $3$，得 $3 + 8 = 11$。

  两人算法都有道理——可数学需要唯一答案。怎么办？

  数学界约定了一套大家都遵守的规则，让每道题只有一种算法：

  + 先算*括号*里的
  + 再算*乘除*
  + 最后算*加减*
  + 同级运算从左往右算

  按这套约定，$3 + 4 times 2$ 先算乘法：$4 times 2 = 8$，再算 $3 + 8 = 11$。

  回到便利店：薯片的价格 $3 times 5 = 15$ 元是一个整体，所以正确算法是

  $3 times 5 + 3 = 15 + 3 = 18$ 元

  如果想先算加法呢？用括号强制改变顺序！$(3 + 5) times 3 = 24$
]

#tryit[
  *先想想生活场景*：你去买了 3 盒饼干，每盒 4 元，另外又散装买了 2 块饼干，每块 1 元。

  这笔花销写成算式是 $2 + 3 times 4 - 1$。

  在知道运算顺序规则之前，先猜一猜：哪种算法才符合这道购物题的意思？

  - 是"从左往右"：$(2 + 3) times 4 - 1 = 5 times 4 - 1 = 19$？
  - 还是"先算乘法"：$2 + 12 - 1 = 13$？

  想一想实际花了多少钱，再决定哪个答案合理，写下你的判断，然后往下对照规则验证。
]

#blueprint[
  运算顺序规则：
  + *括号最优先*：先算小括号 $()$，再算中括号 $[]$
  + *先乘除，后加减*
  + *同级从左到右*

  例：$2 + 3 times 4 - 6 div 2 = 2 + 12 - 3 = 11$
]

#pitfall[
  *高频错误*

  ❌ 从左往右依次算，不管运算符优先级：$3 + 4 times 2$ 算成 $(3 + 4) times 2 = 14$
  → ✓ 乘除优先于加减：先算 $4 times 2 = 8$，再算 $3 + 8 = 11$

  ❌ 有括号时，忘记先算括号里面：$(80 - 56) div [2 times (9 - 5)]$ 中先算了外层
  → ✓ 从最内层括号开始：先算 $(9 - 5) = 4$，再算 $[2 times 4] = 8$，最后算 $(80 - 56) div 8 = 3$
]

#mastery[
  *基础*

  + $24 - 8 div 4 + 3 times 2$
  + $5 times (3 + 7) - 20 div 4$
  + $(80 - 56) div [2 times (9 - 5)]$

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_
  + $24 - 2 + 6 = 28$
  + $5 times 10 - 5 = 50 - 5 = 45$
  + $24 div [2 times 4] = 24 div 8 = 3$

  *挑战*

  + 用一对括号（最多加一对），把算式 $3 + 4 times 2 - 1$ 改造成结果等于 $13$ 的式子。写出加括号后的算式和计算过程。

  + 同样的数字 $2, 3, 4, 5$（按这个顺序），用加减乘除和括号，能不能凑出结果 $24$？试一试，写出一个可能的算式。（若凑不到 $24$，找到你能凑到的最接近 $24$ 的整数。）

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _挑战参考答案：_

  第一题：$(3 + 4) times 2 - 1 = 7 times 2 - 1 = 14 - 1 = 13$。（也可以试 $3 + 4 times (2 - 1) = 3 + 4 = 7$，不对；$3 + (4 times 2 - 1) = 3 + 7 = 10$，不对。所以第一个方案正确。）

  第二题：这是一道开放挑战题，展示尝试过程比找到唯一答案更重要。一种保持顺序的尝试：$2 times 3 times 4 + 5 - 5 = ...$（超出题意）；$(2 + 3) times 4 + 5 - 5 = 20$（接近）；$2 times (3 + 4 times ... )$……答案不唯一，重点是展示你的尝试过程。
]
