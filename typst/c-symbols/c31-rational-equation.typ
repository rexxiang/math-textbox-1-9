#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, secref, tryit, pitfall

== §c31 分式方程：去掉分母的诀窍 <tool:c31-rational-equation>

#vocab[分式方程 rational equation / 增根 extraneous root / 验根 checking the solution]

#crisis[
  在 §6.5A 里，你学会了加减两个分式。现在，一个含分式的*方程*摆在你面前：

  $ frac(2, x - 1) = frac(x + 1, x - 1) + 1 $

  这看起来没什么难的——直接两边乘以 $(x - 1)$ 去掉分母，变成整式方程，再用平衡法解，不就行了？

  你试试看：两边乘以 $(x - 1)$：

  $ 2 = (x + 1) + (x - 1) $

  $ 2 = 2x $

  $ x = 1 $

  好，得到 $x = 1$。把它代回原式试试：$frac(2, 1 - 1) = frac(2, 0)$……

  分母为零！这个"解"根本不能代入原来的方程！

  我们明明没有算错，但答案是假的——这是怎么回事？
]

#discovery[
  *去分母时发生了什么？*

  原方程只在 $x - 1 != 0$，即 $x != 1$ 时有意义。

  但两边乘以 $(x - 1)$ 之后，方程变成了整式方程——整式方程对任何 $x$ 都有意义，包括 $x = 1$。

  这就是问题所在：我们*扩大了方程的定义域*。新的整式方程可能有原来分式方程没有的解——这个多出来的假解，叫做*增根*。

  增根是什么样的值？让某个分母为零的那些值。因为这些值代入原分式方程会让分母变成 $0$，方程无意义，它们不是合法的解。

  所以，解分式方程多了一步不可省略的检验：*把解代回原方程的分母，看看分母是否为零*。这一步叫做*验根*。

  增根不是你算错了，是去分母这个操作的副作用。只要记得验根，就能把增根过滤掉。
]

#tryit[
  先自己试一试，再对照下面的 blueprint！

  解方程：$frac(2, x) = frac(1, 3)$

  步骤提示：
  + 最简公分母是什么？
  + 两边乘以公分母，把分式消掉，得到一个整式方程
  + 用 undo 直觉（#secref("c04-equation")）解这个整式方程
  + 把解代回原方程的每个分母，检验是否为零

  写出完整过程后，对照 blueprint 里的例 1 检验。
]

#blueprint[
  *工具一：分式方程*

  分母中含有未知数的方程叫做*分式方程*。

  例如：$frac(2, x - 1) = 3$，$frac(1, x) + frac(1, x + 1) = 1$，$frac(3, x + 2) + frac(1, x - 2) = frac(8, x^2 - 4)$

  解分式方程的标准流程：

  + *去分母*：方程两边乘以最简公分母，把分式方程化为整式方程（利用 §6.5A 找最简公分母的方法）
  + *用平衡法（#secref("c04-equation") 的 undo 直觉）解去分母后的整式方程*：去分母后得到形如 $a x = b$ 或 $a x + b = c$ 的方程，用"倒着 undo"的思路一步步解出 $x$
  + *验根*：把解代入原分式方程的所有分母，检查是否为零；为零的解是增根，必须舍去

  *工具二：增根*

  去分母后，整式方程多出来的、使原方程某个分母为零的根，叫做*增根*。

  增根一定让某个分母等于零——验根时只需要检查这一点，不必重新代入整个方程验算。

  *例 1*：解方程 $frac(2, x) = frac(1, 3)$

  最简公分母是 $3x$，两边乘以 $3x$：

  $ 6 = x $

  验根：$x = 6$ 时，分母 $x = 6 != 0$。✓

  答：$x = 6$。

  *例 2*：解方程 $frac(2, x - 1) = frac(x + 1, x - 1) + 1$（§6.5B 危机中的那道题）

  最简公分母是 $x - 1$，两边乘以 $x - 1$：

  $ 2 = (x + 1) + (x - 1) $

  $ 2 = 2x $

  用 undo 直觉（§4.4）：$x$ 被乘以 $2$ 得 $2$，所以 $x = 2 div 2 = 1$。

  *工具三：验根*

  验根：$x = 1$ 时分母 $x - 1 = 0$，分母为零，代入原方程无意义！$x = 1$ 是增根，必须舍去。

  结论：*原方程无解*。

  *例 3*：解方程 $frac(1, x) = frac(2, 3x) + frac(1, 6)$

  最简公分母是 $6x$，两边乘以 $6x$：

  $ 6 = 4 + x $

  用 undo 直觉（§4.4）：$x$ 加了 $4$ 得 $6$，所以 $x = 6 - 4 = 2$。

  验根：$x = 2$ 时，分母 $x = 2 != 0$，$3x = 6 != 0$。✓

  答：$x = 2$。
]

#pitfall[
  *分式方程最大的坑——解完忘记验根！*

  ❌ 解方程 $frac(2, x - 1) = frac(x + 1, x - 1) + 1$ 得 $x = 1$，直接写「答：$x = 1$」。

  ✓ 把 $x = 1$ 代入分母：$x - 1 = 0$，分母为零，原式无意义。$x = 1$ 是增根，原方程*无解*。

  *每一道分式方程，最后一步必须验根*——哪怕感觉解起来很顺利。增根不是你的失误，是去分母操作的必然副产品。

  *另一个常见错误：去分母时常数项也要乘以公分母*

  比如 $frac(1, x - 1) = 2$，两边乘以 $(x-1)$：

  ❌ 错误：$1 = 2$（右边的 $2$ 没有乘以 $(x-1)$）

  ✓ 正确：$1 = 2(x-1)$。方程每一项都要乘公分母（去掉括号展开需要更多步骤，这里先记住"每项都要乘"这一原则）。
]

#side-hack[
  验根有个捷径：*增根只可能是让某个分母为零的值*。

  所以在解出答案之后，只需要把每个解代入所有分母，看哪个分母变成了零——变成零的就是增根，直接舍去。不用把解代入整个方程重新计算。
]

#mastery[
  *基础*

  + 解下列分式方程（去分母后，用 #secref("c04-equation") 的 undo 直觉解出 $x$）：
    - $frac(3, x) = frac(1, 4)$
    - $frac(5, x) = frac(1, 2)$
    - $frac(2, x + 1) = frac(1, 3)$

  *应用*

  + 解方程，并标注是否有增根：
    - $frac(x, x - 3) - frac(3, x - 3) = 1$（提示：去分母后看左边能否直接合并）
    - $frac(t, 10) + frac(t, 15) = 1$（工程题：公分母是 30，乘后得 $3t + 2t = 30$）
  + 甲单独完成一项工程需 12 天，乙单独需 6 天。两人合作几天可以完成？（设合作 $d$ 天，列出分式方程后解方程）

  *挑战*（提示：这些题去分母后需要移项或合并同类项，我们在 §6.6 会系统学到——现在试试看能不能用 undo 思路摸索出答案）

  + 解方程：$frac(1, x) = frac(2, x + 3)$
  + 若方程 $frac(a, x - 2) = frac(3, x - 2) - 1$ 无解，求 $a$ 的值，并解释为什么。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + $frac(3, x) = frac(1, 4)$：乘 $4x$，得 $12 = x$。验根：$x = 12 != 0$ ✓ 答：$x = 12$。
  + $frac(5, x) = frac(1, 2)$：乘 $2x$，得 $10 = x$。验根：$x = 10 != 0$ ✓ 答：$x = 10$。
  + $frac(2, x+1) = frac(1, 3)$：乘 $3(x+1)$，得 $6 = x + 1$，undo 减 $1$，$x = 5$。验根：$x + 1 = 6 != 0$ ✓ 答：$x = 5$。

  *应用*
  + $frac(x, x-3) - frac(3, x-3) = 1$：乘 $(x-3)$，得 $x - 3 = x - 3$，即 $0 = 0$，恒成立，但 $x = 3$ 使分母为零，故方程的解为所有不等于 $3$ 的实数（无数个解）。无增根（$x=3$ 使全式无意义，是要排除的值，不是增根，而是定义域限制）。
  + $frac(t, 10) + frac(t, 15) = 1$：公分母 $30$，乘后得 $3t + 2t = 30$，$5t = 30$，$t = 6$。验根：$t = 6$ 时分母 $10, 15$ 均不为零 ✓ 答：$t = 6$ 天。
  + 设合作 $d$ 天：$frac(d, 12) + frac(d, 6) = 1$，公分母 $12$，乘后得 $d + 2d = 12$，$3d = 12$，$d = 4$。验根：分母均不为零 ✓ 答：合作 $4$ 天。

  *挑战*
  + $frac(1, x) = frac(2, x+3)$：乘 $x(x+3)$，得 $x + 3 = 2x$。这里需要移项（§6.6 的方法），得 $x = 3$。验根：分母均不为零 ✓ 答：$x = 3$。
  + $frac(a, x-2) = frac(3, x-2) - 1$：乘 $(x-2)$，得 $a = 3 - (x-2) = 5 - x$，解为 $x = 5 - a$。若 $5 - a = 2$，即 $a = 3$，则这个"解"使分母为零，是增根，方程无解。答：$a = 3$ 时方程无解。

  _当方程更复杂时（如去分母后需去括号、合并同类项），我们在 §6.6 会学到更系统的解法。_
]
