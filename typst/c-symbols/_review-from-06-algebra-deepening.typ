#import "../lib/theme-v2.typ": blueprint, mastery, side-hack, secref

== 第六章回顾：代数深化

#blueprint[
  *知识速查卡*

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *整式整理*（#secref("c07-polynomial")）
      - 先辨认项，再决定哪些能合并
      - 去括号时，减号后的每一项都要变号
      - 同类项只动系数，字母部分不动
    ],
    block[
      *乘开与公式*（#secref("c12-polynomial-multiply")）
      - 多项式乘多项式：每一项都要乘
      - 平方差：$(a+b)(a-b) = a^2 - b^2$
      - 完全平方：$(a plus.minus b)^2 = a^2 plus.minus 2 a b + b^2$
    ],
  )

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *幂与指数*（#secref("c15-power-notation")）
      - 幂是重复相乘的短写法
      - 同底数幂相乘指数加，幂的乘方指数乘
      - 同底数幂相除指数减，$a^0 = 1$
    ],
    block[
      *因式分解*（#secref("c21-factorization")）
      - 顺序：一提公因式、二看公式、三十字相乘
      - $a^2 - b^2 = (a+b)(a-b)$
      - $a^2 plus.minus 2 a b + b^2 = (a plus.minus b)^2$
    ],
  )

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *分式与一元一次方程*（#secref("c25-rational-expression")、#secref("c34-linear-equation")）
      - 先因式分解，再约分、通分
      - 分式方程：去分母后一定验根
      - 五步：去分母→去括号→移项→合并→系数化 1
      - *特殊情况*：化简后两边矛盾（如 $3 = 6$）→ 无解；化简后恒成立（如 $0 = 0$）→ 无穷多解
    ],
    block[
      *方程组、二次方程与不等式*（#secref("c37-system-of-equations")、#secref("c40-quadratic-equation")、#secref("c45-inequality")）
      - 方程组：代入法 / 加减消元法
      - 二次方程：先看能否因式分解，不顺手时用配方法 / 求根公式
      - 不等式：乘除负数必须变号，解组看交集
    ],
  )
]

#mastery[
  *混合自测题*（不标知识点，综合调用）

  + 化简：$(3x - 2)^2 - (3x + 2)(3x - 2)$

  + 因式分解：$2x^3 - 8x$

  + 化简：$frac(x^2 - 9, x^2 - 6x + 9)$

  + 解方程：$frac(x + 1, 3) - frac(2x - 1, 6) = 1$

  + 解方程组：$cases(3x + 2y = 16, 5x - 2y = 8)$

  + 解方程：$2x^2 - 3x - 2 = 0$，并说说你为什么选这条路线。

  + 解不等式，在数轴上标出解集：$-3x + 6 > 0$

  + 解方程：$x^2 - 4x - 1 = 0$，并说说你为什么不先选因式分解。

  + 已知 $a + b = 3$，$a b = -10$，求 $a^2 + b^2$ 的值。

  + 解分式方程：$frac(2, x - 1) + 1 = frac(3, x - 1)$（注意验根）

  + 一个长方形的长比宽多 $4$ 厘米，面积为 $60$ cm²。求长和宽。

  + *进阶*：解不等式组 $cases(2x - 1 > 3, x + 4 <= 10)$，并求满足条件的所有整数 $x$。

  + 化简：$(2x^2)^3 div (4x^4)$。

  + 把 $x^2 + 5x + 6$ 因式分解。

  + 计算：$frac(1, x-1) - frac(2, x^2-1)$（$x != plus.minus 1$）。
]

#line(length: 100%, stroke: 0.3pt + luma(200))
*参考答案*

  + $(3x-2)^2 - (3x+2)(3x-2) = (3x-2)[(3x-2) - (3x+2)] = (3x-2)(-4) = -12x + 8$
  + $2x^3 - 8x = 2x(x^2 - 4) = 2x(x+2)(x-2)$
  + $frac(x^2 - 9, x^2 - 6x + 9) = frac((x+3)(x-3), (x-3)^2) = frac(x+3, x-3)$，但原式分母不能为 0，所以 $x != 3$
  + 两边乘 6：$2(x+1) - (2x-1) = 6$，化简得 $2x+2-2x+1 = 6$，也就是 $3 = 6$——矛盾！说明不是算错，而是这个方程本来就无解。
  + 两式相加消去 $y$：$8x = 24$，$x = 3$；代入 $3x + 2y = 16$，得 $9 + 2y = 16$，所以 $y = frac(7, 2)$
  + 因为这个方程不容易直接因式分解，所以选求根公式最自然：$x = frac(3 plus.minus sqrt(9+16), 4) = frac(3 plus.minus 5, 4)$，$x = 2$ 或 $x = -frac(1, 2)$。
  + $-3x > -6$，除以 $-3$ 时不等号要变号，所以 $x < 2$；数轴上在 2 处画空心圆，向左涂黑
  + 因为它不能直接分成两个整式因式，但左边很接近完全平方，所以先配方最顺：$x^2 - 4x = 1$，两边加 4，得 $(x - 2)^2 = 5$，所以 $x = 2 plus.minus sqrt(5)$。
  + $a^2 + b^2 = (a + b)^2 - 2 a b = 9 - 2 times (-10) = 29$
  + 两边乘 $(x-1)$：$2 + (x-1) = 3$，得 $x = 2$。验根时别漏掉原分母：$x=2$ 时 $x-1=1 != 0$，所以是有效解。
  + 设宽 $x$，长 $x+4$：$x(x+4) = 60$，$x^2+4x-60=0$，$(x+10)(x-6)=0$。代数上有 $x=-10$ 或 $x=6$，但宽不能是负数，所以取 $x=6$。宽 6 cm，长 10 cm。
  + 解第一个：$x > 2$；解第二个：$x <= 6$。解集 $2 < x <= 6$，整数为 $3, 4, 5, 6$。

  + $(2x^2)^3 div (4x^4) = 8x^6 div 4x^4 = 2x^2$。

  + $x^2 + 5x + 6 = (x+2)(x+3)$（找两个数，积为 6，和为 5，即 2 和 3）。

  + $frac(1, x-1) - frac(2, x^2-1) = frac(1, x-1) - frac(2, (x-1)(x+1))$；通分后 $= frac(x+1, (x-1)(x+1)) - frac(2, (x-1)(x+1)) = frac(x+1-2, (x-1)(x+1)) = frac(x-1, (x-1)(x+1)) = frac(1, x+1)$（$x != plus.minus 1$）。

*螺旋复习（与前面主干的联系）*

  + 回看 #secref("c01-variable")：把”长方形长为 $x+3$、宽为 $x-1$”先写成面积代数式，再化简。你应得到 $(x+3)(x-1) = x^2 + 2x - 3$。

  + 回看 #secref("c04-equation")：桥上用 undo 解决的是一步方程；现在请用同样的平衡观解释，为什么从 $-3x > -6$ 到 $x < 2$ 时不等号必须反向。

  + 回看 #secref("a44-ratio") 和 #secref("c04-equation")：一种果汁按浓缩液 : 水 $= 1 : 4$ 调配。现在要配成总量 750 mL 的果汁。先用比例判断浓缩液是多少，再解释为什么这个比例判断和列方程求解说的是同一件事。（答：浓缩液 150 mL，水 600 mL。）
