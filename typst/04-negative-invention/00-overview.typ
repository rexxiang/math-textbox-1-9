#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab

_负数的发明——自然数只能表示「有」，不能表示「欠」。_

在小学阶段，你已经掌握了自然数、分数和小数。可是，当我们需要表示「温度低于零度」「账户欠款」这些概念时，现有的数不够用了。

本章的核心问题：*如何把「方向」嵌入数字系统？*

你将发明两组新工具：

+ *有理数* ——在数轴上向左延伸，发明负数
+ *实数* ——填满数轴上所有的「空隙」

这两步做完后，数轴才真正完整：既能表示“往左还是往右”，也能表示像 $sqrt(2)$ 这样写不成分数、却真实存在的位置。后面的代数、函数与几何，都要站在这条更完整的数轴上继续生长。

#history-note[
  中国《九章算术》用红色算筹表示正数、黑色表示负数——比欧洲接受负数早了约 1500 年。
]
