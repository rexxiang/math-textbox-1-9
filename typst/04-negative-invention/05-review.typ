#import "../lib/theme-v2.typ": blueprint, mastery, secref

== 第四章回顾：负数的发明

#blueprint[
  *知识速查卡*

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *有理数*（#secref("4.1")）
      - 有理数 $= $ 整数 $+$ 分数（包括正负和零）
      - 绝对值：$|a|$ = $a$ 到原点的距离（非负）
      - 相反数：$a$ 和 $-a$，和为零
      - 有理数大小：正数 $>$ 0 $>$ 负数；负数绝对值越大越小
      - 运算律对有理数全部成立（见 #secref("1.6")）
    ],
    block[
      *实数*（#secref("4.2")）
      - 无理数：无限不循环小数（$sqrt(2)$、$sqrt(3)$、$pi$……）
      - 实数 $=$ 有理数 $+$ 无理数
      - $sqrt(a)$ 的化简：$sqrt(a^2) = |a|$（不是 $a$！）
      - 算术平方根：$sqrt(a) >= 0$（非负）
    ],
  )

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *点、线、面*（#secref("4.3")）
      - 两点确定一条直线
      - 不在同一直线的三点确定一个平面
      - 两直线位置：平行（不相交，无公共点）或相交（有一公共点）
      - 三维空间：两平面平行或相交（交线是直线）
    ],
    block[
      *幂的运算*（#secref("4.4")）
      - $a^m times a^n = a^{m+n}$（同底相乘，指数相加）
      - $(a^m)^n = a^{m times n}$（幂的乘方，指数相乘）
      - $(a b)^n = a^n b^n$（积的乘方，指数分配）
      - $a^m div a^n = a^{m-n}$（$a != 0$）
      - $a^0 = 1$（$a != 0$）；$a^{-n} = frac(1, a^n)$（$a != 0$）
    ],
  )
]

#mastery[
  *混合自测题*

  + 把下列数分类（有理数/无理数/整数/自然数）：$-5$、$0$、$sqrt(9)$、$sqrt(2)$、$pi$、$frac(2, 3)$、$-frac(3, 4)$

  + 计算：$(-3) + 7 - (-5) + (-2)$

  + 计算：$(-6) times (-4) div (-8)$

  + 若 $|a| = 5$，$|b| = 3$，且 $a - b > 0$，求 $a + b$ 的所有可能值。

  + 比较大小：$-sqrt(3)$ 和 $-sqrt(2)$（提示：哪个绝对值更大？）

  + 化简：$sqrt((-5)^2)$；$-sqrt(16)$；$(sqrt(3))^2$

  + 化简：$3 x^2 times (-2 x^3)$；$(a^2 b)^3$；$frac(6 a^4 b^2, 2 a^2 b)$

  + 计算：$2^3 times 2^(-1) + 3^0 - 4^(1/2)$（提示：$4^(1/2) = sqrt(4)$）

  + 空间中：3 条直线最多有几个交点？（考虑各种情形）

  + *进阶*：若 $a + b = -1$，$a b = -6$，求 $a^2 + b^2$ 和 $|a - b|$。

  + *进阶*：化简 $frac(a^{2n+1} times b^{n-1}, a^{2n-1} times b^{n+1})$（结果用 $a$ 和 $b$ 表示，$n$ 消去）。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  + $-5$：有理数、整数；$0$：有理数、整数、自然数；$sqrt(9)=3$：有理数、整数、自然数；$sqrt(2)$：无理数；$pi$：无理数；$frac(2, 3)$：有理数；$-frac(3, 4)$：有理数
  + $(-3) + 7 = 4$，$4 - (-5) = 9$，$9 + (-2) = 7$
  + $(-6) times (-4) = 24$，$24 div (-8) = -3$
  + $a$ 可以是 $5$ 或 $-5$；$b$ 可以是 $3$ 或 $-3$。条件 $a - b > 0$（即 $a > b$）：$a=5, b=3$ → $a+b=8$；$a=5, b=-3$ → $a+b=2$；$a=-5, b=-3$：$-5-(-3)=-2<0$，不满足；$a=-5, b=3$：$-5-3=-8<0$，不满足。所以 $a+b = 8$ 或 $2$。
  + $|-sqrt(3)| = sqrt(3) > sqrt(2) = |-sqrt(2)|$，所以 $-sqrt(3) < -sqrt(2)$
  + $sqrt((-5)^2) = sqrt(25) = 5$（绝对值）；$-sqrt(16) = -4$（负号在根号外）；$(sqrt(3))^2 = 3$
  + $-6x^5$；$a^6 b^3$；$3a^2 b$
  + $2^3 times 2^{-1} = 2^2 = 4$；$3^0 = 1$；$4^{1/2} = 2$；结果 $= 4 + 1 - 2 = 3$
  + 最多：3 条直线两两相交且不共点，有 $C_3^2 = 3$ 个交点
  + $a^2+b^2 = (a+b)^2-2 a b = 1-(-12) = 13$；$(a-b)^2 = (a+b)^2-4 a b = 1+24 = 25$，$|a-b| = 5$
  + $frac(a^{2n+1}, a^{2n-1}) = a^2$；$frac(b^{n-1}, b^{n+1}) = b^{-2} = frac(1, b^2)$；结果 $= frac(a^2, b^2)$

  *螺旋复习*

  + 本章幂运算是 #secref("5.1") 整式运算的基础——$3x^2 times (-2x^3)$ 和本章规律完全一致。
  + 第 5 章二次方程中，判别式 $Delta = b^2 - 4 a c$ 里出现了负数的运算——负数不能开实数平方根，这正是本章的知识！
  + #secref("7.2") 反比例函数 $y = k/x$ 用到了本章的负指数：$y = k x^{-1}$。
]
