#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §4.3 点、线、面 <sec-4-3>

#vocab[点 point / 线段 segment / 射线 ray / 直线 line]

#crisis[
  打开手机地图，你看到一个小圆点标记着「你在这里」。

  这个圆点代表你的位置——可是你的身体明明有一定大小，为什么地图上只需要一个*没有大小*的点？

  GPS 导航告诉你「沿这条路直走 500 米」。路在地图上是一条线，500 米是一段长度。

  *点、线、面——这些最基本的几何概念，到底是什么？*
]

#history-note[
  公元前 300 年，欧几里得在《几何原本》中写下了几何学的起点：「点是没有部分的东西。」「线是没有宽度的长度。」这种从最基本概念出发、通过逻辑推理建立整个数学体系的方法，影响了人类文明 2300 年。
]

#discovery[
  *发现点*：把铅笔尖削得越来越细，在纸上轻轻一点——这个印记就越来越接近数学中的「点」。数学中的点*只有位置，没有大小*。

  *发现线*：把一根绳子拉直——这就是*线段*，它有两个端点，有确定的长度。

  如果把线段的一端无限延伸呢？那就是*射线*——有一个端点，向一个方向无限延伸。

  如果把线段的两端都无限延伸呢？那就是*直线*——没有端点，向两个方向无限延伸。

  *关键实验*：在纸上画两个点 A 和 B，用不同方式连接它们——弯弯曲曲的线、折线、直线。哪一条最短？

  *两点之间，线段最短。*
]

#tryit[
  在看例题前，先自己思考一思考：

  平面上两条直线，一共有几种位置关系？

  + 每种情况各有几个交点？
  + 你能用一句话描述每种情况吗？

  写下你的答案，再往下看。
]

#blueprint[
  *基本概念*

  #table(
    columns: (auto, auto, auto, auto),
    inset: 6pt,
    stroke: 0.4pt,
    table.header([*名称*], [*端点*], [*延伸*], [*长度*]),
    [线段], [2 个], [不延伸], [有限],
    [射线], [1 个], [一个方向], [无限],
    [直线], [0 个], [两个方向], [无限],
  )

  *基本事实*（公理）
  - 两点确定一条直线
  - 两点之间线段最短
  - 线段的*中点*把线段分成两段相等的部分

  *线段长度的表示*：线段 $A B$ 的长度记作 $A B$ 或 $overline(A B)$。

  *面的初步概念*：桌面、黑板面、水面……都是面的一部分。面可以是平的（平面），也可以是弯曲的。本书主要研究平面。
]

#pitfall[
  *最常见的两类错误：*

  #grid(
    columns: (1fr, 1fr),
    gutter: 8pt,
    block[
      *❌ 错误：混淆平行与相交*

      两条直线没有交点，

      所以它们一定平行
    ],
    block[
      *✓ 正确：在平面内，无交点才是平行；在空间中还有异面直线*

      平面内两直线：相交（1 个交点）

      或平行（0 个交点，且同向延伸不会相遇）
    ],
  )

  #grid(
    columns: (1fr, 1fr),
    gutter: 8pt,
    block[
      *❌ 错误：任意三点都确定一个平面*

      平面上有三个点 A、B、C，

      所以这三点确定唯一一个平面
    ],
    block[
      *✓ 正确：三点不共线才能确定唯一平面*

      若 A、B、C 三点共线（在同一条直线上），

      则经过这三点的平面有无数个
    ],
  )
]

#mastery[
  *基础*

  + 下列图形各有几条线段？
    - 一个三角形
    - 连接 4 个点（不是三点共线）的所有线段
  + 线段 $A B = 10 "cm"$，$M$ 是 $A B$ 的中点，求 $A M$ 的长度。

  *应用*

  + 蚂蚁从长方体的一个顶点爬到对角顶点，怎样爬路线最短？（提示：展开长方体表面）

  *挑战*

  + 平面上有 $n$ 个点（没有三点共线），一共可以画多少条线段？写出关于 $n$ 的公式。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + 三角形有 3 条线段；4 个点（不三点共线）共 $frac(4 times 3, 2) = 6$ 条线段。
  + $A M = frac(1, 2) A B = 5 "cm"$

  *应用*
  + 将长方体表面展开为平面图形，蚂蚁从起点到终点的直线距离就是最短路径（两点之间线段最短）。

  *挑战*
  + 每个点与其他 $n - 1$ 个点各连一条线段，共 $n(n-1)$ 次，但每条线段被算了两次，所以总数为 $frac(n(n-1), 2)$。
]
