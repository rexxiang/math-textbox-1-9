#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, secref

== §4.2 实数 <sec-4-2>

#vocab[平方根 square root / 无理数 irrational number / 实数 real number]

#crisis[
  教室里有一块正方形的地砖，边长恰好是 1 米。你量了它的对角线——

  根据勾股定理（#secref("6.8")会详细讲），对角线长度 $d$ 满足：

  $ d^2 = 1^2 + 1^2 = 2 $

  所以 $d = sqrt(2)$。可是 $sqrt(2)$ 到底等于多少？

  $1.4^2 = 1.96$（小了）；$1.5^2 = 2.25$（大了）；$1.41^2 = 1.9881$……

  怎么除都除不尽！*$sqrt(2)$ 不是分数，那它是什么数？*
]

#history-note[
  毕达哥拉斯学派相信「万物皆数」——他们认为一切量都能用分数（有理数）表示。当他们发现 $sqrt(2)$ 无法用分数表示时，据说震惊到「将发现者希帕索斯溺死在海中」。这个故事虽有传说成分，但 $sqrt(2)$ 的发现确实引发了古希腊的一场数学危机。
]

#discovery[
  *平方根*：如果 $x^2 = a$（$a >= 0$），那么 $x$ 叫做 $a$ 的平方根。

  - $3^2 = 9$，$(-3)^2 = 9$ → $9$ 的平方根是 $plus.minus 3$
  - *算术平方根*特指非负的那个：$sqrt(9) = 3$

  *立方根*：如果 $x^3 = a$，那么 $x$ 叫做 $a$ 的立方根，记作 $root(3, a)$。

  - $root(3, 8) = 2$，$root(3, -27) = -3$（负数也有立方根！）

  回到 $sqrt(2)$：它是无限不循环小数 $1.41421356...$

  *关键发现*：分数写成小数，要么有限，要么无限循环。而 $sqrt(2)$ 是无限*不*循环的——它不是任何分数！

  这种数叫做*无理数*。
]

#blueprint[
  *平方根与算术平方根*
  - $a$ 的平方根：$plus.minus sqrt(a)$（$a >= 0$）
  - $a$ 的算术平方根：$sqrt(a)$（非负）
  - $sqrt(0) = 0$，$sqrt(1) = 1$，$sqrt(4) = 2$，$sqrt(9) = 3$

  *立方根*
  - $root(3, a)$：任何实数 $a$ 都有唯一的立方根

  *无理数*：无限不循环小数。常见的无理数有 $sqrt(2)$、$sqrt(3)$、$pi$。

  *实数的分类*

  $ "实数" cases(
    "有理数" cases("整数", "分数"),
    "无理数"
  ) $

  *实数与数轴*：有理数和无理数一起，恰好*填满*了整个数轴。数轴上的每一个点都对应一个实数，每一个实数都对应数轴上的一个点。

  实数的运算律（加法交换律、结合律，乘法交换律、结合律、分配律）对所有实数都成立。
]

#side-hack[
  快速判断：开方能开尽就是有理数（如 $sqrt(4) = 2$），开不尽就是无理数（如 $sqrt(5)$）。但注意 $sqrt(0.04) = 0.2$ 是有理数！
]

#mastery[
  *基础*

  + 求下列各数的平方根和算术平方根：$25$、$0.49$、$frac(9, 16)$
  + 求下列各数的立方根：$64$、$-125$、$frac(1, 8)$
  + 判断下列各数是有理数还是无理数：$sqrt(3)$、$-sqrt(16)$、$0.333...$、$pi$、$3.14$

  *应用*

  + 一块正方形草坪面积为 $50 "m"^2$，它的边长是多少米？（保留两位小数）
  + 一个正方体水箱容积为 $27 "dm"^3$，它的棱长是多少？

  *挑战*

  + 用反证法证明 $sqrt(3)$ 是无理数。

    提示：假设 $sqrt(3) = frac(p, q)$（$p$、$q$ 是互质的正整数），两边平方得 $3 q^2 = p^2$，说明 $p$ 是 $3$ 的倍数。设 $p = 3k$，代入得 $q^2 = 3k^2$，说明 $q$ 也是 $3$ 的倍数。但 $p$、$q$ 互质，矛盾！

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + $25$：平方根 $plus.minus 5$，算术平方根 $5$；$0.49$：平方根 $plus.minus 0.7$，算术平方根 $0.7$；$frac(9, 16)$：平方根 $plus.minus frac(3, 4)$，算术平方根 $frac(3, 4)$
  + $root(3, 64) = 4$；$root(3, -125) = -5$；$root(3, frac(1, 8)) = frac(1, 2)$
  + $sqrt(3)$ 无理数；$-sqrt(16) = -4$ 有理数；$0.333... = frac(1, 3)$ 有理数；$pi$ 无理数；$3.14$ 有理数

  *应用*
  + 边长 $= sqrt(50) = 5sqrt(2) approx 7.07$（m）
  + 棱长 $= root(3, 27) = 3$（dm）
]

=== 二次根式运算

#blueprint[
  掌握根式的四则运算，是处理含根号表达式的基础。

  *乘法*：$sqrt(a) dot sqrt(b) = sqrt(a b)$（$a >= 0$，$b >= 0$）

  *除法*：$frac(sqrt(a), sqrt(b)) = sqrt(frac(a, b))$（$a >= 0$，$b > 0$）

  *最简根式*：被开方数中不含能开尽的因数。例如 $sqrt(12) = sqrt(4 times 3) = 2 sqrt(3)$。

  *同类根式合并*：被开方数相同的根式可以合并系数：
  $ 3 sqrt(2) + 5 sqrt(2) = 8 sqrt(2) $

  *分母有理化*：去掉分母中的根号：
  $ frac(1, sqrt(2)) = frac(sqrt(2), 2), quad frac(1, sqrt(3) - 1) = frac(sqrt(3) + 1, (sqrt(3))^2 - 1^2) = frac(sqrt(3) + 1, 2) $
  第二个例子用了平方差公式 $(a - b)(a + b) = a^2 - b^2$。
]

#mastery[
  *练一练*

  + 化简：$sqrt(48)$，$sqrt(75)$，$sqrt(frac(8, 9))$

  + 计算：$2 sqrt(3) + 5 sqrt(3) - sqrt(3)$

  + 有理化分母：$frac(6, sqrt(3))$，$frac(2, sqrt(5) + 1)$

  + 计算：$(sqrt(3) + sqrt(2))(sqrt(3) - sqrt(2))$
]
