#import "../lib/theme-v2.typ": blueprint, mastery, secref

== 第四章回顾：负数的发明

#blueprint[
  *知识速查卡*

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *有理数*（#secref("4.1")）
      - 有理数 $=$ 整数 $+$ 分数（包括正数、负数和 $0$）
      - 绝对值：$|a|$ 表示 $a$ 到原点的距离，永远非负
      - 相反数：$a$ 与 $-a$ 互为相反数，和为 $0$
      - 大小比较：正数 $>$ $0$ $>$ 负数；负数绝对值越大，数值越小
      - 加减乘除运算律在有理数范围内继续成立（除数不能为 $0$）
    ],
    block[
      *实数*（#secref("4.2")）
      - 无理数：无限不循环小数，如 $sqrt(2)$、$sqrt(3)$、$pi$
      - 实数 $=$ 有理数 $+$ 无理数
      - 算术平方根满足 $sqrt(a) >= 0$（当 $a >= 0$）
      - 注意：$sqrt(a^2) = |a|$，不是直接等于 $a$
      - 实数把数轴上的“空隙”补满了
    ],
  )
]

#mastery[
  *混合自测题*

  + 把下列数分类（有理数 / 无理数 / 整数 / 自然数）：$-5$、$0$、$sqrt(9)$、$sqrt(2)$、$pi$、$frac(2, 3)$、$-frac(3, 4)$。

  + 计算：$(-3) + 7 - (-5) + (-2)$。

  + 计算：$(-6) times (-4) div (-8)$。

  + 若 $|a| = 5$，$|b| = 3$，且 $a - b > 0$，求 $a + b$ 的所有可能值。

  + 比较大小：$-sqrt(3)$ 和 $-sqrt(2)$。

  + 化简：$sqrt((-5)^2)$；$-sqrt(16)$；$(sqrt(3))^2$。

  + 已知一个正方形的边长是 $sqrt(2)$，求它的对角线长度。

  + 若一个数的平方是 $13$，写出这个数的两个可能值。

  + *进阶*：若 $a + b = -1$，$a b = -6$，求 $a^2 + b^2$ 和 $|a - b|$。

  + *进阶*：在数轴上，点 $A$ 对应 $-3$，点 $B$ 对应 $2$。求 $A B$ 的长度，并解释为什么它等于 $|-3 - 2|$。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  + $-5$：有理数、整数；$0$：有理数、整数、自然数；$sqrt(9)=3$：有理数、整数、自然数；$sqrt(2)$：无理数；$pi$：无理数；$frac(2, 3)$：有理数；$-frac(3, 4)$：有理数。
  + $(-3) + 7 = 4$，$4 - (-5) = 9$，$9 + (-2) = 7$。
  + $(-6) times (-4) = 24$，$24 div (-8) = -3$。
  + $a$ 可以是 $5$ 或 $-5$，$b$ 可以是 $3$ 或 $-3$。由 $a - b > 0$ 得可行情况为 $(5, 3)$ 与 $(5, -3)$，故 $a + b = 8$ 或 $2$。
  + 因为 $sqrt(3) > sqrt(2)$，所以 $-sqrt(3) < -sqrt(2)$。
  + $sqrt((-5)^2) = 5$；$-sqrt(16) = -4$；$(sqrt(3))^2 = 3$。
  + 正方形对角线 $= sqrt((sqrt(2))^2 + (sqrt(2))^2) = sqrt(2 + 2) = 2$。
  + 若 $x^2 = 13$，则 $x = plus.minus sqrt(13)$。
  + $a^2 + b^2 = (a+b)^2 - 2 a b = 1 - (-12) = 13$；$(a-b)^2 = (a+b)^2 - 4 a b = 1 + 24 = 25$，所以 $|a-b| = 5$。
  + $A B = |(-3) - 2| = |-5| = 5$。数轴上两点距离只看相差多少个单位，不看方向，所以要取绝对值。

  *螺旋复习*

  + 用 #secref("2.4") 的分数小数互化说明：$-0.125 = -frac(1, 8)$，所以它为什么属于有理数？
  + 回想 #secref("1.6") 的运算律，解释为什么 $(-12) + 7 + 12 = 7$ 可以先把 $-12$ 与 $12$ 凑成一对相反数。
]
