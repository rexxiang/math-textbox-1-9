#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, secref, tryit, pitfall

== §a25 最大公因数与最小公倍数：找"公约"和"公倍" <tool:a25-prime-factorization>

#vocab[质因数分解 prime factorization / 最大公因数 Greatest Common Divisor (GCD) / 最小公倍数 Least Common Multiple (LCM)]

#crisis[
  小明和小红一起出发骑单车：小明每 6 秒踩一圈，小红每 8 秒踩一圈。出发时两人同时踩下，再过多少秒才会再次同时踩下？

  另一个问题：有 24 颗糖和 36 块橡皮糖，想把它们平均分成若干堆，每堆里糖和橡皮糖的数量都是整数且没有剩余——最多能分几堆？
]

#history-note[
  "公因数"和"公倍数"的概念在中国古代《九章算术》里就已经出现，主要用于分数约分与通分。欧几里得算法（辗转相除法）是求最大公因数的经典算法，早在公元前 300 年就被系统描述，是世界上最古老的算法之一，今天仍在计算机程序中广泛使用。
]

#discovery[
  §2.1A 里你已经试过把合数拆成质数相乘了——现在就来用这个技能。

  *质因数分解*：把一个合数逐步拆成质数相乘，直到每个因子都是质数为止。有两种方式：

  - *短除法*：每次用最小的质数去除，商写在下方，继续除直到商为 1
  - *因数树*：把数拆成两个因数，再对每个因数继续拆，直到叶子全为质数

  以 12 和 18 为例：
  $ 12 = 2 times 6 = 2 times 2 times 3 = 2^2 times 3 $
  $ 18 = 2 times 9 = 2 times 3 times 3 = 2 times 3^2 $

  有了这个分解，找"公共"的东西就变得清晰了：

  - 12 的因数：$1, 2, 3, 4, 6, 12$
  - 18 的因数：$1, 2, 3, 6, 9, 18$
  - 共同拥有的：$1, 2, 3, 6$——其中最大的是 $6$

  再来看倍数问题，以 6 和 8 为例：
  $ 6 = 2 times 3, quad 8 = 2^3 $

  列出倍数（前几个）：
  - 6 的倍数：$6, 12, 18, 24, 30, dots$
  - 8 的倍数：$8, 16, 24, 32, dots$
  - 共同出现的：$24, 48, 72, dots$——其中最小的是 $24$

  质因数分解后：
  - GCD 取每个质因数的*较小*次幂相乘
  - LCM 取每个质因数的*较大*次幂相乘
]

#tryit[
  先自己试试：

  + 用短除法或因数树，对 36 和 45 各做质因数分解。
  + 求 24 和 36 的最大公因数（先各做质因数分解，再取共同部分）。
  + 求 6 和 15 的最小公倍数。
]

#blueprint[
  - *质因数分解*：把一个合数写成若干质数相乘的形式，每个合数的分解结果唯一（顺序不计）。
    $ 12 = 2^2 times 3, quad 18 = 2 times 3^2, quad 60 = 2^2 times 3 times 5 $
  - *最大公因数（GCD）*：两个数共有因数中最大的那个。用质因数分解求时，取每个共同质因数的*较小*次幂相乘
    $ gcd(12, 18) = 2^1 times 3^1 = 6 $
  - *最小公倍数（LCM）*：两个数共有倍数中最小的那个正整数。用质因数分解求时，取每个质因数的*较大*次幂相乘
    $ "lcm"(6, 8) = 2^3 times 3^1 = 24 $
  - 常用场景：
    - "最长能剪成几米""最多几人一组" → 想 *GCD*
    - "最早再次同时""最少几个才能对齐" → 想 *LCM*
]

#side-hack[
  有一个巧记公式：$gcd(a, b) times "lcm"(a, b) = a times b$（对两个正整数成立）。

  知道其中一个，就能快速算出另一个：比如 $gcd(6, 8) = 2$，则 $"lcm"(6, 8) = (6 times 8) / 2 = 24$。
]

#pitfall[
  *高频错误*

  ❌ 求 LCM 时，只保留共同部分——把 6 和 15 的最小公倍数误写成 3
  → ✓ 3 只是它们的公因数，不是公倍数；LCM 要把所有质因数（取较大次幂）都乘进来：$6 = 2 times 3$，$15 = 3 times 5$，$"lcm"(6, 15) = 2 times 3 times 5 = 30$。

  ❌ 求 GCD 时，把两数相乘当答案
  → ✓ GCD 是"共同拥有"的部分，两数相乘是"各自全取"；$gcd(4, 6) = 2$，不是 $24$。
]

#mastery[
  *基础*

  + 对 18 和 60 各做质因数分解。
  + 求 18 和 24 的最大公因数。
  + 求 8 和 12 的最小公倍数。

  *应用*

  + 两根彩带长 20 厘米和 30 厘米，要剪成等长的小段且没有剩余，最长每段多少厘米？
  + 两盏信号灯分别每 6 秒、8 秒闪一次。如果现在同时闪，至少再过多少秒会再次同时闪？

  *挑战*

  + 三个数 12、18、24 同时求最大公因数：先求 $gcd(12, 18)$，再用结果和 24 继续求，看看这个方法是否有效？
  + 一个数既是 4 的倍数，也是 6 的倍数，还是 10 的倍数，问这个数最小是多少？

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_
  + $18 = 2 times 3^2$；$60 = 2^2 times 3 times 5$。
  + $gcd(18, 24) = 6$。
  + $"lcm"(8, 12) = 24$。
  + 最长每段 10 厘米（$gcd(20, 30) = 10$）。
  + 至少再过 24 秒（$"lcm"(6, 8) = 24$）。
  + $gcd(12, 18) = 6$，$gcd(6, 24) = 6$，所以三数最大公因数为 6。是的，逐步法有效。
  + $"lcm"(4, 6, 10)$：$4 = 2^2$，$6 = 2 times 3$，$10 = 2 times 5$，$"lcm" = 2^2 times 3 times 5 = 60$。
]
