#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §a28 分数的意义：整数世界装不下的数 <tool:a28-fraction-definition>

#vocab[分数 fraction / 真分数 proper fraction / 假分数 improper fraction]

#crisis[
  3 个好朋友平分 2 张披萨，每人吃多少？

  $2 div 3 = ?$ 答案不是整数！*整数世界装不下这个结果。*

  我们需要一种新的数——它能描述"部分"，能描述"整体的一块"，还能作为除法的结果。
]

#history-note[
  古埃及人只用"单位分数"——分子必须是 1，比如 $1/3$、$1/7$。要表示 $2/5$，他们得写成 $1/3 + 1/15$，非常麻烦。直到中国《九章算术》（约公元 1 世纪）才系统使用一般分数，把分子分母都当作独立的数来处理。今天我们写 $2/5$，正是沿用了这个传统。
]

#discovery[
  把一张披萨切成 3 等份，每份是这张披萨的 $1/3$。

  2 张披萨，每张取 $1/3$，合起来就是 $2/3$。所以 $2 div 3 = 2/3$。

  现在来观察不同的分数：

  - $3/4$：分母 4，分子 3，取了 4 份中的 3 份——分子比分母小，还没到一整个
  - $4/4$：正好取满 4 份中的 4 份，恰好等于 1 整个
  - $5/3$：分子比分母大——已经超过"一整个"了

  规律浮现：分子和分母的大小关系，决定了这个分数是"不足一个"还是"超过一个"。
]

#tryit[
  先自己想一想：

  + $3/4$ 和 $4/3$，哪个比 1 大，哪个比 1 小？你怎么知道的？
  + $7/7$ 等于多少？$8/7$ 呢，比 1 大还是小？
  + 一条丝带剪去了它的 $2/5$，剩下的部分用分数怎么表示？
]

#blueprint[
  - *分数* $a/b$：把整体分成 $b$ 等份，取其中 $a$ 份；$b$ 叫分母，$a$ 叫分子，$b eq.not 0$。同时它也表示除法 $a div b$
  - *真分数*：分子 $<$ 分母（如 $2/3$，$3/4$），值小于 1
  - *假分数*：分子 $>=$ 分母（如 $5/3$，$7/4$，$4/4$），值大于或等于 1
]

#side-hack[
  分数和除法是"同一件事的两张脸"：$a/b = a div b$。遇到"平均分"问题，自然就能写出分数；看到分数，也可以想象成一个除法。
]

#pitfall[
  *高频错误*

  ❌ 认为假分数"不正常"或"写错了"，看到 $7/4$ 就想马上化掉
  → ✓ 假分数完全合法。什么时候化、什么时候不化，要看题目需要——下一节我们就会专门讨论假分数的"另一种写法"。

  ❌ 把 $a/b$ 中的 $b$ 理解成"总共有几个"而不是"分成几等份"
  → ✓ 分母表示的是*等份数*，每份必须一样大。把 1 张饼不均匀地撕成 3 块，最大的那块不是 $1/3$。
]

#mastery[
  *基础*

  + 下列分数中，哪些是真分数，哪些是假分数？$3/5,\ 7/4,\ 9/9,\ 2/11,\ 11/10$
  + 用分数表示：把一条线段分成 7 等份，取其中 3 份，写出分数，并说明它是真分数还是假分数。
  + 两个分数 $5/8$ 和 $8/5$，哪个大于 1，哪个小于 1？不用计算，只用真/假分数的定义来判断。

  *应用*

  + 小华走了全程的 $3/8$，小明走了全程的 $5/8$，谁走得更多？不用计算，只用分数意义来解释。
  + 一块蛋糕切成 6 等份，吃了 5 份。用真分数表示吃掉的部分，用假分数表示还需要多少份才能再吃完一整块。

  *挑战*

  + 找出所有分母为 5 的真分数（分子为正整数）。这样的分数有几个？再找分母为 5 的最小假分数是哪个？

  _假分数还可以写成整数加上一个真分数的形式——比如 $7/4$ 能写成 $1$ 又 $3/4$。这种写法叫带分数，下一节我们就来研究它。_

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_
  + 真分数：$3/5$，$2/11$；假分数：$7/4$，$9/9$，$11/10$。
  + $3/7$，真分数（$3 < 7$）。
  + $8/5 > 1$（假分数）；$5/8 < 1$（真分数）。
  + 分母相同，分子越大取的份数越多，$5/8 > 3/8$，小明走得更多。
  + 吃掉 $5/6$（真分数）；再需要 $1/6$ 才能再吃完一整块，$1/6$ 是真分数；也可以说"当前吃了 $5/6$，还差 $1/6$ 凑成一整块"，$7/6$ 是假分数表示已吃和即将吃的总量。
  + 分母为 5 的真分数：$1/5, 2/5, 3/5, 4/5$，共 4 个。最小的假分数是 $5/5$（等于 1）。
]
