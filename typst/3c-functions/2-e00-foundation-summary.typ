#import "../lib/theme-v2.typ": blueprint, mastery, side-hack, secref

== §8.3 函数基础总结：先把两类基础模型看稳 <meta:e-foundation-summary>

#blueprint[
  *基础段速查卡*

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *一次函数*（#secref("e01-function-concept")）
      - 固定增加量，对应直线模型
      - 读式子时先看“每步加多少”和“起点在哪里”
      - 表格、公式、图象、情境要能互相翻译
    ],
    block[
      *反比例函数*（#secref("e04-inverse-proportion")）
      - 固定乘积，对应双曲线模型
      - 读情境时先问“是不是一个快、一个慢”
      - 先抓联动关系，再谈象限和图象走势
    ],
  )
]

#side-hack[
  *基础半程先站稳*：

  到这里，函数路线的基础半程先把“固定增量”和“固定乘积”两种经典变化模型站稳了。先把这两种读法收好：看到数据先看怎么变，看到图象先看走势，看到情境先问两个量是不是在一起稳定联动。
]

#mastery[
  *过桥自检*

  + 面对一个表格，你会先看“差”“商”还是“积”？请说出这三种观察各自最容易指向哪类模型。

  + 为什么函数路线在基础半程里要反复练“情境—表格—公式—图象”四种表示互译，而不是只背解析式？

  + 请用一句话说明：#secref("e01-function-concept") 和 #secref("e04-inverse-proportion") 虽然图象不同，但都在训练你读“两个量怎样一起变化”。

  + 拿到一个新情境时，你会怎样先判断它更像“固定增量”还是“固定乘积”？请说出你最先检查的一个信号。
]
