#import "../lib/theme-v2.typ": blueprint, mastery, secref

== 第八章回顾：函数模型

#blueprint[
  *知识速查卡*

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *模型识别*
      - 稳定比值：$y = k x$（#secref("e01-function-concept")）
      - 固定增量：$y = k x + b$（#secref("e01-function-concept")）
      - 固定乘积：$y = frac(k, x)$（#secref("e04-inverse-proportion")）
      - 最值与对称：$y = a x^2 + b x + c$（#secref("e05-quadratic-function")）
    ],
    block[
      *四种表示互译*
      - 情境 $arrow.r$ 表格：先找“怎么一起变”
      - 表格 $arrow.r$ 公式：看差、看商、看积，以及是否在连续按比例变化
      - 公式 $arrow.r$ 图象：直线 / 双曲线 / 抛物线
      - 图象 $arrow.r$ 情境：把走势翻回“固定增加、固定乘积、最值”，并比较增长率应用里的不同写法
    ],
  )

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *参数怎样读*
      - 一次函数里，$k$ 管每步变多少，$b$ 管起点
      - 反比例函数里，$k$ 管固定乘积
      - 二次函数里，$a$ 管开口方向与弯曲程度
      - 顶点式 $y = a(x - h)^2 + k$ 直接给顶点 $(h, k)$
    ],
    block[
      *回看旧工具*
      - 比例意识来自 #secref("a44-ratio")
      - 坐标读图来自 #secref("b36-coordinate-system")
      - 字母模板来自 #secref("c01-variable")
      - 列式 / 列方程意识来自 #secref("c04-equation")
      - 函数不是另起炉灶，而是把这些工具重新拧成一条变化语言
    ],
  )
]

#mastery[
  *混合自测题*（不标知识点，综合调用）

  + 判断下列关系分别更像哪一种模型：$y = 4 x$、$y = 4 x + 7$、$y = frac(36, x)$、$y = -x^2 + 6 x$、“每次都在上一次结果上再乘 1.03”。

  + 已知一次函数过点 $(0, 5)$ 和 $(3, 11)$，求解析式。

  + 某长方形面积固定为 60。设长为 $x$，宽为 $y$，写出函数关系式，并求 $x = 12$ 时的 $y$。

  + 已知二次函数 $y = (x - 4)^2 + 4$。写出顶点、对称轴、最小值。

  + 某水箱原有 50 升水，每分钟流入 4 升。写出函数关系式，并求 15 分钟后的水量。

  + 下表记录某停车场的收费规则：

    #align(center, table(
      columns: 5,
      align: center,
      [停车时长 $x$（小时）], [$0$], [$1$], [$2$], [$3$],
      [费用 $y$（元）], [$2$], [$6$], [$10$], [$14$],
    ))

    判断它更像哪一种模型，并写出函数关系式。

  + 某账户初始金额为 3000 元，之后每年按上一年余额增长 $4%$。求 1 年后和 2 年后的金额，并写出你使用的“下一年怎样由上一年得到”的规则。

  + 一条抛物线与 $x$ 轴交于两点。把这件事翻成情境语言时，常常意味着什么？

  + 已知反比例函数图象经过点 $(5, -2)$。求解析式，并判断图象所在象限。

  + 下列哪个情境更适合用一次函数，哪个更适合用“连续按比例变化”的写法？说明理由：
    - 每月稳定存入 500 元
    - 每月余额都在上月基础上增加 $2%$

  + *进阶*：某商家销售额第一月为 100 万元。方案 A 以后每月增加 8 万元；方案 B 以后每月在上月基础上增长 $6%$。不用精确求很远的月份，只解释：开始时为什么可能感觉差不多，后面为什么会拉开？

  + *进阶*：某班男生 : 女生 $= 3 : 5$。设女生人数为 $x$，男生人数为 $y$。写出 $y$ 与 $x$ 的函数关系式；如果全班共 40 人，求 $x$ 和 $y$。
]

#line(length: 100%, stroke: 0.3pt + luma(200))
*参考答案*

  + 依次是：正比例函数、一次函数、反比例函数、二次函数、连续按比例变化的增长模型。

  + 由 $(0, 5)$ 得 $b = 5$。再代入 $(3, 11)$：$11 = 3k + 5$，得 $k = 2$，所以 $y = 2 x + 5$。

  + 面积固定说明 $x y = 60$，所以 $y = frac(60, x)$。当 $x = 12$ 时，$y = 5$。

  + 顶点 $(4, 4)$，对称轴 $x = 4$，最小值为 4。

  + $y = 50 + 4 x$。15 分钟后 $y = 110$（升）。

  + 费用每增加 1 小时就稳定增加 4 元，所以它更像一次函数。又因为 $x = 0$ 时 $y = 2$，所以关系式是 $y = 4 x + 2$。

  + 1 年后金额：$3000 times 1.04 = 3120$（元）；2 年后金额：$3120 times 1.04 = 3244.8$（元）。使用的规则是：下一年金额 = 上一年金额 $times 1.04$。

  + 交点满足 $y = 0$，所以常表示“这个量在两个时刻或两个位置变成 0”，例如高度落地、利润归零、面积消失等。

  + 反比例函数里先抓固定乘积：$k = 5 times (-2) = -10$，所以 $y = frac(-10, x)$。$k < 0$，图象在第二、四象限。

  + “每月稳定存入 500 元”适合一次函数，因为每期固定增加量；“每月余额都在上月基础上增加 $2%$”适合“连续按比例变化”的写法，因为每期都在乘同一个倍率。

  + 方案 A 每期都加 8，方案 B 每期都乘 1.06。前期总量还不大时，两者增加量可能接近；后期随着基数变大，方案 B 每期增加得越来越多，所以会逐渐拉开。

  + 由男生 : 女生 $= 3 : 5$，得 $y = frac(3, 5) x$。又因为 $x + y = 40$，所以 $x + frac(3, 5) x = 40$，解得 $x = 25$，$y = 15$。

*螺旋复习（与前面主干的联系）*

  + 回看 #secref("c04-equation")：一支笔 6 元，另收 4 元运费。设买 $x$ 支共花 $y$ 元，写出 $y$ 与 $x$ 的关系式，并求买 5 支时的总价。

  + *【作图型】* 在坐标纸上（#secref("b36-coordinate-system")），画出 $y = 2x - 3$ 的图象：先令 $x = 0, 1, 2$ 算出对应的 $y$，在坐标系里描点，连成直线，并标出纵截距。再用 #secref("c04-equation") 的平衡法：解 $2x - 3 = 5$，并在图上找到对应的点。

  + *【比例+函数综合】* 某配比溶液，浓缩液与水之比为 $1 : 4$（#secref("a44-ratio")）。若配好溶液总量为 $x$ 升，则浓缩液 $y$ 升与 $x$ 的关系是什么？
    - 写出 $y$ 与 $x$ 的函数关系式，说明这是什么类型的函数（#secref("e01-function-concept")）
    - 若总量为 $10$ 升，需要多少升浓缩液？
    - 图象是否过原点？解释为什么（联系 #secref("a44-ratio") 的比值不变性）

_参考答案（螺旋复习第 2、3 题）：_

第 2 题：$x=0$ 时 $y=-3$，$x=1$ 时 $y=-1$，$x=2$ 时 $y=1$；描点 $(0,-3)$、$(1,-1)$、$(2,1)$，纵截距 $-3$；解 $2x-3=5$，$2x=8$，$x=4$，对应点 $(4,5)$ 在直线上。

第 3 题：浓缩液占总量的 $frac(1, 1+4) = frac(1, 5)$，故 $y = frac(1, 5) x$，这是正比例函数（#secref("e01-function-concept")）；$y = frac(1, 5) times 10 = 2$ 升；因为"总量为 0 时浓缩液也为 0"，图象过原点，对应比例关系的恒常性（#secref("a44-ratio")）。
