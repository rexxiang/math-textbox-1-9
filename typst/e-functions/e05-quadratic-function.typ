#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall
#import "../lib/diagram-packages.typ": cetz

== §e05 二次函数：弯曲变化与最值模型 <tool:e05-quadratic-function>

#vocab[二次函数 quadratic function / 顶点 vertex / 对称轴 axis of symmetry / 抛物线 parabola]

#crisis[
  喷泉水柱从喷口射出后，先升高，再落下。某次测量得到水柱高度 $y$（米）与水平距离 $x$（米）的关系大致是：

  #align(center, table(
    columns: 6,
    align: center,
    [$x$], [$0$], [$1$], [$2$], [$3$], [$4$],
    [$y$], [$0$], [$1.5$], [$2$], [$1.5$], [$0$],
  ))

  这时最关心的问题不再是“代一个 $x$ 算一个 $y$”，而是：

  - 最高点在哪里？
  - 最高能到多高？
  - 水柱什么时候落回地面？

  一次函数只能描述稳定增减，显然不够。我们需要一种能表达“变化本身也在改变”的模型。
]

#history-note[
  伽利略研究抛体运动时发现：忽略空气阻力后，很多飞出去再落回来的轨迹都像同一种左右对称的弯曲线路。人们后来才把这种轨迹系统地写成公式，用来描述“先快后慢、先升后降、先减后增”这类现象。
]

#discovery[
  先只盯住这张表格本身，不急着写公式。

  这里最显眼的现象是：

  - $x = 1$ 和 $x = 3$ 时，$y$ 一样高
  - $x = 0$ 和 $x = 4$ 时，都回到地面
  - 中间的 $x = 2$ 给出了最高点

  如果把这些点画到坐标图上，再平滑地连起来，就会看到：这不是直线，而是一条关于 $x = 2$ 左右对称的弯曲轨迹。

  #figure(
    cetz.canvas(length: 1.0cm, {
      import cetz.draw: *
      // axes
      line((-0.5, 0), (5.0, 0), stroke: 0.7pt, mark: (end: ">", size: 0.15))
      line((0, -0.5), (0, 3.0), stroke: 0.7pt, mark: (end: ">", size: 0.15))
      content((5.0, 0), $x$, anchor: "west", padding: 3pt)
      content((0, 3.0), $y$, anchor: "south", padding: 3pt)
      content((0, 0), $O$, anchor: "north-east", padding: 3pt)
      // axis of symmetry (dashed)
      line((2, 0), (2, 2.3), stroke: (paint: gray, thickness: 0.6pt, dash: "dashed"))
      content((2, -0.35), text(8pt)[$x{=}2$])
      // data points from table: (0,0),(1,1.5),(2,2),(3,1.5),(4,0)
      let pts = ((0,0), (1,1.5), (2,2.0), (3,1.5), (4,0))
      for p in pts {
        circle(p, radius: 0.07, fill: black, stroke: none)
      }
      // parabola curve y = -0.5(x-2)^2 + 2 sampled at many points
      bezier((0,0), (2,2), (0.8, 2.2), (3.2, 2.2))
      bezier((2,2), (4,0), (3.2, 2.2), (4.0, 0.5))
      // vertex label
      content((2, 2.0), text(8pt)[顶点$(2,2)$], anchor: "south", padding: 4pt)
      // x axis ticks
      for xi in (1, 2, 3, 4) {
        content((xi, -0.3), text(7pt)[#xi])
      }
      // y axis ticks
      for yi in (1, 2) {
        content((-0.3, yi), text(7pt)[#yi])
      }
    }),
    caption: [表格中五个数据点连成关于 $x = 2$ 对称的弯曲轨迹（抛物线）]
  )

  所以在研究这类关系时，我们会优先抓住三件事：

  - 哪个位置最高或最低
  - 图象围绕哪条竖线左右对称
  - 什么时候回到 $y = 0$
]

#tryit[
  先自己练一遍：

  观察下表：

  #align(center, table(
    columns: 6,
    align: center,
    [$x$], [$1$], [$2$], [$3$], [$4$], [$5$],
    [$y$], [$5$], [$2$], [$1$], [$2$], [$5$],
  ))

  写出：

  + 最低点坐标
  + 中间那条左右对称的竖线
  + 最低高度
]

#side-hack[
  看二次函数时，优先找三件事：

  - *开口朝上还是朝下*——先看图象整体是在“托住”还是“压下”
  - *最高点或最低点在哪里*——最值往往就在这里
  - *与 $x$ 轴交不交、交几次*——这决定情境里“归零”或“落地”发生没有
]

#blueprint[
  *一、什么是二次函数？*

  形如

  $ y = a x^2 + b x + c quad (a != 0) $

  的函数叫*二次函数*。它的图象是一条抛物线。

  和一次函数相比，二次函数描述的是“变化率还在变”的情境：每前进一步，增加量本身也会改变。

  *二、用顶点式读出最高点、最低点与对称线*

  把二次函数写成

  $ y = a (x - h)^2 + k $

  叫作*顶点式*。它会直接告诉我们：

  - 顶点是 $(h, k)$
  - 对称轴是 $x = h$
  - 若 $a > 0$，顶点给出最小值
  - 若 $a < 0$，顶点给出最大值

  *三、从图象回到情境*

  从前面的表格观察走到正式模型时，可以把注意力压缩成三条最值得先读的信息。二次函数图象有三条常见信息：

  - *开口方向*：$a > 0$ 向上，$a < 0$ 向下
  - *顶点位置*：决定最高点或最低点
  - *与 $x$ 轴的交点*：表示函数值回到 $0$ 的位置，可以从图象、表格或情境中读出来

  这一节先把重点放在”会读图、会找顶点、会解释最值”。至于交点，这里先把它当成图象上的”归零位置”来理解：够用来读情境，但还不把这一节改成精确解方程训练。

  #figure(
    cetz.canvas(length: 0.85cm, {
      import cetz.draw: *
      // axes for left figure (a>0)
      line((-0.3, 0), (4.3, 0), stroke: 0.7pt, mark: (end: “>”, size: 0.12))
      line((2, -0.5), (2, 3.5), stroke: 0.7pt, mark: (end: “>”, size: 0.12))
      content((4.3, 0), $x$, anchor: "west", padding: 2pt)
      content((2, 3.5), $y$, anchor: "south", padding: 2pt)
      // parabola opening up: y = (x-2)^2 + 1
      bezier((0.0, 5.0), (2, 1.0), (0.8, 1.5), (3.2, 1.5))
      bezier((2, 1.0), (4.0, 5.0), (3.2, 1.5), (4.0, 4.0))
      // vertex (2,1)
      circle((2, 1), radius: 0.07, fill: blue, stroke: none)
      content((2, 1), text(7pt, fill: blue)[$(h,k)$], anchor: "north", padding: 4pt)
      // symmetry axis
      line((2, 0), (2, 1), stroke: (paint: gray, thickness: 0.5pt, dash: "dashed"))
      content((2, -0.35), text(7pt)[$x{=}h$])
      // label: a>0
      content((0.5, 3.0), text(8pt, fill: blue)[$a > 0$])
      content((0.5, 2.3), text(7pt)[开口向上])
      // O label
      content((2, 0), $O$, anchor: "north-east", padding: 2pt)
    }),
    caption: [$a > 0$ 时抛物线开口向上，顶点 $(h, k)$ 是最低点；对称轴为 $x = h$]
  )

  例 1：喷泉模型

  $ y = -0.5 (x - 2)^2 + 2 $

  所以顶点为 $(2, 2)$，最高点在距喷口 2 米处，高度为 2 米。

  再结合上面的表格可见：$x = 0$ 和 $x = 4$ 时高度都为 $0$，说明水柱在 4 米处落地。

  再把同样的读图思路换到另一个情境里看，就能发现“顶点 = 最值”并不只出现在喷泉这类轨迹里。

  例 2：周长固定为 20 的长方形，设长为 $x$，面积为 $y$。

  宽是 $10 - x$，所以

  $ y = x (10 - x) = -x^2 + 10 x $

  做一个表格看看：

  #align(center, table(
    columns: 6,
    align: center,
    [$x$], [$2$], [$3$], [$4$], [$5$], [$6$],
    [$y$], [$16$], [$21$], [$24$], [$25$], [$24$],
  ))

  面积在 $x = 5$ 左右呈对称变化，并在 $x = 5$ 处达到最大值 $25$。也就是说：周长固定时，正方形面积最大。
]

#pitfall[
  *陷阱 1：只看开口，不看顶点。*

  看到“开口向下”，只能知道它有最大值；真正的最高点还要看顶点位置。

  *陷阱 2：把顶点横坐标的符号看反。*

  看到 $(x + 1)^2$，顶点横坐标不是 $1$，而是 $-1$。因为

  $ (x + 1)^2 = (x - (-1))^2 $
]

#mastery[
  *基础*

  + 判断下列函数是否为二次函数：
    - $y = 2 x^2 - 3 x + 1$
    - $y = 4 x + 7$
    - $y = -x^2$
    - $y = frac(6, x)$

  + 直接观察 $y = (x - 2)^2 + 3$，写出顶点、对称轴、最小值。

  + 直接观察 $y = -2 (x + 1)^2 + 5$，写出顶点和最大值。

  + 把 $y = x^2 + 6x + 5$ 化为顶点式，并指出抛物线的顶点坐标和对称轴方程。

    （提示：$y = x^2 + 6x + 5 = (x^2 + 6x + 9) - 9 + 5 = ?$）

  *应用*

  + 某球被竖直抛起，高度模型为 $h = -5 (t - 2)^2 + 21$。求球的最大高度及对应时间。

  + 某矩形花坛周长固定为 32 米。设一边长为 $x$，面积为 $y = x (16 - x)$。计算 $x = 6, 7, 8, 9, 10$ 时的面积，并据此判断最大面积。

  *挑战*

  + 已知某条抛物线过点 $(1, 5)$ 和 $(3, 5)$（两点纵坐标相同），且顶点纵坐标为 $2$。写出这条抛物线的函数表达式。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*

  + 第一、第三个是二次函数；第二个不是；第四个不是。

  + 顶点 $(2, 3)$，对称轴 $x = 2$，最小值为 3。

  + 顶点 $(-1, 5)$，最大值为 5。

  + $y = (x^2 + 6x + 9) - 9 + 5 = (x + 3)^2 - 4$，顶点为 $(-3, -4)$，对称轴 $x = -3$。

  *应用*

  + 最大高度 21，在 $t = 2$ 秒时取得。

  + 面积分别为：$6 times 10 = 60$，$7 times 9 = 63$，$8 times 8 = 64$，$9 times 7 = 63$，$10 times 6 = 60$。所以最大面积为 64 平方米，在 $x = 8$ 时取得。

  *挑战*

  + 两点 $(1, 5)$ 和 $(3, 5)$ 纵坐标相同，说明它们关于对称轴对称，对称轴为 $x = frac(1+3, 2) = 2$。用顶点式写出 $y = a(x-2)^2 + k$，其中 $k = 2$（顶点纵坐标）。再代入 $(1, 5)$：$5 = a(1-2)^2 + 2 = a + 2$，解得 $a = 3$。所以函数表达式为 $y = 3(x-2)^2 + 2$。
]
