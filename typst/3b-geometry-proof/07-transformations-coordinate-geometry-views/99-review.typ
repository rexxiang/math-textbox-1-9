#import "../../lib/theme-v2.typ": blueprint, mastery, side-hack, secref

== 本章回顾 <meta:transformations-coordinate-geometry-views>

#blueprint[
  *知识速查卡*

  #table(
    columns: (1fr, 1fr),
    inset: 6pt,
    stroke: 0.4pt,
    [*主题*], [*一句话小结*],
    [刚体变换族], [平移 / 旋转 / 反射都是保距变换 $=>$ 全等。],
    [平移], [一个向量决定；对应点连线互相平行且等长。],
    [旋转], [中心 $+$ 角度决定；对应点到中心等距。],
    [反射], [对称轴决定；对应点关于轴对称。],
    [距离公式], [$P Q = sqrt((x_2 - x_1)^2 + (y_2 - y_1)^2)$。],
    [中点公式], [$M = ((x_1 + x_2) slash 2, (y_1 + y_2) slash 2)$。],
    [平移坐标], [$(x, y) -> (x + a, y + b)$。],
    [关于 $x$-轴反射], [$(x, y) -> (x, -y)$。],
    [关于 $y$-轴反射], [$(x, y) -> (-x, y)$。],
    [原点旋转 $90 degree$ 逆时针], [$(x, y) -> (-y, x)$。],
    [原点旋转 $180 degree$], [$(x, y) -> (-x, -y)$。],
    [三视图], [正视（长 $times$ 高）、侧视（宽 $times$ 高）、俯视（长 $times$ 宽）。],
    [长方体表面积], [$= 2 times$ 三视图面积之和（*仅对长方体*；凹凸 / 空腔物体要逐面计数）。],
  )
]

#mastery[
  *混合自测*

  + 把 $triangle A B C$（$A(0, 0)$、$B(2, 0)$、$C(1, 2)$）按平移向量 $(3, -1)$ 平移，写出像三角形坐标。
  + $A(-1, 4)$、$B(5, -4)$。求 $A B$ 与中点。
  + $(3, -2)$ 关于*原点旋转 $180 degree$* 后的像是什么？
  + 判断“先关于 $x$-轴反射，再关于 $y$-轴反射”是否等同于“关于原点旋转 $180 degree$”。用坐标公式验证。
  + 长方体尺寸 $= 8 times 6 times 4$（长 $times$ 宽 $times$ 高）。求三视图面积与表面积。
  + $(2, 3)$ 关于直线 $y = x$ 反射，像是什么？再将此像按向量 $(1, -4)$ 平移，最终坐标是什么？
  + 三视图都是边长 $= 5$ 的正方形，最可能的立体是什么？尺寸多少？
  + $A(1, 1)$、$B(4, 1)$、$C(4, 5)$、$D(1, 5)$。判断四边形 $A B C D$ 是什么特殊四边形，并求它的面积与两条对角线长。
  // 跨章回顾
  + 用 #secref("cb04-rectangular-system") 的坐标系基础，在坐标平面上画出 $A(1,2), B(4,2), C(4,6)$，然后说明 $triangle A B C$ 是什么类型的三角形（用两点距离公式验证）。
  // 跨章回顾
  + 刚体变换保持全等（#secref("ge01-congruence-correspondence")）。把 $triangle A B C$ 沿向量 $(3, -2)$ 平移后得到 $triangle A' B' C'$，说明为什么 $triangle A B C tilde.eq triangle A' B' C'$。
  + 一个公园的入口在坐标原点 $O(0,0)$，喷泉在 $A(6, 0)$，凉亭在 $B(3, 4)$。公园想在 $x$ 轴上找一个点 $P$ 建垃圾桶，使 $P$ 到喷泉和凉亭的距离之和最小。设 $P = (t, 0)$，写出 $P A + P B$ 的表达式，并说明如何利用对称（关于 $x$ 轴的轴对称）把这个问题转化为"两点间直线最短"。
  // 跨章回顾
  + 给定点 $A(-1, 2)$（坐标系回顾 #secref("cb04-rectangular-system")）。先把 $A$ 关于 $y$ 轴反射得到 $A'$，再把 $A'$ 关于直线 $y = x$ 反射得到 $A''$（本章 #secref("ge05-rigid-transforms") 反射变换）。写出 $A'$ 与 $A''$ 的坐标，并判断 $A$、$A'$、$A''$ 三点是否共线（提示：可比较 $A A'$ 与 $A' A''$ 两段线段的方向 / 斜率）。
]

==== 参考答案

+ $A'(3, -1)$、$B'(5, -1)$、$C'(4, 1)$。
+ $A B = sqrt((5 - (-1))^2 + (-4 - 4)^2) = sqrt(36 + 64) = 10$；中点 $= (2, 0)$。
+ $(3, -2) -> (-3, 2)$。
+ 关于 $x$-轴：$(x, y) -> (x, -y)$；再关于 $y$-轴：$(x, -y) -> (-x, -y)$。与原点旋转 $180 degree$ 的像 $(-x, -y)$ 一致 $=>$ 等同。
+ 三视图面积：$8 times 4 = 32$、$6 times 4 = 24$、$8 times 6 = 48$；和 $= 104$；表面积 $= 2 times 104 = 208$。
+ 关于 $y = x$ 反射：$(2, 3) -> (3, 2)$；平移 $(1, -4)$：$-> (4, -2)$。
+ 棱长 $= 5$ 的正方体。
+ 矩形 $A B$ $= 3$、$B C$ $= 4$，且对角线 $= sqrt(9 + 16) = 5$，两条对角线相等、互相平分（由矩形定义）；面积 $= 12$。
+ $A B = sqrt((4-1)^2 + (2-2)^2) = 3$，$B C = sqrt((4-4)^2 + (6-2)^2) = 4$，$A C = sqrt((4-1)^2 + (6-2)^2) = sqrt(9 + 16) = 5$。因为 $3^2 + 4^2 = 5^2$，所以 $triangle A B C$ 是直角三角形，$angle B = 90 degree$。
+ 平移是刚体变换（保距变换），每条边在平移前后长度不变、每对夹角不变；三对边与三对角全部对应相等，满足全等定义，所以 $triangle A B C tilde.eq triangle A' B' C'$。
+ $P A = |t - 6|$，$P B = sqrt((t - 3)^2 + 16)$。把 $B$ 关于 $x$ 轴对称得 $B'(3, -4)$，则 $P B = P B'$。$P A + P B = P A + P B' >= A B' = sqrt((6-3)^2 + (0-(-4))^2) = sqrt(9+16) = 5$（两点距离公式）。等号在 $P$ 落在 $A B'$ 连线上时取到。$A B'$ 直线方程：$y = (-4 slash (3-6))(x - 6) = (4 slash 3)(x - 6)$。令 $y = 0$：$x = 6$，即 $P = (6, 0) = A$。最小距离和为 $5$。
+ $A' = (1, 2)$（关于 $y$ 轴反射：$x$ 取反）；$A'' = (2, 1)$（关于 $y = x$ 反射：交换 $x, y$）。$A A'$ 由 $(-1, 2)$ 到 $(1, 2)$ 是水平段（方向 $(2, 0)$）；$A' A''$ 由 $(1, 2)$ 到 $(2, 1)$ 是斜向下方向 $(1, -1)$。两段方向不平行，三点*不共线*。（参见 #secref("ge05-rigid-transforms") 与 #secref("cb04-rectangular-system")）

#blueprint[
  *巩固复习*

  - 回看 #secref("ge01-congruence-correspondence")：刚体变换保距 $=>$ 变换后和变换前全等。这是本章的一条“再解释”而不是新事实。
  - 回看 #secref("ge04-pythagorean")：两点距离公式是勾股定理的直接坐标化。
  - 回看 #secref("cb04-rectangular-system")：坐标系提供了“点的名字”，使得上面所有公式可以落地。
  - 衔接 3c 域函数：直线 $y = k x + b$ 整体向上平移 $c$ 得 $y = k x + (b + c)$——图像的平移和本章的坐标平移是同一件事。
]

#side-hack[
  *☞ 高中桥 / 继续往前*

  这只是向前看的预告，不作为后续主线先修。

  _以下是预告，现在看不懂完全正常 — 等到高中再回来。_

  - *向量*：高中把“从 $P$ 到 $Q$ 的位移”正式命名为向量 $accent(P Q, arrow)$；平移不过是对所有点加同一向量，旋转、反射则用矩阵统一表示——本章的几条坐标公式在高中阶段会写成矩阵乘法。
  - *解析几何*：直线、圆、圆锥曲线在坐标系里被写成方程，几何问题就变成代数问题。本章的中点、距离公式会反复出现。
  - *三视图*到*工程制图*与*计算机图形学*：再往前走是透视变换、投影矩阵、三维建模。
]
