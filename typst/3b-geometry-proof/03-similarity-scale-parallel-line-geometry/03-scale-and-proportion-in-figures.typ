#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref
#import "../../lib/diagram-packages.typ": cetz

=== 放缩规律、比例尺与测量 <tool:ge02-scale-and-measurement>

#vocab[周长比 perimeter ratio][面积比 area ratio][比例尺 scale][相似测量 similar-triangle measurement]

#crisis[
  相似三角形不只是一对“长得像”的图形——它是一整套*放缩规律*。图放大 $3$ 倍，对应的边也放大 $3$ 倍，这是直觉；可是面积呢？面积变成了 $9$ 倍，不是 $3$ 倍。这件事如果没有在一开始就说清楚，所有涉及“面积比”的题都会做错。

  *相似比 $k$ 到底管哪些量？长度、周长、面积、体积各按几次方放缩？为什么面积是 $k^2$ 而不是 $k$？把这套规律搬到日常，比例尺和“测高测距”就有了数学上的依据。*
]

#history-note[
  “比例尺” 1 : 1000 是古代地图和建筑图的核心语言；它说的就是“图上 $1$ 厘米代表实际 $1000$ 厘米”。这一条看似平凡的习惯，其实就是我们这节的核心：长度按 $k$ 放缩，这意味着面积按 $k^2$ 放缩——所以同一张比例尺 $1 : 100$ 的地图上，面积倍率是 $10000$ 倍，不是 $100$ 倍。许多规划报告里“换算面积”出错的起点就在这里。
]

#discovery[
  *长度跟 $k$ 走；面积跟 $k^2$ 走*

  设 $triangle A B C tilde triangle D E F$，相似比为 $k$（即 $D E = k dot A B$，余同）。那么：

  $ frac("周长"(triangle D E F), "周长"(triangle A B C)) = frac(D E + E F + F D, A B + B C + C A) = frac(k(A B + B C + C A), A B + B C + C A) = k. $

  *周长比 $= k$*，直接延自“边比 $= k$”。

  面积就不一样了。想象一个最简单的例子：正方形边长变成 $k$ 倍，面积从 $s^2$ 变成 $(k s)^2 = k^2 s^2$。一般三角形也一样——若底与高都按 $k$ 放缩，面积 $= 1 slash 2 dot ("底") dot ("高")$ 就要乘 $k dot k = k^2$。结论：

  $ frac("面积"(triangle D E F), "面积"(triangle A B C)) = k^2. $

  #figure(
    cetz.canvas(length: 1cm, {
      import cetz.draw: *
      // Small triangle
      let A = (0, 0)
      let B = (2, 0)
      let C = (0.8, 1.5)
      line(A, B, C, close: true, stroke: 0.7pt)
      content((1, -0.35), text(8pt)[$a$])
      content((1.6, 0.9), text(8pt)[$b$])
      content((0.15, 0.85), text(8pt)[$c$])
      // Large triangle (scaled by 1.5)
      let k = 1.5
      let D = (4, 0)
      let E = (4 + 2 * k, 0)
      let F = (4 + 0.8 * k, 1.5 * k)
      line(D, E, F, close: true, stroke: 0.7pt + rgb("#1976D2"))
      content(((D.at(0) + E.at(0)) / 2, -0.35), text(8pt, fill: rgb("#1976D2"))[$k a$])
      content(((E.at(0) + F.at(0)) / 2 + 0.2, (E.at(1) + F.at(1)) / 2 + 0.1), text(8pt, fill: rgb("#1976D2"))[$k b$])
      content(((D.at(0) + F.at(0)) / 2 - 0.3, (D.at(1) + F.at(1)) / 2 + 0.1), text(8pt, fill: rgb("#1976D2"))[$k c$])
      // Labels
      content((1, -0.8), text(8pt)[面积 $S$])
      content(((D.at(0) + E.at(0)) / 2, -0.8), text(8pt, fill: rgb("#1976D2"))[面积 $k^2 S$])
    }),
    caption: [边长按 $k$ 放缩，面积按 $k^2$ 放缩——一维乘 $k$，二维乘 $k^2$。]
  )

  更广地讲：

  $ cases("长度量（边、周长、中线、高、对角线）比例" = k,
          "面积比例" = k^2,
          "体积比例（相似立体图形）" = k^3). $

  “一维按 $k$、二维按 $k^2$、三维按 $k^3$”是整个相似放缩的核心口诀。

  *比例尺：把放缩搬到纸面*

  地图、工程图上的*比例尺*（如 $1 : 50000$）就是一条人为选定的相似比。

  - 图上距离 $ell_{图}$，实际距离 $ell_{实}$：$ell_{实} = k times ell_{图}$，其中 $k = 50000$。
  - 图上面积 $A_{图}$，实际面积 $A_{实}$：$A_{实} = k^2 times A_{图}$。

  *例*：某比例尺为 $1 : 25000$ 的地图上，一块长方形林地图示长 $4$ 厘米、宽 $3$ 厘米。实际面积多少平方千米？

  解：$k = 25000$。实际长 $= 4 times 25000 = 100000$ 厘米 $= 1000$ 米；宽 $= 3 times 25000 = 75000$ 厘米 $= 750$ 米。实际面积 $= 1000 times 750 = 750000$ 平方米 $= 0.75$ 平方千米。

  （校对：$A_{图} = 12$ 平方厘米；$A_{实} = 12 times 25000^2 = 12 times 6.25 times 10^8 = 7.5 times 10^9$ 平方厘米 $= 7.5 times 10^5$ 平方米 $= 0.75$ 平方千米，与上式吻合。）

  *实地测量：借相似三角形“量不到的量”*

  现实里常遇到“能量 $A$，但量不到 $B$”的窘况。相似三角形是解围工具。

  *场景一：测旗杆高（影长法）*

  同一时刻太阳光近似平行 $=>$ 所有直立物体的“物体 $+$ 影子”两个直角三角形*彼此相似*（共用太阳角）。

  若已知标杆高 $h_0 = 1.5$ 米、其影长 $ell_0 = 2$ 米；旗杆影长 $L = 16$ 米。则

  $ frac(h, h_0) = frac(L, ell_0) => h = 1.5 times 16 slash 2 = 12 " 米". $

  #figure(
    cetz.canvas(length: 1cm, {
      import cetz.draw: *
      // Ground line
      line((-0.5, 0), (9, 0), stroke: 0.4pt)
      // Stick (short)
      line((1, 0), (1, 1.2), stroke: 0.7pt)
      content((1, 1.45), text(8pt)[$h_0$])
      // Stick shadow
      line((1, 0), (2.6, 0), stroke: 0.7pt + rgb("#388E3C"))
      content((1.8, -0.3), text(8pt, fill: rgb("#388E3C"))[$ell_0$])
      // Sun ray to stick top
      line((2.6, 0), (1, 1.2), stroke: 0.4pt + luma(140))
      // Flagpole (tall)
      line((6, 0), (6, 4.8), stroke: 0.7pt)
      content((6, 5.1), text(8pt)[$h = ?$])
      // Flagpole shadow
      line((6, 0), (8.4, 0), stroke: 0.7pt + rgb("#388E3C"))
      // Dotted shadow extension
      content((7.2, -0.3), text(8pt, fill: rgb("#388E3C"))[$L$])
      // Sun ray to flagpole top
      line((8.4, 0), (6, 4.8), stroke: 0.4pt + luma(140))
      // Parallel sun ray indicators
      content((1.5, 0.9), text(7pt, fill: luma(120))[$alpha$])
      content((6.8, 3.5), text(7pt, fill: luma(120))[$alpha$])
    }),
    caption: [影长法：同一时刻太阳光平行 $arrow.r.double$ 标杆与旗杆的"高 $slash$ 影长"比相等。]
  )

  *场景二：测河宽（构造相似）*

  若要测不可跨越的一段距离 $A B$，可在同一岸上作一对相似三角形（已知角度和一对对应边），由比例关系反算 $A B$。这就是初中阶段常见的“相似测距”思路。

  *场景三：缩小模型 / 放大样图*

  产品设计图、建筑模型也是相似的例子。模型长度是实体的 $1 slash 50$，则模型表面积是实体的 $1 slash 2500$，体积是 $1 slash 125000$。这在估算材料、重量时一脚踩空就会错上万倍。
]

#side-hack[
  碰到“相似”加“面积 / 体积”，先在草稿纸写下口诀：

  $ "长度比" = k, quad "周长比" = k, quad "面积比" = k^2, quad "体积比" = k^3. $

  再按题目要的是哪一种放缩关系代入。*99 % 的面积错题都是直接把 $k^2$ 漏成 $k$ 引起的。*
]

#tryit[
  先动手算：

  + 两相似三角形边长比 $2 : 3$。它们的周长比、面积比各是多少？
  + 比例尺 $1 : 10000$ 的地图上，图示面积 $5$ 平方厘米的一块地，实际面积多少平方米？
  + 标杆高 $1.2$ 米、影长 $1.5$ 米；同一时刻树的影长 $9$ 米。求树的高度。
]

#blueprint[
  *工具一：相似放缩口诀*

  若两个相似图形的相似比为 $k$，则：

  - 对应*长度量*（边长、周长、中线长、高线长、对角线、弧长）之比 $= k$；
  - 对应*面积* 之比 $= k^2$；
  - 对应*体积*（若为相似立体）之比 $= k^3$。

  *工具二：比例尺的正反向运用*

  比例尺 $1 : n$ 把图 / 地图与现实挂钩，$k = n$（实际 $=$ 图 $times n$）：

  - *图 $->$ 实际长度*：乘 $n$。
  - *图 $->$ 实际面积*：乘 $n^2$。
  - *实际 $->$ 图长度*：除 $n$。
  - *实际 $->$ 图面积*：除 $n^2$。

  *例 1*：比例尺 $1 : 200$ 的平面图上，一间教室画成 $5$ 厘米 $times$ $3$ 厘米。求实际面积。

  解：图面积 $= 15$ 平方厘米；$k^2 = 200^2 = 40000$；实际面积 $= 15 times 40000 = 600000$ 平方厘米 $= 60$ 平方米。

  *工具三：相似测量（影长法 / 构造相似）*

  *影长法*：在同一时刻，任意两根直立物体与它们的影子都构成相似直角三角形（共角：太阳高度角；共边方向：竖直 $bot$ 水平）。写比例：

  $ frac("物高", "影长") = frac("标杆高", "标杆影长"). $

  *构造相似测距*：若要求不可直接量到的距离 $A B$，在可达区域选点构造包含 $A B$ 的一对相似三角形（通常靠“同位角相等 $+$ 公共角”实现），再由比例计算 $A B$。

  *例 2*：标杆高 $1.8$ 米、影长 $1.2$ 米；旗杆影长 $8$ 米。求旗杆高。

  解：$frac(h, 1.8) = frac(8, 1.2) => h = 12$ 米。

  *例 3*：一块三角形菜地的边长（按某种单位）分别为 $60, 80, 100$ 米，面积 $2400$ 平方米。按 $1 : 2000$ 的比例尺画在图上，*图上面积*多少平方厘米？

  解：图与实际相似比 $k_{图 slash 实} = 1 slash 2000$；面积比 $= 1 slash 4000000$。实际面积 $2400$ 平方米 $= 2400 times 10000 = 2.4 times 10^7$ 平方厘米；图面积 $= 2.4 times 10^7 slash 4 times 10^6 = 6$ 平方厘米。
]

#pitfall[
  *高频错误*

  ✗ 面积比直接抄边比
  → ✓ 边比 $= k$，面积比 $= k^2$。$k = 3$ 时面积倍数是 $9$，不是 $3$。

  ✗ 比例尺读反 / 换算单位错
  → ✓ “$1 : 50000$”表示 $k = 50000$（实际比图大 $50000$ 倍）。单位要统一：图上厘米 $times 50000 =$ 实际厘米，必要时再化为米或千米。

  ✗ “影长法”默认用却忘记前提“同一时刻”
  → ✓ 不同时刻阳光方向不同，两个直角三角形的那一个夹角不同，就不再相似。测量要求“同一时刻”。

  ✗ 构造相似测距时只画图、不写对应角相等
  → ✓ 所有相似都必须明文亮出判据——通常是 AA。不写判据的“相似”在证明题里会被扣分。

  ✗ 把体积比当成 $k^2$
  → ✓ 体积是三维，比例是 $k^3$；建模 / 材料核算时这个错误代价极大。
]

#mastery[
  *基础*

  + 两相似三角形周长比 $4 : 9$。求它们的相似比、面积比。

  + 比例尺 $1 : 20000$ 的地图上，图上 $3$ 厘米 $times$ $2$ 厘米的长方形代表实际多大面积（平方米）？

  + 标杆高 $1.5$ 米、影长 $1.25$ 米；同一时刻旗杆影长 $20$ 米。求旗杆高。

  *应用*

  + 两相似三角形面积比为 $16 : 25$。求边长比与周长比。

  + 一个长方体水箱缩小成 $1 slash 10$ 大小的模型（所有棱长按 $1 slash 10$）。原水箱能装水 $500$ 升，模型能装多少毫升？

  + 河边在甲岸上可量出 $A B = 24$ 米、$A E = 40$ 米，河对岸 $C$ 与 $A$、$B$ 共一条线；测得 $D$ 在 $A E$ 某处、$B D parallel E C$，且 $A D = 15$ 米。求河宽 $B C$。（提示：$triangle A B D tilde triangle A C E$。）

  *挑战 ☞ 选做*

  + 若两个相似三角形面积比为 $9 : 16$，而其中一个三角形面积比另一个大 $35$ 平方米。求两三角形各自面积。

  + 某工程的 $1 : 500$ 施工图上，一块三角形地基画成底 $6$ 厘米、高 $4$ 厘米。求*实际面积*（平方米）；若另外一张设计图把同一地基画成比例尺 $1 : 200$，图上面积多少平方厘米？（验证两次算得到的实际面积相同。）

  + 为什么“面积比 $= k^2$”这件事对所有相似图形都成立（不只是三角形、正方形）？用“把图形分成许多小正方形再求和”的思路写一句话说明理由。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + 相似比 $k = 4 slash 9$；面积比 $k^2 = 16 slash 81$。
  + 图面积 $6$ 平方厘米；$k^2 = 4 times 10^8$；实际面积 $6 times 4 times 10^8 = 2.4 times 10^9$ 平方厘米 $= 2.4 times 10^5$ 平方米。
  + $frac(h, 1.5) = frac(20, 1.25) => h = 24$ 米。

  *应用*
  + 边长比 $= sqrt(16 slash 25) = 4 slash 5$；周长比亦 $= 4 slash 5$。
  + 体积比 $= (1 slash 10)^3 = 1 slash 1000$。模型容积 $= 500 slash 1000 = 0.5$ 升 $= 500$ 毫升。
  + 由 $B D parallel E C$ 及 $triangle A B D tilde triangle A C E$（AA）：$frac(A B, A C) = frac(A D, A E) => frac(24, A C) = frac(15, 40) => A C = 64$ 米；$B C = A C - A B = 64 - 24 = 40$ 米。

  *挑战 ☞ 选做*
  + 设面积分别为 $9 x$、$16 x$，差 $16 x - 9 x = 7 x = 35$，$x = 5$。两面积分别为 $45$ 与 $80$ 平方米。
  + 第一张图：图面积 $= 1 slash 2 times 6 times 4 = 12$ 平方厘米；$k^2 = 500^2 = 2.5 times 10^5$；实际 $= 12 times 2.5 times 10^5 = 3 times 10^6$ 平方厘米 $= 300$ 平方米。\
    第二张图：实际 $300$ 平方米 $= 3 times 10^6$ 平方厘米；$k^2 = 200^2 = 4 times 10^4$；图面积 $= 3 times 10^6 slash 4 times 10^4 = 75$ 平方厘米。两次算得的实际面积相同（$300$ 平方米）。
  + 任意图形在网格里都能用小正方形逼近。若每条边按 $k$ 放缩，小正方形的边也按 $k$ 放缩，每个小正方形的面积就按 $k^2$ 放缩；再求和也跟着放缩 $k^2$。所以“面积比 $= k^2$”与具体形状无关。
]
