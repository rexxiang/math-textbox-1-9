#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref, answer-cut, self-check, proof-step
#import "../../lib/diagram-packages.typ": cetz

=== 四边形家族：从一般到正方形 <tool:ge03-quadrilateral-family>

#vocab[平行四边形 parallelogram][矩形 rectangle][菱形 rhombus][正方形 square][梯形 trapezoid]

#crisis[
  小学画过正方形、长方形、菱形、平行四边形、梯形，像一组互不相关的图形卡片。可进了初中，这些名称突然都和*性质、判定*、*对角线*、*证明*连在一起：“证明一个四边形是矩形”到底要证什么？“正方形”就是“又矩又菱”吗？

  *这章要做的，是把四边形家族梳成一棵*继承树*：从最一般的平行四边形出发，每加一条约束多出一类；每个子类*继承*上层的全部性质，再带上自己独有的性质。这样题目要什么判据就到哪一层取。*
]

#tryit[
  在 $square A B C D$ 中，$A B = C D$、$A D = B C$。它是平行四边形吗？给出一句话理由。
]

#discovery[
  *第 0 层：一般四边形*

  只需要四条线段首尾相连。内角和 $= 360 degree$（这是 #secref("pf06-polygons-and-circle") 已经有的前面学过的知识）。

  *第 1 层：平行四边形 $=$ 两组对边分别平行*

  在 $square A B C D$ 中，若 $A B parallel C D$ 且 $A D parallel B C$，称其为*平行四边形*。由平行线与全等三角形可以*推出*一串性质：

  - 对边相等：$A B = C D$、$A D = B C$。
  - 对角相等：$angle A = angle C$、$angle B = angle D$。
  - 邻角互补：$angle A + angle B = 180 degree$。
  - *对角线互相平分*：$A C$ 与 $B D$ 的交点 $O$ 满足 $O A = O C$、$O B = O D$。

  这四条性质都可以通过连对角线 $+$ 全等三角形（SSS 或 ASA）读出。

  如图 @fig-parallelogram-properties 所示。
  #figure(
    cetz.canvas(length: 1cm, {
      import cetz.draw: *
      let A = (0, 0)
      let B = (4, 0)
      let C = (5, 2.2)
      let D = (1, 2.2)
      let O = (2.5, 1.1)
      // Parallelogram
      line(A, B, C, D, close: true, stroke: 0.7pt)
      // Diagonals
      line(A, C, stroke: 0.5pt + luma(130))
      line(B, D, stroke: 0.5pt + luma(130))
      // Center point
      circle(O, radius: 0.06, fill: black, stroke: none)
      // Labels
      content((-0.3, -0.2), $A$)
      content((4.3, -0.2), $B$)
      content((5.3, 2.4), $C$)
      content((0.7, 2.4), $D$)
      content((2.5, 0.75), $O$)
      // Parallel marks
      content((2, -0.25), text(7pt)[$arrow.r$])
      content((3, 2.45), text(7pt)[$arrow.r$])
      content((-0.1, 1.1), text(7pt)[$arrow.t$])
      content((4.6, 1.1), text(7pt)[$arrow.t$])
      // Equal diagonal halves
      content((1.0, 0.35), text(7pt)[$|$])
      content((4.0, 1.85), text(7pt)[$|$])
      content((3.6, 0.35), text(7pt)[$||$])
      content((1.4, 1.85), text(7pt)[$||$])
    }),
    caption: [平行四边形 $square A B C D$：对边平行且相等，对角线互相平分于 $O$（$O A = O C$, $O B = O D$）。]
  ) <fig-parallelogram-properties>

  *判定（逆定理）*：满足下列任意一条 $=>$ 四边形是平行四边形：

  + 两组对边分别平行（定义）；
  + 两组对边分别相等；
  + 一组对边平行且相等；
  + 两组对角分别相等；
  + 对角线互相平分。

  *第 2 层：三类“加强型”平行四边形*

  #grid(
    columns: (1fr, 1fr, 1fr),
    gutter: 10pt,
    [*矩形*：有一个角是直角的平行四边形。
      由此*四角皆为直角*；对角线*相等*（$A C = B D$）且仍互相平分。],
    [*菱形*：四条边相等的平行四边形（等价：有一组邻边相等）。
      对角线*互相垂直*，且*分别平分一组对角*。],
    [*正方形*：既是矩形又是菱形（等价：一个角是直角 $+$ 一组邻边相等）。
      同时具备矩形与菱形的*所有*性质：四角直角 $+$ 四边相等 $+$ 对角线相等、互相垂直、互相平分、还平分对角。],
  )

  如图 @fig-rect-rhombus-square 所示。
  #figure(
    cetz.canvas(length: 0.8cm, {
      import cetz.draw: *
      // Rectangle
      let rA = (0, 0)
      let rB = (3, 0)
      let rC = (3, 2)
      let rD = (0, 2)
      line(rA, rB, rC, rD, close: true, stroke: 0.6pt)
      line(rA, rC, stroke: 0.3pt + luma(160))
      line(rB, rD, stroke: 0.3pt + luma(160))
      // right angle marks
      rect((0, 0), (0.25, 0.25), stroke: 0.3pt + rgb("#B71C1C"), fill: none)
      content((1.5, -0.45), text(8pt)[矩形])
      // Rhombus
      let dA = (5, 1)
      let dB = (7, 0)
      let dC = (9, 1)
      let dD = (7, 2)
      line(dA, dB, dC, dD, close: true, stroke: 0.6pt)
      line(dA, dC, stroke: 0.3pt + luma(160))
      line(dB, dD, stroke: 0.3pt + luma(160))
      // perpendicular mark at center
      rect((6.85, 0.85), (7.15, 1.15), stroke: 0.3pt + rgb("#1976D2"), fill: none)
      content((7, -0.45), text(8pt)[菱形])
      // Square
      let sA = (11, 0)
      let sB = (13, 0)
      let sC = (13, 2)
      let sD = (11, 2)
      line(sA, sB, sC, sD, close: true, stroke: 0.6pt)
      line(sA, sC, stroke: 0.3pt + luma(160))
      line(sB, sD, stroke: 0.3pt + luma(160))
      rect((11, 0), (11.25, 0.25), stroke: 0.3pt + rgb("#B71C1C"), fill: none)
      rect((11.85, 0.85), (12.15, 1.15), stroke: 0.3pt + rgb("#1976D2"), fill: none)
      content((12, -0.45), text(8pt)[正方形])
    }),
    caption: [三种特殊平行四边形：矩形（直角）、菱形（对角线垂直）、正方形（兼具两者）。]
  ) <fig-rect-rhombus-square>

  *第 3 层（侧枝）：梯形*

  有且只有*一组*对边平行的四边形称为*梯形*。两腰若相等，则为*等腰梯形*；等腰梯形的两条对角线相等、两底角相等。

  *继承树*（$->$ 表示“特殊化”）

  $ "四边形" -> "平行四边形" ->^{"一角 " = 90 degree} "矩形" ->^{"邻边相等"} "正方形" $
  $ "平行四边形" ->^{"邻边相等"} "菱形" ->^{"一角 " = 90 degree} "正方形" $
  $ "四边形" ->^{"恰一组对边平行"} "梯形" ->^{"两腰相等"} "等腰梯形" $

  下面这张图把"矩形 / 菱形 / 正方形"这三层用图形并排画出来——矩形和菱形是*并行*的两条加强路线，都向下汇合到正方形。

  #figure(
    cetz.canvas(length: 0.9cm, {
      import cetz.draw: *
      let blue = rgb("#1565C0")
      let gray = luma(120)

      content((0, 5), text(weight: "bold")[四边形])
      line((0, 4.7), (0, 4.0), stroke: 0.6pt + gray)
      content((0, 3.7), text(weight: "bold")[平行四边形])

      line((-0.6, 3.4), (-3.0, 1.9), stroke: 0.6pt + gray)
      line((0.6, 3.4), (3.0, 1.9), stroke: 0.6pt + gray)
      content((-2.5, 2.9), text(size: 8pt, fill: blue)[$+$ 一角直角])
      content((2.5, 2.9), text(size: 8pt, fill: blue)[$+$ 邻边相等])

      content((-3.4, 1.6), text(weight: "bold")[矩形])
      content((3.4, 1.6), text(weight: "bold")[菱形])

      line((-3.0, 1.3), (-0.4, 0.3), stroke: 0.6pt + gray)
      line((3.0, 1.3), (0.4, 0.3), stroke: 0.6pt + gray)
      content((-2.4, 0.5), text(size: 8pt, fill: blue)[$+$ 邻边相等])
      content((2.4, 0.5), text(size: 8pt, fill: blue)[$+$ 一角直角])

      content((0, 0), text(weight: "bold")[正方形])
    }),
    caption: [四边形继承树：矩形与菱形是平行四边形的两条并行特化路线，都汇合到正方形。],
  ) <fig-quad-inheritance-tree>

  *一句话*：每下一层多一条约束，就多一串性质；但不要忘了*它也继承了上层全部性质*。
]

#side-hack[
  *证“某四边形是……”的通法*：先*落到平行四边形*（五个判定之一），再看题目要的是矩形还是菱形还是正方形——按“加强条件”再凑一条。

  | 想证 | 一般套路 |
  |------|----------|
  | 平行四边形 | 任一条平行四边形判定 |
  | 矩形 | 已是平行四边形 $+$ 一个角直角，或对角线相等 |
  | 菱形 | 已是平行四边形 $+$ 邻边相等，或对角线互相垂直 |
  | 正方形 | 已是矩形 $+$ 邻边相等，或已是菱形 $+$ 一个角直角 |
]

#tryit[
  已知某平行四边形对角线互相平分且相等，但没垂直。它是什么特殊四边形？
]

#history-note[
  约前 300 年，欧几里得（Euclid）在《几何原本》第一卷命题 I.34–I.36 中，用全等三角形系统证明了平行四边形的对边相等、对角相等、对角线互相平分等性质。在中国，先秦时期的《墨经》（约前 4 世纪）已有对矩形"矩"的讨论——墨家把直角作为定义矩形的核心要素。矩形、菱形、正方形从平行四边形"特殊化"的思路，近代教材延续至今：先立最一般的概念，再用约束切出子类。
]

#blueprint[
  *工具一：平行四边形的性质 $+$ 判定（五 $+$ 五）*

  *性质（必背）*：平行四边形的 $=>$ 对边平行 $+$ 对边相等 $+$ 对角相等 $+$ 邻角互补 $+$ 对角线互相平分。

  *判定（选一条即可）*：

  + 两组对边分别平行。
  + 两组对边分别相等。
  + 一组对边平行且相等。
  + 两组对角分别相等。
  + 对角线互相平分。

  *工具二：矩形 / 菱形 / 正方形的特性*

  #table(
    columns: (1fr, 1.5fr, 1.5fr),
    inset: 6pt,
    stroke: 0.4pt,
    [*图形*], [*独有性质*], [*独有判定（已是平行四边形前提下）*],
    [矩形], [四角直角；对角线相等。], [一角直角；或对角线相等。],
    [菱形], [四边相等；对角线互相垂直并平分对角。], [一组邻边相等；或对角线互相垂直。],
    [正方形], [同时具矩形与菱形全部性质。], [矩形 $+$ 邻边相等；或菱形 $+$ 一角直角。],
  )

  *工具三：梯形与等腰梯形*

  *梯形*：只有*一组*对边平行；平行的两条叫*底*，不平行的两条叫*腰*。
  *等腰梯形*：两腰相等。性质：两底角相等；两条对角线相等。判定：两腰相等；或两底角相等；或对角线相等且不是平行四边形。

  *例*：在 $square A B C D$ 中，$A C$ 与 $B D$ 相交于 $O$，且 $O A = O C$、$O B = O D$、$A C = B D$。证明它是矩形。

  *证*：

  #proof-step(
    ([$O A = O C$ 且 $O B = O D$], [对角线互相平分]),
    ([$square A B C D$ 是平行四边形], [对角线互相平分 $=>$ 平行四边形]),
    ([$A C = B D$], [已知]),
    ([$square A B C D$ 是矩形], [平行四边形 $+$ 对角线相等 $=>$ 矩形]),
  )
]

#self-check[
  正方形是矩形吗？是菱形吗？把“四边形家族”的包含关系用一句话讲清楚。
]


#pitfall[
  Luca 这样判定：

  #block(inset: (left: 12pt))[
    *题目*：四边形 $A B C D$ 中，$A B parallel C D$。能否断定它是平行四边形？

    第 1 行："平行四边形的定义是'有一组对边平行'。"

    第 2 行：本题给了 $A B parallel C D$，正好一组对边平行。

    第 3 行：所以 $A B C D$ 是平行四边形。
  ]

  ✎ 把梯形和平行四边形画在一起对照一下——梯形也满足"$A B parallel C D$"。Luca 错在哪？

  #block(width: 100%, fill: rgb("#FFFDE7"), radius: 2pt, inset: 8pt)[
    *卡在哪里*：第 1 行的定义记错了一半。平行四边形要求*两组对边都平行*，不是"一组"。只有一组对边平行的四边形是*梯形*。

    判定平行四边形可用："两组对边分别平行"、"两组对边分别相等"、"对角线互相平分"、或"一组对边*平行且相等*"——都至少要"够到两组对边的关系"。单独一句 $A B parallel C D$ 远远不够。

    *正确做法*：要补一条独立信息，比如还得到 $A B = C D$（一组对边平行*且相等*），或 $A D parallel B C$（两组对边都平行），才能断定是平行四边形。

    ✓ 正确结论：仅凭 $A B parallel C D$ *不能*判定平行四边形（可能是梯形）。
  ]

  其余常见走错：

  ✗ "正方形不算矩形" → ✓ 正方形*是*矩形（也是菱形）；继承树里它属于矩形的子集。

  ✗ 以"对角线相等"判定菱形 / "对角线互相垂直"判定矩形 → ✓ 反了。对角线相等 $->$ 矩形；对角线互相垂直 $->$ 菱形。

  ✗ 把梯形也当成平行四边形 → ✓ 梯形要求*只有一组*对边平行；两组都平行就升级为平行四边形了。

  ✗ 忘了"四边相等"不一定是正方形 → ✓ 四边相等只到菱形；要正方形还需补直角或对角线相等。
]

#mastery[
  *帮晨晨找错*——下面是同学晨晨在做四边形题时的解题过程，他在判定条件、性质应用、家族继承上踩了不少坑。请你逐题指出他错在哪一步、为什么错，并写出正确的判定 / 算法。

  *基础*

  + 晨晨判断"四边形 $A B C D$ 中 $A B parallel C D$，所以它是平行四边形"。请找出错误位置，并说明：晨晨为什么会这样想？他混淆了*平行四边形的定义*与哪个图形的定义？这两类四边形分别要求几组对边平行？
  + 晨晨证明"$square A B C D$ 中 $A B = C D$、$A D = B C$，因为对角线*相等*，所以它是矩形"。请找出错误位置，并说明：错误的根源是把*哪条平行四边形判定条件*与*哪条矩形独有判定*混在了一起？把"题目给的"和"题目没给的"分别列出来。
  + 晨晨说："对角线互相*垂直*的平行四边形是矩形"。请找出错误位置，并说明：他把矩形与菱形的*对角线性质*交换错位了——具体是哪两条性质对调了？写出正确的对应。
  + 晨晨说："四边相等的四边形一定是正方形"。请找出错误位置，并说明：他混淆了*菱形定义*与*正方形定义*——少了哪一条进入正方形必需的约束？(可结合继承树回答)

  *应用*

  + 晨晨算菱形对角线长 $6$ 和 $8$ 的边长：「对角线就是边的两倍长，所以边长 $= 6 div 2 = 3$。」请指出他错把哪条性质用反了，并写出正确的边长。
  + 矩形对角线长 $10$，$A B = 6$。晨晨说："对角线和一条边相加等于另一条边，所以 $B C = 10 - 6 = 4$。"请指出他错把哪条结论替换成了哪条，并写出正确的 $B C$。

  *挑战*

  + 晨晨说："正方形是矩形，但*不是*菱形——因为菱形对角线必须不等长，而正方形对角线相等。"请用四边形继承树解释他错在哪儿——他把"独有性质"和"必要条件"搞混了哪一步？给出正方形与菱形之间正确的继承关系。
  + 晨晨说："等腰梯形对角线相等；反过来，对角线相等的四边形都是等腰梯形。"请举一个反例（具体的图形）证明他逆用错了；并写出"对角线相等"这个结论*在不同四边形里*都意味着什么。

  + *[反直觉]*：平行四边形的对角线一定互相平分，但是否一定相等？等腰梯形的对角线一定相等，但是否互相平分？用特殊图形举例说明两种条件之间的差异。
  + *[组合]*：矩形 $A B C D$ 中，$A B = 6$，$B C = 8$。求对角线 $A C$ 的长，并说明你用了哪两个矩形性质。
  + *[组合]*：证明：菱形的对角线互相垂直。（提示：利用菱形四边相等，在两条对角线交叉点处用等腰三角形性质。）
  + *[构造题]*：构造一个既是矩形又是菱形的四边形。它是什么？写出它满足的全部特殊性质。

#answer-cut[

  *基础*
  + 错。仅一组对边平行不足以判定平行四边形，那是*梯形*。判定平行四边形需要两组对边都平行，或一组对边*平行且相等*，或两组对边分别相等等五条之一。
  + 错在判定条件。已知"两组对边分别相等"得到的是*平行四边形*，不是矩形。再加"对角线相等"才能升级到矩形——而题目里没有给出对角线相等。正确结论：$square A B C D$ 是平行四边形（不能判定矩形）。
  + 不对，反了。对角线*相等* $=>$ 矩形；对角线*互相垂直* $=>$ 菱形。晨晨把矩形和菱形的对角线判定颠倒了。
  + 漏了"加一个直角"。四边相等只能判定*菱形*；菱形再加一个直角（或对角线相等）才升级为正方形。

  *应用*
  + 晨晨把对角线长当成了边长。菱形对角线*互相垂直平分*，半对角线分别是 $3$ 和 $4$，由勾股定理：边长 $= sqrt(3^2 + 4^2) = 5$，不是 $3$。
  + 错把"勾股关系"替换成了"线性差"。矩形里 $A C^2 = A B^2 + B C^2$（勾股定理），$10^2 = 6^2 + B C^2$，$B C^2 = 64$，$B C = 8$，不是 $4$。

  *挑战*
  + 错在"独有 $!=$ 必要"。正方形*同时是矩形又是菱形*——它继承菱形的*所有*性质。"对角线垂直"是菱形的独有判定（充分），不是"判定为菱形必须满足的额外条件"。继承树正确读法："正方形 $arrow$ 矩形且 $arrow$ 菱形"——独有性质往子类越走越多，*不是越走越少*。
  + 反例：矩形（非正方形）的对角线也相等，但它不是等腰梯形（它是平行四边形）。再举：正方形对角线也相等，也不是等腰梯形。所以"对角线相等"在不同图形里意味着不同：
    - *矩形 / 正方形*：对角线相等是它们的*独有性质*。
    - *等腰梯形*：对角线相等也是其性质，但前提是*它本来就是梯形*（只有一组对边平行）。
    晨晨的逆用漏掉了"先确认它是梯形"这一前提。"对角线相等"独立于其他条件时不能反推一种特定四边形。
  + *[反直觉]*：平行四边形对角线不一定相等（如一般平行四边形，只有矩形才等）；等腰梯形对角线相等但不互相平分（梯形一般不是平行四边形）（四边形性质的对比分析）。
  + 矩形对角相等且为直角；$A C = sqrt(A B^2 + B C^2) = sqrt(36 + 64) = sqrt(100) = 10$。用到：矩形四角为直角（角的性质）+ 勾股定理（斜边计算）。
  + 设对角线交于 $O$，菱形 $A B C D$ 四边相等。$A B = B C$，$O B = O B$（公共边）——但 $O A$ 不一定等 $O C$，需要先用平行四边形性质知 $O$ 是对角线中点，$O A = O C$，$O B = O D$。在 $triangle O A B$ 中，$A B = A D$（菱形），$O A = O C$ 且 $O B = O D$，用 $A B^2 = O A^2 + O B^2$... 简洁证明：$O A^2 + O B^2 = A B^2$，$O A^2 + O D^2 = A D^2 = A B^2$，故 $O B = O D$；同理，菱形中对角线平分角，在等腰 $triangle A B D$ 中 $A B = A D$，角平分线即高，所以 $A C perp B D$（菱形性质 + 等腰三角形）。
  + *[构造题]*：既是矩形（四角为直角）又是菱形（四边相等）的图形就是*正方形*。它满足：四边相等、四角为直角、对角线相等且互相垂直平分（综合矩形与菱形的全部性质）。
]
]

#history-note[
  *多丽丝·沙特施奈德（Doris Schattschneider，$1939$–）*——美国数学家，因系统研究画家埃舍尔（M. C. Escher）作品中的对称密铺（用一种或几种多边形把整个平面"无缝拼贴"）而广为人知。她的研究澄清了一个反直觉的事实：*任意*凸四边形——不管多么"歪斜"——都能单独密铺平面，平行四边形与梯形则给出最对称的几种密铺方式；相比之下，凸五边形能否密铺要复杂得多，分类工作直到 $2017$ 年才被宣告完成。这一节里"四边形大家族"中各成员的性质——平行、等长、对角线交点——正是看清这些密铺规律的入口。
]
