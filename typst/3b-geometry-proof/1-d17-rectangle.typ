#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, secref, tryit, pitfall

== §d17 矩形、菱形与正方形：特殊的平行四边形 <tool:d17-rectangle>

#vocab[矩形 rectangle / 菱形 rhombus / 正方形 square / 直角 right angle]

#crisis[
  上一节（#secref("d14-quadrilateral-angle-sum")）我们发现：平行四边形对边相等、对角相等、对角线互相平分——这已经很整齐了。

  但如果再往上加一个条件会怎样？

  - 加上"所有角都相等"：四个角都变成 $90 degree$，多出什么性质？
  - 加上"所有边都相等"：四条边一样长，又多出什么？

  *两个"加强版"的平行四边形，各自会带来哪些额外保证？*
]

#history-note[
  矩形（rectangle）来自拉丁语"直角的"，菱形（rhombus）来自希腊语"旋转的菱状物"——古希腊人用陀螺的形状命名它。正方形（square）则是拉丁语"四方相等"的简化。这三个词在欧洲数学传统里沿用了两千多年。
]

#discovery[
  *第一步：矩形的对角线为什么相等？*

  矩形 $A B C D$ 中四个角都是 $90 degree$。看对角线 $A C$ 和 $B D$：

  由 $A B = A B$（公共边）、$angle D A B = angle C B A = 90 degree$、$A D = B C$（平行四边形对边相等），

  用 SAS 得 $triangle D A B tilde.eq triangle C B A$，所以 $D B = C A$——*矩形的两条对角线相等*。

  *第二步：菱形的对角线为什么垂直？*

  菱形 $A B C D$ 中四条边都等于 $a$。设对角线交点为 $O$。

  在 $triangle A O B$ 中，$O A = O C = frac(1, 2) A C$，$O B = O D = frac(1, 2) B D$，且 $A B = A D = a$。

  由 $A O$ 是公共边，$A B = A D$，$O B = O D$（SSS），得 $triangle A O B tilde.eq triangle A O D$，

  于是 $angle A O B = angle A O D$，两角相邻且相等，所以各为 $90 degree$——*菱形的对角线互相垂直*。

  *第三步：正方形是两个条件的交集。*

  正方形 = 矩形（四个直角）+ 菱形（四边相等）。它同时拥有两者的全部性质，既对角线相等，又对角线互相垂直。
]

#tryit[
  先想再算：

  + 矩形的两条对角线长度相等，这一点在哪类平行四边形里不成立？
  + 菱形的两条对角线分别为 $6$ 和 $8$。不用勾股定理，能不能用面积算出菱形面积？
  + 正方形比矩形"多出"哪条菱形的性质？比菱形"多出"哪条矩形的性质？
]

#side-hack[
  *记住三者关系的最简单方法：*

  - 矩形 = 平行四边形 + 一个直角
  - 菱形 = 平行四边形 + 一组邻边相等
  - 正方形 = 矩形 $inter$ 菱形（两个条件都满足）
]

#blueprint[
  *工具 1：矩形*

  *定义*：有一个角是直角的平行四边形叫做*矩形*。

  矩形的额外性质（在平行四边形全部性质之上）：
  - 四个角都是 $90 degree$
  - 两条对角线相等：$A C = B D$

  矩形的判定（选其一即可）：
  - 平行四边形有一个角是直角
  - 平行四边形的两条对角线相等

  *工具 2：菱形*

  *定义*：四条边都相等的平行四边形叫做*菱形*。

  菱形的额外性质：
  - 两条对角线互相垂直：$A C perp B D$
  - 对角线平分对角：$angle 1 = angle 2$（对角线平分每个顶角）

  菱形的面积公式：

  $ S = frac(1, 2) d_1 d_2 $

  其中 $d_1$、$d_2$ 是两条对角线的长度。

  菱形的判定（选其一即可）：
  - 平行四边形有一组邻边相等
  - 平行四边形的两条对角线互相垂直

  *工具 3：正方形*

  *定义*：既是矩形又是菱形的四边形叫做*正方形*。

  正方形同时拥有矩形和菱形的全部性质：
  - 四个角都是 $90 degree$（来自矩形）
  - 四条边都相等（来自菱形）
  - 两条对角线相等且互相垂直平分（两者合并）

  *例 1*：菱形两条对角线长分别为 $10$ 和 $24$。求菱形面积。

  解：$ S = frac(1, 2) times 10 times 24 = 120 $

  *例 2*：矩形 $A B C D$ 中，$A B = 6$，$B C = 8$。求对角线 $A C$ 的长。

  解：矩形四角都是直角，在直角 $triangle A B C$ 中，$A C^2 = A B^2 + B C^2 = 36 + 64 = 100$，所以 $A C = 10$。（这里用到了后续 #secref("d23-pythagorean-theorem") 将正式发明的勾股定理。）

  *例 3*：正方形边长为 $5$。说明它同时具备哪些关键性质。

  解：四个角均为 $90 degree$（矩形）；四条边均为 $5$（菱形）；两条对角线相等且互相垂直，交点平分两条对角线。
]

#pitfall[
  *陷阱 1：菱形不一定有直角。*

  四边相等 $arrow.r.double.not$ 正方形。只有同时"四边相等+有一个直角"才能断定是正方形。

  *陷阱 2：矩形的对角线相等，但不一定垂直。*

  对角线相等是矩形性质，对角线垂直是菱形性质。只有正方形两者兼备。

  *陷阱 3：对角线垂直平分不能直接说明是菱形。*

  对角线互相垂直且平分才是菱形（需要两个条件：垂直 + 互相平分）；只是垂直而不互相平分，不能得到菱形结论。
]

#mastery[
  *基础*

  + 判断正误：
    - 对角线相等的平行四边形是矩形。
    - 对角线互相垂直的平行四边形是菱形。
    - 正方形既是矩形又是菱形。
    - 菱形的对角线相等。

  + 矩形 $A B C D$ 中，$angle A B D = 30 degree$。求 $angle A D B$。

  + 菱形的两条对角线分别是 $16$ 和 $12$。求菱形面积。

  *应用*

  + 矩形 $A B C D$ 中，$A B = 5$，$B C = 12$。（不使用 §7.8 勾股定理）已知 $A C = 13$，验证矩形对角线的计算是否与此一致，并求 $B D$。

  + 菱形 $A B C D$ 的边长为 $10$，一条对角线长为 $12$。设对角线交点为 $O$，求 $O A$ 和 $O B$，并计算另一条对角线长。（提示：用面积等式 $frac(1, 2) times 12 times d_2 = ?$，或先用对角线各自平分的性质配合勾股定理——若已有 §7.8 则直接用；否则用以下给定条件直接列方程求 $O B$：$O B = sqrt(100 - 36)$，可直接写出结果 $O B = 8$，对角线 $= 16$。）

  + 正方形面积为 $50$。求它的对角线长。（提示：设对角线为 $d$，正方形面积可用对角线表示：$S = frac(1, 2) d^2$；解方程即可，不需勾股定理。）

  *挑战*

  + 已知矩形 $A B C D$，对角线交点为 $O$。说明为什么 $O A = O B = O C = O D$。

  + 菱形 $A B C D$ 的两条对角线长之比为 $3 : 4$，面积为 $96$。求两条对角线各多长。

  + 解释"正方形既是矩形又是菱形"的具体含义：它能同时调用哪些来自矩形的性质、哪些来自菱形的性质？各举两条。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*

  + 对；对；对；错（菱形对角线不一定相等，只有正方形才对角线相等且垂直）。

  + 矩形中 $angle A B C = 90 degree$，所以 $angle A B D + angle D B C = 90 degree$，$angle D B C = 60 degree$；在 $triangle A B D$ 中，$angle A B D = 30 degree$，$angle B A D = 90 degree$（矩形直角），所以 $angle A D B = 60 degree$。

  + $S = frac(1, 2) times 16 times 12 = 96$。

  *应用*

  + 矩形对角线相等，所以 $B D = A C = 13$，与给定 $A C = 13$ 一致。

  + $O A = 6$，$O B = sqrt(10^2 - 6^2) = sqrt(64) = 8$，另一条对角线 $= 2 times 8 = 16$。

  + 设对角线为 $d$，$frac(1, 2) d^2 = 50$，$d^2 = 100$，$d = 10$。

  *挑战*

  + 矩形对角线相等 $A C = B D$，且平行四边形对角线互相平分，所以 $O A = O C = frac(A C, 2)$，$O B = O D = frac(B D, 2)$；又 $A C = B D$，故四段均相等。

  + 设两条对角线为 $3k$ 和 $4k$，面积 $= frac(1, 2) times 3k times 4k = 6k^2 = 96$，$k^2 = 16$，$k = 4$，两对角线为 $12$ 和 $16$。

  + 来自矩形的性质：四个角都是 $90 degree$；两条对角线相等。来自菱形的性质：四条边都相等；两条对角线互相垂直平分。
]
