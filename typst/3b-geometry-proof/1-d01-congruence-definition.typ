#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref

== 全等与证明入口：先学会“完全重合”怎么说 <tool:d01-congruence-definition>

#vocab[全等 congruent / 对应 correspondence / 证明链 proof chain / 两边和一个非夹角 Side-Side-Angle (SSA)]

#crisis[
  造桥时，工程师最担心的不是”这两块三角板看起来差不多”，而是”它们到底是不是同一个形状、同一个大小”。如果要把这种”看起来一样”写成真正可靠的几何语言，该从哪里起步？

  到 #secref("b26-complementary") 里，你见过对顶角和角的补余关系；到 #secref("b29-parallel-lines") 里，截线在平行线间产生的那些相等角；这些前面学过的知识，正是证明两个三角形全等时最常使用的弹药。

  *什么时候两个三角形算”完全一样”？又怎样把一句几何结论写成一条清清楚楚的证明链？*
]

#history-note[
  几何证明真正成熟之前，人们常靠图感和经验说“应该一样”。全等这个概念的重要之处，就在于它把“应该一样”翻译成了可写、可检验、可复用的语言：能完全重合，而且谁对应谁必须讲清楚。
]

#discovery[
  *第一步：全等不是“差不多像”。*

  两个三角形若能完全重合，才叫全等。这里同时要求大小一样、形状一样。

  *第二步：重合之前，先得说清谁对谁。*

  若 $A$ 对 $D$、$B$ 对 $E$、$C$ 对 $F$，那后面所有对应边和对应角都要跟着这个顺序走。这就叫“对应关系”。

  *第三步：证明不是堆结论，而是搭链。*

  写证明就像搭桥：先说你知道什么，再搭一座桥（通常是先证全等），最后走过去拿结论。
]

#tryit[
  先自己想一想：

  + 若写作 $triangle A B C tilde.eq triangle D E F$，那么 $B$ 对应谁？$C$ 又对应谁？
  + 若题目最后想证一条边相等，你觉得证明里最该先盯住“目标边”，还是先找能不能把两个三角形连起来？
  + 若只知道两边和一个*非夹角*相等，你敢不敢立刻说“两三角形已经一样了”？
]

#side-hack[
  *写证明前先摆好三格子*：

  - *已知*：题目已经给了哪些边、角、平行、垂直、中点
  - *中间桥*：要不要先证全等或别的稳定关系
  - *结论*：最后到底要的是边相等、角相等，还是平行、垂直

  这样写，证明就不会一上来乱跳。
]

#blueprint[
  *一、什么叫全等*

  如果两个三角形可以完全重合，就叫*全等三角形*，记作

  $ triangle A B C tilde.eq triangle D E F $

  一旦全等，就有：

  - 对应边相等
  - 对应角相等

  这条原则常简称为：*全等对应元素相等*。

  *二、对应关系一定要跟着名字顺序走*

  若写成 $triangle A B C tilde.eq triangle D E F$，就表示

  - $A$ 对 $D$
  - $B$ 对 $E$
  - $C$ 对 $F$

  所以后面的对应边、对应角都必须跟着这个顺序来读。

  *三、最基础的证明链*

  几何证明里，最常见的一条链是：

  - 先写*已知*了什么
  - 再写中间桥：怎样把两个三角形连到一起
  - 最后用“全等对应元素相等”推出结论

  下一节开始，我们会把“怎样把两个三角形锁成全等”分成几把具体工具来学。

  *四、两边和一个非夹角（Side-Side-Angle，`SSA`）先当成警报，不当成工具*

  若只知道两边和一个*非夹角*相等，通常还不能保证全等。这种情形常记作 `SSA`。它更像一句警报：*信息还不够，先别急着下结论*。

  *例 1*：已知 $triangle A B C tilde.eq triangle D E F$，且 $angle A = 52 degree$，$angle B = 68 degree$。求 $angle F$。

  解：由三角形内角和知 $angle C = 60 degree$。又因全等对应角相等，所以 $angle F = 60 degree$。

  *例 2*：若写成 $triangle A B C tilde.eq triangle D F E$，那么谁和谁对应？

  解：这时 $A$ 对 $D$，$B$ 对 $F$，$C$ 对 $E$。名字顺序一变，对应关系也跟着变。
]

#pitfall[
  *陷阱 1：对应顺序乱了，后面全乱。*

  不是字母长得像就自动对应，而是要按写出的顺序走。

  *陷阱 2：把结论写在前面，当成“证明”。*

  证明要先有已知和中间桥，再走到结论；不能一上来就把想证的东西当成已经证过。

  *陷阱 3：`SSA` 不是现成工具。*

  两边和一个非夹角相等时，常常还留着摆成另一种三角形的可能。
]

#mastery[
  *基础*

  + 已知 $triangle A B C tilde.eq triangle D E F$。写出三组对应顶点、对应边、对应角。

  + 若已知 $angle A = 40 degree$，$angle B = 65 degree$，求 $angle F$。

  + 用一句话解释：为什么写 $triangle A B C tilde.eq triangle D F E$ 和写 $triangle A B C tilde.eq triangle D E F$ 不是一回事？

  *应用*

  + 把下面这条证明链补完整：已知 ……；中间桥 ……；所以两个三角形全等；因此 ……。

  + 已知题目最后想证 $B C = E F$。请先不管具体判定工具，说明你会怎样安排“已知 / 中间桥 / 结论”三步。

  + 解释为什么 `SSA` 在这里要先被当成“别急着下结论”的警报。

  *挑战*

  + 如果有人说“两个三角形边差不多长，所以就算全等”，你会怎样反驳？

  + 为什么“全等对应元素相等”能成为后面很多几何题的共同出口？

  + 设想一个证明题最后想证两个角相等。请用一句话说明：为什么很多时候你会先去找“能不能把两个三角形锁成全等”。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*

  + 对应顶点：$A$ 对 $D$，$B$ 对 $E$，$C$ 对 $F$。对应边：$A B$ 对 $D E$，$B C$ 对 $E F$，$A C$ 对 $D F$。对应角：$angle A$ 对 $angle D$，$angle B$ 对 $angle E$，$angle C$ 对 $angle F$。

  + $angle C = 180 degree - 40 degree - 65 degree = 75 degree$，所以 $angle F = 75 degree$。

  + 因为对应关系是按字母顺序决定的，顺序一变，对应边角就会跟着变。

  *应用*

  + 先写题目给了什么；再写怎样把两个三角形连到一起；最后再用全等对应元素相等推出目标结论。

  + 先整理已知条件，再找能不能把两个三角形作为“中间桥”连起来，最后把 $B C = E F$ 放到结尾当作对应边结论推出。

  + 因为两边和一个非夹角相等时，三角形还可能摆成不止一种形状，所以信息还不够。

  *挑战*

  + 全等要求的是能完全重合，而“差不多长”既没有说明三条边是否一一对应，也没有说明角是否被锁定，所以远远不够。

  + 因为一旦先拿到全等，很多想要的边相等、角相等都会自动变成“对应元素相等”的现成出口。

  + 因为直接证角相等往往绕，而一旦先把两个三角形锁成全等，目标角就会自动作为对应角跟出来。
]
