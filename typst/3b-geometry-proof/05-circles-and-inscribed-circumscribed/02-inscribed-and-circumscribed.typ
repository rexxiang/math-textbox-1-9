#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref
#import "../../lib/diagram-packages.typ": cetz

=== 内接与外接：三角形、四边形与正多边形的圆 <tool:ge03-inscribed-circumscribed>

#vocab[内接 inscribed][外接 circumscribed][外心 circumcenter][内心 incenter][外接圆 circumcircle][内切圆 incircle]

#crisis[
  第 2 节里我们碰到：*三点不共线 $=>$ 它们的外接圆唯一*。但这个外接圆的*圆心*到底在哪？怎么用尺规把它*作*出来？如果把三角形换成四边形、正多边形，又是什么情况？

  *这一节把“内接 / 外接”这对关系从定义立起来，给出三角形外心 $=$ 三边中垂线交点、内心 $=$ 三个内角平分线交点，这两把“找圆心”的万能钥匙；再把它推广到正多边形的内接圆 / 外接圆同心结构。*
]

#history-note[
  把“在一个三角形里塞进最大的圆”这件事形式化为*内切圆*，在欧几里得《原本》里已是定理。三角形的三条中垂线必交于一点、三条内角平分线也必交于一点——这两条*“三线共点”*在古代几何里是非常令人惊讶的规律，它们分别给出了外心和内心，也是后来“九点圆、欧拉线”等一长串高等定理的起点。
]

#discovery[
  #figure(
    cetz.canvas(length: 1cm, {
      import cetz.draw: *
      // triangle
      let A = (-2.2, -1.2)
      let B = (2.4, -1.2)
      let C = (0.6, 1.9)
      line(A, B, stroke: 0.6pt)
      line(B, C, stroke: 0.6pt)
      line(C, A, stroke: 0.6pt)
      content((A.at(0) - 0.25, A.at(1) - 0.25), $A$)
      content((B.at(0) + 0.25, B.at(1) - 0.25), $B$)
      content((C.at(0), C.at(1) + 0.3), $C$)
      // circumcircle approximate
      let O = (0.3, -0.05)
      circle(O, radius: 2.55, stroke: 0.4pt + luma(140))
      circle(O, radius: 0.05, fill: black)
      content((O.at(0) + 0.25, O.at(1) - 0.2), $O$)
    }),
    caption: [$triangle A B C$ 的外接圆：圆心 $O$ 是三条边中垂线的交点，$O A = O B = O C$。]
  )

  *“内接 / 外接”的两种视角*

  给定一个图形 $F$（多边形）和一个圆 $Gamma$：

  - 若 $F$ 的所有顶点都*在圆上*，且圆把 $F$“裹在里面”，称 $F$ *内接于*圆 $Gamma$；同时称 $Gamma$ 是 $F$ 的*外接圆*。
  - 若 $F$ 的所有边都*与圆相切*，圆被 $F$“包在里面”，称 $F$ *外切*圆 $Gamma$；同时称 $Gamma$ 是 $F$ 的*内切圆*。

  一句口诀：*谁在外谁“外接”*——圆在外叫外接圆（顶点在圆上），圆在内叫内切圆（边与圆相切）。

  *三角形外心与外接圆*

  任给一个三角形 $triangle A B C$：

  - *三条边中垂线的交点 $O$ 到三顶点等距*：$O A = O B = O C$。
  - 因此以 $O$ 为圆心、$O A$ 为半径的圆经过 $A$、$B$、$C$——这就是*外接圆*。
  - $O$ 叫*外心*。锐角三角形外心在内部，直角三角形外心在斜边中点（Thales 的反向结论），钝角三角形外心在外部。

  *三角形内心与内切圆*

  - *三个内角平分线的交点 $I$ 到三边等距*（距离 $= $ 内切圆半径 $r$）。
  - 以 $I$ 为圆心、$r$ 为半径的圆与三条边都相切——这就是*内切圆*。
  - $I$ 叫*内心*，一定在三角形内部。

  #figure(
    cetz.canvas(length: 1cm, {
      import cetz.draw: *
      let A = (-2.5, -1.5)
      let B = (2.8, -1.5)
      let C = (0.2, 2.2)
      // Triangle
      line(A, B, C, close: true, stroke: 0.6pt)
      content((-2.8, -1.7), $A$)
      content((3.1, -1.7), $B$)
      content((0.2, 2.5), $C$)
      // Incircle (approximate incenter and inradius)
      let I = (0.15, -0.25)
      let r = 1.1
      circle(I, radius: r, stroke: 0.5pt + rgb("#388E3C"))
      circle(I, radius: 0.05, fill: rgb("#388E3C"))
      content((0.45, -0.15), text(fill: rgb("#388E3C"))[$I$])
      // Angle bisectors (dashed)
      line(A, (1.6, 0.4), stroke: 0.3pt + luma(160))
      line(B, (-1.2, 0.4), stroke: 0.3pt + luma(160))
      line(C, (0.15, -1.5), stroke: 0.3pt + luma(160))
      // Radius to one side
      line(I, (0.15, -1.35), stroke: 0.4pt + rgb("#388E3C"))
      content((0.5, -0.85), text(7pt, fill: rgb("#388E3C"))[$r$])
    }),
    caption: [三角形的内切圆：三条内角平分线交于内心 $I$，$I$ 到三边的垂直距离都等于 $r$。]
  )

  （为什么两种“三线共点”成立？——都可由“到两条线（或两点）等距的点的轨迹”这一基本结果推出，第 1 章的全等 $+$ 轨迹思想已经足够。）

  *正多边形：内接圆与外接圆同心*

  *正 $n$ 边形*的所有顶点可内接于同一个圆，所有边可外切于另一个圆；且这两个圆*同心*（共用中心 $O$）。
  两个圆的半径分别记作：

  - *外接圆半径* $R$：$O$ 到顶点的距离。
  - *内切圆半径* $r$（又叫*边心距*）：$O$ 到边的垂直距离。

  对正 $n$ 边形，若边长 $= a$，则*中心、边中点、顶点*构成一个直角三角形，其中一条直角边 $= a slash 2$，另一条 $= r$（边心距），斜边 $= R$（外接圆半径）。由直角三角形三边关系（下一章将正式证明为*勾股定理*）：

  $ R^2 = r^2 + (a slash 2)^2. $

  对正三角形、正方形、正六边形，$R$ 与 $r$ 可分别由 30-60-90 / 45-45-90 特值直接算出（见本节蓝图表）；一般的正 $n$ 边形公式涉及三角函数，属于高中内容，本节不展开。这条勾股关系会在 #secref("ch:pythagorean-radicals-trigonometric-ratios")（勾股定理）继续被使用。
]

#side-hack[
  作图流程口诀：

  - *作三角形外接圆*：两条边的中垂线一交 $->$ 外心 $O$；以 $O A$ 为半径画圆。
  - *作三角形内切圆*：两条内角平分线一交 $->$ 内心 $I$；过 $I$ 向任一边作垂线，垂足到 $I$ 的距离 $= r$。
  - *正 $n$ 边形*：若已有外接圆，把圆周等分 $n$ 份连起来；若已有边长 $a$，用 $R$、$r$ 公式反推。
]

#tryit[
  + 一个三角形的外心恰好落在某条边的中点上。这个三角形是什么形状？
  + 三角形的内心到三边距离都是 $3$。三角形的面积 $= ?$ （用 $r$ 与周长 $p$ 表示。）
  + 正六边形边长 $= 2$。求外接圆半径 $R$ 与内切圆半径 $r$。
]

#blueprint[
  *工具一：三角形外接圆（外心构造）*

  *结论*：任意三角形都有*唯一*外接圆，其圆心 $O$ 是三条边中垂线的交点；$R = O A = O B = O C$。

  *作图*：

  + 作 $A B$、$B C$ 的中垂线；
  + 相交得 $O$；
  + 以 $O$ 为圆心、$O A$ 为半径画圆，自动经过第三点 $C$。

  *位置*：锐角 $=>$ $O$ 在内；直角 $=>$ $O$ 是斜边中点（对应 Thales）；钝角 $=>$ $O$ 在外。

  *工具二：三角形内切圆（内心构造）*

  *结论*：任意三角形都有*唯一*内切圆，其圆心 $I$ 是三个内角平分线的交点；$I$ 到三边的垂直距离相等，记为 $r$。

  *面积关系（inline 推论）*：设三角形周长 $p$、半周长 $s = p slash 2$，则

  $ "面积" = s dot r. $

  （把三角形按 $I$ 到三边的垂直切成三个小三角形，高都是 $r$，底分别是三边；加起来即得。这是考试里“已知周长 $+$ 面积 $=>$ 内切圆半径”的万能桥。更一般的外接圆公式 $R = a b c slash (4 dot "面积")$、海伦公式，本节不立目，高中《解三角形》会系统化。）

  *工具三：正多边形的内接 / 外接同心圆*

  对边长 $= a$ 的正 $n$ 边形，外接圆与内切圆*同心*，并且中心、边中点、顶点三者组成的直角三角形满足

  $ R^2 = r^2 + (a slash 2)^2. $

  初中只处理 $n in {3, 4, 6}$，对应 30-60-90 或 45-45-90 特殊直角三角形；一般 $n$ 的闭式公式依赖三角函数，高中再学。

  *最常用的三种*：

  #table(
    columns: (auto, auto, auto, auto),
    inset: 6pt,
    stroke: 0.4pt,
    [*$n$*], [*$R$（以 $a$ 表）*], [*$r$（边心距）*], [*面积*],
    [$3$（正三角形）], [$a slash sqrt(3)$], [$a slash (2 sqrt(3))$], [$sqrt(3) a^2 slash 4$],
    [$4$（正方形）], [$a sqrt(2) slash 2$], [$a slash 2$], [$a^2$],
    [$6$（正六边形）], [$a$], [$a sqrt(3) slash 2$], [$3 sqrt(3) a^2 slash 2$],
  )

  *例*：$triangle A B C$ 是直角三角形，直角边 $A C = 6$、$B C = 8$。求它的外接圆半径与内切圆半径。

  解：斜边 $A B = sqrt(36 + 64) = 10$。由 Thales，外心是斜边中点，$R = 10 slash 2 = 5$。面积 $= 1 slash 2 times 6 times 8 = 24$；周长 $p = 6 + 8 + 10 = 24$，$s = 12$；由 $"面积" = s r$ 得 $r = 24 slash 12 = 2$。
]

#pitfall[
  *高频错误*

  ✗ 把*外心*说成三角形“中心”
  → ✓ 三角形的三种心（外心、内心、重心）各不相同；外心由中垂线定义，到*顶点*等距；内心由角平分线定义，到*边*等距。

  ✗ 钝角三角形里仍按“外心在内部”作外接圆
  → ✓ 钝角 $=>$ 外心在三角形*外*；但外接圆仍唯一，圆心可以在外。

  ✗ 认为“正多边形的外接圆与内切圆半径相等”
  → ✓ 只有 $n -> infinity$ 时（变成圆）$R = r$；否则 $R > r$，差值由 $R^2 - r^2 = (a slash 2)^2$ 给出。

  ✗ 把“内接”和“外切”张冠李戴
  → ✓ 顶点在圆上 $=$ 内接（图形“进入”圆）；边与圆相切 $=$ 外切（图形“裹住”圆）。

  ✗ 用 $"面积" = s r$ 时忘了 $s$ 是*半周长*
  → ✓ $s = p slash 2$，不是周长。
]

#mastery[
  *基础*

  + 一个直角三角形两直角边为 $5$ 与 $12$。求外接圆半径。

  + 一个三角形周长 $20$、面积 $30$。求内切圆半径。

  + 正方形边长 $= 4$。求外接圆半径与内切圆半径。

  *应用*

  + 正六边形边长 $= 6$。求它的外接圆半径、内切圆半径、面积。

  + $triangle A B C$ 中 $A B = 13$、$B C = 14$、$C A = 15$，已知面积为 $84$。求它的内切圆半径 $r$。

  + 证明：直角三角形的外接圆半径 $=$ 斜边的一半。

  *挑战 ☞ 选做*

  + 一个三角形的外心、内心、重心都重合。它是什么特殊三角形？为什么？

  + 证明：若一个四边形既有外接圆又有内切圆，则它的两组对边长度之和相等（切线段等长的直接推论）。

  + 正六边形边长 $= a$。用 $R^2 = r^2 + (a slash 2)^2$ 配合 $R = a$（正六边形由 $6$ 个等边三角形拼成，中心到顶点 $= $ 边长）直接推出 $r = (sqrt(3) slash 2) a$。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + 斜边 $= 13$；$R = 13 slash 2 = 6.5$。
  + $s = 10$；$r = 30 slash 10 = 3$。
  + $R = 4 sqrt(2) slash 2 = 2 sqrt(2)$；$r = 4 slash 2 = 2$。

  *应用*
  + $R = 6$；$r = 6 sqrt(3) slash 2 = 3 sqrt(3)$；面积 $= 3 sqrt(3) times 36 slash 2 = 54 sqrt(3)$。
  + $s = 21$（半周长）；由 $"面积" = s r$ 得 $r = 84 slash 21 = 4$。
  + 直角 $=>$ 斜边对应的圆心角 $= 180 degree$（由 Thales 的反向结论），故外接圆圆心是斜边中点，半径 $=$ 斜边 $slash 2$。
  + 等边三角形。因等边三角形三个顶点到中心距离相等、三条边到中心距离相等，而“重合”要求三心都是中心；反过来其它三角形里三心不重合。
  + 设切点分别为 $P$、$Q$、$R$、$S$（内切圆切四条边 $A B$、$B C$、$C D$、$D A$）。由“自圆外一点作圆的两条切线，切线长相等”得 $A P = A S$、$B P = B Q$、$C Q = C R$、$D R = D S$。于是 $A B + C D = A P + P B + C R + R D = A S + B Q + C Q + D S = A D + B C$。
  + 由 $R = a = $ 边长（正六边形是 $6$ 个等边三角形拼起来）；再由 $R^2 = r^2 + (a slash 2)^2$ 得 $r^2 = a^2 - a^2 slash 4 = 3 a^2 slash 4$，故 $r = (sqrt(3) slash 2) a$。
]
