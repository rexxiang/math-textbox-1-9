#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref, lab, answer-cut
#import "../../lib/diagram-packages.typ": cetz

=== 勾股定理与根号边长 <tool:ge04-pythagorean>

#vocab[直角三角形 right triangle][直角边 leg][斜边 hypotenuse][勾股定理 Pythagorean theorem][勾股逆定理 converse of the Pythagorean theorem][根号边长 radical length]

#crisis[
  直角三角形在工地、屏幕、栏杆、门窗里随处可见。它的三条边中，最长的那一条——*斜边*——到底和另外两条*直角边*有什么关系？

  把一张边长 $1$ 的正方形沿对角线剪开，两个直角三角形的斜边正是这条对角线。它的长度到底是多少？量一量大约是 $1.414...$——不是整数，不是分数，而是一个*写不尽的小数*。

  *两条直角边是 $a$、$b$，斜边会是 $a$、$b$ 的什么函数？* 这个函数能不能避开“要量”、只用 $a$、$b$ 之间的四则 $+$ 开方？
]

#tryit[
  直角三角形两直角边为 $6$、$8$。求斜边。
]

#discovery[
  直角三角形两直角边 $a = 3$、$b = 4$，斜边 $c$ 是多少？画出来量一量约是 $5$——整数，真巧。再换 $a = 5$、$b = 12$，量出约 $13$，又是整数。会不会"斜边 $=$ 两直角边之和的某个固定倍数"？$3 + 4 = 7$、$7 times ? = 5$，倍数 $approx 0.714$；$5 + 12 = 17$、$17 times ? = 13$，倍数 $approx 0.765$。

  再添一组 $a = 1$、$b = 1$：斜边 $approx 1.414$，$(1 + 1) times ? = 1.414$，倍数 $approx 0.707$。三组的"倍数"是 $0.714, 0.765, 0.707$——并不恒定。换"斜边 $= a + b - $ 某数"、"$= a^2 / b + $ 某数"……同样找不到一条全适用的公式。

  在加法、减法这一层翻来翻去都没结果。也许该问的是：把三条边*乘起来*，会不会出现固定关系？

  *一张拼图说明一切*

  如图 @fig-pythagorean-square-decomposition 所示。
  #figure(
    cetz.canvas(length: 1cm, {
      import cetz.draw: *
      // large square of side a+b with inscribed tilted square
      let a = 2.2
      let b = 1.4
      let s = a + b
      rect((0, 0), (s, s), stroke: 0.5pt)
      // four triangles arranged around tilted square (c, c, c, c)
      // vertices of tilted square
      let P1 = (b, 0)
      let P2 = (s, b)
      let P3 = (a, s)
      let P4 = (0, a)
      line(P1, P2, P3, P4, close: true, stroke: 0.6pt)
      // label legs a and b on bottom edge and right edge
      content((b / 2, -0.35), $b$)
      content((b + a / 2, -0.35), $a$)
      content((s + 0.35, b / 2), $b$)
      content((s + 0.35, b + a / 2), $a$)
      // label c on one tilted edge
      content(((b + s) / 2 + 0.3, b / 2 + 0.25), $c$)
    }),
    caption: [边长 $a + b$ 的大正方形 $=$ 四个直角三角形（两直角边 $a$、$b$，斜边 $c$）$+$ 中间一个边长 $c$ 的正方形。]
  ) <fig-pythagorean-square-decomposition>

  *两种方式算同一块面积*

  - *方法一*：大正方形面积 $= (a + b)^2 = a^2 + 2 a b + b^2$。
  - *方法二*：$4$ 个三角形的面积 $= 4 dot 1 / 2 a b = 2 a b$；中间那块是边长 $c$ 的正方形，面积 $= c^2$。合计 $2 a b + c^2$。

  两个表达式算的是同一块面积，所以

  $ a^2 + 2 a b + b^2 = 2 a b + c^2 => a^2 + b^2 = c^2. $

  *勾股逆定理*

  反过来：如果三角形三边 $a$、$b$、$c$ 满足 $a^2 + b^2 = c^2$，它必然是直角三角形（$c$ 所对的角 $= 90 degree$）。

  *证明思路*：给一个三边为 $a$、$b$、$c$ 的三角形 $T$。另作一个*直角*三角形 $T'$，两直角边分别为 $a$、$b$，它的斜边 $= sqrt(a^2 + b^2) = c$。$T$、$T'$ 三边分别相等，由 SSS 全等 $=>$ $T$ 与 $T'$ 全等 $=>$ $T$ 里对应 $T'$ 直角的那个角也是 $90 degree$。

  *边长不再只是整数*

  一旦允许开方，斜边就不再被“整数 / 分数”关住。边长 $1$ 的正方形的对角线 $= sqrt(2)$；边长 $1$ 的等边三角形的高 $= sqrt(3) slash 2$；直角边 $1$、$2$ 的直角三角形斜边 $= sqrt(5)$。它们都是*根号长度*——合法的几何长度，只是写不成分数。
]


#lab[
  *剪拼正方形：亲手验证 $a^2 + b^2 = c^2$*

  *材料*：硬卡纸、直尺、剪刀、铅笔。

  *步骤*：

  + 在卡纸上画一个直角三角形，两直角边分别取 $a = 3$ cm、$b = 4$ cm。量出斜边 $c$（应接近 $5$ cm）。
  + 分别以三条边为边长，画出三个正方形：$a times a$（$9$ cm²）、$b times b$（$16$ cm²）、$c times c$（$25$ cm²）。剪下来。
  + 把 $a times a$ 和 $b times b$ 两个小正方形剪成若干块（提示：一个不剪，另一个沿对角线或 T 形切两刀），尝试*拼满*大正方形 $c times c$。
  + 如果拼满成功，说明了什么？

  *进阶*：
  - 换一组直角边试试：$a = 5$ cm、$b = 12$ cm。先用勾股定理算出 $c$，再用剪拼验证。
  - 如果三角形*不是*直角三角形（比如三边 $4, 5, 7$），$a^2 + b^2$ 和 $c^2$ 还相等吗？用面积剪拼能看出什么？
]

#side-hack[
  *看到“直角”就反射三步*

  + 哪两条是直角边？哪一条是斜边？
  + 需要求哪条？如果求斜边，用 $c = sqrt(a^2 + b^2)$；如果求某条直角边，用 $a = sqrt(c^2 - b^2)$（必须 $c > b$）。
  + 如果结果含根号，记得先看能不能化简（$sqrt(8) = 2 sqrt(2)$、$sqrt(12) = 2 sqrt(3)$……回到 #secref("cb07-square-and-square-root")）。
]

#tryit[
  直角三角形斜边 $= 13$，一条直角边 $= 5$。求另一条直角边。
]

#history-note[
  这条定理的历史惊人地久远：古巴比伦泥板 Plimpton 322（约前 1800 年）上就记录了大量勾股数组；中国的《周髀算经》（成书年代有争议，约前 1100–前 100 年）记载了商高与周公的对话——"勾三股四弦五"；古希腊的毕达哥拉斯学派（Pythagoras，约前 570–495 年）把它上升为普适定理并给出了证明。三世纪时，中国数学家赵爽用"弦图"给出了一个漂亮的面积拼合证法。至今已知的独立证明超过 400 种。两条文明路径最终汇合到同一句结论：*两直角边平方之和 $=$ 斜边平方*。

  ☞ 历史接力 → 勾股定理是第一个被严格证明的几何定理之一；它在坐标系下化为两点距离公式，见 #secref("cb04-rectangular-system")。
]

#blueprint[
  *工具一：勾股定理*

  在直角三角形中，设两直角边为 $a$、$b$，斜边为 $c$：

  $ a^2 + b^2 = c^2. $

  用法要点：

  - 求斜边：$c = sqrt(a^2 + b^2)$。
  - 求直角边：$a = sqrt(c^2 - b^2)$（必须 $c > b$，否则题设有问题）。

  如图 @fig-right-triangle-pythagorean 所示。
  #figure(
    cetz.canvas(length: 1cm, {
      import cetz.draw: *
      let A = (0, 0)
      let B = (4, 0)
      let C = (0, 3)
      // Triangle
      line(A, B, C, close: true, stroke: 0.7pt)
      // Right angle mark at A
      rect((0, 0), (0.35, 0.35), stroke: 0.4pt, fill: none)
      // Labels
      content((-0.3, -0.25), $A$)
      content((4.3, -0.2), $B$)
      content((-0.3, 3.2), $C$)
      // Side labels
      content((2, -0.4), text(9pt)[$a$ (直角边)])
      content((-0.6, 1.5), text(9pt)[$b$ (直角边)])
      content((2.4, 1.9), text(9pt, fill: rgb("#B71C1C"))[$c$ (斜边)])
      // Formula
      content((2, -1.1), text(9pt)[$a^2 + b^2 = c^2$])
    }),
    caption: [直角三角形三边关系：两直角边 $a$、$b$ 的平方和等于斜边 $c$ 的平方。]
  ) <fig-right-triangle-pythagorean>

  *工具二：勾股逆定理*

  对任意三角形三边 $a$、$b$、$c$，若 $a^2 + b^2 = c^2$，则此三角形是直角三角形，$c$ 所对的角 $= 90 degree$。

  用法要点：题目只给三边长度时，用这条定理*判定*“是不是直角三角形”。

  *工具三：根号边长*

  几何长度不再被“整数 / 分数”限制。一般会遇到 $sqrt(2)$、$sqrt(3)$、$sqrt(5)$、$sqrt(6)$、$sqrt(8) = 2 sqrt(2)$、$sqrt(12) = 2 sqrt(3)$ 等。写答案时先把根号化到最简。

  *综合例*：矩形的两边长为 $6$、$8$。求它的对角线长。

  *解*：矩形对角线把它切成两个直角三角形（两直角边 $= 6, 8$）。由勾股定理：对角线 $= sqrt(36 + 64) = sqrt(100) = 10$。
]

#pitfall[
  *高频错误*

  ✗ 把“直角边”和“斜边”混了，写成 $c^2 + a^2 = b^2$
  → ✓ *斜边是最长的那条*，平方等于另外两条平方之和。题目没标时，先确认“直角对着哪条边”。

  ✗ 逆定理只用 $a^2 + b^2 >= c^2$ 这种弱条件
  → ✓ 逆定理要*等号*：$a^2 + b^2 = c^2$。如果 $a^2 + b^2 > c^2$ 说明角 $C$ 是锐角，$<$ 说明是钝角，但本章只用“等号 $<=>$ 直角”这一条。

  ✗ 求直角边时忘了*哪个是斜边*，硬套 $sqrt(a^2 + b^2)$
  → ✓ 斜边已知时要写成 $sqrt(c^2 - a^2)$，被减数是斜边平方。

  ✗ 答案停在 $sqrt(50)$ 不化简
  → ✓ 写到最简：$sqrt(50) = 5 sqrt(2)$。这是本章答题的默认要求。
]

#mastery[
  *基础*

  + 直角三角形两直角边 $5$、$12$。求斜边。
  + 直角三角形斜边 $17$、一条直角边 $8$。求另一条直角边。
  + 直角三角形两直角边 $1$、$2$。求斜边（写到最简根号）。

  *应用*

  + 矩形边长 $9$、$12$。求对角线长。
  + 三边为 $9$、$40$、$41$ 的三角形是直角三角形吗？如果是，直角对着哪条边？
  + 等腰直角三角形的腰（直角边）$= 1$。求斜边。

  *挑战*

  + 三角形三边为 $sqrt(3)$、$sqrt(5)$、$sqrt(8)$。判断它是否直角三角形；若是，直角对着哪条边？
  + 边长 $a$ 的等边三角形。求它的高 $h$（提示：把高作到一边，分成两个直角三角形）。
  + 矩形对角线 $= 10$，一边 $= 6$。求另一边与面积。

  + *[反直觉]*：已知三角形两边为 $3$ 和 $4$。第三边*一定*是 $5$ 吗？解释在什么条件下是 $5$，在什么条件下不是。
  + *[组合]*：坐标平面上两点 $A(1, 2)$ 和 $B(4, 6)$。用勾股定理求 $A B$ 的距离（提示：构造直角三角形）。
  + *[构造题]*：构造一个直角三角形，使两条直角边都是无理数，但斜边是整数。给出一个具体例子。

#answer-cut[

  *基础*
  + $c = sqrt(25 + 144) = sqrt(169) = 13$（勾股定理）。
  + 另一边 $= sqrt(17^2 - 8^2) = sqrt(289 - 64) = sqrt(225) = 15$（勾股定理）。
  + $c = sqrt(1 + 4) = sqrt(5)$（勾股定理）。

  *应用*
  + $sqrt(81 + 144) = sqrt(225) = 15$。
  + $9^2 + 40^2 = 81 + 1600 = 1681 = 41^2$，成立 $=>$ 是直角三角形，直角对着 $41$ 这条斜边。
  + 斜边 $= sqrt(1 + 1) = sqrt(2)$。

  *挑战*
  + $(sqrt(3))^2 + (sqrt(5))^2 = 3 + 5 = 8 = (sqrt(8))^2$，成立 $=>$ 是直角三角形，直角对着 $sqrt(8)$ 这条斜边。
  + 把高 $h$ 作到底边，底边被平分成长度 $a slash 2$ 的两段。用勾股：$h^2 + (a slash 2)^2 = a^2 => h^2 = a^2 - a^2 slash 4 = 3 a^2 slash 4 => h = (sqrt(3) slash 2) a$。
  + 另一边 $= sqrt(100 - 36) = sqrt(64) = 8$。面积 $= 6 times 8 = 48$。
  + *[反直觉]*：不一定是 $5$。只有当 $3^2 + 4^2 = 5^2$ 成立时才是直角三角形，第三边才是 $5$。若三角形不是直角三角形，第三边可以是 $1$ 到 $7$（三角不等式限制）之间除 $5$ 以外的任何值（勾股定理要求 + 三角不等式）。
  + 水平差 $4-1=3$，垂直差 $6-2=4$。$A B = sqrt(3^2 + 4^2) = sqrt(25) = 5$（勾股定理 + 坐标距离公式）。
  + *[构造题]*：取 $sqrt(2)$ 和 $sqrt(2)$，$sqrt{2}^2 + sqrt{2}^2 = 4$，斜边 $= 2$（整数）✓。或取 $sqrt(3)$ 和 $sqrt(6)$，$3 + 6 = 9$，斜边 $= 3$ ✓（勾股定理逆用 + 构造：令两直角边平方和为完全平方数）。
]
]
