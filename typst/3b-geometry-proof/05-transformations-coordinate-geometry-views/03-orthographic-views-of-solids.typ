#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref
#import "../../lib/diagram-packages.typ": cetz

== 立体的三视图 <tool:ge05-orthographic-views>

#vocab[正视图 front view / 侧视图 side view / 俯视图 top view / 三视图 orthographic views / 平行投影 parallel projection]

#crisis[
  立体的东西画在纸上，总要损失一个维度。一张照片会带来透视扭曲——远处的东西看起来比实际小。工程上需要一种*不扭曲*、*能复现原尺寸*的画法：从*正前方、正侧面、正上方*分别把立体投影在纸上，三张小图加起来要能让制图工人把它还原出来。

  *怎样用三张平面图像把一个立体物体的形状完整地表示出来？反过来，给三张图能否推回唯一的立体？*
]

#history-note[
  工程制图里沿用至今的“三视图”源自欧洲工业革命——机器零件需要在纸上传递给千里之外的车间。中国的机械制图国标规定：正视图放左上、侧视图放右上（通常是*左*侧视图）、俯视图放正下。这种标准化的布局让不同工厂之间可以互读图纸。
]

#discovery[
  *什么是三视图*

  把一个立体物体放进一个“玻璃盒子”，*分别从正前方、正侧方、正上方*用*平行光*垂直照射，物体在三个方向的盒壁上投下的轮廓，就是：

  - *正视图*（从前往后看）：长 $times$ 高；
  - *侧视图*（通常从左往右看）：宽 $times$ 高；
  - *俯视图*（从上往下看）：长 $times$ 宽。

  三张图共用“长”“宽”“高”三个尺寸，因此存在一条*对齐规律*：

  - *正视图 $<->$ 俯视图*：宽度（左右方向）对齐——同一条“长”。
  - *正视图 $<->$ 侧视图*：高度对齐——同一条“高”。
  - *侧视图 $<->$ 俯视图*：深度（前后方向）对齐——同一条“宽”。

  *长方体的三视图*

  #figure(
    cetz.canvas(length: 1cm, {
      import cetz.draw: *
      // front view: 4 × 2 rectangle
      rect((0, 0), (4, 2), stroke: 0.6pt)
      content((2, -0.35), [正视图（长 $4 times$ 高 $2$）])
      // side view: 3 × 2, to the right
      rect((5, 0), (8, 2), stroke: 0.6pt)
      content((6.5, -0.35), [侧视图（宽 $3 times$ 高 $2$）])
      // top view: 4 × 3, below front view
      rect((0, -3.5), (4, -0.5), stroke: 0.6pt)
      content((2, -3.85), [俯视图（长 $4 times$ 宽 $3$）])
    }),
    caption: [$4 times 3 times 2$ 长方体的三视图：正视 $4 times 2$、侧视 $3 times 2$、俯视 $4 times 3$。]
  )

  *由视图反推立体*

  给三张视图，能否*唯一*确定立体？对“由小立方体堆起来”的物体来说，三张视图加上对齐规律通常能定到相当精细——例外是少数“空腔”情形，这种情形靠本节所给信息可以确诊为歧义，须要额外信息。

  *视图面积与表面积的关系*

  对一个长方体（尺寸 $a times b times c$，长 $times$ 宽 $times$ 高）：

  - 三视图面积分别是 $a c$、$b c$、$a b$。
  - 它们的*和*是 $a c + b c + a b$。
  - 而长方体的*表面积*是 $2(a b + b c + a c)$。

  所以：*长方体的表面积 $= 2 times$（三视图面积之和）*。每张视图各对应长方体的一对“对立面”（前 / 后、左 / 右、上 / 下），每对两张面积相等——这就是 $2 times$ 的来源。

  对凹凸形物体（例如由小立方体拼出的 L 形、台阶形、带缺口的物体）这条关系*一般不成立*：视图只看*外轮廓*，把“朝内的面”都压掉了。只要物体的某条平行于坐标轴的直线能*两次进出物体*（比如“C”形或带凹槽的物体），就会有一部分外表面被视图漏记，导致 $2 times$ 视图面积之和*小于*真实表面积。本节把“$2 times$ 三视图面积之和”当作*长方体*的稳定公式；其他形状要回到“逐面计数”的朴素算法。
]

#side-hack[
  *先“看轮廓”再“填格子”*

  + 把物体对准坐标方向摆正；
  + 先画*俯视图的外框*——它决定“占地多大”；
  + 把正视图、侧视图的*外框*画出来，让它们的长、宽、高与俯视图对齐；
  + 再在外框里画*台阶 / 洞 / 缺角*——这些对应的是“凹凸”。

  “先框后细节”能避免最常见的那种“长宽对齐搞反”的翻车。
]

#tryit[
  + 一个棱长 $= 3$ 的正方体。它的三视图各是什么形状、多大？
  + 两个棱长 $= 1$ 的小立方体*左右*并排拼在一起。它的三视图各是什么？
  + 长方体尺寸 $= 5 times 3 times 2$（长 $times$ 宽 $times$ 高）。三视图的面积之和是多少？它的表面积呢？两者关系对上了吗？
]

#blueprint[
  *工具一：三视图（作为一个工具看）*

  - *正视图*：从前往后看，保留长 $times$ 高。
  - *侧视图*：从左（或右）往内看，保留宽 $times$ 高。
  - *俯视图*：从上往下看，保留长 $times$ 宽。

  *对齐规律*：正视 $<->$ 俯视 同“长”，正视 $<->$ 侧视 同“高”，侧视 $<->$ 俯视 同“宽”。

  *工具二：由三视图还原立体*

  - 用俯视图决定*占地形状*；
  - 用正视图 $+$ 侧视图决定每个占地格上*叠了多少层*；
  - 对由小立方体拼出的物体：俯视图每格内标一个数字（表示那一堆上有多少个立方体）时，正、侧视图就完全确定；反之给三视图，能定到的颗粒度就是“每格高度”，此时唯一（除非允许“空腔”）。

  *工具三：长方体表面积 $= 2 times$（三视图面积之和）*

  对尺寸 $a times b times c$ 的长方体：三视图面积之和 $= a b + b c + a c$；表面积 $= 2 (a b + b c + a c)$。

  理由：每张视图 $=$ 一对*对立外表面*各自的面积，两张面积相等，所以*表面积 $= 2 times$ 三视图面积之和*。

  （对凹凸形、带缺口、带洞的物体，视图会把朝内的面压掉，上式*一般不成立*；这时应回到“逐面计数 / 去掉黏合面”的朴素算法，见本节 §挑战 #3。）

  *综合例*：长方体 $6 times 4 times 3$。

  - 三视图面积：$6 times 3 = 18$、$4 times 3 = 12$、$6 times 4 = 24$；和 $= 54$。
  - 表面积 $= 2 (6 times 4 + 4 times 3 + 6 times 3) = 2 (24 + 12 + 18) = 108 = 2 times 54$。✓
]

#pitfall[
  *高频错误*

  ✗ 正视图和侧视图互换
  → ✓ *正视图沿“长 $times$ 高”*，侧视图沿“宽 $times$ 高”。常用方法是先确定“哪条边是长 / 宽 / 高”，再对应。

  ✗ 把透视收缩（像照片里远处变小）当三视图
  → ✓ 三视图使用*平行投影*，不会有近大远小的收缩。三张图里的尺寸都是物体在该方向上的真实长度。

  ✗ 对任意凹凸物体都用“表面积 $= 2 times$ 三视图面积之和”
  → ✓ 该公式只对*长方体*（更一般地，沿三条坐标轴方向都“凸”的轴对齐物体）成立。一旦物体有凹槽、缺口、C 形或空洞，视图会漏掉朝内的面，$2 times$ 视图面积之和会*小于*真实表面积。没把握时回到“逐面计数 / 去掉黏合面”的朴素算法。

  ✗ 给三视图却忽略“每格立方体数量”这种潜在歧义
  → ✓ 只有三张*轮廓图*时，内部的堆叠可能有几种方案都吻合。碰到“还原立体”的题目，要么题目额外给俯视图上的数字标注，要么在答案里写明“可能不唯一”。
]

#mastery[
  *基础*

  + 棱长 $= 4$ 的正方体的三视图各是什么形状、多大？
  + 长方体 $7 times 4 times 3$（长 $times$ 宽 $times$ 高）。求三视图面积与它们的和。
  + 圆柱（底半径 $r = 2$、高 $h = 5$）的三视图各是什么形状？

  *应用*

  + 两个棱长 $= 1$ 的小立方体，前后并排拼在一起（沿“宽”方向）。写出三视图。
  + 长方体 $6 times 4 times 5$。计算表面积，并用“$2 times$ 三视图面积之和”核对。
  + 三视图都是边长 $= 4$ 的正方形，能推出立体是什么？尺寸多大？

  *挑战*

  + 三张视图：正视图是 $3 times 2$ 矩形、侧视图是 $4 times 2$ 矩形、俯视图是 $3 times 4$ 矩形。推出立体形状与尺寸；求表面积。
  + 把 $4$ 个棱长 $= 1$ 的小立方体拼成一个 L 形：在一条 $3$ 连排的底面上再把第 $1$ 个上方叠 $1$ 个（看作一行 $3$ 个立方体，左端上方再叠 $1$ 个）。画出它的三视图（用方格描述即可，注明形状）。
  + 上题的 L 形物体表面积是多少？（提示：$4$ 个立方体每个表面积 $6$，每对相邻共用面在合体后各去掉两个单位面。）

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + 三张视图都是边长 $= 4$ 的正方形。
  + 正视 $= 7 times 3 = 21$；侧视 $= 4 times 3 = 12$；俯视 $= 7 times 4 = 28$；和 $= 61$。
  + 正视图 / 侧视图都是矩形 $4 times 5$（直径 $times$ 高）；俯视图是半径 $= 2$ 的圆。

  *应用*
  + 正视图 $= 1 times 1$ 正方形；侧视图 $= 2 times 1$ 矩形；俯视图 $= 1 times 2$ 矩形。
  + 表面积 $= 2 (6 times 4 + 4 times 5 + 6 times 5) = 2 (24 + 20 + 30) = 148$。三视图面积和 $= 24 + 20 + 30 = 74$；$2 times 74 = 148$ ✓。
  + 三视图都是边长 $4$ 的正方形 $=>$ 长、宽、高都是 $4$ $=>$ 棱长 $4$ 的*正方体*。

  *挑战*
  + 长 $= 3$、高 $= 2$（来自正视图）；宽 $= 4$（来自侧视图的“$4 times 2$”的 $4$，以及俯视图的 $3 times 4$ 的 $4$）。长方体 $3 times 4 times 2$，表面积 $= 2 (3 times 4 + 4 times 2 + 3 times 2) = 2 (12 + 8 + 6) = 52$。
  + 正视图：一条底排 $3$ 个方格，再在*左上方*加 $1$ 个方格（“L 倒着”的形状）。俯视图：$1 times 3$ 的一行 $3$ 个方格。侧视图：左端是 $2$ 层、其余 $1$ 层，即一个 $2 times 1$ 矩形带左上角 $1 times 1$ 的凸起——化简后就是 $1 times 2$ 的矩形（宽 $1$、高 $2$）。
  + $4$ 个小立方体每个表面积 $= 6$；相邻立方体共用 $3$ 对黏合面（底排 $3$ 个相邻共 $2$ 对，加上左端竖叠 $1$ 对），每对去掉 $2$ 个单位面 $=>$ 总表面积 $= 4 times 6 - 3 times 2 = 24 - 6 = 18$。*对凹凸物体必须用这种“逐面计数 / 去掉黏合面”的朴素算法*——“$2 times$ 三视图面积之和”只能对长方体使用。
]
