#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref
#import "../../lib/diagram-packages.typ": cetz

=== 刚体变换与全等 <tool:ge05-rigid-transforms>

#vocab[刚体变换 rigid motion / 平移 translation / 旋转 rotation / 反射（关于直线）reflection / 保距 distance-preserving / 对应点 corresponding points]

#crisis[
  把一张三角形纸片往右挪一段——它和原来相等；转一个角度——还相等；对着一条直线翻个面——依然相等。这三种“动它”的办法长得完全不一样，为什么得到的都是*全等*的图形？

  到底是哪条共同的性质让它们“同族”？能不能给这整族变换一个统一的定义，而不是每次都另起炉灶？

  *平移、旋转、反射这三种“整体移动”，背后是不是同一个东西？*
]

#history-note[
  欧几里得几何里的“全等”其实一直就藏着“可以通过这几种动作让它们重合”的想法；近代数学把这件事正式化，叫“等距变换”或“刚体运动”。今天我们认为*全等 $<=>$ 能通过一组刚体变换叠合*——这不是新结论，而是对“全等”定义的*另一种说法*。
]

#discovery[
  #figure(
    cetz.canvas(length: 1cm, {
      import cetz.draw: *
      // original triangle
      let A = (0, 0)
      let B = (1.2, 0)
      let C = (0.6, 1.0)
      line(A, B, C, close: true, stroke: 0.6pt)
      content((A.at(0) - 0.2, -0.25), $A$)
      content((B.at(0) + 0.2, -0.25), $B$)
      content((C.at(0), C.at(1) + 0.25), $C$)
      // translation by (3, 0)
      let A2 = (3.5, 0)
      let B2 = (4.7, 0)
      let C2 = (4.1, 1.0)
      line(A2, B2, C2, close: true, stroke: 0.6pt + rgb("#1565C0"))
      content((A2.at(0) - 0.2, -0.25), [$A'$])
      content((B2.at(0) + 0.2, -0.25), [$B'$])
      content((C2.at(0), C2.at(1) + 0.25), [$C'$])
      // arrows showing corresponding-point links
      line(A, A2, stroke: 0.3pt + luma(160), mark: (end: ">"))
      line(B, B2, stroke: 0.3pt + luma(160), mark: (end: ">"))
      line(C, C2, stroke: 0.3pt + luma(160), mark: (end: ">"))
      content((2, -0.6), [平移向量 $(3.5, 0)$])
    }),
    caption: [平移：每一点都沿同一方向、同一距离移动；对应点连线互相平行且等长。]
  )

  *三种变换的共同点*

  把三角形 $A B C$ 变成 $A' B' C'$ 的三种*保形*方式：

  - *平移*：沿一个*固定方向、固定距离*把整个图形挪走。每一对*对应点*连线都平行且等长。
  - *旋转*：绕一个*固定点*（旋转中心）把整个图形转动一个*固定角度*。每一对对应点到中心的距离相等，连线与中心的夹角都等于同一个角度。
  - *反射*：关于一条*固定直线*（对称轴）把整个图形翻过去。每一对对应点关于这条直线对称——中点在轴上，连线垂直于轴。

  *三种里共通的那条性质*

  任取两点 $P$、$Q$ 及其像 $P'$、$Q'$，三种变换都使得

  $ P' Q' = P Q. $

  换句话说，*任意两点的距离在变换前后完全不变*。这就是*保距*。

  *保距带来什么？*

  + 所有边长不变 $=>$ 对应边相等；
  + 同一对边夹的角也不变 $=>$ 对应角相等（角大小由三条边长通过余弦式/相似原理决定，而所有长度都没变）；
  + 面积不变。

  所以：*经过任意平移、旋转、反射之后的图形，和原图形全等*。而且反过来也成立：两个全等图形之间，总能找到平移、旋转、反射（或它们的复合）让它们重合。

  *三种变换的区别在哪里*

  + 平移：没有*不动点*（除非是零平移）。
  + 旋转：恰好一个不动点——旋转中心本身。
  + 反射：一整条不动直线——对称轴上所有点都不动；而且它会*翻转方向*（左手变右手），平移和旋转不会。
]

#side-hack[
  *三条变换，一张对照*

  #table(
    columns: 4,
    inset: 5pt,
    stroke: 0.4pt,
    align: center,
    [*类型*], [*特征参数*], [*对应点连线*], [*不动点*],
    [平移], [一个向量], [互相平行且等长], [无],
    [旋转], [中心 $+$ 角度], [到中心等距，夹角相等], [中心一个],
    [反射], [一条对称轴], [中点在轴上，垂直于轴], [整条轴],
  )
]

#tryit[
  + 在方格纸上画 $triangle A B C$，顶点 $A(0, 0)$、$B(3, 0)$、$C(1, 2)$。把它向右平移 $4$、向上平移 $1$ 个单位，画出像三角形 $triangle A' B' C'$。
  + 在同一张方格纸上，把原来的 $triangle A B C$ 关于直线 $x = 0$（即 $y$-轴）反射，画出 $triangle A'' B'' C''$。它的顶点坐标？
  + 任意取平面上一对不重合的点 $P$、$Q$，又给 $triangle A B C$ 关于某条直线做一次反射得到 $triangle A' B' C'$；那么 $P Q$ 与 $P' Q'$ 的关系是？（提示：依靠保距性质。）
]

#blueprint[
  *工具一：刚体变换的统一定义*

  平移、旋转、反射都叫*刚体变换*（或*保距变换*）——它们都把两点间的距离保持不变：

  $ "变换前" quad P Q = "变换后" quad P' Q' quad ("任意两点"). $

  由此自动得到：对应边相等、对应角相等、面积相等。

  *工具二：三种变换的参数*

  - *平移* 由一个*向量*决定（方向 $+$ 距离）。对应点满足“像 $-$ 原”$=$ 同一向量。
  - *旋转* 由*旋转中心 $O$* 与*旋转角 $theta$*（带方向：逆时针为正）决定。对任意点 $P$ 的像 $P'$：$O P = O P'$，$angle P O P' = theta$（方向一致）。
  - *反射* 由*对称轴 $ell$* 决定。对任意点 $P$ 的像 $P'$：线段 $P P'$ 垂直于 $ell$，且中点在 $ell$ 上。

  *工具三：刚体变换与全等的等价性*

  - 经过任意一组刚体变换（或它们的复合）后的图形与原图形*全等*；
  - 反过来，两个全等的图形之间*总能*找到一组刚体变换让它们完全重合。

  于是*全等*可以等价地理解为“存在一组刚体变换把一个变成另一个”。

  *综合例*：$triangle A B C$ 关于某条直线反射得到 $triangle A' B' C'$。已知 $A B = 5$、$B C = 7$、$A C = 9$，$angle A = 40 degree$。判断 $A' B'$、$B' C'$、$A' C'$ 的长度与 $angle A'$ 的大小。

  *解*：反射是刚体变换 $=>$ 保距、保角：$A' B' = 5$、$B' C' = 7$、$A' C' = 9$、$angle A' = 40 degree$。
]

#pitfall[
  *高频错误*

  ✗ 旋转题把*顺时针*与*逆时针*搞反
  → ✓ 默认*逆时针*为正方向。顺时针 $60 degree$ 等同逆时针 $300 degree$；一定在图里标清方向再动笔。

  ✗ 反射题把“关于 $y = 0$”和“关于 $x = 0$”搞反
  → ✓ $y = 0$ 是 $x$-轴；$x = 0$ 是 $y$-轴。关于 $x$-轴反射 $(x, y) -> (x, -y)$；关于 $y$-轴反射 $(x, y) -> (-x, y)$。

  ✗ 把“放缩”也算刚体变换
  → ✓ 缩放不保距 $=>$ 不是刚体变换。相似 $≠$ 全等。本节只谈三种保距变换。

  ✗ 对应点连线一律写成“互相平行”
  → ✓ 只有*平移*的对应点连线互相平行；旋转的连线都过*中心*（它们共点），反射的连线则都*垂直于对称轴*。三种变换的“连线关系”各不相同。
]

#mastery[
  *基础*

  + 在方格纸上画 $triangle A B C$，$A(-1, 1)$、$B(2, 1)$、$C(0, -2)$，再按平移向量 $(2, 3)$ 平移，写出像三角形的三个顶点坐标。
  + 把上题的 $triangle A B C$ 关于 $x$-轴反射，写出像三角形三个顶点坐标。
  + 把 $triangle A B C$（同上）关于原点旋转 $180 degree$，写出像三角形三个顶点坐标。

  *应用*

  + 刚体变换 $T$ 把 $P(1, 3)$ 变到 $P'(4, 3)$，把 $Q(1, 5)$ 变到 $Q'(4, 5)$。$T$ 最可能是什么变换？并写出它的特征参数。
  + $triangle A B C$ 和 $triangle A' B' C'$ 全等；$A A'$、$B B'$、$C C'$ 三条线段互相平行且等长。判断从 $triangle A B C$ 到 $triangle A' B' C'$ 最可能的刚体变换是什么。
  + 对于关于 $y$-轴的反射，点 $(3, -2)$ 的像是什么？

  *挑战*

  + 先将 $triangle A B C$ 关于 $x$-轴反射，再将像关于 $y$-轴反射。这两步复合之后，整体上相当于什么单一变换？
  + 对一张三角形纸片做*两次反射*（关于两条相交直线，交角 $= theta$）。最后得到的三角形，相对于原三角形整体是什么变换？（提示：它还是全等的——那是平移还是旋转？角度是多少？）
  + 写出能把 $triangle A B C$（顶点 $A(0, 0)$、$B(1, 0)$、$C(0, 1)$）变到 $triangle A' B' C'$（顶点 $A'(2, 1)$、$B'(3, 1)$、$C'(2, 2)$）的一个刚体变换。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + $A'(1, 4)$、$B'(4, 4)$、$C'(2, 1)$。
  + $A(-1, -1)$、$B(2, -1)$、$C(0, 2)$。
  + $A(1, -1)$、$B(-2, -1)$、$C(0, 2)$。

  *应用*
  + 两对对应点的连线都是 $(3, 0)$，一样——说明是*平移*，平移向量 $= (3, 0)$。
  + 对应点连线互相平行且等长 $=>$ 平移；平移向量就是 $A A'$（即 $B - A$ 对应到 $B' - A'$ 的那条）。
  + $(3, -2)$ 关于 $y$-轴反射 $=>$ $(-3, -2)$。

  *挑战*
  + 关于 $x$-轴反射后 $(x, y) -> (x, -y)$；再关于 $y$-轴反射 $-> (-x, -y)$。整体上相当于*关于原点旋转 $180 degree$*。
  + 两次反射（关于两条夹角 $theta$ 的直线）的复合 $=$ 绕两轴交点的*旋转*，旋转角 $= 2 theta$。
  + 按*平移向量 $(2, 1)$* 平移即可。验证：$A(0, 0) -> (2, 1) = A'$、$B(1, 0) -> (3, 1) = B'$、$C(0, 1) -> (2, 2) = C'$，三对对应点全部吻合。该平移是满足对应关系的最简刚体变换；也可写成“关于 $y = x$ 反射 $+$ 关于 $y = x + 3 slash 2$ 反射”等两次反射的复合，但平移是最直接的答案。
]
