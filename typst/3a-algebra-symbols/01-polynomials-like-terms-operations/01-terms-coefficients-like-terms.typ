#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref

=== 单项式、多项式与同类项 <tool:al01-terms-like>

#vocab[单项式 monomial][系数 coefficient][次数 degree][多项式 polynomial][项 term][同类项 like terms]

#crisis[
  在 #secref("cb04-expressions-and-like-terms") 里，你已经会说“$3x$ 和 $2x$ 是同类项”“$3x + 2x = 5x$”。但真正开始学代数之后，式子会很快长成这样：

  $ -5 a^2 b + 7 a b^2 - a^2 b + 4 - a b^2 $

  这时“一眼看出谁是同类项”已经不够——你得先回答：

  - 每一“块”到底是什么？它的*系数*是多少？有几个字母？每个字母的*次数*是多少？
  - 整个式子一共有几项？最高项的次数有多大？
  - 两项要怎么比才叫“真同类”，而不是“看起来差不多”？

  我们需要给“一块式子”和“一串式子”各自起正式的名字，并把判定同类项的标准收紧。
]

#history-note[
  “单项式/多项式”这套名字来自拉丁语 _monomium / polynomium_，字面就是“一个名”和“多个名”。在韦达把字母引进算式之前，欧洲数学家还习惯用整句话描述关系；一旦字母可以当数，就需要这套更细的分类来讨论“谁能和谁合并”。
]

#discovery[
  *一块 vs 一串*

  先只看没有加减号把它切开的*一块*式子：

  $ 7, quad -3 x, quad a^2 b, quad -5 a^2 b, quad (1/2) x y^2 $

  它们都是“数字 × 若干字母的乘积”这种形状。这样的式子，叫作*单项式*。

  每个单项式都可以拆成两部分看：

  - *系数*：前面那个数（含正负号），如 $-5 a^2 b$ 的系数是 $-5$。
  - *次数*：所有字母指数之和，如 $-5 a^2 b$ 的次数是 $2 + 1 = 3$。

  约定：纯数字 $7$ 也是单项式，它的次数是 $0$；而系数是 $1$ 或 $-1$ 时通常省略写，$-a^2 b$ 的系数是 $-1$。

  再看*一串*用加减号连起来的式子：

  $ -5 a^2 b + 7 a b^2 - a^2 b + 4 - a b^2 $

  几个单项式的和，叫作*多项式*。每个单项式叫作它的一*项*（符号连在项的前面，$-a^2 b$ 是一项，而不是 $a^2 b$）。项数就是用加减号数出来的块数：这里是 $5$ 项。多项式的*次数*，规定为各项次数里的最大值；上面式子最高次项是 $3$ 次，所以这是一个三次五项式。

  *“真同类项”的判据*

  两项要互为同类项，必须：

  + *出现的字母完全一样*（比如都只含 $a, b$，不多也不少）；
  + *每个字母的次数分别相等*（$a^2 b$ 和 $a b^2$ 不同类——前者 $a$ 为 2 次，后者 $a$ 为 1 次）。

  系数可以不同，正负号也可以不同。常数项之间一律是同类项。按这条判据重新看开头那串：

  - $-5 a^2 b$ 与 $-a^2 b$ 同类；
  - $7 a b^2$ 与 $-a b^2$ 同类；
  - $4$ 是常数项，只跟其它常数项同类。
]

#side-hack[
  “同类”只看字母骨架，不看系数。正负号属于系数，不影响是否同类。
]

#tryit[
  自己先写下答案再往下看。

  + 把 $-(2/3) x^3 y$ 的系数、次数各说一遍。
  + 指出 $8 m^2 - 3 m n + m - 5$ 共有几项，并说出整个多项式的次数。
  + 在 $3 x^2 y, " " -x y^2, " " 7 x^2 y, " " 4 y^2 x, " " -2 x^2$ 里，哪些两两互为同类项？
]

#blueprint[
  *工具一：单项式 + 它的系数与次数*

  - 形如“数字 $times$ 若干字母幂”的一块式子叫*单项式*。
  - *系数* = 前面的数（含正负号）；省略写的时候系数默认是 $1$ 或 $-1$。
  - *次数* = 所有字母指数之和；纯数字（非零）的次数为 $0$。

  *例 1*：$-4 x y^2$ 的系数是 $-4$，次数是 $1 + 2 = 3$。

  *工具二：多项式 + 项数 + 次数*

  - 几个单项式相加减连起来的式子叫*多项式*；每个单项式叫它的一项。
  - 项数 = 用加减号数出的块数；符号属于它后面的项。
  - 多项式的次数 = 所有项次数的最大值。

  *例 2*：$2 x^3 - x^2 + 5 x - 7$ 是一个四项、三次的多项式；它最高次项是 $2 x^3$。

  *工具三：同类项（精确判据）*

  两项是同类项 $<=>$ 两项中字母部分完全一致（字母种类相同、每个字母指数分别相等）。常数项彼此都是同类项。

  *例 3*：$-5 a^2 b$ 和 $(1/2) a^2 b$ 是同类项（都是 $a^2 b$ 结构）；而 $a^2 b$ 与 $a b^2$ *不是*同类项（$a$ 的次数不同）。
]

#pitfall[
  *高频错误*

  ✗ 把 $-a^2 b$ 的系数记成 $1$
  → ✓ 省略写时系数是 $-1$，别漏了负号。

  ✗ 看到两项都带 $a, b$ 就当同类，比如把 $a^2 b$ 和 $a b^2$ 合并
  → ✓ 每个字母的指数必须分别相等，只字母种类相同是不够的。

  ✗ 把多项式 $2 x^3 - x^2 + 5 x - 7$ 的次数说成 $3+2+1+0=6$
  → ✓ 多项式的次数是*各项次数的最大值*，不是求和，这里是 $3$。
]

#mastery[
  *基础*

  + 写出下列单项式的系数和次数：$-6 p q$、$(1/4) x^2 y^3$、$-a^3$、$9$。
  + 指出 $4 u^2 v - u v + v - 2$ 的项数、每项的系数、多项式的次数。
  + 下列各组里哪些是同类项：
    - $3 m n$ 与 $-5 n m$
    - $2 p^2 q$ 与 $2 p q^2$
    - $-7$ 与 $(1/3)$
    - $x y^2$ 与 $x^2 y$

  *应用*

  + 在多项式 $-3 s t^2 + 5 s^2 t - s t^2 + 2 s^2 t - 4$ 中，把互为同类项的项用下划线分别圈出，并写出合并后的结果。
  + 某单项式的系数是 $-2$，次数是 $4$，且只含字母 $x, y$。请写出两种不同的可能写法。
  + 判断：若两个单项式次数相同，它们一定是同类项。给出判断并举例说明。

  *挑战 ☞ 选做*

  + 已知 $-5 x^a y^3$ 与 $2 x^2 y^b$ 是同类项，求 $a + b$ 的值。
  + 多项式 $k x^2 - 3 x + 1$ 中，$k$ 取什么值时它是一个真正的二次多项式？$k$ 取什么值时它会降为一次？
  + 设计一个三项式，使它同时满足：次数是 $4$、系数之和为 $0$、且含有 $a, b$ 两个字母。写出一种合适的例子，并说明它为什么成立。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + $-6 p q$：系数 $-6$，次数 $2$；$(1/4) x^2 y^3$：系数 $1/4$，次数 $5$；$-a^3$：系数 $-1$，次数 $3$；$9$：系数 $9$，次数 $0$。
  + 四项；系数分别为 $4, -1, 1, -2$；次数为 $3$。
  + $3 m n$ 与 $-5 n m$ 同类；$-7$ 与 $1/3$ 同类；其余两组不同类。

  *应用*
  + $-3 s t^2$ 与 $-s t^2$ 同类；$5 s^2 t$ 与 $2 s^2 t$ 同类。合并后 $-4 s t^2 + 7 s^2 t - 4$。
  + 例如 $-2 x^3 y$ 与 $-2 x y^3$（次数都是 $4$，系数都是 $-2$）。
  + 判断错误；反例 $x^2 y$ 与 $x y^2$ 次数都是 $3$，但不是同类项。

  *挑战 ☞ 选做*
  + 必须 $a = 2, b = 3$，所以 $a + b = 5$。
  + $k != 0$ 时是真正的二次多项式；$k = 0$ 时降为一次多项式 $-3 x + 1$。
  + 例如 $5 a^3 b - 8 a^2 b^2 + 3 a b^3$：每一项次数都是 $4$，系数和 $5 - 8 + 3 = 0$，两个字母都出现。
]
