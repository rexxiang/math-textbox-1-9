#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref, answer-cut
#import "../../lib/diagram-packages.typ": cetz

=== 多项式、项数与次数 <tool:al01-polynomials-and-degree>

#vocab[多项式 polynomial][项 term][项数 number of terms][次数 degree]

#crisis[
  你现在能拆解一个单项式了（见 #secref("al01-monomials-coefficient-degree")）。但代数里真正让人头疼的，是好几个单项式拼在一起的情况：

  $ -5 a^2 b + 7 a b^2 - a^2 b + 4 - a b^2 $

  面对这串式子，你需要知道：

  - 它一共有几"块"？每"块"是否都是单项式？
  - 整个式子的"最高阶"是多少——说它是"三次式"还是"五次式"？

  没有正式定义，这些问题没办法精确回答，也没办法系统地把它整理化简。
]

#tryit[
  先自己作答，再往下读。

  + 数一数上面那串式子里用加减号分隔的"块"有几个。
  + 其中最"高阶"的块是哪个？你怎么判断？
]

#discovery[
  *从单项式的"和"到多项式*

  上一节说，单项式是"数 × 字母幂"的一块。现在把若干个这样的块用加减号连起来，就得到了*多项式*。

  每个单独的块（连同它前面的加减号）叫作多项式的一*项*。规则是：*符号属于它后面的那项*——所以 $-a^2 b$ 是一项（系数是 $-1$），不是 $+a^2 b$。

  *项数*就是用加减号数出来的块数：

  $ underbrace(-5 a^2 b)_1 + underbrace(7 a b^2)_2 - underbrace(a^2 b)_3 + underbrace(4)_4 - underbrace(a b^2)_5 $

  这里有 $5$ 项。

  *多项式的次数*，规定为各项次数里的最大值。上面 $5$ 项的次数分别是 $3, 3, 3, 0, 3$，最大值是 $3$——这是一个*三次五项式*。

  #figure(
    cetz.canvas(length: 0.45cm, {
      import cetz.draw: *

      let blue = rgb("#1565C0")
      let green = rgb("#2E7D32")
      let orange = rgb("#E65100")
      let gray = luma(130)

      // Title
      content((9, 9.5), text(size: 9pt, weight: "bold")[$2 x^3 - x^2 + 5 x - 7$ 的结构], anchor: "south")

      // Term boxes
      let terms = ($2 x^3$, $-x^2$, $5 x$, $-7$)
      let degrees = ([次 3], [次 2], [次 1], [次 0])
      let colors = (rgb("#BBDEFB"), rgb("#C8E6C9"), rgb("#FFF9C4"), rgb("#FFCCBC"))
      let xs = (0, 4.5, 9, 13.5)

      for i in range(4) {
        let x0 = xs.at(i)
        rect((x0, 4), (x0 + 3.8, 7.5), fill: colors.at(i), stroke: 0.8pt + gray, radius: 3pt)
        content((x0 + 1.9, 6.4), text(size: 10pt, weight: "bold")[#terms.at(i)])
        content((x0 + 1.9, 4.9), text(size: 7pt, fill: gray)[#degrees.at(i)])
      }

      // Arrow pointing to max degree
      line((1.9, 7.5), (1.9, 8.5), stroke: 1pt + blue, mark: (end: ">"))
      content((1.9, 8.7), text(size: 7.5pt, fill: blue)[最高次项], anchor: "south")

      // Brace for "degree of polynomial = 3"
      content((8.7, 3.3), text(size: 8pt, fill: blue)[多项式次数 = 各项次数最大值 = $3$])
    }),
    caption: [$2 x^3 - x^2 + 5 x - 7$：四项、三次多项式],
  )

  #side-hack[
    *特殊名称*：两项的叫*二项式*，三项的叫*三项式*，更多项直接说"$n$ 项式"；最高次数是几就叫"几次多项式"。单项式不计入"多项式"这个词，但在运算中可以当作"只有一项的多项式"处理。
  ]
]

#blueprint[
  *多项式与它的项*

  *多项式*（polynomial）：几个单项式用加减号连起来构成的式子。每个单项式叫作它的一*项*，符号与后面的项绑在一起。

  *项数* = 用加减号分出的块数。

  *多项式的次数* = 所有项次数的最大值（不是各项次数的和！）。

  *例 1*：$2 x^3 - x^2 + 5 x - 7$ 是四项、三次多项式，最高次项是 $2 x^3$。

  *例 2*：$4 u^2 v - u v + v - 2$ 是四项多项式，次数是 $max(3, 2, 1, 0) = 3$。

  *例 3*：$-x + 3$ 是二项、一次多项式（也叫*一次二项式*）。
]

#pitfall[
  ✗ $2 x^3 - x^2 + 5 x - 7$ 的次数是 $3 + 2 + 1 + 0 = 6$（把各项次数相加）

  → ✓ 多项式的次数是*各项次数的最大值*，这里是 $3$，不是 $6$。

  ✗ $-x^2 + 3$ 里 $-x^2$ 这一项的系数是 $1$（漏了负号）

  → ✓ 符号随项走，系数是 $-1$。

  ✗ 单项式 $5 x^2$ 是"零项式"（因为只有一项，项数是 $1$ 不是 $0$）

  → ✓ 单项式的项数是 $1$（不是零，它本身就是一项），只是一般不把单项式叫"多项式"。
]

#mastery[
  *基础*

  + 指出 $4 u^2 v - u v + v - 2$ 的项数、每项的系数、以及多项式的次数。
  + 写出一个满足以下条件的多项式：三项、二次、含字母 $s$ 和 $t$。
  + 判断对错并解释原因：

    "多项式 $x^3 - x^2 + x - 1$ 的次数是 $3 + 2 + 1 + 0 = 6$。"

  *应用*

  + 某多项式有三项，次数是 $4$，最高次项的系数是 $-3$，另两项分别是 $x$ 和常数 $5$。写出这个多项式的一种可能形式。
  + 把多项式 $p^3 - 2 p^2 q + p q^2 - q^3$ 的项数和次数各说一遍，并说出最高次项是哪个（如果有多个同次项，全都列出来）。

  *挑战 ☞ 选做*

  + 一个多项式的各项次数分别为 $4, 3, 2, 1, 0$。是否一定有 $5$ 项？举出一个次数是 $4$ 但项数少于 $5$ 的多项式，以及一个次数是 $4$ 但项数超过 $5$ 的多项式。

  #answer-cut[
    *基础*
    + 四项；系数分别是 $4, -1, 1, -2$；次数是 $max(3, 2, 1, 0) = 3$。
    + 例如 $3 s^2 t - 2 s t + 1$（三项、最高次 $3$）——注意题目要求"二次"，要保证最高次项次数恰好是 $2$，例如 $s^2 - s t + 4$（次 $2$，三项，含 $s$ 和 $t$）。
    + 错。次数是各项次数的*最大值*，即 $3$，不是 $6$。

    *应用*
    + 例如 $-3 x^2 y^2 + x + 5$（最高次 $4$，系数 $-3$，另两项 $x$ 和 $5$）。
    + 四项；次数 $= max(3, 3, 3, 3) = 3$；四项次数均为 $3$，最高次项有四个：$p^3, -2 p^2 q, p q^2, -q^3$。

    *挑战 ☞ 选做*
    + 不一定有 $5$ 项。次数 $4$ 但项数少于 $5$：例如 $x^4 + 1$（只有两项，次数 $4$）。次数 $4$ 但项数超过 $5$：例如 $x^4 + x^3 + x^2 + x + 1 + x^0 + x^2 = x^4 + x^3 + 2 x^2 + x + 1$（这已化简为 $5$ 项），所以更简单的做法是 $x^4 + a^3 b + a^2 b^2 + a b^3 + b^4 + a^3 b^2$（六项，最高次 $4$）——只要各项字母不同就不会合并。
  ]
]
