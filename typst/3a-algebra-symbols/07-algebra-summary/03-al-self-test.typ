#import "../../lib/theme-v2.typ": blueprint, side-hack, tryit, secref

=== 代数分支混合自测 <meta:al-self-test>

#blueprint[
  *做题方式*

  下面 $6$ 道题覆盖代数分支的主要动作：因式分解、分式方程、不等式、二次方程、应用建模。每题末尾标注主要用到的章节链；动笔前先想清"这题会动用哪几样工具"，做完后对照下方答案。

  #line(length: 100%, stroke: 0.3pt + luma(200))

  + 化简 $frac(x^2 - 4, x^2 - 4 x + 4)$，并写出*未化简前*哪些 $x$ 取值是被禁止的。（#secref("al02-special-identities")、#secref("al03-rational-expression-structure")）

  + 解方程 $frac(2, x - 1) - frac(1, x + 1) = frac(3, x^2 - 1)$，请*显式*写出验根步骤。（#secref("al03-rational-equations-and-extraneous-roots")）

  + 解不等式 $-2 (x - 3) >= 4 x + 1$，并把解集画在数轴上。（#secref("al05-linear-inequalities")）

  + 用因式分解解方程 $x^2 - x - 6 = 0$；再用判别式 $Delta$ 验证根的数目。（#secref("al05-quadratic-solving")）

  + 已知一元二次方程 $x^2 - 4 x + k = 0$ 有两个相等实根。求 $k$；再用韦达定理写出这两个相等根。（#secref("al05-quadratic-solving")、#secref("al05-vieta")）

  + 某商品进价 $40$ 元/件，以 $x$ 元/件出售时每天销量为 $(120 - 2 x)$ 件（$x > 40$ 且 $120 - 2 x > 0$）。要使每天利润达到 $600$ 元，定价 $x$ 应为多少？（#secref("al04-modeling-word-problems")、#secref("al05-quadratic-solving")）
]

#line(length: 100%, stroke: 0.3pt + luma(200))

#side-hack[
  *参考答案*

  + 分子 $= (x - 2)(x + 2)$，分母 $= (x - 2)^2$。约分得 $frac(x + 2, x - 2)$。化简前禁止取值：分母 $(x - 2)^2 = 0 => x = 2$。约分*后*得到的式子在 $x = -2$ 处也有值，但*原表达式没有*——所以化简前后定义域要分开记。

  + 最简公分母 $(x - 1)(x + 1)$。两边同乘：$2 (x + 1) - (x - 1) = 3$，得 $x + 3 = 3$，故 $x = 0$。验根：$x = 0$ 时 $x - 1 = -1 != 0$、$x + 1 = 1 != 0$ ✓。所以 $x = 0$ 是解。

  + $-2 (x - 3) >= 4 x + 1$；展开 $-2 x + 6 >= 4 x + 1$；移项 $-6 x >= -5$；两边除以 $-6$（*乘除负数变号*）得 $x <= 5 / 6$。数轴上 $5 / 6$ 处实心点向左涂色。

  + $x^2 - x - 6 = (x - 3)(x + 2) = 0 => x = 3$ 或 $x = -2$。$Delta = (-1)^2 - 4 dot 1 dot (-6) = 25 > 0 =>$ 两个不同实根 ✓。

  + 两个相等实根 $<=> Delta = 0 <=> 16 - 4 k = 0 => k = 4$。方程化为 $x^2 - 4 x + 4 = 0$ 即 $(x - 2)^2 = 0$，两个相等根都是 $2$。韦达验证：$x_1 + x_2 = 4$、$x_1 x_2 = 4$ ✓。

  + 利润 $= ($ 售价 $-$ 进价 $) times $ 销量 $ = (x - 40)(120 - 2 x) = 600$。 \
    展开：$-2 x^2 + 200 x - 4800 = 600$，即 $x^2 - 100 x + 2700 = 0$。 \
    $Delta = 10000 - 10800 < 0$——*无实数解*。也就是按本题给的销量函数，每天利润不可能达到 $600$ 元；最大利润出现在 $x = 50$ 处（顶点），此时利润 $= 10 dot 20 = 200$ 元。 \
    本题考的是*建模后是否检验解的存在性*——发现"无解"也是合理回答，比硬凑出错误数字重要得多。

  #line(length: 100%, stroke: 0.3pt + luma(200))

  *自测说明*：能不看答案独立做出 $4$ 题以上即算走完代数分支。任一题卡住，沿着题尾标注的章节回看。
]
