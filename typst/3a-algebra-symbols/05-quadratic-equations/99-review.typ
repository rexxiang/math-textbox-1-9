#import "../../lib/theme-v2.typ": blueprint, mastery, side-hack, secref

== 本章回顾 <meta:quadratic-equations>

#blueprint[
  *一句话口诀*：看 $Delta = b^2 - 4 a c$——是完全平方数走因式分解，否则用求根公式；少走一半冤枉路。
]

#blueprint[
  *知识速查卡*

  #table(
    columns: (1fr, 1fr),
    inset: 6pt,
    stroke: 0.4pt,
    [*主题*], [*一句话记忆*],
    [配方法], [一次项系数 $div 2$ 再平方，补到两边——把左边凑成 $(x + m)^2$。],
    [求根公式], [展开式 $ x = frac(-b plus.minus sqrt(Delta), 2 a) $（配方法的"打包"）],
    [判别式 $Delta$], [$Delta = b^2 - 4 a c$；正 → 两不等实根，零 → 二重根，负 → 无实根。],
    [因式分解法], [只是"$Delta$ 恰为完全平方"的快捷入口，不是独立方法。],
    [韦达定理], [$x_1 + x_2 = -b/a$、$x_1 x_2 = c/a$（需 $Delta >= 0$）。],
    [对称式求值], [一切对 $x_1, x_2$ 对称的量都能写成"和 + 积"的函数。],
  )
]

#mastery[
  *混合自测*

  + 解 $x^2 - 6 x + 5 = 0$，任选一条路线并说明为什么。
  + 解 $3 x^2 + 5 x - 2 = 0$。
  + 判断 $x^2 + 3 x + 4 = 0$ 的根的个数（实数范围内），并说明判据。
  + 用韦达定理求 $x^2 - 4 x + 1 = 0$ 的 $x_1^2 + x_2^2$。
  + 构造以 $1, -2$ 为两根、首项系数为 $3$ 的二次方程。
  + 若方程 $x^2 + (m - 2) x + m + 1 = 0$ 两根互为相反数，求 $m$。
  + 一块长方形菜地，长比宽多 $2$ 米、面积 $48$ 平方米。求长和宽。
  // 跨章回顾
  + 先因式分解 $x^2 - 5x + 6$（#secref("al02-factorization-as-reverse")），再解方程 $x^2 - 5x + 6 = 0$，验证因式分解法和求根公式给出相同结果。
  // 跨章回顾
  + 解分式方程 $1/(x-1) + 1/(x+2) = 1$（#secref("al03-rational-equations-and-extraneous-roots")），去分母后会得到一个一元二次方程——用本章工具解并验根。
  + 构造一个一元二次方程，使得它有两个实数根、且两根之和为 $4$、两根之积为 $-5$。写出方程并验证。
  // 跨章回顾
  + 一个正方形的面积为 $50$ 平方厘米。设边长为 $x$ 厘米（$x > 0$），列方程 $x^2 = 50$ 并求解（本章 #secref("al05-quadratic-solving")，结合平方根 #secref("cb07-square-and-square-root")）。结果用最简根号形式表示。

]

==== 参考答案

+ 因式分解：$(x - 1)(x - 5) = 0 => x = 1, 5$（$Delta = 16$ 为完全平方，分解最快）。
+ $Delta = 25 + 24 = 49$；$x = frac(-5 plus.minus 7, 6) = 1/3$ 或 $-2$。
+ $Delta = 9 - 16 = -7 < 0$，实数范围内*无解*。
+ $(x_1 + x_2)^2 - 2 x_1 x_2 = 16 - 2 = 14$。
+ $3 (x - 1)(x + 2) = 0 => 3 x^2 + 3 x - 6 = 0$。
+ 两根互为相反数即 $x_1 + x_2 = 0$：$-(m - 2) = 0 => m = 2$。代回得 $x^2 + 3 = 0$，$Delta = -12 < 0$。因此在实数范围内*不存在*满足题意的 $m$。
+ 设宽 $x$：$x (x + 2) = 48 => x^2 + 2 x - 48 = 0 => (x + 8)(x - 6) = 0$。宽取 $6$ 米，长 $8$ 米。
+ 因式分解：$x^2 - 5x + 6 = (x - 2)(x - 3)$，故 $x = 2$ 或 $x = 3$。求根公式：$x = (5 plus.minus sqrt(25 - 24))/2 = (5 plus.minus 1)/2$，得 $x = 3$ 或 $x = 2$。结果一致 ✓。
+ 去分母（公分母 $(x-1)(x+2)$）：$(x+2) + (x-1) = (x-1)(x+2)$，即 $2x + 1 = x^2 + x - 2$，$x^2 - x - 3 = 0$。$Delta = 1 + 12 = 13$，$x = (1 plus.minus sqrt(13))/2$。验根：$x - 1 != 0$ 且 $x + 2 != 0$，两根均合法 ✓。
+ 由韦达定理反用，构造 $x^2 - 4x - 5 = 0$。验证：$(x-5)(x+1) = 0$，根 $5, -1$；和 $= 4$，积 $= -5$ ✓。
+ $x^2 = 50$；$x = sqrt(50) = 5 sqrt(2)$（$x > 0$）。边长 $= 5 sqrt(2)$ 厘米。（参见 #secref("cb07-square-and-square-root") 与 #secref("al05-quadratic-solving")）

#blueprint[
  *巩固复习*

  - 回看 #secref("al02-special-identities")：完全平方公式正是配方法的原料——把 $x^2 + b x$ 硬补成 $(x + b/2)^2$ 的依据。
  - 回看 #secref("al02-factorization-as-reverse")：因式分解是二次方程"快捷入口"；分解不成就退到求根公式。
  - 衔接 #secref("ch:linear-inequalities")：下一章把等号松动为不等号——一元一次不等式。

  *☞ 继续往前*

  这只是向前看的预告，不作为后续主线先修。

  - 高中会从二次方程走向*二次函数*：判别式 $Delta$ 的符号就是抛物线与 $x$ 轴相交个数的表达。
  - 韦达定理可推广到 $n$ 次方程——"根与系数"的关系在所有代数方程里都是基础工具。
]

#side-hack[
  *☞ 高中桥*

  本章用判别式 $b^2 - 4 a c$ 判断实根个数。到了高中你会进一步遇到：

  - 当 $b^2 - 4 a c < 0$ 时引入"复数 $i$"（满足 $i^2 = -1$），所有二次方程在复数集里都有两个根，不再有"无解"的情况。
  - 韦达定理推广到任意次多项式：$n$ 次方程的根之和、根之积等"对称量"都能从系数直接读出来。

  这些扩展不影响现在你掌握的工具——只是它们之后会被推到更广的舞台上。
]
