#import "../../lib/theme-v2.typ": blueprint, mastery, secref

== 本章回顾 <meta:quadratic-equations>

#blueprint[
  *知识速查卡*

  #table(
    columns: (1fr, 1fr),
    inset: 6pt,
    stroke: 0.4pt,
    [*主题*], [*一句话记忆*],
    [配方法], [一次项系数 $div 2$ 再平方，补到两边——把左边凑成 $(x + m)^2$。],
    [求根公式], [$x = frac(-b plus.minus sqrt(Delta), 2 a)$；它是配方法的"打包"。],
    [判别式 $Delta$], [$Delta = b^2 - 4 a c$；正 → 两不等实根，零 → 二重根，负 → 无实根。],
    [因式分解法], [只是"$Delta$ 恰为完全平方"的快捷入口，不是独立方法。],
    [韦达定理], [$x_1 + x_2 = -b/a$、$x_1 x_2 = c/a$（需 $Delta >= 0$）。],
    [对称式求值], [一切对 $x_1, x_2$ 对称的量都能写成"和 + 积"的函数。],
  )
]

#mastery[
  *混合自测*

  + 解 $x^2 - 6 x + 5 = 0$，任选一条路线并说明为什么。
  + 解 $3 x^2 + 5 x - 2 = 0$。
  + 判断 $x^2 + 3 x + 4 = 0$ 的根的个数（实数范围内），并说明判据。
  + 用韦达定理求 $x^2 - 4 x + 1 = 0$ 的 $x_1^2 + x_2^2$。
  + 构造以 $1, -2$ 为两根、首项系数为 $3$ 的二次方程。
  + 若方程 $x^2 + (m - 2) x + m + 1 = 0$ 两根互为相反数，求 $m$。
  + 一块长方形菜地，长比宽多 $2$ 米、面积 $48$ 平方米。求长和宽。

  *参考答案*

  + 因式分解：$(x - 1)(x - 5) = 0 => x = 1, 5$（$Delta = 16$ 为完全平方，分解最快）。
  + $Delta = 25 + 24 = 49$；$x = frac(-5 plus.minus 7, 6) = 1/3$ 或 $-2$。
  + $Delta = 9 - 16 = -7 < 0$，实数范围内*无解*。
  + $(x_1 + x_2)^2 - 2 x_1 x_2 = 16 - 2 = 14$。
  + $3 (x - 1)(x + 2) = 0 => 3 x^2 + 3 x - 6 = 0$。
  + 两根互为相反数即 $x_1 + x_2 = 0$：$-(m - 2) = 0 => m = 2$。代回得 $x^2 + 3 = 0$，$Delta = -12 < 0$。因此在实数范围内*不存在*满足题意的 $m$。
  + 设宽 $x$：$x (x + 2) = 48 => x^2 + 2 x - 48 = 0 => (x + 8)(x - 6) = 0$。宽取 $6$ 米，长 $8$ 米。
]

#blueprint[
  *巩固复习*

  - 回看 #secref("al02-special-identities")：完全平方公式正是配方法的原料——把 $x^2 + b x$ 硬补成 $(x + b/2)^2$ 的依据。
  - 回看 #secref("al02-factorization-as-reverse")：因式分解是二次方程"快捷入口"；分解不成就退到求根公式。
  - 衔接 #secref("ch:linear-inequalities")：下一章把等号松动为不等号——一元一次不等式。

  *☞ 继续往前*

  - 高中会从二次方程走向*二次函数*：判别式 $Delta$ 的符号就是抛物线与 $x$ 轴相交个数的表达。
  - 韦达定理可推广到 $n$ 次方程——"根与系数"的关系在所有代数方程里都是基础工具。
]
