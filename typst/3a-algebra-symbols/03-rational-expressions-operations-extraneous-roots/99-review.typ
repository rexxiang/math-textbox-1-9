#import "../../lib/theme-v2.typ": blueprint, mastery, secref

== 本章回顾 <meta:rational-expressions-operations-extraneous-roots>

#blueprint[
  *知识速查卡*

  #table(
    columns: (1fr, 1fr),
    inset: 6pt,
    stroke: 0.4pt,
    [*主题*], [*一句话小结*],
    [分式的定义], [$A/B$，$B$ 含字母；始终带着“$B != 0$”的定义域。],
    [分式基本性质], [分子分母同乘同除同一个*非零*整式，值不变。],
    [约分], [先因式分解，再约掉公因式；只能约因式，不能约“项”。],
    [最简公分母], [各分母先因式分解，取每个因式的最高次幂乘起来。],
    [分式加减], [异分母先通分；分母不变，分子加减；最后再约分。],
    [分式乘除], [乘：分子乘分子、分母乘分母；除：乘以倒数；先约后乘。],
    [分式方程], [去分母 → 解整式 → 验根；*增根*必须舍去。],
    [增根], [去分母扩大了有意义范围的副作用；使原分母为零的“假解”。],
  )
]

#mastery[
  *混合自测*

  + 写出 $(x + 4)/((x - 1)(x + 3))$ 有意义的条件。
  + 化简 $(4 a^2 - 16)/(a^2 - 4 a + 4)$。
  + 合并并化简：$1/(x + 2) + 1/(x - 2)$。
  + 计算 $(x^2 - 1)/(x + 2) dot (x + 2)/(x - 1)$。
  + 计算 $((x + 3))/((x - 4)) div ((x + 3)^2)/((x - 4))$。
  + 解分式方程 $6/x = 1/2$，并验根。
  + 解分式方程 $1/(x - 2) = (3)/(x - 2) - 1$，并说明是否有增根。
  + 解分式方程 $1/x + 1/(x + 1) = 1/2$，并验根。
  + 一项工程，甲独做 $a$ 天完成，乙独做 $b$ 天完成。写出两人合做每天完成的比例并化简。
  + 说明：为什么解分式方程必须验根？（不超过三句话。）
  // 跨章回顾
  + 用小学分数加法 $1/3 + 1/4 = 7/12$（#secref("pf04-fraction-arithmetic")）的通分思路，解释为什么 $1/(x-1) + 1/(x+1)$ 的公分母是 $(x-1)(x+1)$ 而非 $x^2 - 1 + (x-1)(x+1)$。
  // 跨章回顾
  + 先对 $x^2 - 4$ 做因式分解（#secref("al02-factorization-as-reverse")），再化简分式 $(x^2 + 4x + 4)/(x^2 - 4)$，写出定义域限制。
  + 不用详细计算，判断：分式 $(x^2 + 1)/(x^2 - 1)$ 在 $x = 10$ 时的值是大于 $1$ 还是小于 $1$？说明理由。
  // 跨章回顾
  + 化简分式 $(3 (x + 2)(x - 2))/((x - 2)^2)$。提示：用最大公因数的类比思想（回顾 #secref("pf03-gcd-lcm")）找出分子分母的公因式，再约分（本章 #secref("al03-rational-expression-structure")）。写出化简结果与 $x$ 的限制条件。
  // 跨章回顾：步骤一致性
  + 把 $1/3 + 1/4$ 和 $1/x + 1/y$ 写在一起，逐步对照（参见 #secref("pf04-fraction-arithmetic")）：(i) 找公分母；(ii) 各项化为公分母形式；(iii) 分子相加；(iv) 化简。说明这两题中*哪一步骤完全一致*、*哪一步骤多了一道"是否要写定义域"的额外手续*。
  // 跨章回顾：分式方程与建模五步
  + 解分式方程 $1/(x - 1) + 2/(x + 1) = 1$，并用 #secref("ch:linear-equations-systems-modeling") 的*建模五步*（设量 $arrow$ 列等式 $arrow$ 解 $arrow$ 验根 $arrow$ 写答）回答：在分式方程里，"验根"对应五步中的哪一步？为什么这一步在分式方程里不能省？
]

==== 参考答案

+ $x != 1$ 且 $x != -3$。
+ $(4(a - 2)(a + 2))/((a - 2)^2) = (4(a + 2))/(a - 2) quad (a != 2)$。
+ $((x - 2) + (x + 2))/((x + 2)(x - 2)) = (2 x)/(x^2 - 4)$。
+ $((x - 1)(x + 1))/(x + 2) dot (x + 2)/(x - 1) = x + 1 quad (x != 1, x != -2)$。
+ $((x + 3))/((x - 4)) dot ((x - 4))/((x + 3)^2) = 1/(x + 3) quad (x != 4, x != -3)$。
+ 两边同乘 $2 x$：$12 = x$，代回 $x = 12 != 0$ ✓。
+ 乘 $(x - 2)$：$1 = 3 - (x - 2) = 5 - x$，$x = 4$。代回：$x - 2 = 2 != 0$ ✓。有解 $x = 4$，无增根。
+ 公分母 $2 x(x + 1)$：$2(x + 1) + 2 x = x(x + 1)$，$x^2 - 3 x - 2 = 0$——此方程可用第 5 章的求根公式解出；若本章尚未学到，可留作第 5 章回扣题。
+ $1/a + 1/b = (a + b)/(a b)$，限制：$a != 0, b != 0$。
+ 去分母会把方程的有意义范围*扩大*，整式方程可能“多出”让原分母为零的“解”，即增根；不验根就可能把增根当真解，因此验根是必要的一步。
+ 小学通分的思路是取各分母的最小公倍数。$(x-1)$ 和 $(x+1)$ 互质，最小公倍数就是 $(x-1)(x+1)$。$x^2 - 1$ 其实等于 $(x-1)(x+1)$（平方差），所以写 $x^2 - 1 + (x-1)(x+1)$ 是重复了——公分母只需要 $(x-1)(x+1)$ 一份就够。
+ $x^2 - 4 = (x-2)(x+2)$，$x^2 + 4x + 4 = (x+2)^2$。化简：$((x+2)^2)/((x-2)(x+2)) = (x+2)/(x-2)$。定义域：$x != 2$ 且 $x != -2$。
+ $x = 10$ 时分子 $= 101$，分母 $= 99$，分子 > 分母 > 0，所以值大于 $1$。更一般地，当 $|x| > 1$ 时 $x^2 + 1 > x^2 - 1 > 0$，故分式值恒大于 $1$。
+ 分子 $= 3(x+2)(x-2)$，分母 $= (x-2)^2$；公因式 $(x-2)$；约分结果 $= 3(x+2)/(x-2)$（$x eq.not 2$）。公因式类比整数最大公因数：提出并约去使分子分母同时缩小，如整数 $6/4 = 3/2$（约去公因数 $2$）。（参见 #secref("pf03-gcd-lcm") 与 #secref("al03-rational-expression-structure")）
+ 步骤 (i)(ii)(iii)(iv) 完全一致：先找公分母（$1/3 + 1/4$ 取 $12$；$1/x + 1/y$ 取 $x y$）；再把每项化为公分母形式（$4/12 + 3/12$；$y/(x y) + x/(x y)$）；再相加（$7/12$；$(x + y)/(x y)$）；最后化简或确认互质。多出的一道手续是*写定义域*：$1/x + 1/y$ 必须声明 $x != 0, y != 0$，而 $1/3 + 1/4$ 不存在分母为零的隐患。结构同源，多了一层"是否含字母 $arrow$ 是否要限定取值"的把关。
+ 通分得 $((x + 1) + 2(x - 1))/((x - 1)(x + 1)) = 1$ $arrow$ $3 x - 1 = (x - 1)(x + 1) = x^2 - 1$ $arrow$ $x^2 - 3 x = 0$ $arrow$ $x(x - 3) = 0$，候选解 $x = 0$ 或 $x = 3$。验根：$x = 0$ 代入 $(x - 1)(x + 1) = -1 != 0$ ✓；$x = 3$ 代入 $(x - 1)(x + 1) = 8 != 0$ ✓。两解均成立。"验根"对应五步中的*第四步"检验"*；分式方程里这一步不能省，因为去分母会扩大有意义范围，可能引入使原分母为零的*增根*——五步流程里把它从一般"代回原题验"专门强化为"先看是否让分母为零"。

#blueprint[
  *巩固复习*

  - 回看 #secref("pf04-fraction-equivalence")：分式基本性质其实就是分数基本性质——把“数字”换成“整式”而已。
  - 回看 #secref("pf04-fraction-arithmetic")：通分、加减、乘除的套路同源。
  - 回看 #secref("al02-factorization-as-reverse")：本章所有约分、通分的关键前置，都是“能不能把分子分母再因式分解”。
  - 回看 #secref("cb06-solving-linear")：解分式方程先把它化回整式方程再套这一节的解法。
  - 衔接 #secref("ch:polynomials-like-terms-operations")：分子分母的所有运算都要依赖那里的整式加减乘法。

  *☞ 高中桥*

  _以下是预告，现在看不懂完全正常 — 等到高中再回来。_

  - 高中把本章的“分式 + 定义域条件”正式升级为*有理函数* $y = p(x) / q(x)$ 与它的定义域问题；“去分母扩大定义域”“需要验根”也是那里的标准动作。
  - 处理分式方程时配合第 5 章的“二次方程解法”会更顺；若整式方程是二次的，后续章节的工具会让本章的例 3 类题目更容易。
]
