#import "../../lib/theme-v2.typ": blueprint, mastery, secref

== 本章回顾 <meta:linear-inequalities>

#blueprint[
  *知识速查卡*

  #table(
    columns: (1fr, 1fr),
    inset: 6pt,
    stroke: 0.4pt,
    [*主题*], [*一句话记忆*],
    [不等式性质], [加减、乘正数 → 方向不变；乘除*负数* → 方向*反转*。],
    [不等式组], [解集取*交集*；同大取大、同小取小、大小小大取中间、大大小小无解。],
    [$|x|$ 直观], ["到 $0$ 的距离"；$|x| < r$ ⟺ $-r < x < r$，$|x| > r$ ⟺ $x < -r$ 或 $x > r$。],
  )
]

#mastery[
  *混合自测*

  + 解不等式 $-4 x + 2 > 18$。
  + 解不等式组 $cases(2 x + 1 >= 5, 3 - x > 0)$。
  + 解 $|2 x + 1| <= 7$。
  + 解不等式 $3(x - 1) < 2 x + 5$。
  + 解不等式组 $cases(x + 4 > 1, 2 x - 3 <= 7)$。

  *参考答案*

  + $-4 x > 16 => x < -4$（除以负数反转）。
  + $x >= 2$ 且 $x < 3$ → $2 <= x < 3$。
  + $-7 <= 2 x + 1 <= 7 => -4 <= x <= 3$。
  + $3 x - 3 < 2 x + 5 => x < 8$。
  + $x > -3$ 且 $x <= 5$ → $-3 < x <= 5$。
]

#blueprint[
  *巩固复习*

  - 回看 #secref("al04-linear-one-variable")：解不等式的前四步完全照搬五步法——差别只在"乘除负数时方向反转"。
  - 回看 #secref("cb02-opposite-and-absolute")：$|x|$ 的定义在数轴第二章就做过；本章只是把它推广到不等式。
  - 衔接 #secref("ch:algebra-summary")：下一章会把本章的工具串到整条代数知识地图里做一次总览。

  *☞ 继续往前*

  - $|x|$ 的三条转译也会在"绝对值不等式""距离度量""范数"等更高维的场景里反复出现。
  - 高中会把不等式推广到二次不等式 $a x^2 + b x + c > 0$——到那时判别式会再次登场。
]
