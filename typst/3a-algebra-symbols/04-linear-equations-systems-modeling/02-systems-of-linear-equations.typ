#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref, answer-cut, self-check
#import "../../lib/diagram-packages.typ": cetz

=== 二元一次方程组：代入与加减消元 <tool:al04-systems-linear>

#vocab[二元一次方程组 system of linear equations in two unknowns][代入消元法 substitution][加减消元法 elimination by addition or subtraction][消元 elimination]

#crisis[
  *鸡兔同笼*：笼子里有鸡和兔共 $35$ 个头、$94$ 只脚。鸡和兔各几只？

  设鸡 $x$ 只，兔 $y$ 只。
  - “头”给出 $x + y = 35$；
  - “脚”给出 $2 x + 4 y = 94$。

  可是，单独一条 $x + y = 35$ 能被 $(1, 34)$、$(10, 25)$ 等无数组满足——单方程根本*锁不住*唯一解。

  只有把两个条件*同时*要求满足，答案才唯一。“多条件、多未知量”这个常见情景，靠一元一次方程是解决不了的——需要一套新的工具把它拎起来。

  #figure(
    cetz.canvas(length: 0.45cm, {
      import cetz.draw: *

      // Axes
      let ax-len = 18
      let ay-len = 14
      line((-1, 0), (ax-len, 0), stroke: 1pt + rgb("#424242"),
           mark: (end: "stealth", fill: rgb("#424242"), scale: 0.5))
      line((0, -1), (0, ay-len), stroke: 1pt + rgb("#424242"),
           mark: (end: "stealth", fill: rgb("#424242"), scale: 0.5))
      content((ax-len + 0.8, 0), text(size: 7pt)[$x$])
      content((0.6, ay-len + 0.5), text(size: 7pt)[$y$])
      content((-0.6, -0.6), text(size: 6pt)[$O$])

      // Scale ticks
      for i in range(1, 8) {
        let v = i * 5
        if v <= 45 {
          line((v / 3, -0.3), (v / 3, 0.3), stroke: 0.5pt + rgb("#9E9E9E"))
        }
      }
      for i in range(1, 8) {
        let v = i * 5
        if v <= 35 {
          line((-0.3, v / 3), (0.3, v / 3), stroke: 0.5pt + rgb("#9E9E9E"))
        }
      }

      // Line 1: x + y = 35, from (0,35) to (35,0)
      // Scale: divide coords by 3 for display
      line((0, 35/3), (35/3, 0), stroke: 1.5pt + rgb("#1565C0"))
      content((35/3 + 1.5, 0.8), text(size: 6pt, fill: rgb("#1565C0"))[$x + y = 35$])

      // Line 2: 2x + 4y = 94, from (0,23.5) to (47,0)
      line((0, 23.5/3), (47/3, 0), stroke: 1.5pt + rgb("#C62828"))
      content((47/3 + 0.5, 1.2), text(size: 6pt, fill: rgb("#C62828"))[$2 x + 4 y = 94$])

      // Intersection point (23, 12) → display (23/3, 12/3)
      let px = 23 / 3
      let py = 12 / 3
      circle((px, py), radius: 0.25, fill: rgb("#2E7D32"), stroke: 0.8pt + rgb("#1B5E20"))
      content((px + 2.5, py + 0.8), text(size: 7pt, weight: "bold", fill: rgb("#2E7D32"))[解 $(23, 12)$])
    }),
    caption: [两条直线的交点就是方程组的解：同时满足两个条件的唯一一点],
  )
]

#tryit[
  先动手试试，下面再读。

  + $cases(y = 2 x - 1, 3 x + 2 y = 12)$
]

#discovery[
  *先用旧工具试试。* 有了一元一次方程的经验，看到 $x + y = 35$ 自然想直接解它：移项得 $x = 35 - y$，然后……等等，还有个未知量 $y$，解到一半解不下去。

  试着推下去——

  把 $x = 35 - y$ 代回*同一个*方程 $x + y = 35$：$(35 - y) + y = 35$，化简得 $35 = 35$——恒成立，两个未知量都消掉了，什么信息也没多得到。这个方程确实有无数个解：$(1, 34), (10, 25), (20, 15)dots$

  *这里卡住了。* 一个方程对两个未知量来说*约束不够*——它确定的是一条直线上的无穷多点，而不是唯一一组答案。只有把两个条件*同时*要求满足，才能把解锁到唯一一组，所以需要一种能*同时处理两个等式约束*的工具。

  *二元一次方程组*：含两个未知量、每个方程都是一次式的方程组，写作

  $ cases(a_1 x + b_1 y = c_1, a_2 x + b_2 y = c_2). $

  核心思路只有一条——*消元*：想办法把两个未知量减成一个未知量的方程，再回到我们熟悉的一元一次方程（#secref("al04-linear-one-variable"))。

  *消元路线一：代入消元*

  挑一个方程，把其中一个未知量*用另一个表示出来*，然后代入另一个方程。

  拿鸡兔同笼做：由 $x + y = 35$ 得 $x = 35 - y$，代入 $2 x + 4 y = 94$：

  $ 2 (35 - y) + 4 y = 94 => 70 + 2 y = 94 => y = 12. $

  再回代 $x = 35 - 12 = 23$。

  *消元路线二：加减消元*

  对两个方程做*同倍数*的线性组合，让某个未知量的系数*相等或相反*，再相加 / 相减。

  还是鸡兔题。把第一个方程乘 $2$：

  $ cases(2 x + 2 y = 70, 2 x + 4 y = 94). $

  下式减上式：$2 y = 24$，$y = 12$；再回代到任一原方程求 $x$。

  *两条路线，哪条更顺？*

  #table(
    columns: (1fr, 1.4fr),
    inset: 6pt,
    stroke: 0.4pt,
    [*情景*], [*倾向路线*],
    [某个方程已经写成 $y = "…"$ 或 $x = "…"$], [代入消元——一步到位],
    [两个方程里某个未知量系数相同 / 相反], [加减消元——直接加减],
    [系数都不整齐], [加减消元——*最小公倍数*凑系数通常最快],
    [方程里带分数 / 小数], [先按 #secref("al04-linear-one-variable") 的第 1 步去分母，再看代入 / 加减],
  )

  选哪条其实不用死记——*哪条少抄几步就选哪条*。
]

#side-hack[
  解完一组 $(x, y)$，一定要把它*同时*代入*两个*原方程。一个方程满足不代表另一个也满足——尤其在凑系数时很容易漏乘导致两个方程各自“似乎都对”，但联立起来错了。
]

#tryit[
  先别翻下面的方法总结，自己挑合适的消元路线：

  + $cases(x + y = 10, x - y = 4)$（建议加减）
]

#history-note[
  《九章算术》（约公元 1 世纪）第八章“方程”中记载了“方程术”：把方程组各未知数的系数排成表格（纵横排列，形似现代矩阵），再逐列相消——这套做法和 18 世纪德国数学家高斯（Carl Friedrich Gauss, 1777–1855）系统化的“高斯消元法”本质相同，但早了约 1700 年。书中甚至出现了负数系数的处理（“正负术”），堪称线性代数思想的最早萌芽。
]

#blueprint[
  *工具一：代入消元法*

  + 从一个方程里把一个未知量*解出来*（例如 $x = "(关于 " y " 的式子)"$）。
  + 代入*另一个*方程，得到只含一个未知量的一元一次方程。
  + 按 #secref("al04-linear-one-variable") 解，再回代求另一个未知量。

  *例 1*：$cases(y = 2 x - 1, 3 x + 2 y = 12)$。代入第二个：$3 x + 2 (2 x - 1) = 12 => 7 x = 14 => x = 2$，回代 $y = 3$。

  #figure(
    cetz.canvas(length: 0.45cm, {
      import cetz.draw: *

      // Equation ① box
      let y1 = 6
      rect((0, y1), (16, y1 + 3), fill: rgb("#E3F2FD"), stroke: 1pt + rgb("#1565C0"), radius: 3pt)
      content((1.5, y1 + 1.5), text(size: 8pt, weight: "bold", fill: rgb("#1565C0"))[①], anchor: "west")
      content((3.5, y1 + 1.5), text(size: 8pt)[$y = $], anchor: "west")
      content((6.5, y1 + 1.5), text(size: 8pt, fill: rgb("#E65100"), weight: "bold")[$2 x$], anchor: "west")
      content((9.5, y1 + 1.5), text(size: 8pt)[$- 1$], anchor: "west")

      // Equation ② box
      let y2 = 2
      rect((0, y2), (16, y2 + 3), fill: rgb("#FFCDD2"), stroke: 1pt + rgb("#C62828"), radius: 3pt)
      content((1.5, y2 + 1.5), text(size: 8pt, weight: "bold", fill: rgb("#C62828"))[②], anchor: "west")
      content((3.5, y2 + 1.5), text(size: 8pt)[$3 x + $], anchor: "west")
      content((7.5, y2 + 1.5), text(size: 8pt, fill: rgb("#E65100"), weight: "bold")[$2 y$], anchor: "west")
      content((10.5, y2 + 1.5), text(size: 8pt)[$= 12$], anchor: "west")

      // Arrow: substitute ① into ②
      line((8, y1), (8, y2 + 3.3), stroke: 1.5pt + rgb("#E65100"),
           mark: (end: "stealth", fill: rgb("#E65100"), scale: 0.6))
      content((12, (y1 + y2 + 3) / 2), text(size: 7pt, fill: rgb("#E65100"), weight: "bold")[代入消元], anchor: "west")

      // Result
      let yr = -1.5
      rect((2, yr), (14, yr + 2.5), fill: rgb("#C8E6C9"), stroke: 1pt + rgb("#2E7D32"), radius: 3pt)
      content((8, yr + 1.25), text(size: 8pt, weight: "bold", fill: rgb("#1B5E20"))[$7 x = 14 => x = 2$])
      line((8, y2), (8, yr + 2.8), stroke: 1.2pt + rgb("#2E7D32"),
           mark: (end: "stealth", fill: rgb("#2E7D32"), scale: 0.6))
    }),
    caption: [代入消元：把①式的 $y$ 代入②式，消去 $y$，只剩一个未知量],
  )


  *工具二：加减消元法*

  + 通过两个方程*分别乘以合适的倍数*，让某个未知量的系数相等或相反。
  + 相加（相反时）或相减（相等时），消去一个未知量。
  + 解剩下的一元一次方程，回代求另一个未知量。

  *例 2*：$cases(3 x + 2 y = 16, 5 x - 2 y = 8)$。$y$ 的系数已经相反，直接相加：$8 x = 24 => x = 3$，回代得 $y = 7 / 2$。

  *例 3*：$cases(3 x + 4 y = 25, 2 x + 3 y = 18)$。上式 $times 3$、下式 $times 4$：$cases(9 x + 12 y = 75, 8 x + 12 y = 72)$，相减 $x = 3$，回代 $y = 4$。

  *工具三：怎么选路线*

  - 若某一式已写成 $y = "…"$ 或 $x = "…"$ → *代入*。
  - 若某未知量系数相同或相反 → *加减*。
  - 都不明显 → 凑系数的最小公倍数后 *加减*；或者自问“哪条少抄几步”。
]

#self-check[
  代入法和加减消元法解出来的解一样吗？为什么？两种方法分别消掉了哪一个未知数？
]


#pitfall[
  *高频错误*

  ✗ 乘倍数时漏乘方程里的某一项
  → ✓ 等式两侧的*每一项*都要乘以那个倍数；$3 x + 2 y = 16$ 乘以 $5$ 应该写成 $15 x + 10 y = 80$，不要写成 $15 x + 2 y = 80$。

  ✗ 加减后弄反了符号
  → ✓ 下式 $-$ 上式时，*整条下式每项都变号*再相加；宁愿写“下式 $+ (- "上式"$)”也不要在脑内跳步。

  ✗ 只把解代入一个方程检验
  → ✓ 必须代入*两个*原方程；只满足一个说明你可能在消元时抄错。

  ✗ 化简到 $0 = 0$ 就写“方程组有唯一解”
  → ✓ $0 = 0$ 说明两方程代表同一条直线，有*无穷多组解*；$0 = k$（$k != 0$）说明两条直线平行，*无解*。
]

#mastery[
  *基础*

  + 用代入法解：
    - $cases(x = 3 y - 1, 2 x - 5 y = 1)$
    - $cases(y = x + 3, 2 x + y = 12)$
  + 用加减法解：
    - $cases(x + y = 7, x - y = 1)$
    - $cases(3 x + 2 y = 12, 4 x - 2 y = 16)$

  *应用*

  + 买 $3$ 支笔和 $2$ 本笔记本共 $32$ 元；买 $1$ 支笔和 $3$ 本笔记本共 $27$ 元。笔和笔记本各多少钱？
  + 解方程组：$cases(frac(x, 2) + frac(y, 3) = 4, frac(x, 4) - frac(y, 6) = 0)$（提示：先去分母再挑方法）。
  + 两位数的十位是 $a$、个位是 $b$。若 $a + b = 9$，且交换位置后新数比原数大 $27$，求这个两位数。

  *挑战 ☞ 选做*

  + 方程组 $cases(a x + b y = 1, b x + a y = 7)$ 的解为 $(x, y) = (2, 3)$。求 $a, b$。
  + 讨论方程组 $cases(2 x + y = 5, 4 x + 2 y = k)$ 在不同 $k$ 下的解的个数（$k$ 是常数）。
  + 一个工程，甲独做 $12$ 天、乙独做 $18$ 天完成。两人合做 $d$ 天后，甲又独做 $4$ 天完成全部工作。求 $d$（提示：设甲、乙每天的工作量为 $u, v$，列方程组）。

  + *[反直觉]*：判断："如果一个二元一次方程组的两条直线斜率相等且 $y$ 截距不同，则方程组*无解*；若斜率与截距都相等，则有无穷多解。" 用方程组 $cases(2 x + y = 5, 4 x + 2 y = 10)$ 验证后者，再写一个无解的例子。

  + *[构造题]*：构造一个三元一次方程组（$x, y, z$），要求解为 $(1, 2, 3)$，且每个方程都含三个未知数。给出方程组并代入验证。

  + *[找 Bug]*：小华解 $cases(x + y = 5, x - y = 1)$，写："两式相加 $2 x = 6$，$x = 3$；代回第一式 $y = 2$。" 答案对，但她说"加减消元法只能消去 $y$"，这条说法有问题——她错在哪？请构造一个加减消元应*消 $x$* 的例子。

  #answer-cut[

  *基础*
  + $2 (3 y - 1) - 5 y = 1 => 6 y - 2 - 5 y = 1 => y = 3$，$x = 3 dot 3 - 1 = 8$。（代入消元）
  + $2 x + (x + 3) = 12 => 3 x = 9 => x = 3$，$y = 6$。（代入消元）
  + 相加：$2 x = 8 => x = 4$，$y = 3$。（加减消元）
  + 相加：$7 x = 28 => x = 4$，回代 $12 + 2 y = 12 => y = 0$。（加减消元）

  *应用*
  + $cases(3 p + 2 n = 32, p + 3 n = 27)$。第二式 $times 3$ 再减第一式：$7 n = 49 => n = 7$，$p = 6$。笔 $6$ 元、笔记本 $7$ 元。
  + 两边去分母得 $cases(3 x + 2 y = 24, 3 x - 2 y = 0)$，相减 $4 y = 24 => y = 6$，$x = 4$。
  + $cases(a + b = 9, 10 b + a - (10 a + b) = 27)$ → $9 b - 9 a = 27$，即 $b - a = 3$。与 $a + b = 9$ 联立：$b = 6, a = 3$。原数为 $36$。

  *挑战 ☞ 选做*
  + 代入 $(2, 3)$：$cases(2 a + 3 b = 1, 2 b + 3 a = 7)$。两式相加 $5 (a + b) = 8$，$a + b = 8 / 5$；两式相减 $a - b = 6$。解得 $a = 19 / 5$，$b = -11 / 5$。
  + 下式恰是上式的 $2$ 倍，故 $k = 10$ 时两方程同线 → *无穷多组解*；$k != 10$ 时两方程平行 → *无解*；不存在“唯一解”的情形。
  + $cases(12 u = 1, 18 v = 1)$ 给出 $u = 1/12$，$v = 1/18$。联立“$d$ 天合作 + 甲再做 $4$ 天完成”：$(u + v) d + 4 u = 1$，代入解得 $d = (1 - 4 u) / (u + v) = (1 - 1/3) / (1/12 + 1/18) = (2/3) / (5/36) = 24 / 5 = 4.8$ 天。
  + 验证 $cases(2 x + y = 5, 4 x + 2 y = 10)$：第二式恰是第一式 $times 2$，方程组退化为单条直线 $2 x + y = 5$，解集为这条直线上所有点（无穷多解）。无解例：$cases(2 x + y = 5, 4 x + 2 y = 11)$，第二式比 $2 times 5 = 10$ 大 $1$，矛盾，无解（两条平行直线不相交）。
  + 一例：$cases(x + y + z = 6, 2 x - y + z = 3, x + 2 y - z = 2)$。代 $(1, 2, 3)$：第一式 $1 + 2 + 3 = 6$ ✓；第二式 $2 - 2 + 3 = 3$ ✓；第三式 $1 + 4 - 3 = 2$ ✓。
  + 错。加减消元法可以消任一未知数，关键是让该未知数的系数*互为相反数*。例：$cases(x + y = 5, -x + y = 3)$，相加消去 $x$ 得 $2 y = 8 => y = 4 => x = 1$。她把"恰好一次相加 $y$ 系数没抵消"误以为"只能消 $y$"——选哪个变量消，由系数关系决定，不固定。
  ]
]
