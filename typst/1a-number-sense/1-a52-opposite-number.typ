#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== 相反数、绝对值与有理数运算 <tool:a52-opposite-number>

#vocab[相反数 opposite number / 绝对值 absolute value / 有理数运算 rational arithmetic]

#crisis[
  你已经会算 $5 + (-5) = 0$（在数轴上向左走 5 步，正好走回 0），
  但 $3 + (-7) = ?$ 呢？$(-8) times 3 = ?$ 呢？

  用旧方法一遇到负号就得靠猜——正负相加谁的符号？乘出来是正还是负？我们需要一套统一规则。
]

#history-note[
  负数真正被接受，不只是因为“能记亏欠”，还因为一整套运算规则逐渐稳定下来。相反数、绝对值和有理数运算，正是让这套新数系真正可用的关键。
]

#discovery[
  *第一步*：观察成对的位置。

  $5$ 和 $-5$ 分居 $0$ 的两边，离 $0$ 一样远，但方向相反。

  *第二步*：只盯着“离 $0$ 有多远”。

  数 $-4$ 到 $0$ 的距离是 $4$，数 $4$ 到 $0$ 的距离也是 $4$。一旦把“方向”和“距离”分开看，很多带正负号的问题就不容易混乱。

  *第三步*：把运算也放回数轴直觉里。

  - 加上正数：向右走
  - 加上负数：向左走
  - 乘除法则：先算绝对值部分，再看正负——同号得正，异号得负。比如 $(-3) times (-8)$：绝对值算是 $3 times 8 = 24$，两个都是负号（同号），所以结果是 $+24$。
]

#tryit[
  先不要往下看，自己试一试：

  + 在数轴上画出 $(-8) + 3$ 的过程（从 $-8$ 出发，向右走 3 步），你落在哪里？
  + 在数轴上画出 $(-3) + 8$ 的过程（从 $-3$ 出发，向右走 8 步），你落在哪里？
  + 观察这两道题：都是一正一负相加，但顺序不同。根据你的结果归纳：*当两个数符号相反时，结果的符号和数字大小是怎么决定的？*

  （先写出自己的猜测，再看下面的工具蓝图验证。）
]

#blueprint[
  - *相反数*：分居 0 两边、离 0 一样远、方向相反的一对数。$a$ 的相反数是 $-a$。
  - *绝对值*：一个数到 0 的距离，所以 $abs(-4)=4$，$abs(4)=4$，$abs(0)=0$。
  - *有理数运算*：
    - 同号相加：绝对值相加，符号不变
    - 异号相加：大绝对值减小绝对值，符号跟着绝对值较大的那个数
    - 减法可改写成加法：$a-b=a+(-b)$
    - 乘除法：同号得正，异号得负
]

#side-hack[
  看到绝对值时，先别背公式，先问一句：*它离 0 有多远？*
]

#pitfall[
  *常见错误 1*：把绝对值算成负数。

  绝对值表示距离，所以不会是负数。

  *常见错误 2*：做减法时忘了“减一个数等于加它的相反数”。
]

#mastery[
  *练一练*

  + 写出下列各数的相反数和绝对值：$-7$，$3.2$，$0$
  + 计算：$(-9)+4$，$6-(-5)$，$(-3)times(-8)$，$35 div (-7)$
  + 小亮的账户余额是 $-18$ 元，后来收到 25 元零花钱，现在余额是多少？
  + 已知 $abs(a)=5$，写出 $a$ 可能取的值。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_
  + $-7$ 的相反数是 $7$，绝对值是 $7$；$3.2$ 的相反数是 $-3.2$，绝对值是 $3.2$；$0$ 的相反数和绝对值都是 $0$。
  + $-5$，$11$，$24$，$-5$。
  + $-18+25=7$（元）。
  + $a=5$ 或 $a=-5$。

  *挑战*

  + 若 $|a| > |b|$ 且 $a + b < 0$，判断 $a$ 和 $b$ 的符号各是什么？要求用分类讨论：
    - 情况一：$a > 0, b > 0$（是否可能？）
    - 情况二：$a < 0, b < 0$（是否可能？）
    - 情况三：$a > 0, b < 0$（是否可能？）
    - 情况四：$a < 0, b > 0$（是否可能？）
    哪些情况能同时满足 $|a| > |b|$ 且 $a + b < 0$？

  + 设 $x$ 是任意一个负数。判断以下各式是正数、负数，还是无法确定：
    - $-x$
    - $|x|$
    - $x^2$
    - $x + |x|$

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _挑战参考答案：_

  第一题：
  - 情况一（$a>0, b>0$）：两个正数绝对值就是本身，$|a|>|b|$ 意味着 $a > b > 0$，此时 $a + b > 0$，与 $a + b < 0$ 矛盾，不可能。
  - 情况二（$a<0, b<0$）：$|a| = -a$，$|b| = -b$，$|a|>|b|$ 即 $-a > -b$ 即 $a < b$，两者都是负数，$a + b < 0$ 总成立。这种情况可能（例如 $a = -5, b = -2$，$|a|=5>|b|=2$，$a+b=-7<0$）。
  - 情况三（$a>0, b<0$）：$|a|>|b|$ 意味着 $a > -b$，所以 $a + b = a - (-b) > 0$，与 $a + b < 0$ 矛盾，不可能。
  - 情况四（$a<0, b>0$）：$|a|>|b|$ 意味着 $-a > b$，所以 $a + b = a + b < 0$（因为 $a$ 是负的且绝对值更大）。可能（例如 $a=-5, b=2$，$a+b=-3<0$）。
  结论：情况二和情况四满足条件，即 $a < 0$（$a$ 一定是负数）。

  第二题：$x$ 是负数：
  - $-x > 0$（正数，对任意负 $x$ 成立）
  - $|x| > 0$（正数）
  - $x^2 > 0$（正数，负数的平方是正数）
  - $x + |x| = x + (-x) = 0$（零！负数加上其绝对值等于零）
]
