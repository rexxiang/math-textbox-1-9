#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref

== 数轴、负数与有理数入口 <tool:a49-number-line>

#vocab[数轴 number line / 负数 negative number / 有理数 rational number]

#crisis[
  早上妈妈给小林 30 元午餐费，放学前他又帮班级垫付了 50 元材料费。

  如果只用前面学过的“够不够减”，$30 - 50$ 好像卡住了；可现实里这件事一点也不神秘——他现在是*少了 20 元*。

  还有天气预报里的“零下 6 度”、电梯的“地下 2 层”、银行账户里的“欠款 80 元”，都在提醒我们：光靠 $0, 1, 2, 3, dots.h$ 已经不够用了。
]

#history-note[
  中国古代《九章算术》已经用不同颜色的算筹区分“正”和“负”，把收入、支出，盈余、亏空放进同一套计算里。负数并不是“反常”的数，而是把现实里相反方向、相反意义的量一起纳入计算后的自然结果。
]

#discovery[
  *第一步*：把熟悉的刻度线向两边都延长。

  $ arrow.l #h(1.5em) -4 #h(1.5em) -3 #h(1.5em) -2 #h(1.5em) -1 #h(1.5em) 0 #h(1.5em) 1 #h(1.5em) 2 #h(1.5em) 3 #h(1.5em) 4 #h(1.5em) arrow.r $

  这样一来，$0$ 的右边和左边都能表示位置，而且越往右越大，越往左越小。

  *第二步*：把“方向相反”的量写出来。

  - 向东走 3 米，记作 $+3$
  - 向西走 3 米，记作 $-3$
  - 存入 80 元，记作 $+80$
  - 欠款 80 元，记作 $-80$

  *第三步*：把分数、小数也放到同一条线上。

  $-1.5$、$frac(2, 3)$、$-0.2$ 都能在数轴上找到位置。可见，整数、分数、小数并不是三套互不相干的东西，而是在同一个位置系统里一起工作。
]

#tryit[
  先自己试一试：

  + 把 $-2.5, 0, 3, -1, frac(1, 2)$ 放到数轴上，想一想谁更靠左，谁更靠右。
  + “零下 4 度”和“地上 4 层”分别更适合用正数还是负数表示？
]

#blueprint[
  - *数轴*：把数按顺序排在一条直线上得到的位置图。
  - *负数*：出现在 0 左边、用来表示相反方向或相反意义的数。
  - *有理数*：像整数、分数、有限小数、循环小数这样都能放到数轴上的数。
  - 在数轴上，越靠右的数越大，越靠左的数越小。
]

#side-hack[
  见到带正负号的量，先别急着算，先问一句：*它在 0 的哪一边？*
]

#pitfall[
  *常见错误 1*：觉得负数里数字大的就更大。

  在负数里，越靠左反而越小，所以 $-9 < -2$。

  *常见错误 2*：把负号只当装饰，不把它理解成方向或意义。
]

#mastery[
  *练一练*

  + 在数轴上按从左到右顺序排列：$-2.5, 0, 3, -1, frac(1, 2)$
  + 判断：$3 > -2$ 对吗？$-1 > -5$ 对吗？
  + 说说“欠款 20 元”“零下 6 度”“地下 2 层”为什么都适合用负数表示。

  *应用题*

  + 冬天里，某城市一天内最高气温是 $-3°C$，最低气温是 $-12°C$。在数轴上标出这两个温度，并说明：谁更冷？两者相差多少度？
  + 小明的银行账户：1 月存入 $500$ 元，2 月取出 $800$ 元，3 月又存入 $300$ 元。用正负数记录每次变动，并用数轴说明账户现在的状态。

  *挑战题*

  + 小明说：$-100 < -1$，所以 $-100$ 在数轴上离原点更远，因此 $abs(-100) > abs(-1)$。他的判断是否正确？如果*绝对值越大*的数在数轴上*越靠左*，能不能设计一个例子说明这个规律？
  + 设 $a$ 是一个有理数。填空：如果 $a < 0$，那么 $-a$ 是\_\_\_\_\_（正/负）数；$abs(-a)$ 与 $abs(a)$ 的关系是\_\_\_\_\_。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_
  + 从左到右：$-2.5, -1, 0, frac(1, 2), 3$
  + 都对。
  + 因为它们都在表示与”0 的正向”相反的方向或意义。

  *应用题参考答案：*
  + $-12°C$ 在数轴上更靠左，所以 $-12°C$ 更冷。两者相差 $-3-(-12)=9$ 度。
  + 三次变动依次记作 $+500$，$-800$，$+300$。总变动：$500-800+300=0$，账户现在余额为 $0$ 元（在数轴上刚好回到原点）。

  *挑战题参考答案：*
  + 小明的判断正确。$abs(-100)=100 > abs(-1)=1$。在负数范围内，数字越大（绝对值越大）在数轴上反而越靠左；例如：$-3$ 比 $-1$ 更靠左，同时 $abs(-3)=3 > abs(-1)=1$，体现了”绝对值越大，在负半轴上越远离原点（越靠左）”的规律。
  + 如果 $a < 0$，那么 $-a$ 是*正*数；$abs(-a) = abs(a)$（绝对值与符号无关，只看到原点的距离）。
]
