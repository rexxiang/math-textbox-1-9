#import "../../lib/theme-v2.typ": blueprint, side-hack, secref

== 本章回顾：全书结束 <meta:next-learning-map-review>

#blueprint[
  *速查卡：12 条高中桥*

  #table(
    columns: (auto, auto),
    inset: 6pt,
    stroke: 0.4pt,
    align: left,
    [*分支*], [*高中桥*],
    [代数], [数列 · 二项式 · 复数 · 指对方程（4 条）],
    [几何], [向量 · 立体几何 · 圆锥曲线（3 条）],
    [函数], [三角函数 · 指对函数 · 导数（3 条）],
    [数据], [正态分布 + 假设检验 · 相关与回归（2 条）],
  )

  共 *12 条*。每条都从本书某节生长出去；该节就是你上桥前的最后一块地基。
]

#blueprint[
  *巩固复习：地基节 Top 10*

  如果你打算很快进高中，下列 10 节是最值得提前再确认*一遍*的（每节 $10$–$20$ 分钟）：

  + #secref("cb05-letters-represent-numbers")
  + #secref("cb06-equation-as-balance")
  + #secref("cb07-real-number-line")
  + #secref("al02-special-identities")
  + #secref("al05-quadratic-solving")
  + #secref("ge04-pythagorean")
  + #secref("ge02-similarity-aa-ratio")
  + #secref("fn03-formula-as-function-view")
  + #secref("fn06-slope-as-rate")
  + #secref("dt06-variance")

  这 10 节支撑了*几乎所有* 12 条高中桥。
]

#blueprint[
  *全书综合练习*

  // 跨章回顾
  + 已知二次函数 $y = x^2 - 6x + 8$ 的图象与 $x$ 轴交于 $A$、$B$ 两点（$A$ 在 $B$ 左边），顶点为 $C$。
    - (a) 求 $A$、$B$ 的坐标（#secref("al05-quadratic-solving")）。
    - (b) 求顶点 $C$ 的坐标（#secref("fn08-quadratic-function")）。
    - (c) 求 $triangle A B C$ 的面积（#secref("pf07-perimeter-area")）。
    - (d) 判断 $triangle A B C$ 是否为等腰三角形，并用两点距离公式验证（#secref("cb04-rectangular-system")）。

  // 跨章回顾
  + 甲、乙两人从同一地点出发，甲向东走 $8$ km，乙向北走 $6$ km。
    - (a) 求两人之间的直线距离（#secref("ge04-pythagorean")）。
    - (b) 以出发点为原点，东为 $x$ 正方向、北为 $y$ 正方向，写出甲、乙的坐标，并用两点距离公式验证 (a) 的结果（#secref("cb04-rectangular-system")）。
    - (c) 若甲的速度为 $4$ km/h、乙的速度为 $3$ km/h，写出两人出发 $t$ 小时后各自离出发点的距离函数，并求出发多少小时后两人相距恰好 $10$ km（#secref("fn05-linear-function")）。

  // 估算判断题
  + *估算判断*：以下四个说法，不用精确计算，判断每个是否合理，并简要说明理由。
    - (a) "一个边长为 $7$ cm 的正方形，面积大约是 $50$ cm#super[2]。"
    - (b) "一枚硬币抛 $1000$ 次，正面朝上 $723$ 次，所以这枚硬币正面概率约为 $0.72$。"
    - (c) "$sqrt(50)$ 大约是 $6$。"
    - (d) "某班 $50$ 人的数学平均分是 $85$ 分，那么至少有 $1$ 人得分 $>= 85$。"
]

==== 参考答案

+ (a) $x^2 - 6x + 8 = 0$，$(x - 2)(x - 4) = 0$。$A(2, 0)$，$B(4, 0)$。
  (b) $y = (x - 3)^2 - 1$，顶点 $C(3, -1)$。
  (c) $|A B| = 2$，$C$ 到 $x$ 轴距离 $= 1$。面积 $= 2 times 1 / 2 = 1$。
  (d) $|A C| = sqrt((3 - 2)^2 + (-1 - 0)^2) = sqrt(2)$，$|B C| = sqrt((4 - 3)^2 + (-1 - 0)^2) = sqrt(2)$。$|A C| = |B C|$，$triangle A B C$ *是*等腰三角形。

+ (a) $d = sqrt(8^2 + 6^2) = sqrt(64 + 36) = sqrt(100) = 10$ km。
  (b) 甲 $(8, 0)$，乙 $(0, 6)$。距离 $= sqrt((8 - 0)^2 + (0 - 6)^2) = sqrt(64 + 36) = 10$ km ✓。
  (c) 甲离出发点 $d_1 = 4t$，乙离出发点 $d_2 = 3t$。两人距离 $D = sqrt((4t)^2 + (3t)^2) = sqrt(25t^2) = 5t$。令 $5t = 10$，$t = 2$ 小时。

+ (a) 合理。$7^2 = 49 approx 50$ ✓。
  (b) 不太合理。大量试验中均匀硬币正面频率应接近 $0.5$，而 $0.723$ 严重偏离——这枚硬币很可能不均匀，或者记录有误。
  (c) 不合理。$7^2 = 49$，$8^2 = 64$，所以 $sqrt(50)$ 应略大于 $7$（约 $7.07$），而非 $6$。
  (d) 合理。若所有人得分都低于 $85$，总分 $< 50 times 85$，平均分 $< 85$，与已知矛盾。故至少有 $1$ 人得分 $>= 85$ ✓。

#blueprint[
  *过关信号（全书层）*

  ☞ 能在一张白纸上画出本书四支分支的知识地图主干（不看书）。 \
  ☞ 拿到任何一道题，能*先口述*“本题使用哪几支工具”再动笔。 \
  ☞ 知道自己哪几节*最可能卡*——并且已经排好补缺优先级。 \
  ☞ 对“下一阶段学什么”有具体的名字（不只是“学高中数学”），至少能点出 3 条高中桥。
]

#side-hack[
  *最后一句*

  数学不是一座山，是一张*持续扩张的网*。本书搭起的是这张网的最初几百条边——它已经足以支撑你一个人走下去。*稳住地基*，比赶进度更重要。祝你在往后的学习里始终保有“再往下钻一层就清楚了”的信心。
]
