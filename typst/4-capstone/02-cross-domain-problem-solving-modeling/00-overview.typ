#import "../../lib/theme-v2.typ": chapter-nav, chapter-route, side-hack, secref

== 跨领域解题与建模 <meta:cross-domain-problem-solving-modeling>

真实世界的问题不会贴着标签告诉你"请用代数"或"请用几何"。一道关于操场扩建的题，可能同时需要面积公式、一次方程和预算数据。从这一章开始，你要自己决定：该从工具箱里拿出哪几样。

#chapter-nav[
  *难度*：★★★★★ \
  *核心问题*：拿到一道*真实世界*的题，它并不会告诉你“这是代数题”“这是几何题”。题目里混着数、形、变化与数据。本章训练的就是：在不被*学科标签*提示的情况下，主动拆出这道题里*应该使用哪几条知识链*，并让不同分支的工具在同一道题里合作。 \
  *需要先会*：本章使用的每个工具都在前 3 部分学过，包括但不限于 #secref("al04-modeling-word-problems")、#secref("al05-quadratic-solving")、#secref("ge04-pythagorean")、#secref("ge02-similarity-aa-ratio")、#secref("fn05-linear-function")、#secref("fn08-quadratic-function")、#secref("fn11-model-choice")、#secref("dt03-center-and-spread")、#secref("dt08-probability-language")、#secref("dt09-experimental-frequency")。本章*不引入新数学概念*，只是给你一直在用的解题策略起正式名字，再让不同分支的工具在同一道题里合作。 \
  *本章在全书的位置*：综合运用层——像一场四支工具的合奏演出，每一题都标出谁出场、为何出场。
]

#chapter-route[

  + §1 *解题策略工具箱*：给你在前面章节反复用过的五条策略（画图、逆向、分类、不变量、特殊值）起正式名字。
  + §2 *综合建模流程*：读题 → 设元 → 建关系 → 求解 → 回检。3 个逐步示范题，每题至少跨 2 个分支。
  + §3 *跨领域已解案例*：4 道已解案例——几何+代数、函数+数据、数据+概率、代数+函数。重点展示"为什么该用这支工具"。
  + §4 *完整答卷示范*：3 道题，把*草稿 / 正式书写 / 回检*三栏分开——这是自学者最该模仿的一项工作流。
]

#include "01-strategy-toolbox.typ"
#include "02-multi-step-modeling.typ"
#include "03-geometry-algebra-data-mix.typ"
#include "04-full-solution-writeups.typ"
