#import "../../lib/theme-v2.typ": blueprint, side-hack, secref

=== 解题策略工具箱：给你一直在用的方法起个名字 <tool:cp03-strategy-toolbox>

#side-hack[
  下面五条策略不是新发明——你在前面的章节里已经反复用过它们。现在把它们集中列出来，是为了让你在面对一道"不知道从哪下手"的题时，能够有意识地逐条试探，而不是凭感觉乱撞。
]

#blueprint[
  *策略一：画图*

  遇到看不清关系的题目，先把信息画出来。

  - *几何题*：画辅助线、标已知量（#secref("ge01-congruence-correspondence")、#secref("ge04-pythagorean")）。
  - *代数应用题*：画线段图或示意图把"谁比谁多""谁是谁的几倍"变成可见的长度关系（#secref("al04-modeling-word-problems")）。
  - *函数题*：画草图判断走势，再用代数精确求解（#secref("fn05-linear-function")、#secref("fn08-quadratic-function")）。

  *什么时候用*：题目里有"长""宽""距离""比例""增减"但你还看不清它们怎么连在一起时。
]

#blueprint[
  *策略二：逆向思考*

  如果正面推导很复杂，试着从结论倒着走回去。

  - 要证 $A => B$，先假设 $B$ 成立，看需要什么条件，再检查那些条件是否就是 $A$。
  - 要求某个量，先假设答案是 $x$，列出 $x$ 满足的等式，然后解方程（#secref("cb06-equation-as-balance")）。
  - 应用题中，"最后变成了 …… 原来是多少？"天然适合从结果往前倒推。

  *什么时候用*：正面走了两三步就分叉太多，或题目直接给了结果让你找来源时。
]

#blueprint[
  *策略三：分类讨论*

  一条路走不通，可能是因为情况不止一种，需要分开处理。

  - 绝对值方程 $|x - 3| = 5$ 要分 $x >= 3$ 和 $x < 3$ 两种情况（#secref("al05-linear-inequalities")）。
  - 几何题里"点在线段上还是延长线上"可能导致不同的角关系。
  - 概率题里"至少一个"往往用"1 − 全都不满足"来分类（#secref("dt08-probability-language")）。

  *什么时候用*：题目中出现"或""至少""不确定正负"等字眼，或一个条件对应多种几何位置时。
]

#blueprint[
  *策略四：寻找不变量*

  当题目里有反复变化的操作，问自己："有没有什么东西在变化中一直不变？"

  - 等式两边同时做相同操作，等号不变（#secref("cb06-equality-properties")）。
  - 全等变换保持边长和角度不变（#secref("ge05-rigid-transforms")）。
  - 函数平移只改变位置，不改变形状。

  *什么时候用*：题目反复做同一种操作（折叠、旋转、加减同一个量），让你判断最终状态时。
]

#blueprint[
  *策略五：试特殊值 / 极端情况*

  在一般结论不好直接证明时，先用特殊数字试一试，找到规律再推广。

  - 猜测一个恒等式，先代入 $x = 0$、$x = 1$、$x = -1$ 检验（#secref("al02-special-identities")）。
  - 几何猜想先画最简单的图（等腰、直角）验证，再考虑一般情况。
  - 概率或统计问题用"只有 2 个数据"的极端简例快速检验公式是否合理（#secref("dt03-center-and-spread")）。

  *什么时候用*：面对"对所有 $x$ 成立"或"任意三角形"这类一般性问题，先用简单例子建立直觉。
]

#side-hack[
  这五条策略不是互斥的——一道好的综合题往往同时用到两三条。下面几节的每道示范题，都会在解题步骤里标出"本步用了哪条策略"，方便你回来对照。
]
