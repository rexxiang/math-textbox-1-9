#import "../../lib/theme-v2.typ": blueprint, side-hack, tryit, secref

=== 综合建模流程：读题 → 设元 → 关系 → 求解 → 回检 <tool:cp03-multi-step-modeling>

#blueprint[
  *五步法*

  + *读题*：把数据、图形、所求分开圈出。*单位*、*未知*、*已知*用不同符号标记。
  + *设元*：用字母命名所有未知量。参考关系模板 #secref("cb04-relationship-templates")。
  + *建关系*：把题目里的每一句话翻成一个等式或不等式，通常来自*几何定理*、*函数式*或*概率定义*。
  + *求解*：用代数工具解方程组或不等式（#secref("al04-linear-one-variable")、#secref("al04-systems-linear")、#secref("al05-quadratic-solving")）。
  + *回检*：代回原题验证*数值*与*物理合理性*。负数长度、超过 1 的概率等都是信号。
]

#blueprint[
  *示范题 1：矩形花园*（使用知识链：代数 + 几何）

  题目：一块矩形花园周长 $60$ m，对角线长 $6 sqrt(13)$ m。求长和宽。

  *读题*：已知周长 $= 60$，对角线 $= 6 sqrt(13)$；所求长、宽。

  *设元*：设长 $L$、宽 $W$（m），$L > W > 0$。

  *建关系*：
  + 周长公式（#secref("pf07-perimeter-area")）：$2 (L + W) = 60 =>$ $L + W = 30$。
  + 勾股定理（#secref("ge04-pythagorean")）：$L^2 + W^2 = (6 sqrt(13))^2 = 36 times 13 = 468$。

  *求解*：从 $(L + W)^2 = L^2 + 2 L W + W^2$ 得 $900 = 468 + 2 L W$，故 $L W = 216$。
  $L, W$ 是二次方程 $t^2 - 30 t + 216 = 0$ 的两根（#secref("al05-quadratic-solving")）：
  $ Delta = 900 - 864 = 36, quad t = (30 plus.minus 6) / 2 = 18 "或" 12. $

  *回检*：$L = 18, W = 12$。$18 + 12 = 30$ ✓；$18^2 + 12^2 = 324 + 144 = 468$ ✓；都为正 ✓。长 $18$ m，宽 $12$ m。
]

#blueprint[
  *示范题 2：书店周销量*（使用知识链：函数 + 数据）

  题目：一家小书店记录前 5 周销量（本）：

  #table(
    columns: (auto, auto, auto, auto, auto, auto),
    inset: 5pt,
    align: center,
    [*周 $x$*], [$1$], [$2$], [$3$], [$4$], [$5$],
    [*销量 $y$*], [$30$], [$43$], [$56$], [$69$], [$82$],
  )

  若趋势继续，第 $10$ 周销量大概多少？该模型成立的*前提*是什么？

  *读题*：已知 5 组 $(x, y)$；所求 $x = 10$ 时的 $y$ 估计。

  *设元*：设 $y$ 作为 $x$ 的函数 $y = f(x)$。

  *建关系*：按 #secref("fn11-model-choice") 的模型选择流程算相邻差：
  $ Delta y: space 43 - 30 = 13, space 56 - 43 = 13, space 69 - 56 = 13, space 82 - 69 = 13. $
  四个差*全都等于 $13$* $=>$ 线性关系成立（#secref("fn05-linear-function")），斜率 $k = 13$（#secref("fn06-slope-as-rate")）。

  *求解*：由 $x = 1, y = 30$ 定截距：$30 = 13 times 1 + b =>$ $b = 17$。故 $y = 13 x + 17$。$x = 10$ 时 $y = 130 + 17 = 147$ 本。

  *回检*：代入 $x = 5$：$13 times 5 + 17 = 82$ ✓。预测第 $10$ 周约 $147$ 本。

  *前提*：*前 5 周呈严格线性*。这属于描述性统计（#secref("dt02-charts-and-readings")），意味着“按过去趋势外推”。现实中影响销量的还有节假日、宣传、库存——模型只在*这些外部条件不剧变*的前提下有效。
]

#blueprint[
  *示范题 3：抛体运动*（使用知识链：代数 + 函数）

  题目：从 $2$ m 高处以初速度 $20$ m/s 竖直上抛一只小球，忽略空气阻力，高度随时间变化为
  $ h(t) = -5 t^2 + 20 t + 2 space ("m"), quad t >= 0 space ("s"). $
  求 (a) 最高点的高度；(b) 落地时刻。

  *读题*：已知 $h(t)$ 为二次函数（抛物线开口向下）；所求顶点与零点。

  *建关系*：
  - (a) 最高点即顶点（#secref("fn08-quadratic-function")）：对称轴 $t = -b / (2 a) = -20 / (-10) = 2$ s。
  - (b) 落地即 $h(t) = 0$。

  *求解*：
  - (a) $h(2) = -5 times 4 + 20 times 2 + 2 = -20 + 40 + 2 = 22$ m。
  - (b) $-5 t^2 + 20 t + 2 = 0$，即 $5 t^2 - 20 t - 2 = 0$。用求根公式（#secref("al05-quadratic-solving")）：
  $ Delta = 400 + 40 = 440, quad t = (20 plus.minus sqrt(440)) / 10 = (20 plus.minus 2 sqrt(110)) / 10 = 2 plus.minus sqrt(110) / 5. $
  $sqrt(110) approx 10.488$，所以 $t approx 2 + 2.098 = 4.098$ s 或 $approx -0.098$ s（舍负根）。

  *回检*：$t approx 4.10$ s 时代回 $h$：$-5 times 16.79 + 20 times 4.10 + 2 approx -83.95 + 82 + 2 = 0.05$ m，在舍入误差内 ✓。最高点 $22$ m 在 $t = 2$ s 出现，合乎“上抛初速度越大飞得越久”的直觉 ✓。
]

#tryit[
  *你来试（3 道，答案在 本章回顾）*

  + 一块三角形花坛，三边长为 $5, 12, 13$ m。(a) 它是直角三角形吗？(b) 求面积。（使用 ge04、pf07）

  + 某网店前 $6$ 周注册用户数（人）：$100, 150, 225, 338, 506, 760$（末位已四舍五入）。(a) 相邻差是否恒定？(b) 相邻比呢？(c) 属于哪一族？（使用 fn11、fn10）

  + 一个盒子装 $3$ 个红球、$5$ 个白球。不放回连抽 $2$ 次，两次都是红球的概率是多少？（使用 dt07、dt08）
]
