#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §5.6 一元二次方程 <sec-5-6>

#vocab[一元二次方程 quadratic equation / 判别式 discriminant / 韦达定理 Vieta's formulas / 配方法 completing the square]

#crisis[
  一个篮球被抛出后，高度随时间变化的关系为：

  $ h = -5 t^2 + 10 t + 2 $

  其中 $h$ 是高度（米），$t$ 是时间（秒）。

  *问题*：篮球什么时候落地？落地意味着 $h = 0$：

  $ -5 t^2 + 10 t + 2 = 0 $

  未知数的*平方*出现了！一元一次方程的方法不够用了——我们需要解*二次方程*的工具。
]

#history-note[
  古巴比伦人早在公元前 1800 年就能解二次方程了。印度数学家婆罗摩笈多（628 年）给出了求根公式的早期版本。现代形式的求根公式在 16 世纪由欧洲数学家完善。韦达在 1591 年发现了根与系数的关系——后人称之为「韦达定理」。
]

#discovery[
  *什么是一元二次方程？*

  含一个未知数，最高次数为 2 的方程。*一般形式*：

  $ a x^2 + b x + c = 0 quad (a != 0) $

  其中 $a$ 是二次项系数，$b$ 是一次项系数，$c$ 是常数项。

  *最简单的情况*：$x^2 = 9$

  显然 $x = 3$ 或 $x = -3$，即 $x = plus.minus 3$。

  这就是第一种解法——*直接开平方*。

  *稍复杂*：$(x - 1)^2 = 4$

  $x - 1 = plus.minus 2$，所以 $x = 3$ 或 $x = -1$。

  可如果方程不是完全平方的形式呢？比如 $x^2 + 6 x + 5 = 0$，能不能*凑成*完全平方？
]

#tryit[
  不看下面的内容，先试着解方程 $x^2 - 5 x + 6 = 0$。

  提示：能不能把左边写成两个括号相乘的形式？思考哪两个数，相乘等于 6，相加等于 $-5$。

  然后向下翻，看因式分解法（例 6）验证你的做法。
]

#blueprint[
  *一、直接开平方法*

  适用于 $x^2 = k$ 或 $(x + m)^2 = k$ 形式。

  *例 1*：解 $(2 x - 1)^2 = 9$

  $2 x - 1 = plus.minus 3$

  $2 x = 4$ 或 $2 x = -2$

  $x = 2$ 或 $x = -1$

  *二、配方法*

  核心思想：把 $a x^2 + b x + c = 0$ 变成 $(x + m)^2 = k$ 的形式。

  *例 2*：解 $x^2 + 6 x + 5 = 0$

  $ x^2 + 6 x = -5 $

  两边加 $9$（一次项系数一半的平方：$(frac(6, 2))^2 = 9$）：

  $ x^2 + 6 x + 9 = -5 + 9 $

  $ (x + 3)^2 = 4 $

  $ x + 3 = plus.minus 2 $

  $ x = -1 "或" x = -5 $

  *例 3*：解 $2 x^2 - 4 x - 1 = 0$

  先除以 2：$x^2 - 2 x - frac(1, 2) = 0$

  $ x^2 - 2 x = frac(1, 2) $

  $ x^2 - 2 x + 1 = frac(3, 2) $

  $ (x - 1)^2 = frac(3, 2) $

  $ x = 1 plus.minus sqrt(frac(3, 2)) = 1 plus.minus frac(sqrt(6), 2) $

  *三、求根公式*

  对一般方程 $a x^2 + b x + c = 0$，用配方法推导：

  $ x^2 + frac(b, a) x + frac(c, a) = 0 $

  $ (x + frac(b, 2 a))^2 = frac(b^2 - 4 a c, 4 a^2) $

  $ x = frac(-b plus.minus sqrt(b^2 - 4 a c), 2 a) $

  这就是*求根公式*——只要知道 $a, b, c$，直接代入就能得到解。

  *例 4*：解 $3 x^2 - 5 x + 1 = 0$

  $a = 3$，$b = -5$，$c = 1$

  $ x = frac(5 plus.minus sqrt(25 - 12), 6) = frac(5 plus.minus sqrt(13), 6) $

  *例 5*：回到开头的篮球问题 $-5 t^2 + 10 t + 2 = 0$

  $a = -5$，$b = 10$，$c = 2$

  $ t = frac(-10 plus.minus sqrt(100 + 40), -10) = frac(-10 plus.minus sqrt(140), -10) = frac(10 minus.plus 2 sqrt(35), 10) = 1 minus.plus frac(sqrt(35), 5) $

  取正值 $t = 1 + frac(sqrt(35), 5) approx 2.18$（秒）

  *四、因式分解法*

  如果方程左边能因式分解，直接令每个因式为零。

  *例 6*：解 $x^2 - 5 x + 6 = 0$

  $ (x - 2)(x - 3) = 0 $

  $ x = 2 "或" x = 3 $

  *例 7*：解 $2 x^2 + 3 x = 0$

  $ x(2 x + 3) = 0 $

  $ x = 0 "或" x = -frac(3, 2) $

  *五、判别式 $Delta$*

  定义 $Delta = b^2 - 4 a c$（读作「德尔塔」）。

  - $Delta > 0$：两个不相等的实数根
  - $Delta = 0$：两个相等的实数根（$x_1 = x_2 = -frac(b, 2 a)$）
  - $Delta < 0$：*无实数根*

  *例 8*：判断 $x^2 - 4 x + 5 = 0$ 的根的情况。

  $Delta = 16 - 20 = -4 < 0$，无实数根。

  *例 9*：若方程 $x^2 + m x + 4 = 0$ 有两个相等实数根，求 $m$。

  $Delta = m^2 - 16 = 0$，$m = plus.minus 4$。

  *六、韦达定理（根与系数的关系）*

  若 $x_1, x_2$ 是 $a x^2 + b x + c = 0$ 的两根，则：

  $ x_1 + x_2 = -frac(b, a), quad x_1 x_2 = frac(c, a) $

  *例 10*：已知 $x^2 - 5 x + 3 = 0$ 的两根为 $x_1, x_2$，求 $x_1^2 + x_2^2$。

  由韦达定理：$x_1 + x_2 = 5$，$x_1 x_2 = 3$

  $ x_1^2 + x_2^2 = (x_1 + x_2)^2 - 2 x_1 x_2 = 25 - 6 = 19 $
]

#pitfall[
  *三个经典陷阱*，二次方程最容易栽：

  *① 韦达定理符号写反*

  #grid(columns: (1fr, 1fr), gutter: 8pt,
    block[❌ $x^2 - 5x + 3 = 0$ \ $x_1 + x_2 = 5$（对）\ $x_1 x_2 = 3$（对）\ $x_1 + x_2 = frac(b, a) = frac(-5, 1) = -5$（❌ 忘了负号！）],
    block[✓ 记忆口诀：\ 和 $= -frac(b, a)$（有负号）\ 积 $= frac(c, a)$（无负号）\ 例：$a=1, b=-5, c=3$ 时\ 和 $= -(-5)/1 = 5$ ✓]
  )

  *② 判别式 $Delta < 0$ 时强行开根*

  $Delta = b^2 - 4 a c < 0$ → 此方程*没有实数解*，不能写出 $sqrt(Delta)$。

  ❌ 错误：$Delta = -4$，$x = frac(-b plus.minus sqrt(-4), 2 a)$（不合法）

  ✓ 正确：$Delta < 0$，无实数根，解题到此为止。

  *③ 配方时「补几」写错*

  配方关键：一次项系数的一半，再*平方*。$x^2 + 6x$，补 $(6/2)^2 = 9$，不是 $6^2 = 36$。
]

#side-hack[
  *选方法决策树*：
  + 能直接写成 $(x + m)^2 = k$ 吗？ → *直接开方*
  + 能轻松因式分解吗？ → *因式分解法*
  + 都不行 → *求根公式*（万能钥匙，永远管用！）

  配方法一般用于推导和证明，做题时直接用公式更快。
]

#mastery[
  *基础*

  + 用直接开方法解：
    - $(x + 2)^2 = 16$
    - $4 x^2 - 25 = 0$
  + 用因式分解法解：
    - $x^2 - 7 x + 10 = 0$
    - $x^2 + 4 x = 0$
    - $3 x^2 - 12 = 0$
  + 用求根公式解：
    - $2 x^2 - 3 x - 2 = 0$
    - $x^2 + 4 x + 1 = 0$

  *应用*

  + 不解方程，判断下列方程根的情况：
    - $x^2 - 6 x + 9 = 0$
    - $2 x^2 + x + 1 = 0$
    - $3 x^2 - x - 1 = 0$
  + 一块矩形花圃，长比宽多 3 m，面积为 40 m²。求长和宽。
  + 一个数比它的倒数大 $frac(3, 2)$，求这个数。
  + 已知 $x^2 - 3 x - 1 = 0$ 的两根为 $x_1, x_2$，求 $frac(1, x_1) + frac(1, x_2)$ 的值。

  *挑战*

  + 用配方法解 $3 x^2 + 8 x - 3 = 0$。
  + 若方程 $x^2 - (2 k - 1) x + k^2 - k = 0$ 有两个正整数根，求所有可能的 $k$ 值。
  + 已知方程 $a x^2 + b x + c = 0$（$a != 0$）的两根之差为 $d$，证明 $d^2 = frac(b^2 - 4 a c, a^2)$。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + $x + 2 = plus.minus 4$，$x = 2$ 或 $x = -6$；$x^2 = frac(25, 4)$，$x = plus.minus frac(5, 2)$
  + $(x - 2)(x - 5) = 0$，$x = 2$ 或 $x = 5$；$x(x + 4) = 0$，$x = 0$ 或 $x = -4$；$3(x^2 - 4) = 0$，$x = plus.minus 2$
  + $x = frac(3 plus.minus sqrt(9 + 16), 4) = frac(3 plus.minus 5, 4)$，$x = 2$ 或 $x = -frac(1, 2)$；$x = frac(-4 plus.minus sqrt(16 - 4), 2) = -2 plus.minus sqrt(3)$

  *应用*
  + $Delta = 36 - 36 = 0$，两个相等实根；$Delta = 1 - 8 = -7 < 0$，无实根；$Delta = 1 + 12 = 13 > 0$，两个不等实根
  + 设宽 $x$ m：$x(x + 3) = 40$，$x^2 + 3 x - 40 = 0$，$(x + 8)(x - 5) = 0$，$x = 5$（负值舍去）。宽 5 m，长 8 m。
  + 设此数 $x$：$x - frac(1, x) = frac(3, 2)$，乘以 $2 x$：$2 x^2 - 3 x - 2 = 0$，$(2 x + 1)(x - 2) = 0$，$x = 2$ 或 $x = -frac(1, 2)$。
  + $frac(1, x_1) + frac(1, x_2) = frac(x_1 + x_2, x_1 x_2) = frac(3, -1) = -3$

  *挑战*
  + $x^2 + frac(8, 3) x - 1 = 0$，$x^2 + frac(8, 3) x + frac(16, 9) = 1 + frac(16, 9) = frac(25, 9)$，$(x + frac(4, 3))^2 = frac(25, 9)$，$x = -frac(4, 3) plus.minus frac(5, 3)$，$x = frac(1, 3)$ 或 $x = -3$
  + 韦达定理：$x_1 + x_2 = 2 k - 1$，$x_1 x_2 = k^2 - k = k(k - 1)$。两根为正整数，需 $Delta >= 0$：$(2 k - 1)^2 - 4 k(k - 1) = 4 k^2 - 4 k + 1 - 4 k^2 + 4 k = 1 > 0$，恒成立。$x_1 x_2 = k(k - 1)$，$x_1 + x_2 = 2 k - 1$。设 $x_1 = k$，$x_2 = k - 1$（代入验证两根之和确为 $2 k - 1$，积为 $k(k - 1)$）。需两根都为正整数，$k >= 2$ 且 $k$ 为整数。取 $k = 2$ 时 $x_1 = 2, x_2 = 1$；实际上对所有整数 $k >= 2$，均可。但题目问所有可能的 $k$，答案为 $k >= 2$ 的正整数。
  + $(x_1 - x_2)^2 = (x_1 + x_2)^2 - 4 x_1 x_2 = frac(b^2, a^2) - frac(4 c, a) = frac(b^2 - 4 a c, a^2)$。所以 $d^2 = frac(b^2 - 4 a c, a^2)$。证毕。
]

=== 拓展：韦达定理的深入应用

#mastery[
  回顾：若 $x_1$、$x_2$ 是 $a x^2 + b x + c = 0$ 的两根，则 $x_1 + x_2 = -frac(b, a)$，$x_1 x_2 = frac(c, a)$。

  *技巧 1：不求根计算对称式*

  $x_1^2 + x_2^2 = (x_1 + x_2)^2 - 2 x_1 x_2$

  *例*：$x^2 - 5 x + 3 = 0$ 的两根为 $x_1, x_2$，求 $x_1^2 + x_2^2$。

  解：$x_1 + x_2 = 5$，$x_1 x_2 = 3$。$x_1^2 + x_2^2 = 25 - 6 = 19$。

  *技巧 2：由根构造方程*

  已知两根 $alpha, beta$，方程为 $x^2 - (alpha + beta)x + alpha beta = 0$。

  *例*：构造以 $3 + sqrt(2)$ 和 $3 - sqrt(2)$ 为根的一元二次方程。

  解：和 $= 6$，积 $= 9 - 2 = 7$。方程为 $x^2 - 6x + 7 = 0$。

  *挑战：不求根判断两根符号*

  当方程有两个实根时（即 $Delta >= 0$），由韦达定理 $x_1 x_2 = frac(c, a)$、$x_1 + x_2 = -frac(b, a)$ 可判断根的符号：
  - 两根同正 $arrow.l.r.double$ 积 $> 0$ 且和 $> 0$
  - 两根同负 $arrow.l.r.double$ 积 $> 0$ 且和 $< 0$
  - 一正一负 $arrow.l.r.double$ 积 $< 0$
]
