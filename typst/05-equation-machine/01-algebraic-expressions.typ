#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab

== §5.1 代数式与整式 <sec-5-1>

#vocab[代数式 algebraic expression / 单项式 monomial / 多项式 polynomial / 同类项 like terms]

#crisis[
  学校组织春游，门票价格如下：成人票每张 40 元，学生票每张 25 元。

  如果有 $n$ 个学生和 2 个老师，总门票费用怎么写？

  当 $n = 30$ 时你可以直接算：$25 times 30 + 40 times 2 = 830$。

  但如果人数还没确定呢？你需要写一个*通用的表达式*，不管 $n$ 是多少都能用：

  $ 25 n + 80 $

  这就是一个*代数式*——用字母和数字搭建的「计算模板」。
]

#history-note[
  16 世纪法国数学家弗朗索瓦·韦达（François Viète）首次系统地用字母表示已知数和未知数。在他之前，人们只能用文字叙述方程——想象一下用一整段话来描述 $a x^2 + b x + c = 0$！韦达的符号革命让代数成为了一门真正的「符号语言」。
]

#discovery[
  *第一步*：用字母表示数。

  - 小明每天跑 $x$ 千米，一周跑 $7 x$ 千米
  - 长方形长 $a$，宽 $b$，面积 $a b$，周长 $2(a + b)$

  像 $7 x$、$a b$、$2(a + b)$、$25 n + 80$ 这些含有字母的式子，都叫*代数式*。

  *第二步*：认识单项式。

  $3 x^2$、$-5 a b$、$7$、$frac(1, 2) x y^2$ 这些式子有什么共同点？

  它们都是数与字母的*乘积*（包括单独一个数或一个字母）。我们把它们叫做*单项式*。

  - *系数*：单项式中的数字因数。$3 x^2$ 的系数是 $3$；$-5 a b$ 的系数是 $-5$
  - *次数*：所有字母指数之和。$3 x^2$ 是 2 次；$-5 a b$ 是 2 次；$frac(1, 2) x y^2$ 是 3 次

  *第三步*：认识多项式。

  几个单项式的*和*叫做*多项式*。

  $ 3 x^2 - 5 x + 7 $

  这个多项式有 3 项，最高次项是 $3 x^2$，所以它是*二次三项式*。

  单项式和多项式统称*整式*。

  *第四步*：发现同类项。

  观察 $3 x^2$ 和 $-7 x^2$——字母和指数完全相同，只是系数不同。这样的项叫*同类项*。

  同类项可以合并：$3 x^2 + (-7 x^2) = (3 - 7) x^2 = -4 x^2$
]

#side-hack[
  合并同类项的口诀——*字母指数不变，系数相加减*。就像收拾房间：3 个苹果加 5 个苹果等于 8 个苹果，$3 x$ 加 $5 x$ 等于 $8 x$，但 $3 x$ 和 $5 y$ 不能合并——苹果和橘子不是一类！
]

#blueprint[
  *一、整式加减——合并同类项*

  合并同类项的步骤：
  + 找出同类项（字母和指数都相同的项）
  + 系数相加减，字母部分不变

  *例 1*：化简 $(3 x^2 - 2 x + 5) + (x^2 + 4 x - 3)$

  $ &= 3 x^2 - 2 x + 5 + x^2 + 4 x - 3 \
    &= (3 + 1) x^2 + (-2 + 4) x + (5 - 3) \
    &= 4 x^2 + 2 x + 2 $

  *例 2*：化简 $(5 a^2 b - 3 a b) - (2 a^2 b + a b - 4)$

  $ &= 5 a^2 b - 3 a b - 2 a^2 b - a b + 4 \
    &= (5 - 2) a^2 b + (-3 - 1) a b + 4 \
    &= 3 a^2 b - 4 a b + 4 $

  注意：去括号时，减号后面的括号内*每一项都变号*。

  *二、整式乘法*

  *单项式乘单项式*：系数相乘，同底数幂相加。

  $ 3 x^2 dot (-2 x^3) = -6 x^5 $

  *单项式乘多项式*：分配律。

  $ 2 x(3 x^2 - 4 x + 1) = 6 x^3 - 8 x^2 + 2 x $

  *多项式乘多项式*：每一项都要乘。

  $ (a + b)(c + d) = a c + a d + b c + b d $

  *例 3*：展开 $(2 x + 3)(x - 5)$

  $ &= 2 x dot x + 2 x dot (-5) + 3 dot x + 3 dot (-5) \
    &= 2 x^2 - 10 x + 3 x - 15 \
    &= 2 x^2 - 7 x - 15 $

  *三、乘法公式*

  通过多项式乘法，我们发现两个特别重要的公式：

  *平方差公式*：

  $ (a + b)(a - b) = a^2 - b^2 $

  验证：$(a + b)(a - b) = a^2 - a b + a b - b^2 = a^2 - b^2$ ✓

  *例 4*：$101 times 99 = (100 + 1)(100 - 1) = 100^2 - 1^2 = 9999$

  *完全平方公式*：

  $ (a + b)^2 = a^2 + 2 a b + b^2 \
    (a - b)^2 = a^2 - 2 a b + b^2 $

  *例 5*：展开 $(3 x - 2)^2$

  $ (3 x - 2)^2 = (3 x)^2 - 2 dot 3 x dot 2 + 2^2 = 9 x^2 - 12 x + 4 $

  *四、整式除法*

  *单项式除以单项式*：系数相除，同底数幂相减。

  $ 12 x^3 y^2 div 4 x y = 3 x^2 y $

  *多项式除以单项式*：每一项分别除。

  $ (6 x^3 - 9 x^2 + 3 x) div 3 x = 2 x^2 - 3 x + 1 $
]

#mastery[
  *基础*

  + 指出下列各式的系数和次数：$-3 x^2 y$、$frac(2, 3) a^3 b^2$、$-x$
  + 合并同类项：
    - $5 a - 3 b + 2 a + 7 b$
    - $x^2 - 3 x y + 2 x^2 + x y - 4$
  + 计算：
    - $(-2 x)^3$
    - $3 a^2 b dot (-4 a b^3)$
    - $-2 x(x^2 - 3 x + 1)$

  *应用*

  + 用乘法公式计算：
    - $(x + 5)(x - 5)$
    - $(2 a + 3 b)^2$
    - $998 times 1002$（提示：写成 $(1000 - 2)(1000 + 2)$）
  + 展开并化简：$(x + 1)(x - 3) - (x - 2)^2$

  *挑战*

  + 若 $x + y = 5$，$x y = 6$，求 $x^2 + y^2$ 和 $(x - y)^2$ 的值。
  + 已知 $(a + b)^2 - (a - b)^2 = k a b$，求常数 $k$ 的值。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + $-3 x^2 y$：系数 $-3$，次数 $3$；$frac(2, 3) a^3 b^2$：系数 $frac(2, 3)$，次数 $5$；$-x$：系数 $-1$，次数 $1$
  + $5 a - 3 b + 2 a + 7 b = 7 a + 4 b$；$x^2 - 3 x y + 2 x^2 + x y - 4 = 3 x^2 - 2 x y - 4$
  + $(-2 x)^3 = -8 x^3$；$3 a^2 b dot (-4 a b^3) = -12 a^3 b^4$；$-2 x(x^2 - 3 x + 1) = -2 x^3 + 6 x^2 - 2 x$

  *应用*
  + $(x + 5)(x - 5) = x^2 - 25$；$(2 a + 3 b)^2 = 4 a^2 + 12 a b + 9 b^2$；$998 times 1002 = (1000 - 2)(1000 + 2) = 1000^2 - 4 = 999996$
  + $(x + 1)(x - 3) - (x - 2)^2 = (x^2 - 2 x - 3) - (x^2 - 4 x + 4) = 2 x - 7$

  *挑战*
  + $x^2 + y^2 = (x + y)^2 - 2 x y = 25 - 12 = 13$；$(x - y)^2 = (x + y)^2 - 4 x y = 25 - 24 = 1$
  + 左边 $= (a^2 + 2 a b + b^2) - (a^2 - 2 a b + b^2) = 4 a b$。所以 $k = 4$。
]
