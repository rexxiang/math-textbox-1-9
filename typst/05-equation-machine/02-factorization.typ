#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, secref, tryit, pitfall

== §5.2 因式分解 <sec-5-2>

#vocab[因式分解 factorization / 公因式 common factor / 十字相乘 cross multiplication]

#crisis[
  在 #secref("5.1") 中，你学会了展开：

  $ (x + 3)(x - 2) = x^2 + x - 6 $

  现在反过来：给你 $x^2 + x - 6$，*能否还原成两个因式的乘积？*

  就好像你很容易把一个鸡蛋打碎，但能否把碎蛋壳重新拼回去？

  展开是「拆散」，因式分解是「拼回」。这个逆向技能在解方程时至关重要。
]

#discovery[
  *什么是因式分解？*

  把一个多项式写成几个*整式的乘积*的形式，叫做*因式分解*（也叫分解因式）。

  $
    x^2 + x - 6 = (x + 3)(x - 2)
  $

  注意区分：
  - 展开：$(x + 3)(x - 2) arrow.r x^2 + x - 6$（乘积 → 和）
  - 因式分解：$x^2 + x - 6 arrow.r (x + 3)(x - 2)$（和 → 乘积）

  它们是*互逆运算*。

  因式分解什么时候有用？

  当你要解 $x^2 + x - 6 = 0$ 时——如果左边 $= (x + 3)(x - 2)$，那么只要 $x + 3 = 0$ 或 $x - 2 = 0$，就有 $x = -3$ 或 $x = 2$。比硬算快得多！
]

#side-hack[
  分解因式的顺序口诀——*一提二看三十字*。

  + *一提*：先看有没有公因式可以提取
  + *二看*：再看能否套乘法公式（平方差、完全平方）
  + *三十字*：最后尝试十字相乘法

  按这个顺序来，绝大多数因式分解都能搞定！
]

#tryit[
  先试一试：把 $x^2 + 5x + 6$ 分解因式。

  思路：找两个数，它们的*乘积等于 6*，*和等于 5*。想想哪对整数符合条件？

  写出答案后，对照 blueprint 里的例 7 验证。
]

#blueprint[
  *一、提公因式法*

  如果多项式各项有*公因式*（公共的因数和字母），就把它提到括号外面。

  *例 1*：分解 $6 x^3 - 9 x^2 + 3 x$

  各项公因式是 $3 x$：

  $ 6 x^3 - 9 x^2 + 3 x = 3 x(2 x^2 - 3 x + 1) $

  *例 2*：分解 $4 a(x - y) + 2 b(x - y)$

  公因式是 $2(x - y)$：

  $ 4 a(x - y) + 2 b(x - y) = 2(x - y)(2 a + b) $

  *二、公式法*

  *平方差公式*：$a^2 - b^2 = (a + b)(a - b)$

  *例 3*：分解 $4 x^2 - 9$

  $ 4 x^2 - 9 = (2 x)^2 - 3^2 = (2 x + 3)(2 x - 3) $

  *例 4*：分解 $x^4 - 1$

  $ x^4 - 1 = (x^2)^2 - 1^2 = (x^2 + 1)(x^2 - 1) = (x^2 + 1)(x + 1)(x - 1) $

  注意：$x^2 + 1$ 在实数范围内*不能再分解*。

  *完全平方公式*：

  $ a^2 + 2 a b + b^2 = (a + b)^2 \
    a^2 - 2 a b + b^2 = (a - b)^2 $

  *例 5*：分解 $x^2 + 6 x + 9$

  $ x^2 + 6 x + 9 = x^2 + 2 dot x dot 3 + 3^2 = (x + 3)^2 $

  *例 6*：分解 $4 a^2 - 12 a b + 9 b^2$

  $ 4 a^2 - 12 a b + 9 b^2 = (2 a)^2 - 2 dot 2 a dot 3 b + (3 b)^2 = (2 a - 3 b)^2 $

  *判断完全平方的技巧*：检查首末两项是否为完全平方数，中间项是否等于首末两项算术平方根之积的 2 倍。

  *三、十字相乘法*

  对于 $x^2 + b x + c$ 形式：找两个数 $p$ 和 $q$，使得 $p + q = b$ 且 $p q = c$。

  $ x^2 + b x + c = (x + p)(x + q) $

  *例 7*：分解 $x^2 + 5 x + 6$

  找两个数，和为 $5$，积为 $6$ → $p = 2$，$q = 3$

  $ x^2 + 5 x + 6 = (x + 2)(x + 3) $

  *例 8*：分解 $x^2 - 3 x - 10$

  找两个数，和为 $-3$，积为 $-10$ → $p = 2$，$q = -5$

  $ x^2 - 3 x - 10 = (x + 2)(x - 5) $

  十字相乘法的名字来源于以下「交叉」图示：把 $p$ 和 $q$ 写在右侧，$1$ 和 $1$ 写在左侧，交叉相乘再相加应等于中间项系数。

  *四、综合运用*

  复杂的因式分解往往需要*多步*：先提公因式，再用公式或十字相乘。

  *例 9*：分解 $2 x^3 - 8 x$

  $ 2 x^3 - 8 x &= 2 x(x^2 - 4) \
               &= 2 x(x + 2)(x - 2) $

  *例 10*：分解 $-3 a^2 + 6 a b - 3 b^2$

  $ -3 a^2 + 6 a b - 3 b^2 &= -3(a^2 - 2 a b + b^2) \
                            &= -3(a - b)^2 $
]

#pitfall[
  *两个最坑的因式分解错误：*

  *① 完全平方公式用错——把 $a^2 + b^2$ 当成 $(a+b)^2$*

  ❌ $x^2 + 9 = (x+3)^2$（不对！完全平方展开是 $x^2 + 6 x + 9$，中间有 $2 a b$ 项）

  ✓ $x^2 + 9$ *在实数范围内不能因式分解*。

  记住完全平方公式：$(a+b)^2 = a^2 + bold(2 a b) + b^2$，中间那一项不能少。

  *② 分解不彻底——提公因式后停手，没继续分解*

  ❌ $3x^2 - 27 = 3(x^2 - 9)$（停在这里）

  ✓ $3x^2 - 27 = 3(x^2 - 9) = 3(x+3)(x-3)$（继续用平方差分解）

  口诀：*分解到每个括号里面不能再分为止！*
]

#mastery[
  *基础*

  + 提公因式法分解：
    - $8 a^2 b - 12 a b^2$
    - $5 x(a + b) - 10 y(a + b)$
  + 用公式法分解：
    - $25 x^2 - 16$
    - $a^2 + 10 a + 25$
    - $9 m^2 - 24 m n + 16 n^2$
  + 用十字相乘法分解：
    - $x^2 + 7 x + 12$
    - $x^2 - 2 x - 15$
    - $x^2 - 8 x + 15$

  *应用*

  + 综合分解（按「一提二看三十字」顺序）：
    - $3 x^2 - 27$
    - $2 a^3 - 4 a^2 + 2 a$
    - $x^3 - x^2 - 6 x$
  + 利用因式分解计算：$73^2 - 27^2$

  *挑战*

  + 分解 $x^2 + 2 x y + y^2 - 9$（提示：先把前三项合并）
  + 分解 $(x^2 + x)^2 - 8(x^2 + x) + 12$（提示：设 $t = x^2 + x$）
  + 已知 $a + b = 7$，$a b = 12$，利用因式分解求 $a^2 - b^2$ 的值（若 $a > b$）。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + $8 a^2 b - 12 a b^2 = 4 a b(2 a - 3 b)$；$5 x(a + b) - 10 y(a + b) = 5(a + b)(x - 2 y)$
  + $25 x^2 - 16 = (5 x + 4)(5 x - 4)$；$a^2 + 10 a + 25 = (a + 5)^2$；$9 m^2 - 24 m n + 16 n^2 = (3 m - 4 n)^2$
  + $x^2 + 7 x + 12 = (x + 3)(x + 4)$；$x^2 - 2 x - 15 = (x + 3)(x - 5)$；$x^2 - 8 x + 15 = (x - 3)(x - 5)$

  *应用*
  + $3 x^2 - 27 = 3(x^2 - 9) = 3(x + 3)(x - 3)$；$2 a^3 - 4 a^2 + 2 a = 2 a(a^2 - 2 a + 1) = 2 a(a - 1)^2$；$x^3 - x^2 - 6 x = x(x^2 - x - 6) = x(x + 2)(x - 3)$
  + $73^2 - 27^2 = (73 + 27)(73 - 27) = 100 times 46 = 4600$

  *挑战*
  + $x^2 + 2 x y + y^2 - 9 = (x + y)^2 - 3^2 = (x + y + 3)(x + y - 3)$
  + 设 $t = x^2 + x$，原式 $= t^2 - 8 t + 12 = (t - 2)(t - 6) = (x^2 + x - 2)(x^2 + x - 6) = (x + 2)(x - 1)(x + 3)(x - 2)$
  + $(a - b)^2 = (a + b)^2 - 4 a b = 49 - 48 = 1$，所以 $a - b = 1$（因为 $a > b$）。$a^2 - b^2 = (a + b)(a - b) = 7 times 1 = 7$
]

=== 拓展：立方和与立方差公式

#mastery[
  *公式*
  $ a^3 + b^3 = (a + b)(a^2 - a b + b^2) $
  $ a^3 - b^3 = (a - b)(a^2 + a b + b^2) $

  *验证*：展开右边。以第二个为例：
  $ (a - b)(a^2 + a b + b^2) = a^3 + a^2 b + a b^2 - a^2 b - a b^2 - b^3 = a^3 - b^3 quad checkmark $

  *例题*：分解 $x^3 - 8$。

  $8 = 2^3$，所以 $x^3 - 8 = x^3 - 2^3 = (x - 2)(x^2 + 2x + 4)$。

  *挑战*：完全分解 $x^6 - 1$。

  提示：$x^6 - 1 = (x^3)^2 - 1 = (x^3 - 1)(x^3 + 1)$，然后对每个因式再用立方差/和公式。

  答：$x^6 - 1 = (x - 1)(x^2 + x + 1)(x + 1)(x^2 - x + 1)$。
]
