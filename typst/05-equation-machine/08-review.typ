#import "../lib/theme-v2.typ": blueprint, mastery, side-hack, secref

== 第五章回顾：方程——未知数猎手

#blueprint[
  *知识速查卡*

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *代数式与整式*（#secref("5.1")）
      - 乘法公式：$(a+b)(a-b) = a^2 - b^2$
      - 完全平方：$(a pm b)^2 = a^2 pm 2ab + b^2$
      - 单项式次数：所有字母指数之和
    ],
    block[
      *因式分解*（#secref("5.2")）
      - 顺序：一提公因式、二看公式、三十字相乘
      - $a^2 - b^2 = (a+b)(a-b)$
      - $a^2 pm 2ab + b^2 = (a pm b)^2$
    ],
  )

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *一元一次方程*（#secref("5.4")）
      - 五步：去分母→去括号→移项→合并→系数化1
      - 移项变号！
      - 分式方程需验根
    ],
    block[
      *一元二次方程*（#secref("5.6")）
      - 求根公式：$x = frac(-b pm sqrt(b^2 - 4ac), 2a)$
      - 判别式 $Delta = b^2 - 4ac$
      - 韦达定理：$x_1 + x_2 = -frac(b, a)$，$x_1 x_2 = frac(c, a)$
    ],
  )

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *二元一次方程组*（#secref("5.5")）
      - 代入法：解出一个，代入另一个
      - 加减法：系数变相同，两式加减消元
      - 验根：代入*两个*原方程
    ],
    block[
      *不等式*（#secref("5.7")）
      - 乘除负数：不等号*必须*反转
      - 解集：数轴上用空心/实心圆表示
      - 不等式组：取各不等式解集的交集
    ],
  )
]

#mastery[
  *混合自测题*（不标知识点，综合调用）

  + 化简：$(3x - 2)^2 - (3x + 2)(3x - 2)$

  + 因式分解：$2x^3 - 8x$

  + 解方程：$frac(x + 1, 3) - frac(2x - 1, 6) = 1$

  + 解方程组：$cases(3x + 2y = 16, 5x - 2y = 8)$

  + 用求根公式解：$2x^2 - 3x - 2 = 0$

  + 解不等式，在数轴上标出解集：$-3x + 6 > 0$

  + 若方程 $x^2 - 5x + m = 0$ 有两个相等实数根，求 $m$ 的值。

  + 已知 $a - b = 3$，$ab = -10$，求 $a^2 + b^2$ 的值。

  + 解方程 $frac(2, x - 1) + 1 = frac(3, x - 1)$（注意验根！）

  + 一个长方形的长比宽多 $4$ 厘米，面积为 $60$ cm²。求长和宽。

  + *进阶*：解不等式组 $cases(2x - 1 > 3, x + 4 <= 10)$，并求满足条件的所有整数 $x$。

  + *进阶*：已知 $x_1, x_2$ 是方程 $x^2 + px + q = 0$ 的两根，且 $x_1 + x_2 = -3$，$x_1^2 + x_2^2 = 13$。求 $p$ 和 $q$ 的值。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  + $(3x-2)^2 - (3x+2)(3x-2) = (3x-2)[(3x-2) - (3x+2)] = (3x-2)(-4) = -12x + 8$
  + $2x^3 - 8x = 2x(x^2 - 4) = 2x(x+2)(x-2)$
  + 两边乘 6：$2(x+1) - (2x-1) = 6$，$2x+2-2x+1 = 6$，$3 = 6$——矛盾！无解。
  + 两式相加 $8x = 24$，$x = 3$；代入得 $y = frac(7, 2)$
  + $x = frac(3 pm sqrt(9+16), 4) = frac(3 pm 5, 4)$，$x = 2$ 或 $x = -frac(1, 2)$
  + $-3x > -6$，除以 $-3$（变号！），$x < 2$
  + $Delta = 25 - 4m = 0$，$m = frac(25, 4)$
  + $a^2 + b^2 = (a-b)^2 + 2ab = 9 + (-20) = -11$
  + 两边乘 $(x-1)$：$2 + (x-1) = 3$，$x = 2$。验根：$x=2$ 时 $x-1=1 != 0$，有效。$x = 2$
  + 设宽 $x$，长 $x+4$：$x(x+4) = 60$，$x^2+4x-60=0$，$(x+10)(x-6)=0$，$x=6$（取正）。宽 6 cm，长 10 cm。
  + 解第一个：$x > 2$；解第二个：$x <= 6$。解集 $2 < x <= 6$，整数为 $3, 4, 5, 6$
  + 韦达：$x_1+x_2 = -p = -3$，$p = 3$。$x_1^2+x_2^2 = (x_1+x_2)^2 - 2x_1x_2 = 9-2q = 13$，$q = -2$

  *螺旋复习（与其他章节的联系）*

  + 第 2 章的分数知识——解方程 $frac(x, 3) + frac(x}{4} = 1$，需要通分，这是 #secref("2.2") 的分数加法。（答：乘 12，$4x + 3x = 12$，$x = frac(12, 7)$）

  + 第 7 章将学一次函数 $y = kx + b$。如果一次函数过点 $(0, 3)$ 和 $(2, 7)$，用本章的方程组方法求 $k$ 和 $b$。（答：$b = 3$，$2k+3=7$，$k=2$）

  + 第 6 章勾股定理会遇到 $a^2 + b^2 = c^2$——这是一个含平方的方程，你现在已经学会了用求根公式或因式分解来解决它！
]
