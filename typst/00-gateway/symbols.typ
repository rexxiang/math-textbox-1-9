== 符号约定

本书使用以下数学符号。它们按「发明时间线」排列——最古老的符号在前，最年轻的在后。

=== 基础运算符号

#table(
  columns: (auto, auto, 1fr),
  inset: 8pt,
  stroke: 0.4pt,
  table.header([*符号*], [*含义*], [*发明故事*]),
  [$+$, $-$], [加、减], [15 世纪德国商人的仓库记号],
  [$times$, $div$], [乘、除], [$times$ 由威廉·奥特雷德于 1631 年引入],
  [$=$], [等于], [罗伯特·雷科德 1557 年发明——「没有什么比两条平行线更相等」],
  [$!=$], [不等于], [表示两个量不相同],
  [$<$, $>$], [小于、大于], [托马斯·哈里奥特 1631 年引入],
  [$<=$, $>=$], [小于等于、大于等于], [比较大小的组合符号],
)

=== 数集符号

#table(
  columns: (auto, auto, 1fr),
  inset: 8pt,
  stroke: 0.4pt,
  table.header([*符号*], [*名称*], [*含义*]),
  [$NN$], [自然数集], [$0, 1, 2, 3, dots.h$],
  [$ZZ$], [整数集], [$dots.h, -2, -1, 0, 1, 2, dots.h$],
  [$QQ$], [有理数集], [所有能写成分数的数],
  [$RR$], [实数集], [数轴上所有的点],
)

=== 几何符号

#table(
  columns: (auto, auto),
  inset: 8pt,
  stroke: 0.4pt,
  table.header([*符号*], [*含义*]),
  [$angle$], [角],
  [$triangle$], [三角形],
  [$tilde.eq$], [全等],
  [$tilde$], [相似],
  [$perp$], [垂直],
  [$parallel$], [平行],
)

=== 代数与函数符号

#table(
  columns: (auto, auto),
  inset: 8pt,
  stroke: 0.4pt,
  table.header([*符号*], [*含义*]),
  [$pi$], [圆周率（$approx 3.14159$）],
  [$sqrt(space)$], [平方根],
  [$abs(space)$], [绝对值],
  [$Delta$], [判别式（$b^2 - 4a c$）],
)
