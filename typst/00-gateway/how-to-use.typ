== 使用指南

=== 先看懂这本书的地图

这本书按「*主干 → 闸门 → 分支 → 终章*」来组织。你可以把它想成一次长期探险：

#table(
  columns: (auto, 1fr),
  inset: 8pt,
  stroke: 0.4pt,
  table.header([*位置*], [*它负责什么*]),
  [*共同主干*], [先把几乎所有后续内容都会反复调用的基础工具装稳],
  [*主干闸门*], [在换路线前做一次总检查，确认旧工具已经能混合使用],
  [*深化分支*], [按问题类型分别深入：有的分支更重关系与运算，有的更重图形、变化或数据],
  [*终章*], [把整本书重新连起来，看见工具之间的远距离呼应],
)

=== 四种阅读路线

如果你不知道从哪里开始，可以直接照下面四条路线选一种：

#table(
  columns: (auto, 1fr),
  inset: 8pt,
  stroke: 0.4pt,
  table.header([*路线*], [*建议怎么走*]),
  [*主干路线*], [按顺序先走共同主干。适合第一次系统学习，或者想把底盘补齐的人。],
  [*闸门路线*], [当你觉得“前面都学过，但连不起来”时，先做一次主干总检，再决定哪里要回看。],
  [*分支路线*], [当共同主干已经比较稳时，挑一条深化分支连续读完，先形成一条完整工具链。],
  [*自学路线*], [先看全书地图，再按“当前问题需要什么工具”逆向找路；卡住时，优先回主干与闸门补地基。],
)

=== 每一节怎么学

每个知识点内部，通常都会重复同样的学习节奏：

#table(
  columns: (auto, 1fr),
  inset: 8pt,
  stroke: 0.4pt,
  table.header([*环节*], [*你要做什么*]),
  [*现代困境*], [先看见旧工具为什么不够用，不急着背新结论],
  [*探索发现*], [跟着问题自己试，尽量先提出猜想],
  [*工具蓝图*], [把新工具的定义、规则和边界看清楚],
  [*工具磨砺*], [用练习把工具磨顺，直到能稳定调用],
)

=== 学习时的三个小约定

- *遇到路线选择时，先问自己：我是在补主干，还是在进分支？*
- *遇到不会的题时，不只问“答案是什么”，还问“我缺的是哪一种旧工具”？*
- *看到 §X.Y 这样的编号时，把它当作地图坐标：它提醒你，这个工具在整本书里有自己的位置。*
