#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall
#import "../lib/diagram-packages.typ": cetz

== §3.5 坐标入门：在方格纸上定位 <sec-3-5>

#vocab[坐标 coordinate / 原点 origin / 横轴 x-axis / 纵轴 y-axis / 象限 quadrant / 有序数对 ordered pair]

#crisis[
  你拿到一张方格地图，想告诉同学“图书角在第几列、第几行”。如果只说“在左边一点、上面一点”，别人很容易找错。

  *能不能用一对数，把平面上的位置说得又短又准？*
]

#history-note[
  笛卡尔把“数轴”和“图形”接了起来：点的位置可以写成数对，线和图形也能放进坐标网格里观察。这样一来，几何不再只是“看图”，还可以“读数”。
]

#discovery[
  *第一步*：画出两条互相垂直的数轴。

  水平的叫 $x$ 轴，竖直的叫 $y$ 轴，它们的交点叫*原点* $O$。平面上的一个点，可以用有序数对 $(x, y)$ 表示：先看左右位置，再看上下位置。

  #figure(
    cetz.canvas(length: 1.8cm, {
      import cetz.draw: *
      line((-3, 0), (3.5, 0), stroke: 0.7pt, mark: (end: ">", size: 0.15))
      line((0, -2.5), (0, 3), stroke: 0.7pt, mark: (end: ">", size: 0.15))
      content((3.5, 0), $x$, anchor: "west", padding: 3pt)
      content((0, 3), $y$, anchor: "south", padding: 3pt)
      content((0, 0), $O$, anchor: "north-east", padding: 3pt)

      let A = (2, 1)
      let B = (-2, 2)
      let C = (-1, -1.5)
      circle(A, radius: 0.05, fill: black, stroke: none)
      circle(B, radius: 0.05, fill: black, stroke: none)
      circle(C, radius: 0.05, fill: black, stroke: none)
      content(A, text(8pt)[$A(2,1)$], anchor: "south-west", padding: 3pt)
      content(B, text(8pt)[$B(-2,2)$], anchor: "south-east", padding: 3pt)
      content(C, text(8pt)[$C(-1,-1.5)$], anchor: "north-east", padding: 3pt)

      content((1.5, 1.8), text(8pt, fill: luma(120))[第一象限])
      content((-1.7, 1.8), text(8pt, fill: luma(120))[第二象限])
      content((-1.7, -1.8), text(8pt, fill: luma(120))[第三象限])
      content((1.5, -1.8), text(8pt, fill: luma(120))[第四象限])
    }),
    caption: [坐标系里，点的位置由有序数对 $(x, y)$ 表示：先横后纵]
  )

  *第二步*：认识四个象限。

  - 第一象限：$x > 0, y > 0$
  - 第二象限：$x < 0, y > 0$
  - 第三象限：$x < 0, y < 0$
  - 第四象限：$x > 0, y < 0$

  在 $x$ 轴上的点，纵坐标一定是 $0$；在 $y$ 轴上的点，横坐标一定是 $0$。

  *第三步*：学会读点与描点。

  例如点 $A(2, 1)$：先从原点沿 $x$ 轴向右走 2 格，再沿平行于 $y$ 轴的方向向上走 1 格，就找到点 $A$。

  反过来，如果图上某个点在原点左边 2 格、上方 2 格，它的坐标就是 $(-2, 2)$。
]

#tryit[
  先自己做一做：

  已知点 $P(-3, 2)$、$Q(0, -4)$。

  + $P$ 在第几象限？
  + $Q$ 在哪条坐标轴上？
  + 点 $(4, -1)$ 应该画在原点的哪个方向？
]

#blueprint[
  *平面直角坐标系*

  - 两条互相垂直且有公共原点的数轴组成坐标系
  - 点 $(a, b)$ 中，$a$ 是*横坐标*，$b$ 是*纵坐标*
  - 读坐标顺序：*先横后纵*

  *象限与特殊点*

  - 第一象限：$(+, +)$
  - 第二象限：$(-, +)$
  - 第三象限：$(-, -)$
  - 第四象限：$(+, -)$
  - $x$ 轴上的点：$y = 0$
  - $y$ 轴上的点：$x = 0$
  - 原点：$(0, 0)$

  *描点与读点*

  - 点 $(a, b)$：先在横轴上找到 $a$，再看纵向的 $b$
  - $a > 0$ 表示在原点右边，$a < 0$ 表示在原点左边
  - $b > 0$ 表示在原点上方，$b < 0$ 表示在原点下方
  - 坐标写成数对时，顺序不能交换
]

#side-hack[
  看到 $(x, y)$ 时，心里默念一句：*先左右，后上下；先横坐标，后纵坐标。* 这样最不容易把顺序写反。
]

#pitfall[
  *陷阱 1*：把 $(x, y)$ 看成“先上后右”。

  ❌ 点 $(2, -1)$ 被误画成“向上 2，再向左 1”

  ✓ 正确顺序是：先沿 $x$ 轴向右 2，再沿 $y$ 方向向下 1。

  *陷阱 2*：把坐标轴上的点误判进象限。

  在坐标轴上的点*不属于任何象限*。

  - $(0, 5)$ 在 $y$ 轴上，不在第一或第二象限
  - $(-3, 0)$ 在 $x$ 轴上，不在第二或第三象限
]

#mastery[
  *基础*

  + 写出下列各点所在的象限：$A(3, 2)$，$B(-4, 1)$，$C(-2, -5)$，$D(6, -3)$。
  + 判断下列点在哪条坐标轴上：$(0, 7)$，$(-3, 0)$，$(0, 0)$。
  + 写出下列点所在的方向：$(4, -1)$，$(-2, 5)$。

  *应用*

  + 在方格纸上描出点 $A(2, 3)$、$B(-1, 2)$、$C(-3, -2)$、$D(1, -4)$，并分别判断它们所在的象限。
  + 已知点 $M$ 在第二象限，横坐标是 $-4$，纵坐标是 $3$，写出点 $M$ 的坐标。
  + 点 $P$ 在 $x$ 轴上，且在原点左边 5 个单位处；点 $Q$ 在 $y$ 轴上，且在原点下方 2 个单位处。写出 $P$、$Q$ 的坐标。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  + $A$ 在第一象限，$B$ 在第二象限，$C$ 在第三象限，$D$ 在第四象限。
  + $(0, 7)$ 在 $y$ 轴上；$(-3, 0)$ 在 $x$ 轴上；$(0, 0)$ 在原点（也同时在两条坐标轴上）。
  + $(4, -1)$ 在原点右下方；$(-2, 5)$ 在原点左上方。
  + $A$ 在第一象限，$B$ 在第二象限，$C$ 在第三象限，$D$ 在第四象限。
  + $M = (-4, 3)$。
  + $P = (-5, 0)$，$Q = (0, -2)$。
]
