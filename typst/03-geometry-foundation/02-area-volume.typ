#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, secref, tryit, pitfall
#import "../lib/diagram-packages.typ": cetz

== §3.2 面积与体积 <sec-3-2>

#vocab[面积 area / 体积 volume / 割补法 cut-and-rearrange]

#crisis[
  学校要在操场上铺人造草坪。操场是一块不规则形状的地面。工人说：「告诉我面积，我才知道要买多少草坪。」

  面积到底怎么算？一个正方形好算，一个三角形呢？一个圆呢？
]

#discovery[
  *面积的推导链*——从长方形出发，一步步推出所有公式。

  *第一步*：长方形面积 = 长 $times$ 宽

  用 $1 "cm"^2$ 的小正方形去铺。长 $a$ 厘米、宽 $b$ 厘米的长方形，恰好铺 $a times b$ 个小正方形。所以：
  $ S_"长方形" = a b $

  *第二步*：平行四边形 → 割补为长方形

  #align(center, cetz.canvas(length: 1.5cm, {
    import cetz.draw: *
    // 平行四边形
    line((0, 0), (3, 0), (3.8, 1.5), (0.8, 1.5), close: true, stroke: 0.7pt, fill: blue.lighten(90%))
    // 割补虚线
    line((3, 0), (3, 1.5), stroke: (dash: "dashed", paint: red, thickness: 0.5pt))
    line((0.8, 0), (0.8, 1.5), stroke: (dash: "dashed", paint: red, thickness: 0.5pt))
    // 标注
    content((1.9, -0.3), $b$)
    content((-0.25, 0.75), $h$)
  }))

  沿高剪开左边的三角形，拼到右边，就变成了长方形。底不变，高不变，所以：
  $ S_"平行四边形" = b h $

  *第三步*：三角形 = 平行四边形的一半

  两个完全相同的三角形可以拼成一个平行四边形。所以三角形面积是平行四边形面积的一半：
  $ S_"三角形" = frac(1, 2) b h $

  *第四步*：梯形——两个全等梯形拼成平行四边形

  上底 $a$、下底 $b$、高 $h$ 的梯形，两个拼在一起是底为 $(a + b)$、高为 $h$ 的平行四边形。所以：
  $ S_"梯形" = frac(1, 2)(a + b) h $

  *第五步*：圆——无限多扇形拼成近似长方形

  把圆分成很多（比如 32 个）等份扇形，交错拼在一起，就近似一个长方形。分得越细，越接近长方形。这个「长方形」的长是半个周长 $pi r$，宽是半径 $r$，所以：
  $ S_"圆" = pi r^2 $
]

#tryit[
  先不看公式，自己算一算：

  一个圆柱，底面半径 $r = 3$ cm，高 $h = 5$ cm。

  + 底面积是多少（用 $pi$ 表示）？
  + 圆柱的体积是多少（用 $pi$ 表示，再用 $pi approx 3.14$ 算近似值）？

  算完后，再对照下面的公式检查自己的思路是否正确。
]

#history-note[
  *九章算术·方田*：中国最早的数学经典《九章算术》（约公元 1 世纪）的第一章「方田」，就系统地给出了长方形、三角形、梯形、圆等的面积公式，比欧洲同类成果早了约一千年。
]

#blueprint[
  *面积公式汇总*

  #table(
    columns: (1fr, 1fr),
    align: left,
    [*图形*], [*面积公式*],
    [长方形（长 $a$，宽 $b$）], [$S = a b$],
    [正方形（边长 $a$）], [$S = a^2$],
    [平行四边形（底 $b$，高 $h$）], [$S = b h$],
    [三角形（底 $b$，高 $h$）], [$S = frac(1, 2) b h$],
    [梯形（上底 $a$，下底 $b$，高 $h$）], [$S = frac(1, 2)(a + b) h$],
    [圆（半径 $r$）], [$S = pi r^2$],
  )

  *立体图形与体积*

  体积是物体占据空间的大小。

  #table(
    columns: (1fr, 1fr),
    align: left,
    [*图形*], [*体积公式*],
    [长方体（长 $a$，宽 $b$，高 $c$）], [$V = a b c$],
    [正方体（棱长 $a$）], [$V = a^3$],
    [圆柱（底面半径 $r$，高 $h$）], [$V = pi r^2 h$],
  )

  *度量单位*

  #table(
    columns: (1fr, 1fr, 1fr),
    align: left,
    [*类别*], [*单位*], [*进率*],
    [长度], [mm, cm, dm, m, km], [相邻 $times 10$（km 与 m 之间 $times 1000$）],
    [面积], [$"mm"^2$, $"cm"^2$, $"dm"^2$, $"m"^2$, $"km"^2$], [相邻 $times 100$],
    [体积], [$"cm"^3$, $"dm"^3$ (= 1 L), $"m"^3$], [相邻 $times 1000$],
    [质量], [g, kg, t], [相邻 $times 1000$],
  )
]

#pitfall[
  *陷阱 1*：$pi approx 3.14$ 只是近似值，精确计算时要写 $pi$，不要随意替换。

  *陷阱 2*：圆的面积是 $pi r^2$，不是 $pi d^2$。$r$ 是半径，$d = 2r$ 是直径，两者不要混淆。

  *陷阱 3*：面积单位和体积单位的进率别混。

  相邻面积单位通常乘 $100$，相邻体积单位通常乘 $1000$。
]

#mastery[
  *练一练*

  + 求下列图形的面积：
    - 平行四边形，底 12 cm，高 5 cm
    - 三角形，底 8 m，高 6 m
    - 梯形，上底 3 cm，下底 7 cm，高 4 cm

  + 圆形水池半径 5 m，水面面积是多少？（用 $pi$ 表示）

  + 一个长方体水箱，长 60 cm，宽 40 cm，高 30 cm。最多能装多少升水？（提示：$1 "dm"^3 = 1$ L）

  + 一个圆柱形粮仓，底面半径 2 m，高 3 m。它的容积是多少？

  + *整理一下*：回头看本节出现过的图形，说说它们的面积或体积公式各和哪一个量有关（底、边长、半径、高）。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  + 平行四边形面积 $= 12 times 5 = 60$ cm²；三角形面积 $= frac(1, 2) times 8 times 6 = 24$ m²；梯形面积 $= frac(1, 2) times (3 + 7) times 4 = 20$ cm²。

  + 水面面积 $= pi r^2 = 25pi$ m²。

  + 体积 $= 60 times 40 times 30 = 72000$ cm³ $= 72$ dm³ $= 72$ L。

  + 容积 $= pi r^2 h = pi times 2^2 times 3 = 12pi$ m³。

  + 例如：长方形、正方形的面积分别看长宽或边长；平行四边形、三角形、梯形都要抓底和高；圆与圆柱都要抓半径，圆柱还要再看高。
]
