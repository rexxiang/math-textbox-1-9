#import "../../lib/theme-v2.typ": blueprint, side-hack, secref

=== 3 条推荐路线

下面给出 3 条已经走通的样板路线。它们不是"必须按这个顺序"，而是"这样走最稳、不容易卡死"。选一条作为起点即可——读到一半发现节奏不合，可以随时切换。

==== 路线 A：完整脊梁顺序（推荐给第一次系统自学的读者）

#blueprint[
  *顺序*：导读 → 公共基础（含基础总检） → 共通桥梁（含 4 份入口契约） → 代数 → 几何 → 函数 → 数据 → 终章。

  *特点*：和书的章节编号顺序完全一致，每读到一章其先修都已经读过。最不容易卡，但耗时最长——按每周 3–5 节算，全套读完需要约 30–40 周。

  *什么时候选*：你是第一次自学完整初中数学；或者你想把所有分支都打通、为高中做最完整准备。

  *卡住怎么办*：每章末有 *99-review*，每节末有"试一试"和"防坑"——按*查漏路线*（导读 §2）回补即可。
]

==== 路线 B：先攻 1 条分支（推荐给"我现在最想拿下代数"型读者）

#blueprint[
  *顺序*：导读 → 公共基础 → 共通桥梁 → *只读 1 条分支*（比如代数）→ 该分支总结 → 回头补另外 3 条分支 → 终章。

  *特点*：能更快建立"我学完了一整片"的成就感。代价是 3 条分支被推后，期间它们的工具用不上。

  *什么时候选*：你有一个明确的近期目标（如某次考试、某个项目），其内容只覆盖其中 1 条分支；或者你之前在某条分支上特别卡，想集中火力先攻下来。

  *选哪一条*：代数支兼容性最高（其他 3 条分支都会在某些节里调用代数符号），所以*若不知道挑哪条，从代数开始最稳*。函数支需要"完全平方/配方法"——若先攻函数支，确认你已掌握桥梁入口契约里"代数支必备工具"那一行（见 #secref("meta:ready-for-functions")）。

  *卡住怎么办*：如果回头补另外 3 条分支时发现新分支引用了代数支的高级工具——这是正常的，因为本书的分支*独立但不绝缘*。按各分支入口契约（#secref("meta:bridge-readiness-contracts")）逐条核对即可。
]

==== 路线 C：以应用为导向（推荐给"我想用数学解决实际问题"型读者）

#blueprint[
  *顺序*：导读 → 公共基础 → 共通桥梁 → *函数 + 数据*（这两条分支应用最直接） → 按需回补几何与代数 → 终章。

  *特点*：最早接触"建模"和"用数据说话"。代价是几何与代数被推后，遇到证明题或纯符号题会感到陌生。

  *什么时候选*：你的兴趣点在数据科学、经济、自然科学等"用数学描述真实世界"的方向；纯符号操作和几何证明对你不是当下的优先项。

  *为什么是函数 + 数据*：函数支建立"变量随什么变化"的语言，数据支建立"从样本估计总体"的语言——这两套语言合起来，就是绝大多数应用学科的入门工具。

  *卡住怎么办*：函数支的"二次顶点 / 配方法"会需要代数支的"完全平方"工具——按 #secref("meta:ready-for-functions") 提示就地补。数据支的"频率分布"会需要分数与小数互化——按 #secref("meta:ready-for-data") 提示回到公共基础对应章节。
]

#side-hack[
  *关于切换路线*

  3 条路线不是"选定就不能改"。如果你按路线 A 读到几何支发现兴趣不大、想跳到函数，那就跳——只需要先翻一下 #secref("meta:ready-for-functions")，把那张清单上的工具都打钩，就可以无缝切过去。本书的*入口契约*设计就是为了让"中途换路"这件事不付额外代价。
]
