#import "../../lib/theme-v2.typ": blueprint, side-hack, secref
#import "../../lib/diagram-packages.typ": fletcher

=== 全书结构地图

全书 5 大部分按下图串接：公共基础是必经主干，共通桥梁把"小学算术语言"翻译成"中学符号语言"，之后 4 条分支*并联*——你可以按任意顺序进入它们，最后由终章把四条路线重新会合。

#figure(
  {
    import fletcher: diagram, node, edge
    diagram(
      node-stroke: 0.8pt,
      spacing: (3em, 1.6em),
      node((0, 1), [*导读*\ ★☆☆☆☆], shape: rect, fill: rgb("#ECEFF1")),
      node((1, 1), [*公共基础*\ ★☆☆☆☆ — ★★★☆☆], shape: rect, fill: rgb("#E8F5E9")),
      node((2, 1), [*共通桥梁*\ ★★★☆☆ — ★★★★☆], shape: rect, fill: rgb("#FFF8E1")),
      node((3, 0), [*代数与符号*\ ★★★★☆ — ★★★★★], shape: rect, fill: rgb("#E3F2FD")),
      node((3, 0.7), [*几何与证明*\ ★★★★☆ — ★★★★★], shape: rect, fill: rgb("#E3F2FD")),
      node((3, 1.4), [*函数与变化*\ ★★★★☆ — ★★★★★], shape: rect, fill: rgb("#E3F2FD")),
      node((3, 2.1), [*数据与不确定性*\ ★★★★☆ — ★★★★★], shape: rect, fill: rgb("#E3F2FD")),
      node((4, 1), [*终章*\ ★★★☆☆ — ★★★★★], shape: rect, fill: rgb("#FCE4EC")),
      edge((0, 1), (1, 1), "->"),
      edge((1, 1), (2, 1), "->"),
      edge((2, 1), (3, 0), "->"),
      edge((2, 1), (3, 0.7), "->"),
      edge((2, 1), (3, 1.4), "->"),
      edge((2, 1), (3, 2.1), "->"),
      edge((3, 0), (4, 1), "->"),
      edge((3, 0.7), (4, 1), "->"),
      edge((3, 1.4), (4, 1), "->"),
      edge((3, 2.1), (4, 1), "->"),
    )
  },
  caption: [全书 5 大部分串接图：公共基础与共通桥梁是必经主干，4 条分支并联，终章会合。],
) <fig-book-map>

#blueprint[
  *5 大部分逐项简介*

  - *导读（★☆☆☆☆）*：自学方法 + 符号约定 + 全书地图。不学新数学。
  - *公共基础（★☆☆☆☆ — ★★★☆☆）*：数与位值、四则、分数小数、点线面、面积体积、平行与三角形——后续每条分支都会反复用到。最后一章是*基础总检*。
  - *共通桥梁（★★★☆☆ — ★★★★☆）*：比例、负数、坐标、字母代数式、方程、平方根。这一段把"算术语言"翻译成"符号语言"。最后一章是 4 份*分支入口契约*。
  - *4 条分支（★★★★☆ — ★★★★★）*：代数与符号、几何与证明、函数与变化、数据与不确定性。每条分支自成体系，可独立深读。
  - *终章（★★★☆☆ — ★★★★★）*：共同发明树回顾、跨域建模、下一阶段学习地图。把 4 条分支重新连成一张网，并指向高中。
]

#side-hack[
  *关于"4 条分支并联"*

  这是这本书的设计核心：走完共通桥梁之后，*你不需要按某个固定顺序学完 4 条分支*。每条分支的入口契约（共通桥梁最后一章）都明确写出"进入本分支需要哪些前置工具"——只要清单里的工具都掌握了，你就可以进入任意分支。这意味着你可以："只攻 1 条"先做完一条再回头补其他、"主线 + 副线"挑 2 条交替读、或者按本书默认的代数 → 几何 → 函数 → 数据顺序全部读完。

  *但终章假设你 4 条都读完了*——它的"跨域建模"必然要同时调用 4 条分支的工具。所以如果你只读了部分分支，可以读终章但要心里有数：会遇到陌生引用。
]
