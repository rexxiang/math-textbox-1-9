#import "../../lib/theme-v2.typ": blueprint, vocab, side-hack, pitfall

=== 符号与阅读约定

数学里的符号不是凭空规定的——大多数符号在被发明时都有一个具体的理由：要表达一类关系，或者要把一个常用的描述变短。

读懂符号的方法不是死记，而是知道它"在表达什么关系"。

==== 怎样遇见一个新符号

第一次看到一个陌生符号时，可以按下面三步处理：

#blueprint[
  *第一步：确认它属于哪一类。*符号大致分为四类：

  - 运算符号（$+$, $-$, $times$, $div$）：表示做某种变换或操作
  - 关系符号（$=$, $<$, $>$, $!=$）：表示两个量之间的关系
  - 代表符号（$x$, $f(x)$, $n$）：表示一个可以变化的量或位置
  - 缩写符号（GCD, sin, cos）：表示一个常用概念的简写

  *第二步：找到它第一次出现的地方。*本书在符号第一次正式使用时会给出完整说明。如果只是在符号表里查到了，还没看到说明，先把它记下来，继续往下读，等遇到正式引入时再回来。

  *第三步：用一个具体例子验证你的理解。*能用自己的数造出一个符合这个符号含义的例子，才算真正读懂了它。
]

==== 标注框的含义

本书使用几种颜色标注框，每一种都有固定含义：

#table(
  columns: (auto, 1fr),
  inset: 8pt,
  stroke: 0.4pt,
  table.header([*标注框*], [*用途*]),
  [这节的方法（蓝色）], [正式给出定义、规则、适用范围和关键例子],
  [四、练一练（绿色）], [练习、巩固与训练；章末回顾是其中一种用途，也可出现在章节中间用于即时强化],
  [一、遇到的问题（红色）], [展示前面学过的知识为什么不够用，引出新工具的动机],
  [二、想一想（橙色）], [跟着问题自己试，通常是正式定义前的引导],
  [小提示（绿色边线）], [帮你记住或理解某个容易混淆的地方],
  [先试一下（蓝色虚线）], [要求你先动手，再继续读——不要跳过它],
  [常见陷阱（红色 / 黄色）], [高频错误提示，先对照自己的草稿再往下读],
)

#side-hack[
  看到"先试一下"时，哪怕只是在草稿纸上写下自己的第一步，都比直接跳过强。这个框存在的意义就是让你在看答案之前先暴露出自己的理解状态。
]

#pitfall[
  *符号 ≠ 规则本身。*看懂符号怎么写，不等于理解这条规则。例如看懂 $f(x)$ 的写法，不等于理解函数的含义。符号是入口，理解需要走进去。
]

==== 阅读约定小结

- 遇到符号：先分类，再找正式引入，然后用例子验证
- 遇到标注框：先看标题判断它的类型，再决定要不要停下来互动
- 遇到书内引用提示（指向某节或某章的可跳转位置标记）：把它当坐标，它告诉你这个工具在全书的哪个位置
