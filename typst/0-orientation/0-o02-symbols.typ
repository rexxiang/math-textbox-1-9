=== 符号约定

这份表不是按旧章节顺序排的，而是按你在整本书里最常遇到的工具类别来整理。看不懂某个式子时，可以先回来查这里。

==== 数与关系

#table(
  columns: (auto, auto, 1fr),
  inset: 8pt,
  stroke: 0.4pt,
  table.header([*符号*], [*名称*], [*常见含义*]),
  [$+$, $-$], [加、减], [表示合并、求差，或带方向的变化],
  [$times$, $div$], [乘、除], [表示成倍变化、平均分配或单位换算],
  [$=$], [等于], [表示两个量相同，方程里尤其重要],
  [$!=$], [不等于], [表示两个量不相同],
  [$<$, $>$, $<=$, $>=$], [大小关系], [表示比较、范围与约束],
  [$NN$, $ZZ$, $QQ$, $RR$], [常见数集], [自然数、整数、有理数、实数],
)

==== 图形与空间

#table(
  columns: (auto, auto, 1fr),
  inset: 8pt,
  stroke: 0.4pt,
  table.header([*符号*], [*名称*], [*常见含义*]),
  [$angle$], [角], [表示图形张开的大小],
  [$triangle$], [三角形], [几何里最常见的基本图形之一],
  [$parallel$], [平行], [两条直线永不相交],
  [$perp$], [垂直], [两条直线夹角是直角],
  [$tilde.eq$], [全等], [形状和大小都相同],
  [$tilde$], [相似], [形状相同、大小按比例缩放],
)

==== 代数与变化

#table(
  columns: (auto, auto, 1fr),
  inset: 8pt,
  stroke: 0.4pt,
  table.header([*符号*], [*名称*], [*常见含义*]),
  [$x$, $y$, $a$, $b$], [字母变量], [可以表示未知数、可变的量，或一般规律中的位置],
  [$f(x)$], [函数记号], [表示“把输入 $x$ 放进某个变化规则后得到的结果”],
  [$abs(x)$], [绝对值], [表示一个数离 $0$ 有多远],
  [$sqrt(x)$], [平方根], [表示平方后回到 $x$ 的数],
  [$pi$], [圆周率], [圆与周长、面积有关的常数],
  [$Delta$], [判别式], [在二次方程里用来判断根的情况],
)

==== 数据与统计

#table(
  columns: (auto, auto, 1fr),
  inset: 8pt,
  stroke: 0.4pt,
  table.header([*符号*], [*名称*], [*常见含义*]),
  [$x_1, x_2, dots.h, x_n$], [一组数据], [表示第 1 个到第 $n$ 个数据],
  [$sum_(i=1)^n$], [求和记号], [表示把从第 1 项到第 $n$ 项按规则全部加起来],
  [$n$], [数据个数 / 样本容量], [表示一组数据有多少项，或一次抽样取了多少个个体],
  [$bar(x)$], [平均数], [表示一组数据的平均水平],
  [$S^2$], [方差], [表示一组数据离平均数有多分散],
  [$P(A)$], [事件 $A$ 的概率], [表示某件事发生的可能性大小],
)

==== 数论缩写

#table(
  columns: (auto, auto, 1fr),
  inset: 8pt,
  stroke: 0.4pt,
  table.header([*缩写*], [*英文全称*], [*含义*]),
  [GCD], [Greatest Common Divisor], [最大公因数——两个整数公有因数中最大的那个],
  [LCM], [Least Common Multiple], [最小公倍数——两个整数公有倍数中最小的那个（0 除外）],
)

==== 三角函数

#table(
  columns: (auto, auto, 1fr),
  inset: 8pt,
  stroke: 0.4pt,
  table.header([*缩写*], [*英文全称*], [*含义*]),
  [sin], [Sine], [正弦——直角三角形中，某锐角的对边比斜边],
  [cos], [Cosine], [余弦——直角三角形中，某锐角的邻边比斜边],
  [tan], [Tangent], [正切——直角三角形中，某锐角的对边比邻边],
)

==== 几何判定条件

#table(
  columns: (auto, auto, 1fr),
  inset: 8pt,
  stroke: 0.4pt,
  table.header([*缩写*], [*英文全称*], [*含义*]),
  [SSS], [Side-Side-Side], [三边全等判定——三组对应边相等，则两三角形全等],
  [SAS], [Side-Angle-Side], [两边夹角全等判定——两组对应边及其夹角相等，则全等],
  [ASA], [Angle-Side-Angle], [两角夹边全等判定——两组对应角及其夹边相等，则全等],
  [AAS], [Angle-Angle-Side], [两角及其对边全等判定——两组对应角和任一对应边相等，则全等],
  [HL], [Hypotenuse-Leg], [斜边直角边全等判定——仅适用于直角三角形，斜边和一直角边对应相等，则全等],
  [AA], [Angle-Angle], [两角相似判定——两组对应角相等，则两三角形相似],
)
