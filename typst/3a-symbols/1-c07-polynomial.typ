#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref

== §c07 整式整理 <tool:c07-polynomial>

#vocab[整式 polynomial expression / 同类项 like terms / 去括号 removing parentheses]

#crisis[
  在 #secref("c01-variable") 里，字母已经能做”计算模板”；在 #secref("c04-equation") 里，你也已经会守住”左右一样多”的平衡。可一旦这些模板开始彼此拼接，新的麻烦马上出现：

  - 两盒彩笔分别写成 $(n + 3)$ 和 $(n + 2)$，合在一起能不能再写短一点？
  - $2(a + b)$ 这种“两个同样的总量”，打开括号后会变成什么？
  - $(3x + 5) - (x - 2)$ 里，减号一旦碰到括号，里面每一项会不会都受影响？

  这一节的任务只有一件：把已经会写的式子整理干净。
]

#history-note[
  16 世纪法国数学家弗朗索瓦·韦达（François Viète）把“用字母表示数”这件事真正系统化之后，代数才慢慢从“用文字讲关系”走向“用符号整理关系”。很多后来很强的技巧，第一步其实都只是：先把式子看清、收拾整齐。
]

#discovery[
  在 #secref("c01-variable") 里，字母是模板里的占位符。现在先不急着发明更多新记号，而是把这些模板真的拿来整理。

  - $(n + 3) + (n + 2)$ 里，哪些部分其实在说同一种数量？
  - $2(a + b)$ 如果按你熟悉的分配想法打开括号，会变成哪几块？
  - $(3x + 5) - (x - 2)$ 里的第二个括号前面有减号，去掉括号时，里面每一项会发生什么？

  所以这一节真正要建立的是：先认清式子由哪些部分组成，再决定哪些能合并、哪些要先去括号。
]

#tryit[
  先别看下面，自己试着整理：

  - $(n + 3) + (n + 2)$
  - $2(a + b) + a - b$
  - $(3x + 5) - (x - 2)$

  写出每一步，再对照 blueprint 里的例子检查。
]

#blueprint[
  *一、先把式子看成由“项”组成的整式*

  像 $25n + 80$、$2(a + b)$、$3x - 5$ 这样可以继续整理和运算的式子，这里先统一看成*整式*。

  用加号或减号隔开的每一块，叫作一项。

  *例 1*：指出 $4a - 3b + 7$ 里有哪些项。

  三项分别是 $4a$、$-3b$、$7$。

  看清“分成了哪几块”，后面才知道哪些可以直接合并，哪些要先去括号。

  *二、同类项与合并*

  字母部分完全相同的项，叫*同类项*。合并时，字母部分不动，只把前面的系数相加减。

  *例 2*：化简 $(n + 3) + (n + 2)$

  $ &= n + 3 + n + 2 \
    &= (1 + 1)n + (3 + 2) \
    &= 2n + 5 $

  *例 3*：化简 $5a - 3b + 2a + b$

  $ &= (5 + 2)a + (-3 + 1)b \
    &= 7a - 2b $

  *三、去括号*

  括号前面是加号时，去括号后各项不变；括号前面是减号时，括号里的每一项都要变号。

  *例 4*：化简 $2(a + b) + a - b$

  根据你已经会用的分配律：

  $ &= 2a + 2b + a - b \
    &= 3a + b $

  *例 5*：化简 $(3x + 5) - (x - 2)$

  $ &= 3x + 5 - x + 2 \
    &= 2x + 7 $

  注意：减号后的括号里，不只是第一项变号，而是*每一项都一起变号*。
]

#pitfall[
  *两个最常见的整理坑：*

  *① 减号后的括号，里面每一项都要变号，不只是第一项！*

  $(3x + 5) - (x - 2)$

  ❌ 错误：$= 3x + 5 - x - 2$

  这里最后的 $-2$ 本来在括号里是负的，前面再遇到减号，应该变成 $+2$：

  ✓ 正确：$= 3x + 5 - x + 2$

  *② 不是“都带字母”就能合并。*

  ❌ $3a + 2b = 5 a b$

  ✓ 只有字母部分完全相同，才是同类项。  
  $3a$ 和 $2b$ 不是一类；  
  但 $3a$ 和 $2a$ 可以合并成 $5a$。
]

#side-hack[
  每次整理前先问三句：

  - 我现在看到的是哪几项？
  - 哪些项真的同类？
  - 有没有括号要先处理？

  先看清，再下手，通常就不容易乱。
]

#mastery[
  *基础*

  + 指出下列整式各有几项：$5x - 3$、$2(a+b)$、$4a - b + 7$
  + 合并同类项：
    - $5a - 3b + 2a + b$
    - $7x - 4 + x + 9$
  + 去括号并化简：
    - $3(m + 2) + m$
    - $(2x - 1) - (x + 4)$

  *应用*

  + 文具盒 A 记作 $(n + 3)$，文具盒 B 记作 $(n + 2)$。把两盒合在一起，写成最简整式。
  + 同学 A 的零花钱是 $n$ 元，先买了一支笔花了 3 元，又收到压岁钱 10 元。写出他最终的钱数并化简。
  + 化简：$2(a + b) + (3a - b)$

  *挑战*

  + 已知 $A = 2x - 3$，$B = x + 5$，化简 $(A + B) - (B - A)$。
  + 为什么 $4p + 3q - q + p$ 能整理，而 $4p + 3q$ 不能再继续合成一项？请用“同类项”解释。
  + 设计一个带括号的整式，要求整理后得到 $5x + 4$。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + 两项；一项；三项
  + $5a - 3b + 2a + b = 7a - 2b$；$7x - 4 + x + 9 = 8x + 5$
  + $3(m + 2) + m = 3m + 6 + m = 4m + 6$；$(2x - 1) - (x + 4) = 2x - 1 - x - 4 = x - 5$

  *应用*
  + $(n + 3) + (n + 2) = 2n + 5$
  + $n - 3 + 10 = n + 7$
  + $2(a + b) + (3a - b) = 2a + 2b + 3a - b = 5a + b$

  *挑战*
  + $(A + B) - (B - A) = A + B - B + A = 2A = 2(2x - 3) = 4x - 6$
  + 因为 $4p$ 和 $p$ 的字母部分相同、$3q$ 和 $-q$ 的字母部分相同，所以前者可整理成 $5p + 2q$；而 $p$ 和 $q$ 说的是不同量，不能合成一项
  + 答案不唯一，例如 $(3x + 1) + (2x + 3)$
]
