#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref

== 幂的高级规则：乘方与零次幂 <tool:c18-zero-exponent>

#vocab[零次幂 zero exponent / 幂的乘方 power of a power / 积的乘方 power of a product]

#crisis[
  上一节你学了：同底数幂相除，指数相减。现在算一算：$a^3 div a^3 = ?$

  有人说：任何数除以自己 $= 1$，所以答案是 $1$。

  另一个人说：用上节的规则，$a^3 div a^3 = a^(3-3) = a^0$，但 $a^0$ 是什么？？

  这两种说法必须得到同一个答案——矛盾倒逼我们定义：$a^0$ 必须等于 $1$（当 $a != 0$ 时）。

  类似地，$(x^2)^3$ 到底是 $x^5$（把指数加起来？）还是 $x^6$（把 $x^2$ 当整体乘了 3 次）？
]

#history-note[
  零次幂不是人们凭空定义的。它是从已有规则往里一步步推出来的——推出来的结果再反过来变成定义，让整套规则自洽。数学里这种"从规律倒推定义"的做法，在指数、阶乘、空集大小这些地方反复出现。
]

#discovery[
  先来搞清 $a^0 = 1$（$a != 0$）。

  用 #secref("c15-power-notation") 里的同底数幂相除规则：

  $ a^3 div a^3 = a^(3-3) = a^0 $

  但左边同时也可以直接算：

  $ a^3 div a^3 = 1 $

  两边都等于同一件事，所以 $a^0 = 1$——不是规定，是推出来的。

  再来看 $(a^m)^n$。把 $a^m$ 当成一整块，写出来：

  $ underbrace(a^m times a^m times dots.c times a^m, n "个") $

  每一块里有 $m$ 个 $a$，共 $n$ 块，总共有 $m times n$ 个 $a$。

  最后是 $(a b)^n$。把 $(a b)$ 当成一整块，写 $n$ 次，再重新排列：

  $ underbrace((a b)(a b) dots.c (a b), n "个") = underbrace(a dot a dot dots.c dot a, n "个") times underbrace(b dot b dot dots.c dot b, n "个") = a^n b^n $
]

#tryit[
  先不看下面，自己试试：

  - $5^0$ 等于多少？$(-3)^0$ 呢？
  - $(x^3)^4$，外面的 4 次方该怎样处理指数？
  - $(2a)^3$ 里的 2 和 $a$ 都会被"乘 3 次"吗？
]

#blueprint[
  *一、零次幂*

  对任意非零数 $a$：

  $ a^0 = 1 quad (a != 0) $

  来源：$a^n div a^n = a^(n-n) = a^0$，而左边等于 1。

  *例 1*：$7^0 = 1$，$(-4)^0 = 1$，$(x y)^0 = 1$（$x y != 0$）

  *二、幂的乘方*

  $ (a^m)^n = a^(m n) $

  指数相乘（不是相加）。

  *例 2*：化简 $(x^2)^3$

  $ (x^2)^3 = x^(2 times 3) = x^6 $

  *例 3*：化简 $((a^3)^2)^4$

  $ ((a^3)^2)^4 = (a^6)^4 = a^(6 times 4) = a^(24) $

  *三、积的乘方*

  $ (a b)^n = a^n b^n $

  乘方对乘法"分配"——每个因子都要被乘方。

  *例 4*：化简 $(2a)^3$

  $ (2a)^3 = 2^3 a^3 = 8a^3 $

  *例 5*：化简 $(3x^2)^4$

  $ (3x^2)^4 = 3^4 (x^2)^4 = 81 x^8 $
]

#pitfall[
  *幂的乘方是指数相乘，不是相加。*

  ❌ $(a^3)^4 = a^7$

  ✓ $(a^3)^4 = a^(3 times 4) = a^12$

  *积的乘方要每个因子都乘方，加法没有这条性质。*

  ❌ $(a + b)^n = a^n + b^n$

  ✓ 只有乘积才能拆开：$(a b)^n = a^n b^n$
]

#side-hack[
  遇到外面套了一层乘方，先分辨里面是什么：

  - 里面是同底数的幂（$a^m$ 外面又套一层）→ 指数相乘：$(a^m)^n = a^(m n)$
  - 里面是几个因子的乘积 → 每个因子分别乘方：$(a b)^n = a^n b^n$
  - 指数是 0 → 不管底数是什么（只要不是 0），结果都是 1

  三条规则一起用，先认形状，再对号入座。
]

#mastery[
  *基础*

  + 化简（直接写结果）：
    - $(a^2)^4$
    - $(3m)^2$
    - $x^0$（$x != 0$）
    - $(2x^3)^3$

  *应用*

  + 化简：$frac((2a)^3 times a^2, 4a)$
  + 化简：$(x^2)^3 times (x^0) div x^4$
  + 已知 $(2x)^n = 32x^5$，求 $n$。

  *挑战*

  + 比较 $(a b)^2$ 和 $a^2 + b^2$：为什么它们不相等？
  + 化简并说明每一步用了哪条规则：$(3x^2)^0 times (2x)^3 div (4x^2)$

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + $(a^2)^4 = a^8$；$(3m)^2 = 9m^2$；$x^0 = 1$；$(2x^3)^3 = 8x^9$

  *应用*
  + $frac((2a)^3 times a^2, 4a) = frac(8a^3 times a^2, 4a) = frac(8a^5, 4a) = 2a^4$
  + $(x^2)^3 times x^0 div x^4 = x^6 times 1 div x^4 = x^2$
  + $(2x)^n = 2^n x^n = 32 x^5$，所以 $n = 5$（$2^5 = 32$）

  *挑战*
  + $(a b)^2 = a^2 b^2$（积的乘方）；$a^2 + b^2$ 是两个平方相加。当 $a = b = 1$ 时，前者等于 1，后者等于 2，所以不同。
  + $(3x^2)^0 = 1$（零次幂）；$(2x)^3 = 8x^3$（积的乘方）；$8x^3 div 4x^2 = 2x$（同底数幂相除）；所以原式 $= 1 times 8x^3 div 4x^2 = 2x$
]
