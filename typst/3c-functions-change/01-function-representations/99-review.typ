#import "../../lib/theme-v2.typ": blueprint, mastery, side-hack, secref

== 本章回顾 <meta:function-representations>

#blueprint[
  *知识速查卡*

  #table(
    columns: (1fr, 1.6fr),
    inset: 6pt,
    stroke: 0.4pt,
    [*主题*], [*一句话提要*],
    [函数的定义], [一份输入范围 $+$ 一份输出范围 $+$ 一条*每个输入都对应唯一输出*的规则。],
    [表作面孔 #secref("fn01-table-as-function-view")], [查看是否“同一输入对应不同输出”——有则不是函数。输出列允许重复。],
    [图作面孔 #secref("fn02-graph-as-function-view")], [*垂线检验*：任一铅垂线与图最多相交一次 $<=>$ 是函数。圆不是函数，抛物线是函数。],
    [公式作面孔 #secref("fn03-formula-as-function-view")], [$y = f(x)$；独立变量 $x$、因变量 $y$；默认定义域是让公式合法的全部实数。],
    [定义域常见限制], [分母 $!= 0$（从 #secref("ch:fractions") 你已知除以 $0$ 无意义——含 $x$ 的分式同理）；根号下 $>= 0$（#secref("cb06-square-and-square-root")）；现实约束（正数、整数等）。],
    [函数记号 $f(x)$], [$f$ 是函数名；$f(x)$ 是“$f$ 在 $x$ 处的输出”，不是乘法。$f(a + b) != f(a) + f(b)$ 一般不成立。],
    [三面孔互译], [表 → 图：描点；公式 → 表：代入；图 → 公式：读几个特征点（后续章节展开）。],
  )
]

#mastery[
  *混合自测*

  + 下表是否为函数？说明理由。
    #align(center, table(
      columns: 5,
      align: center,
      stroke: 0.4pt,
      [*输入*], [$-1$], [$0$], [$1$], [$-1$],
      [*输出*], [$2$], [$5$], [$8$], [$11$],
    ))
  + “输入一天里某时刻、输出这一刻该城市的气温”——是函数吗？相反地，“输入某个温度值、输出该温度出现过的时刻”呢？
  + 某函数 $f(x) = 2 x - 3$。算 $f(-2)$、$f(0)$、$f(5)$。
  + 求 $f(x) = frac(3, x + 2)$ 的定义域，并算 $f(1)$、$f(-3)$。
  + 判断 $y = sqrt(x - 1)$ 的定义域。$f(1) = ?$、$f(10) = ?$
  + 下列坐标图哪些是函数图像？(a) 一条铅垂线 $x = 3$；(b) 抛物线 $y = x^2$；(c) 半圆 $y = sqrt(9 - x^2)$；(d) 整圆 $x^2 + y^2 = 9$。
  + 设 $f(x) = x^2$。$f(-3) = f(3)$ 成立吗？这是否违反“函数”的定义？
  + 一根蜡烛长 $h$（厘米）随燃烧时间 $t$（分钟）变化：$t = 0$ 时 $h = 20$，每分钟烧掉 $0.5$ 厘米。写出 $h(t)$ 的公式，并给出在现实情境下的定义域。
  + 在坐标系（#secref("cb03-rectangular-system")）中描出 $f(x) = x^2 - 4$ 在 $x = -3, -2, -1, 0, 1, 2, 3$ 处的七个点，连成光滑曲线。该图像能通过垂线检验吗？ // 跨章回顾
  + 设 $f(x) = frac(2 x + 1, x - 3)$，先写出定义域，再用解方程的等式性质（#secref("cb05-equality-properties")）求满足 $f(x) = 5$ 的 $x$。 // 跨章回顾
  + *找错误*：小明说"$y^2 = x$ 是 $y$ 关于 $x$ 的函数，因为给定正数 $x$ 都能算出 $y$。"找出错误并纠正。

  *参考答案*

  + *不是函数*。同一输入 $-1$ 出现两次但配了不同输出 $2$ 与 $11$——违反单值对应。
  + 第一问：每个时刻只有一个气温，*是函数*。第二问：同一个温度值可能出现在*多个*时刻（气温先升后降再升很常见）——*不是函数*。
  + $f(-2) = -7$，$f(0) = -3$，$f(5) = 7$。
  + 定义域 ${x | x != -2}$（分母 $x + 2 != 0$）。$f(1) = 3 / 3 = 1$；$f(-3) = 3 / (-1) = -3$。 // 工具：分母非零条件
  + 定义域 ${x | x >= 1}$（根号下 $x - 1 >= 0$）。$f(1) = sqrt(0) = 0$；$f(10) = sqrt(9) = 3$。 // 工具：根号下非负条件
  + (a) 铅垂线 $x = 3$ 本身就是“铅垂线的极端情形”——它与自身重合、与任何穿过 $x = 3$ 的铅垂线交在无数点——*不是函数*（$x = 3$ 对应所有 $y$ 值，违反单值）。(b) 抛物线：*是*。(c) 半圆：*是*。(d) 整圆：*不是*（铅垂线交两次）。
  + *成立*。$x = -3$ 和 $x = 3$ 是两个*不同输入*，都对应输出 $9$——输出列重复合法。*不违反*函数定义（函数只禁“同输入对不同输出”）。
  + $h(t) = 20 - 0.5 t$。蜡烛烧完：$20 - 0.5 t >= 0 => t <= 40$；$t >= 0$。现实定义域 ${t | 0 <= t <= 40}$。 // 工具：一次函数公式 + 不等式求定义域
  + 七个点：$(-3, 5), (-2, 0), (-1, -3), (0, -4), (1, -3), (2, 0), (3, 5)$。描点连线得开口向上的抛物线。任一条铅垂线至多交曲线一次——*通过*垂线检验，是函数。 // 工具：代入求值 + 描点法（#secref("cb03-rectangular-system")） // 跨章回顾
  + 定义域 ${x | x != 3}$（分母非零）。解 $frac(2 x + 1, x - 3) = 5$：两边乘 $(x - 3)$ 得 $2 x + 1 = 5 x - 15$，$16 = 3 x$，$x = 16 / 3$。检验 $x != 3$ ✓。 // 工具：等式性质（#secref("cb05-equality-properties")）+ 分母非零 // 跨章回顾
  + 错误：$x = 4$ 时 $y = 2$ 或 $y = -2$，同一输入对应两个不同输出——违反函数单值性。纠正：$y = sqrt(x)$（只取非负根）才是函数；$y^2 = x$ 整体不是 $y$ 关于 $x$ 的函数。 // 工具：垂线检验 / 函数定义
]

#blueprint[
  *巩固复习*

  - 坐标点、四象限、描点：#secref("cb03-rectangular-system")、#secref("cb03-plotting-transforming")。函数图像就是“所有 $(x, f(x))$ 描出来”的那条曲线。
  - 字母代替数、代数式：#secref("cb04-letters-represent-numbers")、#secref("cb04-expressions-and-like-terms")。函数公式本质是一条*带独立变量*的代数式 $+$ 一份定义域。
  - 方程的等式性质：#secref("cb05-equality-properties")。求“$f(x) = 0$ 对应什么 $x$”时要把方程技术拿过来用。
  - 分式的合法性：从 #secref("ch:fractions") 你已经知道分母不能为 $0$——含字母的分式也一样，哪些 $x$ 让分母变 $0$ 就必须排除出定义域。
  - 根号合法性：#secref("cb06-square-and-square-root")。$sqrt$ 下面必须 $>= 0$，决定含根号函数的定义域。
  - 下一章衔接：把独立变量与因变量的关系写成*最简单的*一类公式 $y = k x$、$y = k x + b$，它们的图像是直线。
]

#side-hack[
  *☞ 高中桥 / 继续往前*

  _以下是预告，现在看不懂完全正常 — 等到高中再回来。_

  - 本章默认定义域只用“分式 / 根号”两类简单限制。高中会再加上*对数底数 $> 0$ 且 $!= 1$*、*反三角函数值域限制*、*复合函数的外层先看内层值域*等更复杂规则。
  - 函数的三张面孔在高中又会扩充出第四张：*用方程 $F(x, y) = 0$ 隐式定义*。这时候“是不是函数”就要逐段拆看。
  - 分段函数（像出租车计费表）是高中常见的情境化函数，其公式需要用 $cases(...)$ 写；本章暂不展开。
]
