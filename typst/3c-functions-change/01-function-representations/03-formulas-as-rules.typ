// [wave7-mastery-variant: 推广反推]
#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref, answer-cut
#import "../../lib/diagram-packages.typ": cetz

=== 公式：函数的第三张面孔 <tool:fn03-formula-as-function-view>

#vocab[独立变量 independent variable][因变量 dependent variable][定义域 domain][对应规则 rule of correspondence][函数记号 function notation]

#crisis[
  表给出了几个样本；图给出了整体形状——但若要*精确*算“输入 $x = 17.3$ 时输出是多少”，查表也画不出，读图也读不准。幸好，上一节里售水机那条规则“输出 $=$ 输入 $div 2$”只是一句代数式——这让我们能随便取一个 $x$，机械地算出对应 $y$。

  从 #secref("cb05-letters-represent-numbers") 起，我们已经让字母代替数了，从 #secref("cb06-equation-as-balance") 起等号两边能互相变形。*现在我们要用同样的字母语言写下函数的对应规则*——让一条公式代替一整张查询表、代替一整条手绘曲线。

  可是新的麻烦也出现了：$y = 1 / x$ 里 $x = 0$ 不能代入；$y = sqrt(x)$ 里 $x < 0$ 不能代入。公式不像表能自带“输入范围”——我们得自己把*允许取的 $x$*（定义域）说清楚。
]

#tryit[
  先动手试试，下面再读。

  + 售水机的规则是"输出 $=$ 输入 $div$ $2$"。当输入分别是 $4$、$10$、$17.3$ 时，输出各是多少？用字母 $x$ 表示输入，写出"输出"的代数式，试着算当 $x = 0$ 时输出是多少。
]

#discovery[
  *从表 / 图到公式*

  售水机那组点 $(2, 1)$、$(4, 2)$、$(6, 3)$ ……的规律是“输出等于输入的一半”。用字母写：设输入为 $x$、输出为 $y$，

  $ y = x / 2. $

  这样不管 $x$ 取 $17.3$ 还是 $999.8$，都能算出对应的 $y$。

  *独立变量与因变量*

  一条函数公式里*总有两个变量*在角色上不对称：

  - 可以*自由选值*的那个叫*独立变量*（或“自变量”），通常用 $x$。
  - 被规则*算出来*的那个叫*因变量*，通常用 $y$（或 $f(x)$）。

  “输出 $=$ 输入的一半”里 $x$ 是独立变量（我自由投币）、$y$ 是因变量（售水机算给我）。

  *函数记号 $f(x)$*

  为了让公式和它所描述的函数的名字分开，引入专门的记号：

  $ f(x) = x / 2. $

  这里 $f$ 是这个函数的名字，$f(x)$ 读作“$f$ 在 $x$ 处的值”。这样可以把公式和它*产生的输出*分开说：$f(6) = 3$ 意思是“把 $6$ 投给 $f$，算出来是 $3$”；$f(6)$ 本身是一个*数*，不是公式。

  *定义域*

  定义域（domain）：允许代入的 $x$ 组成的集合。它通常由两类限制决定：

  + *现实情境*。售水机只接 $2, 4, 6, 8, 10$ 元 $=>$ 定义域 ${2, 4, 6, 8, 10}$。
  + *代数式本身的合法性*：
    - $y = 1 / x$：$x = 0$ 让分母为 $0$，*不合法* $=>$ 定义域 ${x | x != 0}$。
    - $y = sqrt(x)$：$x < 0$ 让根号下为负，*不合法*（#secref("cb07-square-and-square-root") 只允许对非负数取算术平方根）$=>$ 定义域 ${x | x >= 0}$。
    - $y = x^2$、$y = 2 x + 3$：任何实数都合法 $=>$ 定义域 $RR$。

  如果题目没说现实限制，我们默认把定义域取成*让公式合法的所有实数*。

  #figure(
    cetz.canvas(length: 0.5cm, {
      import cetz.draw: *
      // y = 1/x domain: number line with hole at 0
      content((-7, 1.5), text(8pt)[$y = 1 / x$], anchor: "west")
      line((-6, 0), (6, 0), stroke: 0.5pt, mark: (end: ">", start: ">"))
      circle((0, 0), radius: 0.2, stroke: 0.6pt + rgb("#B71C1C"), fill: white)
      content((0, -0.6), text(7pt)[$0$])
      content((6.5, 0), text(7pt)[$x$])
      // y = sqrt(x) domain: ray from 0 rightward
      content((-7, -2.5), text(8pt)[$y = sqrt(x)$], anchor: "west")
      line((-6, -4), (6, -4), stroke: 0.3pt + luma(180))
      line((0, -4), (6, -4), stroke: 0.7pt + rgb("#1976D2"), mark: (end: ">"))
      circle((0, -4), radius: 0.15, fill: rgb("#1976D2"))
      content((0, -4.6), text(7pt)[$0$])
      content((6.5, -4), text(7pt)[$x$])
      content((3, -3.4), text(7pt)[$x >= 0$])
    }),
    caption: [$y = 1 / x$ 的定义域在 $x = 0$ 处挖去一点；$y = sqrt(x)$ 的定义域只取 $x >= 0$ 的半轴。]
  )


  *同一个函数的三张面孔*

  #align(center, table(
    columns: 2,
    stroke: 0.4pt,
    inset: 6pt,
    [*面孔*], [*具体例子：$f(x) = x / 2$*],
    [*表*], [$f(2) = 1$、$f(4) = 2$、$f(6) = 3$……（选一些 $x$ 列出）],
    [*图*], [平面上的一条直线——过原点，斜率 $1/2$（下一章会讲斜率）],
    [*公式*], [$f(x) = x / 2$，定义域 $RR$],
  ))

  同一个函数可以用这三种面孔*互相翻译*——遇到一种面孔不方便时换一种。这是本章最重要的一条自学技能。

  #figure(
    cetz.canvas(length: 0.6cm, {
      import cetz.draw: *
      // triangle vertices
      let top = (0, 3)
      let bl = (-3.5, -1)
      let br = (3.5, -1)
      // triangle edges with arrows
      line(top, bl, stroke: 0.5pt + rgb("#1976D2"), mark: (end: ">", start: ">"))
      line(bl, br, stroke: 0.5pt + rgb("#1976D2"), mark: (end: ">", start: ">"))
      line(br, top, stroke: 0.5pt + rgb("#1976D2"), mark: (end: ">", start: ">"))
      // labels at vertices
      content(top, text(9pt)[*公式*], anchor: "south", padding: 0.3)
      content(bl, text(9pt)[*表*], anchor: "north-east", padding: 0.3)
      content(br, text(9pt)[*图*], anchor: "north-west", padding: 0.3)
      // edge labels
      content((-2.2, 1.3), text(7pt)[代入求值], anchor: "east")
      content((0, -1.6), text(7pt)[逐点描出], anchor: "north")
      content((2.2, 1.3), text(7pt)[读特征反推], anchor: "west")
    }),
    caption: [函数的"三张面孔"可以互相翻译：公式 $arrow.l.r$ 表 $arrow.l.r$ 图。]
  )

]

#tryit[
  + 设 $f(x) = 2 x + 1$。算出 $f(-3)$、$f(0)$、$f(5)$、$f(1 / 2)$。
  + 写出函数“把输入 $x$ 减去 $3$ 再平方”的公式，并算 $f(5)$、$f(3)$、$f(0)$。
]

#history-note[
  $1748$ 年欧拉（Leonhard Euler）出版《无穷分析引论》（_Introductio in analysin infinitorum_），第一次系统地把函数写成公式表达式，并推广了沿用至今的 $f(x)$ 记号。在此之前，"函数"只是一个模糊的几何概念；欧拉让它变成了可以运算、可以展开、可以组合的代数对象。从那之后，*函数 $=$ 一条公式 $+$ 一份定义域*成为主流写法，直到 $1837$ 年狄利克雷（Dirichlet）把定义拓宽到"不一定有公式"的对应关系。
]

#blueprint[
  *工具：公式作为函数的规则*

  *函数公式*：由独立变量 $x$（或其它字母）和已知运算符号写成的表达式 $f(x) = "（某个关于 x 的表达式）"$。代入一个合法的 $x$，算出的那个值就是这个函数对该输入的输出，写作 $f(x)$。

  *定义域*（domain）：允许代入的 $x$ 组成的集合。没有现实限制时，默认为*让公式合法的所有实数*。两类最常见的违规：
  - 分母变成 $0$（和分数运算一样，分母不能为 $0$——见 #secref("ch:fractions")）；
  - 根号下为负数（见 #secref("cb07-square-and-square-root")）。

  *独立变量 / 因变量*：公式里*能自由选*的字母叫独立变量；被算出来的那个字母叫因变量。“$y = f(x)$”这种写法里 $x$ 是独立变量、$y$ 是因变量。

  *三面孔互译*

  - 表 → 公式：看差、看比、看二差（后面章节陆续展开）。
  - 公式 → 表：代入一串 $x$，填输出。
  - 公式 → 图：逐点代入 $=>$ 标点 $=>$ 连线（下一章专门讲一次函数的画法）。
  - 图 → 公式：读几个特征点（截距、斜率、顶点 ……）反推形式。

  *综合例*：$f(x) = frac(x + 1, x - 2)$。求 $f(3)$、$f(0)$、定义域。

  *解*：$f(3) = 4 / 1 = 4$；$f(0) = 1 / (-2) = -1/2$。定义域：分母 $x - 2 != 0 => x != 2$，即 ${x | x != 2}$。
]

#pitfall[
  *高频错误*

  ✗ 把 $f(x)$ 当成“$f$ 乘 $x$”
  → ✓ $f(x)$ 是函数 $f$ 在 $x$ 处的*输出值*，不是乘法。

  ✗ 写 $f(2 + 3) = f(2) + f(3)$
  → ✓ 一般*不成立*。要先算括号里的 $2 + 3 = 5$，再代入：$f(2 + 3) = f(5)$。只有少数特殊函数（一次且过原点等）才恰好满足 $f(a + b) = f(a) + f(b)$。

  ✗ 忘了检查定义域，直接代入让分母为 $0$ 或让根号下变负
  → ✓ 写公式的同时就写出定义域；遇到“求 $f(2)$ 但 $2$ 不在定义域”的情况，答“该值在定义域外，无定义”。

  ✗ 同一个 $x$ 代进去算出两个不同 $y$
  → ✓ 这说明你要么代入有误，要么公式里写了 $plus.minus$ 之类使输出不唯一的表达式（如 $y = plus.minus sqrt(x)$）——后者*不是函数公式*。
]

#mastery[
  *基础*

  + 设 $f(x) = 3 x - 2$。算 $f(0)$、$f(1)$、$f(-2)$、$f(1 / 3)$。
  + 写出公式：“输入一个数，先乘 $2$ 再加 $5$”。用 $f$ 命名它，算 $f(4)$ 与 $f(-1)$。
  + 写下 $f(x) = frac(1, x)$ 的定义域。$f(1)$、$f(-1)$、$f(0)$ 各是多少？

  *应用*

  + 一辆汽车匀速 $60$ 公里 / 小时。设 $d$（公里）为行驶里程，$t$（小时）为时间。写出 $d$ 作为 $t$ 的函数公式，并指出独立变量、因变量、合理的定义域。
  + 一个正方形的边长 $s$ 变化。写出周长 $P(s)$、面积 $A(s)$ 两个函数。它们的定义域是什么？
  + 设 $f(x) = sqrt(4 - x)$。求 $f(0)$、$f(3)$、$f(4)$。它的定义域是什么？

  *挑战 ☞ 选做*

  + 设 $f(x) = x^2 - 2 x$。计算 $f(x + 1) - f(x)$ 并化简。结果能用来说明什么？
  + 设 $f(x) = frac(x + 3, x - 5)$。求定义域；对于什么样的 $x$ 值，$f(x) = 0$？
  + *推广反推题：从特例到一般，再反向求解定义域*
    - *特例*：先看 $f(x) = sqrt(4 - x)$，求 $f(0)$、$f(3)$、$f(4)$，并写出定义域 $\{x | x <= 4\}$。
    - *推广*：把 $f(x) = sqrt(a - x)$ 中的 $a$ 视为参数（$a$ 是任意实数）。$f$ 的定义域用 $a$ 表达是什么？$f(0)$、$f(a/2)$、$f(a)$ 分别是多少？
    - *反推*：现在反过来——*已知* $f$ 的定义域是 $\{x | x <= 7\}$，能反求 $a$ 吗？再问：若已知 $f(0) = 3$，能反求 $a$ 吗？两个反推得到的 $a$ 一致吗？为什么？

  + *[反直觉]* 设 $f(x) = x^2$。判断："因为 $f(2) = 4$，所以 $f^(-1)(4) = 2$，即'输入 $4$ 反推输入'唯一确定原来的 $x$。" 给反例并解释：什么样的公式才能保证"反推唯一"？

  + *[跨知识点]* 设 $f(x) = (x^2 - 1)/(x - 1)$。先用 #secref("tool:al03-rational-expression-structure") 分析定义域，再化简公式。化简后的表达式与原 $f(x)$ 在哪里"看起来一样"、哪里仍然不同？画出 $f(0), f(2), f(1)$ 三处的值（如果存在），强调约分对定义域的影响。

  + *[找 Bug]* 小慕写："$f(x) = 1/(x - 3) + sqrt(x - 5)$ 的定义域是 $x != 3$。" 她漏了什么？正确定义域是？

  #answer-cut[

  *基础*
  + $f(0) = -2$，$f(1) = 1$，$f(-2) = -8$，$f(1 / 3) = 1 - 2 = -1$。（代入求值：$f(x) = 3 x - 2$）
  + $f(x) = 2 x + 5$；$f(4) = 13$，$f(-1) = 3$。（文字转公式，再代入求值）
  + 定义域 ${x | x != 0}$。$f(1) = 1$，$f(-1) = -1$，$f(0)$ 无定义。（定义域：分母不为 $0$）

  *应用*
  + $d = 60 t$（或 $d(t) = 60 t$）。独立变量 $t$、因变量 $d$。现实定义域 $t >= 0$（时间不可为负）。（写出公式，识别独立变量与因变量，确定现实定义域）
  + $P(s) = 4 s$；$A(s) = s^2$。边长必须取正：定义域 ${s | s > 0}$。（分别写周长、面积公式，由现实约束确定定义域）
  + $f(0) = 2$，$f(3) = 1$，$f(4) = 0$。定义域：$4 - x >= 0 <=> x <= 4$。（先代入求值，再由根号下非负确定定义域）

  *挑战 ☞ 选做*
  + $f(x + 1) = (x + 1)^2 - 2 (x + 1) = x^2 - 1$；$f(x) = x^2 - 2 x$。相减：$f(x + 1) - f(x) = (x^2 - 1) - (x^2 - 2 x) = 2 x - 1$。
    说明：$f$ 每次"$x$ 增加 $1$"时，$f$ 的变化量随 $x$ 线性增加——提示 $f$ 不是一次函数（一次函数的变化量应当是常数）。（先展开 $f(x+1)$，再作差化简，比较变化量是否为常数）
  + 定义域 ${x | x != 5}$。$f(x) = 0$ 要求分子 $x + 3 = 0$ 且分母不为 $0$，故 $x = -3$。（分母 $x - 5 != 0$ 定义域；令分子为 $0$ 求零点）
  + *推广反推*：
    - 特例：$f(0) = 2$，$f(3) = 1$，$f(4) = 0$；定义域 $\{x | x <= 4\}$。
    - 推广：$f(x) = sqrt(a - x)$ 要求 $a - x >= 0 <=> x <= a$，所以*定义域 $\{x | x <= a\}$*。$f(0) = sqrt(a)$（要求 $a >= 0$ 才有意义），$f(a/2) = sqrt(a/2)$（要求 $a >= 0$），$f(a) = 0$。
    - 反推 1：定义域是 $\{x | x <= 7\}$ $=>$ $a = 7$。
    - 反推 2：$f(0) = 3$ $=>$ $sqrt(a) = 3$ $=>$ $a = 9$。
    - 两个反推得到的 $a$ *不一致*（$7$ 与 $9$）——这说明：*同一个函数族 $f(x) = sqrt(a - x)$ 不可能同时满足"定义域是 $\{x | x <= 7\}$"和"$f(0) = 3$"*。每条信息分别决定了不同的 $a$。要让两条都成立，必须修改函数形式或承认题意冲突。这正是反推题的价值——*反推不只是解出一个数，更要检查"信息之间是否相容"*。
  + 反例：$f(2) = 4$，但 $f(-2) = 4$ 也成立。所以"输入 $4$"反推时，$x$ 可以是 $2$ 或 $-2$，不唯一。要保证反推唯一，公式必须是*单射*——同一个 $x$ 的输出唯一（这是函数定义本身的要求），且*不同的 $x$ 输出也不同*。$f(x) = x^2$ 在整个实数域上不是单射（关于 $y$ 轴对称），但若把定义域限制为 $x >= 0$ 就单射了，此时反推唯一为 $x = 2$。
  + 由 #secref("tool:al03-rational-expression-structure")，原 $f(x) = (x^2 - 1)/(x - 1)$ 要求分母 $x - 1 != 0$，即*定义域 $x != 1$*。化简：$(x^2 - 1)/(x - 1) = ((x - 1)(x + 1))/(x - 1) = x + 1 quad (x != 1)$。化简后看起来与 $g(x) = x + 1$ "一样"，但 $g$ 的定义域是全体实数，$f$ 在 $x = 1$ 没有定义——图像上 $x = 1$ 处是个空心点。$f(0) = 1$，$f(2) = 3$，$f(1)$ 不存在（约分扩大定义域）。
  + 漏了 $sqrt(x - 5)$ 要求 $x - 5 >= 0$ 即 $x >= 5$。两个限制同时满足：$x >= 5$ 且 $x != 3$；由于 $5 > 3$，$x >= 5$ 自动包含 $x != 3$。正确定义域：$x >= 5$（多项条件取*交集*）。
  ]
]
