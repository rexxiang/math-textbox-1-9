#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref
#import "../../lib/diagram-packages.typ": cetz

=== 图像：函数的第二张面孔 <tool:fn02-graph-as-function-view>

#vocab[函数图像 graph of a function / 垂线检验 vertical line test / 单值性 single-valuedness]

#crisis[
  上一节里函数是一张*表*。但生活里很多关系*连续*在变：水温随时间下降、气球高度随时间上升——每一瞬都有一个数值，而表只能挑几个瞬间记下来。

  既然坐标平面（#secref("cb03-rectangular-system")）可以给每个点发身份证，一个很自然的想法是：把表里每一行“输入 $x$、输出 $y$”画成平面上一点 $(x, y)$；点连成的曲线就把*所有*输入同时展示出来。

  *但新的陷阱也随之出现*：不是每一条画在平面上的曲线都代表函数。一个圆也能画在坐标平面上，它却不是函数——同一个 $x$ 可以对应圆上两个不同的 $y$。*如何只看图就判断它是不是一个函数？*
]

#history-note[
  把*规则*画成*曲线*的做法最早由 Descartes（#secref("ge05-coordinate-transforms") 提到过）在 $17$ 世纪成型。正是这一步让“图像”成为和“公式”“表格”并列的第三种函数面孔——也是物理学家、工程师最常使用的一张面孔。
]

#discovery[
  *从表到图*

  上一节售水机的表 $x -> y$：$2 -> 1$、$4 -> 2$、$6 -> 3$、$8 -> 4$、$10 -> 5$。把每一行当成平面上一点 $(x, y)$ 画出来：

  #figure(
    cetz.canvas(length: 0.5cm, {
      import cetz.draw: *
      line((-0.5, 0), (12, 0), stroke: 0.4pt, mark: (end: ">"))
      line((0, -0.5), (0, 7), stroke: 0.4pt, mark: (end: ">"))
      content((12.3, 0), $x$, anchor: "west")
      content((0, 7.3), $y$, anchor: "south")
      let pts = ((2, 1), (4, 2), (6, 3), (8, 4), (10, 5))
      for p in pts {
        circle(p, radius: 0.15, fill: black)
      }
      // dashed line through them
      line((2, 1), (10, 5), stroke: (paint: rgb("#1976D2"), thickness: 0.5pt, dash: "dashed"))
      for xi in (2, 4, 6, 8, 10) {
        content((xi, -0.5), text(7pt)[#xi])
      }
      for yi in (1, 2, 3, 4, 5) {
        content((-0.5, yi), text(7pt)[#yi])
      }
    }),
    caption: [售水机“投币 $->$ 瓶数”函数的图像：$5$ 个离散点。它们恰好落在同一条直线上——提示规则是 $y = x slash 2$。]
  )

  这 $5$ 个点*看起来*像落在一条直线上——这就是图像相比表的优势：*结构*一眼就能看出来。

  *规则连续时图像是连续曲线*

  若函数的输入范围是*所有实数*（或一段区间），可以在每个输入上都取值画点，点挤得足够密就成一条连续曲线。例如“每小时气温”的函数，图像会是一条*折线*或*平滑曲线*。

  *危险：不是每张曲线都是函数*

  下面两张图哪张是函数图像？

  #grid(
    columns: (1fr, 1fr),
    gutter: 8pt,
    figure(
      cetz.canvas(length: 0.5cm, {
        import cetz.draw: *
        line((-4, 0), (4, 0), stroke: 0.4pt, mark: (end: ">"))
        line((0, -4), (0, 4), stroke: 0.4pt, mark: (end: ">"))
        content((4.3, 0), $x$, anchor: "west")
        content((0, 4.3), $y$, anchor: "south")
        circle((0, 0), radius: 2.5, stroke: 0.7pt + rgb("#B71C1C"))
        // vertical probe
        line((1.5, -3.5), (1.5, 3.5), stroke: (paint: rgb("#2E7D32"), thickness: 0.5pt, dash: "dashed"))
      }),
      caption: [圆：$x = 1.5$ 这条铅垂线与圆相交两次——同一个 $x$ 对应两个 $y$。]
    ),
    figure(
      cetz.canvas(length: 0.5cm, {
        import cetz.draw: *
        line((-4, 0), (4, 0), stroke: 0.4pt, mark: (end: ">"))
        line((0, -4), (0, 4), stroke: 0.4pt, mark: (end: ">"))
        content((4.3, 0), $x$, anchor: "west")
        content((0, 4.3), $y$, anchor: "south")
        // parabola y = x^2/2 - 2 between x=-3..3
        let samples = range(0, 61).map(i => {
          let x = -3 + 0.1 * i
          (x, x * x / 2.0 - 2)
        })
        for i in range(0, samples.len() - 1) {
          line(samples.at(i), samples.at(i + 1), stroke: 0.7pt + rgb("#1976D2"))
        }
        line((1.5, -3.5), (1.5, 3.5), stroke: (paint: rgb("#2E7D32"), thickness: 0.5pt, dash: "dashed"))
      }),
      caption: [抛物线：任一条铅垂线与它只相交一次——是函数。]
    ),
  )

  *垂线检验*

  “左图不是函数、右图是函数”的判别很漂亮：

  *在坐标平面上画一条铅垂线 $x = a$，它与图像的交点数就是 $a$ 对应的输出数。*

  - 函数要求“每个输入唯一对应一个输出” $=>$ 任意一条铅垂线最多与图像相交*一次*。
  - 如果某条铅垂线与图像相交 $2$ 次或更多 $=>$ 这张图不是函数。

  这叫作*垂线检验*（vertical line test）。
]

#tryit[
  + 下面三张图分别画出以下关系：
    - (a) 直线 $y = x + 1$；
    - (b) “X 形”的两条直线 $y = x$ 与 $y = -x$；
    - (c) 抛物线 $y = x^2$。
    用垂线检验判断哪些是函数。
  + 一只在垂直地面 $1$ 米高处释放的小球，其高度 $h$（米）随时间 $t$（秒）变化的图像大致是一段从左上到右下的*弧线*。它是函数吗？为什么？
  + 表示“输入一个正数 $a$，输出平方后等于 $a$ 的*所有*数”的图像——即 $x = y^2$ 的整条曲线——是函数吗？为什么？
]

#blueprint[
  *工具：函数图像 $+$ 垂线检验*

  *函数图像*：把函数的每一个“输入 $x$、输出 $y$”画成坐标平面上的点 $(x, y)$。所有这样的点组成的集合，就是函数的*图像*。

  *从图读值*：要知道输入 $x = a$ 对应的输出，做一条铅垂线 $x = a$，它与图像的交点的纵坐标就是 $y$。

  *垂线检验（函数判别）*：平面上给你一张图。它是某个函数的图像*当且仅当*：

  *任何一条铅垂线与图像至多相交一次。*

  *判图三步*

  + 画一条可变位置的铅垂线，让它从左向右扫过。
  + 若每一步它与图像的交点数都 $<= 1$，它是函数。
  + 若某处与图像交点 $>= 2$，立刻判定*不是函数*。

  *综合例*：半圆 $y = sqrt(4 - x^2)$（上半圆）的图像是函数吗？整圆 $x^2 + y^2 = 4$ 呢？

  *解*：上半圆每条铅垂线（$-2 <= x <= 2$）最多交一次 $=>$ *是函数*。整圆在 $-2 < x < 2$ 内每条铅垂线都交 $2$ 次 $=>$ *不是函数*。
]

#pitfall[
  *高频错误*

  ✗ 用*水平线*检验函数（“一条水平线与图像交两次 $=>$ 不是函数”）
  → ✓ 要用*铅垂线*。水平线交两次只说明两个输入配同一输出——这是合法的。

  ✗ 圆“看起来很对称”就认定是函数
  → ✓ 圆在中央竖线左右两侧都交两次，明显不是函数。“对称”和“是函数”没关系。

  ✗ 离散点图看不出垂线交几次，就当它总是函数
  → ✓ 如果两个点的横坐标相同（比如 $(3, 5)$ 和 $(3, 7)$），$x = 3$ 这条铅垂线就交图像两次——不是函数。离散图里只要*有两点横坐标相同但纵坐标不同*就不是函数。

  ✗ 把“函数图像”和“曲线方程”搞混：$x^2 + y^2 = 4$ 是方程，它的图像（整圆）不是函数
  → ✓ “是不是函数”取决于它把*哪个*变量当输入。如果选 $y$ 当输入、$x$ 当输出，把图翻过来（横着看），整圆依旧不是函数。
]

#mastery[
  *基础*

  + 用坐标纸画出函数 $y = 2x$ 在输入范围 $x = -2, -1, 0, 1, 2$ 的图像（$5$ 个点）。连线后判断：是函数吗？
  + 下图（文字描述）是函数图像吗：两条相交于原点、斜率分别为 $1$ 和 $-1$ 的直线？
  + “输入是学生姓名、输出是年龄”的关系，能否画成函数图像？如果能，把你班上 $5$ 个同学的数据画成点——有没有碰到同一条铅垂线交两点的情况？

  *应用*

  + 某水库水位 $h$（米）随月份 $t$（$t = 1, 2, ..., 12$）变化的图像：先升后降再升，且 $7$ 月与 $11$ 月高度相同。这是函数吗？从图上如何读出 $t = 7$ 时水位？
  + 抛物线 $y = (x - 2)^2 - 1$ 的图像——有一个最低点在 $(2, -1)$，两条开口朝上的臂。用垂线检验判断它是函数吗。再做*水平线*检验——$y = 3$ 这条水平线和图交几次？说明什么？
  + “一个人的身高随年龄变化”的图像大致是单调上升后趋于平稳。这是函数吗？为什么？

  *挑战*

  + 一段离散数据：$(1, 3)$、$(2, 5)$、$(3, 5)$、$(4, 8)$、$(2, 7)$。把这 $5$ 点画在纸上。它们代表函数吗？如果不是，哪一对点违反了垂线检验？
  + 把函数 $y = x^2$ 的图像沿 $y = x$ 翻折（把每点 $(x, y)$ 换成 $(y, x)$）。翻折后的图是否还是函数？给出判断。
  + 设计一张“看起来像 M 形”的曲线（两个尖上升—下降—上升—下降），判断它是不是函数。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + 点 $(-2, -4)$、$(-1, -2)$、$(0, 0)$、$(1, 2)$、$(2, 4)$ 落在一条直线上。任一铅垂线最多与直线交一次。*是函数*。
  + 两条直线在原点交叉构成 X 形。$x = 0$ 这条铅垂线与 X 只交一次（原点），其它铅垂线和每条直线各交一次——一条铅垂线与 X 交*两次*（两个输出）。*不是函数*。
  + 重名时，同一姓名（同一铅垂线）可能对应两个不同年龄——不一定是函数。

  *应用*
  + *是函数*——每个 $t$ 恰好对应一个水位。$7$ 月水位通过图读出其纵坐标即可；“$7$ 月与 $11$ 月水位相同”只是输出列重复，不违反函数定义。
  + 抛物线：每条铅垂线交一次 $=>$ *是函数*。$y = 3$ 这条水平线交两次 $=>$ 两个输入 $x$ 给出同一输出——合法，水平线检验不用于判函数性质。
  + 每个年龄唯一对应一个身高 $=>$ *是函数*。“趋于平稳”是输出变化的描述，不影响函数性质。

  *挑战*
  + $(2, 5)$ 与 $(2, 7)$ 横坐标相同但纵坐标不同 $=>$ $x = 2$ 这条铅垂线与图像交两次。*不是函数*。
  + $y = x^2$ 翻成 $x = y^2$（横卧的抛物线）。$x = 4$ 这条铅垂线与翻折图像交两次（$y = 2, -2$）——*不是函数*。
  + 典型 M 形在每个 $x$ 上都仍只交一次铅垂线（仅上下起伏、没有自我回绕）。*是函数*。（只要 M 形不要“回头自绕”，铅垂线就只交一次。）
]
