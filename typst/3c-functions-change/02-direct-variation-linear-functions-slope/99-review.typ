#import "../../lib/theme-v2.typ": blueprint, mastery, side-hack, secref
#import "../../lib/chapter-kit.typ": diagnostic-table, answer-cut

== 本章回顾 <meta:direct-variation-linear-functions-slope>

#blueprint[
  *知识速查卡*

  #table(
    columns: (1fr, 1.6fr),
    inset: 6pt,
    stroke: 0.4pt,
    [*主题*], [*一句话提要*],
    [正比例 #secref("fn04-direct-variation")], [$y = k x$（$k != 0$）；图像过原点的直线；$y / x = k$ 恒为常数。],
    [一次函数 #secref("fn05-linear-function")], [$y = k x + b$（$k != 0$）；图像与 $y$ 轴交于 $(0, b)$；$=$ 正比例图像上移 $b$。],
    [斜率 #secref("fn06-slope-as-rate")], [$m = (y_2 - y_1) slash (x_2 - x_1)$；意义是“$x$ 每加 $1$、$y$ 变多少”，与选点无关（相似）。],
    [斜率的正负], [$m > 0$ 右上斜；$m < 0$ 右下斜；$m = 0$ 水平线；铅垂线无斜率。],
    [画图三点速写], [标 $(0, b)$ 与 $(-b / k, 0)$，连直线。若 $b = 0$ 就用原点 $+$ 任一点。],
    [判表是否一次], [$x$ 等差时 $y$ 差恒为常数；$x$ 不等差时算 $Delta y / Delta x$ 是否恒定。],
    [从两点求解析式], [先求斜率 $m$；再代入任一点解 $b$：$y = m x + b$。],
    [$k = 0$ 的退化], [$y = b$ 是常值函数，不算一次函数（本章约定）。],
  )
]

#mastery[
  *混合自测*

  + 写出下列关系式并判断是否正比例：
    - (a) 每升汽油 $8$ 元，$x$ 升花 $y$ 元；
    - (b) 某班共 $40$ 人，男生 $x$ 人，女生 $y$ 人。
  + $y = -3 x + 6$ 的斜率、截距、与两坐标轴的交点各是什么？
  + 直线过 $(2, -1)$ 与 $(6, 7)$，求斜率与解析式。
  + 下表是否符合一次函数？若是，写出 $y = k x + b$：
    #align(center, table(
      columns: 5,
      align: center,
      stroke: 0.4pt,
      [$x$], [$0$], [$1$], [$3$], [$5$],
      [$y$], [$1$], [$4$], [$10$], [$16$],
    ))
  + 一次函数图像与 $y$ 轴交于 $(0, 4)$、与 $x$ 轴交于 $(2, 0)$，求解析式。
  + 某出租车起步 $6$ 元（含 $1$ 公里），之后每公里 $4$ 元。设行驶 $x >= 1$ 公里总价 $y$ 元。写出 $y = f(x)$。斜率是什么？现实意义是什么？
  + 两直线 $y = 2 x + 1$ 与 $y = -x + 4$ 交点坐标。
  + 判断正误：“斜率 $= 0$ 的直线斜率无定义”。
  + 某商品每件定价 $p$ 元，售出 $n$ 件的收入 $y = p n$。若总收入与总件数之比始终为 $25$（#secref("cb01-ratio-basics")），求 $p$ 与对应的正比例函数。 // 跨章回顾
  + 下表记录了 $x$ 与 $y$ 的对应值。先判断是否为函数（#secref("fn01-table-as-function-view")），若是，再判断是否为一次函数。 // 跨章回顾
    #align(center, table(
      columns: 6,
      align: center,
      stroke: 0.4pt,
      [$x$], [$1$], [$2$], [$2$], [$3$], [$4$],
      [$y$], [$5$], [$3$], [$3$], [$1$], [$-1$],
    ))
  + *估算判断*：一条直线过 $(0, 100)$ 和 $(1000, 103)$。不用精确计算，判断以下说法是否合理：(a)“该直线斜率大约为 $3$”；(b)“该直线几乎水平”。

  *[选择题（多选）]* 下列函数图像过原点的是（多选）（）

  - A. $y = 3 x$
  - B. $y = 3 x + 1$
  - C. $y = -2 x$
  - D. $y = 5$
  - E. $y = 5 x - 0$

  *[选择题（单选）]* 直线 $y = -3 x + 4$ 的斜率是（）

  - A. $4$ B. $-3$ C. $3$ D. $-1/3$

  *[选择题（单选）]* 一次函数 $y = k x + b$（$k != 0$），当 $k < 0$ 且 $b > 0$ 时，图像经过（）

  - A. 第一、二、三象限
  - B. 第一、二、四象限
  - C. 第二、三、四象限
  - D. 第一、三象限

  *估算题*：某城市一条地铁线路全长约 $36$ 千米，共 $18$ 站（包括起终点）。相邻两站的平均间距约是多少千米？若用一次函数 $d = k t$ 表示从起点出发的距离（$d$ 千米）与时间（$t$ 分钟）的关系，假设平均速度为 $60$ 千米/小时，估算斜率 $k$ 的值（$k$ 的单位为千米/分钟），并解释其现实含义。

  *挑战*

  + *反例构造*：举一个具体的一次函数，其图像不经过第三象限，并指出该函数的 $k$ 和 $b$ 满足什么条件，解释为何不过第三象限。
  + *找 Bug*：奥梅尔（Omer）求过 $(1, 3)$ 和 $(3, 7)$ 两点的直线斜率时写道："斜率 $= (7 - 3) / (1 - 3) = 4 / (-2) = -2$"。找出错误，写出正确斜率，并说明斜率公式中分子分母各代表什么。

]

==== 参考答案

+ (a) $y = 8 x$，正比例（$k = 8$）；(b) $y = 40 - x$，*不是*正比例（有常数项 $40$，代入 $x = 0$ 得 $y = 40 != 0$）。
+ 斜率 $-3$，截距 $6$；$y$ 轴交 $(0, 6)$，$x$ 轴交 $(2, 0)$。
+ $m = (7 - (-1)) / (6 - 2) = 2$。代 $(2, -1)$：$-1 = 4 + b => b = -5$。$y = 2 x - 5$。
+ 取 $(0, 1), (1, 4)$：$m = (4 - 1) / (1 - 0) = 3$。代 $(0, 1)$：$1 = 0 + b => b = 1$。$y = 3 x + 1$。验 $(3, 10)$：$9 + 1 = 10$ ✓，验 $(5, 16)$：$15 + 1 = 16$ ✓。
+ $m = (4 - 0) / (0 - 2) = -2$，$b = 4$，$y = -2 x + 4$。
+ $y = 6 + 4 (x - 1) = 4 x + 2$。斜率 $= 4$，即*每公里车费 $4$ 元*。
+ 联立：$2 x + 1 = -x + 4 => 3 x = 3 => x = 1$；$y = 2 times 1 + 1 = 3$。交点 $(1, 3)$。
+ *错误*。斜率 $= 0$ 表示水平线，斜率*是*定义且等于 $0$。无定义的是*铅垂线*（分母 $Delta x = 0$）。
+ 比值恒为 $25 =>$ $p = 25$，$y = 25 n$。
+ $x = 2$ 出现两次但输出都是 $3$——同一输入对应同一输出，*是函数*。检查一次性：取 $(1, 5), (2, 3), (3, 1)$，差 $= -2$ 恒定。$y = -2 x + 7$。验 $(4, -1)$：$-8 + 7 = -1$ ✓。*是一次函数*。
+ (a) *不合理*。$Delta y = 3$，$Delta x = 1000$，斜率 $= 3 / 1000 = 0.003$，远小于 $3$。(b) *合理*。斜率 $0.003$ 接近 $0$，$y$ 在 $x$ 变化 $1000$ 时仅增 $3$，图像确实几乎水平。
+ 选择题（多选）答案：A、C、E。过原点 $<=>$ 当 $x = 0$ 时 $y = 0$，即无常数项（$b = 0$）。A：$y = 3x$，B 不过（$b = 1$）；C：$y = -2x$；D 不过（$y = 5$，水平线）；E：$y = 5x - 0 = 5x$ 过原点。
+ 选择题（单选 1）答案：B。$y = k x + b$ 中斜率就是 $k$，这里 $k = -3$。
+ 选择题（单选 2）答案：B。$k < 0$ 右下斜；$b > 0$ 纵截距在正 $y$ 轴。图像从左上（第二象限）到右下（第四象限），经过正 $y$ 轴截距（第一象限上方），故经过一、二、四象限。
+ 估算题：平均间距 $= 36 / 17 approx 2.1$ 千米（$18$ 站有 $17$ 段间距）。斜率 $k = 60$ 千米/小时 $= 60/60 = 1$ 千米/分钟。含义：每分钟前进约 $1$ 千米。
+ 反例：$y = -2x + 5$（$k = -2 < 0, b = 5 > 0$）。$y$ 截距为 $(0, 5)$（正），$x$ 截距为 $(2.5, 0)$（正）。两截距均在正半轴，直线不进入第三象限（$x < 0, y < 0$ 的区域）。$k < 0, b > 0$ 时图像不过第三象限。
+ 奥梅尔的错误：把两点的横纵坐标顺序混淆，分子用了 $Delta y = 7 - 3 = 4$，分母用了"颠倒的"$x_1 - x_2 = 1 - 3 = -2$，但正确公式是 $m = (y_2 - y_1) / (x_2 - x_1)$，分子分母要用*同一顺序*。正确：$m = (7 - 3) / (3 - 1) = 4 / 2 = 2$。分子是纵坐标差（$Delta y$），分母是横坐标差（$Delta x$）。

#blueprint[
  *巩固复习*

  - #secref("cb01-ratio-basics") / #secref("cb01-proportion")：正比例“同 $k$”就是“同比”的函数化表达。
  - #secref("cb04-rectangular-system") / #secref("cb04-plotting-transforming")：描点、平移；一次函数 $=$ 正比例平移 $b$。
  - #secref("cb06-solving-linear")：求 $y = k x + b$ 的零点就是解方程 $k x + b = 0$。
  - 斜率“选点无关”的直觉：直线上任两点构成的直角三角形形状相同（同样的“宽高比”），所以 $Delta y / Delta x$ 不随选点改变——在 #secref("cb04-plotting-transforming") 的坐标平面中可以直接验证。
  - #secref("fn03-formula-as-function-view")：定义域的合法性——一次函数定义域默认为 $RR$。
  - 下一章衔接：把*除法* $1 / x$ 与*平方* $x^2$ 搬进函数公式，得到反比例与二次函数；它们的图像不再是直线。
]

#side-hack[
  *☞ 高中桥 / 继续往前*

  _以下是预告，现在看不懂完全正常 — 等到高中再回来。_

  - 点斜式 $y - y_0 = m (x - x_0)$、两点式 $(y - y_1) / (y_2 - y_1) = (x - x_1) / (x_2 - x_1)$、一般式 $A x + B y + C = 0$：高中用这三种等价写法把“直线方程”系统化。
  - 斜率与*倾斜角* $alpha$：$m = tan alpha$（$alpha$ 是直线与 $x$-轴正方向的夹角）。$tan$ 是高中三角函数的内容；到那时斜率与角度会正式对接。
  - 两直线*平行*：同 $k$、不同 $b$；两直线*垂直*：$k_1 k_2 = -1$（高中给出证明）。
  - 线性回归：现实数据不会正好落在一条直线上，高中 / 大学引入“最小二乘法”选出“最贴合”的那条直线。
]

#diagnostic-table(
  [斜率公式分子分母搞反],
  secref("fn06-slope-as-rate"),
  [$m = (y_2 - y_1) / (x_2 - x_1) = Delta y / Delta x$；纵变化量除以横变化量，两点要用同一顺序],

  [一次函数和正比例分不清],
  secref("fn04-direct-variation"),
  [正比例 $y = k x$：过原点（$b = 0$）；一次函数 $y = k x + b$：多了纵截距 $b$],

  [从两点求解析式找不到 $b$],
  secref("fn05-linear-function"),
  [先用两点坐标差求斜率 $k$；再代任一点 $(x_0, y_0)$ 入 $y_0 = k x_0 + b$，解出 $b$],
)
