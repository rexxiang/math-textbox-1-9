#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref
#import "../../lib/diagram-packages.typ": cetz

=== 一次函数：$y = k x + b$ <tool:fn05-linear-function>

#vocab[一次函数 linear function][截距 y-intercept][平移 translation]

#crisis[
  某出租车起步价 $10$ 元，起步里程内不额外计价；超过部分每公里 $2$ 元。设你坐车行驶 $x$ 公里（$x >= $ 起步里程），总价 $y$ 元：

  $ y = 10 + 2 x. $

  这不是正比例——$x = 0$ 时 $y = 10 != 0$。但它长得和正比例很像——多了个 $+ 10$ 的*起点*。

  日常类似的关系非常多：手机套餐“月租 $+$ 按分钟计费”、游泳馆“开卡费 $+$ 按次计费”……它们共同的骨架是：*一个固定“起点”$+$ 一个“每单位 $x$ 固定变化量”*。

  *怎样把这类函数和正比例函数统一看待？它们的图像长什么样？和正比例的那条过原点的直线有什么关系？*
]

#history-note[
  “一次函数”这个名字来自公式 $y = k x + b$ 里 $x$ 的最高次数是 $1$。中世纪用几何方法处理的比例问题在文艺复兴后被逐步代数化，直到 Descartes 把它和坐标平面挂钩，“一次函数 ↔ 直线”才真正定型——并成为初等代数与几何之间最稳固的一座桥。
]

#discovery[
  *回到出租车的例子*

  $y = 2 x + 10$。列表看看：

  #align(center, table(
    columns: 7,
    align: center,
    stroke: 0.4pt,
    [$x$（公里）], [$0$], [$1$], [$2$], [$3$], [$4$], [$5$],
    [$y$（元）], [$10$], [$12$], [$14$], [$16$], [$18$], [$20$],
  ))

  两件事值得注意：

  + $x = 0$ 时 $y = 10$——这个 $10$ 就是*起步价*，对应公式里的*常数项*。
  + $x$ 每加 $1$，$y$ 加 $2$——这个 $2$ 就是*每公里收费*，对应 $x$ 的*系数*。

  如果把 $y = 2 x + 10$ 和正比例 $y = 2 x$ 放在同一张图上：

  #figure(
    cetz.canvas(length: 0.45cm, {
      import cetz.draw: *
      line((-1, 0), (7, 0), stroke: 0.4pt, mark: (end: ">"))
      line((0, -2), (0, 7), stroke: 0.4pt, mark: (end: ">"))
      content((7.2, 0), $x$, anchor: "west")
      content((0, 7.3), $y$, anchor: "south")
      // y = 2x (dashed blue)
      line((-0.5, -1), (3, 6), stroke: (paint: rgb("#1976D2"), thickness: 0.7pt, dash: "dashed"))
      content((2.8, 5.5), text(7pt)[$y = 2 x$])
      // y = 2x + 2 (solid red)  — use b=2 so fits frame (illustration)
      line((-0.5, 1), (3, 8), stroke: 0.8pt + rgb("#B71C1C"))
      content((3, 8.0), text(7pt)[$y = 2 x + b$])
      // arrow showing translation
      line((2, 4), (2, 6), stroke: (paint: rgb("#2E7D32"), thickness: 0.5pt), mark: (end: ">"))
      content((2.4, 5), text(7pt)[上移 $b$])
      circle((0, 2), radius: 0.12, fill: rgb("#B71C1C"))
      content((-0.3, 2), text(7pt)[$(0, b)$], anchor: "east")
      circle((0, 0), radius: 0.12, fill: rgb("#1976D2"))
    }),
    caption: [$y = 2 x + b$ 的图像 $=$ 把 $y = 2 x$ 的图像*整条*沿 $y$ 轴方向上移 $b$。交 $y$ 轴于 $(0, b)$。]
  )

  整条直线被原封不动地*上移* $10$ 格。在上移后的图像里，原来经过原点的那条直线 $y = 2 x$ 被抬到经过 $(0, 10)$——*常数项就是与 $y$ 轴的交点纵坐标*。

  *一次函数*

  形如

  $ y = k x + b quad (k != 0) $

  的函数叫*一次函数*。$k$ 叫一次项系数（也叫*斜率*，下一节专门讲）；$b$ 叫*截距*（$y$-intercept），是函数图像与 $y$ 轴交点的纵坐标。

  - $b = 0$ 时，$y = k x$ 是正比例——正比例是一次函数的特殊情形。
  - $b != 0$ 时，图像和正比例图像*平行*（同斜率）但不过原点；差别是*整体上 / 下移 $b$*。
  - "$k != 0$"仍然保留，否则退化为水平常值函数 $y = b$。

  *画图的三点速写*

  + 把 $x = 0$ 代入 $=>$ 得 $(0, b)$，标上 $y$ 轴交点。
  + 把 $y = 0$ 代入 $=>$ 得 $x = -b / k$，标上 $x$ 轴交点。
  + 连直线，延长。

  *从表看一次函数*


  #figure(
    cetz.canvas(length: 0.4cm, {
      import cetz.draw: *
      line((-5, 0), (5, 0), stroke: 0.4pt, mark: (end: ">"))
      line((0, -5), (0, 5), stroke: 0.4pt, mark: (end: ">"))
      content((5.2, 0), $x$, anchor: "west")
      content((0, 5.3), $y$, anchor: "south")
      // y = 2x - 3 (red)
      line((-1, -5), (4, 5), stroke: 0.5pt + rgb("#E53935"))
      content((3.5, 4.5), text(6pt)[$b = -3$], anchor: "west")
      // y = 2x (blue, dashed)
      line((-2.5, -5), (2.5, 5), stroke: (paint: rgb("#1976D2"), thickness: 0.5pt, dash: "dashed"))
      content((2, 4.5), text(6pt)[$b = 0$], anchor: "west")
      // y = 2x + 3 (green)
      line((-4, -5), (1, 5), stroke: 0.5pt + rgb("#2E7D32"))
      content((0.5, 4.5), text(6pt)[$b = 3$], anchor: "west")
      // y-intercept dots
      circle((0, -3), radius: 0.12, fill: rgb("#E53935"))
      circle((0, 0), radius: 0.12, fill: rgb("#1976D2"))
      circle((0, 3), radius: 0.12, fill: rgb("#2E7D32"))
    }),
    caption: [同一斜率 $k = 2$，不同截距 $b = -3, 0, 3$：三条*平行*直线。$b$ 决定整条直线上下平移多少。]
  )

  在 $x$ 每加 $1$ 的表里，一次函数的*相邻输出差*是常数 $k$（每次加 $k$）。这是判别“数据是否由一次函数产生”的快捷方式。
]

#tryit[
  + 一次函数 $y = -3 x + 6$ 的截距是什么？它与 $x$ 轴、$y$ 轴分别交在哪？画出图像。
  + 一张表：
    #align(center, table(
      columns: 5,
      align: center,
      stroke: 0.4pt,
      [$x$], [$0$], [$1$], [$2$], [$3$],
      [$y$], [$4$], [$6$], [$8$], [$10$],
    ))
    数据像一次函数吗？若是，写出 $y = k x + b$。
]

#blueprint[
  *工具：一次函数 $y = k x + b$*

  *定义*：形如 $y = k x + b$（$k != 0$）的函数叫一次函数。

  - $k$：一次项系数（与 $y$ 轴刚才那张图上直线“陡度”相关，下一节把这件事严格化为“斜率”）。
  - $b$：*截距*。图像与 $y$ 轴交点的*纵坐标*。

  *图像*：一条直线。

  - 与 $y$ 轴交于 $(0, b)$。
  - 与 $x$ 轴交于 $(-b / k, 0)$（仅当 $k != 0$ 时存在且唯一）。

  *与正比例的关系*：把正比例 $y = k x$ 图像*整条*沿 $y$ 轴*上移 $b$* 就得到 $y = k x + b$。两条直线平行。

  *识别法（任一成立即可）*

  + 公式能写成 $y = k x + b$（$k != 0$），没有 $x^2$、$1 / x$、$sqrt(x)$ 等。
  + 在 $x$ 等差递增的表中，$y$ 相邻差是*同一个非零常数*。
  + 图像是一条*不与 $x$ 轴平行*的直线。

  *综合例*：函数 $y = -2 x + 8$。

  - 截距：$b = 8 =>$ 图像交 $y$ 轴于 $(0, 8)$。
  - 与 $x$ 轴交点：令 $y = 0 => -2 x + 8 = 0 => x = 4$，交点 $(4, 0)$。
  - 画图：标 $(0, 8)$ 与 $(4, 0)$ 两点连直线。
  - 识别：是一次函数（$k = -2 != 0$）。

  #figure(
    cetz.canvas(length: 0.38cm, {
      import cetz.draw: *
      line((-1, 0), (7, 0), stroke: 0.4pt, mark: (end: ">"))
      line((0, -2), (0, 10), stroke: 0.4pt, mark: (end: ">"))
      content((7.2, 0), $x$, anchor: "west")
      content((0, 10.3), $y$, anchor: "south")
      // y = -2x + 8: from (0,8) to (4,0)
      line((-0.5, 9), (5, -2), stroke: 0.8pt + rgb("#1976D2"))
      // intercept labels
      circle((0, 8), radius: 0.15, fill: rgb("#B71C1C"))
      content((-0.5, 8), text(7pt)[$(0, 8)$], anchor: "east")
      circle((4, 0), radius: 0.15, fill: rgb("#B71C1C"))
      content((4, -0.6), text(7pt)[$(4, 0)$])
      content((2.5, 6), text(7pt)[$y = -2 x + 8$], anchor: "west")
      // axis ticks
      for i in (2, 4) {
        content((i, -0.4), text(6pt)[#i])
      }
      for j in (4, 8) {
        content((-0.4, j), text(6pt)[#j])
      }
    }),
    caption: [$y = -2 x + 8$：交 $y$ 轴于 $(0, 8)$、交 $x$ 轴于 $(4, 0)$。标两点、连直线。]
  )

]

#pitfall[
  *高频错误*

  ✗ 把 $y = b$（如 $y = 5$）叫一次函数
  → ✓ $y = 5$ 时 $k = 0$，没有“随 $x$ 变化”的动作，退化为*常值函数*，本章约定不算一次函数。

  ✗ 截距指 $x$ 轴交点的横坐标
  → ✓ 一般*截距*默认指 $y$ 轴交点的纵坐标（$y$-截距）。需要特别提 $x$ 轴交点时要写“$x$ 轴截距”。

  ✗ 把 $y = k x + b$ 画图时忘了 $b$ 的正负方向
  → ✓ $b > 0$ 上移、$b < 0$ 下移。$y = 2 x - 3$ 比 $y = 2 x$ 整体*下*移 $3$。

  ✗ 拿到表 $x = 1, 2, 4, 7$ 算差就判是不是一次
  → ✓ 看“相邻差”必须要求 $x$ 是*等差*的。如果 $x$ 步长不同，要改看*变化率* $Delta y / Delta x$（下一节）。

  ✗ 把 $y = 3 x$ 说“不是一次函数”因为没有常数项
  → ✓ $y = 3 x$ 是 $y = k x + b$ 中 $b = 0$ 的特殊情形，仍是一次函数（只是同时也叫正比例）。
]

#mastery[
  *基础*

  + 指出下列函数的 $k$、$b$，以及与两坐标轴的交点。
    - (a) $y = x + 4$
    - (b) $y = -2 x + 6$
    - (c) $y = 5 x$
    - (d) $y = -3 x - 9$
  + 画出 $y = -x + 3$ 的图像（标出两坐标轴交点）。
  + 一次函数图像过 $(0, -2)$，且 $k = 3$。写出解析式。

  *应用*

  + 某停车场收费：起步 $5$ 元，超过 $1$ 小时后每小时 $3$ 元。设停 $x$（$x >= 1$）小时收费 $y$ 元。写出 $y$ 与 $x$ 的关系——它是一次函数吗？停 $4$ 小时多少钱？
  + 小明用手机流量套餐：月费 $20$ 元含 $1$ GB，超出部分每 $1$ GB 收 $10$ 元。设超出流量 $x$ GB（$x >= 0$），月账单 $y$ 元。写出 $y = k x + b$。当 $y = 80$ 时，$x = ?$
  + 一次函数 $y = 2 x + c$ 图像过点 $(1, 5)$。求 $c$；再求图像与 $x$ 轴的交点。

  *挑战 ☞ 选做*

  + 下表
    #align(center, table(
      columns: 5,
      align: center,
      stroke: 0.4pt,
      [$x$], [$0$], [$2$], [$4$], [$6$],
      [$y$], [$7$], [$3$], [$-1$], [$-5$],
    ))
    是否符合一次函数？若是，写出 $y = k x + b$。
  + 两条一次函数图像：$y = 2 x + 1$ 与 $y = 2 x - 4$。它们有没有交点？为什么？画出图像说明。
  + 两条一次函数 $y = k_1 x + b_1$ 与 $y = k_2 x + b_2$ 交于点 $(3, 7)$。若 $k_1 = 1, b_2 = 1$，求 $b_1$ 与 $k_2$。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + (a) $k = 1, b = 4$；$y$ 轴 $(0, 4)$，$x$ 轴 $(-4, 0)$。
    (b) $k = -2, b = 6$；$y$ 轴 $(0, 6)$，$x$ 轴 $(3, 0)$。
    (c) $k = 5, b = 0$；$y$ 轴 $(0, 0)$，$x$ 轴 $(0, 0)$——两交点重合于原点（这是正比例）。
    (d) $k = -3, b = -9$；$y$ 轴 $(0, -9)$，$x$ 轴 $(-3, 0)$。（一次函数定义：读 $k$、$b$；$y$ 轴截距 $b$，$x$ 轴交点 $x = -b / k$）
  + 图像过 $(0, 3)$ 与 $(3, 0)$，斜向右下。（标两坐标轴交点连直线）
  + $y = 3 x - 2$。（$y$ 轴截距 $b = -2$，由已知 $k$ 直接写解析式）

  *应用*
  + $y = 5 + 3 (x - 1) = 3 x + 2$。是一次函数（$k = 3, b = 2$）。$x = 4$ 时 $y = 14$ 元。（列出费用公式，化简为 $y = k x + b$，再代入求值）
  + $y = 20 + 10 x$。当 $y = 80$：$10 x = 60 => x = 6$ GB 超量。（写出一次函数公式，再反解 $x$）
  + 代 $(1, 5)$：$5 = 2 + c => c = 3$，$y = 2 x + 3$。$x$ 轴交点 $y = 0 => x = -3 / 2$，即 $(-1.5, 0)$。（代已知点求 $c$，再令 $y = 0$ 求 $x$ 轴交点）

  *挑战 ☞ 选做*
  + $x$ 步长为 $2$，$y$ 相邻差均 $= -4 =>$ 每步 $x$ 加 $2$、$y$ 减 $4$，折算成每加 $1$ 减 $2$，$k = -2$；$b = 7$（从 $x = 0$ 读）。$y = -2 x + 7$。（先算 $k = Delta y / Delta x$，再从 $x = 0$ 读 $b$）
  + 同 $k$、不同 $b =>$ 两条直线*平行*、*永不相交*。（同斜率不同截距 $=>$ 平行无交点）
  + 甲：$7 = 1 times 3 + b_1 => b_1 = 4$。乙：$7 = k_2 times 3 + 1 => k_2 = 2$。（分别代入交点坐标 $(3, 7)$ 求未知参数）
]
