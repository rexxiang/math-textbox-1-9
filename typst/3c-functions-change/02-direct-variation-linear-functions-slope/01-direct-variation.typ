#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref
#import "../../lib/diagram-packages.typ": cetz

=== 正比例函数：$y = k x$ <tool:fn04-direct-variation>

#vocab[正比例函数 direct variation / 比例系数 constant of proportionality / 过原点直线 line through the origin]

#crisis[
  *比例*我们早就会（#secref("cb01-ratio-basics")、#secref("cb01-proportion")）：两袋米 $10$ 公斤、四袋米 $20$ 公斤，不管几袋，每袋永远 $5$ 公斤。这类关系在生活中最多：匀速行进的路程与时间、买多件同款商品的总价与件数、正方形的周长与边长。

  有了上一章的*函数*语言后，我们想给这一类关系一个正式的名字、一张标准的公式、一张标准的图像：

  *在所有函数里，“每加 $1$ 份输入、输出就加同一份数”这一类最简单的函数，长什么样？对应什么公式、什么图像、什么直接识别法？*
]

#history-note[
  $17$ 世纪伽利略研究自由落体时发现下落距离和*时间的平方*成比例——这和“和时间成正比”明显不同。于是“$y$ 随 $x$ 成比例”成为物理学家把一个变化剥离出来的*第一步语言*。后来牛顿力学、胡克弹性定律 $F = k x$、欧姆定律 $U = I R$ ……无数物理公式都以“正比例”作为最基础的模板。
]

#discovery[
  *从比例到函数*

  $1$ 袋米 $5$ 公斤、$2$ 袋米 $10$ 公斤、$3$ 袋米 $15$ 公斤…… 用函数的语言：以“袋数”为独立变量 $x$、以“总重”为因变量 $y$：

  $ y = 5 x. $

  这里 $5$ 是*每袋多少公斤*，也是“$x$ 每加 $1$，$y$ 加多少”。换成不同物品，这个 $5$ 会换成别的数——我们把它起名叫 *比例系数* $k$。

  *正比例函数*

  形如

  $ y = k x quad (k != 0) $

  的函数叫*正比例函数*。$k$ 叫*比例系数*。"$k != 0$"是因为 $k = 0$ 时恒有 $y = 0$，失去了比例的意义。

  *它的三张面孔*

  *表*（以 $k = 5$ 为例）：

  #align(center, table(
    columns: 7,
    align: center,
    stroke: 0.4pt,
    [$x$], [$-2$], [$-1$], [$0$], [$1$], [$2$], [$3$],
    [$y$], [$-10$], [$-5$], [$0$], [$5$], [$10$], [$15$],
  ))

  观察：$x$ 每加 $1$，$y$ 固定加 $5$——这是“加法节奏”。$y / x$ 在 $x != 0$ 时总等于 $5$（即 $k$）——这是*正比例的本质特征*。

  *图*：

  #figure(
    cetz.canvas(length: 0.45cm, {
      import cetz.draw: *
      line((-6, 0), (6, 0), stroke: 0.4pt, mark: (end: ">"))
      line((0, -4), (0, 4), stroke: 0.4pt, mark: (end: ">"))
      content((6.2, 0), $x$, anchor: "west")
      content((0, 4.3), $y$, anchor: "south")
      // y = 0.7 x
      line((-5, -3.5), (5, 3.5), stroke: 0.7pt + rgb("#1976D2"))
      content((4.5, 3.5), text(7pt)[$y = k x$, $k > 0$])
      // y = -0.5 x
      line((-5, 2.5), (5, -2.5), stroke: 0.7pt + rgb("#B71C1C"))
      content((-4.5, 2.5), text(7pt)[$y = k x$, $k < 0$])
      circle((0, 0), radius: 0.12, fill: black)
      content((0.3, -0.4), text(7pt)[$O$])
    }),
    caption: [正比例函数图像：两条*过原点*的直线。$k > 0$ 斜向右上，$k < 0$ 斜向右下。]
  )

  两个关键特征立刻可见：
  - *过原点* $(0, 0)$：代入 $x = 0$ 必得 $y = 0$。
  - *是一条直线*：从表看，输入加固定步长 $=>$ 输出加固定步长，所以图像是*直线*（严格证明放在下一节）。

  *公式 $->$ 图的速写*：只需在纸上找两个点——原点 $O = (0, 0)$ 和另一点（比如 $x = 1, y = k$），连直线即可。

  *识别“是不是正比例”*

  给一组 $(x, y)$ 数据或一个公式，如何判定它是正比例？

  + *看比值*：在数据里算 $y / x$（$x != 0$），若*所有行都等于同一个常数 $k$* $=>$ 是正比例。
  + *看公式*：化简后形如 $y = k x$ 且 $k != 0$ $=>$ 是正比例。注意 $y = k x + 3$ *不是*正比例（多出一个常数项）。
  + *看图*：是否经过原点且是直线 $=>$ 是正比例。
]

#tryit[
  + 某水管每小时注水 $3$ 立方米。以小时数为 $x$、总注水量为 $y$，写出 $y$ 作为 $x$ 的函数。它是正比例吗？比例系数是什么？
  + 下面哪些是正比例函数？
    - (a) $y = 7 x$
    - (b) $y = -2 x$
    - (c) $y = 2 x + 1$
    - (d) $y = x^2$
    - (e) $y = -x$
    - (f) $2 y = 6 x$
  + 表：
    #align(center, table(
      columns: 5,
      align: center,
      stroke: 0.4pt,
      [$x$], [$1$], [$2$], [$3$], [$4$],
      [$y$], [$3$], [$6$], [$9$], [$12$],
    ))
    是不是正比例？$k = ?$
]

#blueprint[
  *工具：正比例函数 $y = k x$*

  *定义*：形如 $y = k x$（$k != 0$）的函数叫正比例函数。$k$ 叫*比例系数*。

  *图像*：过原点的*直线*。
  - $k > 0$：从第三象限经过原点到第一象限（“向右上斜”）。
  - $k < 0$：从第二象限经过原点到第四象限（“向右下斜”）。

  *三条识别法*（满足其中任一条）

  + 公式能写成 $y = k x$（$k != 0$）。
  + 所有数据点 $(x, y)$（$x != 0$）都满足 $y / x = k$（同一个常数）。
  + 图像是过原点的直线。

  *综合例*：某弹簧受力 $F$（牛）时伸长 $L$（厘米）满足 $F = 20 L$。

  - 这是正比例吗？是，比例系数 $k = 20$。
  - $L = 2.5$ 厘米时 $F = ?$：$F = 20 times 2.5 = 50$ 牛。
  - $F = 60$ 牛时 $L = ?$：$L = 60 / 20 = 3$ 厘米。
  - 图像：过原点的直线，斜向右上。
]

#pitfall[
  *高频错误*

  ✗ 把 $y = k x + b$ 也叫正比例
  → ✓ 正比例*必须*是 $y = k x$，常数项 $b = 0$。下一节再讲 $y = k x + b$。

  ✗ 从表里看“$y$ 每次加 $5$”就认定是正比例，没检查 $x = 0$ 时 $y = 0$ 不
  → ✓ “输出加固定步长”是所有直线函数的共同特征，不够区分正比例。严格地说，还要*过原点*（或等价：所有行 $y / x$ 都等于同一个 $k$）。

  ✗ 化简前武断判断
  → ✓ $2 y = 6 x$ 化简是 $y = 3 x$，是正比例（$k = 3$）；$y - 3 x = 5$ 化简是 $y = 3 x + 5$，不是正比例。

  ✗ 以为 $k = 0$ 也算正比例
  → ✓ $k = 0$ 时 $y$ 恒 $= 0$，退化了，不再刻画“比例”关系；约定不算正比例。
]

#mastery[
  *基础*

  + 写出下面情境对应的正比例函数：
    - (a) 每升油 $8$ 元，$x$ 升共 $y$ 元。
    - (b) 某人匀速行走 $4$ 公里 / 小时，$x$ 小时走 $y$ 公里。
    - (c) 正方形边长 $x$、周长 $y$。
  + 下列函数是不是正比例？若是，给出 $k$。
    - $y = -5 x$、$y = 0.1 x$、$y = x + 2$、$y = x^2$、$3 y = 12 x$。
  + 把 $y = -4 x$ 在输入 $x = -2, -1, 0, 1, 2$ 处的输出列成表。画出它的图像（两个特征点即可）。

  *应用*

  + 某地区规定：居民用电每度 $0.6$ 元。设 $x$ 度电费 $y$ 元。写出 $y$ 作为 $x$ 的函数。如果某户两个月电费共 $180$ 元，共用了多少度？
  + 下表给出某商品件数 $x$ 和总价 $y$：
    #align(center, table(
      columns: 5,
      align: center,
      stroke: 0.4pt,
      [$x$], [$2$], [$3$], [$5$], [$7$],
      [$y$], [$14$], [$21$], [$35$], [$49$],
    ))
    是否为正比例？写出函数式。购买 $12$ 件要多少钱？
  + 一辆车以 $v$ 公里 / 小时匀速行驶，$t$ 小时走 $d$ 公里。对*固定*的 $v$，$d$ 与 $t$ 是什么关系？对*固定*的 $t$，$d$ 与 $v$ 是什么关系？

  *挑战 ☞ 选做*

  + 若 $y$ 与 $x$ 成正比例且 $x = 3$ 时 $y = -12$，求比例系数 $k$ 与 $x = -5$ 时的 $y$。
  + 两位同学分别说：
    - 甲：$y - 1 = 2 (x - 1)$ 是正比例函数。
    - 乙：$y - 1 = 2 (x - 1)$ 不是正比例函数。
    哪位对？请化简这条式子说明理由。
  + 正比例函数的图像经过点 $(2, 6)$。写出它的解析式，并求图像上横坐标为 $-3$ 的点的纵坐标。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + (a) $y = 8 x$；(b) $y = 4 x$；(c) $y = 4 x$。
  + $y = -5 x$ 是（$k = -5$）；$y = 0.1 x$ 是（$k = 0.1$）；$y = x + 2$ 不是（有常数项）；$y = x^2$ 不是（非一次）；$3 y = 12 x <=> y = 4 x$ 是（$k = 4$）。
  + 表：$(-2, 8), (-1, 4), (0, 0), (1, -4), (2, -8)$。图像是过原点、向右下斜的直线。两个特征点：$(0, 0)$、$(1, -4)$。

  *应用*
  + $y = 0.6 x$。若 $y = 180$，则 $x = 180 / 0.6 = 300$ 度。
  + 算比值：$14 / 2 = 7$、$21 / 3 = 7$、$35 / 5 = 7$、$49 / 7 = 7$，都等于 $7 =>$ *是正比例*，$y = 7 x$。购买 $12$ 件：$y = 7 times 12 = 84$ 元。
  + 固定 $v$：$d = v t$，即 $d$ 与 $t$ 成正比例，$k = v$。固定 $t$：$d = t v$，即 $d$ 与 $v$ 成正比例，$k = t$。

  *挑战 ☞ 选做*
  + $k = y / x = -12 / 3 = -4$，故 $y = -4 x$。$x = -5$ 时 $y = 20$。
  + 乙对。化简：$y - 1 = 2 x - 2 => y = 2 x - 1$。有常数项 $-1$，*不是*正比例函数（它是一次函数，下一节会处理）。
  + $k = 6 / 2 = 3$，$y = 3 x$。$x = -3$ 时 $y = -9$。
]
