#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref, answer-cut, self-check
#import "../../lib/diagram-packages.typ": cetz

=== 正比例函数：$y = k x$ <tool:fn04-direct-variation>

#vocab[正比例函数 direct variation][比例系数 constant of proportionality][过原点直线 line through the origin]

#crisis[
  到这里，你已经掌握两件强有力的工具：

  - 桥梁章 #secref("cb04-rectangular-system") 给了你*平面直角坐标系*——平面上每一个点都有唯一的 $(x, y)$ 身份证。
  - 上一节 #secref("fn01-table-as-function-view") 给了你*函数*——每个输入对应唯一一个输出。

  把两者合起来：随便给一个函数，列出"$x$、$y$"两列表，然后把每行 $(x, y)$ 当成坐标平面上的一个点画出来——这样的"图像"看上去无所不能。

  可一旦碰到真实工程，问题就来了。

  *场景*：一根水管以 $3$ 立方米 / 小时的恒定速率给 $500$ 立方米的蓄水池注水。工程师想立刻回答两个问题：

  + "$t$ 小时后蓄水池里有多少水？"——例如 $t = 47$、$t = 132.5$、$t = 1\,000$。
  + "$500$ 立方米的水池什么时候装满？"

  要按"列表 $+$ 描点"的老办法，你得先列上百行甚至上千行表，再一个个画点，才能找到 $t = 47$ 的对应高度——这显然不现实。每问一个具体时刻，就得新画一行；每改一次水管流量，整张表全部作废。

  问题的根本：你*有*函数语言（可以写 $y = 3 t$），也*有*坐标系（可以画点），但还不知道——

  *正比例关系在坐标平面上长什么形状？这个形状里能不能直接读出"任意时刻的水量"，乃至"装满的时刻"，而不必每次回去描表？*
]

#tryit[
  先动手试试，下面再读。

  某水管每小时注水 $3$ 立方米。以小时数为 $x$、总注水量为 $y$，写出 $y$ 作为 $x$ 的函数。它是正比例吗？比例系数是什么？
]

#discovery[
  *先把"水管"画到坐标系里*

  以小时数为 $x$、注水量（立方米）为 $y$，规则是 $y = 3 x$。算几个点：

  #align(center, table(
    columns: 6,
    align: center,
    stroke: 0.4pt,
    [$x$], [$1$], [$2$], [$3$], [$4$], [$5$],
    [$y$], [$3$], [$6$], [$9$], [$12$], [$15$],
  ))

  把这五个点 $(1, 3), (2, 6), (3, 9), (4, 12), (5, 15)$ 描在坐标平面上。一画就发现：它们*完美共线*，而且这条直线*经过原点*。

  *会不会是巧合？换一个比例系数试试*

  令 $y = 2 x$（每小时注 $2$ 立方米）。算点：$(1, 2), (2, 4), (3, 6), (4, 8)$ ——又是一条过原点的直线，只是没那么陡。

  再试 $y = -x$（每小时蒸发 $1$ 立方米的"反向注水"）：$(1, -1), (2, -2), (3, -3)$ ——还是一条过原点的直线，只是这次"右下斜"。

  *猜想*：所有正比例关系 $y = k x$（$k$ 是固定常数）画出来，*都*是过原点的一条直线。

  *为什么必然是直线、必然过原点？*

  - *过原点*：把 $x = 0$ 代入 $y = k x$ 必得 $y = 0$，所以原点 $(0, 0)$ *永远*在图像上。
  - *是直线*：从表看，$x$ 每加 $1$，$y$ 固定加 $k$（"加法节奏不变"），这正是直线的代数特征。等价地说，对所有 $x != 0$ 的点，比值 $y / x = k$ 恒定——这意味着每个点和原点的连线斜率都一样，所以全在同一条直线上。

  *正比例函数的标准定义*

  形如

  $ y = k x quad (k != 0) $

  的函数叫*正比例函数*；$k$ 叫*比例系数*。规定 $k != 0$ 是因为 $k = 0$ 时 $y$ 恒等于 $0$，已经不是真正的"比例"关系。

  以 $k = 5$ 为例的输入输出表：

  #align(center, table(
    columns: 7,
    align: center,
    stroke: 0.4pt,
    [$x$], [$-2$], [$-1$], [$0$], [$1$], [$2$], [$3$],
    [$y$], [$-10$], [$-5$], [$0$], [$5$], [$10$], [$15$],
  ))

  $x$ 每加 $1$，$y$ 固定加 $5$；$y / x$（$x != 0$）恒为 $5$——这就是正比例的"加法节奏"和"恒定比值"两个等价特征。

  *它的图像族*

  如图 @fig-direct-variation-pos-neg 所示。
  #figure(
    cetz.canvas(length: 0.45cm, {
      import cetz.draw: *
      line((-6, 0), (6, 0), stroke: 0.4pt, mark: (end: ">"))
      line((0, -4), (0, 4), stroke: 0.4pt, mark: (end: ">"))
      content((6.2, 0), $x$, anchor: "west")
      content((0, 4.3), $y$, anchor: "south")
      // y = 0.7 x
      line((-5, -3.5), (5, 3.5), stroke: 0.7pt + rgb("#1976D2"))
      content((4.5, 3.5), text(7pt)[$y = k x$, $k > 0$])
      // y = -0.5 x
      line((-5, 2.5), (5, -2.5), stroke: 0.7pt + rgb("#B71C1C"))
      content((-4.5, 2.5), text(7pt)[$y = k x$, $k < 0$])
      circle((0, 0), radius: 0.12, fill: black)
      content((0.3, -0.4), text(7pt)[$O$])
    }),
    caption: [正比例函数图像：两条*过原点*的直线。$k > 0$ 斜向右上，$k < 0$ 斜向右下。]
  ) <fig-direct-variation-pos-neg>

  $k$ 越大、直线越陡：

  如图 @fig-direct-variation-slopes 所示。
  #figure(
    cetz.canvas(length: 0.4cm, {
      import cetz.draw: *
      line((-6, 0), (6, 0), stroke: 0.4pt, mark: (end: ">"))
      line((0, -5), (0, 5), stroke: 0.4pt, mark: (end: ">"))
      content((6.2, 0), $x$, anchor: "west")
      content((0, 5.3), $y$, anchor: "south")
      line((-5, -2.5), (5, 2.5), stroke: 0.5pt + rgb("#90CAF9"))
      content((4.8, 2.8), text(6pt)[$k = 0.5$])
      line((-4.5, -4.5), (4.5, 4.5), stroke: 0.5pt + rgb("#42A5F5"))
      content((4, 4.5), text(6pt)[$k = 1$])
      line((-2.2, -4.4), (2.2, 4.4), stroke: 0.6pt + rgb("#1565C0"))
      content((2, 4.5), text(6pt)[$k = 2$])
      line((-4.5, 4.5), (4.5, -4.5), stroke: 0.5pt + rgb("#EF9A9A"))
      content((4.5, -4.5), text(6pt)[$k = -1$], anchor: "north")
      circle((0, 0), radius: 0.12, fill: black)
    }),
    caption: [$k$ 越大，直线越陡。$k > 0$ 在一、三象限，$k < 0$ 在二、四象限。所有正比例直线都过原点。]
  ) <fig-direct-variation-slopes>

  *公式 $->$ 图的速写*：只要找两个点——原点 $O = (0, 0)$ 和 $(1, k)$，连成直线即可。

  *回答开头的水池问题*

  $y = 3 x$ 这条直线一画完，"$x = 47$ 时 $y = ?$"和"$y = 500$ 时 $x = ?$"立刻不需要描图——直接代公式或反解一次乘除即可。

  *识别"是不是正比例"的三把钥匙*

  + *公式*：化简后能写成 $y = k x$（$k != 0$）吗？$y = 3 x$ 是；$y = 3 x + 2$ 不是（多出常数项）。
  + *数据表*：算每行的 $y / x$（$x != 0$）。所有行都等于*同一个*常数 $k$ $->$ 是正比例。
  + *图像*：是一条经过原点的直线 $->$ 是正比例。
]

#tryit[
  + 下面哪些是正比例函数？
    - (a) $y = 7 x$
    - (b) $y = -2 x$
    - (c) $y = 2 x + 1$
    - (d) $y = x^2$
    - (e) $y = -x$
    - (f) $2 y = 6 x$
]

#history-note[
  伽利略（Galileo Galilei）在 $1638$ 年出版的《两门新科学的对话》中报告了著名的斜面实验：小球沿斜面滚下时，下落距离和*时间的平方*成比例——这和"和时间成正比"明显不同，让"$y$ 随 $x$ 成比例"成为物理学家剥离变化关系的*第一步语言*。$1676$ 年胡克（Robert Hooke）发表弹簧定律 $F = k x$（力与形变成正比），这是最经典的正比例实例之一。此后欧姆定律 $U = I R$、牛顿第二定律 $F = m a$ ……无数物理公式都以正比例作为最基础的模板。
]

#blueprint[
  *工具：正比例函数 $y = k x$*

  *定义*：形如 $y = k x$（$k != 0$）的函数叫正比例函数。$k$ 叫*比例系数*。

  *图像*：过原点的*直线*。
  - $k > 0$：从第三象限经过原点到第一象限（“向右上斜”）。
  - $k < 0$：从第二象限经过原点到第四象限（“向右下斜”）。

  *三条识别法*（满足其中任一条）

  + 公式能写成 $y = k x$（$k != 0$）。
  + 所有数据点 $(x, y)$（$x != 0$）都满足 $y / x = k$（同一个常数）。
  + 图像是过原点的直线。

  *综合例*：某弹簧受力 $F$（牛）时伸长 $L$（厘米）满足 $F = 20 L$。

  - 这是正比例吗？是，比例系数 $k = 20$。
  - $L = 2.5$ 厘米时 $F = ?$：$F = 20 times 2.5 = 50$ 牛。
  - $F = 60$ 牛时 $L = ?$：$L = 60 / 20 = 3$ 厘米。
  - 图像：过原点的直线，斜向右上。

  如图 @fig-spring-direct-variation 所示。
  #figure(
    cetz.canvas(length: 0.4cm, {
      import cetz.draw: *
      line((-1, 0), (8, 0), stroke: 0.4pt, mark: (end: ">"))
      line((0, -1), (0, 8), stroke: 0.4pt, mark: (end: ">"))
      content((8.3, 0), $L$, anchor: "west")
      content((0, 8.3), $F$, anchor: "south")
      // F = 20L scaled: plot L in [0,3], F = 20L but scale F by 1/5 for display
      // Use L on x-axis, F/10 on y-axis for readability
      line((0, 0), (3, 6), stroke: 0.8pt + rgb("#1976D2"))
      // mark points
      circle((0, 0), radius: 0.1, fill: black)
      circle((1, 2), radius: 0.1, fill: rgb("#1976D2"))
      circle((2, 4), radius: 0.1, fill: rgb("#1976D2"))
      circle((2.5, 5), radius: 0.1, fill: rgb("#1976D2"))
      circle((3, 6), radius: 0.1, fill: rgb("#1976D2"))
      // axis labels
      for i in (1, 2, 3) {
        content((i, -0.5), text(6pt)[#i])
      }
      content((-0.8, 2), text(6pt)[$20$])
      content((-0.8, 4), text(6pt)[$40$])
      content((-0.8, 6), text(6pt)[$60$])
      content((2, 5), text(7pt)[$F = 20 L$], anchor: "west")
    }),
    caption: [弹簧伸长量 $L$（厘米）与受力 $F$（牛）的正比例关系：过原点直线，$k = 20$。]
  ) <fig-spring-direct-variation>

]

#self-check[
  正比例函数 $y = k x$ 必须经过哪一个点？为什么 $b = 0$ 是它和一般一次函数的关键差别？
]


#pitfall[
  *高频错误*

  ✗ 把 $y = k x + b$ 也叫正比例
  → ✓ 正比例*必须*是 $y = k x$，常数项 $b = 0$。下一节再讲 $y = k x + b$。
  式子里只要看到 $k x$ 就觉得“成比例”——但多出来的 $b$ 让“比”不再恒定。

  ✗ 从表里看“$y$ 每次加 $5$”就认定是正比例，没检查 $x = 0$ 时 $y = 0$ 不
  → ✓ “输出加固定步长”是所有直线函数的共同特征，不够区分正比例。严格地说，还要*过原点*（或等价：所有行 $y / x$ 都等于同一个 $k$）。

  ✗ 化简前武断判断
  → ✓ $2 y = 6 x$ 化简是 $y = 3 x$，是正比例（$k = 3$）；$y - 3 x = 5$ 化简是 $y = 3 x + 5$，不是正比例。

  ✗ 以为 $k = 0$ 也算正比例
  → ✓ $k = 0$ 时 $y$ 恒 $= 0$，退化了，不再刻画“比例”关系；约定不算正比例。
]

#mastery[
  *基础*

  + 写出下面情境对应的正比例函数：
    - (a) 每升油 $8$ 元，$x$ 升共 $y$ 元。
    - (b) 某人匀速行走 $4$ 公里 / 小时，$x$ 小时走 $y$ 公里。
    - (c) 正方形边长 $x$、周长 $y$。
  + 下列函数是不是正比例？若是，给出 $k$。
    - $y = -5 x$、$y = 0.1 x$、$y = x + 2$、$y = x^2$、$3 y = 12 x$。
  + 把 $y = -4 x$ 在输入 $x = -2, -1, 0, 1, 2$ 处的输出列成表。画出它的图像（两个特征点即可）。

  *应用*

  + 某地区规定：居民用电每度 $0.6$ 元。设 $x$ 度电费 $y$ 元。写出 $y$ 作为 $x$ 的函数。如果某户两个月电费共 $180$ 元，共用了多少度？
  + 下表给出某商品件数 $x$ 和总价 $y$：
    #align(center, table(
      columns: 5,
      align: center,
      stroke: 0.4pt,
      [$x$], [$2$], [$3$], [$5$], [$7$],
      [$y$], [$14$], [$21$], [$35$], [$49$],
    ))
    是否为正比例？写出函数式。购买 $12$ 件要多少钱？
  + 一辆车以 $v$ 公里 / 小时匀速行驶，$t$ 小时走 $d$ 公里。对*固定*的 $v$，$d$ 与 $t$ 是什么关系？对*固定*的 $t$，$d$ 与 $v$ 是什么关系？

  *挑战 ☞ 选做*

  + 若 $y$ 与 $x$ 成正比例且 $x = 3$ 时 $y = -12$，求比例系数 $k$ 与 $x = -5$ 时的 $y$。
  + 两位同学分别说：
    - 甲：$y - 1 = 2 (x - 1)$ 是正比例函数。
    - 乙：$y - 1 = 2 (x - 1)$ 不是正比例函数。
    哪位对？请化简这条式子说明理由。
  + 正比例函数的图像经过点 $(2, 6)$。写出它的解析式，并求图像上横坐标为 $-3$ 的点的纵坐标。
  + *[反直觉]* 判断："如果 $y$ 与 $x$ 成正比例，那么 $y$ 一定随 $x$ 的增大而增大。" 给反例并解释什么时候 $y$ 反而*减小*。
  + *[跨知识点]* $y$ 与 $x$ 成正比例，$z$ 与 $y$ 成正比例。则 $z$ 与 $x$ 是不是也成正比例？请用 $y = k_1 x$、$z = k_2 y$ 推导，最后给出 $z$ 关于 $x$ 的比例系数。再借助 #secref("tool:cb01-ratio-basics") 的"比"语言重新表述这个传递关系。
  + *[构造题]* 给两组数据 $(x_1, y_1) = (3, ?)$ 和 $(x_2, y_2) = (?, -10)$。要让两组数据落在同一条正比例函数 $y = k x$ 上（$k != 0$），并且第二组的 $x_2$ 是第一组 $x_1$ 的相反数。请给出 $y_1$ 和 $x_2$ 的具体值，并写出 $k$。

  #answer-cut[

  *基础*
  + (a) $y = 8 x$；(b) $y = 4 x$；(c) $y = 4 x$。（正比例定义：$y = k x$）
  + $y = -5 x$ 是（$k = -5$）；$y = 0.1 x$ 是（$k = 0.1$）；$y = x + 2$ 不是（有常数项）；$y = x^2$ 不是（非一次）；$3 y = 12 x <=> y = 4 x$ 是（$k = 4$）。（正比例判别：化简后是否为 $y = k x$）
  + 表：$(-2, 8), (-1, 4), (0, 0), (1, -4), (2, -8)$。图像是过原点、向右下斜的直线。两个特征点：$(0, 0)$、$(1, -4)$。（代入求值，过原点连直线）

  *应用*
  + $y = 0.6 x$。若 $y = 180$，则 $x = 180 / 0.6 = 300$ 度。（写出正比例公式 $y = k x$，再反解 $x = y / k$）
  + 算比值：$14 / 2 = 7$、$21 / 3 = 7$、$35 / 5 = 7$、$49 / 7 = 7$，都等于 $7 =>$ *是正比例*，$y = 7 x$。购买 $12$ 件：$y = 7 times 12 = 84$ 元。（$k = y / x$ 逐行验证比值相同）
  + 固定 $v$：$d = v t$，即 $d$ 与 $t$ 成正比例，$k = v$。固定 $t$：$d = t v$，即 $d$ 与 $v$ 成正比例，$k = t$。（固定一个量，另一个量与结果成正比例）

  *挑战 ☞ 选做*
  + $k = y / x = -12 / 3 = -4$，故 $y = -4 x$。$x = -5$ 时 $y = 20$。（先由 $k = y / x$ 求比例系数，再代入求值）
  + 乙对。化简：$y - 1 = 2 x - 2 => y = 2 x - 1$。有常数项 $-1$，*不是*正比例函数（它是一次函数，下一节会处理）。（先化简为 $y = k x + b$ 形式，检查 $b$ 是否为 $0$）
  + $k = 6 / 2 = 3$，$y = 3 x$。$x = -3$ 时 $y = -9$。（由已知点求 $k = y / x$，写出解析式再代入）
  + 不一定。当 $k > 0$ 时 $y$ 随 $x$ 增大而增大；当 $k < 0$ 时 $y$ 随 $x$ 增大而*减小*。例：$y = -2 x$，$x = 1$ 时 $y = -2$，$x = 2$ 时 $y = -4$，$y$ 减小。"成正比例"只是说 $y / x$ 是常数，不规定符号；$k$ 的符号决定单调方向（正比例的单调方向看 $k$）。
  + 是。$y = k_1 x$，$z = k_2 y = k_2 (k_1 x) = (k_1 k_2) x$，所以 $z$ 与 $x$ 成正比例，比例系数 $= k_1 k_2$。比的语言（呼应 #secref("tool:cb01-ratio-basics")）：$y : x = k_1 : 1$，$z : y = k_2 : 1$，把"$y$ 当中介"消去得 $z : x = k_1 k_2 : 1$（正比例的传递性 = 比的复合）。
  + 取 $x_1 = 3$，则 $x_2 = -3$；要让 $(3, y_1)$ 和 $(-3, -10)$ 在同一条 $y = k x$ 上，先由第二组求 $k$：$-10 = k times (-3)$，$k = 10/3$。再求 $y_1 = (10/3) times 3 = 10$。验证：$y_1 / x_1 = 10/3$，$y_2 / x_2 = -10/-3 = 10/3$ ✓ 一致。结果：$y_1 = 10$，$x_2 = -3$，$k = 10/3$。
  ]
]
