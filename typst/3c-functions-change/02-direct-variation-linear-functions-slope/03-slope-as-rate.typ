#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref
#import "../../lib/diagram-packages.typ": cetz

=== 斜率：把“每加 $1$ 单位 $x$，$y$ 变多少”写成数 <tool:fn06-slope-as-rate>

#vocab[斜率 slope][变化率 rate of change][水平变化 run][铅直变化 rise]

#crisis[
  上一节我们已经知道 $y = k x + b$ 里的 $k$ 控制*陡度*——两条同 $k$ 的直线平行，$k$ 越大看起来越陡。但到现在 $k$ 只是“公式里的一个系数”——它为什么叫*斜率*？怎么从图里、从表里、从两个点读出它？

  更重要：上一节的表里我们用的是“$x$ 每加 $1$，$y$ 加 $k$”——但实际题目里 $x$ 的步长常常不是 $1$：
  #align(center, table(
    columns: 4,
    align: center,
    stroke: 0.4pt,
    [$x$], [$1$], [$4$], [$9$],
    [$y$], [$3$], [$9$], [$19$],
  ))
  $x$ 从 $1$ 到 $4$ 加了 $3$，$y$ 从 $3$ 到 $9$ 加了 $6$。*每加 $1$ 单位 $x$，$y$ 加多少？* 这不再是直接读“相邻差”就行的问题。

  *如何把“$x$ 每加 $1$，$y$ 变多少”严格定义成一个数，并让这个数同时能在公式、图、表里读出来？*
]

#history-note[
  “斜率”这一概念最早来自 $17$ 世纪的几何学家，用来描述一条直线相对水平方向的*倾斜程度*——同一条直线无论在哪一段量，倾斜程度都不变。这种“局部变化率恒定”的特征，正是后来 Newton / Leibniz 发展微积分时抽出的*瞬时变化率*在直线上的平凡情形。
]

#discovery[
  *先做一次“把步长换算回 $1$”的练习*

  上面那张表里，$x: 1 -> 4$ 加了 $3$；$y: 3 -> 9$ 加了 $6$。如果我们假设“$y$ 对 $x$ 的变化是均匀的”（这是直线的定义），那$x$ 每加 $1$，$y$ 应该加 $6 / 3 = 2$。

  同样地，$x: 4 -> 9$ 加了 $5$；$y: 9 -> 19$ 加了 $10$——$x$ 每加 $1$，$y$ 加 $10 / 5 = 2$。

  两段算出的*每单位变化量*都是 $2$——这正是直线的特征：*不管你在哪一段量，每加 $1$ 单位 $x$，$y$ 变化量恒为同一个数*。

  把这个数正式命名为*斜率*，记作 $m$（或沿用 $k$，与一次函数里的 $k$ 是同一个数）。

  *斜率的两点定义*

  在直线上取两个不同的点 $P_1 (x_1, y_1)$、$P_2 (x_2, y_2)$：

  - *水平变化* $Delta x = x_2 - x_1$（run，"跑了多远"）；
  - *铅直变化* $Delta y = y_2 - y_1$（rise，"升了多高"）；
  - *斜率*

  $ m = frac(Delta y, Delta x) = frac(y_2 - y_1, x_2 - x_1). $

  由于 $P_1, P_2$ 不同且不在同一条铅垂线上（否则 $Delta x = 0$，斜率无意义）必有 $Delta x != 0$。

  *为什么这个比值和选哪两点无关*

  用*相似三角形*的语言最清楚：把直线上任两段拿出来做直角三角形（水平边 $Delta x$、铅直边 $Delta y$、斜边就是直线上的那段），它们*全是同一条直线的部分*、*锐角都相同* $=>$ 三角形两两*相似* $=>$ 对应边比 $Delta y : Delta x$ 恒相等。

  #figure(
    cetz.canvas(length: 0.5cm, {
      import cetz.draw: *
      line((-1, 0), (8, 0), stroke: 0.4pt, mark: (end: ">"))
      line((0, -1), (0, 7), stroke: 0.4pt, mark: (end: ">"))
      content((8.2, 0), $x$, anchor: "west")
      content((0, 7.3), $y$, anchor: "south")
      // line y = 0.7 x + 0.5 through say (0, 0.5), (2, 1.9), (6, 4.7)
      line((-0.5, 0.15), (7, 5.4), stroke: 0.8pt + rgb("#1976D2"))
      // two small right triangles along the line
      circle((1, 1.2), radius: 0.12, fill: black)
      content((0.7, 1.2), text(7pt)[$P_1$], anchor: "east")
      circle((3, 2.6), radius: 0.12, fill: black)
      content((3.3, 2.6), text(7pt)[$P_2$], anchor: "west")
      circle((5.5, 4.35), radius: 0.12, fill: black)
      content((5.8, 4.35), text(7pt)[$P_3$], anchor: "west")
      // small right triangle from P1 to P2
      line((1, 1.2), (3, 1.2), stroke: 0.5pt + rgb("#2E7D32"))
      line((3, 1.2), (3, 2.6), stroke: 0.5pt + rgb("#2E7D32"))
      content((2, 0.85), text(7pt)[$Delta x$])
      content((3.35, 1.9), text(7pt)[$Delta y$])
      // bigger right triangle from P2 to P3
      line((3, 2.6), (5.5, 2.6), stroke: 0.5pt + rgb("#B71C1C"))
      line((5.5, 2.6), (5.5, 4.35), stroke: 0.5pt + rgb("#B71C1C"))
      content((4.2, 2.3), text(7pt)[$Delta x'$])
      content((5.85, 3.45), text(7pt)[$Delta y'$])
    }),
    caption: [同一条直线上的任意两段各作直角三角形。它们相似（对应锐角相同），故 $Delta y slash Delta x = Delta y' slash Delta x' = m$ 恒定。]
  )

  *斜率 $=$ 每增加 $1$ 单位 $x$，$y$ 的变化量*：让 $Delta x = 1$，就有 $Delta y = m$。所以斜率的一句话直觉是：*“$x$ 每加 $1$，$y$ 加 $m$”*。

  *斜率的三种读法（图 / 表 / 式）*

  + *从公式*：$y = k x + b$ 里 $k$ 就是斜率。
  + *从表*：$m = (y_2 - y_1) / (x_2 - x_1)$，任意选两行。
  + *从图*：找两个整齐的格点，数水平和铅直位移，$m = "上升格数" / "右移格数"$（上升为正、下降为负）。

  *斜率的几何正负*

  - $m > 0$：$x$ 增 $=>$ $y$ 增（图像右上斜）。
  - $m < 0$：$x$ 增 $=>$ $y$ 减（图像右下斜）。
  - $m = 0$：$y$ 恒定（水平线）。
  - 一条铅垂线不是函数图像（#secref("fn02-graph-as-function-view")），所以斜率不定义“铅垂线的斜率”——换句话说斜率要求 $Delta x != 0$。

  #figure(
    cetz.canvas(length: 0.4cm, {
      import cetz.draw: *
      // Left panel: positive slope
      line((-8, 0), (-2, 0), stroke: 0.4pt, mark: (end: ">"))
      line((-5, -3), (-5, 4), stroke: 0.4pt, mark: (end: ">"))
      line((-7.5, -2.5), (-2.5, 2.5), stroke: 0.7pt + rgb("#1976D2"))
      content((-5, 4.5), text(7pt)[$m > 0$])
      // rise/run triangle
      line((-7, -2), (-4, -2), stroke: 0.4pt + rgb("#2E7D32"))
      line((-4, -2), (-4, 1), stroke: 0.4pt + rgb("#2E7D32"))
      content((-5.5, -2.5), text(6pt)[run])
      content((-3.5, -0.5), text(6pt)[rise])
      // Right panel: negative slope
      line((2, 0), (8, 0), stroke: 0.4pt, mark: (end: ">"))
      line((5, -3), (5, 4), stroke: 0.4pt, mark: (end: ">"))
      line((2.5, 2.5), (7.5, -2.5), stroke: 0.7pt + rgb("#B71C1C"))
      content((5, 4.5), text(7pt)[$m < 0$])
      // rise/run triangle (rise is negative = drop)
      line((3, 2), (6, 2), stroke: 0.4pt + rgb("#2E7D32"))
      line((6, 2), (6, -1), stroke: 0.4pt + rgb("#2E7D32"))
      content((4.5, 2.5), text(6pt)[run])
      content((6.5, 0.5), text(6pt)[drop])
    }),
    caption: [$m > 0$ 时直线右上斜（rise 为正）；$m < 0$ 时直线右下斜（rise 为负，即 drop）。]
  )

]

#tryit[
  + 直线过 $(1, 3)$、$(4, 12)$。求斜率。
  + 一次函数 $y = -2 x + 5$ 的斜率是多少？$x$ 加 $3$ 时 $y$ 变化多少？
]

#blueprint[
  *工具：斜率 = 直线上“每加 $1$ 单位 $x$ 时 $y$ 的变化量”*

  *定义*：直线上取两不同点 $P_1(x_1, y_1)$、$P_2(x_2, y_2)$（$x_1 != x_2$）。斜率

  $ m = frac(y_2 - y_1, x_2 - x_1) = frac(Delta y, Delta x). $

  *性质*：直线上的斜率*与选哪两点无关*（由相似三角形）。

  *三种来源*

  - 公式 $y = k x + b$：$m = k$。
  - 表：任挑两行 $(x_1, y_1), (x_2, y_2)$，计算 $(y_2 - y_1) / (x_2 - x_1)$。
  - 图：在格点上数水平、铅直位移，取比值（注意符号）。

  *意义*：
  - 数值上，斜率是*每加 $1$ 单位 $x$，$y$ 的变化量*。
  - 符号上，$m$ 决定方向（正 $->$ 右上，负 $->$ 右下，$0 ->$ 水平）。
  - 绝对值大小决定陡度（$|m|$ 越大越陡）。

  #figure(
    cetz.canvas(length: 0.4cm, {
      import cetz.draw: *
      line((-6, 0), (6, 0), stroke: 0.4pt, mark: (end: ">"))
      line((0, -5), (0, 5), stroke: 0.4pt, mark: (end: ">"))
      content((6.2, 0), $x$, anchor: "west")
      content((0, 5.3), $y$, anchor: "south")
      // m = 0.5 (gentle)
      line((-5, -2.5), (5, 2.5), stroke: 0.5pt + rgb("#90CAF9"))
      content((5, 3), text(6pt)[$m = 0.5$], anchor: "west")
      // m = 1
      line((-4.5, -4.5), (4.5, 4.5), stroke: 0.5pt + rgb("#42A5F5"))
      content((4, 4.5), text(6pt)[$m = 1$], anchor: "west")
      // m = 3 (steep)
      line((-1.5, -4.5), (1.5, 4.5), stroke: 0.6pt + rgb("#0D47A1"))
      content((1.5, 4.5), text(6pt)[$m = 3$], anchor: "west")
      // m = -2
      line((-2.2, 4.4), (2.2, -4.4), stroke: 0.5pt + rgb("#E53935"))
      content((-2, 4.5), text(6pt)[$m = -2$], anchor: "east")
      circle((0, 0), radius: 0.1, fill: black)
    }),
    caption: [$|m|$ 越大直线越陡：$|m| = 0.5$ 最平缓，$|m| = 3$ 最陡。$m$ 的正负决定方向，$|m|$ 决定陡度。]
  )


  *综合例*：直线过 $(2, 1)$ 和 $(6, 9)$，求斜率；写出解析式；$x = 10$ 时 $y = ?$

  *解*：$m = (9 - 1) / (6 - 2) = 8 / 4 = 2$。用点斜“还原” $b$：$y = 2 x + b$，代入 $(2, 1)$ 得 $1 = 4 + b => b = -3$，故 $y = 2 x - 3$。$x = 10$：$y = 17$。
]

#pitfall[
  *高频错误*

  ✗ 把 $Delta y$ 和 $Delta x$ 上下写反，成了 $Delta x / Delta y$
  → ✓ 斜率恒是*纵变化 / 横变化* $= Delta y / Delta x$。一句话记：“上升除以右移”。

  ✗ 两点顺序前后调换一次、差号乱了
  → ✓ 只要*分子、分母用同一顺序*（都“减第一点”或都“减第二点”），结果一样：$(y_2 - y_1) / (x_2 - x_1) = (y_1 - y_2) / (x_1 - x_2)$。

  ✗ 在 $x$ 步长不一致的表里直接读相邻 $y$ 差当斜率
  → ✓ $x$ 步长不 $= 1$ 时要除掉 $Delta x$；或者用任意两行的公式。

  ✗ 说水平线 $y = 5$ “斜率无定义”
  → ✓ 水平线斜率 $= 0$（$Delta y = 0$、$Delta x != 0$）。*铅垂线*才是斜率无定义（$Delta x = 0$、分母为 $0$；且铅垂线不是函数图像）。

  ✗ 斜率“越大越陡”理解成只看正号大
  → ✓ 陡度看 $|m|$：$m = -7$ 比 $m = 3$ 更陡（绝对值更大）。
]

#mastery[
  *基础*

  + 写出下列各条直线的斜率。
    - (a) $y = 4 x - 1$；(b) $y = -3 x + 7$；(c) $y = 6$；(d) $y = 0.5 x$。
  + 下列两点决定的直线，斜率是多少？
    - (a) $(0, 0), (3, 9)$；(b) $(-1, 2), (3, -6)$；(c) $(2, 5), (7, 5)$。
  + 某直线斜率为 $-4$，$x = 1$ 时 $y = 10$。$x$ 加 $2$ 时 $y$ 变多少？$x = 4$ 时 $y = ?$

  *应用*

  + 一辆车 $2$ 小时走 $120$ 公里，匀速前进。以时间 $t$（小时）为横、里程 $d$（公里）为纵，这条 $d = v t$ 直线的斜率是什么？现实意义是什么？
  + 上一节出租车情境 $y = 2 x + 10$，斜率 $= 2$：这个 $2$ 在现实里代表什么？
  + 给定两点 $A(-2, -5)$ 与 $B(4, 7)$。求：
    - 过 $A, B$ 直线的斜率；
    - 过 $A, B$ 直线的解析式 $y = k x + b$；
    - 这条直线与 $x$ 轴、$y$ 轴的交点。

  *挑战 ☞ 选做*

  + 下表
    #align(center, table(
      columns: 5,
      align: center,
      stroke: 0.4pt,
      [$x$], [$-3$], [$1$], [$4$], [$10$],
      [$y$], [$11$], [$3$], [$-3$], [$-15$],
    ))
    是否符合一次函数？若是，求 $y = k x + b$，并求 $x = 100$ 时的 $y$。
  + 一位同学算斜率时写：“两点 $(1, 2)$、$(3, 8)$，斜率 $= (3 - 1) / (8 - 2) = 1 / 3$。” 错在哪？写出正确斜率。
  + 一条直线经过原点与点 $(5, 10)$。求斜率，并说明它与正比例函数 $y = 2 x$ 的关系。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + (a) $4$；(b) $-3$；(c) $0$；(d) $0.5$。（斜率 $=$ 公式中 $x$ 的系数 $k$）
  + (a) $(9 - 0) / (3 - 0) = 3$；(b) $(-6 - 2) / (3 - (-1)) = -8 / 4 = -2$；(c) $(5 - 5) / (7 - 2) = 0$。（斜率公式 $m = Delta y / Delta x$）
  + $x$ 加 $2$ $=>$ $y$ 加 $-4 times 2 = -8$。$x = 4$ 时 $y = 10 + (-4)(4 - 1) = 10 - 12 = -2$。（斜率定义：$Delta y = m dot Delta x$）

  *应用*
  + 斜率 $= 120 / 2 = 60$ 公里 / 小时，就是*速度*（每加 $1$ 小时多走 $60$ 公里）。（$m = Delta d / Delta t$，即速度的定义）
  + 每加 $1$ 公里里程，车费加 $2$ 元，即*每公里费率*。（斜率 $=$ 公式中 $k$，现实意义为单价）
  + $m = (7 - (-5)) / (4 - (-2)) = 12 / 6 = 2$。代 $(4, 7)$：$7 = 8 + b => b = -1$，$y = 2 x - 1$。$y = 0 => x = 1 / 2$，$x$ 轴交 $(0.5, 0)$；$y$ 轴交 $(0, -1)$。（先求斜率，再代点求 $b$——两点求解析式）

  *挑战 ☞ 选做*
  + 取 $(-3, 11)$ 与 $(1, 3)$：$m = (3 - 11) / (1 - (-3)) = -8 / 4 = -2$。检查 $(4, -3)$：$11 + (-2) times 7 = -3$ ✓。$b$ 由 $(1, 3)$：$3 = -2 + b => b = 5$。$y = -2 x + 5$。$x = 100$：$y = -200 + 5 = -195$。（任取两点求 $m$，再验证其余点，最后代点求 $b$）
  + 写反了。斜率是 $Delta y slash Delta x = (8 - 2) / (3 - 1) = 3$，不是 $1 / 3$。（斜率恒为纵变化除以横变化，不能写反）
  + $m = (10 - 0) / (5 - 0) = 2$。因过原点且 $k = 2$ $=>$ 这条直线就是 $y = 2 x$，所以和正比例函数 $y = 2 x$ *完全重合*。（由斜率和过原点判定正比例 $y = k x$）
]
