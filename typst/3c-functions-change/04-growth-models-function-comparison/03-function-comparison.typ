#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref

=== 函数家族的长期较量 <tool:fn12-function-comparison>

#vocab[长期行为 long-run behavior][超越 overtaking][量级 order of magnitude]

#crisis[
  把几种函数放在同一个坐标系里，短期 / 中期 / 长期的“名次”经常*重新洗牌*：

  - 在 $x = 1$ 附近，$y = 100 x$、$y = x^2$、$y = 2^x$、$y = 10000 / x$ 这四条相差悬殊——线性最大；
  - 在 $x = 10$ 附近，$x^2$ 已经超过 $100 x$，$2^x approx 1024$ 冲到最前；
  - 在 $x = 100$ 附近，$2^x$ 是天文数字（$approx 10^(30)$），$x^2 = 10^4$，$100 x = 10^4$——线性和二次打平；
  - 反比例 $10000 / x$ 在 $x = 1$ 时 $= 10000$，很大；$x = 100$ 时只有 $100$，早被踩在地上。

  *函数族之间到底存不存在“稳定的大小关系”？*如果存在，那规律是怎样的？这是这一节要回答的。
]

#discovery[
  *四类函数族的长期“级别”*

  为了眼见为实，取 $x = 1, 2, 5, 10, 20, 50$ 放同一张表（结果四舍五入到两位有效位）：

  #align(center, table(
    columns: 7,
    align: center,
    stroke: 0.4pt,
    [*$x$*], [$1$], [$2$], [$5$], [$10$], [$20$], [$50$],
    [*$100 / x$（反比例）*], [$100$], [$50$], [$20$], [$10$], [$5$], [$2$],
    [*$10 x$（线性）*], [$10$], [$20$], [$50$], [$100$], [$200$], [$500$],
    [*$x^2$（二次）*], [$1$], [$4$], [$25$], [$100$], [$400$], [$2500$],
    [*$2^x$（乘法）*], [$2$], [$4$], [$32$], [$approx 10^3$], [$approx 10^6$], [$approx 10^(15)$],
  ))

  *观察*

  - 反比例随 $x$ 变大而*变小*——它在“正无穷方向”上*排倒数第一*。
  - $x = 10$：线性 $=$ 二次 $= 100$；$x > 10$ 以后二次*超过*线性。
  - $x = 5$：$2^x = 32 > x^2 = 25$，乘法增长已经追平二次；之后越拉越大。
  - $x = 20$：$2^x approx 10^6$，$x^2 = 400$——乘法增长对二次领先 $2500$ 倍以上。

  *长期排序（$x$ 足够大时）*

  对正的 $x$：
  $ underbrace("反比例"space y = c / x, "趋于 0") ≪ underbrace("常数", "不动") ≪ underbrace("线性"space 10 x, "随 $x$ 增长") ≪ underbrace("二次"space x^2, "增长更快") ≪ underbrace("乘法"space 2^x, "增长最快") $

  这里 “$≪$” 读作“远远小于”，意思是：存在某个*阈值*，$x$ 超过它以后左边始终比右边小（而且差距越来越大）。

  *关键点*：系数大小*在长期里不起决定作用*，起决定作用的是*族*。例子：$y = 1000000 x$ 是个*系数很大的*线性函数，但它最终被 $y = x^2$ 超过——$x > 10^6$ 时 $x^2 > 10^6 x$。常数的系数无论多大，都赢不过“族的等级”。

  *图像直觉*

  把四条在同一坐标系里画（合适缩放后）：

  - 反比例：向右下“贴近 $x$ 轴”（$y arrow 0$）；
  - 线性：稳步向右上走，斜率是 $k$；
  - 二次：先平缓、再越来越陡向右上翘起；
  - 乘法增长（$r > 1$）：几乎贴着 $x$ 轴一段后*突然冲天*——就是前一节见到的那种“先贴地后翘起”。

  在任何足够远的区间内，*“翘得最厉害的”永远是乘法增长*。
]

#side-hack[
  *“量级”概念*

  随便两个正数 $a, b$，若 $a / b$ 在 $[1 / 10, 10)$ 之间，叫“同一*量级*”；相差 $10$ 倍叫“差一个量级”。当 $x = 20$ 时 $2^x approx 10^6$ 与 $x^2 = 400$ 之间相差*约 $4$ 个量级*——这种差距在实际问题中基本意味着*不在同一回合里*。
]

#tryit[
  + 找出 $y = x^2$ 和 $y = 3 x + 10$ 在 $x > 0$ 时的*首个整数交点及其后*的“超越”$x$ 值（从 $x = 1, 2, 3, ...$ 找）。
  + 把 $y = 100 x, y = x^2, y = 2^x$ 在 $x = 1, 2, ..., 10$ 列一张表，指出 “$x^2$ 超过 $100 x$” 以及 “$2^x$ 超过 $x^2$” 各发生在 $x = ?$。
  + 反比例 $y = 100 / x$ 在 $x$ 分别为 $1, 10, 100, 1000$ 时 $y$ 各是多少？$x$ 越大 $y$ 怎么变？
]

#blueprint[
  *工具：函数族长期比较*

  对正的 $x$，当 $x$ 足够大时有*族的稳定排序*（系数不影响最终排序）：
  $ "反比例" space y = c / x quad ≪ quad "线性" space k x + b quad ≪ quad "二次" space a x^2 + ... quad ≪ quad "乘法增长" space y(0) r^x quad (r > 1) $

  “$≪$”的含义：*存在阈值 $x_0$*，$x > x_0$ 以后左边一直小于右边。

  *使用时的三个提示*：

  + 这个排序对*长期*才一定成立，*短期*可能被系数翻盘。
  + “阈值” $x_0$ 依赖两个式子的系数——题目要求时通常要“列表找过渡点”。
  + 反比例随 $x$ 变大在趋于 $0$，比一切正常数更小，但比 $0$ 大（属于“正无穷小量”）。

  *与已有族的连接*：本表来自 #secref("fn05-linear-function")、#secref("fn07-inverse-proportion")、#secref("fn08-quadratic-function")、#secref("fn09-comparing-function-families") 以及 #secref("fn10-additive-vs-multiplicative-growth") 的综合。
]

#pitfall[
  ✗ 直接把“系数最大者在任意 $x$ 下最大”当真理
  → ✓ *长期*才成立族等级；短期（尤其 $x in [0, 10]$）完全可能翻转。看 $y = 100 x$ 与 $y = x^2$：$x = 10$ 时才刚打平。

  ✗ 把“乘法增长胜二次”理解成“所有 $x$ 下”
  → ✓ $y = x^2$ 与 $y = 2^x$ 在 $x = 4$ 时 $= 16$ 打平；$x = 3$ 时 $x^2 = 9 > 2^3 = 8$；$x >= 5$ 才稳定乘法领先。

  ✗ 把反比例“跑向 $0$”误写成“等于 $0$”
  → ✓ $y = c / x$ 在正 $x$ 方向永远 $> 0$，只是*任意小*（趋于 $0$）。写“$y arrow 0$”不是“$y = 0$”。

  ✗ 在 $x <= 0$ 时直接套正 $x$ 的排序
  → ✓ 本节结论限定在 $x > 0$ 的“正无穷方向”。$x < 0$ 的行为要各自分析（尤其涉及偶次幂的对称性、反比例在 $x < 0$ 侧也有双曲线的另一支）。
]

#mastery[
  *基础*

  + 填空：“当 $x$ 很大时，线性 $y = k x + b$ 总是远远小于 \_\_\_\_ 函数族的一切成员。”
  + $y_1 = 1000 x$，$y_2 = x^2$。找一个*整数* $x$ 使得 $y_2 > y_1$。
  + $y_1 = x^2$，$y_2 = 2^x$。列 $x = 1, 2, ..., 10$ 的表，找出 $y_2 > y_1$ 稳定成立*起始的* $x$。

  *应用*

  + 某服务器吞吐 $y$（单位：请求 / 秒）与连接数 $x$ 模型有 $3$ 个候选：
    - M1：$y = 100 x$（线性）
    - M2：$y = x^2$（二次）
    - M3：$y = 2^x$（乘法增长）
    观测得 $x = 4 =>$ $y = 16$；$x = 5 =>$ $y = 32$；$x = 6 =>$ $y = 64$。最贴合哪个模型？用相邻比 / 差指纹论证。
  + 反比例 $y = 100 / x$：找一个正整数 $x$ 使 $y < 1$。
  + 假设某快递公司年利润 $y$（万元）增长符合 $y = 10 times 1.2^t$（$t$ 为年份数，$t = 0$ 时 $y = 10$），另一家公司按 $y = 10 + 5 t$ 增长。何时后者被前者追上（$t$ 取正整数近似）？

  *挑战 ☞ 选做*

  + 设 $y_1 = k x$ 与 $y_2 = x^2$。证明对任意 $k > 0$，存在 $x_0$ 使得 $x > x_0 =>$ $y_2 > y_1$。请给出 $x_0$ 的具体值（用 $k$ 表示）。
  + 在同一坐标系里画 $y = x^2$ 和 $y = 2^x$，标注它们的两个交点横坐标（整数点），并说明为什么 $2^x$ 最终超过 $x^2$。
  + 比较 $y_1 = 10^6 x$ 与 $y_2 = x^2$。*起初*谁大？什么时候追平？追平后谁主导？用这个例子说明“系数大小 $!=$ 族等级”。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + 二次（进一步还有乘法增长，$r > 1$）。
  + 取 $x = 1001$：$y_2 = 1001^2 = 1002001 > y_1 = 1001 times 1000 = 1001000$，成立。
  + 表：$x | x^2 | 2^x$：$1 | 1 | 2$；$2 | 4 | 4$；$3 | 9 | 8$；$4 | 16 | 16$；$5 | 25 | 32$；$6 | 36 | 64$；$7 | 49 | 128$；$8 | 64 | 256$；$9 | 81 | 512$；$10 | 100 | 1024$。$x = 5$ 起 $2^x > x^2$ 并此后稳定领先。

  *应用*
  + 相邻比 $32 / 16 = 2, 64 / 32 = 2$——*乘法增长，$r = 2$* $=>$ *M3* $y = 2^x$。（代入 $x = 4$: $2^4 = 16$ ✓）
  + $x = 101 =>$ $y = 100 / 101 < 1$。
  + 前者 $t = 0: 10$，$t = 1: 12$，$t = 2: 14.4$，$t = 3: 17.28$，$t = 4: 20.74$，$t = 5: 24.88$，$t = 6: 29.86$，$t = 7: 35.83$，$t = 8: 42.998$。后者 $t = 0: 10, 15, 20, 25, 30, 35, 40, 45, 50$。$t = 7$ 前者 $approx 35.83$，后者 $45$——后者仍领先；$t = 8$：前者 $approx 43$，后者 $50$；$t = 9$：前者 $approx 51.6$，后者 $55$；$t = 10$：前者 $approx 61.9$，后者 $60$——*$t = 10$ 前者首次反超*。

  *挑战 ☞ 选做*
  + 若 $x > k$，则 $x^2 = x times x > k times x = y_1$。故取 $x_0 = k$ 即可。
  + 整数交点 $x = 2, x = 4$（$2^2 = 4 = 2 times 2, 2^4 = 16 = 4^2$）。$x > 4$ 以后 $2^x$ 每加 $1$ 乘 $2$，$x^2$ 每加 $1$ 只是 $(x + 1) / x times$ 倍（$< 2$），乘法自然超前。
  + 起初（$x < 10^6$）$y_1$ 大；$x = 10^6$ 追平；之后 $y_2$ 超过 $y_1$ 并*持续拉大*。系数 $10^6$ 再大，*族等级*（线性）也赢不过二次。
]
