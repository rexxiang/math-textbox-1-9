#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref, answer-cut
#import "../../lib/diagram-packages.typ": cetz

=== 模型选择：看表、看图、选函数 <tool:fn11-model-choice>

#vocab[建模 modeling][模型选择 model selection][拟合 fit][趋势 trend]

#crisis[
  之前每一节都告诉你“*这是* 线性 / 反比例 / 二次 / 乘法增长”，然后教你用。现实里*没有人*来告诉你用哪种。老师给你一张陌生的表、或者一张描点图，问：“这批数据最像下面哪种关系？”你该怎么答？

  - 表 P：$x: 1, 2, 3, 4, 5$；$y: 3, 6, 9, 12, 15$
  - 表 Q：$x: 1, 2, 3, 4, 5$；$y: 12, 6, 4, 3, 2.4$
  - 表 R：$x: 0, 1, 2, 3, 4$；$y: 1, 1, 4, 9, 16$
  - 表 S：$x: 0, 1, 2, 3, 4$；$y: 1, 2, 4, 8, 16$

  *用目前已学过的四种“指纹”：相邻差恒、相邻积恒、二阶差恒、相邻比恒——能不能一眼分辨它们？*
]

#tryit[
  表 U：$x: 1, 2, 3, 4$；$y: 2, 8, 18, 32$。属于哪族？参数？
]

#discovery[
  *阿明和小雯的对话*

  *阿明*：crisis 那四张表 P、Q、R、S，我手里有了。可看着这堆数字，*我怎么判断该用哪种函数*？挨个套公式吗？

  *小雯*：别挨个套——*先做差*，看相邻 $y$ 的差值恒不恒定。

  *阿明*：试一下表 P：$3, 6, 9, 12, 15$，相邻差全都是 $3$——

  *小雯*：差恒定就是*线性*的指纹，$y = 3 x$。这一步不中再做下一步。

  *阿明*：表 Q 的差呢？$-6, -2, -1, -0.6$，越来越小，明显不恒。

  *小雯*：那就*算积*——把每行的 $x times y$ 算出来。

  *阿明*：$1 times 12, 2 times 6, 3 times 4, 4 times 3, 5 times 2.4$，全是 $12$！

  *小雯*：积恒定是*反比例*的指纹，$y = 12 / x$。继续——

  *阿明*：表 S：$1, 2, 4, 8, 16$。差是 $1, 2, 4, 8$，不恒；积是 $1, 4, 12, 32, 80$，也不恒。

  *小雯*：差不恒、积不恒，就*算相邻比值* $y(x+1) / y(x)$。

  *阿明*：$2/1, 4/2, 8/4, 16/8$ 全等于 $2$！

  *小雯*：相邻比恒定是*乘法增长*的指纹，每次乘 $2$。

  *阿明*：那二次怎么辨？

  *小雯*：*再做一次差*——把刚才那串"相邻差"再做差，叫*二阶差*。二次函数的二阶差恒定。

  *阿明*：等下，表 R 看着像 $y = x^2$（$1, 4, 9, 16$）。可前面 $x = 0$ 时 $y = 1$ 而不是 $0$。差是 $0, 3, 5, 7$，二阶差 $3, 2, 2$ — *不恒定*！

  *小雯*：对。这就是模型选择最容易翻车的地方：*只看前两三行像*就盖章。表 R 不属于本节四族中的任何一种——你必须对整张表负责。

  *阿明*：所以流程是：差→积→比→二阶差。三步内命中就归到一族；都不中就诚实写"四族都不像"。

  *小雯*：还有一条戒律——*别只看头几行*。一个偏离点就能击碎一个模型。

  *把指纹写到一张总表里*：

  #align(center, table(
    columns: (auto, 1fr, 1fr, 1fr),
    inset: 6pt,
    stroke: 0.4pt,
    [*族*], [*公式*], [*表指纹*], [*图像特征*],
    [线性 / 一次 #secref("fn05-linear-function")], [$y = k x + b$], [相邻差 $Delta y$ 恒 $= k$], [直线，斜率 $k$],
    [反比例 #secref("fn07-inverse-proportion")], [$y = k / x$], [$x y$ 恒 $= k$], [双曲线，两轴为渐近线],
    [二次 #secref("fn08-quadratic-function")], [$y = a x^2 + b x + c$], [二阶差 $Delta^2 y$ 恒 $= 2 a (Delta x)^2$（$x$ 等间距；$Delta x = 1$ 时简化为 $2 a$）], [抛物线，有顶点与对称轴],
    [乘法增长（非正式族）#secref("fn10-additive-vs-multiplicative-growth")], [$y = y(0) r^x$], [相邻比 $y(x + 1) / y(x)$ 恒 $= r$], [“先贴地后向上翘”的弯曲],
  ))

  #figure(
    cetz.canvas(length: 0.3cm, {
      import cetz.draw: *
      // Panel 1: Linear
      let ox1 = -12
      line((ox1, 0), (ox1 + 5, 0), stroke: 0.3pt, mark: (end: ">"))
      line((ox1 + 2.5, -2), (ox1 + 2.5, 3), stroke: 0.3pt, mark: (end: ">"))
      line((ox1 + 0.5, -1.5), (ox1 + 4.5, 2.5), stroke: 0.6pt + rgb("#1976D2"))
      content((ox1 + 2.5, -3), text(7pt)[一次])
      // Panel 2: Inverse
      let ox2 = -5
      line((ox2, 0), (ox2 + 5, 0), stroke: 0.3pt, mark: (end: ">"))
      line((ox2 + 2.5, -2), (ox2 + 2.5, 3), stroke: 0.3pt, mark: (end: ">"))
      let inv = range(0, 20).map(i => {
        let x = 0.5 + 0.15 * i
        (ox2 + x + 2.5, 1.5 / x)
      })
      for i in range(0, inv.len() - 1) {
        if inv.at(i).at(1) <= 3 {
          line(inv.at(i), inv.at(i + 1), stroke: 0.6pt + rgb("#B71C1C"))
        }
      }
      content((ox2 + 2.5, -3), text(7pt)[反比例])
      // Panel 3: Quadratic
      let ox3 = 2
      line((ox3, 0), (ox3 + 5, 0), stroke: 0.3pt, mark: (end: ">"))
      line((ox3 + 2.5, -2), (ox3 + 2.5, 3), stroke: 0.3pt, mark: (end: ">"))
      let quad = range(0, 41).map(i => {
        let x = -2 + 0.1 * i
        (ox3 + x + 2.5, 0.5 * x * x - 1)
      })
      for i in range(0, quad.len() - 1) {
        if quad.at(i).at(1) <= 3 and quad.at(i + 1).at(1) <= 3 {
          line(quad.at(i), quad.at(i + 1), stroke: 0.6pt + rgb("#2E7D32"))
        }
      }
      content((ox3 + 2.5, -3), text(7pt)[二次])
      // Panel 4: Exponential
      let ox4 = 9
      line((ox4, 0), (ox4 + 5, 0), stroke: 0.3pt, mark: (end: ">"))
      line((ox4 + 2.5, -2), (ox4 + 2.5, 3), stroke: 0.3pt, mark: (end: ">"))
      let expo = range(0, 35).map(i => {
        let x = -2 + 0.12 * i
        (ox4 + x + 2.5, calc.pow(1.8, x) - 0.5)
      })
      for i in range(0, expo.len() - 1) {
        if expo.at(i).at(1) <= 3 and expo.at(i + 1).at(1) <= 3 {
          line(expo.at(i), expo.at(i + 1), stroke: 0.6pt + rgb("#FF6F00"))
        }
      }
      content((ox4 + 2.5, -3), text(7pt)[乘法增长])
    }),
    caption: [四族函数各有标志性图像形状：直线、双曲线、抛物线、"先贴地后翘起"。]
  )

  *四张表逐一用流程*

  - 表 P：$Delta y = 3, 3, 3, 3 =>$ *线性*，$k = 3$。$y = 3 x$。
  - 表 Q：$x y = 12, 12, 12, 12, 12 =>$ *反比例*，$k = 12$。$y = 12 / x$。
  - 表 R：差不恒、积不恒、比不恒、二阶差也不恒——*以上四族均不符合*。前几行像 $x^2$，但 $x = 0$ 时 $y = 1 != 0$，整张表对不上。
  - 表 S：相邻比 $2, 2, 2, 2 =>$ *乘法增长*，$r = 2$。

  *模型选择第一戒律*：别只看前两行就下结论；要对*整张表*负责。
]

#side-hack[
  *口诀：差比差*

  拿到陌生表第一反应：算一遍*差*（相邻差），不恒就算一遍*比*（相邻比），还不恒就算一遍*二阶差*。三步内命中的话，就是我们已有的族里的一个；不中就诚实写“这批数据不太像本章任何一族”。
]

#tryit[
  + 表 T：$x: 1, 2, 3, 4$；$y: 5, 10, 15, 20$。属于哪族？参数？
]

#blueprint[
  *工具：模型选择流程（$x$ 等间距）*

  输入：一张 $(x, y)$ 表，$x$ 等间距且至少 $5$ 个点。

  步骤：
  + 算相邻差 $Delta y$。恒定 $=>$ *线性* $y = k x + b$，$k = Delta y / Delta x$。
  + 算相邻积 $x y$（要求 $x != 0$）。恒定 $=>$ *反比例* $y = k / x$，$k = x y$。
  + 算相邻比 $y(x + 1) / y(x)$。恒定 $=>$ *乘法增长* $y = y(0) r^x$（现象标签，不是正式族）。
  + 算二阶差 $Delta^2 y$（$Delta y$ 再做差）。恒定 $=>$ *二次* $y = a x^2 + b x + c$，$a = Delta^2 y / (2 (Delta x)^2)$。
  + 都不恒定 $=>$ 诚实写“以上四族均不符合”。

  *图像侧的“表 $=>$ 图”直觉*：

  - 直线 $<=>$ 线性；
  - 双曲线（两轴为渐近线）$<=>$ 反比例；
  - 抛物线 $<=>$ 二次；
  - “先贴地后翘起” $<=>$ 乘法增长。

  #figure(
    cetz.canvas(length: 0.4cm, {
      import cetz.draw: *
      line((-1, 0), (8, 0), stroke: 0.4pt, mark: (end: ">"))
      line((0, -1), (0, 8), stroke: 0.4pt, mark: (end: ">"))
      content((8.3, 0), $x$, anchor: "west")
      content((0, 8.3), $y$, anchor: "south")
      // scatter points following y = x^2 / 3 + 1
      let pts = ((1, 1.3), (2, 2.3), (3, 4), (4, 6.3), (5, 7.5))
      for p in pts {
        circle(p, radius: 0.15, fill: rgb("#424242"))
      }
      // wrong fit: linear (dashed)
      line((0.5, 0.5), (5.5, 8), stroke: (paint: rgb("#90CAF9"), thickness: 0.5pt, dash: "dashed"))
      content((5.5, 7.5), text(6pt)[一次?], anchor: "west")
      // correct fit: quadratic (solid)
      let s = range(0, 51).map(i => {
        let x = 0.5 + 0.1 * i
        (x, x * x / 3 + 0.8)
      })
      for i in range(0, s.len() - 1) {
        if s.at(i).at(1) <= 8 and s.at(i + 1).at(1) <= 8 {
          line(s.at(i), s.at(i + 1), stroke: 0.6pt + rgb("#2E7D32"))
        }
      }
      content((4.5, 5.5), text(6pt)[二次 ✓], anchor: "west")
    }),
    caption: [散点图上"看形状选模型"：这组散点沿抛物线排列，用二次拟合（实线）比一次（虚线）更贴合。]
  )

  反过来，若先给的是散点图，可以*反推选模型*：点落在直线上 $=>$ 线性；落在抛物线上 $=>$ 二次；落在类似双曲线上 $=>$ 反比例；落在先平后翘曲线上 $=>$ 乘法增长。
]

#pitfall[
  ✗ 只看前两项差值恒定就断言线性
  → ✓ 至少取 $4$ 个相邻差都恒定才安全。（表 R 就是只看前两项会上当的例子。）

  ✗ 把 $y$ 列里几个看起来像完全平方的数（$1, 4, 9, 16$）立刻认作 $y = x^2$，不查对应 $x$
  → ✓ *一定对照 $x$ 列*。$y$ 值与 $x$ 值的对应关系才是函数。

  ✗ 把“相邻差变大”一律当作二次
  → ✓ “差变大”还可能是乘法增长。要靠*二阶差是否恒定*来分辨：二次的二阶差恒；乘法增长的二阶差仍在变化。

  ✗ 表里 $x = 0$ 出现在反比例模型中
  → ✓ 反比例 $y = k / x$ 在 $x = 0$ 无定义。若表里包含 $x = 0$，*必然*不是反比例。

  ✗ 数据带“噪声”（现实测量误差）还硬挑出一族
  → ✓ 现实数据一般*近似*符合，不会完全恒定。本章为简化，只处理*理想、无噪声*的表。若数据显然带误差，请注明“近似属于某族”。

  ✗ 把 $x$ 不等间距的表直接套步骤 1–4
  → ✓ 这些指纹都*要求 $x$ 等间距*。若 $x$ 不等距，先换成等距 $x$（若可行）或改用图像法观察趋势。
]

#mastery[
  *基础*

  + 判断族并写参数：$x: 1, 2, 3, 4, 5$；$y: 4, 8, 12, 16, 20$。
  + 判断族并写参数：$x: 1, 2, 3, 4, 5$；$y: 20, 10, 20 / 3, 5, 4$。
  + 判断族并写参数：$x: 0, 1, 2, 3, 4$；$y: 1, 4, 7, 10, 13$。

  *应用*

  + 判断族并写公式：$x: 0, 1, 2, 3, 4$；$y: 2, 5, 10, 17, 26$。（提示：二阶差。）
  + 判断族并写公式：$x: 0, 1, 2, 3, 4$；$y: 3, 6, 12, 24, 48$。
  + 给出下列图像的可能函数族：
    - (a) 一条穿过原点斜率 $2$ 的直线。
    - (b) 一条从左上下来、到最低点再上翘的对称曲线。
    - (c) 一条在第一象限、靠近两轴但不相交的曲线。
    - (d) 一条从 $(0, 1)$ 开始先几乎水平、后快速向上冲的曲线。

  *挑战*

  + 表 $x: 0, 1, 2, 3, 4$；$y: 0, 1, 4, 9, 16$——不是 $y = x^2$ 吗？算一遍二阶差验证它确属二次；并把 $(0, 0)$ 连起来，发现比表 R 的 $(0, 1), (1, 1)$ 开头干净得多。总结：*一个“偏离”点就能击碎一个模型*。
  + 给你一个连续三组 $x$ 等间距表：$x = 1, 2, 3, 4, 5, 6$；$y = 2, 5, 10, 17, 26, 37$。用流程找出它属于哪族，并写出公式 $y = f(x)$。

    #side-hack[
      *思路骨架*（卡住了再看下面的步骤）：

      + 第 1 步：先算*一阶差* $Delta y$。若恒定，是线性族；否则继续。
      + 第 2 步：算*二阶差* $Delta^2 y$。若恒定，是二次族（$Delta^2 y = 2 a (Delta x)^2$，反推 $a$）。
      + 第 3 步：若二阶差也不恒，再试*相邻比* $y(x+1) / y(x)$；恒定则是乘法增长族。
      + 第 4 步：确定族后，用任意 $1$–$2$ 个已知点反求剩下的参数（$b$、$c$），最后再代一个其它点验证公式。

      再回到上面的题目，按这个流程走一遍：
    ]
  + 表 $x: 1, 2, 3, 4$；$y: 2, 4, 8, 16$——既可能是 $y = 2^x$（乘法），也可能是 $y$ 随 $x$ 按 $"某规则"$ 的其他表达。用相邻比检验 $=>$ 偏向乘法。再*追加一点* $x = 5, y = ?$，如果是 $32$ $=>$ 乘法；如果是 $20$ $=>$ 反而是 $y = x^2 - x + 2$ 之类。*想说明*什么？——流程只有*在有足够多数据点时*才稳。

  + *[反直觉]*：乘法增长（$y = y_0 dot r^x$，$r > 1$）和线性增长哪个"快"？对 $x$ 比较小时和 $x$ 比较大时分别回答，并用 $y = 2^x$ 和 $y = 100 x$ 为例。
  + *[组合]*：表 $x: 2, 4, 6, 8$；$y: 3, 12, 48, 192$。$x$ 不是从 $0$ 开始也不是等差步长 $1$——你能用"相邻比"法识别出它属于哪族吗？写出公式。
  + *[构造题]*：构造一张 $6$ 行的 $(x, y)$ 表（$x$ 等间距），使得它*恰好符合*二次函数 $y = 2x^2 - 1$。然后用"二阶差"验证你的表确实是二次族。

#answer-cut[

  *基础*
  + $Delta y$ 恒 $= 4 =>$ *线性*（$Delta y$ 恒定 → 线性），$k = 4, b = 0 =>$ $y = 4 x$。
  + $x y$：$1 times 20, 2 times 10, 3 times 20 / 3, 4 times 5, 5 times 4 = 20 =>$ *反比例*（$x y$ 恒定 → 反比例），$k = 20 =>$ $y = 20 / x$。
  + $Delta y$ 恒 $= 3 =>$ *线性*（$Delta y$ 恒定 → 线性），$k = 3, b = 1 =>$ $y = 3 x + 1$。

  *应用*
  + 差→积→比→二阶差四步法：$Delta y: 3, 5, 7, 9$ 不恒；$Delta^2 y: 2, 2, 2$ 恒 $=>$ *二次*（二阶差恒定 → 二次）。$a = 1$，代入得 $y = x^2 + 2 x + 2$。
  + 相邻比 $2, 2, 2, 2 =>$ *乘法增长*（相邻比恒定 → 乘法），$r = 2$，$y(0) = 3 =>$ $y = 3 times 2^x$。
  + (a) 线性 $y = 2 x$（线性指纹：$Delta y$ 恒定）。(b) 二次（顶点在低处）。(c) 反比例（双曲线 $y = k / x, k > 0$）。(d) 乘法增长（$r > 1$）。

  *挑战*
  + $Delta^2 y: 2, 2, 2$ 恒 $=>$ 二次确认（二阶差恒定 → 二次）。$a = 1$，$b = 0$，$c = 0 =>$ $y = x^2$。表 R 只改了 $y(0)$ 和 $y(1)$ 两项就不再恒——一个偏离点就能击碎模型。
  + 差→积→比→二阶差四步法：$Delta^2 y: 2, 2, 2, 2$ 恒 $=>$ *二次*。$a = 1$，$b = 0$，$c = 1 =>$ $y = x^2 + 1$。
  + 用已给 $4$ 点算相邻比恒 $= 2$，结论*偏向*乘法增长（相邻比恒定 → 乘法）。但仅 $4$ 点不能排除其他多项式。若补 $x = 5$ 得 $32$ $=>$ 支持 $y = 2^x$；若得其他值则不是。*流程只有点够多时才稳*。
  + *[反直觉]*：$x$ 较小时线性更大，$x$ 较大时乘法增长远更大。提示：比较两族函数的长期行为。
  + 步长 $Delta x = 2$，相邻比 $y(x+2)/y(x)$：$12/3 = 4$，$48/12 = 4$，$192/48 = 4$——比恒 $= 4$，属*乘法增长族*。每隔 $Delta x = 2$ 乘以 $4 = 2^2$，所以 $r = 2$（每 $1$ 步的比）。公式 $y = 3 dot 2^x / 4$...实际从 $x = 2, y = 3$：$y = 3 dot 2^{x-2} dot 4^{(x-2)/2}$... 简洁：$y = (3/4) dot 2^x$（相邻比恒定 → 乘法增长，步长修正）。
  + *[构造题]*：取 $x = 0,1,2,3,4,5$：$y = -1, 1, 7, 17, 31, 49$。一阶差：$2, 6, 10, 14, 18$；二阶差：$4, 4, 4, 4$——恒为 $4 = 2 times 2 times (Delta x)^2 = 2 times 2 times 1$ ✓（二次函数 $a=2$，二阶差 $= 2a = 4$）。确认是二次族（二次函数构表 + 二阶差验证）。
]
]

#history-note[
  *索菲娅·柯瓦列夫斯卡娅（Sofya Kovalevskaya，$1850$–$1891$）*——俄国数学家。她研究的"刚体绕固定点的旋转"问题，至今被叫作"柯瓦列夫斯卡娅陀螺"，是少数几种能写出精确解析解的力学模型之一。她的工作给后人立下一个范式：面对一个真实运动，先要判断该用线性、指数还是周期模型，再用方程拟合数据；模型选错了，再多数据也救不回来。她是欧洲历史上第一位获得数学博士学位的女性，并于 $1889$ 年成为斯德哥尔摩大学正教授。
]
