#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref

== 模型选择：看表、看图、选函数 <tool:fn11-model-choice>

#vocab[建模 modeling / 模型选择 model selection / 拟合 fit / 趋势 trend]

#crisis[
  之前每一节都告诉你“*这是* 线性 / 反比例 / 二次 / 乘法增长”，然后教你用。现实里*没有人*来告诉你用哪种。老师给你一张陌生的表、或者一张描点图，问：“这批数据最像下面哪种关系？”你该怎么答？

  - 表 P：$x: 1, 2, 3, 4, 5$；$y: 3, 6, 9, 12, 15$
  - 表 Q：$x: 1, 2, 3, 4, 5$；$y: 12, 6, 4, 3, 2.4$
  - 表 R：$x: 0, 1, 2, 3, 4$；$y: 1, 1, 4, 9, 16$
  - 表 S：$x: 0, 1, 2, 3, 4$；$y: 1, 2, 4, 8, 16$

  *用目前已装配的四种“指纹”：相邻差恒、相邻积恒、二阶差恒、相邻比恒——能不能一眼分辨它们？*
]

#discovery[
  *四种已有函数族各自的“指纹”*

  我们把本分支到现在为止遇到的 $4$ 种关系，用表格特征 / 图像特征并列：

  #align(center, table(
    columns: (auto, 1fr, 1fr, 1fr),
    inset: 6pt,
    stroke: 0.4pt,
    [*族*], [*公式*], [*表指纹*], [*图像特征*],
    [线性 / 一次 #secref("fn05-linear-function")], [$y = k x + b$], [相邻差 $Delta y$ 恒 $= k$], [直线，斜率 $k$],
    [反比例 #secref("fn07-inverse-proportion")], [$y = k / x$], [$x y$ 恒 $= k$], [双曲线，两轴为渐近线],
    [二次 #secref("fn08-quadratic-function")], [$y = a x^2 + b x + c$], [二阶差 $Delta^2 y$ 恒 $= 2 a (Delta x)^2$（$x$ 等间距；$Delta x = 1$ 时简化为 $2 a$）], [抛物线，有顶点与对称轴],
    [乘法增长（非 blueprint）#secref("fn10-additive-vs-multiplicative-growth")], [$y = y(0) r^x$], [相邻比 $y(x + 1) / y(x)$ 恒 $= r$], [“先贴地后向上翘”的弯曲],
  ))

  *本节的工具是一条决策流程*：

  - 步骤 1：看 $x$ 值是否等间距。*四种指纹都默认 $x$ 等间距*（$Delta x$ 恒）。
  - 步骤 2：算相邻差。若恒定 $=>$ *线性*，$k = Delta y / Delta x$。
  - 步骤 3：若相邻差不恒定，算相邻积 $x y$。恒定 $=>$ *反比例*，$k = x y$。
  - 步骤 4：若还不恒定，算相邻比值 $y(x + 1) / y(x)$。恒定 $=>$ *乘法增长*，$r = $ 该比值。（非 blueprint；只打现象标签）
  - 步骤 5：以上都不恒定，但若 $x$ 等间距下*二阶差*恒定 $=>$ *二次*。“二阶差”$=$ 对 $Delta y$ 再做一次差。

  *四张表逐一用流程*

  - 表 P：$Delta y = 3, 3, 3, 3 =>$ *线性*，$k = 3$。$y = 3 x$。
  - 表 Q：$Delta y = -6, -2, -1, -0.6$ 不恒；$x y$：$1 times 12, 2 times 6, 3 times 4, 4 times 3, 5 times 2.4 = 12, 12, 12, 12, 12$ 恒定 $=>$ *反比例*，$k = 12$。$y = 12 / x$。
  - 表 R（$y = 1, 1, 4, 9, 16$）：$Delta y = 0, 3, 5, 7$ 不恒；$x y$：$0, 1, 8, 27, 64$ 不恒；相邻比 $1, 4,  9 / 4, 16 / 9$ 不恒；二阶差 $Delta^2 y = 3 - 0, 5 - 3, 7 - 5 = 3, 2, 2$——也不恒定。表 R *不是*以上四族里的任何一种。

    这里正是*模型选择最容易翻车的地方*：前四行 $1, 4, 9, 16$ 很像 $x^2$，初学者容易立即盖章“二次”。然而把 $x = 0$ 一并纳入检验，$y(0) = 1 != 0^2 = 0$，破坏了二次族的指纹。这张表充其量是 $x >= 1$ 时才吻合 $y = x^2$。选择模型之前要对*整张表*负责，不能只看“像不像”的前两三行。

  - 表 S：相邻差 $1, 2, 4, 8$ 不恒；相邻比 $2, 2, 2, 2$ 恒 $=>$ *乘法增长*，$r = 2$。（$y = 2^x$，非 blueprint；打现象标签即可。）

  这四例涵盖 $3$ 个正中靶心 + $1$ 个*反面教材*。*模型选择的第一戒律*是：别只看前两行就下结论。
]

#side-hack[
  *口诀：差比差*

  拿到陌生表第一反应：算一遍*差*（相邻差），不恒就算一遍*比*（相邻比），还不恒就算一遍*二阶差*。三步内命中的话，就是我们已有的族里的一个；不中就诚实写“这批数据不太像本章任何一族”。
]

#tryit[
  + 表 T：$x: 1, 2, 3, 4$；$y: 5, 10, 15, 20$。属于哪族？参数？
  + 表 U：$x: 1, 2, 3, 4$；$y: 2, 8, 18, 32$。属于哪族？参数？
  + 表 V：$x: 1, 2, 3, 4$；$y: 60, 30, 20, 15$。属于哪族？参数？
]

#blueprint[
  *工具：模型选择流程（$x$ 等间距）*

  输入：一张 $(x, y)$ 表，$x$ 等间距且至少 $5$ 个点。

  步骤：
  + 算相邻差 $Delta y$。恒定 $=>$ *线性* $y = k x + b$，$k = Delta y / Delta x$。
  + 算相邻积 $x y$（要求 $x != 0$）。恒定 $=>$ *反比例* $y = k / x$，$k = x y$。
  + 算相邻比 $y(x + 1) / y(x)$。恒定 $=>$ *乘法增长* $y = y(0) r^x$（现象标签，不是 blueprint）。
  + 算二阶差 $Delta^2 y$（$Delta y$ 再做差）。恒定 $=>$ *二次* $y = a x^2 + b x + c$，$a = Delta^2 y / (2 (Delta x)^2)$。
  + 都不恒定 $=>$ 诚实写“以上四族均不符合”。

  *图像侧的“表 $=>$ 图”直觉*：

  - 直线 $<=>$ 线性；
  - 双曲线（两轴为渐近线）$<=>$ 反比例；
  - 抛物线 $<=>$ 二次；
  - “先贴地后翘起” $<=>$ 乘法增长。

  反过来，若先给的是散点图，可以*反推选模型*：点落在直线上 $=>$ 线性；落在抛物线上 $=>$ 二次；落在类似双曲线上 $=>$ 反比例；落在先平后翘曲线上 $=>$ 乘法增长。
]

#pitfall[
  ✗ 只看前两项差值恒定就断言线性
  → ✓ 至少取 $4$ 个相邻差都恒定才安全。（表 R 就是只看前两项会上当的例子。）

  ✗ 把 $y$ 列里几个看起来像完全平方的数（$1, 4, 9, 16$）立刻认作 $y = x^2$，不查对应 $x$
  → ✓ *一定对照 $x$ 列*。$y$ 值与 $x$ 值的对应关系才是函数。

  ✗ 把“相邻差变大”一律当作二次
  → ✓ “差变大”还可能是乘法增长。要靠*二阶差是否恒定*来分辨：二次的二阶差恒；乘法增长的二阶差仍在变化。

  ✗ 表里 $x = 0$ 出现在反比例模型中
  → ✓ 反比例 $y = k / x$ 在 $x = 0$ 无定义。若表里包含 $x = 0$，*必然*不是反比例。

  ✗ 数据带“噪声”（现实测量误差）还硬挑出一族
  → ✓ 现实数据一般*近似*符合，不会完全恒定。本章为简化，只处理*理想、无噪声*的表。若数据显然带误差，请注明“近似属于某族”。

  ✗ 把 $x$ 不等间距的表直接套步骤 1–4
  → ✓ 这些指纹都*要求 $x$ 等间距*。若 $x$ 不等距，先换成等距 $x$（若可行）或改用图像法观察趋势。
]

#mastery[
  *基础*

  + 判断族并写参数：$x: 1, 2, 3, 4, 5$；$y: 4, 8, 12, 16, 20$。
  + 判断族并写参数：$x: 1, 2, 3, 4, 5$；$y: 20, 10, 20 / 3, 5, 4$。
  + 判断族并写参数：$x: 0, 1, 2, 3, 4$；$y: 1, 4, 7, 10, 13$。

  *应用*

  + 判断族并写公式：$x: 0, 1, 2, 3, 4$；$y: 2, 5, 10, 17, 26$。（提示：二阶差。）
  + 判断族并写公式：$x: 0, 1, 2, 3, 4$；$y: 3, 6, 12, 24, 48$。
  + 给出下列图像的可能函数族：
    - (a) 一条穿过原点斜率 $2$ 的直线。
    - (b) 一条从左上下来、到最低点再上翘的对称曲线。
    - (c) 一条在第一象限、靠近两轴但不相交的曲线。
    - (d) 一条从 $(0, 1)$ 开始先几乎水平、后快速向上冲的曲线。

  *挑战*

  + 表 $x: 0, 1, 2, 3, 4$；$y: 0, 1, 4, 9, 16$——不是 $y = x^2$ 吗？算一遍二阶差验证它确属二次；并把 $(0, 0)$ 连起来，发现比表 R 的 $(0, 1), (1, 1)$ 开头干净得多。总结：*一个“偏离”点就能击碎一个模型*。
  + 给你一个连续三组 $x$ 等间距表：$x = 1, 2, 3, 4, 5, 6$；$y = 2, 5, 10, 17, 26, 37$。用流程找出它属于哪族，并写出公式 $y = f(x)$。
  + 表 $x: 1, 2, 3, 4$；$y: 2, 4, 8, 16$——既可能是 $y = 2^x$（乘法），也可能是 $y$ 随 $x$ 按 $"某规则"$ 的其他表达。用相邻比检验 $=>$ 偏向乘法。再*追加一点* $x = 5, y = ?$，如果是 $32$ $=>$ 乘法；如果是 $20$ $=>$ 反而是 $y = x^2 - x + 2$ 之类。*想说明*什么？——流程只有*在有足够多数据点时*才稳。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + $Delta y$ 恒 $= 4 =>$ *线性*，$k = 4, b = 0 =>$ $y = 4 x$。
  + $x y$：$1 times 20, 2 times 10, 3 times 20 / 3, 4 times 5, 5 times 4 = 20 =>$ *反比例*，$k = 20 =>$ $y = 20 / x$。
  + $Delta y$ 恒 $= 3 =>$ *线性*，$k = 3, b = 1 =>$ $y = 3 x + 1$。

  *应用*
  + $Delta y: 3, 5, 7, 9$，$Delta^2 y: 2, 2, 2 =>$ *二次*。$a = 2 / 2 = 1$。代入 $(0, 2)$：$c = 2$；$(1, 5) = 1 + b + 2 =>$ $b = 2$。$=>$ $y = x^2 + 2 x + 2$。
  + 相邻比 $2, 2, 2, 2 =>$ *乘法增长*，$r = 2$，$y(0) = 3 =>$ $y = 3 times 2^x$。
  + (a) 线性 $y = 2 x$。(b) 二次（顶点在低处）。(c) 反比例（双曲线 $y = k / x, k > 0$）。(d) 乘法增长（$r > 1$）。

  *挑战*
  + $Delta y: 1, 3, 5, 7$，$Delta^2 y: 2, 2, 2$ 恒 $=>$ 二次确认。$a = 2 / 2 = 1$，代入 $(0, 0)$: $c = 0$；$(1, 1)$: $a + b + c = 1 + b = 1 =>$ $b = 0 =>$ $y = x^2$。表 R 只改了 $y(0)$ 和 $y(1)$ 两项就不再恒，可见一个偏离点的毁坏力。
  + $Delta y: 3, 5, 7, 9, 11$；$Delta^2 y: 2, 2, 2, 2$ 恒 $=>$ *二次*，$a = 1$。$(1, 2): 1 + b + c = 2$；$(2, 5): 4 + 2 b + c = 5 =>$ $2 b + c = 1$。两式相减：$b + 0 = 0 =>$ $b = 0$，$c = 1 =>$ $y = x^2 + 1$。
  + 用已给 $4$ 点算相邻比恒 $= 2$，结论*偏向*乘法增长。但仅 $4$ 点不能排除其他多项式。若补 $x = 5$ 得 $32$ $=>$ 支持 $y = 2^x$；若得 $20$ $=>$ 不是 $2^x$，而是别的（例如 $y = x^2 - x + 2$ 在前 $4$ 点恰好 $= 2, 4, 8, 14$…注：此式 $x = 4$ 给 $14$ 而非 $16$，并非完美反例，保留结论：*流程只有点够多时才稳*）。
]
