#import "../../lib/theme-v2.typ": blueprint, side-hack, tryit, secref

=== 函数分支混合自测 <meta:functions-mixed-self-test>

#blueprint[
  *题目*

  每题末尾标注所用到的主要知识链。答案与讲解见 #secref("meta:functions-summary-review")。

  + 某商场停车费：进场免费停 $1$ 小时，之后每小时 $3$ 元（不足 $1$ 小时按 $1$ 小时计，简化为连续计费）。写出停车 $x$ 小时（$x >= 1$）时费用 $y$ 关于 $x$ 的函数，画出图像，指出当 $x = 5$ 时费用。（用到 #secref("fn03-formula-as-function-view")、#secref("fn05-linear-function")。）

  + 一直线过 $(0, -2)$ 和 $(4, 6)$。写一次函数表达式并求它与 $x$ 轴的交点。（用到 #secref("fn06-slope-as-rate")、#secref("fn05-linear-function")。）

  + $y$ 与 $x$ 成反比例，且 $x = -3$ 时 $y = 4$。求 $k$；$x = 6$ 时 $y$ 的值；图像分布在哪两个象限？（用到 #secref("fn07-inverse-proportion")。）

  + 二次函数 $y = x^2 - 6 x + 5$：用配方法写顶点式；给出顶点、对称轴、最小值、与 $x$ 轴交点。（用到 #secref("fn08-quadratic-function")、配方法。）

  + 一张表 $x: 0, 1, 2, 3, 4$；$y: 2, 6, 18, 54, 162$。属于哪族？写公式。$x = 5$ 时 $y$ 是多少？（用到 #secref("fn10-additive-vs-multiplicative-growth")、#secref("fn11-model-choice")。）

  + 以下四个函数在 $x = 10$ 时按 $y$ 从大到小排列：$y = 1000 / x$、$y = 50 x + 10$、$y = x^2$、$y = 2^x$。长期（$x -> infinity$）时按 $y$ 从大到小排列又如何？（用到 #secref("fn12-function-comparison")。）

  + 从高 $80$ m 的楼顶抛出一物体，$t$ 秒后高度 $h(t) = 80 + 15 t - 5 t^2$（$t >= 0$）。何时落地？最高点在几秒、高度多少？（用到 #secref("fn08-quadratic-function")、配方法。）

  + 把 $y = 100$ 万元放入银行，两家方案：甲*每年加 $8$ 万*，乙*每年乘 $1.08$*。分别写 $5$ 年、$10$ 年、$20$ 年后的余额，并指出谁在什么时候反超。（用到 #secref("fn10-additive-vs-multiplicative-growth")、#secref("fn12-function-comparison")。）
]

#side-hack[
  *自测说明*

  - 只要能独立解出 $6$ 道以上（不看答案），就算走完函数分支。
  - 不确定的题目，顺着题目末尾标注的章节号回到对应章节复看。
]
