#import "../../lib/theme-v2.typ": blueprint, mastery, side-hack, secref

= 本章回顾 <meta:inverse-proportion-quadratic-functions>

#blueprint[
  *知识速查卡*

  #table(
    columns: (1fr, 1.6fr),
    inset: 6pt,
    stroke: 0.4pt,
    [*主题*], [*一句话提要*],
    [反比例 #secref("fn07-inverse-proportion")], [$y = k / x$（$k != 0$）$<=>$ $x y = k$；图像是两支双曲线；定义域 ${x | x != 0}$。],
    [反比例图像所在象限], [$k > 0$：I、III 象限；$k < 0$：II、IV 象限。不过原点、渐近于两坐标轴。],
    [二次函数一般式 #secref("fn08-quadratic-function")], [$y = a x^2 + b x + c$（$a != 0$）；图像是抛物线。],
    [顶点式], [$y = a (x - h)^2 + k$；顶点 $(h, k)$；对称轴 $x = h$。],
    [对称轴公式], [$x = -b / (2 a)$；代回原式得顶点纵坐标 $k$。],
    [开口与最值], [$a > 0$ 开口朝上、顶点取最小值 $k$；$a < 0$ 开口朝下、顶点取最大值 $k$。],
    [配方法], [$y = a (x + b / (2 a))^2 + (c - b^2 / (4 a))$——把一般式压回顶点式（回 #secref("al05-quadratic-solving")）。],
    [三族识别 #secref("fn09-comparing-function-families")], [$Delta y$ 恒 $=>$ 一次；$Delta^2 y$ 恒 $=>$ 二次；$x y$ 恒 $=>$ 反比例。],
  )
]

#mastery[
  *混合自测*

  + $y$ 与 $x$ 成反比例且 $x = 5$ 时 $y = -6$。求 $k$ 与 $x = 3$ 时 $y$。
  + $y = -6 / x$ 的图像在哪两个象限？它与直线 $y = x$ 有几个交点？
  + 把 $y = x^2 - 4 x + 7$ 用配方法化成顶点式；指出顶点、对称轴、最小值。
  + 抛物线 $y = -2 (x - 1)^2 + 8$ 的顶点、对称轴、最大值；与 $x$ 轴交点。
  + 一张表 $x: 1, 2, 3, 4$；$y: 1, 4, 9, 16$。哪一族？写公式。
  + 一张表 $x: 1, 2, 3, 6$；$y: 12, 6, 4, 2$。哪一族？写公式。
  + 二次函数过点 $(0, -3)$、$(-1, 0)$、$(3, 0)$，求 $y = a x^2 + b x + c$。
  + 某矩形周长固定为 $20$，设一边 $x$、面积 $S$。写 $S(x)$，给出定义域，求最大面积与对应 $x$。

  *参考答案*

  + $k = 5 times (-6) = -30$，$y = -30 / x$。$x = 3 => y = -10$。
  + $k = -6 < 0 =>$ II、IV 象限。与 $y = x$ 联立 $x = -6 / x => x^2 = -6$——无实数解 $=>$ *无交点*。
  + 配方：$x^2 - 4 x + 7 = (x - 2)^2 + 3$。顶点 $(2, 3)$；对称轴 $x = 2$；最小值 $3$（$a = 1 > 0$）。
  + 顶点 $(1, 8)$；对称轴 $x = 1$；最大值 $8$（$a = -2 < 0$）。与 $x$ 轴交：$-2 (x - 1)^2 + 8 = 0 => (x - 1)^2 = 4 => x = -1, 3$。
  + 一阶差：$3, 5, 7$；二阶差 $2, 2$ 恒 $=>$ *二次*。$y = x^2$。
  + $x y$：$12, 12, 12, 12$ 恒 $=>$ *反比例*，$y = 12 / x$。
  + $(0, -3)$：$c = -3$。$(-1, 0)$ 与 $(3, 0)$ 是 $x$ 轴两交点 $=>$ $y = a (x + 1)(x - 3) = a (x^2 - 2 x - 3)$。令 $x = 0$：$y = -3 a = -3 => a = 1$。$y = x^2 - 2 x - 3$。
  + 另一边 $= 10 - x$。$S(x) = x (10 - x) = -x^2 + 10 x$。定义域 $0 < x < 10$。对称轴 $x = 5$，$S_max = S(5) = -25 + 50 = 25$。当 $x = 5$（正方形）时面积最大 $= 25$。
]

#blueprint[
  *螺旋复习*

  - #secref("al05-quadratic-solving")：配方法、求根公式、$Delta$ 判别式——在本章用来（a）把一般式压回顶点式；（b）解“抛物线与 $x$ 轴交点”；（c）判抛物线是否与 $x$ 轴相交。
  - #secref("al05-vieta")：两交点的和 / 积可由 $-b / a$、$c / a$ 读出——下一步可接“过两个已知 $x$ 轴交点的二次”构造法。
  - #secref("al03-rational-expression-structure")：反比例定义域的“$x != 0$”直接来自分母非零。
  - #secref("ge05-coordinate-transforms")：顶点式正是把 $y = a x^2$ 做平移得到的，平移公式就是这里用到的。
  - #secref("fn05-linear-function") / #secref("fn06-slope-as-rate")：一次族作为“直线”与本章的双曲线 / 抛物线形成对比。
  - 下一章衔接：反比例、二次这两类*弯曲*模型 $+$ 第 $2$ 章的直线模型 $+$ 第 $4$ 章的乘法增长现象，构成本分支“四种基本变化节奏”的全家福。
]

#side-hack[
  *☞ 高中桥 / 继续往前*

  - 二次函数的*判别式* $Delta = b^2 - 4 a c$：$Delta > 0$ 两零点、$= 0$ 一零点（顶点在 $x$ 轴）、$< 0$ 无实数零点（抛物线不碰 $x$ 轴）——这是 #secref("al05-quadratic-solving") 已有的工具迁移到函数。
  - 反比例的进一步：$y = (a x + b) / (c x + d)$（分式一次 / 一次，图像是被平移的双曲线）——高中 /更高阶课程里处理。
  - 三角函数 $y = sin x$、$y = cos x$：这是高中会正式引入的*周期函数*族，图像是反复的波——不是直线、不是抛物线、不是双曲线。
  - 多项式函数 $y = a_n x^n + ...$：次数更高时图像可以有多个“峰 / 谷”。二次只是多项式函数的最小非线性成员。
]
