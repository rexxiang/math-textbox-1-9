#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref
#import "../../lib/diagram-packages.typ": cetz

=== 抛物线：二次函数 $y = a x^2 + b x + c$ <tool:fn08-quadratic-function>

#vocab[二次函数 quadratic function][抛物线 parabola][顶点 vertex][顶点式 vertex form][对称轴 axis of symmetry]

#crisis[
  一次函数“每加 $1$ 单位 $x$、$y$ 加固定 $k$”描述*稳定*的变化，反比例描述*反向*的变化。但日常里还有另一类现象：*变化本身也在变化*。

  喷泉水柱的高度随水平距离先增后减；炮弹在空中飞，高度先升后降；抛起的石子，其离地高度 $h$ 随时间 $t$ 先升后降。它们的表格看起来都像：

  #align(center, table(
    columns: 7,
    align: center,
    stroke: 0.4pt,
    [$t$], [$0$], [$1$], [$2$], [$3$], [$4$], [$5$],
    [$h$], [$0$], [$15$], [$20$], [$15$], [$0$], [$-25$],
  ))

  $h$ 先升、到最大、再下降，直到 $0$ 之下（此时石子到达地面之下没有意义，但数学上继续）。这类“先升后降”或“先降后升”的关系，*不可能*被一次函数描述——一次函数只会单调一个方向。

  *怎样把这种“先升后降（或先降后升）、左右对称”的函数精确写出来？* 它的图像怎么画？能不能一眼看出最高 / 最低点在哪？
]

#history-note[
  抛物线本来是纯几何概念——约公元前 $200$ 年古希腊数学家阿波罗尼奥斯（Apollonius of Perga）在《圆锥曲线论》中，把圆锥的不同截面整理为椭圆、抛物线、双曲线三族，并给了它们沿用至今的名字。$1638$ 年伽利略（Galileo Galilei）在《两门新科学的对话》中用实验证明：忽略空气阻力时，抛出物（平抛、斜抛）的轨迹都是抛物线。同一时期笛卡尔（Descartes）用坐标把它代数化为 $y = a x^2 + b x + c$，几何和代数在这里再次合流。
]

#discovery[
  *最朴素的二次：$y = x^2$*

  先丢掉所有系数，只看 $y = x^2$。列表：

  #align(center, table(
    columns: 9,
    align: center,
    stroke: 0.4pt,
    [$x$], [$-4$], [$-3$], [$-2$], [$-1$], [$0$], [$1$], [$2$], [$3$],
    [$y$], [$16$], [$9$], [$4$], [$1$], [$0$], [$1$], [$4$], [$9$],
  ))

  三件事立刻显眼：

  - *左右对称*：$x$ 与 $-x$ 对应的 $y$ 相同（$(-2)^2 = 2^2 = 4$）。对称轴是 $y$ 轴。
  - *最低点在 $x = 0$*：其余位置 $y > 0$。最低点叫*顶点*，这里是 $(0, 0)$。
  - *图像向两边无限升起*：$|x|$ 大 $=>$ $y$ 大。

  画出来：

  #figure(
    cetz.canvas(length: 0.4cm, {
      import cetz.draw: *
      line((-5, 0), (5, 0), stroke: 0.4pt, mark: (end: ">"))
      line((0, -1), (0, 10), stroke: 0.4pt, mark: (end: ">"))
      content((5.3, 0), $x$, anchor: "west")
      content((0, 10.3), $y$, anchor: "south")
      let samples = range(0, 81).map(i => {
        let x = -4 + 0.1 * i
        (x, x * x)
      })
      for i in range(0, samples.len() - 1) {
        line(samples.at(i), samples.at(i + 1), stroke: 0.7pt + rgb("#1976D2"))
      }
      circle((0, 0), radius: 0.14, fill: rgb("#B71C1C"))
      content((0.5, -0.5), text(7pt)[顶点 $(0, 0)$])
    }),
    caption: [$y = x^2$ 的图像：一条关于 $y$ 轴对称的*抛物线*，顶点在原点，开口朝上。]
  )

  这种左右对称的弯曲轨迹，正式名字叫*抛物线*。

  *引入 $a$：控制开口方向与“胖瘦”*

  看 $y = a x^2$：

  - $a > 0$：开口朝上，顶点是最低点；
  - $a < 0$：开口朝下（图像颠倒），顶点是最高点；
  - $|a|$ 大：开口窄（“瘦”）；$|a|$ 小：开口宽（“胖”）；
  - $a = 0$：退化为 $y = 0$，不再是二次，排除。

  #figure(
    cetz.canvas(length: 0.35cm, {
      import cetz.draw: *
      line((-6, 0), (6, 0), stroke: 0.4pt, mark: (end: ">"))
      line((0, -6), (0, 8), stroke: 0.4pt, mark: (end: ">"))
      content((6.2, 0), $x$, anchor: "west")
      content((0, 8.3), $y$, anchor: "south")
      // y = 0.3 x^2 (wide/fat, light blue)
      let s1 = range(0, 101).map(i => {
        let x = -5 + 0.1 * i
        (x, 0.3 * x * x)
      })
      for i in range(0, s1.len() - 1) {
        if s1.at(i).at(1) <= 8 and s1.at(i + 1).at(1) <= 8 {
          line(s1.at(i), s1.at(i + 1), stroke: 0.5pt + rgb("#90CAF9"))
        }
      }
      content((5, 7.5), text(6pt)[$a = 0.3$])
      // y = x^2 (medium, blue)
      let s2 = range(0, 101).map(i => {
        let x = -5 + 0.1 * i
        (x, x * x)
      })
      for i in range(0, s2.len() - 1) {
        if s2.at(i).at(1) <= 8 and s2.at(i + 1).at(1) <= 8 {
          line(s2.at(i), s2.at(i + 1), stroke: 0.6pt + rgb("#1976D2"))
        }
      }
      content((2.5, 7.5), text(6pt)[$a = 1$])
      // y = -x^2 (opening down, red)
      let s3 = range(0, 101).map(i => {
        let x = -5 + 0.1 * i
        (x, -1 * x * x)
      })
      for i in range(0, s3.len() - 1) {
        if s3.at(i).at(1) >= -6 and s3.at(i + 1).at(1) >= -6 {
          line(s3.at(i), s3.at(i + 1), stroke: 0.6pt + rgb("#B71C1C"))
        }
      }
      content((2.5, -5.5), text(6pt)[$a = -1$])
      circle((0, 0), radius: 0.12, fill: black)
    }),
    caption: [$a > 0$ 开口朝上，$a < 0$ 开口朝下。$|a|$ 小（如 $0.3$）开口宽，$|a|$ 大开口窄。]
  )

  *顶点式：给抛物线“搬家”*

  $y = x^2$ 的顶点在原点。若把图像*整体*左右移 $h$、上下移 $k$，顶点就落到 $(h, k)$。回忆桥梁层的平移直觉（#secref("cb03-plotting-transforming")，下方有速成）——把公式里的 $x$ 换成 $(x - h)$、再整体加 $k$：

  $ y = a (x - h)^2 + k. $

  这个形式叫*顶点式*。好处是*顶点坐标 $(h, k)$ 直接写在式子里*；对称轴是铅垂线 $x = h$。

  #figure(
    cetz.canvas(length: 0.35cm, {
      import cetz.draw: *
      line((-3, 0), (8, 0), stroke: 0.4pt, mark: (end: ">"))
      line((0, -2), (0, 8), stroke: 0.4pt, mark: (end: ">"))
      content((8.2, 0), $x$, anchor: "west")
      content((0, 8.3), $y$, anchor: "south")
      // y = x^2 (original, dashed)
      let s1 = range(0, 61).map(i => {
        let x = -3 + 0.1 * i
        (x, x * x)
      })
      for i in range(0, s1.len() - 1) {
        if s1.at(i).at(1) <= 8 and s1.at(i + 1).at(1) <= 8 {
          line(s1.at(i), s1.at(i + 1), stroke: (paint: luma(150), thickness: 0.5pt, dash: "dashed"))
        }
      }
      content((-2, 4.5), text(6pt)[$y = x^2$], anchor: "east")
      // y = (x-3)^2 (shifted right by 3, dotted blue)
      let s2 = range(0, 81).map(i => {
        let x = -1 + 0.1 * i
        (x, (x - 3) * (x - 3))
      })
      for i in range(0, s2.len() - 1) {
        if s2.at(i).at(1) <= 8 and s2.at(i + 1).at(1) <= 8 {
          line(s2.at(i), s2.at(i + 1), stroke: (paint: rgb("#1976D2"), thickness: 0.5pt, dash: "dotted"))
        }
      }
      content((5.5, 5), text(6pt)[$y = (x - 3)^2$], anchor: "west")
      // y = (x-3)^2 + 2 (shifted right 3, up 2, solid green)
      let s3 = range(0, 81).map(i => {
        let x = -1 + 0.1 * i
        (x, (x - 3) * (x - 3) + 2)
      })
      for i in range(0, s3.len() - 1) {
        if s3.at(i).at(1) <= 8 and s3.at(i + 1).at(1) <= 8 {
          line(s3.at(i), s3.at(i + 1), stroke: 0.6pt + rgb("#2E7D32"))
        }
      }
      content((5.8, 3), text(6pt)[$y = (x - 3)^2 + 2$], anchor: "west")
      // mark vertices
      circle((0, 0), radius: 0.12, fill: luma(150))
      circle((3, 0), radius: 0.12, fill: rgb("#1976D2"))
      circle((3, 2), radius: 0.12, fill: rgb("#2E7D32"))
      // arrows showing shifts
      line((0.3, 0), (2.7, 0), stroke: 0.4pt + rgb("#FF9800"), mark: (end: ">"))
      content((1.5, -0.5), text(6pt)[右移 $3$])
      line((3, 0.3), (3, 1.7), stroke: 0.4pt + rgb("#FF9800"), mark: (end: ">"))
      content((3.5, 1), text(6pt)[上移 $2$])
    }),
    caption: [顶点式的“搬家”过程：$y = x^2$ 先右移 $3$ 变成 $y = (x - 3)^2$，再上移 $2$ 变成 $y = (x - 3)^2 + 2$，顶点从 $(0, 0)$ 搬到 $(3, 2)$。]
  )

  *一般式：展开顶点式*

  把 $y = a (x - h)^2 + k$ 展开：

  $ y = a x^2 - 2 a h x + (a h^2 + k) = a x^2 + b x + c, $

  其中 $b = -2 a h$、$c = a h^2 + k$。这条公式叫*一般式* $y = a x^2 + b x + c$（要求 $a != 0$）。

  #side-hack[
    *☞ 工具速成——本节用到的三把钥匙*

    *A. 完全平方公式*

    $ (a + b)^2 = a^2 + 2 a b + b^2, quad (a - b)^2 = a^2 - 2 a b + b^2. $

    验证：$(3 + 2)^2 = 9 + 12 + 4 = 25$ ✓ 。$(5 - 1)^2 = 25 - 10 + 1 = 16$ ✓ 。

    *B. 配方法*（把 $x^2 + p x$ 凑成完全平方）

    诀窍：加上再减去 $(p / 2)^2$，就能凑出 $(x + p / 2)^2$：

    $ x^2 + p x = x^2 + p x + (p / 2)^2 - (p / 2)^2 = (x + p / 2)^2 - (p / 2)^2. $

    例 1：$x^2 + 6 x + 2 = (x^2 + 6 x + 9) - 9 + 2 = (x + 3)^2 - 7$。

    例 2：$2 x^2 - 8 x + 5 = 2 (x^2 - 4 x) + 5 = 2 (x^2 - 4 x + 4 - 4) + 5 = 2 (x - 2)^2 - 3$。

    *C. 图形平移*（来自 #secref("cb03-plotting-transforming")）

    把 $y = f(x)$ 的图像向右移 $h$、向上移 $k$：在公式里把 $x$ 换成 $(x - h)$，再加 $k$。例如 $y = x^2$ 向右 $2$、向上 $3$，得 $y = (x - 2)^2 + 3$。
  ]

  *从一般式反推顶点：配方法*

  给一条 $y = a x^2 + b x + c$，怎样读出顶点 $(h, k)$？用上面速成的*配方法*：

  $ y = a x^2 + b x + c = a (x^2 + b / a x) + c = a (x + b / (2 a))^2 - b^2 / (4 a) + c. $

  对比顶点式 $y = a (x - h)^2 + k$：

  $ h = -b / (2 a), quad k = c - b^2 / (4 a). $

  也就是说——

  *一般式 $y = a x^2 + b x + c$ 的对称轴是 $x = -b / (2 a)$*，顶点纵坐标 $k$ 把 $h$ 代回原式算 $y$ 即可。

  *具体例*：$y = 2 x^2 - 8 x + 5$。

  - 对称轴：$x = -(-8) / (2 times 2) = 2$。
  - 顶点纵坐标：代 $x = 2 => y = 2 times 4 - 16 + 5 = -3$。顶点 $(2, -3)$。
  - $a = 2 > 0$ 开口朝上，顶点是*最低点*。
  - 顶点式重写：$y = 2 (x - 2)^2 - 3$。

  *画抛物线的五点速写*

  + 对称轴 $x = h$ 标铅垂虚线。
  + 顶点 $(h, k)$ 标上。
  + 顶点两侧各取 $1 - 2$ 个 $x$ 值（用对称性一次配两个点）。
  + 连光滑曲线。
]

#tryit[
  + 下列函数的顶点与对称轴：
    - (a) $y = (x - 3)^2 + 1$
    - (b) $y = -2 (x + 1)^2 + 5$
  + 把 $y = x^2 - 6 x + 10$ 化成顶点式（用配方法）。顶点与对称轴是什么？
]

#blueprint[
  *工具一：二次函数 $y = a x^2 + b x + c$（$a != 0$）*

  *定义*：最高次为 $x^2$ 的函数，形如一般式 $y = a x^2 + b x + c$ 或顶点式 $y = a (x - h)^2 + k$。图像都是一条*抛物线*。

  *工具二：顶点式 $y = a (x - h)^2 + k$*

  - 顶点 $= (h, k)$；对称轴 $x = h$。
  - $a > 0$ 开口朝上，顶点是最低点（最小值 $y_min = k$）。
  - $a < 0$ 开口朝下，顶点是最高点（最大值 $y_max = k$）。
  - $|a|$ 大 $=>$ 瘦，$|a|$ 小 $=>$ 胖。

  *工具三：对称轴 $x = -b / (2 a)$*

  从一般式读顶点的公式，由上面速成的*配方法*推得：

  $ h = -b / (2 a), quad k = c - b^2 / (4 a). $

  也就是说：先算 $h = -b / (2 a)$，再代回原式算 $k = y(h)$。

  *综合例*：$y = -x^2 + 4 x + 5$。

  - $a = -1, b = 4, c = 5$。对称轴 $x = -4 / (2 times (-1)) = 2$。
  - 代入 $x = 2$：$y = -4 + 8 + 5 = 9$。顶点 $(2, 9)$。
  - $a < 0$ 开口朝下 $=>$ 顶点为*最大值*点，最大值 $= 9$。
  - 顶点式：$y = -(x - 2)^2 + 9$。
]

#pitfall[
  *高频错误*

  ✗ 套顶点式时 $(x + 3)$ 读作 $h = 3$
  → ✓ 顶点式是 $(x - h)^2$；$(x + 3) = (x - (-3))$，故 $h = -3$。看到加号就反号。

  ✗ 用对称轴公式时丢 $a$ 的符号：$y = -2 x^2 + 8 x - 1$ 写成 $x = -8 / (2 times 2)$
  → ✓ $a$ 取原式里的 $-2$，$x = -8 / (2 times (-2)) = 2$。别自动把 $a$ 当正。

  ✗ 把 $y_min$ 或 $y_max$ 直接当顶点横坐标
  → ✓ 顶点两坐标不同：横坐标是 $h$、纵坐标是 $k$；最值是 $k$（当且仅当 $a > 0$ 时为 $y_min$，$a < 0$ 时为 $y_max$）。

  ✗ 把 $y = 3 (x - 1)^2 - 4$ 的顶点写成 $(1, 4)$
  → ✓ 顶点是 $(h, k) = (1, -4)$，注意 $k$ 保留原号。

  ✗ 抛物线画成像 V 形或 U 形尖角
  → ✓ 抛物线是*光滑*曲线，在顶点处是一个平缓的转折，不带尖角。
]

#mastery[
  *基础*

  + 指出下列抛物线的顶点、对称轴、开口方向。
    - (a) $y = (x - 2)^2 + 1$
    - (b) $y = -3 (x + 4)^2 - 2$
    - (c) $y = 2 x^2$
  + 画出 $y = (x - 1)^2 - 4$ 的图像（标出顶点、对称轴、与 $x / y$ 轴的交点）。
  + 把 $y = x^2 + 6 x + 5$ 用配方法化成顶点式。顶点在哪？

  *应用*

  + 一个抛物形喷泉的水柱离地高度 $h$（米）与水平距离 $x$（米）的关系为 $h = -0.5 x^2 + 2 x$。求喷到最高时的位置、最高高度、落回地面时的 $x$。
  + 某商品售价 $p$ 元时每天能卖 $y = -2 p^2 + 40 p$ 件。$p$ 为多少时销量最大？最大销量多少？
  + $y = 2 x^2 - 4 x - 6$。求顶点、最小值、与 $x$ 轴的交点（令 $y = 0$ 解方程）。

  *挑战 ☞ 选做*

  + 抛物线过三个点 $(0, 3)$、$(1, 0)$、$(2, -1)$。用一般式 $y = a x^2 + b x + c$ 解出 $a, b, c$。
  + 一块矩形围栏沿墙围出，墙为一边、另三边用篱笆 $24$ 米，设宽 $x$ 米。求面积 $S(x)$ 的公式、定义域、最大面积及对应宽度。
  + 二次函数 $y = a x^2 + b x + c$ 的顶点式为 $y = 2 (x - 3)^2 - 5$。展开成一般式。再验证“对称轴 $x = -b / (2 a) = 3$”。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + (a) 顶点 $(2, 1)$；对称轴 $x = 2$；开口朝上（顶点式读法）。
    (b) 顶点 $(-4, -2)$；对称轴 $x = -4$；开口朝下（顶点式读法：$(x + 4) = (x - (-4))$）。
    (c) 顶点 $(0, 0)$；对称轴 $x = 0$（即 $y$ 轴）；开口朝上（顶点式读法）。
  + 顶点 $(1, -4)$，对称轴 $x = 1$，开口朝上（顶点式读法）。与 $y$ 轴交：$x = 0 => y = -3$，$(0, -3)$；与 $x$ 轴交：$(x - 1)^2 = 4 => x = -1$ 或 $3$，$(-1, 0)$、$(3, 0)$。
  + 配方：$x^2 + 6 x + 5 = (x + 3)^2 - 4$（配方法）。顶点式 $y = (x + 3)^2 - 4$，顶点 $(-3, -4)$。

  *应用*
  + 对称轴 $x = -2 / (2 times (-0.5)) = 2$（对称轴公式 $x = -b / (2a)$），顶点 $h = -0.5 times 4 + 4 = 2$（最高高度 $2$ 米）。落地 $h = 0 => x(-0.5 x + 2) = 0 => x = 0$（起点）或 $x = 4$（令 $y = 0$ 解方程求落点）。
  + 对称轴 $p = -40 / (2 times (-2)) = 10$（对称轴公式 $x = -b / (2a)$）。最大销量 $= -2 times 100 + 400 = 200$ 件（$a < 0$，顶点为最大值）。
  + 对称轴 $x = -(-4) / (2 times 2) = 1$（对称轴公式 $x = -b / (2a)$），顶点 $y = 2 - 4 - 6 = -8 =>$ 顶点 $(1, -8)$，最小值 $-8$。与 $x$ 轴交：$2 x^2 - 4 x - 6 = 0 => x^2 - 2 x - 3 = 0 => (x - 3)(x + 1) = 0 => x = 3, -1$，交点 $(3, 0), (-1, 0)$。

  *挑战 ☞ 选做*
  + 先由 $x = 0$ 得 $c = 3$。代 $(1, 0)$：$a + b + 3 = 0 => a + b = -3$。代 $(2, -1)$：$4 a + 2 b + 3 = -1 => 2 a + b = -2$。两式相减得 $a = 1$，$b = -4$（代点列方程组求 $a, b, c$）。$y = x^2 - 4 x + 3$。
  + 围栏 $2 x + "长" = 24 =>$ 长 $= 24 - 2 x$。$S(x) = x (24 - 2 x) = -2 x^2 + 24 x$。对称轴 $x = -24 / (2 times (-2)) = 6$（对称轴公式 $x = -b / (2a)$）。$S_max = S(6) = -72 + 144 = 72$ 平方米，宽 $6$ 米、长 $12$ 米。
  + 展开：$2 (x - 3)^2 - 5 = 2 x^2 - 12 x + 13$（配方法逆向展开）。$a = 2, b = -12$。对称轴 $x = -(-12) / (2 times 2) = 3$ ✓。
]
