#import "../../lib/theme-v2.typ": chapter-nav, chapter-route, side-hack, secref

== 反比例 · 二次函数 <ch:inverse-proportion-quadratic-functions>

一块披萨分给越多人，每人拿到的就越少——但这种"越多越少"并不是一条直线。还有喷泉的水流：先升后降，轨迹是一条弧。这一章要认识两种弯曲的函数，它们的图像告诉你：不是所有变化都走直线。

#chapter-nav[
  *难度*：★★★★★ \
  *核心问题*：除了“每加 $1$ 单位 $x$ 加固定量”的直线一族外，还有两类非常常见的函数：*乘积不变*（反比例 $y = k / x$）和*左右对称、先升后降或先降后升*（二次 $y = a x^2 + b x + c$）。它们的公式、图像、数据指纹各是什么？拿到一张数据表如何快速判断它出自哪一族？ \
  *需要先会*：完全平方公式与配方法（本章 §2 有速成）、分母不能为零的直觉（#secref("ch:fractions") 已建立）、平移与坐标变换的直觉（#secref("cb04-plotting-transforming")，本章 §2 有速成）、#secref("fn05-linear-function")（一次函数）、#secref("fn06-slope-as-rate")（斜率 / 变化率）。 \
  *本章在全书的位置*：函数分支*第二类 & 第三类具体函数族*——反比例由“乘积恒定”抽象出来；二次由“先增后减”与“先减后增”的左右对称轨迹抽象出来，并借助已学的配方法把一般式 $y = a x^2 + b x + c$ 与顶点式 $y = a (x - h)^2 + k$ 打通。
]

#chapter-route[

  + §1 *反比例函数* $y = k / x$：从“$12$ 个苹果分给 $x$ 人”出发，抽象出“乘积不变”的关系；图像是两支双曲线，定义域必须挖掉 $x = 0$。
  + §2 *二次函数 / 抛物线*：从“喷泉 / 抛石”先升后降开始。先给最朴素的 $y = x^2$，再引入 $a$（开口方向与胖瘦）、$(h, k)$（顶点式 $y = a (x - h)^2 + k$）、最后用*配方法*（本章自带速成）把一般式 $y = a x^2 + b x + c$ 与顶点式打通，并推出对称轴 $x = -b / (2 a)$。
  + §3 *三类函数对照*：面对一张数据表，用“一阶差恒定 $=>$ 一次；二阶差恒定 $=>$ 二次；乘积恒定 $=>$ 反比例”这三条指纹快速识别。
]

#side-hack[
  *入章自检*——以下几道小题只用前面章节学过的知识。如果卡住，请先回到上面"需要先会"的链接补一补。

  + 展开 $(x - 3)^2$ 与 $(x + 5)^2$。再问：$(x - 3)^2$ 与 $x^2 - 9$ 是同一个东西吗？为什么？
  + $y = x^2$ 的图像向*右*平移 $2$、向*上*平移 $3$ 后，新顶点在哪里？新解析式怎么写？请把 $x$ 替换成 $(x - ?)$ 这一步*独立*写出来，再加 $k$。
  + 直线 $y = -2x + 4$ 与 $x$ 轴的交点在哪？与 $y$ 轴的交点在哪？*斜率* $-2$ 在图像上代表"$x$ 每加 $1$、$y$ 加几"？
]

#side-hack[
  *☞ 上一章吃力？*

  如果你做 #secref("ch:direct-variation-linear-functions-slope") 的"两点求解析式 / 斜率公式"题感觉吃力，先回去把 #secref("fn05-linear-function") 的*平移直觉*（$y = k x + b$ $=$ $y = k x$ 上下平移 $b$）再走 $2$ 道题。本章 §2 把抛物线 $y = a x^2$ 平移成 $y = a (x - h)^2 + k$，用的是同一种"在公式里替换 $x$ 与加常数"的手法。
]

#include "01-inverse-proportion.typ"
#include "02-parabolas-and-quadratics.typ"
#include "03-comparing-function-families.typ"
