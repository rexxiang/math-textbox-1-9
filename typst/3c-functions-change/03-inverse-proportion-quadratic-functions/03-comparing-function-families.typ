#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref, answer-cut
#import "../../lib/diagram-packages.typ": cetz

=== 三类函数对照：一次 / 反比例 / 二次 <tool:fn09-comparing-function-families>

#vocab[函数族 function family][模型识别 model recognition]

#crisis[
  $3$ 张常见数据表摆在你面前——但没人告诉你它们出自什么现象：

  #align(center, table(
    columns: 5,
    align: center,
    stroke: 0.4pt,
    [*情景 A*], [$x$], [$1$], [$2$], [$3$],
    [], [$y$], [$4$], [$7$], [$10$],
  ))

  #align(center, table(
    columns: 5,
    align: center,
    stroke: 0.4pt,
    [*情景 B*], [$x$], [$1$], [$2$], [$3$],
    [], [$y$], [$24$], [$12$], [$8$],
  ))

  #align(center, table(
    columns: 5,
    align: center,
    stroke: 0.4pt,
    [*情景 C*], [$x$], [$1$], [$2$], [$3$],
    [], [$y$], [$2$], [$8$], [$18$],
  ))

  三张表都在“$x$ 变了、$y$ 变了”——但*变化的结构*完全不同。如果我们能从表里就分辨出它是哪一族函数，就能迅速使用对应工具。

  *一次、反比例、二次三族各有什么可辨认的“指纹”？怎样*一眼*判断一张表出自哪一族？*
]

#tryit[
  先动手试试，下面再读。

  判断下列表分别出自哪一族，并写出解析式。
  - (a) $x: 1, 2, 3, 4$；$y: 3, 6, 9, 12$
  - (b) $x: 1, 2, 3, 4$；$y: 1, 4, 9, 16$
  - (c) $x: 1, 2, 3, 4$；$y: 12, 6, 4, 3$
]

#discovery[
  *一次的指纹：$y$ 的*相邻差*恒定*

  $y = k x + b$ 里 $x$ 每加 $1$、$y$ 加 $k$。所以在*$x$ 等差*的表里，$y$ 的一阶差（相邻两行 $y$ 的差）恒 $= k$。

  情景 A：$y$ 相邻差 $= 3, 3$——恒定 $=>$ *一次*。$k = 3$，$y(1) = 4 = 3 + b => b = 1$。$y = 3 x + 1$。

  *反比例的指纹：$x times y$ 恒定*

  $y = k / x$ 即 $x y = k$。每行 $x, y$ 乘起来都等于同一个常数。

  情景 B：$1 times 24 = 24$、$2 times 12 = 24$、$3 times 8 = 24$——恒定 $=>$ *反比例*，$k = 24$，$y = 24 / x$。

  *二次的指纹：$y$ 的*二阶差*恒定*

  $y = a x^2 + b x + c$ 在 $x$ 等差（步长 $h$）时满足“一阶差 $Delta y$ 是关于 $x$ 的一次函数”，而*二阶差* $Delta^2 y$（即 $Delta y$ 的相邻差）恒 $= 2 a h^2$。通俗说：*二阶差恒定* $<=>$ 是二次。

  *快速验证*：若 $y = x^2$，表 $x: 0, 1, 2, 3, 4$，$y: 0, 1, 4, 9, 16$。一阶差 $1, 3, 5, 7$；二阶差 $2, 2, 2$——恒定 $= 2 times 1 times 1^2 = 2 a h^2$ 符合。

  情景 C：$y$ 一阶差 $= 6, 10$，二阶差 $= 4$——只有一项，再补算：如果 $x = 0$ 有 $y = 0$（常见试探），并补 $x = 4, y = ?$ 用二次假设验证。直接拟合：令 $y = a x^2 + b x + c$。代 $(1, 2), (2, 8), (3, 18)$：$a + b + c = 2$；$4 a + 2 b + c = 8$；$9 a + 3 b + c = 18$。后两减前：$3 a + b = 6$；$5 a + b = 10$。再减：$2 a = 4 => a = 2$，$b = 0$，$c = 0$。故 $y = 2 x^2 =>$ *二次*。

  *三族识别流程*

  给一张 $x$ 等差的数据表：

  + 算一阶差 $Delta y$。若恒定 $=>$ *一次*。
  + 否则再算二阶差 $Delta^2 y$。若恒定 $=>$ *二次*。
  + 都不恒定？算 $x times y$。若恒定 $=>$ *反比例*。
  + 再不符合？可能是更复杂的模型——本章暂不处理。

  #figure(
    cetz.canvas(length: 0.4cm, {
      import cetz.draw: *
      // Panel 1: Linear - constant differences
      content((-7, 3.5), text(8pt)[*一次*], anchor: "west")
      // bar-like steps showing constant delta y
      for i in range(0, 4) {
        let x = -7 + i * 2
        rect((x, 0), (x + 1.5, 1 + i), stroke: 0.3pt, fill: rgb("#BBDEFB"))
      }
      // delta y arrows
      for i in range(0, 3) {
        let x = -7 + i * 2 + 1.7
        line((x, 1 + i), (x, 2 + i), stroke: 0.4pt + rgb("#1976D2"), mark: (end: ">"))
        content((x + 0.3, 1.5 + i), text(5pt)[$+k$])
      }

      // Panel 2: Inverse - constant product
      content((3, 3.5), text(8pt)[*反比例*], anchor: "west")
      let widths = (4, 2, 1.33, 1)
      let heights = (1, 2, 3, 4)
      for i in range(0, 4) {
        let x = 3 + i * 1.5
        rect((x, 0), (x + 1, heights.at(i)), stroke: 0.3pt, fill: rgb("#FFCDD2"))
      }
      content((5, -0.6), text(6pt)[$x y = k$])
    }),
    caption: [一次函数的指纹：相邻输出差 $Delta y$ 恒定（每阶等高增长）；反比例的指纹：$x y$ 乘积恒定（柱面积不变）。]
  )

  *三族图像速览*

  #figure(
    cetz.canvas(length: 0.35cm, {
      import cetz.draw: *
      line((-6, 0), (8, 0), stroke: 0.4pt, mark: (end: ">"))
      line((0, -5), (0, 8), stroke: 0.4pt, mark: (end: ">"))
      content((8.2, 0), $x$, anchor: "west")
      content((0, 8.3), $y$, anchor: "south")
      // linear: y = 3x + 1
      line((-2, -5), (2.3, 7.9), stroke: 0.7pt + rgb("#1976D2"))
      content((2.5, 7.5), text(6pt)[一次], anchor: "west")
      // inverse: y = 6/x (quadrant I only for clarity)
      let s1 = range(0, 55).map(i => {
        let x = 0.8 + 0.1 * i
        (x, 6.0 / x)
      })
      for i in range(0, s1.len() - 1) {
        if s1.at(i).at(1) <= 8 {
          line(s1.at(i), s1.at(i + 1), stroke: 0.7pt + rgb("#B71C1C"))
        }
      }
      // inverse quadrant III
      let s2 = range(0, 40).map(i => {
        let x = -5 + 0.1 * i
        (x, 6.0 / x)
      })
      for i in range(0, s2.len() - 1) {
        if s2.at(i).at(1) >= -5 {
          line(s2.at(i), s2.at(i + 1), stroke: 0.7pt + rgb("#B71C1C"))
        }
      }
      content((5, 1.5), text(6pt)[反比例], anchor: "west")
      // quadratic: y = 0.5 x^2
      let s3 = range(0, 81).map(i => {
        let x = -4 + 0.1 * i
        (x, 0.5 * x * x)
      })
      for i in range(0, s3.len() - 1) {
        if s3.at(i).at(1) <= 8 and s3.at(i + 1).at(1) <= 8 {
          line(s3.at(i), s3.at(i + 1), stroke: 0.7pt + rgb("#2E7D32"))
        }
      }
      content((-3.5, 7), text(6pt)[二次], anchor: "east")
      circle((0, 0), radius: 0.1, fill: black)
    }),
    caption: [一次（直线）、反比例（双曲线）、二次（抛物线）三族函数在同一坐标系中的图像对比。]
  )

  #align(center, table(
    columns: (auto, 1fr, 1fr, 1fr),
    inset: 6pt,
    stroke: 0.4pt,
    [], [*一次 $y = k x + b$*], [*反比例 $y = k / x$*], [*二次 $y = a x^2 + b x + c$*],
    [图像形状], [*直线*], [*两支双曲线*], [*抛物线*],
    [定义域], [$RR$], [${x | x != 0}$], [$RR$],
    [过原点？], [$b = 0$ 时过], [从不过（渐近于轴）], [$c = 0$ 时过],
    [对称？], [无（过顶点除外）], [关于原点中心对称], [关于 $x = -b / (2 a)$ 轴对称],
    [变化节奏], [每加 $1$ 加 $k$], [$x y = k$（积恒定）], [每加 $1$ 的加量*本身*再加 $2 a$（步长 $1$）],
    [典型现象], [匀速行进、起步价计费], [固定总量分配、波义耳定律], [抛体运动、最值优化],
  ))

  *在真实数据上小心*

  真实数据很少*精确*匹配某族。我们通常是*挑最接近*的那一族来模拟——这就进入了下一章*模型选择*的主题。
]

#tryit[
  + 下列情境哪一族最合适？
    - 一批快递派给 $x$ 名员工，平均每人要送 $y$ 单。
    - 正方形边长 $x$、面积 $y$。
    - 手机套餐每月 $20$ 元含 $2$ GB，超出每 GB 加 $5$ 元。
]

#blueprint[
  *工具：三族函数的识别法*

  #table(
    columns: (1fr, 1fr, 1fr),
    inset: 6pt,
    stroke: 0.4pt,
    [*模型*], [*公式*], [*指纹（$x$ 等差步长 $1$ 的表）*],
    [一次（#secref("fn05-linear-function")）], [$y = k x + b$], [$Delta y$ 恒 $= k$],
    [反比例（#secref("fn07-inverse-proportion")）], [$y = k / x$], [$x y$ 恒 $= k$],
    [二次（#secref("fn08-quadratic-function")）], [$y = a x^2 + b x + c$], [$Delta^2 y$ 恒 $= 2 a$],
  )

  *识别流程*

  + 表的 $x$ 是等差吗？不是就先补或重排。
  + 先算一阶差 $Delta y$。恒定 $=>$ 一次。
  + 否则算二阶差 $Delta^2 y$。恒定 $=>$ 二次。
  + 都不恒定 $=>$ 看 $x y$。恒定 $=>$ 反比例。
  + 以上都不满足 $=>$ 需要更复杂模型（下一章引入）。

  *典型应用场景*

  - *一次*：匀速、固定费率、起步价 $+$ 单价计费。
  - *反比例*：*固定总量在多人 / 多天间分配*、力臂杠杆、气体压强与体积（恒温）。
  - *二次*：抛体轨迹、利润 / 面积等*最值优化*、二次关系（距离与刹车反应、重力下落距离）。
]

#pitfall[
  *高频错误*

  ✗ 只看“$y$ 增大”就认定是一次
  → ✓ 反比例里 $x$ 负时 $y$ 也能随 $x$ 增大；二次里也可能先增后减。*形状 / 节奏*才是关键，不是单调方向。

  ✗ 把“$y$ 与 $x$ 成反比”误写成 $y = k - x$
  → ✓ “反比例”在数学里特指乘积恒定，不是“一个加大另一个减小”的泛化直觉。必须是 $y = k / x$。

  ✗ 用 $x$ 不等差的表算一阶差判模型
  → ✓ $x$ 必须等差。否则先折算 $Delta y / Delta x$（这就是斜率）再比较。

  ✗ 只算一次一阶差就判“恒定” = 一次
  → ✓ 至少要*两个*一阶差都相等才有说服力。一张四行表会给出 $3$ 个一阶差；它们三者都相等时才算“恒定”。

  ✗ 看到 $y = x^2 + 5$ 因为多项带 $+ 5$ 就认为“不是二次”
  → ✓ 只看最高次项：$x^2$ 存在 $=>$ 二次；常数项和一次项不影响族别。
]

#mastery[
  *基础*

  + 判断下列表出自哪一族，并写出公式：
    - (a) $x: 1, 2, 3, 4$；$y: 5, 3, 1, -1$
    - (b) $x: 1, 2, 4, 8$；$y: 24, 12, 6, 3$
    - (c) $x: 0, 1, 2, 3$；$y: 0, 3, 8, 15$
  + 给下列函数的解析式各配一个现实情境：
    - $y = 10 / x$
    - $y = 5 x$
    - $y = -x^2 + 4 x$
  + 一张表，$x: -2, -1, 0, 1, 2$；$y: 5, 2, 1, 2, 5$。它*一定*是二次函数吗？为什么？若是，写出它。

  *应用*

  + $100$ 元买糖：每颗 $x$ 元，共能买 $y$ 颗。用哪族函数？写公式。
  + 自由落体：$t$ 秒下落 $y = 1 / 2 g t^2$（$g = 10$）。用哪族？写公式并求 $3$ 秒后下落了多少米。
  + 某图书馆借书：办卡 $15$ 元，每借一次加 $2$ 元。用哪族？写公式。

  *挑战 ☞ 选做*

  + 给表 $x: 1, 2, 3$、$y: 2, 5, 10$。它可能出自几族？分别写出候选公式，比较图像差异。
  + 下表：$x: 1, 2, 3, 4, 5$；$y: 10, 5, 3.33, 2.5, 2$。判族，写公式。验证 $x = 10$ 的预测。
  + 一批 $x$ 张桌子围一圈，每桌坐 $y$ 人，总座位数固定为 $24$。写 $y = f(x)$；再写“每桌人均重量（$72$ 斤 $div y$）”为新函数 $g(y)$。$g$ 是不是函数 $g(x)$？若是用 $y$ 替换表示 $x$ 作自变量。
  + *反直觉*：表 $(1, 2)$、$(2, 5)$、$(3, 10)$ 既符合 $y = x^2 + 1$，*也*符合 $y = (x - 1)(x - 2)(x - 3) + x^2 + 1$。请代入验证三点。这说明什么？
  + *跨知识*：表 $x: 1, 2, 3$；$y: 6, 12, 24$。判族（提示：相邻比是否恒定？）；写公式。验证 $x = 5$ 时 $y$ 的值。（跨乘法增长）
  + *构造题*：构造一组数据（至少 $4$ 组 $(x, y)$），使其符合 $y = a x + b$（$b != 0$）但*不符合* $y = a x$。请说明 $b$ 在族分类中的作用。

  #answer-cut[

  *基础*
  + (a) $Delta y = -2$ 恒定 $=>$ 一次（一阶差恒定 → 一次），$y = -2 x + 7$。
    (b) $x$ 不等差——但 $x y$：$24, 24, 24, 24$ 都是 $24 =>$ 反比例（$x y$ 恒定 → 反比例），$y = 24 / x$。
    (c) $Delta y = 3, 5, 7$ 不恒；$Delta^2 y = 2, 2$ 恒 $=>$ 二次（二阶差恒定 → 二次）。$a = 1, b = 2, c = 0$，$y = x^2 + 2 x$。
  + $y = 10 / x$：$10$ 元分给 $x$ 位同学，每位得 $y$ 元（$x y$ 恒定 → 反比例）；$y = 5 x$：每本书 $5$ 元、买 $x$ 本共 $y$ 元（一阶差恒定 → 一次）；$y = -x^2 + 4 x$：某商品定价 $x$ 元时一天销售额的模型（二阶差恒定 → 二次，$x = 2$ 时最大）。
  + $Delta y = -3, -1, 1, 3$，$Delta^2 y = 2, 2, 2$ 恒 $=>$ 是二次（二阶差恒定 → 二次）。顶点 $(0, 1)$，代 $x = 1, y = 2$ 得 $a = 1$，$y = x^2 + 1$。

  *应用*
  + 反比例 $y = 100 / x$（$x y$ 恒定 → 反比例）。
  + 二次 $y = 5 t^2$（二阶差恒定 → 二次）。$t = 3$：$y = 45$ 米。
  + 一次 $y = 15 + 2 x$（一阶差恒定 → 一次，$x$ 是借书次数）。

  *挑战 ☞ 选做*
  + 一阶差 $3, 5$；二阶差 $2$（只有一个，不能判恒定）。先算二阶差再尝试拟合（二阶差恒定 → 二次）：$y = x^2 + 1$（验 $x = 1: 2$ ✓、$x = 2: 5$ ✓、$x = 3: 10$ ✓）。在本章范围内*二次最匹配*。
  + 一阶差不恒定；$x y: 10, 10, 9.99, 10, 10$（约）$= 10$ $=>$ *反比例*（$x y$ 恒定 → 反比例），$y = 10 / x$。$x = 10$：$y = 1$。
  + $y = 24 / x$（$x y$ 恒定 → 反比例）。$g(y) = 72 / y$——若把 $y$ 作为自变量就是 $g$ 的函数面孔。$g(x) = 72 / (24 / x) = 3 x$，反比例复合反比例变回正比例（一阶差恒定 → 一次）。
  + 验证：第一式 $y = x^2 + 1$。$x = 1: 2$ ✓、$x = 2: 5$ ✓、$x = 3: 10$ ✓。第二式：$x = 1: 0 + 2 = 2$ ✓（因为 $(1-1)(1-2)(1-3) = 0$）；$x = 2: 0 + 5 = 5$ ✓；$x = 3: 0 + 10 = 10$ ✓。说明：*仅 $3$ 个数据点不能唯一确定函数*——任何含因子 $(x-1)(x-2)(x-3)$ 的修正项都不影响这 $3$ 个值。要再加更多点才能在族里确定唯一公式（$n$ 点不能定 $n$ 阶以上的函数）。
  + 相邻比 $12 / 6 = 2$、$24 / 12 = 2$，比值恒定 $= 2 ->$ *乘法增长 / 指数族*（相邻比恒定 → 乘法）。公式 $y = 3 dot 2^x$（验 $x = 1: 6$ ✓、$x = 2: 12$ ✓、$x = 3: 24$ ✓）。$x = 5$：$y = 3 dot 32 = 96$（跨乘法增长）。
  + 例如：$x = 1, 2, 3, 4$；$y = 5, 7, 9, 11$（公式 $y = 2 x + 3$，截距 $b = 3$）。一阶差 $= 2$ 恒定，符合一次族（不是正比例：$x = 0$ 时 $y = 3 != 0$，违反 $y = a x$ 的"过原点"性质）。$b$ 的作用：*把直线沿 $y$ 轴平移*——$b = 0$ 时是正比例（过原点），$b != 0$ 时是一般一次（不过原点）。所以"是否过原点"是分判正比例与一般一次的关键（截距 $b$ → 平移 → 是否过原点）。
  ]
]
