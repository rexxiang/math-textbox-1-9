#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §7.3 二次函数 <sec-7-3>

#vocab[抛物线 parabola / 顶点 vertex / 对称轴 axis of symmetry / 顶点式 vertex form / 标准式 standard form / 配方法 completing the square]

#crisis[
  公园要设计一座喷泉。水从喷口斜向上喷出，在空中划出一条弧线后落回水池。

  工程师测得，水柱高度 $y$（米）与水平距离 $x$（米）大致满足：

  $ y = -0.5 x^2 + 2 x $

  现在最关心的不是“代一个 $x$ 算一个 $y$”，而是：

  + 水柱*最高*能到多高？
  + 最高点在什么位置？
  + 它会落在离喷口多远的地方？

  一次函数的直线图象显然不够用了。我们需要一种能描述“先上升再下降”的新函数。
]

#history-note[
  伽利略研究抛体运动时发现：忽略空气阻力，石头、炮弹、水柱这类“飞出去再落下来”的轨迹都像同一种曲线——*抛物线*。二次函数正是研究这种曲线最基础的工具。
]

#discovery[
  *第一步：先看几组数据。*

  把喷泉公式 $y = -0.5 x^2 + 2 x$ 代入几个简单的 $x$：

  #align(center, table(
    columns: 6,
    align: center,
    [$x$], [$0$], [$1$], [$2$], [$3$], [$4$],
    [$y$], [$0$], [$1.5$], [$2$], [$1.5$], [$0$],
  ))

  很有意思：

  - $x = 1$ 和 $x = 3$ 时高度相同
  - $x = 0$ 和 $x = 4$ 时高度都为 $0$
  - 最高点似乎出现在 $x = 2$ 附近

  *第二步：标准式会算值，但不直接告诉我们“最高点在哪”。*

  公式 $y = -0.5 x^2 + 2 x$ 当然能一个一个代数去试，但这很笨：如果公式更复杂，难道还要一直猜下去？

  我们需要一种写法，能把“最高点”直接显出来。

  *第三步：把平方凑出来。*

  先把 $-0.5$ 提出来：

  $ y = -0.5(x^2 - 4 x) $

  现在盯住括号里的 $x^2 - 4 x$。如果它能变成 $(x - 2)^2$，我们就知道平方最小时是什么情况。

  因为

  $ (x - 2)^2 = x^2 - 4 x + 4 $

  所以要“补上 $4$，再减去 $4$”：

  $ y
    &= -0.5(x^2 - 4 x + 4 - 4) \
    &= -0.5((x - 2)^2 - 4) \
    &= -0.5(x - 2)^2 + 2 $

  *第四步：最高点终于现身了。*

  因为任意平方都满足 $(x - 2)^2 >= 0$，所以

  $-0.5(x - 2)^2 <= 0$

  于是整个函数

  $y = -0.5(x - 2)^2 + 2$

  的最大值一定在 $(x - 2)^2 = 0$ 时取得，也就是 $x = 2$ 时。那时 $y = 2$。

  这说明：*把二次函数改写成“平方 + 常数”的形式后，最值就不再藏着掖着了。*
]

#tryit[
  先自己做一遍，再看蓝图：

  把 $y = x^2 - 6 x + 10$ 化成“平方 + 常数”的形式，并写出：

  + 顶点坐标
  + 对称轴
  + 最小值

  提示：先想想 $x^2 - 6 x$ 需要补上什么数，才能变成完整平方。
]

#side-hack[
  顶点式 $y = a(x - h)^2 + k$ 里，三个参数各管一件事：

  - $a$ 决定开口方向和“胖瘦”
  - $h$ 决定左右位置
  - $k$ 决定上下位置

  记忆时小心：括号里是 $x - h$，所以看到 $x + 1$ 时，实际上 $h = -1$。
]

#blueprint[
  *一、什么是二次函数？*

  形如

  $ y = a x^2 + b x + c quad (a != 0) $

  的函数叫做*二次函数*。它的图象是一条*抛物线*。

  这种写法叫*标准式*。它适合看系数，也适合和一元二次方程联系起来。

  *二、为什么要把它改写成顶点式？*

  标准式里，抛物线“开口朝哪儿”容易看，但“最高点或最低点在哪儿”往往不直观。

  顶点式

  $ y = a(x - h)^2 + k $

  会直接告诉我们：

  - 顶点是 $(h, k)$
  - 对称轴是 $x = h$
  - 当 $a > 0$ 时，顶点给出*最小值*
  - 当 $a < 0$ 时，顶点给出*最大值*

  *三、配方法：从标准式走到顶点式*

  对一般式 $y = a x^2 + b x + c$：

  $ y
    &= a x^2 + b x + c \
    &= a(x^2 + frac(b, a) x) + c \
    &= a((x + frac(b, 2 a))^2 - (frac(b, 2 a))^2) + c \
    &= a(x + frac(b, 2 a))^2 + c - frac(b^2, 4 a) $

  所以顶点坐标是

  $(-frac(b, 2 a), c - frac(b^2, 4 a))$

  对称轴是

  $ x = -frac(b, 2 a) $

  *四、抛物线的基本性质*

  对于 $y = a(x - h)^2 + k$：

  - 若 $a > 0$，开口向上，顶点处取最小值 $k$
  - 若 $a < 0$，开口向下，顶点处取最大值 $k$
  - $|a|$ 越大，开口越窄；$|a|$ 越小，开口越宽

  *五、二次函数与一元二次方程*

  若令 $y = 0$，就得到一元二次方程

  $ a x^2 + b x + c = 0 $

  它的解，就是抛物线与 $x$ 轴交点的横坐标。

  *例 1：回到喷泉问题。*

  $ y
    &= -0.5 x^2 + 2 x \
    &= -0.5(x^2 - 4 x) \
    &= -0.5(x - 2)^2 + 2 $

  所以顶点是 $(2, 2)$，最高点在距离喷口 $2$ 米处，高度为 $2$ 米。

  再令 $y = 0$：

  $-0.5 x^2 + 2 x = 0$

  $-0.5 x(x - 4) = 0$

  得 $x = 0$ 或 $x = 4$，所以水柱落在离喷口 $4$ 米处。

  *例 2：将 $y = 2 x^2 - 8 x + 5$ 化为顶点式。*

  $ y
    &= 2(x^2 - 4 x) + 5 \
    &= 2(x^2 - 4 x + 4 - 4) + 5 \
    &= 2(x - 2)^2 - 8 + 5 \
    &= 2(x - 2)^2 - 3 $

  所以顶点是 $(2, -3)$，对称轴是 $x = 2$，开口向上，最小值是 $-3$。

  *例 3：一个长方形周长固定为 $20$ 厘米，设长为 $x$，面积为 $y$。求面积最大值。*

  宽是 $10 - x$，所以

  $ y = x(10 - x) = -x^2 + 10 x $

  配方得

  $ y = -(x - 5)^2 + 25 $

  所以当 $x = 5$ 时，面积最大为 $25$ 平方厘米。也就是说：周长固定时，正方形面积最大。
]

#pitfall[
  *陷阱 1：配方前忘了先把 $a$ 提出来。*

  例如 $2 x^2 - 8 x$，不能直接脑补成 $(x - 4)^2$；要先写成 $2(x^2 - 4 x)$，再去补平方。

  *陷阱 2：看到 $(x + 1)^2$ 就把顶点横坐标写成 $1$。*

  其实 $(x + 1)^2 = (x - (-1))^2$，所以顶点横坐标是 $-1$。
]

#mastery[
  *基础*

  + 将下列函数化成顶点式，并写出顶点、对称轴、最值：
    - $y = x^2 - 6 x + 10$
    - $y = -3 x^2 + 12 x - 7$

  + 抛物线 $y = 2(x + 1)^2 - 5$ 的开口方向、顶点坐标、对称轴分别是什么？

  + 判断 $y = x^2 - 4 x + 5$ 与 $x$ 轴是否有交点，并说明理由。

  *应用*

  + 某小球竖直上抛，高度满足 $h = 30 t - 5 t^2$。求：
    - 何时到达最高点
    - 最高点高度
    - 何时落回地面

  + 某商品每件利润为 $(x - 20)$ 元，日销量为 $(80 - 2 x)$ 件。求日利润最大时的售价。

  *挑战*

  + 已知二次函数图象过点 $(0, 1)$、$(1, 0)$、$(-1, 4)$，求函数解析式。

  + 在抛物线 $y = -x^2 + 4 x$ 上找一点 $P$，使它到 $x$ 轴的距离最大。求 $P$ 的坐标。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*

  + $y = (x - 3)^2 + 1$，顶点 $(3, 1)$，对称轴 $x = 3$，最小值 $1$。 
    $y = -3(x - 2)^2 + 5$，顶点 $(2, 5)$，对称轴 $x = 2$，最大值 $5$。

  + 开口向上，顶点 $(-1, -5)$，对称轴 $x = -1$。

  + 没有交点。因为 $y = x^2 - 4 x + 5 = (x - 2)^2 + 1$，最小值是 $1 > 0$。

  *应用*

  + $h = -5(t - 3)^2 + 45$，所以 $t = 3$ 秒到达最高点，最高点高度 $45$ 米；令 $h = 0$，得 $5 t(6 - t) = 0$，落地时 $t = 6$ 秒。

  + 日利润 
    $y = (x - 20)(80 - 2 x) = -2 x^2 + 120 x - 1600 = -2(x - 30)^2 + 200$。
    所以售价 $30$ 元时日利润最大。

  *挑战*

  + 由 $(0, 1)$ 得 $c = 1$；由 $(1, 0)$ 得 $a + b + 1 = 0$；由 $(-1, 4)$ 得 $a - b + 1 = 4$。解得 $a = 1$，$b = -2$，所以 $y = x^2 - 2 x + 1$。

  + $y = -x^2 + 4 x = -(x - 2)^2 + 4$，最大值在 $x = 2$ 时取得，所以 $P = (2, 4)$。
]
