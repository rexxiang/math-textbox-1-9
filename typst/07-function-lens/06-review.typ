#import "../lib/theme-v2.typ": blueprint, mastery, secref

== 第七章回顾：函数——变化的望远镜

#blueprint[
  *知识速查卡*

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *一次函数*（#secref("7.1")）
      - $y = k x + b$（$k != 0$）
      - $k > 0$：递增；$k < 0$：递减
      - $k$ 为斜率，$b$ 为纵截距（$x=0$ 时的 $y$ 值）
      - 过两点求 $k$：$k = frac(y_2 - y_1, x_2 - x_1)$
      - 特殊：$b=0$ 时为正比例函数 $y = kx$
    ],
    block[
      *反比例函数*（#secref("7.2")）
      - $y = frac(k, x) = k x^{-1}$（$k != 0$，$x != 0$）
      - $k > 0$：图象在第一、三象限；$k < 0$：第二、四象限
      - 每个象限内 $y$ 随 $x$ 增大而减小（不论 $k$ 正负）
      - 两坐标轴为渐近线
    ],
  )

  #grid(columns: (1fr, 1fr), gutter: 12pt,
    block[
      *二次函数*（#secref("7.3")）
      - 一般式：$y = a x^2 + b x + c$（$a != 0$）
      - 顶点式：$y = a(x - h)^2 + k$，顶点 $(h, k)$
      - $a > 0$：开口向上（最小值在顶点）；$a < 0$：开口向下
      - 对称轴：$x = -frac(b, 2a)$
      - 与 $x$ 轴交点数：看判别式 $Delta = b^2 - 4ac$
    ],
    block[
      *坐标几何与三角*（#secref("7.4"), #secref("7.5")）
      - 两点距离：$d = sqrt((x_2-x_1)^2 + (y_2-y_1)^2)$
      - 中点坐标：$M = (frac(x_1+x_2, 2), frac(y_1+y_2, 2))$
      - 直角三角形（$angle C = 90 degree$）：
      - $sin A = frac("对边", "斜边") = frac(a, c)$
      - $cos A = frac("邻边", "斜边") = frac(b, c)$
      - $tan A = frac("对边", "邻边") = frac(a, b)$
      - 特殊角：$sin 30 degree = cos 60 degree = frac{1}{2}$，$sin 45 degree = frac(sqrt(2), 2)$
    ],
  )
]

#mastery[
  *混合自测题*

  + 已知一次函数过点 $(0, -3)$ 和 $(2, 5)$，求函数解析式，并说明它是递增还是递减。

  + 已知反比例函数图象过点 $(-2, 6)$，求 $k$ 值，并判断图象在哪两个象限。

  + 将 $y = x^2 - 4x + 7$ 化为顶点式，写出顶点坐标和对称轴。

  + 二次函数 $y = -2x^2 + 4x + 1$ 的最大值是多少？在哪个 $x$ 处取得？

  + 求直线 $y = 3x - 1$ 与 $x$ 轴、$y$ 轴的交点坐标。

  + 已知 $A(-1, 2)$，$B(3, -4)$，求 $AB$ 的长度和中点 $M$ 的坐标。

  + 在直角三角形中，$angle C = 90 degree$，斜边 $c = 10$，$angle A = 30 degree$，求两直角边 $a$、$b$ 的长。

  + 二次函数 $y = x^2 + 2x - 3$，求它与 $x$ 轴的交点（令 $y = 0$，解方程）。

  + 一次函数 $y_1 = 2x + 1$ 和 $y_2 = -x + 4$，求两直线的交点坐标。

  + 反比例函数 $y = frac(k, x)$ 的图象经过点 $(3, -2)$。当 $x = -1$ 时，$y = ?$

  + *进阶*：已知抛物线 $y = a x^2 + b x + c$ 过三点 $(0, 1)$、$(1, 0)$、$(-1, 4)$，求 $a$、$b$、$c$。

  + *进阶*：在平面直角坐标系中，$A(0,0)$，$B(6,0)$，$C(3, 3sqrt(3))$。证明 $triangle A B C$ 是等边三角形，并用三角函数求 $angle A B C$ 的正弦值。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  + $k = frac(5-(-3), 2-0) = 4$，$b = -3$，函数为 $y = 4x - 3$。$k=4>0$，递增。

  + $k = (-2) times 6 = -12$。$k < 0$，图象在第二、四象限。

  + $y = (x^2 - 4x + 4) + 3 = (x-2)^2 + 3$。顶点 $(2, 3)$，对称轴 $x = 2$。

  + 对称轴 $x = -frac(4, 2 times (-2)) = 1$；最大值 $y = -2(1)^2 + 4(1) + 1 = 3$，在 $x = 1$ 处取得。

  + 令 $y=0$：$x = frac{1}{3}$，$x$ 轴交点 $(frac{1}{3}, 0)$；令 $x=0$：$y=-1$，$y$ 轴交点 $(0, -1)$。

  + $|AB| = sqrt{(3-(-1))^2 + (-4-2)^2} = sqrt{16+36} = sqrt{52} = 2sqrt{13}$；中点 $M = (1, -1)$。

  + $a = c sin A = 10 times frac{1}{2} = 5$；$b = c cos A = 10 times frac{sqrt{3}}{2} = 5sqrt{3}$。

  + $x^2 + 2x - 3 = 0$，$(x+3)(x-1) = 0$，$x = -3$ 或 $x = 1$。交点：$(-3, 0)$ 和 $(1, 0)$。

  + 联立：$2x+1 = -x+4$，$3x = 3$，$x=1$，$y=3$。交点 $(1, 3)$。

  + $k = 3 times (-2) = -6$；$x = -1$ 时，$y = frac{-6}{-1} = 6$。

  + 代入三点：$c=1$；$a+b+1=0$；$a-b+1=4$。由后两式：$2a+2=4$，$a=1$；$b=-2$。$y = x^2 - 2x + 1$。

  + $AB = 6$，$AC = sqrt{9+27} = 6$，$BC = sqrt{9+27} = 6$，三边相等，是等边三角形。$angle ABC = 60 degree$，$sin 60 degree = frac{sqrt{3}}{2}$。

  *螺旋复习*

  + 本章一次函数 $y = kx+b$ 中，若已知两个条件（如过两点），用 #secref("5.5") 的方程组来求 $k$ 和 $b$——这正是方程组的典型应用。

  + 二次函数 $y = ax^2+bx+c$ 与 $x$ 轴的交点，由 #secref("5.6") 的求根公式决定——判别式 $Delta$ 在这里再次登场，控制的是图象是否穿过 $x$ 轴。
]
