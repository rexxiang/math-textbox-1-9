#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab

== §7.2 二次函数 <sec-7-2>

#vocab[抛物线 parabola / 顶点 vertex / 对称轴 axis of symmetry / 开口 opening direction]

#crisis[
  公园要设计一座喷泉。水从喷口以一定速度斜向上喷出，在空中划出一条优美的弧线后落入水池。

  工程师测量发现，水柱的高度 $y$（米）与水平距离 $x$（米）的关系大约满足：

  $ y = -0.5 x^2 + 2x $

  *水柱最高能到多高？会落在离喷口多远的地方？*

  这不是直线——一次函数解决不了这个问题。我们需要认识一种新的函数。
]

#history-note[
  1638 年，意大利物理学家*伽利略*（Galileo）在《两门新科学的对话》中证明：忽略空气阻力，抛射体的运动轨迹是一条*抛物线*。这是人类第一次用数学精确描述弯曲运动的轨迹。抛物线——二次函数的图象——从此成为物理学和工程学的基本工具。
]

#discovery[
  *第一步*：从最简单的开始。画 $y = x^2$ 的图象。

  #align(center, table(
    columns: 8,
    align: center,
    [$x$], [$-3$], [$-2$], [$-1$], [$0$], [$1$], [$2$], [$3$],
    [$y$], [$9$], [$4$], [$1$], [$0$], [$1$], [$4$], [$9$],
  ))

  把这些点连起来，得到一条开口向上的*U 形曲线*——*抛物线*。

  注意到：$x = -2$ 和 $x = 2$ 时 $y$ 相同，$x = -3$ 和 $x = 3$ 时 $y$ 也相同。抛物线关于 $y$ 轴*对称*！最低点在原点 $(0, 0)$。

  *第二步*：改变系数，观察变化。

  - $y = 2x^2$：和 $y = x^2$ 比，开口更*窄*（变陡了）
  - $y = frac(1, 2) x^2$：开口更*宽*（变平了）
  - $y = -x^2$：开口*向下*翻转了！

  *第三步*：平移实验。

  - $y = x^2 + 3$：图象向*上*移了 $3$ 个单位
  - $y = (x - 2)^2$：图象向*右*移了 $2$ 个单位
  - $y = (x - 2)^2 + 3$：先右移 $2$，再上移 $3$

  *关键发现*：二次函数的图象就是抛物线，通过改变参数可以控制开口方向、宽窄和位置。
]

#side-hack[
  顶点式 $y = a(x - h)^2 + k$ 的三个参数各管一件事：
  - *$a$ 定开口*——$a > 0$ 开口向上，$a < 0$ 开口向下
  - *$h$ 定左右*——顶点在 $x = h$（注意括号里是 $x - h$，符号相反！）
  - *$k$ 定上下*——顶点在 $y = k$
]

#blueprint[
  === 二次函数的概念

  形如 $ y = a x^2 + b x + c quad (a != 0) $ 的函数叫做*二次函数*。它的图象是一条*抛物线*。

  === 抛物线的基本性质

  对于 $y = a x^2 + b x + c$（$a != 0$）：

  - *开口方向*：$a > 0$ 开口向上，$a < 0$ 开口向下
  - *对称轴*：$x = -frac(b, 2a)$
  - *顶点坐标*：$(-frac(b, 2a), frac(4a c - b^2, 4a))$

  === 顶点式

  二次函数可以化为*顶点式*：

  $ y = a(x - h)^2 + k $

  其中 $(h, k)$ 是顶点坐标，$x = h$ 是对称轴。

  *配方法*：把 $y = a x^2 + b x + c$ 化为顶点式的过程：
  $ y &= a x^2 + b x + c \
    &= a (x^2 + frac(b, a) x) + c \
    &= a (x + frac(b, 2a))^2 + c - frac(b^2, 4a) \
    &= a (x + frac(b, 2a))^2 + frac(4a c - b^2, 4a) $

  === 二次函数与一元二次方程

  方程 $a x^2 + b x + c = 0$ 的根就是抛物线 $y = a x^2 + b x + c$ 与 $x$ 轴交点的横坐标。

  - $Delta = b^2 - 4a c > 0$：抛物线与 $x$ 轴有*两个*交点
  - $Delta = 0$：抛物线与 $x$ 轴有*一个*交点（顶点在 $x$ 轴上）
  - $Delta < 0$：抛物线与 $x$ 轴*没有*交点

  === 最值问题

  - 当 $a > 0$ 时，$y$ 在 $x = -frac(b, 2a)$ 处取*最小值* $frac(4a c - b^2, 4a)$
  - 当 $a < 0$ 时，$y$ 在 $x = -frac(b, 2a)$ 处取*最大值* $frac(4a c - b^2, 4a)$

  *例题 1*：将 $y = 2x^2 - 8x + 5$ 化为顶点式，求顶点坐标、对称轴，并判断开口方向。

  解：
  $ y &= 2(x^2 - 4x) + 5 \
    &= 2(x^2 - 4x + 4 - 4) + 5 \
    &= 2(x - 2)^2 - 8 + 5 \
    &= 2(x - 2)^2 - 3 $
  顶点 $(2, -3)$，对称轴 $x = 2$，$a = 2 > 0$，开口向上。

  *例题 2*：回到喷泉问题。$y = -0.5 x^2 + 2x$，求水柱的最大高度和落地点。

  解：配方——
  $ y = -0.5(x^2 - 4x) = -0.5(x - 2)^2 + 2 $
  顶点 $(2, 2)$，最大高度为 $2$ 米。令 $y = 0$：$-0.5 x(x - 4) = 0$，$x = 0$ 或 $x = 4$。水柱落在距喷口 $4$ 米处。

  *例题 3*：某商品进价 $20$ 元，售价 $x$ 元。经调查，日销量为 $(80 - 2x)$ 件（$20 <= x <= 40$）。求日利润 $y$ 关于售价 $x$ 的函数关系式，以及什么价格时日利润最大？

  解：
  $ y = (x - 20)(80 - 2x) = -2x^2 + 120x - 1600 $
  配方：
  $ y = -2(x - 30)^2 + 200 $
  因为 $a = -2 < 0$，当 $x = 30$ 时 $y$ 取最大值 $200$（元）。即售价 $30$ 元时日利润最大，为 $200$ 元。
]

#mastery[
  *基础*

  + 将下列二次函数化为顶点式，写出顶点坐标和对称轴：
    - $y = x^2 - 6x + 10$
    - $y = -3x^2 + 12x - 7$
  + 抛物线 $y = 2(x + 1)^2 - 5$ 的开口方向、顶点坐标、对称轴分别是什么？
  + 判断抛物线 $y = x^2 - 4x + 5$ 与 $x$ 轴是否有交点，说明理由。

  *应用*

  + 一个长方形的周长为 $20$ 厘米。设长为 $x$ 厘米，面积为 $y$ 平方厘米。写出 $y$ 关于 $x$ 的函数关系式，求面积的最大值。
  + 从地面以 $30$ 米/秒的速度竖直向上抛出一个小球，$t$ 秒后小球的高度 $h = 30t - 5t^2$（米）。小球何时到达最高点？最高点高度是多少？何时落回地面？

  *挑战*

  + 已知二次函数 $y = a x^2 + b x + c$ 的图象过点 $(0, 1)$、$(1, 0)$、$(-1, 4)$，求该函数的解析式。
  + 在抛物线 $y = -x^2 + 4x$ 上找一点 $P$，使 $P$ 到 $x$ 轴的距离最大。求 $P$ 的坐标。

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + $y = (x - 3)^2 + 1$，顶点 $(3, 1)$，对称轴 $x = 3$。$y = -3(x - 2)^2 + 5$，顶点 $(2, 5)$，对称轴 $x = 2$。
  + 开口向上（$a = 2 > 0$），顶点 $(-1, -5)$，对称轴 $x = -1$。
  + $Delta = (-4)^2 - 4 times 1 times 5 = 16 - 20 = -4 < 0$，没有交点。

  *应用*
  + 宽为 $frac(20 - 2x, 2) = 10 - x$，$y = x(10 - x) = -x^2 + 10x = -(x - 5)^2 + 25$（$0 < x < 10$）。当 $x = 5$ 时面积最大为 $25$ 平方厘米（正方形）。
  + $h = -5(t - 3)^2 + 45$。$t = 3$ 秒时到达最高点，最高高度 $45$ 米。令 $h = 0$：$30t - 5t^2 = 5t(6 - t) = 0$，$t = 0$ 或 $t = 6$，落回地面时间为 $6$ 秒。

  *挑战*
  + 过 $(0, 1)$：$c = 1$。过 $(1, 0)$：$a + b + 1 = 0$。过 $(-1, 4)$：$a - b + 1 = 4$。解得 $a = 2, b = -3, c = 1$，即 $y = 2x^2 - 3x + 1$。
  + $P$ 到 $x$ 轴的距离即 $y$ 值（在 $x$ 轴上方时 $y > 0$）。$y = -(x - 2)^2 + 4$，最大值在 $x = 2$ 时取得，$y = 4$。所以 $P(2, 4)$。
]
