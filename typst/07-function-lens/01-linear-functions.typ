#import "../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall

== §7.1 一次函数 <sec-7-1>

#vocab[函数 function / 斜率 slope / 截距 intercept / 正比例函数 direct proportion function]

#crisis[
  你坐上一辆出租车。计价器显示：

  - 起步价 $13$ 元（上车就要付）
  - 每公里 $2.3$ 元

  你去机场，全程 $18$ 公里。车费是多少？如果去火车站只有 $5$ 公里呢？如果去 $x$ 公里远的地方呢？

  *能不能写出一个公式，让任意里程都能直接算出车费？*
]

#history-note[
  1637 年，法国数学家*笛卡尔*（Descartes）在《方法论》附录《几何》中首次系统地用方程表示曲线。他把代数和几何统一在坐标系里，使得「一个公式对应一条曲线」成为可能。一次函数的图象——直线——是这个思想最简单的体现。
]

#discovery[
  *第一步*：列表找规律。

  #align(center, table(
    columns: 6,
    align: center,
    [里程 $x$（公里）], [1], [2], [5], [10], [18],
    [车费 $y$（元）], [15.3], [17.6], [24.5], [36], [54.4],
  ))

  每一行的车费怎么算？$y = 13 + 2.3 x$。

  *第二步*：把这些点画在坐标系里（横轴 $x$，纵轴 $y$）。你发现了什么？——所有的点排成一条*直线*！

  *第三步*：思考——如果没有起步价（$13 = 0$），公式变成 $y = 2.3 x$。这时直线会过原点。

  *关键发现*：
  - $y = 2.3 x$（过原点的直线）——*正比例函数*
  - $y = 2.3 x + 13$（不过原点的直线）——*一次函数*

  一次函数就是正比例函数「平移」了一下！
]

#side-hack[
  记住斜率 $k$ 的含义：*$k$ 正上坡，$k$ 负下坡*。$k$ 的绝对值越大，坡越陡。$k = 0$ 时就是一条水平线。
]

#tryit[
  不翻书，先试一试：一次函数图象过点 $(0, -2)$ 和 $(3, 4)$，求 $k$ 和 $b$。

  提示：把两个点的坐标代入 $y = kx + b$，建立方程组，联立求解。

  然后对照 blueprint 里的例题 2 核验你的过程。
]

#blueprint[
  === 函数的概念

  一般地，在某个变化过程中，有两个变量 $x$ 和 $y$。如果对于 $x$ 的每一个值，$y$ 都有*唯一*确定的值与之对应，就说 $y$ 是 $x$ 的*函数*，记作 $y = f(x)$。

  函数有三种表示法：
  + *解析式*（公式）：$y = 2.3 x + 13$
  + *列表*：用表格列出对应值
  + *图象*：在坐标系中画出对应点的集合

  === 正比例函数

  形如 $ y = k x quad (k != 0) $ 的函数叫做*正比例函数*。

  - 图象是过*原点*的一条直线
  - $k > 0$ 时，$y$ 随 $x$ 增大而增大（经过一、三象限）
  - $k < 0$ 时，$y$ 随 $x$ 增大而减小（经过二、四象限）

  === 一次函数

  形如 $ y = k x + b quad (k != 0) $ 的函数叫做*一次函数*。

  - $k$ 叫做*斜率*，决定直线的倾斜方向和陡峭程度
  - $b$ 叫做*纵截距*（$y$ 轴截距），是直线与 $y$ 轴的交点纵坐标
  - 当 $b = 0$ 时，一次函数就是正比例函数

  *图象性质*：
  - $k > 0$：直线从左下到右上（上坡），$y$ 随 $x$ 增大而*增大*
  - $k < 0$：直线从左上到右下（下坡），$y$ 随 $x$ 增大而*减小*
  - $abs(k)$ 越大，直线越陡；$abs(k)$ 越小，直线越平缓

  *画一次函数图象*：只需两个点。最方便的选择：
  - 令 $x = 0$，得 $y = b$（纵截距点）
  - 令 $y = 0$，得 $x = -frac(b, k)$（横截距点）

  === 用函数观点看方程和不等式

  一次函数 $y = k x + b$ 的图象可以帮助我们理解方程和不等式：

  - *方程* $k x + b = 0$ 的解就是直线与 $x$ 轴交点的横坐标
  - *不等式* $k x + b > 0$ 的解集就是图象在 $x$ 轴*上方*的部分对应的 $x$ 的范围
  - *不等式* $k x + b < 0$ 的解集就是图象在 $x$ 轴*下方*的部分对应的 $x$ 的范围

  *例题 1*：画出 $y = 2x - 4$ 的图象，求方程 $2x - 4 = 0$ 的解和不等式 $2x - 4 > 0$ 的解集。

  解：
  - 令 $x = 0$，$y = -4$；令 $y = 0$，$x = 2$。过 $(0, -4)$ 和 $(2, 0)$ 画直线。
  - 直线与 $x$ 轴交于 $(2, 0)$，所以方程 $2x - 4 = 0$ 的解是 $x = 2$。
  - 图象在 $x$ 轴上方的部分对应 $x > 2$，所以不等式 $2x - 4 > 0$ 的解集是 $x > 2$。

  *例题 2*：已知一次函数 $y = k x + b$ 的图象过点 $(1, 5)$ 和 $(-2, -1)$，求 $k$ 和 $b$。

  解：将两点代入：
  $ cases(k + b = 5, -2k + b = -1) $
  两式相减：$3k = 6$，$k = 2$。代入第一个方程：$b = 3$。

  所以 $y = 2x + 3$。

  *例题 3*：某手机套餐：月租 $30$ 元，通话每分钟 $0.2$ 元。写出月话费 $y$（元）与通话时间 $x$（分钟）的函数关系式，并求通话 $200$ 分钟时的话费。

  解：$y = 0.2 x + 30$。当 $x = 200$ 时，$y = 0.2 times 200 + 30 = 70$（元）。
]

#pitfall[
  *一次函数两个高频混淆点：*

  *① $k$ 和 $b$ 影响的东西不一样，别搞混*

  $y = kx + b$：
  - $k$ 决定直线的*倾斜方向和陡峭程度*（斜率）
  - $b$ 决定直线与 $y$ 轴的*交点高度*（纵截距）

  ❌ 常见错误：把纵截距 $b$ 和横截距 $-b/k$ 搞混。「图象过 $(0, 3)$」说的是*纵*截距 $b = 3$，不是 $x = 3$。

  *② $k < 0$ 时，$y$ 随 $x$ 增大而减小，不是增大！*

  #grid(columns: (1fr, 1fr), gutter: 8pt,
    block[❌ $y = -2x + 5$ \ $k = -2$，$y$ 随 $x$ 增大而增大（错）],
    block[✓ $y = -2x + 5$ \ $k = -2 < 0$，$y$ 随 $x$ 增大而*减小*（下坡）]
  )
]

#mastery[
  *基础*

  + 判断下列函数是否为一次函数，是否为正比例函数：
    - $y = 3x$
    - $y = -2x + 5$
    - $y = x^2 + 1$
    - $y = frac(x, 4)$
  + 画出 $y = -x + 3$ 的图象，标出与两坐标轴的交点。
  + 一次函数 $y = k x + b$ 的图象过点 $(0, -2)$ 和 $(3, 4)$，求 $k$ 和 $b$。

  *应用*

  + 水箱中原有 $200$ 升水，每分钟流出 $8$ 升。写出水箱剩余水量 $y$（升）与时间 $x$（分钟）的函数关系式。水箱多少分钟后放空？
  + 甲、乙两人同时从 A 地出发去 B 地（相距 $120$ 千米）。甲骑车，速度 $20$ 千米/时；乙开车，速度 $60$ 千米/时。分别写出他们的距离-时间函数，并在同一坐标系中画出图象。乙比甲早到多少小时？

  *挑战*

  + 已知一次函数 $y = (2m - 1)x + m - 3$ 的图象过原点，求 $m$ 的值。如果图象经过第二、三、四象限，求 $m$ 的取值范围。
  + 用函数图象法解不等式组 $cases(x + 1 > 0, -2x + 4 >= 0)$。（提示：画两条直线，找同时满足的 $x$ 范围。）

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_

  *基础*
  + $y = 3x$：一次函数，也是正比例函数。$y = -2x + 5$：一次函数，不是正比例函数。$y = x^2 + 1$：不是一次函数。$y = frac(x, 4) = frac(1, 4) x$：一次函数，也是正比例函数。
  + 令 $x = 0$，$y = 3$；令 $y = 0$，$x = 3$。交点为 $(0, 3)$ 和 $(3, 0)$。
  + 过 $(0, -2)$ 知 $b = -2$。代入 $(3, 4)$：$3k - 2 = 4$，$k = 2$。所以 $y = 2x - 2$。

  *应用*
  + $y = 200 - 8x$（$0 <= x <= 25$）。令 $y = 0$：$200 - 8x = 0$，$x = 25$（分钟）。
  + 甲：$y_1 = 20 t$；乙：$y_2 = 60 t$。甲到达时间 $t_1 = frac(120, 20) = 6$（时），乙 $t_2 = frac(120, 60) = 2$（时）。乙比甲早到 $4$ 小时。

  *挑战*
  + 过原点则 $b = m - 3 = 0$，$m = 3$。此时 $k = 2 times 3 - 1 = 5 > 0$，图象过一、三象限，不符合「经过二、三、四象限」。经过第二、三、四象限要求 $k < 0$ 且 $b < 0$，即 $2m - 1 < 0$ 且 $m - 3 < 0$，解得 $m < frac(1, 2)$。
  + $y_1 = x + 1$ 与 $x$ 轴交于 $(-1, 0)$，$x + 1 > 0$ 即 $x > -1$。$y_2 = -2x + 4$ 与 $x$ 轴交于 $(2, 0)$，$-2x + 4 >= 0$ 即 $x <= 2$。不等式组的解集为 $-1 < x <= 2$。
]
