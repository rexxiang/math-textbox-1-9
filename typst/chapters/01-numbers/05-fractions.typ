#import "../../lib/theme.typ": *

== §1.5 分数 <sec-1-5>

*前置知识*：#secref("1.2") 四则运算，#secref("1.4") 因数与倍数
*适用年级*：3-6 年级

=== 分数的意义

#explore[
妈妈把一个苹果平均切成 `4` 块，你吃了其中的 `1` 块。你吃了这个苹果的多少？
]

#understand[
把一个物体或一个整体平均分成若干份，取其中的一份或几份，就可以用分数来表示。`1/4` 读作“四分之一”。

其中 `4` 叫做分母，表示平均分的总份数；`1` 叫做分子，表示取的份数。中间的横线叫分数线。

更一般地，`a / b`（`b ≠ 0`）表示把整体平均分成 `b` 份，取其中 `a` 份。

分数也可以理解为除法的结果：`a ÷ b = a / b`
]

#workedexamples[
*例 1.* 一条线段平均分成 `5` 段，涂色了其中的 `3` 段，涂色部分用分数怎么表示？

*解：* 平均分成 `5` 份，取 `3` 份，即 `3/5`

*例 2.* 把 `3` 块饼平均分给 `4` 个小朋友，每人分到多少？

*解：* `3 ÷ 4 = 3/4`，每人分到 `3/4` 块饼。
]

#keytakeaway[
- 分数表示“平均分之后取几份”。
- 分母表示总份数，分子表示取的份数，分母不能为 `0`。
- 分数与除法的关系：`a ÷ b = a / b`。
]

#practice[
1. 把一块蛋糕平均分成 `8` 份，吃了 `3` 份，吃了几分之几？
2. `5 ÷ 6` 用分数表示是多少？
3. `7/10` 表示把整体平均分成几份，取了几份？
]

=== 真分数、假分数与带分数

#explore[
小明吃了 `3/4` 个苹果，小红吃了 `5/4` 个苹果。小红怎么能吃超过一个苹果呢？
]

#understand[
- 真分数：分子小于分母的分数，如 `3/4`。真分数小于 `1`。
- 假分数：分子大于或等于分母的分数，如 `5/4`、`4/4`。假分数大于或等于 `1`。
- 带分数：由一个整数和一个真分数组成，如 `1 1/4`。

假分数和带分数可以互化：

`5/4 = 1 1/4`

方法：`5 ÷ 4 = 1` 余 `1`，所以商 `1` 为整数部分，余数 `1` 为分子，分母不变。

反过来：`1 1/4 = (1 × 4 + 1) / 4 = 5/4`
]

#workedexamples[
*例 1.* 把 `11/3` 化成带分数。

*解：* `11 ÷ 3 = 3` 余 `2`，所以 `11/3 = 3 2/3`

*例 2.* 把 `2 3/7` 化成假分数。

*解：* `(2 × 7 + 3) / 7 = 17/7`
]

#keytakeaway[
- 真分数 `< 1`，假分数 `≥ 1`。
- 假分数化带分数：用分子除以分母。
- 带分数化假分数：整数部分乘分母加分子。
]

#practice[
1. 从 `3/5`、`7/4`、`6/6`、`2/9` 中选出真分数和假分数。
2. 把 `17/5` 化成带分数。
3. 把 `3 2/5` 化成假分数。
]

=== 分数的基本性质

#explore[
拿两张同样大小的纸条。第一张平均分成 `2` 份，涂 `1` 份，得到 `1/2`。第二张平均分成 `4` 份，涂 `2` 份，得到 `2/4`。涂色的面积一样大吗？
]

#understand[
分数的基本性质：分数的分子和分母同时乘以（或除以）同一个不为零的数，分数的大小不变。`a / b = (a × k) / (b × k) = (a ÷ m) / (b ÷ m)`（`k ≠ 0, m ≠ 0`）

例如：`1/2 = 2/4 = 3/6 = 50/100`

这条性质有两个重要应用：
- 约分：把一个分数的分子和分母同时除以它们的公因数，化成最简分数。
- 通分：把异分母分数化成同分母分数，以便比较大小或进行加减运算。

通分的关键：找两个分母的最小公倍数作为公分母。
]

#workedexamples[
*例 1.* 把 `24/36` 约分。

*解：* `24` 和 `36` 的最大公因数是 `12`。`24/36 = (24 ÷ 12) / (36 ÷ 12) = 2/3`

*例 2.* 比较 `3/4` 和 `5/7` 的大小。

*解：* 通分。`4` 和 `7` 的最小公倍数是 `28`。`3/4 = 21/28`，`5/7 = 20/28`。因为 `21 > 20`，所以 `3/4 > 5/7`
]

#keytakeaway[
- 分子分母同乘或同除以同一个非零数，分数大小不变。
- 约分：除以最大公因数，化为最简分数。
- 通分：找最小公倍数作公分母，再比较或运算。
]

#practice[
1. 约分：`15/45`。
2. 约分：`28/42`。
3. 比较 `5/6` 和 `7/9` 的大小。
4. 把 `2/3`、`3/4`、`5/6` 通分。
]

=== 分数的加减法

#explore[
小明做作业用了 `1/3` 小时，然后看课外书用了 `1/4` 小时。他一共用了多长时间？这两个分数的分母不一样，怎么加？
]

#understand[
同分母分数加减法：分母不变，分子相加或相减。`2/7 + 3/7 = (2 + 3) / 7 = 5/7`

异分母分数加减法：先通分，再按同分母分数加减。`1/3 + 1/4 = 4/12 + 3/12 = 7/12`

带分数加减法：整数部分和分数部分分别运算。`1 1/3 + 2 1/4 = 3 + 4/12 + 3/12 = 3 7/12`
]

#workedexamples[
*例 1.* 计算 `5/6 - 1/4`。

*解：* `6` 和 `4` 的最小公倍数是 `12`。`5/6 - 1/4 = 10/12 - 3/12 = 7/12`

*例 2.* 计算 `3 1/5 + 1 3/10`。

*解：* 整数部分：`3 + 1 = 4`。分数部分通分：`1/5 + 3/10 = 2/10 + 3/10 = 5/10 = 1/2`

所以 `3 1/5 + 1 3/10 = 4 1/2`

*例 3.* 计算 `5 1/6 - 2 3/4`。

*解：* `6` 和 `4` 的最小公倍数是 `12`。`5 1/6 - 2 3/4 = 5 2/12 - 2 9/12`

分数部分 `2/12 < 9/12`，不够减，从整数部分借 `1`：`= 4 14/12 - 2 9/12 = 2 5/12`
]

#keytakeaway[
- 同分母直接加减分子。
- 异分母先通分再加减。
- 带分数减法中分数部分不够减时，从整数部分借 `1`。
- 结果要约分为最简分数。
]

#practice[
1. 计算：`2/3 + 5/6`。
2. 计算：`7/8 - 2/3`。
3. 计算：`4 1/2 - 1 5/6`。
]

=== 分数的乘法

#explore[
一桶油有 `4/5` 千克，用去了这桶油的 `1/2`。用去了多少千克？“`4/5` 的一半”该怎么算？
]

#understand[
分数乘整数：分子乘以整数，分母不变。`2/5 × 3 = (2 × 3) / 5 = 6/5 = 1 1/5`

分数乘分数：分子相乘作分子，分母相乘作分母。`a / b × c / d = (a × c) / (b × d)`

例如：`4/5 × 1/2 = (4 × 1) / (5 × 2) = 4/10 = 2/5`

计算时可以先约分再相乘，避免数字过大。
]

#workedexamples[
*例 1.* 计算 `3/8 × 4/9`。

*解：* 先约分：`3` 与 `9` 有公因数 `3`，`4` 与 `8` 有公因数 `4`。`3/8 × 4/9 = 1/2 × 1/3 = 1/6`

*例 2.* 一块地有 `3/4` 公顷，其中 `2/3` 种了玉米。种玉米的面积是多少公顷？

*解：* `3/4 × 2/3 = (3 × 2) / (4 × 3) = 6/12 = 1/2`（公顷）

*例 3.* 计算 `2 1/3 × 1 1/7`。

*解：* 先化成假分数：`2 1/3 = 7/3`，`1 1/7 = 8/7`。`7/3 × 8/7 = 8/3 = 2 2/3`
]

#keytakeaway[
- 分数乘法：分子乘分子，分母乘分母。
- 先约分再乘，计算更简便。
- 带分数相乘时先化成假分数。
- “求一个数的几分之几”用乘法。
]

#practice[
1. 计算：`5/6 × 3/10`。
2. 计算：`7/12 × 8`。
3. 计算：`1 1/4 × 2 2/5`。
4. 一筐苹果重 `30` 千克，卖出了 `2/5`，卖出了多少千克？
]

=== 分数的除法

#explore[
一条丝带长 `3/4` 米，要剪成每段 `1/8` 米的小段，可以剪多少段？
]

#understand[
分数除以整数：等于乘以这个整数的倒数。`2/3 ÷ 5 = 2/3 × 1/5 = 2/15`

一个数的倒数：如果两个数的乘积是 `1`，这两个数互为倒数。`3/4` 的倒数是 `4/3`。

分数除以分数：除以一个数等于乘以它的倒数。`a / b ÷ c / d = a / b × d / c`（`c ≠ 0`）

例如：`3/4 ÷ 1/8 = 3/4 × 8/1 = 24/4 = 6`。丝带可以剪成 `6` 段。

为什么除以一个分数等于乘以它的倒数？因为 `c/d × d/c = 1`，把除法转化为乘法可以利用已知的乘法规则。
]

#workedexamples[
*例 1.* 计算 `5/6 ÷ 5/9`。

*解：* `5/6 ÷ 5/9 = 5/6 × 9/5 = 9/6 = 3/2 = 1 1/2`

*例 2.* 计算 `4 ÷ 2/3`。

*解：* `4 ÷ 2/3 = 4 × 3/2 = 12/2 = 6`

*例 3.* 一块长方形土地的面积是 `4/5` 公顷，宽是 `2/5` 公顷。长是多少？

*解：* 长 = 面积 ÷ 宽 = `4/5 ÷ 2/5 = 4/5 × 5/2 = 20/10 = 2`（公顷）
]

#keytakeaway[
- 除以一个数等于乘以它的倒数。
- 倒数：分子分母互换。`0` 没有倒数。
- 分数除法的关键步骤：变除为乘，除数取倒数。
]

#practice[
1. 写出 `5/7` 和 `3` 的倒数。
2. 计算：`7/8 ÷ 3/4`。
3. 计算：`2 1/2 ÷ 1 1/4`。
4. 一根绳子长 `5/6` 米，剪成每段 `1/6` 米，可以剪几段？
]

=== 分数应用题

#explore[
六年级有 `240` 名学生，其中 `3/8` 参加了数学兴趣小组。参加数学兴趣小组的有多少人？这类“求一个数的几分之几是多少”的问题，在生活中非常常见。
]

#understand[
分数应用题的三种基本类型：

类型一：求一个数的几分之几是多少——用乘法。`240 × 3/8 = 90`（人）

类型二：已知一个数的几分之几是多少，求这个数——用除法。如果参加兴趣小组的 `90` 人占全年级的 `3/8`，求全年级人数：`90 ÷ 3/8 = 90 × 8/3 = 240`（人）

类型三：求一个数是另一个数的几分之几——用除法。`90 ÷ 240 = 90/240 = 3/8`

解题关键：找准单位“1”（谁是整体）和对应的分率（几分之几）。
]

#workedexamples[
*例 1.* 一本书有 `300` 页，小明已经看了 `2/5`。他看了多少页？还剩多少页？

*解：*
- 看了的页数：`300 × 2/5 = 120`（页）
- 剩余页数：`300 - 120 = 180`（页）
- 也可以直接：剩余分率 = `1 - 2/5 = 3/5`，`300 × 3/5 = 180`（页）

*例 2.* 水果店运来一批苹果，卖出了 `3/4`，正好卖出了 `120` 千克。这批苹果一共多少千克？

*解：* 单位“1”是这批苹果的总重量。`120 ÷ 3/4 = 120 × 4/3 = 160`（千克）

*例 3.* 甲数是 `48`，乙数是 `60`。甲数是乙数的几分之几？乙数是甲数的几倍？

*解：*
- 甲是乙的几分之几：`48 ÷ 60 = 48/60 = 4/5`
- 乙是甲的几倍：`60 ÷ 48 = 60/48 = 5/4 = 1 1/4`（倍）

*例 4.* 某班有 `45` 名学生，男生占 `4/9`。女生有多少人？

*解：* 女生占全班的 `1 - 4/9 = 5/9`。`45 × 5/9 = 25`（人）
]

#keytakeaway[
- “求几分之几是多少”用乘法；“已知几分之几求整体”用除法。
- 解题第一步：找准单位“1”。
- 分率和对应量必须对应同一个单位“1”。
- 画线段图可以帮助理解数量关系。
]

#practice[
1. 学校图书馆有 `2400` 本书，科技类占 `1/6`。科技类有多少本？
2. 修一条路，已修了 `2/7`，正好修了 `800` 米。这条路全长多少米？
3. 工厂计划生产 `600` 件产品，已经完成了 `450` 件。完成了计划的几分之几？
4. 果园里有桃树 `120` 棵，梨树的棵数是桃树的 `3/4`。梨树有多少棵？两种树一共多少棵？
5. 一件衣服打折后便宜了 `36` 元，便宜的部分占原价的 `2/9`。这件衣服原价多少元？
]

#answer[
*练一练 分数的意义*

1. `3/8`
2. `5/6`
3. 平均分成 `10` 份，取了 `7` 份

*练一练 真分数、假分数与带分数*

1. 真分数：`3/5`、`2/9`。假分数：`7/4`、`6/6`
2. `17/5 = 3 2/5`
3. `3 2/5 = 17/5`

*练一练 分数的基本性质*

1. `15/45 = 1/3`
2. `28/42 = 2/3`
3. 通分。`6` 和 `9` 的最小公倍数是 `18`。`5/6 = 15/18`，`7/9 = 14/18`。因为 `15 > 14`，所以 `5/6 > 7/9`
4. 公分母为 `3, 4, 6` 的最小公倍数 `12`。`2/3 = 8/12`，`3/4 = 9/12`，`5/6 = 10/12`

*练一练 分数的加减法*

1. `2/3 + 5/6 = 4/6 + 5/6 = 9/6 = 3/2 = 1 1/2`
2. `7/8 - 2/3 = 21/24 - 16/24 = 5/24`
3. `4 1/2 - 1 5/6 = 4 3/6 - 1 5/6`。分数部分不够减，借一：`= 3 9/6 - 1 5/6 = 2 4/6 = 2 2/3`

*练一练 分数的乘法*

1. `5/6 × 3/10 = 15/60 = 1/4`
2. `7/12 × 8 = 56/12 = 14/3 = 4 2/3`
3. `1 1/4 × 2 2/5 = 5/4 × 12/5 = 60/20 = 3`
4. `30 × 2/5 = 12`（千克）

*练一练 分数的除法*

1. `5/7` 的倒数是 `7/5`，`3` 的倒数是 `1/3`
2. `7/8 ÷ 3/4 = 7/8 × 4/3 = 28/24 = 7/6 = 1 1/6`
3. `2 1/2 ÷ 1 1/4 = 5/2 ÷ 5/4 = 5/2 × 4/5 = 20/10 = 2`
4. `5/6 ÷ 1/6 = 5/6 × 6 = 5`（段）

*练一练 分数应用题*

1. `2400 × 1/6 = 400`（本）
2. `800 ÷ 2/7 = 800 × 7/2 = 2800`（米）
3. `450 ÷ 600 = 450/600 = 3/4`
4. 梨树：`120 × 3/4 = 90`（棵）。两种树共 `120 + 90 = 210`（棵）
5. `36 ÷ 2/9 = 36 × 9/2 = 162`（元）
]
