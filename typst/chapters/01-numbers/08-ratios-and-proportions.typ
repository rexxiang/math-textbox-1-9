#import "../../lib/theme.typ": *

== §1.8 比与比例 <sec-1-8>

*前置知识*：#secref("1.5") 分数，#secref("1.7") 百分数
*适用年级*：6 年级

=== 比的意义与化简

#explore[
学校篮球队有 `12` 名男生和 `8` 名女生。老师说：“男生和女生的人数比是 `12` 比 `8`。”这个“比”是什么意思？它和除法、分数有什么关系？

在日常生活中，我们经常用“比”来描述两个量之间的关系。比如：
- 调制蜂蜜水，蜂蜜和水的比例是 `1 : 5`
- 照片的长宽比是 `4 : 3`
- 混凝土中水泥、沙子、石子的比是 `1 : 2 : 3`
]

#understand[
比表示两个数量之间的倍数关系。`a` 和 `b` 的比记作 `a : b`，读作“`a` 比 `b`”。

`a : b = a / b`

其中 `a` 叫做比的前项，`b` 叫做比的后项，`a / b` 叫做比值。后项不能为 `0`。

比、除法、分数的关系：
- 比：`a : b`
- 除法：`a ÷ b`
- 分数：`a / b`

比的化简：和分数约分类似，比的前项和后项同时除以它们的最大公因数。`12 : 8 = 3 : 2`

化简的目标是使前项和后项都是整数，且互质。

如果比的前项或后项是分数或小数，先化成整数再化简：
- `0.6 : 0.9 = 6 : 9 = 2 : 3`
- `1/3 : 1/2 = 2 : 3`
]

#workedexamples[
*例 1.* 化简 `24 : 36`。

*解：* `24` 和 `36` 的最大公因数是 `12`。`24 : 36 = (24 ÷ 12) : (36 ÷ 12) = 2 : 3`

*例 2.* 化简 `1.5 : 2.5`。

*解：* 先化为整数比（同乘 `10`）：`1.5 : 2.5 = 15 : 25 = 3 : 5`

*例 3.* 化简 `2/3 : 5/6`。

*解：* 前后项同乘 `6`：`2/3 : 5/6 = 4 : 5`

*例 4.* 小明身高 `1.5` 米，爸爸身高 `1.8` 米。小明和爸爸身高的比是多少？比值是多少？

*解：*
- 身高比：`1.5 : 1.8 = 15 : 18 = 5 : 6`
- 比值：`5/6`（小明的身高是爸爸的 `5/6`）
]

#keytakeaway[
- 比表示两个量的倍数关系，`a : b = a / b`。
- 比的前后项同时乘或除以同一个非零数，比值不变。
- 化简的目标：前后项为互质的整数。
- 比、除法、分数是同一种关系的三种表达方式。
]

#practice[
1. 化简 `45 : 30`。
2. 化简 `0.4 : 1.2`。
3. 化简 `3/4 : 5/8`。
4. 一杯果汁用 `40` 毫升浓缩液加 `160` 毫升水配成。浓缩液和水的比是多少？浓缩液占果汁总量的几分之几？
5. 求 `7 : 4` 的比值。
]

=== 比例的意义与基本性质

#explore[
一幅地图上，`2` 厘米代表实际的 `100` 千米，`4` 厘米代表实际的 `200` 千米。这两组对应关系可以写成：

`2 : 100 = 4 : 200`

这个等式就是一个比例。比例有什么性质？怎么用它来解题？
]

#understand[
比例：表示两个比相等的式子。`a : b = c : d`，即 `a / b = c / d`

其中 `a` 和 `d` 叫做外项，`b` 和 `c` 叫做内项。

比例的基本性质（交叉相乘）：在比例 `a : b = c : d` 中，`a × d = b × c`

即两个外项的积等于两个内项的积。

这条性质可以用来解比例——已知比例中的三个量，求第四个量。
]

#workedexamples[
*例 1.* 判断 `6 : 9` 和 `10 : 15` 能否组成比例。

*解：* 化简：`6 : 9 = 2 : 3`，`10 : 15 = 2 : 3`。两个比相等，所以能组成比例：`6 : 9 = 10 : 15`。

验证：外项之积 = `6 × 15 = 90`，内项之积 = `9 × 10 = 90`。相等，成立。

*例 2.* 解比例：`3 : 8 = x : 24`。

*解：* `3 × 24 = 8 × x`，所以 `72 = 8x`，`x = 9`

*例 3.* 解比例：`5/x = 15/12`。

*解：* `5 × 12 = x × 15`，所以 `60 = 15x`，`x = 4`

*例 4.* 在一幅地图上，`3` 厘米代表实际距离 `150` 千米。如果两地在地图上相距 `7` 厘米，实际距离是多少千米？

*解：* 设实际距离为 `x` 千米。`3 : 150 = 7 : x`，`3x = 150 × 7 = 1050`，`x = 350`

两地实际距离为 `350` 千米。
]

#keytakeaway[
- 比例是两个比相等的等式。
- 比例的基本性质：外项之积 = 内项之积（交叉相乘）。
- 解比例的核心方法：交叉相乘，转化为乘法等式。
- 比例广泛应用于地图、缩放、配比等问题。
]

#practice[
1. 判断 `4 : 7` 和 `12 : 21` 能否组成比例。
2. 解比例：`x : 6 = 5 : 3`。
3. 解比例：`2/5 = x/20`。
4. 解比例：`0.4 : 1.5 = x : 6`。
5. 某地图上 `5` 厘米代表实际距离 `200` 千米。两城市实际相距 `560` 千米，在地图上是多少厘米？
]

=== 正比例与反比例

#explore[
情境一：每本笔记本 `3` 元。买 `2` 本要 `6` 元，买 `5` 本要 `15` 元。本数越多，总价越高。总价和本数之间有什么固定的关系？

情境二：从家到学校 `6` 千米。走路速度 `6` 千米/时需 `1` 小时；骑车速度 `12` 千米/时需 `0.5` 小时。速度越快，时间越短。速度和时间之间又有什么固定的关系？
]

#understand[
正比例关系：两个量 `x` 和 `y`，如果它们的比值始终不变，就说 `y` 和 `x` 成正比例。`y / x = k`（常数，`k ≠ 0`），即 `y = kx`

一个量增大，另一个量也增大；一个量减小，另一个量也减小。

上面的情境一：`总价 / 本数 = 3`（元/本），总价和本数成正比例。

反比例关系：两个量 `x` 和 `y`，如果它们的积始终不变，就说 `y` 和 `x` 成反比例。`x × y = k`（常数，`k ≠ 0`），即 `y = k/x`

一个量增大，另一个量反而减小。

上面的情境二：`速度 × 时间 = 6`（千米），速度和时间成反比例。

判断方法：
1. 找到两个变化的量。
2. 如果它们的商不变——正比例。
3. 如果它们的积不变——反比例。
4. 如果商和积都在变——既不成正比例也不成反比例。
]

#workedexamples[
*例 1.* 下面各组量是否成正比例或反比例？

(a) 长方形的面积一定，长和宽。

*解：* 长 × 宽 = 面积（常数），所以长和宽成反比例。

(b) 正方形的边长和周长。

*解：* `周长 / 边长 = 4`（常数），所以周长和边长成正比例。

(c) 人的年龄和身高。

*解：* 年龄增大身高不一定按固定比例增长，既不是正比例也不是反比例。

*例 2.* 一辆汽车从甲地开往乙地，速度和时间如下：

- 速度（千米/时）：`40, 60, 80, 120`
- 时间（小时）：`6, 4, 3, 2`

判断速度和时间的关系。

*解：* 计算每组的积：`40 × 6 = 240`，`60 × 4 = 240`，`80 × 3 = 240`，`120 × 2 = 240`。积始终为 `240`，所以速度和时间成反比例。

*例 3.* 已知 `y` 和 `x` 成正比例，且当 `x = 4` 时 `y = 10`。求当 `x = 6` 时 `y` 的值。

*解：* `y / x = 10 / 4 = 5 / 2`，当 `x = 6` 时，`y = 5/2 × 6 = 15`

*例 4.* 已知 `y` 和 `x` 成反比例，且当 `x = 3` 时 `y = 8`。求当 `x = 6` 时 `y` 的值。

*解：* `x × y = 3 × 8 = 24`，当 `x = 6` 时，`y = 24 / 6 = 4`
]

#keytakeaway[
- 正比例：`y / x = k`（商不变），两个量同增同减。
- 反比例：`x × y = k`（积不变），一增一减。
- 判断关键：看“商”不变还是“积”不变。
- 不是所有两个相关的量都成比例关系。
]

#practice[
1. 判断下面各组量是正比例、反比例还是不成比例：每天工作量一定，完成任务所需天数和总工作量；路程一定，速度和时间；圆的面积和半径。
2. `y` 和 `x` 成正比例，当 `x = 5` 时 `y = 20`。求 `x = 8` 时 `y` 的值。
3. `y` 和 `x` 成反比例，当 `x = 4` 时 `y = 15`。求 `x = 10` 时 `y` 的值。
4. 一项工程，`6` 个人做需要 `10` 天完成。如果增加到 `15` 个人，需要几天？
5. 购买钢笔，单价 `8` 元。买 `5` 支要多少钱？买 `12` 支呢？总价和支数成什么比例？
]

=== 比例的应用

#explore[
学校秋游要带 `120` 瓶矿泉水，按照三个年级的人数 `2 : 3 : 5` 来分配。每个年级各分到多少瓶？

另一个场景：一幅建筑设计图的比例尺是 `1 : 200`，图上量得一面墙长 `5` 厘米。这面墙的实际长度是多少？
]

#understand[
按比分配：把一个量按照给定的比来分配。

方法：先求出总份数，再用分数乘法分别计算。

以 `120` 瓶水按 `2 : 3 : 5` 分配为例：
- 总份数 = `2 + 3 + 5 = 10`
- 第一组：`120 × 2/10 = 24`（瓶）
- 第二组：`120 × 3/10 = 36`（瓶）
- 第三组：`120 × 5/10 = 60`（瓶）

验算：`24 + 36 + 60 = 120`，正确。

比例尺表示图上距离和实际距离的比。`比例尺 = 图上距离 / 实际距离`

比例尺 `1 : 200` 意味着图上 `1` 厘米代表实际 `200` 厘米（`2` 米）。
]

#workedexamples[
*例 1.* 学校把 `540` 本新书按 `4 : 3 : 2` 分配给三个年级。每个年级各分到多少本？

*解：* 总份数 = `4 + 3 + 2 = 9`
- 第一年级：`540 × 4/9 = 240`（本）
- 第二年级：`540 × 3/9 = 180`（本）
- 第三年级：`540 × 2/9 = 120`（本）

验算：`240 + 180 + 120 = 540`。

*例 2.* 一种混凝土由水泥、沙子、石子按 `1 : 2 : 4` 混合而成。要配制 `2100` 千克混凝土，各需多少千克？

*解：* 总份数 = `1 + 2 + 4 = 7`
- 水泥：`2100 × 1/7 = 300`（千克）
- 沙子：`2100 × 2/7 = 600`（千克）
- 石子：`2100 × 4/7 = 1200`（千克）

*例 3.* 一幅地图的比例尺是 `1 : 5000000`（即 `1 : 500` 万）。图上量得北京到天津的距离为 `2.4` 厘米。北京到天津的实际距离约为多少千米？

*解：* `2.4 × 5000000 = 12000000`（厘米）`= 120`（千米）

*例 4.* 一栋教学楼实际高度为 `15` 米。在一张比例尺为 `1 : 500` 的设计图上，这栋楼画多高？

*解：* 先统一单位。`15` 米 = `1500` 厘米。图上距离 = `1500 × 1/500 = 3`（厘米）

图上画 `3` 厘米。

*例 5.* 甲乙两人合开一个小店，甲投资 `30000` 元，乙投资 `20000` 元。年底盈利 `15000` 元，按投资比例分配。各得多少？

*解：* 投资比 = `30000 : 20000 = 3 : 2`，总份数 = `5`
- 甲：`15000 × 3/5 = 9000`（元）
- 乙：`15000 × 2/5 = 6000`（元）
]

#keytakeaway[
- 按比分配：先求总份数，再按分数乘法分配。
- 比例尺 = 图上距离 : 实际距离，注意统一单位。
- 实际距离 = 图上距离 ÷ 比例尺。
- 分配后一定要验算：各部分之和是否等于总量。
]

#practice[
1. 把 `180` 按 `2 : 3 : 4` 分配。
2. 三角形三个内角的度数比是 `1 : 2 : 3`，求三个角各是多少度？
3. 一幅地图的比例尺是 `1 : 4000000`。图上 `3.5` 厘米代表实际多少千米？
4. 实际距离 `80` 千米，在比例尺 `1 : 2000000` 的地图上应画多少厘米？
5. 甲乙丙三人合伙投资，甲出 `5` 万，乙出 `3` 万，丙出 `2` 万。年底利润 `8` 万元按投资比例分配，各得多少？
6. 稀释一种农药，药液和水的比是 `1 : 150`。现有 `3` 升药液，需要加多少升水？
7. 制作一个学校操场的平面图。操场实际长 `200` 米、宽 `100` 米。如果用比例尺 `1 : 2000`，图上长和宽各画多少厘米？
8. 甲仓库有 `300` 吨货物，乙仓库有 `200` 吨。从甲仓库调多少吨到乙仓库，才能使两个仓库的货物量之比为 `3 : 7`？
]

#answer[
*练一练 比的意义与化简*

1. `45 : 30 = 3 : 2`
2. `0.4 : 1.2 = 4 : 12 = 1 : 3`
3. `3/4 : 5/8 = 6 : 5`
4. 浓缩液和水的比 = `40 : 160 = 1 : 4`。果汁总量 = `40 + 160 = 200`（毫升），浓缩液占 `1/5`
5. `7 : 4` 的比值 = `7/4 = 1.75`

*练一练 比例的意义与基本性质*

1. `4 : 7 = 4/7`，`12 : 21 = 12/21 = 4/7`。比值相等，能组成比例。
2. `x × 3 = 6 × 5`，`3x = 30`，`x = 10`
3. `2 × 20 = 5 × x`，`40 = 5x`，`x = 8`
4. `0.4 × 6 = 1.5 × x`，`2.4 = 1.5x`，`x = 1.6`
5. `x / 5 = 560 / 200`，`200x = 2800`，`x = 14`（厘米）

*练一练 正比例与反比例*

1. (a) 总工作量和天数成正比例；(b) 速度和时间成反比例；(c) 不成比例
2. `y / x = 20 / 5 = 4`，当 `x = 8` 时，`y = 32`
3. `x × y = 4 × 15 = 60`，当 `x = 10` 时，`y = 6`
4. 工作总量 = `6 × 10 = 60`（人天），`15` 人做需要 `60 ÷ 15 = 4`（天）
5. `5` 支：`8 × 5 = 40`（元）；`12` 支：`8 × 12 = 96`（元）。总价和支数成正比例

*练一练 比例的应用*

1. 总份数 = `2 + 3 + 4 = 9`，分别为 `40, 60, 80`
2. 三角形内角和 = `180°`，总份数 = `6`，三个角分别为 `30°`、`60°`、`90°`
3. `3.5 × 4000000 = 14000000`（厘米）`= 140`（千米）
4. `80` 千米 = `8000000` 厘米，`8000000 ÷ 2000000 = 4`（厘米）
5. 投资比 = `5 : 3 : 2`，总份数 = `10`。甲：`4` 万元，乙：`2.4` 万元，丙：`1.6` 万元
6. `3 × 150 = 450`（升），需要加 `450` 升水
7. `200` 米 = `20000` 厘米，`20000 ÷ 2000 = 10`（厘米）；`100` 米 = `10000` 厘米，`10000 ÷ 2000 = 5`（厘米）
8. 总货物 = `500` 吨。按 `3 : 7` 分配：甲 `150` 吨，乙 `350` 吨，所以甲需调出 `150` 吨到乙
]
