#import "../../lib/theme.typ": *

== §1.3 运算律 <sec-1-3>

*前置知识*：#secref("1.2") 四则运算
*适用年级*：3-4 年级

=== 加法交换律与结合律

#explore[
小明从家走到学校要经过一个公园。从家到公园是 `300` 米，从公园到学校是 `500` 米。从家到学校一共走多少米？如果反过来，从学校走到家呢？
]

#understand[
加法交换律：两个数相加，交换加数的位置，和不变。`a + b = b + a`

例如：`300 + 500 = 500 + 300 = 800`。

加法结合律：三个数相加，先把前两个数相加或先把后两个数相加，和不变。`a + b + c = (a + b) + c = a + (b + c)`

例如：`36 + 48 + 52`，如果先算 `48 + 52 = 100`，再算 `36 + 100 = 136`，比按顺序算更方便。
]

#workedexamples[
*例 1.* 用简便方法计算 `67 + 54 + 33`。

*解：* 观察到 `67 + 33 = 100`，利用交换律和结合律：`67 + 54 + 33 = (67 + 33) + 54 = 100 + 54 = 154`

*例 2.* 用简便方法计算 `125 + 98`。

*解：* 把 `98` 看作 `100 - 2`：`125 + 98 = 125 + 100 - 2 = 225 - 2 = 223`
]

#keytakeaway[
- 加法交换律：`a + b = b + a`。
- 加法结合律：`(a + b) + c = a + (b + c)`。
- 利用这两条规律，可以调整加数的顺序和组合，使计算更简便。
]

#practice[
1. 用简便方法计算：`88 + 46 + 12`。
2. 用简便方法计算：`276 + 199`。
3. 用简便方法计算：`75 + 168 + 25 + 32`。
]

=== 乘法交换律与结合律

#explore[
体育课上，同学们站成 `4` 行 `8` 列做操。一共有多少人？不管你先数行还是先数列，结果一样吗？
]

#understand[
乘法交换律：两个数相乘，交换因数的位置，积不变。`a × b = b × a`

乘法结合律：三个数相乘，先把前两个数相乘或先把后两个数相乘，积不变。`a × b × c = (a × b) × c = a × (b × c)`

例如：`25 × 7 × 4`，先算 `25 × 4 = 100`，再算 `100 × 7 = 700`，比按顺序算简单得多。
]

#workedexamples[
*例 1.* 用简便方法计算 `125 × 32`。

*解：* 把 `32` 拆成 `8 × 4`：`125 × 32 = 125 × 8 × 4 = 1000 × 4 = 4000`

*例 2.* 用简便方法计算 `25 × 16`。

*解：* 把 `16` 拆成 `4 × 4`：`25 × 16 = 25 × 4 × 4 = 100 × 4 = 400`
]

#keytakeaway[
- 乘法交换律：`a × b = b × a`。
- 乘法结合律：`(a × b) × c = a × (b × c)`。
- 常见的“好搭档”：`25 × 4 = 100`，`125 × 8 = 1000`。
]

#practice[
1. 用简便方法计算：`8 × 47 × 125`。
2. 用简便方法计算：`25 × 24`。
3. 用简便方法计算：`5 × 13 × 20`。
]

=== 乘法分配律

#explore[
学校买了 `30` 套桌椅，每张桌子 `45` 元，每把椅子 `25` 元。一共花了多少钱？你可以先算每套桌椅多少钱再乘以 `30`，也可以分别算桌子和椅子各多少钱再加起来。两种算法结果一样吗？
]

#understand[
乘法分配律：一个数与两个数的和相乘，等于这个数分别与这两个数相乘再相加。`a × (b + c) = a × b + a × c`

反过来也成立：`a × b + a × c = a × (b + c)`

这条规律也适用于减法：`a × (b - c) = a × b - a × c`

乘法分配律是最常用的简便运算工具。
]

#workedexamples[
*例 1.* 用简便方法计算 `102 × 45`。

*解：*
- `102 × 45 = (100 + 2) × 45`
- `= 100 × 45 + 2 × 45`
- `= 4500 + 90`
- `= 4590`

*例 2.* 用简便方法计算 `99 × 37`。

*解：*
- `99 × 37 = (100 - 1) × 37`
- `= 100 × 37 - 1 × 37`
- `= 3700 - 37`
- `= 3663`
]

#keytakeaway[
- 乘法分配律：`a × (b + c) = a × b + a × c`。
- 反向使用（提取公因数）同样重要：`a × b + a × c = a × (b + c)`。
- 遇到接近整十、整百的数，把它拆成“整数 ± 小数”再分配。
]

#practice[
1. 用简便方法计算：`35 × 98`。
2. 用简便方法计算：`72 × 56 + 72 × 44`。
3. 用简便方法计算：`101 × 67`。
4. 用简便方法计算：`48 × 25`（提示：`48 = 50 - 2` 或 `25 × 4 = 100`）。
]

#answer[
*练一练 加法交换律与结合律*

1. `88 + 46 + 12 = (88 + 12) + 46 = 100 + 46 = 146`。
2. `276 + 199 = 276 + 200 - 1 = 476 - 1 = 475`。
3. `75 + 168 + 25 + 32 = (75 + 25) + (168 + 32) = 100 + 200 = 300`。

*练一练 乘法交换律与结合律*

1. `8 × 47 × 125 = (8 × 125) × 47 = 1000 × 47 = 47000`。
2. `25 × 24 = 25 × 4 × 6 = 100 × 6 = 600`。
3. `5 × 13 × 20 = (5 × 20) × 13 = 100 × 13 = 1300`。

*练一练 乘法分配律*

1. `35 × 98 = 35 × (100 - 2) = 3500 - 70 = 3430`。
2. `72 × 56 + 72 × 44 = 72 × (56 + 44) = 72 × 100 = 7200`。
3. `101 × 67 = (100 + 1) × 67 = 6700 + 67 = 6767`。
4. `48 × 25 = (50 - 2) × 25 = 1250 - 50 = 1200`。也可以写成 `48 × 25 = 12 × (4 × 25) = 12 × 100 = 1200`。
]
