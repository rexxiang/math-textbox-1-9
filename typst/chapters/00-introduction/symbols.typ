#import "../../lib/theme.typ": *

== 符号约定

本书使用以下数学符号。如果在阅读中遇到不认识的符号，可以回到这里查阅。

=== 基本运算符号

- `+`：加，例 `3 + 5 = 8`
- `-`：减，例 `8 - 3 = 5`
- `×`：乘，例 `4 × 6 = 24`
- `÷`：除，例 `24 ÷ 6 = 4`
- `=`：等于，例 `2 + 3 = 5`
- `≠`：不等于，例 `3 ≠ 5`
- `≈`：约等于，例 `π ≈ 3.14`

=== 比较符号

- `>`：大于，例 `5 > 3`
- `<`：小于，例 `3 < 5`
- `≥`：大于或等于，例 `x ≥ 0`
- `≤`：小于或等于，例 `x ≤ 10`

=== 代数符号

- `x, y, z`：未知数 / 变量
- `a, b, c`：常数或已知量
- `f(x)`：函数 `f` 在 `x` 处的值
- `|a|`：`a` 的绝对值
- `a^n`：`a` 的 `n` 次幂
- `√a`：`a` 的算术平方根
- `±`：正或负

=== 几何符号

- `∠`：角
- `△`：三角形
- `∥`：平行
- `⊥`：垂直
- `≅`：全等
- `~`：相似
- `π`：圆周率（约 `3.14159`）
- `sin, cos, tan`：三角函数

=== 集合与逻辑符号

- `∈`：属于
- `ℕ`：自然数集
- `ℤ`：整数集
- `ℚ`：有理数集
- `ℝ`：实数集
- `∴`：所以
- `∵`：因为

=== 统计符号

- `x̄`：平均数
- `S²`：方差
- `P(A)`：事件 `A` 的概率
- `n`：样本量 / 数据个数

=== 本书特殊标记

- `§X.Y`：交叉引用（可点击链接），指向第 `X` 模块第 `Y` 章节
- 前置知识：学习当前内容前需要掌握的章节
- 适用年级：该知识点对应的中国课标年级
