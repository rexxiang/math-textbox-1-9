#import "../../lib/theme.typ": *

== §4.3 数据的波动程度 <sec-4-3>
#import "../../lib/theme.typ": *

在 #secref("4.2") 中，我们学习了用平均数、中位数和众数来描述数据的”中心”位置。但两组数据即使平均数相同，它们的*分布情况*也可能大不一样。本章学习如何衡量数据的*波动程度*（也叫离散程度），核心工具是*极差*和*方差*。

=== 极差

#explore[
甲、乙两位同学最近 5 次数学测验的成绩（分）如下：

#align(center)[
#table(
  columns: 6, stroke: 0.5pt, inset: 6pt,
  [], [第 1 次], [第 2 次], [第 3 次], [第 4 次], [第 5 次],
  [甲], [82], [78], [85], [80], [75],
  [乙], [95], [62], [88], [55], [100],
)
]

两人的平均分都等于 80 分，但甲的成绩比较稳定，乙的成绩忽高忽低。仅凭平均数无法区分它们。
]

#understand[
最简单的衡量波动的方法是看*最大值与最小值之差*。

*极差* = 最大值 - 最小值。

- 甲的极差：$85 - 75 = 10$（分）
- 乙的极差：$100 - 55 = 45$（分）

甲的极差远小于乙，说明甲的成绩波动更小。

极差的优点：计算简单，一目了然。

极差的局限：只用了最大值和最小值这两个数据，忽略了中间数据的信息。
]

#workedexamples[
*例 1.* 某工厂两条生产线一周内每天生产的零件数如下（单位：个）：

A 线：102, 98, 100, 101, 99, 103, 97

B 线：110, 88, 105, 92, 100, 108, 97

求两条生产线的极差。

解：A 线极差 6，B 线极差 22，所以 A 线产量更稳定。
]

#keytakeaway[
- 极差 = 最大值 - 最小值。
- 极差越大，说明数据的波动范围越大。
- 极差只考虑了两个极端数据，不能完全反映所有数据的波动情况。
]

=== 方差

#explore[
继续看甲、乙两位同学的 5 次成绩。仅凭极差还不够，因为中间数据的分布也很重要。我们需要考虑*每个数据*偏离平均数的情况。
]

#understand[
*第一步*：计算每个数据与平均数的偏差。

甲的平均分是 80，各次偏差为 2, -2, 5, 0, -5。

*第二步*：偏差之和为 0，不能直接相加。

*第三步*：把偏差取平方，消除正负号。

*第四步*：求这些偏差平方的平均值，这就是*方差*。

设 n 个数据为 x_1, x_2, …, x_n，平均数记为 m，则方差

S^2 = ((x_1 - m)^2 + (x_2 - m)^2 + … + (x_n - m)^2) / n

方差的意义：
- 方差衡量的是数据偏离平均数的*平均程度*。
- 方差越大，数据越分散，波动越剧烈。
- 方差越小，数据越集中，波动越平缓。
- 方差 = 0 当且仅当所有数据都相等。
]

#workedexamples[
*例 2.* 甲、乙两台机器各生产了 6 个零件，直径（单位：mm）如下：

甲：10.1, 10.0, 9.9, 10.2, 9.8, 10.0

乙：10.0, 10.1, 10.0, 9.9, 10.0, 10.0

哪台机器的产品质量更稳定？

解：两台机器平均值都为 10.0 mm。计算方差可得 A 线大一些，乙机器更稳定。

*例 3.* 数据 3, 5, 7, 5, 10 的方差是多少？

解：平均数 6，方差
$((3-6)^2 + (5-6)^2 + (7-6)^2 + (5-6)^2 + (10-6)^2)/5 = 5.6$
]

#keytakeaway[
- 方差 S^2 = ((x_1 - 平均数)^2 + … + (x_n - 平均数)^2) / n，它衡量数据偏离平均数的平均程度。
- 方差越大，数据波动越剧烈；方差越小，数据越稳定。
- 计算方差的步骤：求平均数 → 求各偏差 → 偏差取平方 → 求平方的平均。
]

#practice[
*1.* 求下列数据的方差：4, 6, 5, 8, 7。

*2.* 甲、乙两台机器各加工了 5 个零件，长度（单位：cm）如下：甲：20.1, 19.9, 20.0, 20.2, 19.8；乙：20.0, 20.0, 19.9, 20.1, 20.0。比较哪台更稳定。

*3.* 某组数据的平均数是 10，方差是 4。（1）每个数据都乘以 2 后，平均数和方差各是多少？（2）每个数据都加上 5 后，平均数和方差各是多少？
]

#answer[
*1.* 平均数 6，方差 2。

*2.* 两组平均值都接近 20 cm，乙的方差更小，因此乙机器更稳定。

*3.*（1）平均数变为 20，方差变为 16。（2）平均数变为 15，方差仍为 4。
]

#keytakeaway[
- 本书在初中阶段主要使用*方差*来比较数据波动程度。
- 标准差等更高阶内容不在本书此处展开。
]
