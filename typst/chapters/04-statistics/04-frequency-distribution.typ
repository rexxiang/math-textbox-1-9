#import "../../lib/theme.typ": *

== §4.4 频率分布直方图与频率分布表 <sec-4-4>
#import "../../lib/theme.typ": *

当数据量很大时，逐个列出所有数据既不方便也看不出规律。我们需要把数据*分组*，然后观察各组中数据出现的*频率*，从而了解数据的整体*分布*情况。本章学习频率分布表和频率分布直方图的编制与解读。

=== 频率与频次

#explore[
某校对八年级全体 200 名学生进行了一次体能测试，测得引体向上的成绩如下（部分数据）：

> 5, 8, 12, 3, 7, 10, 6, 9, 11, 4, 8, 7, 6, 10, 5, ...（共 200 个数据）

面对 200 个数据，直接观察几乎看不出任何规律。这些数据是集中在某个范围，还是分散得很开？大部分同学能做几个？
]

#understand[
*频次*（也叫频数）：某个数值或某个区间内的数据出现的*次数*。

*频率*：某个数值或某个区间内的数据出现的次数占总数据个数的比值。

频率 = 频次 / 数据总个数。

性质：
- 每个区间的频率不小于 0。
- 所有区间的频率之和 = 1（即 100%）。
]

#keytakeaway[
- 频次是数据落入某区间的*次数*，频率是频次占总数的*比例*。
- 所有区间的频率之和等于 1。
]

=== 频率分布表的编制

#explore[
继续上面的体能测试数据。为了了解全年级引体向上成绩的分布，老师决定把数据分组来分析。应该怎么分组？每组多宽合适？
]

#understand[
编制频率分布表的步骤：

1. 确定数据范围，找出最大值和最小值，计算极差。
2. 确定组距和组数，通常取 5 到 8 组。
3. 确定各组区间。为了不重不漏，通常采用左闭右开区间。
4. 逐个统计各区间的频次。
5. 计算各区间的频率。

例如，假设最小值 = 1，最大值 = 15，取组距 = 3，则可分成 5 组：

#align(center)[
#table(
  columns: 3, stroke: 0.5pt, inset: 6pt,
  [成绩区间], [频次], [频率],
  [1-4], [20], [0.10],
  [4-7], [52], [0.26],
  [7-10], [68], [0.34],
  [10-13], [44], [0.22],
  [13-16], [16], [0.08],
  [合计], [200], [1.00],
)
]

验证：频次之和 = 200，频率之和 = 1.00。
]

#workedexamples[
*例 1.* 某班 40 名同学的 100 米跑成绩（单位：秒）如下：

> 14.2, 15.0, 13.8, 16.1, 14.5, 15.3, 13.5, 14.8, 15.6, 14.0,
> 13.9, 15.2, 14.7, 16.0, 13.6, 14.3, 15.1, 14.9, 15.5, 13.7,
> 14.1, 15.4, 14.6, 16.2, 13.4, 14.4, 15.8, 14.2, 15.0, 13.8,
> 14.5, 15.3, 13.6, 14.8, 15.6, 14.0, 13.9, 15.2, 14.7, 16.0

组距取 0.5 秒，编制频率分布表，并观察大部分成绩集中在哪些区间。
]

#keytakeaway[
- 频率分布表编制的五步：确定范围 → 定组距和组数 → 确定区间 → 统计频次 → 计算频率。
- 区间采用“左闭右开”原则，确保数据不重不漏。
- 频次之和 = 数据总个数，频率之和 = 1。
]

=== 频率分布直方图的绘制与解读

#explore[
频率分布表给出了精确数字，但当我们需要*一眼看出*数据分布的形态（集中在哪里？是对称还是偏斜？）时，一张图远比一张表直观。
]

#understand[
*频率分布直方图*是将频率分布表中的信息用图形表示出来的一种统计图。

绘制方法：
1. 画出横轴（数据区间）和纵轴。
2. 纵轴表示频率/组距。
3. 在每个区间上画一个矩形，矩形宽度等于组距，高度等于该区间的频率/组距。
4. 因此，每个矩形的面积 = 频率。

#figure(caption: [频率分布直方图的示意])[
13.4-13.9 | =======
13.9-14.4 | ========
14.4-14.9 | ========
14.9-15.4 | ========
15.4-15.9 | =====
15.9-16.4 | ====
]

从直方图可以直观看出数据集中的区间和分布形态。
]

#workedexamples[
*例 2.* 根据例 1 的频率分布表，绘制频率分布直方图。 

*例 3.* 下面的频率分布直方图表示某次考试 50 名学生的成绩分布（组距为 10 分）：

#align(center)[
#table(
  columns: 2, stroke: 0.5pt, inset: 6pt,
  [成绩区间], [频率/组距],
  [40-50], [0.004],
  [50-60], [0.012],
  [60-70], [0.020],
  [70-80], [0.036],
  [80-90], [0.024],
  [90-100], [0.004],
)
]

（1）成绩在 70 到 80 分这一区间的频率是多少？有多少人？
（2）成绩在 70 分以上（含 70 分）的学生占多大比例？
]

#keytakeaway[
- 频率分布直方图中，每个矩形的面积 = 频率，所有面积之和 = 1。
- 纵轴标注频率/组距，保证面积具有频率意义。
- 从直方图可以直观看出数据集中的区间和分布形态。
]

=== 用频率分布直方图估计统计量

#explore[
如果我们只有一张频率分布直方图（原始数据已经丢失），还能估计出这组数据的平均数、中位数和众数吗？
]

#understand[
*估计众数*：众数大约位于最高矩形所对应区间的中点。

*估计中位数*：中位数就是使得左边所有矩形面积之和 = 0.5 的那条竖线的位置。

*估计平均数*：假设每个区间内的数据都集中在区间中点，用加权平均数估算。
]

#workedexamples[
*例 4.* 某次体检中，60 名学生的身高数据整理成如下频率分布表：

#align(center)[
#table(
  columns: 3, stroke: 0.5pt, inset: 6pt,
  [身高区间（cm）], [频次], [频率],
  [145-150], [3], [0.05],
  [150-155], [9], [0.15],
  [155-160], [18], [0.30],
  [160-165], [21], [0.35],
  [165-170], [6], [0.10],
  [170-175], [3], [0.05],
  [合计], [60], [1.00],
)
]

（1）绘制频率分布直方图（组距为 5 cm）。
（2）估计这组数据的众数、中位数和平均数。

解：众数约为 162.5 cm；中位数约为 160 cm；平均数约为 159.75 cm。
]

#keytakeaway[
- 从频率分布直方图估计众数：取最高矩形的区间中点。
- 从频率分布直方图估计中位数：找使左侧面积 = 0.5 的位置。
- 从频率分布直方图估计平均数：以各区间中点为代表值，用频率作为权重计算加权平均数。
- 这些都是*估计值*，不一定等于真实的统计量。
]

#practice[
*1.* 某校 100 名八年级学生 1 分钟跳绳成绩整理如下：

#align(center)[
#table(
  columns: 2, stroke: 0.5pt, inset: 6pt,
  [成绩区间（次）], [频次],
  [60-80], [8],
  [80-100], [22],
  [100-120], [35],
  [120-140], [25],
  [140-160], [10],
)
]

（1）补全频率分布表。
（2）计算各区间的频率/组距，并描述如何绘制频率分布直方图。
（3）成绩在 100 次以上（含 100 次）的学生占百分之几？

*2.* 根据下面的频率分布表，估计这组数据的众数、中位数和平均数。

#align(center)[
#table(
  columns: 2, stroke: 0.5pt, inset: 6pt,
  [数据区间], [频率],
  [10-20], [0.10],
  [20-30], [0.20],
  [30-40], [0.40],
  [40-50], [0.20],
  [50-60], [0.10],
)
]

*3.* 某频率分布直方图中，20 到 25 分这一区间的频率/组距为 0.08，组距为 5。（1）该区间的频率是多少？（2）如果数据共有 200 个，该区间有多少个数据？
]

#answer[
*1.* 频率依次为 0.08, 0.22, 0.35, 0.25, 0.10；成绩 100 次及以上的频率为 70%。

*2.* 众数约 35，中位数约 35，平均数约 35。

*3.*（1）频率 = 0.40。（2）数据个数 = 80。
]
