#import "../../lib/theme.typ": *

== §4.2 平均数、中位数与众数 <sec-4-2>
#import "../../lib/theme.typ": *

一组数据往往包含很多个数值。我们能不能用*一个数*来代表这组数据的“中心”位置？这就是*集中趋势*的问题。本章介绍三种最常用的集中趋势度量：平均数、中位数和众数。

=== 平均数（5 年级）

#explore[
学校组织植树活动，五个小组种树的棵数分别是：

> 第 1 组：8 棵，第 2 组：6 棵，第 3 组：10 棵，第 4 组：7 棵，第 5 组：9 棵。

老师问：“平均每组种了多少棵？”这个问题该怎么理解？
]

#understand[
*直觉理解*：如果把所有树重新平均分给每个小组，每组能分到几棵？

总棵数 $= 8 + 6 + 10 + 7 + 9 = 40$（棵）

平均每组 $= 40 ÷ 5 = 8$（棵）

*定义*：一组数据的*平均数*（也叫*算术平均数*），等于所有数据的总和除以数据的个数。

设 $n$ 个数据为 $x_1, x_2, …, x_n$，则

平均数 = (x_1 + x_2 + … + x_n) / n

*注意*：平均数不一定等于数据中的某个实际值。
]

#keytakeaway[
- 平均数 = 数据总和 / 数据个数。
- 平均数利用了每一个数据的信息，因此*每个数据都会影响平均数*。
- 平均数是最常用的集中趋势度量。
]

=== 加权平均数（6-7 年级）

#explore[
小华期末总评成绩的计算方法是：平时成绩占 30%，期中考试占 30%，期末考试占 40%。他的三项成绩分别是 85 分、78 分、92 分。

如果直接算普通平均数：$(85 + 78 + 92) ÷ 3 = 85$ 分。

但这没有考虑到各项成绩的重要程度不同——期末考试占比最大，应该对总评的影响也更大。
]

#understand[
当各数据的重要程度不同，需要计算*加权平均数*。每个数据乘以它的*权重*，然后求和。

加权平均数 = x_1 × w_1 + x_2 × w_2 + … + x_n × w_n

其中 $w_1 + w_2 + … + w_n = 1$。

小华的总评：

加权平均数 = 85 × 0.3 + 78 × 0.3 + 92 × 0.4 = 85.7（分）

另一种常见情境是频率作为权重。例如，10 名同学的跳远成绩中，3 人跳了 3.2 米，5 人跳了 3.5 米，2 人跳了 3.8 米，则

加权平均数 = 3.2 × 3/10 + 3.5 × 5/10 + 3.8 × 2/10 = 3.47（米）
]

#workedexamples[
*例 1.* 一家公司招聘，笔试成绩占 40%，面试成绩占 60%。小李笔试 80 分，面试 90 分；小王笔试 95 分，面试 75 分。谁的总评更高？

解：
- 小李总评：$80 × 0.4 + 90 × 0.6 = 86$ 分
- 小王总评：$95 × 0.4 + 75 × 0.6 = 83$ 分

所以小李总评更高。
]

#keytakeaway[
- 当各数据重要程度不同时，使用加权平均数。
- 权重越大的数据，对加权平均数的影响越大。
- 普通平均数是加权平均数的特例——所有权重相等。
]

=== 中位数（7 年级）

#explore[
某公司有 7 名员工，月薪（单位：元）分别是：

> 3000, 3500, 4000, 4200, 4500, 5000, 50000

计算平均数：$74200 ÷ 7 ≈ 10600$（元）。

但 7 个人中有 6 个人的月薪都低于 10600 元。这个“平均数”能代表这家公司大多数人的薪资水平吗？
]

#understand[
*中位数*是将数据从小到大排列后，处于正中间位置的那个数。

求法：
1. 将 $n$ 个数据从小到大排列。
2. 如果 $n$ 是奇数，中位数是第 $(n+1)/2$ 个数据。
3. 如果 $n$ 是偶数，中位数是第 $n/2$ 个与第 $n/2+1$ 个数据的平均值。

回到上面的例子，中位数是第 4 个数据，即 4200 元。

偶数个数据的例子：$2, 5, 7, 8, 10, 12, 15, 20$，中位数为 $(8 + 10) ÷ 2 = 9$。
]

#workedexamples[
*例 2.* 10 名同学的数学考试成绩为：72, 85, 90, 65, 78, 92, 88, 76, 95, 82。求中位数。

解：先排序为 65, 72, 76, 78, 82, 85, 88, 90, 92, 95，所以中位数是 $(82 + 85) ÷ 2 = 83.5$ 分。

*例 3.* 在例 2 中，如果新增一名同学的成绩为 80 分，中位数变为多少？

解：新排列为 65, 72, 76, 78, 80, 82, 85, 88, 90, 92, 95，共 11 个，中位数是第 6 个数据 82 分。
]

#keytakeaway[
- 中位数是排列后处于正中间位置的值。
- 中位数*不受极端值影响*，这是它相对于平均数的最大优势。
- 求中位数前，必须先将数据从小到大排列。
]

=== 众数（7 年级）

#explore[
一家鞋店想进货一批运动鞋。为了决定各号码的进货量，店员记录了一天中卖出的鞋子的号码：

> 38, 39, 40, 40, 40, 41, 41, 42, 40, 39, 40, 41, 40, 38, 40

你认为鞋店应该重点进哪个号码的货？
]

#understand[
这个问题中，平均数和中位数都不是最有用的。鞋店真正关心的是*哪个号码卖得最多*。

*众数*就是一组数据中*出现次数最多*的那个值。

统计各号码出现的次数：

#align(center)[
#table(
  columns: 5, stroke: 0.5pt, inset: 6pt,
  [号码], [38], [39], [40], [41], [42],
  [次数], [2], [2], [7], [3], [1],
)
]

40 号出现了 7 次，最多，所以众数是 40。

一组数据可以有*多个众数*，也可能*没有众数*。
]

#keytakeaway[
- 众数是出现次数最多的值。
- 众数最适合回答“哪个最常见”的问题。
- 众数不需要计算，只需要*数频次*。
]

=== 三种统计量的比较与选择（7 年级）

#explore[
某班 9 名同学参加公益活动的捐款金额（单位：元）为：

> 10, 10, 10, 20, 20, 30, 50, 50, 200

算一算：
- 平均数约为 44.4 元
- 中位数 20 元
- 众数 10 元

三种统计量给出了三个不同的数值！到底该用哪个来描述这组数据？
]

#understand[
#align(center)[
#table(
  columns: 4, stroke: 0.5pt, inset: 6pt,
  [统计量], [含义], [优势], [劣势],
  [平均数], [所有数据的“均匀化”值], [利用了全部数据信息], [受极端值影响大],
  [中位数], [排在正中间的值], [不受极端值影响], [没有利用全部数据的具体值],
  [众数], [出现最多的值], [反映“最常见”的情况], [可能不唯一或不存在；不能反映整体水平],
)
]

选择原则：
- 数据分布比较均匀、没有极端值时，平均数最常用。
- 有明显极端值时，中位数更合适。
- 关心“出现最多的是哪个”时，用众数。
]

#workedexamples[
*例 4.* 两个射击运动员在 10 次练习中的成绩（环数）如下：

甲：7, 8, 7, 9, 8, 10, 7, 8, 8, 8

乙：5, 10, 10, 6, 8, 10, 9, 4, 10, 8

（1）分别求甲、乙的平均数、中位数和众数。
（2）你认为谁的成绩更好？

解：
- 甲：平均数 8，中位数 8，众数 8。
- 乙：平均数 8，中位数 8.5，众数 10。
- 若看稳定性，甲更好；若看高分能力，乙更突出。实际问题中往往要综合判断。
]

#keytakeaway[
- 平均数、中位数、众数分别从不同角度描述数据的“中心”。
- 没有哪个统计量是万能的，选择取决于*数据特征*和*分析目的*。
- 当数据有极端值时，中位数通常比平均数更有代表性。
]

#practice[
*1.* 某超市一周内某款饮料每天的销量（瓶）为：45, 52, 38, 52, 60, 52, 41。求平均数、中位数和众数。

*2.* 某班 8 名同学 1 分钟仰卧起坐的个数为：25, 30, 28, 32, 45, 29, 31, 28。（1）求平均数和中位数。（2）如果有一名同学的成绩改为 80 个，平均数和中位数各变为多少？（3）哪个统计量受极端值的影响更小？

*3.* 一次面试中，综合成绩由笔试（权重 0.3）、面试表现（权重 0.5）和简历评分（权重 0.2）三部分组成。张三三项得分分别为 88 分、92 分、75 分，李四为 95 分、80 分、90 分。谁的综合成绩更高？
]

#answer[
*1.* 排列后为 38, 41, 45, 52, 52, 52, 60。平均数约 48.6， 中位数 52，众数 52。

*2.* 排列后为 25, 28, 28, 29, 30, 31, 32, 45。平均数 31，中位数 29.5。把 45 改成 80 后，平均数变为 35.375，中位数仍为 29.5。

*3.* 张三：87.4 分；李四：86.5 分；所以张三更高。
]
