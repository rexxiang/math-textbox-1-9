#import "../../lib/theme.typ": *

== §3.9 坐标几何 <sec-3-9>

#box(
  inset: 10pt,
  fill: luma(245),
  stroke: (paint: luma(180), thickness: 0.6pt),
  radius: 3pt,
)[
  *前置知识：* #secref("2.10"), #secref("3.1")

  *适用年级：* 8 年级
]

=== 平面直角坐标系

#explore[
电影院里，你的座位票上写着“5 排 12 号”。这两个数字精确地确定了你在整个影厅中的位置。数学中也有类似的方法——用两个数来确定平面上一个点的位置。
]

#understand[
在平面上画两条互相垂直且有公共原点的数轴，就构成了*平面直角坐标系*（笛卡尔坐标系）。

#figure(caption: [直角坐标系：点 $P(x,y)$ 由横坐标和纵坐标唯一确定])[
  #box(
    inset: 8pt,
    fill: luma(248),
    stroke: (paint: luma(200), thickness: 0.6pt),
    radius: 3pt,
  )[
    *基本关系*
    - $x$ 轴水平，向右为正
    - $y$ 轴竖直，向上为正
    - 原点为 $O$
  ]
]

坐标系把平面分成四个*象限*：

#table(
  columns: 3,
  inset: 6pt,
  align: (left, left, left),
  [*象限*], [*$x$ 的符号*], [*$y$ 的符号*],
  [第一象限], [$+$], [$+$],
  [第二象限], [$-$], [$+$],
  [第三象限], [$-$], [$-$],
  [第四象限], [$+$], [$-$],
)

坐标轴上的点不属于任何象限。
]

#workedexamples[
*例 1.* 判断 $A(3,2)$、$B(-1,4)$、$C(0,-5)$、$D(-3,-2)$ 在哪个象限或坐标轴上。

*解：* $A$ 在第一象限，$B$ 在第二象限，$C$ 在 $y$ 轴上，$D$ 在第三象限。

*例 2.* 若点 $P(a,b)$ 在第四象限，则 $a$ 和 $b$ 分别满足什么条件？

*解：* $a > 0$，$b < 0$。

*例 3.* 若点 $P(m-1,m+2)$ 在 $y$ 轴上，求 $m$ 的值和点 $P$ 的坐标。

*解：* $m = 1$，所以 $P = (0,3)$。
]

#keytakeaway[
- 平面直角坐标系用有序数对 $(x,y)$ 表示点的位置。
- 四个象限由两条坐标轴划分，坐标轴上的点不属于任何象限。
- $x$ 轴上的点纵坐标为 $0$，$y$ 轴上的点横坐标为 $0$。
]

#practice[
1. 写出下列点所在的象限或坐标轴：$A(5,-3)$、$B(-4,0)$、$C(-2,-7)$、$D(0,0)$。
2. 若点 $P(2a-4,a+1)$ 在第二象限，求 $a$ 的取值范围。
]

=== 点的坐标

#explore[
在方格纸上画一个坐标系，标出几个格点，读出它们的坐标，这就是“点的坐标”最基本的操作。反过来，给定坐标 $(3,-2)$，你也能准确地在方格纸上找到这个点。
]

#understand[
坐标的读取：从点向 $x$ 轴作垂线，垂足在 $x$ 轴上的读数即为*横坐标*；从点向 $y$ 轴作垂线，垂足在 $y$ 轴上的读数即为*纵坐标*。

特殊点的坐标：
- 原点：$(0,0)$
- $x$ 轴上的点：$(a,0)$
- $y$ 轴上的点：$(0,b)$

坐标与几何位置的关系：
- 横坐标相同的两个点在同一条竖直线上。
- 纵坐标相同的两个点在同一条水平线上。
]

#workedexamples[
*例 1.* 画出 $A(2,3)$、$B(-1,2)$、$C(3,-1)$、$D(-2,-3)$，并说明它们分别在哪个象限。

*解：* $A$ 第一象限，$B$ 第二象限，$C$ 第四象限，$D$ 第三象限。

*例 2.* 已知 $A(-3,0)$、$B(0,4)$，求线段 $"AB"$ 的长度。

*解：* $"AB" = 5$。

*例 3.* 正方形 $"ABCD"$ 的顶点 $A(0,0)$、$B(4,0)$。$C$、$D$ 在 $x$ 轴上方，求坐标。

*解：* $D = (0,4)$，$C = (4,4)$。
]

#keytakeaway[
- 坐标 $(x,y)$ 中，$x$ 是横坐标，$y$ 是纵坐标。
- 坐标的本质是用数来描述位置。
]

#practice[
1. 在坐标平面上标出以下各点：$A(0,4)$、$B(3,0)$、$C(-2,3)$、$D(5,-2)$。
2. 等腰直角三角形 $"OAB"$ 中，$O$ 为原点，$A(6,0)$，$B$ 在 $y$ 轴正半轴上，求 $B$ 的坐标。
]

=== 用坐标表示平移

#explore[
电子游戏中，角色向右走 $3$ 步、向上跳 $2$ 步，角色的坐标就从 $(x,y)$ 变成了 $(x+3,y+2)$。平移在坐标中有非常简洁的表达。
]

#understand[
将图形向右平移 $a$ 个单位、向上平移 $b$ 个单位，则每个点的坐标变化为

#align(center)[
$(x, y) → (x + a, y + b)$
]

向左平移 $a$ 个单位等同于 $a$ 取负值，向下平移 $b$ 个单位等同于 $b$ 取负值。
]

#workedexamples[
*例 1.* $△ "ABC"$ 的顶点为 $A(1,2)$、$B(4,1)$、$C(3,5)$。将三角形向左平移 $3$ 个单位、向下平移 $2$ 个单位，求新的顶点坐标。

*解：* $A'=(-2,0)$，$B'=(1,-1)$，$C'=(0,3)$。

*例 2.* 点 $P(2,5)$ 经平移后到达 $P'(-1,3)$。求平移的方向和距离。

*解：* 向左 $3$、向下 $2$，平移距离为 $sqrt(13)$。

*例 3.* 直线 $y = 2x + 1$ 向上平移 $3$ 个单位后的方程是什么？

*解：* 新方程为 $y = 2x + 4$。
]

#keytakeaway[
- 平移在坐标中表现为坐标的加减：$(x,y) → (x+a,y+b)$。
- 平移不改变图形形状和大小，也不改变直线的斜率。
]

#practice[
1. 点 $A(-3,4)$ 向右平移 $5$、向下平移 $3$ 后的坐标。
2. 线段 $"AB"$ 中，$A(1,2)$、$B(5,6)$。将 $"AB"$ 向右平移 $3$ 个单位后，新线段的两端点坐标是什么？新线段的长度与原来相比如何？
]

=== 用坐标表示轴对称

#explore[
镜子放在 $y$ 轴的位置，你站在 $(3,2)$，你在镜中的像在哪里？答案是 $(-3,2)$——横坐标变号，纵坐标不变。
]

#understand[
*关于 $x$ 轴对称*：$(x,y) → (x,-y)$

*关于 $y$ 轴对称*：$(x,y) → (-x,y)$

*关于原点对称*：$(x,y) → (-x,-y)$

#table(
  columns: 3,
  inset: 6pt,
  align: (left, left, left),
  [*对称类型*], [*坐标变换*], [*什么变、什么不变*],
  [关于 $x$ 轴], [$(x,y) → (x,-y)$], [$x$ 不变，$y$ 变号],
  [关于 $y$ 轴], [$(x,y) → (-x,y)$], [$y$ 不变，$x$ 变号],
  [关于原点], [$(x,y) → (-x,-y)$], [$x$、$y$ 都变号],
)
]

#workedexamples[
*例 1.* 点 $A(3,-4)$ 关于 $x$ 轴、$y$ 轴、原点的对称点分别是什么？

*解：* 分别为 $(3,4)$、$(-3,-4)$、$(-3,4)$。

*例 2.* $△ "ABC"$ 的顶点为 $A(1,3)$、$B(4,1)$、$C(2,5)$。写出关于 $x$ 轴对称后的顶点坐标。

*解：* $A'=(1,-3)$，$B'=(4,-1)$，$C'=(2,-5)$。

*例 3.* 点 $P(a,b)$ 关于 $y$ 轴的对称点为 $Q$，$Q$ 关于 $x$ 轴的对称点为 $R$。求 $R$ 的坐标。

*解：* $R=(-a,-b)$，它就是 $P$ 关于原点的对称点。
]

#keytakeaway[
- $x$ 轴对称变 $y$，$y$ 轴对称变 $x$，原点对称都变。
- 两次轴对称（$x$ 轴 + $y$ 轴）$=$ 一次中心对称（关于原点）。
]

#practice[
1. 点 $M(-5,2)$ 关于 $x$ 轴的对称点是\_\_\_，关于 $y$ 轴的对称点是\_\_\_，关于原点的对称点是\_\_\_。
2. 直线 $y = x + 2$ 关于 $x$ 轴对称后的方程是什么？
]

=== 两点之间的距离公式

#explore[
两座城市在地图上的坐标分别是 $(2,3)$ 和 $(5,7)$。它们之间的“直线距离”是多少格？直觉告诉我们可以用勾股定理来算。
]

#understand[
*两点之间的距离公式*：

设 $A(x_1,y_1)$ 和 $B(x_2,y_2)$，则

#align(center)[
$"AB" = sqrt((x_2 - x_1)^2 + (y_2 - y_1)^2)$
]

#figure(caption: [两点间距离公式本质上是勾股定理])[
  #box(
    inset: 8pt,
    fill: luma(248),
    stroke: (paint: luma(200), thickness: 0.6pt),
    radius: 3pt,
  )[
    *关键关系*
    - 水平差：$| x_2 - x_1 |$
    - 竖直差：$| y_2 - y_1 |$
    - 斜边：$"AB"$
  ]
]

特例：原点 $O(0,0)$ 到点 $P(x,y)$ 的距离为 $"OP" = sqrt(x^2 + y^2)$。
]

#workedexamples[
*例 1.* 求 $A(1,2)$ 与 $B(4,6)$ 之间的距离。

*解：* $"AB" = 5$。

*例 2.* 已知 $A(-1,3)$、$B(2,-1)$，求 $"AB"$ 的长度。

*解：* $"AB" = 5$。

*例 3.* 已知 $A(0,0)$、$B(6,0)$、$C(3,4)$，证明 $△ "ABC"$ 是等腰三角形。

*解：* $"AC" = "BC" = 5$，所以是等腰三角形。

*例 4.* 在 $x$ 轴上找一点 $P$，使 $"PA" = "PB"$，其中 $A(1,3)$、$B(5,1)$。

*解：* $P = (2,0)$。
]

#keytakeaway[
- 距离公式 $= sqrt((x_2 - x_1)^2 + (y_2 - y_1)^2)$，本质是勾股定理。
- 距离公式可以用于判断三角形类型、求线段长度等。
]

#practice[
1. 求 $A(-2,1)$ 与 $B(4,-3)$ 之间的距离。
2. 已知 $A(0,0)$、$B(4,3)$、$C(0,5)$，判断 $△ "ABC"$ 的形状（按边分类）。
3. 在 $y$ 轴上找一点 $P$，使 $P$ 到 $A(3,4)$ 的距离为 $5$。
]

=== 中点坐标公式

#explore[
两个好朋友住在城市的不同地方，他们想找一个“正中间”的地方见面。在坐标地图上，这个“正中间”的位置就是两点的中点。
]

#understand[
*中点坐标公式*：

设 $A(x_1,y_1)$ 和 $B(x_2,y_2)$ 的中点为 $M$，则

#align(center)[
$M = ((x_1 + x_2)/(2), (y_1 + y_2)/(2))$
]

理解：中点的横坐标是两端点横坐标的平均值，纵坐标是两端点纵坐标的平均值。
]

#workedexamples[
*例 1.* 求 $A(2,6)$ 与 $B(8,4)$ 的中点坐标。

*解：* $M = (5,5)$。

*例 2.* 线段 $"AB"$ 的中点为 $M(3,-1)$，$A(1,2)$。求 $B$ 的坐标。

*解：* $B = (5,-4)$。

*例 3.* 平行四边形 $"ABCD"$ 的三个顶点为 $A(0,0)$、$B(4,0)$、$C(5,3)$。求 $D$ 的坐标。

*解：* $D = (1,3)$。

*例 4.* 已知 $△ "ABC"$ 的三个顶点为 $A(0,6)$、$B(-4,0)$、$C(4,0)$。求 $"BC"$ 边上的中线 $"AM"$ 的长度。

*解：* $M=(0,0)$，故 $"AM"=6$。
]

#keytakeaway[
- 中点坐标 $= ((x_1+x_2)/(2), (y_1+y_2)/(2))$，即两端点坐标的平均值。
- 中点公式常与距离公式配合使用。
- 利用对角线中点相同可求平行四边形的顶点。
]

#practice[
1. 求 $A(-3,5)$ 与 $B(7,-1)$ 的中点坐标。
2. 线段 $"AB"$ 的中点为 $M(4,3)$，$A(2,7)$，求 $B$ 的坐标。
3. $△ "ABC"$ 中，$A(2,4)$、$B(6,0)$、$C(-2,-2)$。求三条中线的交点（重心）坐标。
]

=== 参考答案

#answer[
1. 第一象限；第二象限；第三象限；原点。
2. $-1 < a < 2$。
3. 略。
4. $B = (0,6)$。
5. $(2,1)$。
6. $A' = (4,2)$，$B' = (8,6)$；长度不变。
7. $(3,4)$、$(-3,4)$、$(-3,-4)$。
8. $y = -x - 2$。
9. $2sqrt(13)$。
10. $"AB" = "AC" = 5$，$"BC" = 2sqrt(5)$，等腰三角形。
11. 有两个点：$(0,8)$ 或 $(0,0)$。
12. $M = (2,2)$。
13. $B = (6,-1)$。
14. 重心为 $(2, (2)/(3))$。
]
