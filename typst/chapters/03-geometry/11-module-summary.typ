#import "../../lib/theme.typ": *
#import "../../lib/diagram-packages.typ": cetz, fletcher
#import fletcher: node, edge

== 模块三总结：几何

#box(
  inset: 10pt,
  fill: luma(245),
  stroke: (paint: luma(180), thickness: 0.6pt),
  radius: 3pt,
)[
  *模块定位：* 几何部分的收束与复盘
]

=== 易错提醒

==== 1. 全等判定条件不能用 SSA

已知两边和非夹角（SSA）不能判定两个三角形全等。需要用 SSS、SAS、ASA、AAS 或 HL 来判定全等。

==== 2. 圆周角与圆心角混淆

圆周角等于同弧圆心角的*一半*，不是相等。若圆周角是 $80°$，则圆心角是 $160°$。

==== 3. 平行线的“判定”与“性质”方向搞反

- *判定*：角的关系 $=>$ 平行（用来证明两直线平行）
- *性质*：平行 $=>$ 角的关系（用来求角的度数）

==== 4. 等腰三角形忘记分类讨论

题目说“等腰三角形的一个角是 $x°$”，必须考虑这个角是顶角还是底角两种情况，除非条件明确排除某一种。

==== 5. 勾股定理误用于非直角三角形

勾股定理 $a^2 + b^2 = c^2$ 只适用于直角三角形。使用前必须先确认（或证明）三角形有一个直角。

==== 6. 三角函数的边搞混

$sin$、$cos$、$tan$ 的定义都是相对于某个特定角而言的。记住：对边是角“对面”的边，邻边是角“旁边”的直角边，斜边永远是最长的那条。

=== 知识拓展

==== 非欧几何简介

欧几里得几何建立在五条公理之上。若替换平行公理，还可以得到双曲几何和椭圆几何。地球表面近似于椭圆几何，广义相对论也提示时空可能并非完全欧几里得。

==== 四色定理

地图着色只需四种颜色就够了。这个命题曾困扰数学家一百多年，最终由计算机辅助证明。

==== 欧拉公式：多面体的秘密

对于任意凸多面体，若用 $V$ 表示顶点数、$E$ 表示棱数、$F$ 表示面数，则

#align(center)[
$V - E + F = 2$
]

==== 黄金比例

黄金比例

#align(center)[
$φ = (1 + sqrt(5))/(2) ≈ 1.618$
]

出现在正五边形、自然形态和艺术设计中。

=== 知识脉络回顾

==== 基础层：几何的语言

#align(center)[
点、线、面（#secref("3.1")） → 角（#secref("3.2")） → 平行与垂直（#secref("3.3")）
]

==== 核心层：图形的性质

#align(center)[
三角形（#secref("3.4")） ↔ 四边形（#secref("3.5")）
]

#align(center)[
圆（#secref("3.6")）
]

==== 工具层：计算与变换

#align(center)[
勾股定理（#secref("3.7")） → 坐标几何（#secref("3.9")） → 三角函数（#secref("3.10")）
]

#align(center)[
图形变换（#secref("3.8")）
]

=== 全景图

#figure(caption: [几何模块的知识链])[
  #fletcher.diagram(
    spacing: (8mm, 10mm),
    node-stroke: 0.5pt,
    // Foundation layer
    node((0, 0), [§3.1 点线面]),
    node((1, 0), [§3.2 角]),
    node((2, 0), [§3.3 平行垂直]),
    // Shape layer
    node((0, 1), [§3.4 三角形]),
    node((1, 1), [§3.5 四边形]),
    node((2, 1), [§3.6 圆]),
    // Application layer
    node((0, 2), [§3.7 勾股定理]),
    node((1, 2), [§3.8 变换]),
    node((2, 2), [§3.9 坐标几何]),
    node((1, 3), [§3.10 三角函数]),
    // Edges showing dependencies
    edge((0,0), (1,0), "->"),
    edge((1,0), (2,0), "->"),
    edge((0,0), (0,1), "->"),
    edge((1,0), (0,1), "->"),
    edge((2,0), (0,1), "->"),
    edge((0,1), (1,1), "->"),
    edge((0,1), (2,1), "->"),
    edge((0,1), (0,2), "->"),
    edge((2,0), (1,2), "->"),
    edge((0,1), (2,2), "->"),
    edge((0,2), (2,2), "->"),
    edge((0,1), (1,3), "->"),
    edge((2,2), (1,3), "->"),
  )
]

每一章都建立在前面章节的基础上，同时又为后续的学习做好准备。回顾这条知识链，你会发现几何的发展脉络清晰而自然：从最朴素的“点线面”概念出发，经过图形性质的探索，最终用代数工具将几何问题“数值化”。
