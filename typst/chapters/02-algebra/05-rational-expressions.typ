#import "../../lib/theme.typ": *

== §2.5 分式 <sec-2-5>

*前置知识：* #secref("2.3") 代数式与整式，#secref("2.4") 因式分解

*适用年级：* 8 年级

=== 分式的概念

#explore[
分数的分子和分母都可以是整式。分式像分数一样，也有“分母不能为 0”这个底线。
]

#understand[
*分式* 形如 $A/B$，其中 $A$、$B$ 都是整式，并且 $B ≠ 0$。

分式的值依赖于字母取值，所以先要确定分母不为零。
]

#workedexamples[
*例 1.* $x/3$、$(a+b)/(c-d)$、$(2x+1)/(x-1)$ 都是分式。

*例 2.* 当 $x=1$ 时，$(x+2)/(x-1)$ 没有意义，因为分母为 $0$。
]

#keytakeaway[
- 分式本质上是“整式之比”。
- 分母不为零是第一原则。
]

#practice[
1. 判断 $(x+1)/(2x-3)$ 是否为分式。
2. 当 $x=2$ 时，$(x^2-4)/(x-2)$ 是否有意义？
]

=== 分式的基本性质

#explore[
分数可以“约分”，分式也可以。关键是分子、分母同时乘或除同一个不为零的整式。
]

#understand[
分式的基本性质：

$A/B = (A × C) / (B × C) = (A / C) / (B / C)$（$C ≠ 0$）。

这使得约分和通分成为可能。
]

#workedexamples[
*例 1.* $(2x)/(4x^2) = 1/(2x)$（$x ≠ 0$）。

*例 2.* $(x^2-9)/(x-3) = (x+3)(x-3)/(x-3) = x+3$（$x ≠ 3$）。
]

#keytakeaway[
- 约分前要先因式分解。
- 所有变形都必须保持分式值不变。
]

#practice[
1. 化简 $(6a)/(9b)$。
2. 化简 $(x^2-16)/(x-4)$。
3. 说明为什么 $(x+1)/(x+1)$ 不能简单地在 $x=-1$ 时约分。
]

=== 分式的加减乘除运算

#explore[
分式运算和分数运算很像：先统一分母，再进行加减；乘除则按因式直接运算。
]

#understand[
- 加减：先通分，再合并。
- 乘法：分子乘分子，分母乘分母。
- 除法：除以一个分式，等于乘它的倒数。
]

#workedexamples[
*例 1.* $1/x + 1/y = (x + y) / (x y)$。

*例 2.* $(2a/b) × (3b/5c) = 6a/5c$。

*例 3.* $(x/y) ÷ (2 z/ (3 w)) = (x/y) × ((3 w) / (2 z)) = 3 w x / (2 y z)$。
]

#keytakeaway[
- 乘除比加减更直接。
- 复杂表达式先分解因式，再运算更容易。
]

#practice[
1. 计算 $1/x + 1/(x+1)$。
2. 计算 $(3a/2b) × (4b/5c)$。
3. 计算 $(x-1)/x ÷ (2/x)$。
]

=== 分式方程及其应用

#explore[
当未知数出现在分母里时，就得到分式方程。解这种方程时，最重要的是检查是否产生增根。
]

#understand[
解分式方程的一般步骤：

1. 找最简公分母。
2. 两边同乘最简公分母，化为整式方程。
3. 解整式方程。
4. 检验是否使原分母为 $0$。
]

#workedexamples[
*例 1.* 解 $1/x + 1/(x+1)=1$。

*例 2.* 解 $(x+2)/(x-1)=3$。

*例 3.* 分式方程的答案一定要代回原方程检验。
]

#keytakeaway[
- 分式方程最怕增根。
- 化为整式方程后，还要回头检查分母。
]

#practice[
1. 解 $1/(x-2)=2$。
2. 解 $(x+1)/(x-1)=2$。
3. 为什么解分式方程后必须检验？
]

#answer[
*参考答案：*

1. $x=5/2$。
2. $x=3$。
3. 因为同乘最简公分母可能引入增根。
]
