#import "../../lib/theme.typ": *

== §2.7 二元一次方程组 <sec-2-7>

*前置知识：* #secref("2.6") 一元一次方程

*适用年级：* 8 年级

=== 二元一次方程与方程组

#explore[
现实问题里常常需要两个未知数。此时，一个方程不够，就要用两个方程联立求解。
]

#understand[
形如 $a x + b y = c$ 的方程叫做 *二元一次方程*。

含有两个未知数的两个方程合在一起，叫做 *二元一次方程组*。

方程组的解要同时满足两个方程。
]

#workedexamples[
*例 1.* 检验 $(x,y)=(2,3)$ 是否满足 $x+y=5$ 和 $45x+30y=180$。

*例 2.* 方程组 $x+y=3, 2x-y=0$ 的解是 $(1,2)$。
]

#keytakeaway[
- 方程组的解必须同时满足所有方程。
- 代入检验很重要。
]

#practice[
1. 判断 $(3,2)$ 是否满足 $x-y=1, 2x+y=8$。
2. 写出一个你认为容易求解的二元一次方程组。
]

=== 代入消元法

#explore[
如果一个方程里已经能把一个未知数表示出来，就可以把它代入另一个方程，把“两个未知数”变成“一个未知数”。
]

#understand[
代入消元法的思路：

1. 先把一个方程变成 $y=$ 或 $x=$ 的形式。
2. 代入另一个方程。
3. 解出一个未知数，再回代求另一个。
]

#workedexamples[
*例 1.* 解 $y=2x-1, 3x+2y=5$，得 $x=1$，$y=1$。

*例 2.* 解 $x+2y=7, 3x-y=7$，得 $x=3$，$y=2$。

*例 3.* 解 $2x+3y=12, x=y+1$，得 $x=3$，$y=2$。
]

#keytakeaway[
- 代入法适合“一个方程已经显式表示未知数”的情况。
- 代入后要回到原方程检查。
]

#practice[
1. 用代入法解 $y=x+3, 2x+y=9$。
2. 用代入法解 $x-y=4, 3x+2y=7$。
]

=== 加减消元法

#explore[
当两个方程中某个未知数的系数正好相同或相反时，把两个方程相加或相减，就能把这个未知数消掉。
]

#understand[
加减消元法的核心是“消掉一个未知数”。

- 系数相反时，直接相加。
- 系数相同时，直接相减。
- 系数不同，可先乘一个合适的数再消元。
]

#workedexamples[
*例 1.* 解 $3x+2y=16, 3x-2y=8$，相加得 $6x=24$，所以 $x=4$，再求 $y=2$。

*例 2.* 解 $2x+3y=13, 5x+3y=22$，相减得 $3x=9$，所以 $x=3$，再求 $y=7/3$。

*例 3.* 解 $4x-5y=-3, 3x+2y=12$，可先乘合适的数后再消元。
]

#keytakeaway[
- 看到“同系数”就想到加减消元。
- 必要时先统一系数。
]

#practice[
1. 用加减法解 $x+y=10, x-y=4$。
2. 用加减法解 $2x+y=7, 3x+y=9$。
]

=== 三元一次方程组（简单）

#explore[
三个未知数时，通常需要三条独立关系；方法上仍然是“消元”。
]

#understand[
三元方程组可以先消去一个未知数，化成二元一次方程组，再继续求解。
]

#workedexamples[
*例 1.* 解 $x+y+z=10, x-y=2, y-z=1$。

*例 2.* 解 $x+y=4, y+z=6, x+z=8$。
]

#keytakeaway[
- 三元方程组先降维，再求解。
- 消元顺序要清楚。
]

#practice[
1. 解 $x+y+z=12, x-y=4, y-z=2$。
2. 解 $2x+y-z=3, x-y+z=5, 3x+2y-z=6$。
]

=== 方程组的应用

#explore[
很多应用题里，两个量互相关联，最自然的建模方式就是设两个未知数，列两个方程。
]

#understand[
列方程组时要先确定：

- 两个未知数分别表示什么。
- 等量关系从哪里来。
- 最终答案是否符合实际。
]

#workedexamples[
*例 1.* 上衣 $x$ 元，裤子 $y$ 元，若两套搭配满足 $x+2y=250, 2x+y=290$，则上衣 $110$ 元、裤子 $70$ 元。

*例 2.* 鸡兔同笼：设鸡 $x$ 只、兔 $y$ 只，$x+y=20, 2x+4y=56$，得鸡 $12$ 只、兔 $8$ 只。

*例 3.* 某班男生和女生人数满足总数与差数关系，也可列方程组求解。
]

#keytakeaway[
- 方程组适合描述“两个未知量、两条关系”的问题。
- 建模后再选择代入法或加减法。
]

#practice[
1. 解 $x+y=50, x-y=10$。
2. 解 $2x+3y=11, x-y=1$。
3. 若长方形周长 $28$ 厘米，长比宽多 $2$ 厘米，求长和宽。
]

#answer[
*参考答案：*

1. $x=30$，$y=20$。
2. $x=4$，$y=3$。
3. 长 $8$ 厘米，宽 $6$ 厘米。
]
