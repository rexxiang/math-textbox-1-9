#import "../../lib/theme.typ": *

== §2.1 有理数 <sec-2-1>

*前置知识：* #secref("1.1") 整数的认识，#secref("1.5") 分数，#secref("1.6") 小数

*适用年级：* 7 年级

=== 负数的引入

#explore[
冬天的“零下 15 度”、海拔“低于海平面 430 米”、账户“欠款 300 元”，都表示与通常方向相反的量。只有正数和 0 不够用，数学需要负数。
]

#understand[
*正数* 是大于 $0$ 的数，如 $1$、$2.5$、$3/4$。

*负数* 是在正数前加负号的数，如 $-1$、$-2.5$、$-3/4$。

$0$ 既不是正数，也不是负数。

引入负数后，很多现实中的“反方向”量都能被精确表达。
]

#workedexamples[
*例 1.* 用正负数表示：零上 8°C、零下 3°C、海拔 $+200$ 米、海拔 $-50$ 米。

*解：* 分别记为 +8°C、-3°C、$+200$ 米、$-50$ 米。

*例 2.* 盈利 $20$ 万元、亏损 $5$ 万元、盈利 $12$ 万元，可记为 $+20$、$-5$、$+12$（万元）。

*例 3.* 上升 $5$ 层再下降 $8$ 层，位移为 $5 + (-8) = -3$（层）。
]

#keytakeaway[
- 正数和负数表示意义相反的量。
- $0$ 是分界点。
- 数轴上 $0$ 的左边是负数，右边是正数。
]

#practice[
1. 用正负数表示“收入 $1500$ 元”和“支出 $800$ 元”。
2. 向东走 $3$ 千米记为 $+3$ 千米，那么向西走 $7$ 千米记为什么？
3. 海拔 $-155$ 米表示什么？
4. 北京最高气温 5°C、最低气温 -7°C，温差是多少？
5. 判断对错：$0$ 是最小的正数。
]

=== 有理数的分类

#explore[
正整数、零、负整数、正分数、负分数都属于什么“总集合”？答案就是有理数。
]

#understand[
*有理数* 是整数和分数的统称。

按符号分类：

- 正有理数：正整数和正分数。
- $0$：单独成类。
- 负有理数：负整数和负分数。

按是否为整数分类：

- 整数：正整数、$0$、负整数。
- 分数：正分数、负分数。

一个数如果能写成 $p/q$（$q ≠ 0$）的形式，它就是有理数。
]

#workedexamples[
*例 1.* 把 $-7$、$2/3$、$0$、$-3.5$、$100$、$-1/4$、$0.8$、$-11$ 分类。

*解：*
- 正整数：$100$
- 负整数：$-7, -11$
- 正分数：$2/3, 0.8$
- 负分数：$-3.5, -1/4$

*例 2.* $-3.0$ 是整数还是分数？它等于 $-3$，所以是负整数。

*例 3.* $6/2 = 3$，虽然写成分数形式，但本质上是整数。
]

#keytakeaway[
- 有理数 = 整数 + 分数。
- $0$ 是整数，但不是正数或负数。
- 分类前最好先化简。
]

#practice[
1. $-5/1$ 是整数还是分数？
2. 把 $3$、$-7/2$、$0$、$1.5$、$-9$、$4/2$ 分类。
3. 所有分数都是有理数吗？
4. $0$ 是最小的整数吗？
5. 有理数中最大的负整数是多少？
]

=== 数轴、相反数、绝对值

#explore[
温度计把温度分成上下两边，这正像一条直线上的点表示不同的数。数轴就是把“点”和“数”一一对应起来。
]

#understand[
*数轴* 需要三要素：原点、正方向、单位长度。

*相反数*：只有符号不同的两个数互为相反数，例如 $3$ 和 $-3$。$0$ 的相反数还是 $0$。

*绝对值*：数轴上一个点到原点的距离，记作 $|a|$。

- 若 $a > 0$，则 $|a| = a$。
- 若 $a = 0$，则 $|a| = 0$。
- 若 $a < 0$，则 $|a| = -a$。
]

#workedexamples[
*例 1.* 在数轴上标出 $-3$、$-1.5$、$0$、$2$、$4.5$。

*例 2.* 求 $-7$、$3/4$、$0$ 的相反数和绝对值。

*解：*
- $-7$ 的相反数是 $7$，绝对值是 $7$。
- $3/4$ 的相反数是 $-3/4$，绝对值是 $3/4$。
- $0$ 的相反数和绝对值都等于 $0$。

*例 3.* 绝对值总是非负的，因为距离不可能是负数。
]

#keytakeaway[
- 数轴上的点和有理数一一对应。
- 相反数只是符号相反。
- 绝对值表示距离，所以一定不小于 $0$。
]

#practice[
1. $-8$ 的相反数和绝对值分别是多少？
2. $|5|$ 和 $|-5|$ 是否相等？
3. 数轴上原点左边的数有什么共同特点？
4. 比较 $|2|$ 和 $|-2|$。
5. 判断对错：$|a|$ 可能是负数。
]

#answer[
*参考答案：*

1. $8$ 和 $8$。
2. 相等，都是 $5$。
3. 都是负数。
4. 相等，都等于 $2$。
5. 错，绝对值不可能是负数。
]
