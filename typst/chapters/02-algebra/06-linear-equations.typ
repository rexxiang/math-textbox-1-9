#import "../../lib/theme.typ": *

== §2.6 一元一次方程 <sec-2-6>

*前置知识：* #secref("2.3") 代数式与整式，#secref("2.1") 有理数

*适用年级：* 7 年级

=== 方程的概念与方程的解

#explore[
如果把一个未知数隐藏起来，等号两边还能保持相等吗？这就是方程研究的问题。
]

#understand[
含有未知数的等式叫做 *方程*。使方程左右两边相等的未知数的值，叫做方程的 *解*。

例如，$x+5=8$ 的解是 $x=3$，因为代入后左右两边都等于 $8$。
]

#workedexamples[
*例 1.* 判断 $x=2$ 是否是 $x+1=3$ 的解。代入后成立，所以是解。

*例 2.* $2x=10$ 的解是 $x=5$。
]

#keytakeaway[
- 方程是“含未知数的等式”。
- 解方程，就是找出使等式成立的值。
]

#practice[
1. 判断 $x=4$ 是否是 $x+2=7$ 的解。
2. 求 $x-3=5$ 的解。
]

=== 等式的基本性质

#explore[
等式两边同时做相同的事，相等关系通常不会改变。这就是解方程的核心依据。
]

#understand[
等式的基本性质：

- 两边同时加上或减去同一个数，等式仍成立。
- 两边同时乘以或除以同一个不为 $0$ 的数，等式仍成立。

它们让我们可以把方程一步步变简单。
]

#workedexamples[
*例 1.* 若 $x+3=7$，两边同时减 $3$，得 $x=4$。

*例 2.* 若 $3x=12$，两边同时除以 $3$，得 $x=4$。
]

#keytakeaway[
- 解方程的每一步都必须保持“左右同时做相同操作”。
- 除以 $0$ 没有意义。
]

#practice[
1. 若 $x-4=9$，怎样把 $x$ 单独留下？
2. 若 $5x=35$，下一步怎么做？
]

=== 解一元一次方程

#explore[
把方程变成最简单的 $x = "某个数"$，就是解一元一次方程的目标。
]

#understand[
一元一次方程的一般方法是：去括号 → 移项 → 合并同类项 → 化成 $a x = b$ → 求出 $x$。
]

#workedexamples[
*例 1.* 解 $2x+3=9$。

*解：* $2x=6$，所以 $x=3$。

*例 2.* 解 $3(x-1)=2x+4$。

*解：* $3x-3=2x+4$，$x=7$。

*例 3.* 解 $4x-2=2(2x-1)$。

*解：* 化简后两边相等，方程有无穷多解。
]

#keytakeaway[
- 先化简，再求解。
- 要特别注意移项时符号改变。
- 检查答案是好习惯。
]

#practice[
1. 解 $x+7=12$。
2. 解 $5x-4=16$。
3. 解 $2(x+3)=x+8$。
]

=== 一元一次方程的应用

#explore[
现实问题里，很多“总量 = 部分 + 部分”的关系都能变成方程。
]

#understand[
把文字问题翻译成方程，通常要抓住：

- 已知量与未知量。
- 等量关系。
- 单位是否一致。
]

#workedexamples[
*例 1.* 某数加上 $6$ 等于 $15$，设这个数为 $x$，可列方程 $x+6=15$，得 $x=9$。

*例 2.* 一件商品原价 $x$ 元，打 $8$ 折后是 $120$ 元，则 $0.8x=120$，解得 $x=150$。

*例 3.* 一个数的 $3$ 倍比它多 $14$，设这个数为 $x$，得 $3x=x+14$，所以 $x=7$。
]

#keytakeaway[
- 列方程的关键是找等量关系。
- 文字题先设未知数，再列方程，再求解。
]

#practice[
1. 某数的 $2$ 倍加 $5$ 等于 $19$，求这个数。
2. 书包原价 $x$ 元，降价 $30$ 元后卖 $120$ 元，求原价。
3. 一个数比它的 $4$ 倍少 $9$，求这个数。
]

#answer[
*参考答案：*

1. $7$。
2. $150$ 元。
3. $3$。
]
