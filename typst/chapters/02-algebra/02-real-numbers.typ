#import "../../lib/theme.typ": *

== §2.2 实数 <sec-2-2>

*前置知识：* #secref("2.1") 有理数

*适用年级：* 8 年级

=== 平方根与算术平方根

#explore[
正方形面积为 $25$ 平方米，边长是 $5$ 米；面积为 $9$ 平方米，边长是 $3$ 米。那面积为 $2$ 平方米时，边长是多少？这引出了平方根。
]

#understand[
若 $x^2 = a$（$a ≥ 0$），那么 $x$ 叫做 $a$ 的平方根。

- $a > 0$ 时，有两个平方根：$±√a$。
- $a = 0$ 时，平方根是 $0$。
- $a < 0$ 时，在实数范围内没有平方根。

非负数 $a$ 的非负平方根叫做 *算术平方根*，记作 $√a$，并且 $√a ≥ 0$。

因此，$√9 = 3$，而 $9$ 的平方根是 $±3$。
]

#workedexamples[
*例 1.* 求 $16$、$9/4$、$0$ 的平方根。

*解：* 分别是 $±4$、$±3/2$ 和 $0$。

*例 2.* 求 $√49$、$-√0.81$、$√((-3)^2)$。

*解：* 分别是 $7$、$-0.9$ 和 $3$。

*例 3.* 若正方形面积为 $50$ 平方厘米，边长是 $√50 = 5√2 ≈ 7.07$ 厘米。
]

#keytakeaway[
- 平方根有时有两个，有时只有一个。
- $√a$ 表示算术平方根，只取非负值。
- $√(a^2) = |a|$，不是 $a$。
]

#practice[
1. 求 $64$ 的平方根。
2. 求 $√121$。
3. $-√36$ 等于多少？
4. $√((-5)^2)$ 等于多少？
5. 判断对错：$√16 = ±4$。
6. 面积为 $200$ 平方米的正方形边长是多少（用根号表示）？
]

=== 立方根

#explore[
一个正方体体积为 $8$ 立方厘米，棱长是 $2$ 厘米。体积为 $27$ 时是 $3$ 厘米。立方根用来描述这类“立方后得到原数”的关系。
]

#understand[
若 $x^3 = a$，则 $x$ 叫做 $a$ 的立方根，记作 $∛a$。

- 每个实数都有且只有一个立方根。
- 正数的立方根为正，负数的立方根为负，$0$ 的立方根是 $0$。

例如：$∛8 = 2$，$∛0 = 0$，$∛(-27) = -3$。
]

#workedexamples[
*例 1.* 求 $125$、$-64$、$1/8$ 的立方根。

*解：* 分别是 $5$、$-4$、$1/2$。

*例 2.* $∛(-0.001) = -0.1$。

*例 3.* 体积为 $216$ 立方厘米的正方体，棱长是 $∛216 = 6$ 厘米。
]

#keytakeaway[
- 每个实数都恰好有一个立方根。
- 立方根的符号与原数一致。
- $∛(a^3) = a$ 对所有实数 $a$ 都成立。
]

#practice[
1. 求 $∛343$。
2. 求 $∛(-1000)$。
3. 求 $∛(27/8)$。
4. 求 $∛(-0.125)$。
5. 判断对错：负数没有立方根。
6. 体积为 $1728$ 立方厘米的正方体边长是多少？
]

=== 无理数的发现

#explore[
毕达哥拉斯学派曾相信“万物皆数”，但边长为 $1$ 的正方形对角线长度 $√2$ 不能写成分数，这说明数的世界里还有有理数以外的数。
]

#understand[
*无理数* 是不能写成 $p/q$（$q ≠ 0$）形式的数。

常见无理数：$√2$、$√3$、$π$、$∛2$。

无理数的小数形式是无限不循环小数；有理数的小数形式是有限小数或无限循环小数。

例如，$√2 = 1.41421356…$。
]

#workedexamples[
*例 1.* 判断 $√9$、$√5$、$3.14$、$π$ 是有理数还是无理数。

*解：* $√9 = 3$ 是有理数；$√5$ 和 $π$ 是无理数；$3.14$ 是有理数。

*例 2.* 写一个大于 $3$ 小于 $4$ 的无理数，可以取 $√10$。

*例 3.* 因为 $2^2 < 7 < 3^2$，所以 $2 < √7 < 3$，整数部分是 $2$。
]

#keytakeaway[
- 无理数是无限不循环小数。
- 不是完全平方数的正整数开平方通常得到无理数。
- $π$ 是无理数，$3.14$ 只是近似值。
]

#practice[
1. 判断 $√36$ 是有理数还是无理数。
2. 判断 $√11$ 是有理数还是无理数。
3. 估计 $√50$ 的整数部分。
4. 下列数中哪些是无理数：$0.333…$、$√8$、$-22/7$、$π-3$？
5. 在 $√2$ 和 $√3$ 之间写出一个有理数和一个无理数。
]

=== 实数的分类与实数轴

#explore[
把有理数和无理数放在一起，就构成了实数。实数把“数轴上的点”补完整了。
]

#understand[
*实数* 包括有理数和无理数。

- 实数可按有理/无理分类。
- 也可按正负分类：正实数、$0$、负实数。
- 实数与数轴上的点一一对应，这条数轴叫做 *实数轴*。

有理数的运算律对实数仍然成立；相反数和绝对值的定义也可以推广到实数。
]

#workedexamples[
*例 1.* 把 $-3$、$√2$、$0$、$22/7$、$π$、$-√16$、$0.333…$ 分为有理数和无理数。

*解：* 有理数有 $-3$、$0$、$22/7$、$-√16=-4$、$0.333…=1/3$；无理数有 $√2$、$π$。

*例 2.* $√3$ 约在 $1$ 和 $2$ 之间，更精确地约为 $1.73$。

*例 3.* 比较 $√5$ 与 $2.3$：因为 $√5 ≈ 2.236$，所以 $√5 < 2.3$。

*例 4.* $|2-√5| = √5 - 2$。
]

#keytakeaway[
- 实数由有理数和无理数构成。
- 实数轴上的每一个点都对应一个实数。
- 比较实数大小时，可以先估算近似值。
]

#practice[
1. 将 $√25$、$√7$、$-π$、$1/3$、$0$ 分为有理数和无理数。
2. 比较 $√10$ 与 $3.2$。
3. 求 $|√3 - 2|$。
4. 估计 $√20$ 的值（精确到十分位）。
5. 在 $√2$ 和 $√3$ 之间写出一个有理数和一个无理数。
]

#answer[
*参考答案：*

1. 有理数：$√25=5$、$1/3$、$0$；无理数：$√7$、$-π$。
2. $√10 < 3.2$。
3. $|√3 - 2| = 2 - √3$。
4. $√20 ≈ 4.5$。
5. 例如 $1.5$ 和 $√2.5$。
]
