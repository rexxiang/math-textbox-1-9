#import "../../lib/theme.typ": *

== §2.8 一元二次方程 <sec-2-8>

*前置知识：* #secref("2.4") 因式分解，#secref("2.6") 一元一次方程

*适用年级：* 9 年级

=== 一元二次方程的一般形式

#explore[
当未知数最高次数变成 $2$ 时，新的方程类型就出现了。它比一元一次方程更复杂，但结构仍然清晰。
]

#understand[
*一元二次方程* 的一般形式是 $a x^2 + b x + c = 0$（$a ≠ 0$）。

这里 $a$ 不能等于 $0$，否则就退化成一元一次方程。
]

#workedexamples[
*例 1.* $x^2-5x+6=0$ 是一元二次方程。

*例 2.* $2x^2=8$ 也可化为 $2x^2-8=0$。
]

#keytakeaway[
- 最高次数必须是 $2$。
- 首项系数不能为 $0$。
]

#practice[
1. 判断 $3x^2+2x-1=0$ 是否为一元二次方程。
2. 判断 $x^2-4=0$ 是否为一元二次方程。
]

=== 解法：直接开平方法与配方法

#explore[
有些方程能直接开方；有些则需要把式子改造成“平方的形式”。
]

#understand[
常见思路：

- 直接开平方法：先把式子整理成 $x^2=a$。
- 配方法：把二次项和一次项凑成完全平方。
]

#workedexamples[
*例 1.* $x^2=9$，所以 $x=±3$。

*例 2.* $x^2-6x+5=0$ 可化为 $(x-3)^2=4$，所以 $x=1$ 或 $x=5$。
]

#keytakeaway[
- 开平方时要记得正负两个根。
- 配方的目标是把式子凑成平方。
]

#practice[
1. 解 $x^2=16$。
2. 解 $x^2-4x+3=0$。
]

=== 公式法

#explore[
对于一般式，最稳定的方法是直接套求根公式。
]

#understand[
如果 $a x^2 + b x + c = 0$（$a ≠ 0$），那么

$x = (-b ± √(b^2 - 4 a c)) / (2 a)$。

这里 $b^2 - 4 a c$ 叫做判别式的核心部分。
]

#workedexamples[
*例 1.* 对 $x^2 - 2 x - 3 = 0$，有 $x = (2 ± √16) / 2$，所以 $x = 3$ 或 $x = -1$。

*例 2.* 对 $2 x^2 + 4 x - 1 = 0$，可直接代入公式求解。
]

#keytakeaway[
- 公式法适用于所有一元二次方程。
- 先算判别式，再开方。
]

#practice[
1. 用公式法解 $x^2-5x+6=0$。
2. 用公式法解 $2x^2-x-1=0$。
]

=== 因式分解法

#explore[
如果方程左边能分解成两个因式的乘积，就可以把“乘积为 0”转化为“至少一个因式为 0”。
]

#understand[
若 $A B = 0$，则 $A = 0$ 或 $B = 0$。

所以能因式分解的一元二次方程，通常更容易求解。
]

#workedexamples[
*例 1.* $x^2-2x-3=0$ 可写成 $(x-3)(x+1)=0$，所以 $x=3$ 或 $x=-1$。

*例 2.* $x^2-4x+4=0$ 可写成 $(x-2)^2=0$，所以 $x=2$。
]

#keytakeaway[
- “乘积为 0” 是因式分解法的核心。
- 分解后记得逐个因式求零。
]

#practice[
1. 用因式分解法解 $x^2+7x+10=0$。
2. 用因式分解法解 $2x^2-8x=0$。
]

=== 判别式与根的情况

#explore[
有时我们不必真的把方程解出来，只要看判别式就能知道根的情况。
]

#understand[
判别式：$Δ = b^2 - 4 a c$。

- $Δ > 0$：有两个不相等的实数根。
- $Δ = 0$：有两个相等的实数根。
- $Δ < 0$：没有实数根。
]

#workedexamples[
*例 1.* $x^2 - 2 x - 3 = 0$，$Δ = 16 > 0$，所以有两个不同实根。

*例 2.* $x^2 + 2 x + 5 = 0$，$Δ = -16 < 0$，所以没有实根。
]

#keytakeaway[
- 判别式直接告诉我们根的个数与性质。
- 这是研究二次方程的重要工具。
]

#practice[
1. 判断 $x^2+4x+4=0$ 的根的情况。
2. 判断 $2x^2+3x+5=0$ 的根的情况。
]

=== 韦达定理

#explore[
如果方程有两个根，根与系数之间有没有简单关系？答案是有的。
]

#understand[
对于 $a x^2 + b x + c = 0$ 的两个根 $x_1, x_2$，有：

- $x_1 + x_2 = -b / a$
- $x_1 x_2 = c / a$
]

#workedexamples[
*例 1.* 对 $x^2 - 5 x + 6 = 0$，两根和为 $5$，两根积为 $6$。

*例 2.* 若两根为 $2$ 和 $3$，则方程可以写成 $x^2 - 5 x + 6 = 0$。
]

#keytakeaway[
- 韦达定理把“根”与“系数”联系起来。
- 已知两根，可反推出方程。
]

#practice[
1. 若两根和为 $4$、积为 $3$，写出方程。
2. 若方程 $x^2 + b x + 8 = 0$ 的两根为 $2$ 和 $4$，求 $b$。
]

=== 一元二次方程的应用

#explore[
很多最值、面积、运动问题都能最后归结为一元二次方程。
]

#understand[
建模时，常常先设未知数，再把条件写成二次方程，最后检验答案是否符合实际。
]

#workedexamples[
*例 1.* 一块长方形花圃在给定周长下求最大面积，常可转化为二次方程或二次函数问题。

*例 2.* 若某物体运动高度满足二次关系，落地时就对应一个二次方程。
]

#keytakeaway[
- 二次方程常出现在“面积、运动、最大最小值”问题中。
- 先建模，再求解，再检验。
]

#practice[
1. 解 $x^2-1=0$，并解释两个根的实际意义。
2. 一个正方形面积为 $49$，求边长并写出对应方程。
]

#answer[
*参考答案：*

1. $x=±1$。
2. 边长为 $7$，方程是 $x^2=49$。
]
