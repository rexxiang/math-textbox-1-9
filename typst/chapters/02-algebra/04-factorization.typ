#import "../../lib/theme.typ": *

== §2.4 因式分解 <sec-2-4>

*前置知识：* #secref("2.3") 代数式与整式

*适用年级：* 8 年级

=== 因式分解的概念

#explore[
把一个多项式“拆成乘积”，就是因式分解。它和展开乘法正好互逆。
]

#understand[
*因式分解*：把一个多项式化成几个整式乘积的形式。

判断是否完成因式分解，要看结果是不是“乘积形式”，而不是简单地把式子写得更长。
]

#workedexamples[
*例 1.* $x^2-4 = (x+2)(x-2)$。

*例 2.* $a^2 + 2 a b + b^2 = (a + b)^2$。
]

#keytakeaway[
- 因式分解是展开的逆运算。
- 最终结果必须是乘积形式。
]

#practice[
1. 判断 $a^2+a$ 是否已经因式分解。
2. 把 $x^2-9$ 写成乘积。
]

=== 提公因式法

#explore[
很多式子都有共同的“公因式”，先把它提出来，后面的式子会简单很多。
]

#understand[
提公因式的步骤：

- 找系数的最大公因数。
- 找公共字母，取指数最小的。
- 把公因式提到括号外面。
]

#workedexamples[
*例 1.* $6 a^2 b + 9 a b^2 = 3 a b (2 a + 3 b)$。

*例 2.* $-4x^3+8x^2-12x = -4x(x^2-2x+3)$。

*例 3.* $2a(x-y)+3b(x-y) = (x-y)(2a+3b)$。
]

#keytakeaway[
- 公因式 = 系数最大公因数 × 公共字母最低次幂。
- 先提公因式，再考虑其他方法。
]

#practice[
1. 因式分解 $8x+12$。
2. 因式分解 $5 a^2 b - 10 a b^2$。
3. 因式分解 $x^2 (a - b) - y^2 (a - b)$。
]

=== 公式法

#explore[
有些二次三项式可以直接套用乘法公式的逆过程。
]

#understand[
两个最重要的逆用公式：

- $a^2 - b^2 = (a + b)(a - b)$
- $a^2 ± 2 a b + b^2 = (a ± b)^2$
]

#workedexamples[
*例 1.* $25x^2-16=(5x+4)(5x-4)$。

*例 2.* $x^2+6x+9=(x+3)^2$。

*例 3.* $4 a^2 - 12 a b + 9 b^2 = (2 a - 3 b)^2$。

*例 4.* $3x^2-12 = 3(x^2-4)=3(x+2)(x-2)$。
]

#keytakeaway[
- 看到平方差，先想 $a^2-b^2$。
- 看到首尾都是平方、中间项正好是 $2 a b$，就想完全平方。
]

#practice[
1. 因式分解 $x^2+8x+16$。
2. 因式分解 $9a^2-25b^2$。
3. 因式分解 $2x^2-18$。
]

=== 十字相乘法

#explore[
并非所有二次三项式都能直接看出公式。找两个数，满足“和为中项系数、积为常数项”，常常就能拆开。
]

#understand[
对 $x^2 + b x + c$，寻找两个数 $p,q$，使得 $p + q = b$、$p q = c$，那么

$x^2 + b x + c = (x + p)(x + q)$。

更一般的 $a x^2 + b x + c$ 也可以用类似的“交叉”关系处理。
]

#workedexamples[
*例 1.* $x^2+5x+6=(x+2)(x+3)$。

*例 2.* $x^2-7x+12=(x-3)(x-4)$。

*例 3.* $2x^2+7x+3=(2x+1)(x+3)$。
]

#keytakeaway[
- 先找“和”和“积”。
- 对首项系数不是 $1$ 的式子，要更注意交叉关系。
]

#practice[
1. 因式分解 $x^2+7x+10$。
2. 因式分解 $x^2-3x-18$。
3. 因式分解 $3x^2+10x+3$。
]

=== 综合运用

#explore[
复杂题目通常要多种方法配合：先提公因式，再用公式法，最后再看能不能十字相乘。
]

#understand[
因式分解的顺序通常是：

1. 先看有没有公因式。
2. 再看能否用公式法。
3. 最后考虑十字相乘法和整体代换。
]

#workedexamples[
*例 1.* $3 a^2 - 12 b^2 = 3 (a^2 - 4 b^2) = 3 (a + 2 b)(a - 2 b)$。

*例 2.* $x^3 - 2 x^2 - 3 x = x (x^2 - 2 x - 3) = x (x - 3)(x + 1)$。

*例 3.* $2 x^2 y - 12 x y + 18 y = 2 y (x^2 - 6 x + 9) = 2 y (x - 3)^2$。

*例 4.* $(a+b)^2-4(a+b)+4$ 可把 $a+b$ 看成整体，设 $t=a+b$，则原式 $=t^2-4t+4=(t-2)^2$。
]

#keytakeaway[
- 因式分解要“看结构、选方法、验结果”。
- 复杂题目常常是几种方法串联使用。
]

#practice[
1. 因式分解 $6 x^2 - 24$。
2. 因式分解 $x^3 + 2 x^2 - x - 2$。
3. 因式分解 $(m + n)^2 - 6 (m + n) + 9$。
]

#answer[
*参考答案：*

1. $6 x^2 - 24 = 6 (x^2 - 4) = 6 (x + 2)(x - 2)$。
2. $x^3 + 2 x^2 - x - 2 = (x^2 - 1)(x + 2) = (x - 1)(x + 1)(x + 2)$。
3. $(m + n)^2 - 6 (m + n) + 9 = (m + n - 3)^2$。
]
