#import "../../lib/theme.typ": *

== §2.11 二次函数 <sec-2-11>

*前置知识：* #secref("2.8") 一元二次方程，#secref("2.10") 一次函数

*适用年级：* 9 年级

=== 二次函数的概念

#explore[
抛物线、投篮轨迹、桥拱形状，很多变化关系都可以写成二次函数。
]

#understand[
*二次函数* 的一般形式是 $y = a x^2 + b x + c$（$a ≠ 0$）。

- $a$ 是二次项系数。
- $b$ 是一次项系数。
- $c$ 是常数项。
]

#workedexamples[
*例 1.* $y = x^2$、$y = 2 x^2 - 1$、$y = -x^2 + 3 x$ 都是二次函数。

*例 2.* 若 $a=0$，就退化成一次函数或常数函数，不再是二次函数。
]

#keytakeaway[
- 最高次数必须是 $2$。
- 二次项系数不能为 $0$。
]

#practice[
1. 判断 $y = 3 x^2 + 1$ 是否为二次函数。
2. 判断 $y = π x^2$ 是否为二次函数。
]

=== 二次函数的图象

#explore[
最简单的二次函数 $y=x^2$ 的图象是一条开口向上的 U 形曲线。
]

#understand[
二次函数的图象叫做 *抛物线*。

- $a>0$：开口向上。
- $a<0$：开口向下。
- 图象关于对称轴对称。
]

#workedexamples[
*例 1.* $y = x^2$ 的顶点是 $(0,0)$，对称轴是 $y$ 轴。

*例 2.* 比较 $y = 2 x^2$、$y = x^2$、$y = (1/2) x^2$，$|a|$ 越大，抛物线越“窄”。
]

#keytakeaway[
- 开口方向由 $a$ 决定。
- 绝对值越大，图象越窄；绝对值越小，图象越宽。
]

#practice[
1. 说出 $y = -x^2$ 的开口方向。
2. 比较 $y = 4 x^2$ 和 $y = (1/4) x^2$ 哪个更宽。
]

=== 顶点式

#explore[
把二次函数改写成顶点式后，顶点和对称轴就一眼看出来了。
]

#understand[
二次函数的顶点式是 $y = a (x - h)^2 + k$。

其中顶点是 $(h,k)$，对称轴是 $x=h$。
]

#workedexamples[
*例 1.* $y = x^2 - 6 x + 11 = (x - 3)^2 + 2$，顶点是 $(3,2)$。

*例 2.* $y = 2 x^2 + 4 x - 1 = 2 (x + 1)^2 - 3$，顶点是 $(-1,-3)$。
]

#keytakeaway[
- 顶点式最适合看顶点和最值。
- 配方法是一般式转顶点式的主要工具。
]

#practice[
1. 把 $y = x^2 - 4 x + 3$ 化成顶点式。
2. 说出 $y = -1/2 (x + 3)^2 + 4$ 的顶点和开口方向。
]

=== 二次函数与一元二次方程的关系

#explore[
二次函数图象与 $x$ 轴的交点，就是对应一元二次方程的根。
]

#understand[
对于 $y = a x^2 + b x + c$：

- 令 $y = 0$，就得到一元二次方程 $a x^2 + b x + c = 0$。
- 图象与 $x$ 轴交点的横坐标，就是方程的根。
]

#workedexamples[
*例 1.* $y = x^2 - 2 x - 3$ 与 $x$ 轴交于 $x = -1$ 和 $x = 3$。

*例 2.* 若 $Δ < 0$，抛物线与 $x$ 轴没有交点。
]

#keytakeaway[
- 方程的根和图象的交点是一一对应的。
- 这是“数”和“形”之间的重要联系。
]

#practice[
1. 求 $y=x^2-4x+3$ 与 $x$ 轴的交点。
2. 判断 $y = x^2 + 2 x + 5$ 是否与 $x$ 轴相交。
]

=== 二次函数的最值

#explore[
抛物线的顶点往往就是最大值或最小值出现的位置。
]

#understand[
- 若 $a>0$，顶点是最低点，函数有最小值。
- 若 $a<0$，顶点是最高点，函数有最大值。
]

#workedexamples[
*例 1.* $y = 2 (x - 3)^2 + 1$ 的最小值是 $1$，在 $x = 3$ 处取得。

*例 2.* $y = -x^2 + 4 x - 1 = -(x - 2)^2 + 3$，最大值是 $3$。
]

#keytakeaway[
- 顶点就是最值点。
- 顶点式最适合求最值。
]

#practice[
1. 求 $y = x^2 - 6 x + 5$ 的最小值。
2. 求 $y = -2 x^2 + 8 x - 5$ 的最大值。
]

=== 二次函数的应用

#explore[
桥拱、利润、飞行高度、面积优化，很多实际问题都能转化成二次函数。
]

#understand[
建模时通常先设未知数，再列出二次函数表达式，最后利用顶点或图象求最值。
]

#workedexamples[
*例 1.* 拱桥高度可写成二次函数，桥面两端和拱顶位置都能用顶点式描述。

*例 2.* 若利润函数是 $P = -10 x^2 + 900 x - 18000$，配方后可求最大利润。
]

#keytakeaway[
- 二次函数常用于最值问题。
- 先建模，再利用顶点和对称轴求解。
]

#practice[
1. 若飞行高度为 $h = -(1/20) x^2 + 2 x$，求最大高度出现在哪个 $x$。
2. 判断长方形周长固定时，面积最大往往在什么形状时取得。
]

#answer[
*参考答案：*

1. 在 $x=20$ 处。
2. 当长和宽相等时，面积通常最大。
]
