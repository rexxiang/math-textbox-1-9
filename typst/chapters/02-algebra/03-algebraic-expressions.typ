#import "../../lib/theme.typ": *

== §2.3 代数式与整式 <sec-2-3>

*前置知识：* #secref("1.3") 运算律，#secref("2.1") 有理数

*适用年级：* 7 年级

=== 用字母表示数

#explore[
同一个规律在不同情境里往往都成立。比如“长方形面积 = 长 × 宽”，如果用字母表示，就能写成通用的式子。
]

#understand[
*代数式* 是由数、字母和运算符号组成的式子。

字母可以表示任意数，也可以表示变化中的量。比如 $s = v t$、$C = 2 π r$ 都是把规律写成代数式。

代数式的值会随着字母取值而变化。
]

#workedexamples[
*例 1.* 用代数式表示：某数的 $3$ 倍加 $5$，可写成 $3x+5$。

*例 2.* 设长方形长为 $a$，宽为 $b$，周长是 $2(a+b)$。

*例 3.* 若苹果每千克 $x$ 元，买 $5$ 千克付 $5x$ 元。
]

#keytakeaway[
- 字母能表示数、关系和规律。
- 代数式比“逐个列举”更简洁、更通用。
]

#practice[
1. 写出“比 $x$ 多 $8$”的代数式。
2. 写出“$a$ 的平方减去 $2$”。
3. 设速度为 $v$、时间为 $t$，路程如何表示？
]

=== 单项式与多项式

#explore[
把 $3x$、$-2 a b$、$5$ 这样的式子放在一起看，就能发现：有的式子只有一项，有的式子由几项组成。
]

#understand[
*单项式*：由数与字母的乘积构成，或单独的一个数。

*多项式*：由几个单项式相加减组成。

一个多项式里，每个单项式叫做它的“项”；最高次数叫做它的“次数”。
]

#workedexamples[
*例 1.* 在 $3 x^2 - 2 x + 7$ 中，项是 $3 x^2$、$-2 x$、$7$，次数是 $2$。

*例 2.* $-4 a^2 b$ 是单项式，系数是 $-4$，字母部分是 $a^2 b$。
]

#keytakeaway[
- 单项式只有一项，多项式有两项或以上。
- 分析多项式时先看项数，再看每项的次数。
]

#practice[
1. 判断 $2x$、$x+y$、$-5$ 是否是单项式。
2. 写出 $4a^3 - a + 1$ 的项数和次数。
3. $7 x y^2$ 的系数和字母部分分别是什么？
]

=== 整式的加减运算

#explore[
合并同类项就像“把同类物品装在一起”。只有字母和指数都相同的项，才能相加减。
]

#understand[
*同类项*：字母相同且对应字母的指数也相同的项。

整式加减的关键是：先去括号，再合并同类项。

分配律让我们能把括号展开，再整理。
]

#workedexamples[
*例 1.* 化简 $3 x + 5 x - 2 x = 6 x$。

*例 2.* 化简 $(2 a - 3 b) + (5 a + b) = 7 a - 2 b$。

*例 3.* 化简 $4 (x - 2) - 3 (2 x + 1) = 4 x - 8 - 6 x - 3 = -2 x - 11$。
]

#keytakeaway[
- 合并同类项只合并系数。
- 去括号时注意符号。
- 整式的加减，本质上是“展开 + 合并”。
]

#practice[
1. 化简 $2x+3x-x$。
2. 化简 $(a+b)+(2a-3b)$。
3. 化简 $5(2x-1)-2(x+4)$。
]

=== 整式的乘法

#explore[
“面积模型”很适合理解乘法：一个长方形的长和宽都可以是代数式，面积就对应代数式的乘积。
]

#understand[
整式乘法遵循分配律：$a (b + c) = a b + a c$。

单项式乘单项式时，系数相乘，字母分别相乘；单项式乘多项式时，要“一个不漏”地乘进去。
]

#workedexamples[
*例 1.* $3 x × 2 y = 6 x y$。

*例 2.* $2 a (3 a - 4 b) = 6 a^2 - 8 a b$。

*例 3.* $(x+3)(x+2)=x^2+5x+6$。
]

#keytakeaway[
- 乘法要逐项展开。
- 字母相乘时指数相加。
- 乘法运算常和后面的因式分解互为逆过程。
]

#practice[
1. 化简 $4 x × 3 x$。
2. 化简 $-2a(3a+b)$。
3. 展开 $(x-4)(x+1)$。
]

=== 乘法公式

#explore[
有些乘法反复出现，记住公式可以大大提高计算速度。
]

#understand[
三个最常用的乘法公式：

- $(a + b)^2 = a^2 + 2 a b + b^2$
- $(a - b)^2 = a^2 - 2 a b + b^2$
- $(a + b)(a - b) = a^2 - b^2$

它们可以直接套用，也可以看作展开后合并同类项的结果。
]

#workedexamples[
*例 1.* $(x+5)^2 = x^2+10x+25$。

*例 2.* $(2 a - 3 b)^2 = 4 a^2 - 12 a b + 9 b^2$。

*例 3.* $(x+4)(x-4)=x^2-16$。
]

#keytakeaway[
- 完全平方公式和平方差公式要熟练掌握。
- 看到“平方”“相差一项”时，先想公式。
]

#practice[
1. 展开 $(x+2)^2$。
2. 展开 $(3a-b)^2$。
3. 因式分解 $x^2-49$。
]

=== 整式的除法

#explore[
除法是乘法的逆运算。整式除法中，常见的是单项式除以单项式，或多项式中能直接约去的部分。
]

#understand[
同底数幂相除时，指数相减：$a^m/a^n = a^(m-n)$（$a ≠ 0$）。

单项式除以单项式：系数相除、同字母指数相减。

多项式除以单项式时，可把每一项分别除以这个单项式。
]

#workedexamples[
*例 1.* $6 x^3 y / (2 x y) = 3 x^2$。

*例 2.* $(4x^2-8x) / 4x = x-2$。

*例 3.* $a^5/a^2 = a^3$。
]

#keytakeaway[
- 除法常配合“约去”来化简。
- 先看能否约分，再做运算。
]

#practice[
1. 化简 $8 x^2 y / (2 x y)$。
2. 化简 $(6 a^2 - 3 a) / (3 a)$。
3. 计算 $b^7 / b^3$。
]

#answer[
*参考答案：*

1. $8 x^2 y / (2 x y) = 4 x$。
2. $(6 a^2 - 3 a) / (3 a) = 2 a - 1$।
3. $b^7 / b^3 = b^4$。
]
