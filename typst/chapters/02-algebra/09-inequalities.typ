#import "../../lib/theme.typ": *

== §2.9 不等式 <sec-2-9>

*前置知识：* #secref("2.6") 一元一次方程，#secref("2.1") 有理数

*适用年级：* 7-8 年级

=== 不等式的概念与基本性质

#explore[
有些问题不是问“是否相等”，而是问“是否大于”或“是否小于”。
]

#understand[
像 $2x+1 > 5$、$x-3 ≤ 7$ 这样的式子叫做 *不等式*。

不等式的基本性质与等式类似，但有一个重要区别：乘或除以负数时，不等号方向要改变。
]

#workedexamples[
*例 1.* $3>2$，$5<8$，$x+1 ≥ 4$ 都是不等式。

*例 2.* 若 $-2x > 6$，除以 $-2$ 后应得到 $x < -3$。
]

#keytakeaway[
- 不等式描述的是“范围”。
- 乘除负数时要反向。
]

#practice[
1. 写出一个含有 $>$ 的不等式。
2. $-3x ≤ 9$ 中，若化为 $x$ 的形式，不等号会怎样变化？
]

=== 一元一次不等式的解法

#explore[
解不等式和解方程很像，只是最后得到的不再是一个值，而是一个范围。
]

#understand[
一元一次不等式的解法通常是：去括号 → 移项 → 合并同类项 → 化成 $a x > b$、$a x < b$、$a x ≥ b$ 或 $a x ≤ b$ 的形式。
]

#workedexamples[
*例 1.* 解 $2x+3>9$，得 $x>3$。

*例 2.* 解 $3(x-2) ≤ 2x+1$，得 $x ≤ 7$。
]

#keytakeaway[
- 解不等式时步骤与解方程相似。
- 除以负数一定要变号。
]

#practice[
1. 解 $x+5>12$。
2. 解 $4x-1 ≤ 11$。
3. 解 $2(x+3) > x+8$。
]

=== 不等式的解集与数轴表示

#explore[
不等式的解通常不是一个数，而是一整段范围。数轴最适合表示这种结果。
]

#understand[
*解集* 是使不等式成立的所有解组成的集合。

在数轴上，通常用实心点或空心点配合箭头表示解集。
]

#workedexamples[
*例 1.* $x > 2$ 表示数轴上 $2$ 右边的所有点。

*例 2.* $-1 ≤ x < 3$ 表示从 $-1$ 到 $3$ 的区间，左端点实心、右端点空心。
]

#keytakeaway[
- 解集强调“所有满足条件的数”。
- 数轴能直观表示范围。
]

#practice[
1. 说出 $x ≤ 4$ 的解集含义。
2. 画出 $-2 < x ≤ 1$ 的数轴表示。
]

=== 一元一次不等式组

#explore[
两个不等式一起出现时，要找它们的共同解。
]

#understand[
不等式组的解集，是每个不等式解集的“交集”。
]

#workedexamples[
*例 1.* $x+3>0, 2x-4<0$ 的解集是 $-3 < x < 2$。

*例 2.* $3x-6 ≥ 0, 5x-10 ≤ 0$ 的解集是 $x=2$。
]

#keytakeaway[
- 不等式组求的是“共同满足”。
- 交集可能是一段区间，也可能只是一个点。
]

#practice[
1. 解 $x-2>0, x+1<6$。
2. 解 $3x ≥ 6, 2x ≤ 10$。
]

=== 不等式的应用

#explore[
当问题有“至少”“至多”“不超过”“不少于”这些关键词时，往往要用不等式。
]

#understand[
关键词与不等号常常对应如下：

- 至少、不少于：$≥$
- 至多、不超过：$≤$
- 大于：$>$
- 小于：$<$
]

#workedexamples[
*例 1.* 若购物预算不超过 $100$ 元，可写成 $x ≤ 100$。

*例 2.* 若一个班人数至少 $35$ 人，可写成 $x ≥ 35$。
]

#keytakeaway[
- 看到“范围词”就想到不等式。
- 建模后再解不等式，答案要符合实际。
]

#practice[
1. 某产品单价不少于 $20$ 元，写出不等式。
2. 某活动最多允许 $50$ 人参加，写出不等式。
]

#answer[
*参考答案：*

1. $x ≥ 20$。
2. $x ≤ 50$。
]
