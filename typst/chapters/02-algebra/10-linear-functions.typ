#import "../../lib/theme.typ": *

== §2.10 一次函数 <sec-2-10>

*前置知识：* #secref("2.6") 一元一次方程，#secref("2.7") 二元一次方程组

*适用年级：* 8 年级

=== 函数的概念

#explore[
弹簧挂不同重物时，长度会变化；但对于每一个质量，都只有一个对应长度。这种“一一对应”的关系就是函数。
]

#understand[
*变量* 是可以取不同值的量，*常量* 是保持不变的量。

如果对于每一个 $x$，$y$ 都有唯一确定的值与之对应，那么 $y$ 是 $x$ 的函数。
]

#workedexamples[
*例 1.* $y=2x+1$ 中，$x$ 是自变量，$y$ 是因变量。

*例 2.* 表达“路程 = 速度 × 时间”也是函数关系。
]

#keytakeaway[
- 函数强调“唯一对应”。
- 自变量变化，因变量随之变化。
]

#practice[
1. 判断“每个学生对应一个学号”是不是函数关系。
2. 判断“一个名字对应多个电话”是不是函数关系。
]

=== 函数的表示方法

#explore[
同一个函数可以用文字、式子、表格和图象来描述。
]

#understand[
函数常见的表示方法有三种：

- 解析式：用公式写出关系。
- 表格：列出一组对应值。
- 图象：用坐标平面上的点和曲线表示。
]

#workedexamples[
*例 1.* 若 $y=2x+1$，代入若干 $x$ 值可做成表格，再据此描点画图。

*例 2.* 函数图象上的每个点都满足对应关系式。
]

#keytakeaway[
- 公式、表格、图象互相对应。
- 读图时要注意坐标轴的意义。
]

#practice[
1. 把 $y=x+2$ 的几个对应值列成表格。
2. 说明图象上的点 $(3,7)$ 是否可能属于 $y=2x+1$。
]

=== 正比例函数

#explore[
如果图象经过原点，且变量之间的变化“成比例”，我们就得到正比例函数。
]

#understand[
*正比例函数* 的形式是 $y = k x$（$k ≠ 0$）。

它的图象是一条经过原点的直线。
]

#workedexamples[
*例 1.* $y=3x$ 是正比例函数。

*例 2.* $y=-2x$ 也是正比例函数，图象过原点且斜向下。
]

#keytakeaway[
- 正比例函数一定过原点。
- 系数 $k$ 决定直线方向。
]

#practice[
1. 判断 $y=5x$ 是否为正比例函数。
2. 说出 $y=-x$ 的图象特点。
]

=== 一次函数

#explore[
当图象不经过原点，但仍然是直线时，它往往可以写成 $y = k x + b$。
]

#understand[
*一次函数* 的形式是 $y = k x + b$（$k ≠ 0$）。

- $k$ 决定直线的“倾斜程度”。
- $b$ 是 $y$ 轴截距。
]

#workedexamples[
*例 1.* $y=2x+3$ 是一次函数，$k=2$，$b=3$。

*例 2.* $y=-x+5$ 也是一次函数。
]

#keytakeaway[
- 一次函数的图象是一条直线。
- 正比例函数是一次函数的特殊情况。
]

#practice[
1. 判断 $y=4x-1$ 是否为一次函数。
2. 写出 $y=-3x+2$ 的 $k$ 和 $b$。
]

=== 一次函数的图象与性质

#explore[
两点可以确定一条直线，所以画一次函数图象只要找出两个点即可。
]

#understand[
一次函数 $y = k x + b$ 的图象是一条直线。

- $k>0$：直线从左下向右上。
- $k<0$：直线从左上向右下。
- $b$ 决定与 $y$ 轴的交点。
]

#workedexamples[
*例 1.* $y=x+1$ 经过 $(0,1)$ 和 $(1,2)$。

*例 2.* $y=-2x+7$ 的 $y$ 轴截距是 $7$。
]

#keytakeaway[
- 斜率和截距决定直线位置。
- 用两个点画一次函数图象最方便。
]

#practice[
1. 说出 $y=3x-2$ 的图象大致走向。
2. $y=-x+4$ 与 $y$ 轴的交点是什么？
]

=== 用函数观点看方程（组）和不等式

#explore[
把方程或不等式看成图象问题，很多结论会变得非常直观。
]

#understand[
- 方程的解，可以理解为两个图象的交点横坐标。
- 方程组的解，可以理解为两条直线的交点。
- 不等式的解集，可以理解为图象满足“大于”或“小于”的区域。
]

#workedexamples[
*例 1.* 利用函数图象解方程组 $y=x+1, y=-2x+7$，交点对应的解是 $x=2$、$y=3$。

*例 2.* 解 $2x-1>3$，即 $x>2$，也可以看成直线与水平线的位置关系。
]

#keytakeaway[
- 函数图象是研究方程和不等式的另一种语言。
- “交点”与“解”常常是一回事。
]

#practice[
1. 利用图象解方程组 $y=2x-1, y=-x+5$。
2. 说出不等式 $x+1<4$ 的解集。
]

#answer[
*参考答案：*

1. 交点对应的解为 $x=2$、$y=3$。
2. $x<3$。
]
