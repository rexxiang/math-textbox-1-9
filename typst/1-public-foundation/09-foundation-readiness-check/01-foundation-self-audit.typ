#import "../../lib/theme-v2.typ": blueprint, side-hack, tryit, secref

== 自测诊断：四条主干有没有连起来 <tool:pf09-self-audit>

#blueprint[
  *公共基础的四条主干*

  #table(
    columns: (1fr, 1fr),
    inset: 6pt,
    stroke: 0.4pt,
    [*主干*], [*对应章节*],
    [算术与结构：位值、四则、顺序、运算律], [#secref("pf01-decimal-system")、#secref("pf02-order-laws")],
    [部分与整体：因数倍数、分数、小数、单位], [#secref("pf03-gcd-lcm")、#secref("pf04-fraction-equivalence")、#secref("pf05-decimals-conversion")、#secref("pf05-units-measurement")],
    [形状与命名：点线面、基本图形、图形关系], [#secref("pf06-points-and-planes")、#secref("pf06-lines-rays-segments")、#secref("pf06-polygons-and-circle")、#secref("pf06-simple-solids")、#secref("pf06-shape-relations")],
    [度量与位置：周长面积体积、角、坐标], [#secref("pf07-perimeter-area")、#secref("pf07-volume")、#secref("pf07-angles")、#secref("pf08-coordinate-intuition")],
  )

  这四条主干任何一条没站稳，进入共通桥梁层（比、数轴、坐标、字母、方程、平方根）都会处处卡顿。
]

#tryit[
  *自测任务：先独立做完，再看后面的“可以继续前进的信号”*

  === 诊断 1：数与运算是否一起用顺

  + 把 $0.875$ 写成分数（最简形式）。
  + 计算：$12 + 48 div 6 times 2 - 5$。
  + 一袋米原有 $4.5$ 千克，用去 $30%$。还剩多少千克？

  === 诊断 2：分数、比、百分数是否能表示同一个量

  + 比较大小：$0.7$、$3/4$、$72%$。
  + 一瓶饮料配方是原液 $:$ 水 $= 3 : 7$。共 $500$ 毫升中，原液是多少毫升？
  + $5/8$ 化成小数和百分数。

  === 诊断 3：图形、度量、位置是否能一起读图

  + 一个长方形长 $12$ 厘米、宽 $7$ 厘米。周长和面积分别是多少？
  + $l_1 parallel l_2$，横截线 $t$ 交出一对内错角，其中一个是 $48°$，另一个是多少度？
  + 点 $(-3, 4)$ 向下平移 $6$ 个单位后，坐标是多少？它在哪个象限？

  === 诊断 4：基本立体与单位换算

  + 一个正方体的棱长是 $5$ 分米。它的体积是多少立方分米？合多少升？
  + 把 $2.4$ 米改写成厘米；再判断 $3.5$ 米 + $45$ 厘米 能不能直接写成 $3.95$ 米。
]

#blueprint[
  *可以继续前进的信号*

  + 诊断 1 全对，并且能说出自己用了“位值”“运算顺序”“分数—百分数互化”中的哪几样。
  + 诊断 2 全对，并且能分清“部分 : 整体”和“部分 : 部分”不是一回事。
  + 诊断 3 全对，并且能在横截线图上准确区分同位、内错、同旁内角；在坐标上不写反顺序。
  + 诊断 4 全对，并且单位一致，不会把 $"立方"$ 漏写成 $"平方"$。

  这四条都打勾，才能说“前面学过的知识连得起来”。
]

#blueprint[
  *常见误判*

  - *只看单题对不对，不看自己用了哪几样前面学过的知识*——这最容易自我感觉良好，却在桥梁层综合题上立刻卡住。
  - *一卡住就整章重刷*——浪费时间。正确做法是定位到哪一小节再回看。
  - *会“按步骤做”就当自己会了*——桥梁层对“能不能解释”的要求更高，光会套步骤不够。
  - *把“能做出来”和“数量级合理”混为一谈*——体积题一漏“立方”、单位不统一就算错，仍然过不了桥。
]
