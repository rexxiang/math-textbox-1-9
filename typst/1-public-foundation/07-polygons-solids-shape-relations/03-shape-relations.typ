#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, answer-cut
#import "../../lib/diagram-packages.typ": cetz

=== 图形之间的关系：里外、相邻、相交、平行 <tool:pf06-shape-relations>

#vocab[内部 interior][边界 boundary][外部 exterior][相邻 adjacent][相交 intersect][平行 parallel (直观)]

#crisis[
  你画了一张学校平面图，想告诉别人：操场在教学楼旁边、花坛在操场*里面*、两条过道*互相平行*、主干道和小路在门口*相交*。

  如果没有稳定的关系词，只能说“挨着”“靠着”“差不多”，别人照着图走就会迷路。

  *基本图形光有名字还不够，它们之间的位置关系也要能稳定描述。*
]

#tryit[
  + 画一个正方形，再在它里面、边界上、外面各画一个点，分别标 $A$、$B$、$C$。
]


#discovery[
  随便画一个封闭的多边形，它把整个平面切成了三部分：

  - 图形*内部*：围起来的那块区域
  - 图形*边界*：围它的那几条线段
  - 图形*外部*：围外面的其余部分

  所以任意一点，都可以问：“这个点在图形的里、边上，还是外？”

  #figure(
    cetz.canvas(length: 0.4cm, {
      import cetz.draw: *

      let blue = rgb("#2196F3")
      let green = rgb("#4CAF50")
      let red = rgb("#F44336")

      // A pentagon
      let pts = ((8.0, 0.0), (12.76, 3.45), (10.94, 9.05), (5.06, 9.05), (3.24, 3.45))
      line(..pts, close: true, fill: rgb("#E3F2FD40"), stroke: 1.5pt + blue)

      // Interior point
      circle((8, 5), radius: 0.25, fill: green, stroke: 1pt + green)
      content((8, 5.8), text(fill: green, weight: "bold", size: 8pt)[内部], anchor: "south")

      // Boundary point
      circle((10.38, 1.73), radius: 0.25, fill: blue, stroke: 1pt + blue)
      content((11.38, 1.73), text(fill: blue, weight: "bold", size: 8pt)[边界], anchor: "west")

      // Exterior point
      circle((14, 8), radius: 0.25, fill: red, stroke: 1pt + red)
      content((14, 8.8), text(fill: red, weight: "bold", size: 8pt)[外部], anchor: "south")
    }),
    caption: [封闭图形把平面分成三部分：内部、边界、外部],
  )



  接着看两个图形：

  - 如果它们共享一条边或一个顶点，却没有互相穿进对方的内部——说它们*相邻*。
  - 如果它们的边界有公共点，而且一方穿进了另一方——说它们*相交*。
  - 如果两条线段 / 直线所在的方向始终“并排而行”，永远不会相交——直观上说它们*平行*。

  这些关系词可以叠加：“相邻但不相交”“相交在一点”“平行并且离得很近”。
]

#side-hack[
  判断一个点“里 / 边 / 外”时，从这个点画一条射线往外数“穿过边界几次”：*穿奇数次→在里面；穿偶数次→在外面*（凸多边形一眼就能看，不用数）。
]

#tryit[
  + 再画两个三角形，让它们*相邻但不相交*。
]

#blueprint[
  - 一个封闭图形把平面分成三部分：*内部*、*边界*、*外部*。任何一点都能归到其中一类。
  - 两个图形的位置关系常用三个词描述：
    + *相邻*：共享顶点或边，但不互相穿透。
    + *相交*：边界有公共部分，图形互相穿进对方。
    + *平行*（直观）：两条线方向一致，始终保持并排、永远不相遇。
  -
  #figure(
    cetz.canvas(length: 0.45cm, {
      import cetz.draw: *

      let blue = rgb("#2196F3")
      let red = rgb("#F44336")

      // Two parallel lines
      line((0, 0), (16, 0), stroke: 1.5pt + blue)
      line((0, 4), (16, 4), stroke: 1.5pt + red)

      // Labels
      content((17, 0), text(fill: blue, size: 9pt)[$l_1$], anchor: "west")
      content((17, 4), text(fill: red, size: 9pt)[$l_2$], anchor: "west")

      // Parallel arrows
      content((8, 2), text(size: 8pt)[始终等距，永不相交], anchor: "center")

      // Equal distance marks
      line((3, 0.3), (3, 3.7), stroke: (dash: "dashed", paint: luma(150), thickness: 0.5pt))
      line((10, 0.3), (10, 3.7), stroke: (dash: "dashed", paint: luma(150), thickness: 0.5pt))
    }),
    caption: [平行的两条直线：方向一致，永不相遇],
  )

  - 这些关系在这一节只需*直观判断*，正式的“平行判定”会在第 8 章里补上。
]

#pitfall[
  *高频错误*

  ✗ 说“点 $A$ 在图形上”时分不清是“在边界上”还是“在内部”
  → ✓ 在这本书里，“在图形上”默认指“在边界上”；“在图形内部”才指围起来的那块。

  ✗ 看到两个图形紧挨着就说它们“相交”
  → ✓ 相邻 $≠$ 相交；相交需要图形互相穿进对方的内部。
  日常口语里“相交”常被当成“靠在一起”，但几何里它有更严格的含义。

  ✗ 把“两条短线段靠得很近”直接说成平行
  → ✓ 平行的关键是*方向一致，延长后永不相遇*，不是“看起来近”。
]

#mastery[
  *基础*

  + 画一个五边形，标出一个内部点、一个边界点、一个外部点。
  + 说说下列关系：教室门和教室外墙——相邻还是相交？
  + 课本页面的上下两条长边，是相邻还是平行？
  + 判断对错并改正："两个图形只要紧紧挨在一起，就是相交。"
  + 在平面上画一个圆和一条直线。请分别画出三种可能：直线在圆外、直线和圆相交、直线和圆相切（刚好碰到一点）。

  *应用*

  + 一个大正方形里画了一个小圆，小圆完全在里面。这两个图形是相邻、相交还是都不是？
  + 两条直线相交于一点，在交点处会形成几个角？
  + 用“相邻 / 相交 / 平行”描述下面的三组关系：
    - 课桌两条对边
    - 黑板和讲台的前沿
    - 教室地板的对角线与一条墙脚线

  *挑战*

  + 一个凹多边形内部的形状很“弯”。请用“射线穿过边界几次”的方法，自己找一个容易误判内外的点，说明怎样判对。
  + “两条线段永远不相交”和“两条线段平行”是不是同一回事？想一想反例。
  + *反直觉*：两条直线在画纸上"看起来差点就要碰到"，能凭目测判定它们一定会相交吗？
  + *跨知识*：在坐标平面上有 $A(0,0)$、$B(1,0)$、$C(0,1)$ 三点。它们两两组成的三条线段，每两条之间的关系是相邻、相交还是平行？请逐对说明。
  + *构造题*：构造两个图形，使它们有共同的边界又有重叠的内部区域。它还能叫"相邻"吗？给出判定。
  + *反例题*：判断"两条平行直线之间没有任何点"。给出反例（提示：平行线之间的*带状区域*里有无穷多点，只是这些点不在两条线本身上）。

  #answer-cut[
  + 图略；三点位置不同即可。（封闭图形把平面分成内部、边界、外部三部分）
  + 相邻——共享一条边界，不互相穿透。（相邻的定义）
  + 平行——上下两条长边方向一致，延长后永不相遇。（平行的直观定义）
  + 错。“紧紧挨在一起”可能只是*相邻*（共享边界但不穿透），相交需要一方穿进另一方的内部。（相邻 ≠ 相交）
  + 图略。三种情况分别对应：直线与圆无公共点（外离）、有两个公共点（相交）、有且只有一个公共点（相切）。（直线与圆的位置关系）
  + 都不是。分析：小圆完全在正方形内部 → 它们的边界没有公共点 → 既不相邻也不相交。正确说法是“包含关系”。
  + 4 个角。两条直线相交于一点，在交点处把平面分成 4 个区域，每个区域对应 1 个角。
  + 逐个判断：课桌对边（方向一致、等距）→ 平行；黑板和讲台前沿（通常方向一致）→ 平行；墙脚线与地板对角线（方向不同、会相遇）→ 相交。
  + 示例略。方法：在凹处选一点，画射线向外发射。如果穿过边界*奇数*次 → 在内部；*偶数*次 → 在外部。凹多边形的凹处容易目测出错，射线法更可靠。
  + 不一定是同一回事。反例：画两条方向不同但都很短的线段，它们可能永远不相交（因为不够长碰不到），但方向不同所以不平行。平行 = 方向一致 + 延长也不相交，缺一不可。
  + 不能。目测可能有视觉误差；更要紧的是，"看起来要碰"可能只是画图精度有限，两条直线*实际方向*相同时延长再远也不会相交（即平行）。判定关键是*方向是否一致*，不是局部画图的"接近程度"。要诚实判定，可借助方向 / 角度 / 坐标计算（目测 ≠ 判定）。
  + 三条线段：$A B$（连 $A$ 与 $B$）、$A C$（连 $A$ 与 $C$）、$B C$（连 $B$ 与 $C$）。逐对：($A B, A C$) 共享端点 $A$ → *相邻*；($A B, B C$) 共享端点 $B$ → *相邻*；($A C, B C$) 共享端点 $C$ → *相邻*。三条线段两两相邻，共围成一个直角三角形（跨坐标 + 相邻判定）。
  + 例如把两个圆的边界画在一起，使它们既共享一段弧（边界相邻）又有内部重叠区域。这就*不再叫"相邻"*——相邻的定义是"共享部分边界但*内部不重叠*"。一旦内部重叠，关系应称为*相交（穿透）*。要点：相邻和相交的区分依据是内部是否重叠（相邻 = 共边界 + 内部不重叠）。
  + 错。两条平行线之间存在一片*带状区域*，这片区域里有无数个点（每个点离两条线都有一段距离）——这些点既不在第一条线上，也不在第二条线上，但确实存在。"两线之间没有点"的说法把"不在两条线上"误读成了"不存在"——前者是位置关系，后者是存在性，两者不能混淆（点的存在 vs 在某条线上）。
  ]
]
