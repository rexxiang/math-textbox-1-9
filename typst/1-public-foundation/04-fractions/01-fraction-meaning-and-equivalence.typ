#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, secref, answer-cut
#import "../../lib/diagram-packages.typ": cetz

=== 分数的意义与等值 <tool:pf04-fraction-equivalence>

#vocab[分数 fraction][等值分数 equivalent fractions][均分 equal parts]

#crisis[
  把 $2$ 张披萨平均分给 $3$ 个人，每人分到多少？整数里找不到答案——不是 $0$，不是 $1$，卡在中间。但每个人确实分到了一份，"真的分到了多少"不能不说。

  *再看一个真实场景*：每年尼罗河泛滥之后，古埃及的土地测量员要重新丈量农田。有些田刚好 $1$ 个标准单位宽，有些刚好 $2$ 个单位宽——可大量的田卡在中间："比 $1$ 个单位多、比 $2$ 个单位少"。

  - 测量员 A 报告："这块田在 $1$ 到 $2$ 之间。" 国王听了发愁——这种说法没法分等级征税，更没法和邻田比较。
  - 测量员 B 试着说："把标准单位切成 $3$ 等份，这块田占 $2$ 份。" 这就精确多了——可隔壁村的测量员 C 却说："把单位切成 $5$ 等份，那块田占 $3$ 份。" *谁的田更宽？两人光看自己的报告，根本无法直接比较。*

  *老工具真的卡住了*：

  - 自然数（$0, 1, 2, ...$）只能描述"整个"——它根本写不出"不到一整个"这种数量；
  - "凭感觉描述"（"差不多一半"、"接近一个"）不可重复、不可比较；
  - "切成几份取几份"虽然精确，但*不同切法之间无法互相换算*——B 的"$3$ 份切法"与 C 的"$5$ 份切法"鸡同鸭讲。

  有没有一种数，能稳定描述"部分"，而且不管怎么切，同一块量都只有一个"真实的名字"，还能和别的数字直接比大小？
]

#tryit[
  + $1/2$ 和 $2/4$ 一样大吗？为什么？
]


#discovery[
  最自然的尝试是用语言形容："比一半多一点"、"差不多三分之二"——可两块田都"比一半多一点"，一块却明显比另一块大。语言形容*太粗*，不可比较。

  那就更精确一点：统一约定：把"$1$ 个标准整体"*平均切成 $b$ 份*（强调*平均*——每份必须一样大），然后说"占其中的 $a$ 份"。把这个数写作

  $ a / b. $

  - 把 $1$ 张披萨平均分成 $3$ 份，每份就是 $1\/3$；
  - 把 $2$ 张披萨平均分给 $3$ 个人，每人得到 $2\/3$ 张——可见 $2\/3$ 同时也表示"$2 div 3$"，分数也能表示除法结果。

  *麻烦还没完*：测量员 B 用"$3$ 份切法"得到 $2\/3$，测量员 C 用"$5$ 份切法"得到 $3\/5$——*它们能比较吗？甚至会不会其实是同一个量？*

  这就要把两种切法叠在同一张图里看。以"$1$ 个长方形"代表整体，做面积模型：

  - 切成 $2$ 份取 $1$ 份：得到 $1\/2$。
  - *再把这两份每份继续一分为二*——切痕一加，原来"取的那 $1$ 份"现在变成 *$2$ 份*；总份数也变成 $4$。读出新的分数：$2\/4$。
  - 涂色面积*没变*——切痕只是改变了"每份多大、共多少份"，没动总量。

  结论：$1\/2 = 2\/4$。

  类似地，把"$3$ 份切法"中的每份再一分为二，得到"$6$ 份切法"，原来的 $2\/3$ 现在变成 $4\/6$——同一块田的同一个量。这种"看起来分母分子都不同、其实代表同一个量"的写法，叫*等值分数*。

  *规则*：分子分母同时乘上同一个非零数（细切），或者同时除以同一个非零数（合并），分数值不变：

  $ a / b = (a times k) / (b times k) = (a div k) / (b div k) quad (k != 0). $

  *回到比较*：B 的 $2\/3$ 与 C 的 $3\/5$ 怎么比？把它们都化成分母为 $15$ 的等值分数：$2\/3 = 10\/15$，$3\/5 = 9\/15$。$10 > 9$，所以 B 的田更宽——*等值分数让不同切法之间可以互相换算*，从而比较。

  #figure(
    cetz.canvas(length: 0.4cm, {
      import cetz.draw: *

      // --- 面积模型：1/2 vs 2/4 ---
      // 左边：1/2
      rect((0, 0), (6, 2), stroke: 0.6pt)
      line((3, 0), (3, 2), stroke: 0.6pt)
      rect((0, 0), (3, 2), fill: rgb("#a8d8ea"), stroke: 0.6pt)
      content((3, -0.8), $1 / 2$, anchor: "north")

      // 右边：2/4
      let rx = 8
      rect((rx, 0), (rx + 6, 2), stroke: 0.6pt)
      for i in (1, 2, 3) {
        line((rx + i * 1.5, 0), (rx + i * 1.5, 2), stroke: 0.6pt)
      }
      rect((rx, 0), (rx + 1.5, 2), fill: rgb("#a8d8ea"), stroke: 0.6pt)
      rect((rx + 1.5, 0), (rx + 3, 2), fill: rgb("#a8d8ea"), stroke: 0.6pt)
      content((rx + 3, -0.8), $2 / 4$, anchor: "north")

      // 等号
      content((7, 1), $=$, anchor: "center")

      // --- 数轴模型 ---
      let ny = -4
      line((-0.5, ny), (15, ny), stroke: 0.5pt, mark: (end: ">"))
      // 刻度
      for (val, label) in ((0, $0$), (6, $1 / 2$), (12, $1$)) {
        line((val, ny - 0.3), (val, ny + 0.3), stroke: 0.5pt)
        content((val, ny - 0.8), label, anchor: "north")
      }
      // 四等分刻度
      for i in (3, 9) {
        line((i, ny - 0.2), (i, ny + 0.2), stroke: 0.4pt + rgb("#888"))
      }
      content((3, ny + 0.8), $1 / 4$, anchor: "south")
      content((9, ny + 0.8), $3 / 4$, anchor: "south")
      // 标注 2/4 = 1/2
      circle((6, ny), radius: 0.25, fill: rgb("#f5a623"), stroke: 0.5pt)
      content((6, ny + 1.2), text(size: 0.75em, $2 / 4 = 1 / 2$), anchor: "south")
    }),
    caption: [把图形等分后数"占了几份"，和数轴上看分数，都能看到：$1 / 2 = 2 / 4$],
  )
]

#side-hack[
  记住一句话：分母在说“把整体分成几等份”，分子在说“拿了其中几份”。
]

#tryit[
  + 把 $3/5$ 改写成一个分母是 10 的等值分数。
]

#history-note[
  约公元前 1650 年的莱因德纸草书（Rhind Papyrus）表明，古埃及人已经系统地使用分数——但他们几乎只用“单位分数”（分子为 1），比如把 $2\/5$ 写成 $1\/3 + 1\/15$。相比之下，《九章算术》（约公元 1 世纪）从一开始就使用一般分数，并明确提出“约分术”和“合分术”来化简与合并。分数的发明，就是为了精确表达“不到一个整体”或“几个整体加上一部分”这样的真实数量。

  ☞ 历史接力 → 《九章算术》的分数运算体系在桥梁章发展为比与比例；见 #secref("cb01-ratio-basics")。
]

#side-hack[
  *重演古埃及的单位分数分解*

  古埃及人把分子大于 $1$ 的分数写成若干单位分数（分子为 $1$）之和。试着用以下步骤分解 $3/4$：

  + 找最大的单位分数 $1/n$，使得 $1/n <= 3/4$（即 $n >= 4/3$，取最小整数）。这个 $n$ 是多少？
  + 计算 $3/4 - 1/n$，得到余数。
  + 把余数再表示成单位分数（如果余数已经是单位分数，停止；否则重复）。
  + 最终写出 $3/4 = 1/? + 1/?$。

  再试试分解 $2/5$。

  #answer-cut[
    $3/4$：$n = 2$（最大单位分数 $1/2 <= 3/4$）；余数 $3/4 - 1/2 = 1/4$，已是单位分数。所以 $3/4 = 1/2 + 1/4$。
    $2/5$：$n = 3$（$1/3 <= 2/5$？验证：$1/3 approx 0.333 < 2/5 = 0.4$，是）；余数 $2/5 - 1/3 = 6/15 - 5/15 = 1/15$。所以 $2/5 = 1/3 + 1/15$。这就是古埃及表示法的一种算法（"贪心法"）。
  ]
]

#blueprint[
  - *分数* $a/b$ 表示把整体平均分成 b 份，取其中 a 份；它也能看成 $a div b$。
  - *均分* 很关键：每一份必须一样大。
  - *等值分数*：分子分母同时乘或除同一个非零数，分数值不变。

  #figure(
    cetz.canvas(length: 0.4cm, {
      import cetz.draw: *

      let blue = rgb("#2196F3")
      let green = rgb("#4CAF50")

      // 3/4 bar
      for i in range(4) {
        let x = i * 3
        let fill-c = if i < 3 { rgb("#BBDEFB") } else { white }
        rect((x, 3), (x + 2.8, 5), fill: fill-c, stroke: 0.6pt + blue)
      }
      content((6, 2.2), $3 / 4$, anchor: "north")

      // 6/8 bar
      for i in range(8) {
        let x = i * 1.5
        let fill-c = if i < 6 { rgb("#C8E6C9") } else { white }
        rect((x, -1), (x + 1.3, 1), fill: fill-c, stroke: 0.6pt + green)
      }
      content((6, -1.8), $6 / 8$, anchor: "north")

      // Equals sign
      content((13, 2), $=$, anchor: "center")
      content((13, 0), text(size: 8pt)[同样长], anchor: "center")
    }),
    caption: [分数条模型：$3 / 4 = 6 / 8$，涂色部分一样长],
  )

]

#pitfall[
  *高频错误*

  ✗ 只看分母大，就以为分数一定更大
  → ✓ 分母变大时，每一份反而更小；要结合分子和整体一起看。
  这是把整数里“数字大就大”的直觉直接搬到分母上来了，但分母代表的是“分得多细”。

  ✗ 把“分成 3 块”误当成“分成 3 等份”
  → ✓ 分数建立在均分上，不均匀切块不能直接写成标准分数。
]

#mastery[
  *基础*

  + 用分数表示：把一条线段平均分成 5 份，取其中 2 份。
  + 写出与 $2/3$ 相等的两个分数。
  + 解释为什么 $3/4$ 也可以看成 $3 div 4$。
  + 判断对错并改正："$2/5$ 和 $4/9$ 是等值分数，因为分子分母都各加了 2。"
  + 把 $12/18$ 化简成最简分数。

  *应用*

  + 8/12 和 2/3 是同一个量吗？请说明理由。
  + 一桶水用了 $3/5$，如果把每一份再平均分成 2 份，用新的分数怎么写？

  *挑战*

  + 为什么“等值分数”不是“把数写复杂了”，而是在换单位？
  + 请用“份的大小变了，但总量不变”解释 $3/4 = 6/8$。

  + 比较 $frac(3, 7)$ 和 $frac(4, 9)$ 的大小，不把它们通分，能用"分子÷分母"的近似来判断吗？再用通分精确验证。
  + $frac(n, n+1)$ 和 $frac(n+1, n+2)$（$n$ 为正整数）哪个更大？用交叉相乘法判断，结论反直觉吗？
  + *[构造题]*：写出三个互不相等的分数，使它们都和 $frac(6, 15)$ 等值，且分母不超过 $30$。
  + 若把一个最简分数的分子和分母都加上同一个正整数 $k$，新的分数和原来相等吗？举例说明，并说出什么情况下会变大、什么情况下会变小。

#answer-cut[
  + $2/5$（均分定义：5 等份取 2 份）。
  + 例如 $4/6$、$6/9$（等值分数性质：分子分母同乘同一个非零数）。
  + 因为 $3/4$ 表示把 1 平均分成 4 份取 3 份，这和 $3 div 4$ 的运算结果一致（分数 = 除法）。
  + 错；等值分数要求分子分母同时乘或除*同一个数*，而不是各自加同一个数。$2/5 = 4/10 eq.not 4/9$（等值分数性质）。
  + $12/18 = 2/3$（分子分母同除以最大公因数 6；$gcd(12, 18) = 6$）。
  + 是；$8/12$ 的分子分母同除以 4 得 $2/3$，两者占整体的比例完全相同（等值分数性质）。
  + 原来每份是 $1/5$，每份再平均分成 2 份后变成 $1/10$，用了 3 份变成 6 份，即 $3/5 = 6/10$（等值分数性质：分子分母同乘 2）。
  + 因为改写前后量没变，只是“每份的大小”和“份数”在同步变化。这像换了一把更细（或更粗）的尺子来度量同一个量（换单位视角）。
  + 1 个四分之一等于 2 个八分之一，所以 3 个四分之一 = 6 个八分之一，占整体的比例不变。份变小了但份数等比例增加（等值分数性质）。
  + $3 div 7 approx 0.429$，$4 div 9 approx 0.444$，由此猜测 $frac(3, 7) < frac(4, 9)$。通分验证：$frac(3, 7) = frac(27, 63)$，$frac(4, 9) = frac(28, 63)$，$27 < 28$ 故 $frac(3, 7) < frac(4, 9)$ ✓（通分 + 近似双重验证）。
  + 交叉相乘：$n(n+2) = n^2 + 2n$ vs $(n+1)^2 = n^2 + 2n + 1$。因为 $n^2 + 2n < n^2 + 2n + 1$，所以 $frac(n, n+1) < frac(n+1, n+2)$。反直觉点：分子分母越来越"平"，分数越来越接近 $1$，所以更大（交叉相乘判大小）。
  + *[构造题]*：$frac(6, 15) = frac(2, 5)$（化简后）。等值分数：$frac(4, 10)$、$frac(6, 15)$（原样）、$frac(8, 20)$、$frac(10, 25)$（分母 $<= 30$）。任取三个即可（等值分数：分子分母同乘非零整数）。
  + 不一定相等。设原分数 $frac(a, b)$，新分数 $frac(a+k, b+k)$。比较：交叉 $(a+k) b$ vs $a(b+k)$，即 $a b + k b$ vs $a b + a k$，差为 $k(b - a)$。若 $b > a$（分数 $< 1$），新分数更大；若 $b < a$（分数 $> 1$），新分数更小；若 $b = a$，分数本为 $1$，新分数仍为 $1$（等值分数性质 + 大小比较）。
]
]
