#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, vocab, tryit, pitfall
#import "../../lib/diagram-packages.typ": cetz

=== 计数与数量感 <tool:pf01-place-counting>

#vocab[计数 counting][数量 quantity][零 zero]

#crisis[
  桌上放着几支铅笔。你指着它们说：“1，2，3，4，5。”

  可如果你一边数，一边漏指了一支，或者把同一支数了两次，最后报出的“5”还可信吗？

  *数数到底是在背数字名字，还是在追踪真实的数量？*
]

#history-note[
  很多早期文明在没有统一数字记号前，会用石子、刻痕、结绳来记录数量。工具不同，但背后的动作一样：让“一个记号”对应“一个对象”。
]

#discovery[
  假如要统计篮子里的苹果，最可靠的动作不是大声背数，而是做一一对应：

  - 每碰到一个苹果，就说下一个数名
  - 每个苹果只数一次
  - 全部数完时，最后说出的那个数，就表示总共有多少个

  这样，数字不再只是声音，而成了“数量记录”。

  #figure(
    cetz.canvas(length: 0.45cm, {
      import cetz.draw: *

      let green = rgb("#4CAF50")

      for i in range(5) {
        let x = i * 3.5
        circle((x, 2), radius: 0.9, fill: rgb("#C8E6C9"), stroke: 1.2pt + green)
        line((x, 2.9), (x + 0.2, 3.6), stroke: 1pt + rgb("#795548"))
        line((x, 0.8), (x, 0.2), stroke: 0.8pt + luma(120), mark: (end: ">"))
        content((x, -0.5), text(weight: "bold", size: 10pt, str(i + 1)), anchor: "north")
      }

      content((7, -2.2), text(size: 8pt, fill: luma(100))[一个苹果 ↔ 一个数名], anchor: "north")
    }),
    caption: [一一对应：每个苹果恰好对应一个数名],
  )

  再想一个特别的情况：篮子是空的。我们仍然需要一个数来记录“一个也没有”，这个数就是 0。
]

#tryit[
  先自己想一想：

  + 有 6 颗糖，如果数的时候把第 3 颗数了两次，最后报出的结果会怎样？
  + 如果一个盒子里什么也没有，应该写几？
]

#blueprint[
  - *计数* 是把数名和对象做一一对应，目的是追踪“有多少个”。
  - *数量* 指对象的多少；计数完成时，最后一个数名表示总数量。
  - *0* 表示“一个也没有”，它让“没有”也能被准确记录。

  #figure(
    cetz.canvas(length: 0.45cm, {
      import cetz.draw: *

      let blue = rgb("#2196F3")

      line((-1, 0), (17, 0), stroke: 1.2pt + luma(80), mark: (end: ">"))

      for i in range(6) {
        let x = i * 3
        line((x, -0.5), (x, 0.5), stroke: 1pt + luma(60))
        content((x, -1.4), text(weight: "bold", size: 10pt, str(i)), anchor: "north")
      }

      circle((0, 0), radius: 0.35, fill: rgb("#FFF9C4"), stroke: 1pt + rgb("#FBC02D"))
      content((0, 1.5), text(size: 7pt, fill: rgb("#F57F17"))[空篮子 = 0], anchor: "south")
    }),
    caption: [数轴上的 0 到 5：0 表示“一个也没有”],
  )
]

#pitfall[
  *高频错误*

  ✗ 把数数当成背顺口溜，只顾着念“1、2、3、4……”
  → ✓ 数数时必须让“一个数名对应一个对象”，否则最后结果不可靠。

  ✗ 觉得 0 不算数，因为“没有东西”
  → ✓ 正因为“没有”也需要被记录，所以 0 很重要；它是数量记录的一部分。
]

#mastery[
  *基础*

  + 说出数清 8 个物体时，哪一步最关键。
  + 一盒粉笔空了，应该记作几？
  + 在“3 支铅笔”“第 3 个位置”“数到 3”为止”这三句话里，哪一句是在说数量？
  + 判断对错并改正：“数到 10，说明这堆东西一定有 10 个。”
  + 两堆积木，一堆排成一排，一堆堆成塔形。不数的话，你有什么办法比较哪堆多？

  *应用*

  + 小丽数花朵时说出了 “1，2，3，4，5，6”，但她后来发现自己漏掉了一朵。原来的结果为什么不能直接加 1 就算完事？
  + 仓库管理员说“今天没有新到货”。如果账本上不写 0，会带来什么麻烦？

  *挑战 ☞ 选做*

  + 为什么数完一排椅子后，“最后一个数名就是总数”这件事并不显然？请用“一一对应”的想法解释。
  + 如果把 5 个苹果排成不同形状，数量会变吗？这说明数量和什么无关？

  #line(length: 100%, stroke: 0.3pt + luma(200))
  _参考答案：_
  + 关键是每个对象都只对应一个数名，不重不漏（一一对应原则）。
  + 记作 0（0 表示“一个也没有”）。
  + “3 支铅笔”是在说数量（数量含义）。
  + 不一定对；只有在数的过程中保持一一对应（每个物体恰好对应一个数名），结果才可靠（一一对应原则）。
  + 可以把两堆各排成一行，逐个配对，看哪堆有剩余就是哪堆多（一一对应原则）。
  + 她不知道漏掉的花在哪个位置，后续对应可能全部错位。正确做法：重新从头按一一对应数一遍，而不是在旧结果上修补。
  + 账本会分不清“忘了记”还是“确实没有”。写上 0，就把“没有新到货”这件事明确记录下来，以后查账时不会产生歧义。
  + 因为只有在每把椅子都恰好对应一个数名时，最后一个数名才表示所有椅子都被覆盖后的总量。如果中途有跳过或重复，最后的数名就不再是总数。
  + 数量不变；这说明数量和摆放形状无关，只和对象个数有关（数量守恒）。
]
