#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, answer-cut
#import "../../lib/diagram-packages.typ": cetz

=== 数字符号与位值 <tool:pf01-digits-place-value>

#vocab[数字 digit][数 number][位值 place value]

#crisis[
  柜台前来了三笔单子：第一张 $2$ 元，第二张 $20$ 元，第三张 $200$ 元。出纳员手里只有一条规矩——*"看到什么数字就抄什么数字"*——三张单子全被抄成了"$2$"。月底对账，付款收款混作一团。

  眼看不行了，他改口："那就把数字一个个都抄上。" 写下 $305$ 和 $35$：两数都含 $3$ 和 $5$。光按"含哪些数字"判断，只能说"差不多"——可它们其实差了 $270$。

  写得更长也没救：$2002$ 里有两个 $2$，$2020$ 里也有两个 $2$，按"看见数字"怎么数都一样，事实上一个比另一个少 $18$。

  数字的*形状*只有十种（$0$ 到 $9$），可需要表达的数量却有无穷多。光记"长什么样"不够——必须再记一件事：*这个数字站在哪一格里*。
]

#tryit[
  + 507 里的 5、0、7 分别表示什么？
]

#discovery[
  我们手里只有 0 到 9 这十个数字符号，自然会想："数字就是数字，2 在哪里都代表 2。"用这条想法比较 2、20、200——三个数里都有数字 2，按"数字决定大小"来判断，三者应该差不多大。

  试着推下去——

  出纳员用这条规则记账：收到 20 元，认出数字 2，记成 2 元；付出 200 元，还是认出 2，也记成 2 元——三笔账全写成"2 元"，付款和收款完全混作一团。再比较 305 和 35：两者都含数字 3 和 5，加上 305 多一个"什么都没有"的 0，按这条规则两数应该相等，但明明相差 270。

  只认数字的形状，根本无法区分同一个数字站在不同格子里代表的大小。换几组数再试，"数字相同就大小相同"这条规则总是自相矛盾。

  想象把铅笔按捆来放：

  - 散着的一支一支放，叫“几个一”
  - 10 支捆成一捆，叫“几个十”
  - 10 捆再合成一大组，叫“几个百”

  于是 243 不是把 2、4、3 并排写在一起而已，它表示：

  - 2 个百
  - 4 个十
  - 3 个一

  同一个数字 2，写在百位表示 200；写在十位表示 20；写在个位才表示 2。

  #figure(
    cetz.canvas(length: 0.35cm, {
      import cetz.draw: *

      // --- 散铅笔 (个位) ---
      let pencil(x, y) = {
        line((x, y), (x, y + 2.2), stroke: 0.8pt + rgb("#4a90d9"))
        circle((x, y + 2.4), radius: 0.15, fill: rgb("#f5a623"), stroke: 0.5pt)
      }
      for i in range(3) { pencil(i * 0.8, 0) }
      content((1.2, -1), [3 个一], anchor: "north")

      // --- 一捆 (十位) ---
      let bundle-x = 6
      rect((bundle-x - 0.3, -0.2), (bundle-x + 3.5, 2.8), stroke: (dash: "dashed", paint: rgb("#7b7b7b"), thickness: 0.5pt), radius: 3pt)
      for i in range(4) {
        for j in range(0, 10) {
          line((bundle-x + i * 0.8, 0), (bundle-x + i * 0.8, 2.2), stroke: 0.8pt + rgb("#4a90d9"))
          circle((bundle-x + i * 0.8, 2.4), radius: 0.15, fill: rgb("#f5a623"), stroke: 0.5pt)
        }
      }
      // 只画 4 支代表 "4 个十"
      content((bundle-x + 1.6, -1), [4 捆 = 4 个十], anchor: "north")

      // --- 大组 (百位) ---
      let big-x = 14
      rect((big-x - 0.3, -0.5), (big-x + 5.9, 3.2), stroke: (paint: rgb("#7b7b7b"), thickness: 0.8pt), radius: 3pt)
      rect((big-x - 0.1, -0.2), (big-x + 3.5, 2.8), stroke: (dash: "dashed", paint: rgb("#bbb"), thickness: 0.5pt), radius: 3pt)
      rect((big-x + 2.3, -0.2), (big-x + 5.7, 2.8), stroke: (dash: "dashed", paint: rgb("#bbb"), thickness: 0.5pt), radius: 3pt)
      content((big-x + 2.8, 1.2), [10 捆], anchor: "center")
      content((big-x + 2.8, -1), [$times 2$ 大组 = 2 个百], anchor: "north")

      // --- 等号与总数 ---
      content((22, 1.2), $=$, anchor: "center")
      content((24, 1.2), [*243*], anchor: "center")
    }),
    caption: [243 = 2 个百 + 4 个十 + 3 个一],
  )
]

#side-hack[
  比较两个整数时，先看最高位。位数更多的数通常更大；位数相同时，再从左往右逐位比较。
]

#tryit[
  + 为什么 45 和 405 不能只看“都有 4 和 5”就说差不多？
]

#history-note[
  公元 628 年，印度数学家婆罗摩笈多（Brahmagupta）在《婆罗摩历算书》中首次系统地定义了零的运算规则，使"0–9 十个数字 + 位置"这套体系真正完整。此前，中国在春秋战国时期就已用算筹（counting rods）实现了十进位值记数——空位留空来表示零。这套方法后经阿拉伯学者花拉子密（al-Khwārizmī，约 825 年）传入欧洲，成为今天全世界通用的"印度-阿拉伯数字"。

  ☞ *回过头想一想*：正是因为长期没有"$0$"这个占位符号，古代记录大数极其困难——空位、留白、不同符号代表不同位，规则一乱就读错。$0$ 一旦被正式接纳为"占位的数字"，加减乘除才能像今天这样*机械化*——直接对位、列竖式、不必每次反复推敲哪个符号在哪一档。下文 blueprint 会把这件"$0$ 的占位力量"显式写进规则里。
]

#history-note[
  *重演位值的力量：罗马数字 vs 印度-阿拉伯数字*

  用罗马数字规则（I=1, V=5, X=10, L=50, C=100）计算：

  + 写出 $42$（四十二）的罗马数字表示。
  + 写出 $X L V I I$ 对应的阿拉伯数字。
  + 不借助纸笔，估算：用罗马数字直接做 $37 times 12$，会遇到什么困难？用阿拉伯数字为什么更容易？

  记下你的思考：位值系统的核心优势在哪里？

  #answer-cut[
    + $42 = 40 + 2 = "XL" + "II" = "XLII"$（$40 = 50 - 10 = "XL"$，前小后大时相减）。
    + $"XLVII" = 50 - 10 + 5 + 1 + 1 = 47$。
    + 罗马数字没有位值，无法"对齐"乘法的列；没有 $0$ 占位，写大数极其繁琐。位值系统的核心优势：每个数位有固定权重（$10^n$），可以直接竖式计算，且 $0$ 充当占位符（确保 $305$ 不被读成 $35$）。
  ]
]

#blueprint[
  - *数字* 是 0 到 9 这十个符号。
  - *数* 是用这些数字按一定位置写出来的数量表达。
  - *位值* 指数字因为所在位置不同而代表不同大小：个位、十位、百位、千位……
  - 0 除了表示“没有”，还常常负责*占位*，帮助我们看清别的数字在哪一位上——*正是这条"占位力量"让大数可读、让加减乘除可以机械化操作*。

  #figure(
    cetz.canvas(length: 0.4cm, {
      import cetz.draw: *

      let blue = rgb("#2196F3")
      let header-fill = rgb("#E3F2FD")

      // Place value chart header
      for (i, lbl) in ((0, [百位]), (1, [十位]), (2, [个位])) {
        let x = i * 5
        rect((x, 4), (x + 4, 6), fill: header-fill, stroke: 0.8pt + blue)
        content((x + 2, 5), text(weight: "bold", size: 9pt, fill: blue, lbl))
      }

      // Value cells for 507
      for (i, val) in ((0, [5]), (1, [0]), (2, [7])) {
        let x = i * 5
        rect((x, 1.5), (x + 4, 4), stroke: 0.6pt + luma(160))
        content((x + 2, 2.75), text(weight: "bold", size: 14pt, val))
      }

      // Meaning below
      for (i, meaning) in ((0, [\= 500]), (1, [\= 0]), (2, [\= 7])) {
        let x = i * 5
        content((x + 2, 0.5), text(size: 8pt, fill: luma(100), meaning))
      }
    }),
    caption: [位值表：507 中每个数字的位置决定它的大小],
  )
]

#pitfall[
  *高频错误*

  ✗ 说“百位上的 3 的值是 3”
  → ✓ 百位上的 3 表示 3 个百，也就是 300。
  这是把“数字本身”和“数字所代表的值”混在一起了——位置才决定值的大小。

  ✗ 觉得 7003 中间的 0 没用，可以删成 73
  → ✓ 删掉 0 就破坏了位置：7003 是 7 个千和 3 个一，73 是 7 个十和 3 个一，完全不是同一个数。
]

#mastery[
  *基础*

  + 写出 604 中每个数字所在的数位和它表示的值。
  + 把“3 个百、5 个十、8 个一”写成一个数。
  + 比较大小：560 和 506。
  + 判断对错并改正："百位上的 3 表示 3。"
  + 一个三位数，百位上是 7，个位上是 2，十位上是 0。这个数是多少？0 在这里起什么作用？

  *应用*

  + 某商店库存写成 402 箱。为什么这里的 0 不能省略？
  + 一个数的千位是 8，十位是 4，其余各位是 0。这个数是多少？它读起来为什么要带“零”？

  *挑战*

  + 用数字 4、0、9 组成最大的三位数和最小的三位数，并解释为什么最小的不能把 0 放在最高位。
  + 为什么“数位”其实是在记录“有几个某单位”？请用 3206 举例说明。
  + *反直觉*：在 8086 这个数中，两个 8 各表示多少？相同的数字为什么能表示完全不同的“量”？
  + *跨知识*：用位值原理解释，为什么 706 - 70 不等于 6（要求把每一位上“到底减掉了几个什么”讲清楚）。
  + *构造题*：写出至少 2 个四位数，使每个数的“千位 + 百位 = 个位”。这样的四位数唯一吗？
  + *反例题*：判断对错并改正——“数字相同的两个数大小一定相等。”请给出反例。

  #answer-cut[
  + 6 在百位上，表示 600；0 在十位上，表示这一位没有；4 在个位上，表示 4（位值原理）。
  + 358（位值展开：$3 times 100 + 5 times 10 + 8 times 1$）。
  + $560 > 506$，因为百位同为 5，十位上 6 大于 0（逐位比较法）。
  + 错；百位上的 3 表示 3 个百，也就是 300（位值原理）。
  + 702；0 在十位上起占位作用，确保 7 在百位、2 在个位（0 的占位功能）。
  + 因为 0 在提醒我们“十位上没有箱子”，这样 4 仍在百位上、2 仍在个位上（0 的占位功能）。
  + 8040；读作“八千零四十”。因为百位和个位都没有，要用“零”把缺少的位交代清楚（位值原理）。
  + 最大是 940，最小是 409；0 不能放最高位，否则就不再是三位数（最高位不能为 0）。
  + 因为 3206 表示 $3 times 1000 + 2 times 100 + 0 times 10 + 6 times 1$，本质上是把数量按不同单位来记录（位值展开）。
  + 千位上的 8 表示 $8 times 1000 = 8000$；十位上的 8 表示 $8 times 10 = 80$。同一个数字 8 因为所站的位值单位不同，表示的“量”相差 100 倍——位置（位值）才是决定意义的关键，数字本身只是“几个”而已（位值原理）。
  + 706 表示“7 个百 + 0 个十 + 6 个一”；70 表示“0 个百 + 7 个十 + 0 个一”。相减时百位上的 7 没有被动，十位上 0 减去 7 要从更高位借 1 个百（化成 10 个十）才能减——所以结果不是简单地“6”。逐位算：$706 - 70 = 636$。要点：减法是“按位值单位逐档相减”，不是“凭数字外形相减”（位值 + 减法借位）。
  + 例子：1304（$1 + 3 = 4$）、2406（$2 + 4 = 6$）、3508（$3 + 5 = 8$）。不唯一；只要千位 $a >= 1$、百位 $b >= 0$、$a + b in [1, 9]$，再让个位 $= a + b$，十位任意填 $0$ 到 $9$，就有许多答案。
  + 错。例如 12 与 21，数字相同但位置不同，大小也不同（$12 != 21$）。位置（位值）决定数值大小（位值原理）。
  ]
]
