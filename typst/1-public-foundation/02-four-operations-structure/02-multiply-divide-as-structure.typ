#import "../../lib/theme-v2.typ": crisis, discovery, blueprint, mastery, history-note, side-hack, vocab, tryit, pitfall, answer-cut, secref
#import "../../lib/diagram-packages.typ": cetz

=== 乘法与除法：相同结构的打包与拆分 <tool:pf02-mul-div-structure>

#vocab[乘法 multiplication][除法 division][平均分 equal sharing]

#crisis[
  你能不用加法，一口报出“6 把椅子摆 5 排，一共多少把”吗？

  反过来，30 把椅子平均分成 5 排，每排多少把——你又能不能不一把一把地减？

  *当“同样的一组”不断重复时，我们是不是该有一种比反复加减更省力的写法？*
]

#tryit[
  + 203 名学生分组，每组 7 人，至少要分多少组才能把所有人都安排进去？先写下你的第一直觉答案，再算一次。
]


#discovery[
  我们只有加法——"6 把椅子摆 5 排"写成 $6 + 6 + 6 + 6 + 6 = 30$，没问题。现在换成"6 把椅子摆 25 排"：把 6 写 25 次相加……

  试着推下去——

  $6 + 6 + 6 + 6 + 6 + 6 + 6 + 6 + 6 + 6 + 6 + 6 + 6 + 6 + 6 + 6 + 6 + 6 + 6 + 6 + 6 + 6 + 6 + 6 + 6$——抄漏一个就算错，而且改成"1000 排"只是又变长一千倍，看不出"每排 6 把"这个结构；更麻烦的是，从这串加法里根本看不出"一共 6 把乘了多少次"，"排数"和"每排数量"混在一起，完全无法分辨。

  加法把每一个 6 都单独列出来，无法把"同样大小的小组反复出现"这个结构直接打包；组数一多，写到第几组都数不清楚。

  $6 + 6 + 6 + 6 + 6$ 表示 5 个 6。


  #figure(
    cetz.canvas(length: 0.45cm, {
      import cetz.draw: *

      let blue = rgb("#2196F3")
      let light = rgb("#E3F2FD")

      // 5 rows × 6 columns grid (area model)
      for row in range(5) {
        for col in range(6) {
          let x = col * 1.8
          let y = row * 1.8
          rect((x, y), (x + 1.6, y + 1.6), fill: light, stroke: 0.5pt + blue)
        }
      }

      // Dimension labels
      content((5.4, -0.8), text(weight: "bold", size: 9pt)[6 把], anchor: "north")
      content((-1.2, 4.5), text(weight: "bold", size: 9pt)[5 排], anchor: "east")

      // Result
      content((5.4, 10), text(weight: "bold", size: 10pt)[$6 times 5 = 30$], anchor: "south")
    }),
    caption: [面积模型：5 排 × 6 把 = 30 把椅子],
  )

  当同样的小组反复出现时，我们把它写成 $6 times 5 = 30$。这不是换了一道题，而是把重复结构打包了。

  反过来，若把 30 平均分成 5 组，每组是 6，就写成 $30 div 5 = 6$。

  所以除法常常是在追问：

  - 平均分时，每组多少？
  - 或者固定每组多少时，一共能分成几组？
]

#side-hack[
  看到“每组一样多”“每盒装同样多”“平均分”这些词，就该想到乘除法，而不是继续一项一项加。
]

#tryit[
  + 32 颗糖平均分给 8 个小朋友，每人多少颗？
]

#history-note[
  目前已知最早的完整十进制乘法表，是 2002 年在湖南里耶出土的秦代竹简（约公元前 200 年），用“九九八十一”一路列到“二半而一”。更早的巴比伦泥板（约公元前 2000 年）则保存了大量六十进制乘法表。不同文明都发现了同一件事：把“同样大小的小组反复出现”的结构预先压缩成一张表，计算就能快得多。
]

#blueprint[
  - *乘法* 用来表示“若干个相同小组”的总量。
  - *除法* 用来把总量按相同结构拆开，可以是“平均每组多少”，也可以是“能分成几组”。
  -
  #figure(
    cetz.canvas(length: 0.45cm, {
      import cetz.draw: *

      let green = rgb("#4CAF50")
      let orange = rgb("#FF9800")

      // Total bar
      rect((0, 3), (15, 5), fill: rgb("#FFF3E0"), stroke: 1pt + orange)
      content((7.5, 4), text(weight: "bold", size: 10pt)[30])

      // Split into 5 equal groups
      for i in range(5) {
        let x = i * 3
        rect((x, 0), (x + 2.8, 2), fill: rgb("#C8E6C9"), stroke: 0.8pt + green)
        content((x + 1.4, 1), text(weight: "bold", size: 9pt)[6])
      }

      // Arrow from total to groups
      line((7.5, 2.8), (7.5, 2.2), stroke: 1pt + luma(120), mark: (end: ">"))
      content((10, 2.5), text(size: 8pt, fill: luma(100))[$div 5$], anchor: "west")
    }),
    caption: [除法把 30 平均分成 5 组，每组 6],
  )

  - 乘法与除法是互逆运算：若 $a times b = c$，则 $c div a = b$，也有 $c div b = a$。
]


#pitfall[
  *高频错误：除法只有一种样子吗？*

  小华看到两道题：
  - A：$30$ 颗糖平均分给 $5$ 人，每人几颗？
  - B：$30$ 颗糖，每 $5$ 颗装一袋，可以装几袋？

  他的草稿是这样写的：

  #block(inset: (left: 12pt))[
    第 1 行：A 是平均分，所以 $30 div 5 = 6$。$quad$ ✓

    第 2 行：B 不是“分给 $5$ 人”，所以不能用除法。$quad$ ✗
  ]

  *错处*：第 2 行。除法不只有“平均分成几份”这一种身份；它还可以表示“总量里有几个同样大小的组”。B 问的是“$30$ 里面有几个 $5$”，仍然是 $30 div 5$。

  *正确改法*：
  - ✓ A：$30 div 5 = 6$，问的是“每份多少”。
  - ✓ B：$30 div 5 = 6$，问的是“有几份”。

  *自查句*：读到除法题，先问：“我知道的是份数，还是每份大小？”前者求每份多少，后者求有几份。

  另外，乘法也要先确认“每组相同”：

  ✗ 直接把 $8, 7, 6$ 三组写成 $3 times 8$。

  → ✓ 只有每组大小相同，才能用一个乘法式打包；组大小不同就要逐项相加。
]


#mastery[
  *基础*

  + 写出“7 盒，每盒 9 个”对应的乘法式。
  + 写出“63 个球平均装进 7 盒”对应的除法式。
  + 说明两式为什么互相关联。
  + 判断对错并改正："$20 div 4 = 5$ 表示把 20 平均分成 5 份。"
  + 一个菜地有 4 行番茄，每行 8 株。不用逐一去数，一共有多少株？

  *应用*

  + 食堂有 12 张桌子，每张坐 6 人，最多能坐多少人？
  + 72 名学生排队做操，每排 9 人，可以排几排？

  *挑战*

  + “20 里面有几个 4”和“20 平均分成 4 份，每份多少”都写成除法，但它们问的问题为什么不完全一样？
  + 为什么说乘法比重复加法更像"看见了结构"？请用 25 个 4 的例子解释。
  + *[反直觉]* 判断："被除数变大、除数不变，商一定变大；被除数不变、除数变大，商也变大。" 第二句对吗？给一个例子，并解释为什么"除数越大、商反而越小"看起来反直觉。
  + *[跨知识点]* 用 #secref("tool:pf02-add-subtract-undo") 的互逆思想解释：为什么验算 $84 div 7$ 可以用"$7 times "商" = 84$"来确认？这跟用加法验减法是不是同一种做法？
  + *[构造题]* 找一个三位数 $N$，使 $N$ 既能被 $4$ 整除又能被 $6$ 整除，但不能被 $24$ 整除。给出 $N$ 并验证三个除法。
  + *[找 Bug]* 小华算 $48 div 6 div 2$，写成"$48 div (6 div 2) = 48 div 3 = 16$"。她错在哪？正确答案是多少？为什么除法不像乘法那样可以随意结合？

  #answer-cut[
  + $7 times 9 = 63$（乘法：相同小组打包）。
  + $63 div 7 = 9$（除法：按同一结构拆开）。
  + 因为它们说的是同一批 63 个球，只是一个从“组和每组”求总量，一个从“总量和组数”求每组（乘除互逆）。
  + 不够准确；$20 div 4 = 5$ 表示把 20 平均分成 4 份，每份是 5，或者说 20 里面有 5 个 4（除法的两种理解）。
  + $4 times 8 = 32$ 株（乘法：4 组 × 每组 8）。
  + $12 times 6 = 72$ 人（乘法打包）。
  + $72 div 9 = 8$ 排（除法拆分）。
  + 前者在问 20 里面能分出几个 4（包含除），后者在问把 20 均分成 4 份后每份多大（等分除）；一个问组数，一个问每组数量（除法的两种含义）。
  + 因为 $4 + 4 + dots.h$ 写 25 次很长，而 $25 times 4 = 100$ 直接看出"25 个同样的 4 组"，一步到位。这就是从重复加法中提取出"重复结构"的眼光（乘法的结构视角）。
  + 第二句不对：除数越大表示分成的份数越多，每份就越小，所以商减小。例如 $24 div 4 = 6$，$24 div 6 = 4$，商从 $6$ 降到 $4$。看起来反直觉是因为"参与的数大、结果就大"的惯性，但除数恰恰是"分母"——分得越多每份越少（除法语义）。
  + 同一种做法。用乘法去验除法 = 把除法倒过来跑回原数，跟用加法验减法、减法验加法一样属于互逆运算（呼应 #secref("tool:pf02-add-subtract-undo")）。$84 div 7$：商 $12$，验 $7 times 12 = 84$ ✓，所以正确（乘除互逆）。
  + 取 $N = 108$：$108 div 4 = 27$ ✓，$108 div 6 = 18$ ✓，$108 div 24 = 4$ 余 $12$ ✗。能被 $4$ 和 $6$ 整除只保证能被最小公倍数 $12$ 整除，未必到 $24$（最小公倍数 ≠ 两数相乘）。
  + 错在自作主张加了括号改变了顺序：除法不满足结合律，从左到右才对——$48 div 6 div 2 = 8 div 2 = 4$。$48 div (6 div 2) = 16$ 是另一道题，不等价。原因：$48 div 6 div 2 = 48 times 1/6 times 1/2 = 48/12 = 4$；而 $48 div (6 div 2) = 48 times 2/6 = 16$——括号位置改变了哪些数被"取倒数"，所以结果完全不同（除法没有结合律）。
  ]
]
